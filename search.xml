<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java对象创建过程</title>
    <url>//article/jvm_create_object/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言： Java创建一个对象的流程</p>
</blockquote>
<h1 id="📝-创建对象流程"><a href="#📝-创建对象流程" class="headerlink" title="📝 创建对象流程"></a>📝 创建对象流程</h1><p><img src="/images/posts/jvm_create_object/img-1.png" alt="Java对象创建过程"></p>
<h1 id="检查加载"><a href="#检查加载" class="headerlink" title="检查加载"></a>检查加载</h1><p>类加载器会维护一个加载的类的集合，通过检查该集合中是否已经存在类的定义，来确定是否需要重新加载类的字节码。</p>
<p>如果类还没有被加载过，类加载器会尝试从指定的位置加载类的字节码，并进行必要的解析和链接操作。如果类已经被加载过，类加载器会直接返回该类的引用，无需再次加载。</p>
<p><img src="/images/posts/jvm_create_object/img-2.png" alt="Java对象创建过程"></p>
<h1 id="分配内存"><a href="#分配内存" class="headerlink" title="分配内存"></a>分配内存</h1><p>当类被加载完之后，虚拟机就会给对象分配内存，此时对象的大小已经确定，虚拟机会从的内存域中划分出该对象大小空间的区域供存放该对象</p>
<h2 id="指针碰撞"><a href="#指针碰撞" class="headerlink" title="指针碰撞"></a>指针碰撞</h2><p>把堆上的一部分内存分成两部分，一部分依次放满了对象，此时对象在这部分内存上是整齐排放着，另一部分是空白内存区域，等待放对象。<br>两块区域的间隔点是用一个指针进行标记，如果在这块内存中又创建了对象，则指针会向后移动这个对象的大小的地址，供这个对象存放</p>
<p>指针碰撞的优点是分配速度快，只需要移动指针即可完成分配，无需进行复杂的内存搜索或碎片整理。然而，这种方式只适用于具有连续内存布局的情况，例如使用标记整理或复制算法的垃圾回收器。</p>
<p><img src="/images/posts/jvm_create_object/img-3.png" alt="Java对象创建过程"></p>
<h2 id="空闲列表"><a href="#空闲列表" class="headerlink" title="空闲列表"></a>空闲列表</h2><p>当内存上存放的对象<strong>不是整齐规整</strong>存放的话，此时内存空间就会碎片化，就不能使用指针碰撞的方式去分配内存，空闲列表的方式就是维护一个可以存放对象的内存地址的<strong>集合列表</strong>，即记录哪些内存区域是可以存放对象的，当创建对象时，在列表中找一个空间合适的一块区域去存放，然后再更新列表</p>
<p><img src="/images/posts/jvm_create_object/img-4.png" alt="Java对象创建过程"></p>
<h2 id="分配内存并发问题"><a href="#分配内存并发问题" class="headerlink" title="分配内存并发问题"></a>分配内存并发问题</h2><p>  <strong>在以上分配内存的过程中，由于堆空间是线程共享的, 如果在给对象a分配内存的同时指针还没来得及修改，对象b同时也使用了原来的这个指针来分配内存。</strong></p>
<p>**就会出现一块内存分配给了两个对象，**<strong>即并发分配问题</strong></p>
<h3 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h3><p>  通过cas保证只有一个线程能分配到正确的，对象在进行分配内存之前会将该线程期望的指针值与内存上的原有的指针值进行比较，如果相同，则先修改内存上的指针值，即向后移动该对象大小的空间，然后存放对象。如果不相同，则返回现在的指针值，然后重试以上操作</p>
<h3 id="分配优化"><a href="#分配优化" class="headerlink" title="分配优化"></a>分配优化</h3><p><strong>获取操作指针这个操作，可能是多线程并发的，为了保证成功会采用CAS的方式去执行，但是CAS多次失败会有性能瓶颈，会先使用TLAB的分配方式去分配内存</strong></p>
<ol>
<li>TLAB：在内存中分配一块很小的空间作为线程私有的，直接就可以分配了，</li>
<li>条件: 需要对象小于eden区的1%（默认）， 可通过配置TLABsize配置大小</li>
</ol>
<p>使用<strong>TLAB</strong>可以避免一系列的非线程安全问题，同时还能够提升内存分配的吞吐量，因此我们可以讲这种内存分配方式成为<strong>快速分配策略</strong></p>
<p><img src="/images/posts/jvm_create_object/img-5.png" alt="Java对象创建过程"></p>
<h1 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h1><p>给对象赋默认值。如果不赋默认值，这个对象的该字段如果不set值的话，就无法get该字段的内容，程序会出错。如果赋默认值之后，就可以返回默认值</p>
<h1 id="设置对象头"><a href="#设置对象头" class="headerlink" title="设置对象头"></a>设置对象头</h1><p><img src="/images/posts/jvm_create_object/img-6.png" alt="Java对象创建过程"></p>
<p>对象组成：</p>
<ol>
<li>对象头（8） + 指针（4）+ 数组（4） 字节</li>
<li>实例数据</li>
<li>对齐填充</li>
</ol>
<p>对象头： 包含了该对象的必要信息，比如对象的分代年龄，锁信息，hashCode，偏向锁ID，类型指针（压缩4字节，没压缩8字节），数组长度</p>
<p>实例数据： 对象中各个实例字段的数据</p>
<p>对其填充： 其中为了保持对象内存的整齐性，包括了对齐指针来进行对象的内存大小填充，能被8整除，为了保证数据在同一个缓存行，计算寻址最优</p>
<p>年龄分代最大值是15（4个bit只能存储到15）设置超过了15会报错，<strong>must be between 0 and 15</strong></p>
<h1 id="执行Clinet-方法"><a href="#执行Clinet-方法" class="headerlink" title="执行Clinet&lt;&gt;()方法"></a>执行Clinet&lt;&gt;()方法</h1><p>为对象赋用户自己的值，执行构造方法, 给变量赋最终值</p>
<h1 id="对象的类元信息解析"><a href="#对象的类元信息解析" class="headerlink" title="对象的类元信息解析"></a>对象的类元信息解析</h1><p>对象在堆中创建，其对象头中存在指向该对象的<strong>类元信息的指针 Class pointer</strong></p>
<p>也就是说，如果对象想要找到该对象的成员方法的具体方法内容（代码），就是通过这个对象头内的指针找的</p>
<p>比如: 对象的add()方法需要通过这个类指针去找到对应的add方法相关信息</p>
<p>Class对象，也是在堆，是提供给程序去访问方法区类信息的入口对象</p>
<p><strong>区分 对象、类对象、类元信息:</strong></p>
<p>对象:  是一个类的其中一个实体，可以有多个</p>
<p>类对象： class&lt;?extends Duixiang&gt; duixiangClass &#x3D; 对象.getClass();类加载完之后，是java虚拟机为方便用户操作该类的类元信息而创建的镜像对象，里面没有该类的信息，但是可以通过该类对象可以访问该类的信息。</p>
<p>类元信息：存放类的相关信息，比如变量与成员方法，在方法区中, 通过 Class 或者 klass 指针访问操作</p>
<p><img src="/images/posts/jvm_create_object/img-7.png" alt="Java对象创建过程"></p>
<h1 id="对象怎么定位"><a href="#对象怎么定位" class="headerlink" title="对象怎么定位"></a>对象怎么定位</h1><h2 id="句柄池"><a href="#句柄池" class="headerlink" title="句柄池"></a>句柄池</h2><ul>
<li>句柄池映射对象和引用之间的的关系</li>
<li>优点:垃圾回收的时候只要修改句柄对应的地址,更好的统一管理,有稳定性</li>
<li>对象移动了地址，不需要重新改变指针， 只需要改变句柄池</li>
<li>缺点:效率更低,需要转换关系</li>
</ul>
<h2 id="直接指针"><a href="#直接指针" class="headerlink" title="直接指针"></a>直接指针</h2><ul>
<li>优点:查询效率更快,减少了类似句柄的转换关系的开销</li>
<li>缺点:垃圾回收的时候, 引用需要修改, 被改了, 其他在用的时候可能出问题.</li>
</ul>
<h1 id="对象分配策略"><a href="#对象分配策略" class="headerlink" title="对象分配策略"></a>对象分配策略</h1><p><img src="/images/posts/jvm_create_object/img-8.png" alt="Java对象创建过程"></p>
<ol>
<li>能否在栈上分配</li>
<li>对象优先在Eden区分配</li>
</ol>
<details><summary>是否能通过TLAB方式分配在 eden 区</summary>

</details>

<ol>
<li><ol>
<li>大对象直接进入老年代 (** -XX:PretenureSizeThreshold** 配置大小, 默认是0,空间不够了才触发)</li>
</ol>
</li>
<li>长期存活的对象将进入老年代</li>
<li>动态年龄判断（相同年龄占单个S区一半）</li>
<li>空间分配担保（利用Young GC防止频繁的fgc）</li>
</ol>
<h2 id="对象栈上分配原理"><a href="#对象栈上分配原理" class="headerlink" title="对象栈上分配原理"></a>对象栈上分配原理</h2><ul>
<li>通过逃逸分析算法，判断这个对象的作用域，如果该对象只在该栈帧中调用，即方法运行完成即回收，则在栈的该线程栈的栈帧上创建，此方法运行完成，即在对应栈帧上出栈，然后立即被回收, 避免了在堆上创建，然后进行gc后回收的耗时与浪费空间。</li>
<li>上边说是在栈上创建对象，其实是将该对象分解为一个个标量进行创建在栈上，并没有真的在栈上创建对象，因为对象的创建需要一块连续的空间，栈上不一定有。标量即不可被再次分解的量JAVA的基本数据类型就是标量（如：int，long等基本数据类型以及reference类型等），标量的对立就是可以被进一步分解的量，而这种量称之为聚合量。而在JAVA中对象就是可以被进一步分解的聚合量</li>
</ul>
<h2 id="是否能在TLAB分配"><a href="#是否能在TLAB分配" class="headerlink" title="是否能在TLAB分配"></a>是否能在TLAB分配</h2><p>不管是<strong>指针碰撞</strong>还是<strong>空闲散列表</strong>分配的方法,都需要先获得操作指针,获取操作指针可能是并发操作,会通过CAS去进行**,CAS的瓶颈在于多次咨询会影响CPU空消耗**</p>
<p>如果对象大小小于<strong>1%Eden</strong>区大小,JVM会在堆内分配一块<strong>线程私有</strong>的内存地址,给到线程进行<strong>TLAB</strong>分配</p>
<h2 id="优先Eden分配"><a href="#优先Eden分配" class="headerlink" title="优先Eden分配"></a>优先Eden分配</h2><p>大多数情况下，对象在新生代 Eden 区分配，当 Eden 区空间不够时，发起 Minor GC</p>
<details><summary>关于 Minor GC 和 Full GC</summary>

</details>

<h2 id="大对象直接进入老年代"><a href="#大对象直接进入老年代" class="headerlink" title="大对象直接进入老年代"></a>大对象直接进入老年代</h2><p>大对象是指需要连续内存空间的对象，最典型的大对象是那种很长的字符串以及数组。经常出现大对象会提前触发垃 圾收集以获取足够的连续空间分配给大对象。</p>
<p>提供 -<strong>XX:PretenureSizeThreshold</strong> 参数，大于此值的对象或者是大于eden区的剩余空间直接在老年代分配，避免在 Eden 区和 Survivor 区之间的大量内存复制</p>
<h2 id="长期存活的对象进入老年代"><a href="#长期存活的对象进入老年代" class="headerlink" title="长期存活的对象进入老年代"></a>长期存活的对象进入老年代</h2><p>JVM 为对象定义年龄计数器，经过 Minor GC 依然存活，并且能被 Survivor 区容纳的，移被移到 Survivor 区，年龄就增加 1 岁，增加到一定年龄则移动到老年代中（默认 15 岁，通过 -XX:MaxTenuringThreshold 设置）</p>
<h2 id="动态对象年龄判断"><a href="#动态对象年龄判断" class="headerlink" title="动态对象年龄判断"></a>动态对象年龄判断</h2><p>JVM 并不是永远地要求对象的年龄必须达到 MaxTenuringThreshold 才能晋升老年代，如果在 Survivor 区中<strong>相同年龄</strong>所有对象大小的总和大于  <strong>单个Survivor 空间的一半</strong>，则年龄大于或等于该年龄的对象可以直接进入老年代， 无需等待 MaxTenuringThreshold 中要求的年龄</p>
<p><strong>原因分析</strong>:  因为一半的对象占了其中一个 S 区, 理论上存活的次数越久, 而这些对象后续大概率还是存活, 这样挪来挪去会浪费性能,  并且如果这个时候有大量的新对象进来, 空间不够用了,  会把新来的对象直接放到老年代, 这 些新对象本来可能是马上死亡的,  但是由于已经放入老年代, 必须等下一次 full gc 才能把已经无用的新对象清理掉</p>
<h2 id="空间分配担保机制"><a href="#空间分配担保机制" class="headerlink" title="空间分配担保机制"></a>空间分配担保机制</h2><p><strong>当出现大量对象在一次 Minor GC 后仍然存活的情况时，Survivor 区可能容纳不下这么多对象，此时，就需要老年代进行分配担保，即将 Survivor 无法容纳的对象直接进入老年代。</strong></p>
<p>这么做有一个前提，就是老年代得装得下这么多对象。可是在一次 GC 操作前，虚拟机并不知道到底会有多少对象存活，所以空间分配担保有这样一个判断流程</p>
<p>发生了Minor GC,虚拟机先检查老年代的最大可连续空间是否大于新生代所有的对象的总空间：</p>
<ul>
<li>如果大于，Minor GC 一定是安全的</li>
</ul>
<details><summary>如果小于，虚拟机会查看 HandlePromotionFailure 参数，看看是否允许担保失败</summary>

</details>

<ul>
<li>不过 JDK 6 Update 24 后，HandlePromotionFailure 参数就没有用了，规则变为只要老年代的连续空间大于新生代对象总大小或者历次晋升的平均大小就会进行 Minor GC，否则将进行 Full GC</li>
</ul>
<h1 id="new一个对象过程"><a href="#new一个对象过程" class="headerlink" title="new一个对象过程"></a>new一个对象过程</h1><p><img src="/images/posts/jvm_create_object/img-9.png" alt="Java对象创建过程"></p>
<p>0.先开辟一个空间<br>4初始化值<br>7.建立关联关系<br>（可能发生重排序，是可能把7和4换一个顺序的， 因为不影响最终一致性）</p>
<h1 id="如何计算一个对象大小"><a href="#如何计算一个对象大小" class="headerlink" title="如何计算一个对象大小"></a>如何计算一个对象大小</h1><p><img src="/images/posts/jvm_create_object/img-10.png" alt="Java对象创建过程"></p>
<p><img src="/images/posts/jvm_create_object/img-11.png" alt="Java对象创建过程"></p>
<h2 id="借助jol来计算"><a href="#借助jol来计算" class="headerlink" title="借助jol来计算"></a>借助jol来计算</h2><p>maven依赖</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;org.openjdk.jol&lt;/groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;jol-core&lt;/artifactId&gt;</span><br><span class="line">            &lt;version&gt;<span class="number">0.14</span>&lt;/version&gt;</span><br><span class="line">        &lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<p>代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">Person</span> <span class="variable">p</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;baoYang&quot;</span>, <span class="number">25</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;Object Size: &quot;</span> + VM.current().sizeOf(p));</span><br><span class="line">        System.out.println(ClassLayout.parseInstance(p).toPrintable());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object Size: <span class="number">24</span></span><br><span class="line">com.yang.keyword.basic.Person object internals:</span><br><span class="line"> OFFSET  SIZE               TYPE DESCRIPTION                               VALUE</span><br><span class="line">      <span class="number">0</span>     <span class="number">4</span>                    (object header)                           <span class="number">01</span> <span class="number">00</span> <span class="number">00</span> <span class="number">00</span> (<span class="number">00000001</span> <span class="number">00000000</span> <span class="number">00000000</span> <span class="number">00000000</span>) (<span class="number">1</span>)</span><br><span class="line">      <span class="number">4</span>     <span class="number">4</span>                    (object header)                           <span class="number">00</span> <span class="number">00</span> <span class="number">00</span> <span class="number">00</span> (<span class="number">00000000</span> <span class="number">00000000</span> <span class="number">00000000</span> <span class="number">00000000</span>) (<span class="number">0</span>)</span><br><span class="line">      <span class="number">8</span>     <span class="number">4</span>                    (object header)                           <span class="number">43</span> c1 <span class="number">00</span> f8 (<span class="number">01000011</span> <span class="number">11000001</span> <span class="number">00000000</span> <span class="number">11111000</span>) (-<span class="number">134168253</span>)</span><br><span class="line">     <span class="number">12</span>     <span class="number">4</span>                <span class="type">int</span> Person.age                                <span class="number">25</span></span><br><span class="line">     <span class="number">16</span>     <span class="number">4</span>   java.lang.String Person.name                               (object)</span><br><span class="line">     <span class="number">20</span>     <span class="number">4</span>                    (loss due to the next object alignment)</span><br><span class="line">Instance size: <span class="number">24</span> bytes</span><br><span class="line">Space losses: <span class="number">0</span> bytes internal + <span class="number">4</span> <span class="type">bytes</span> <span class="variable">external</span> <span class="operator">=</span> <span class="number">4</span> bytes total</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM基础</title>
    <url>//article/jvm_base/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>JVM基础知识的学习</p>
</blockquote>
<h1 id="📝-为什么用JVM"><a href="#📝-为什么用JVM" class="headerlink" title="📝 为什么用JVM"></a>📝 为什么用JVM</h1><ol>
<li>可以轻松实现Java代码的跨平台执行</li>
<li>JVM 提供<strong>内存管理</strong>、<strong>垃圾回收</strong>、编译时动态校验等功能</li>
<li>使用JVM能够让我们的编程工作更轻松、高效节省公司成本，提示社会化的整体开发效率，我们只关注和业务相关的程序逻辑的编写，其他业务无关但对于编程同样重要的事情交给JVM来处理</li>
</ol>
<p><img src="/images/posts/jvm_base/img-1.png" alt="JVM基础"></p>
<h1 id="📔-JVM是怎样运行字节码的"><a href="#📔-JVM是怎样运行字节码的" class="headerlink" title="📔 JVM是怎样运行字节码的"></a><strong>📔 JVM是怎样运行字节码的</strong></h1><ol>
<li>从虚拟机视角来看，执行 Java 代码首先需要将它编译而成的 class 文件加载到 Java 虚拟机中。</li>
<li>加载后的 Java 类会被存放于方法区（Method Area）中。实际运行时，虚拟机会执行方法区内的代码。</li>
<li>而且，Java 虚拟机同样也在内存中划分出堆和栈来存储运行时数据。不同的是，Java 虚拟机会将栈细分为面向 Java 方法的 Java 方法栈，面向本地方法（用 C++ 写的 native 方法）的本地方法栈，以及存放各个线程执行位置的 PC 寄存器</li>
</ol>
<p><img src="/images/posts/jvm_base/img-2.png" alt="JVM基础"></p>
<h1 id="📔-执行过程"><a href="#📔-执行过程" class="headerlink" title="📔 执行过程"></a><strong>📔 执行过程</strong></h1><ol>
<li>在运行过程中，每当调用进入一个 Java 方法，Java 虚拟机会在当前线程的 Java 方法栈中生成一个<strong>栈帧</strong>，<strong>用以存放局部变量以及字节码的操作数</strong>。</li>
<li>这个栈帧的大小是<strong>提前计算</strong>好的，而且 Java 虚拟机不要求栈帧在内存空间里连续分布。</li>
<li>当退出当前执行的方法时，不管是正常返回还是异常返回，Java 虚拟机均会弹出当前线程的当前栈帧，并将之舍弃</li>
</ol>
<p><img src="/images/posts/jvm_base/img-3.png" alt="JVM基础"></p>
<h1 id="插件安装"><a href="#插件安装" class="headerlink" title="插件安装"></a>插件安装</h1><h2 id="jvisualvm"><a href="#jvisualvm" class="headerlink" title="jvisualvm"></a>jvisualvm</h2><ol>
<li>visualvm新访问地址：<a href="https://visualvm.github.io/index.html">https://visualvm.github.io/index.html</a></li>
</ol>
<p>找到自己jdk对应的版本</p>
<ol>
<li>进入jvisualvm的插件管理  —→ 工具  —&gt; 插件 ；在”设置”中修改url地址为刚才我们在github上找到的对应我们JDK版本的地址</li>
<li>安装VisualGC插件</li>
<li>重启即可看到VisualGC</li>
</ol>
<p><img src="/images/posts/jvm_base/img-4.png" alt="JVM基础"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="http://www.oracle.com/technetwork/java/visualgc-136680.html">http://www.oracle.com/technetwork/java/visualgc-136680.html</a></li>
<li><a href="http://www.oracle.com/technetwork/java/javase/tech/vmoptions-jsp-140102.html">http://www.oracle.com/technetwork/java/javase/tech/vmoptions-jsp-140102.html</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM直接内存</title>
    <url>//article/jvm_direct_memory/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-直接内存"><a href="#📝-直接内存" class="headerlink" title="📝 直接内存"></a>📝 直接内存</h1><p>操作系统的本地内存,就是真正的内存.而不是像JVM<strong>虚拟化</strong>的内存</p>
<p><strong>优点: 更快</strong>,因为不像JVM堆、栈内存,需要先虚拟化一层,再做操作.直接内存是直接使用的.</p>
<p>缺点:需要人工释放</p>
<h2 id="使用案例"><a href="#使用案例" class="headerlink" title="使用案例"></a>使用案例</h2><p><strong>1. Java可以通过Unsafe类去操作本地内存</strong></p>
<p><img src="/images/posts/jvm_direct_memory/img-1.png" alt="JVM直接内存"></p>
<ol>
<li>Netty、ByteBuffer底层操作系统mallcoc函数</li>
</ol>
<p><img src="/images/posts/jvm_direct_memory/img-2.png" alt="JVM直接内存"></p>
<ol>
<li>JNI (native本地方法)</li>
<li>JNA,本质就是对JNI的包装</li>
</ol>
<h2 id="为什么使用直接内存"><a href="#为什么使用直接内存" class="headerlink" title="为什么使用直接内存"></a>为什么使用直接内存</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ol>
<li>减少垃圾回收次数, 不会STW;减少内存碎片化，提高内存利用率</li>
<li>加快操作速度, 减少了堆与直接内存的复制环节,直接省略了这一环节</li>
<li>多个JVM的话,共享很麻烦, 但是直接内存<strong>共享更简单.</strong></li>
<li>不受制于虚拟化的控制,比如搞个1TB内存,堆可能根本不够.直接内存直接加就可以了</li>
</ol>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ol>
<li><strong>不受JVM控制,需要自己手动释放</strong>,<strong>如果忘记了,会发生内存泄露,而且是直接内存泄露</strong>, </li>
<li>**不好排查问题.**因为直接内存和内核关联大,和JVM没什么关联,用JVM的分析工具没什么用</li>
<li>不适合存储复杂对象,复杂对象有引用,操作引用就可以了.</li>
</ol>
<h2 id="一些相关工具"><a href="#一些相关工具" class="headerlink" title="一些相关工具"></a>一些相关工具</h2><ol>
<li>MemoryMappedByteBuffer：Java NIO库中的类，可以将直接内存映射到文件中，实现高效的文件读写操作。</li>
<li>JMH（Java Microbenchmark Harness）：用于进行Java性能测试和微基准测试的工具，可以用于评估直接内存在特定场景下的性能优势。</li>
<li>JProfiler、VisualVM等性能分析工具：用于分析应用程序的内存使用情况，可以帮助发现直接内存的使用情况和性能问题。</li>
</ol>
<h1 id="监控和排查"><a href="#监控和排查" class="headerlink" title="监控和排查"></a>监控和排查</h1><ol>
<li>JVM工具：JVM提供了一些内置工具可以用于监控直接内存的使用情况，如jstat、jmap、jcmd等。这些工具可以查看直接内存的分配量、使用量、释放量等统计信息，帮助了解直接内存的使用情况和趋势。</li>
<li>操作系统工具：操作系统的工具可以提供直接内存的监控和排查功能。例如，在Linux上可以使用命令行工具<code>top</code>、<code>htop</code>、<code>pmap</code>等来查看进程的内存使用情况，包括直接内存的使用量。</li>
<li>第三方工具：一些第三方工具和监控系统专门用于监控Java应用程序的性能和资源使用情况，其中也包括直接内存的监控。例如，Java性能分析工具（如VisualVM、JProfiler、YourKit等）可以提供直接内存的监控和分析功能，帮助定位直接内存使用过多或泄漏的问题。</li>
<li>日志和诊断信息：通过在应用程序中添加适当的日志和诊断信息，可以追踪和记录直接内存的分配和释放情况。这样可以在出现问题时，通过日志和诊断信息进行排查，定位直接内存相关的异常情况</li>
<li>阿里开源的Arthas可以通过<code>jvm.directMemory</code>命令来监控直接内存的使用情况。使用该命令，你可以查看直接内存的分配量、使用量和剩余量等信息</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM垃圾回收</title>
    <url>//article/jvm_gc_info/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>JVM的垃圾回收机制</p>
</blockquote>
<h1 id="📝-简介"><a href="#📝-简介" class="headerlink" title="📝 简介"></a>📝 简介</h1><p>Java 的对象使用的是堆空间的内存, 方法的执行是栈帧的入栈出栈, 引用和局部变量可以随着入栈出栈直接清理, 但是引用对应所在堆空间的内存则不能通过出栈的结束而回收, 因为堆空间是线程共享的, 并不确定是否有其他线程引用了这块内存, 需要 GC 机制来进行自动的清理</p>
<h1 id="📔-垃圾收集"><a href="#📔-垃圾收集" class="headerlink" title="**📔**垃圾收集"></a>**📔**垃圾收集</h1><ul>
<li>程序计数器，虚拟机栈和本地方法栈 这三个区域属于线程私有，只存在于线程私有的，只存在于线程的生命周期内，线程结束后，因此不需要三个区域进行垃圾回收</li>
<li>垃圾回收主要针对JVM的 堆空间 和 方法区进行</li>
</ul>
<h2 id="判断一个对象是否可回收"><a href="#判断一个对象是否可回收" class="headerlink" title="判断一个对象是否可回收"></a>判断一个对象是否可回收</h2><ol>
<li>引用计数： 给对象添加一个引用计数器，当对象增加一个引用计数器加1,引用失效时计数器减1。引用计数为0时，可被回收</li>
</ol>
<p><strong>缺点</strong>: 两个对象出现循环引用的情况下，此时引用计数器永远不为 0，导致无法对它们进行回收</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">objA.instance = objB;</span><br><span class="line">objB.instance = objA;</span><br></pre></td></tr></table></figure>

<ol>
<li>可达性分析</li>
</ol>
<p>通过** GC Roots** 作为起始点进行搜索，能够到达到的对象都是都是可用的，不可达的对象可被回收</p>
<p><strong>GC Roots 一般包含:</strong></p>
<ol>
<li>虚拟机栈中引用的对象</li>
<li>本地方法栈中引用的对象</li>
<li>方法区中的常量引用的对象</li>
<li>方法区中类静态属性引用的对象</li>
</ol>
<p><img src="/images/posts/jvm_gc_info/img-1.png" alt="JVM垃圾回收"></p>
<h2 id="引用类型"><a href="#引用类型" class="headerlink" title="引用类型"></a>引用类型</h2><ol>
<li>强引用</li>
</ol>
<ul>
<li><strong>只要</strong>强引用关系存在**，垃圾回收器永远不会回收调掉被引用的对象**</li>
</ul>
<ol>
<li>软引用</li>
</ol>
<ul>
<li><strong>一些有用但是并非必须的对象，在系统发生OOM之前，将会对这些对象列进回收范围之中进行第二次回收</strong></li>
</ul>
<ol>
<li>弱引用</li>
</ol>
<ul>
<li><strong>只能生存到下一次垃圾收集之前，当垃圾收集工作时，无论当前内存是否满足，都会被回收</strong></li>
</ul>
<ol>
<li>虚引用</li>
</ol>
<ul>
<li><strong>幽灵引用或者幻影引用。一个对象是否有虚引用的存在，完全不会对其生存时间构成影响，也无法通过虚引用 取得一个对象实例。 为一个对象设置虚引用关联的唯一目的就是能在这个对象被收集器回收时收到一个系统通知 业界暂无使用场景， 可能被JVM团队内部用来跟踪JVM的垃圾回收活动</strong></li>
</ul>
<h1 id="垃圾什么时候回收"><a href="#垃圾什么时候回收" class="headerlink" title="垃圾什么时候回收"></a>垃圾什么时候回收</h1><p><img src="/images/posts/jvm_gc_info/img-2.png" alt="JVM垃圾回收"></p>
<p><strong>完成两次标记的会被回收</strong>.</p>
<ol>
<li>第一次标记是通过gc root 标记是否可达</li>
<li>第二次会看不可达的对象中, 是否实现自己的finalize()方法，有就执行一次，但是未必会等到返回结果， 因为你实现的finalze可能有问题</li>
</ol>
<h2 id="finalize"><a href="#finalize" class="headerlink" title="finalize()"></a><strong>finalize()</strong></h2><p>当一个对象可被回收时，会执行该对象的 finalize() 方法，如果通过在该方法中让对象重新被引用，<strong>从而有可能</strong>而实现自救。</p>
<ol>
<li>该方法在Object对象里面</li>
<li>丢到一个 F-QUEUE 队列中, 并且启动一个守护线程(优先级很低) finalizer 来执行,** 但是**<strong>可能CPU轮片切换了,没来得及执行,所以不做保障</strong></li>
</ol>
<h1 id="HotSpot中GC算法实现"><a href="#HotSpot中GC算法实现" class="headerlink" title="HotSpot中GC算法实现"></a>HotSpot中GC算法实现</h1><p><img src="/images/posts/jvm_gc_info/img-3.png" alt="JVM垃圾回收"></p>
<h1 id="📔-OoPMap"><a href="#📔-OoPMap" class="headerlink" title="**📔**OoPMap"></a>**📔**OoPMap</h1><p>进行可达性分析的第一步，就是要枚举 GC Roots，这就需要虚拟机知道哪些地方存放着对象应用。如果每一次枚举 GC Roots 都需要把整个栈上位置都遍历一遍，那可就费时间了，毕竟并不是所有位置都存放在引用呀。所以为了提高 GC 的效率，HotSpot 使用了一种 OopMap 的数据结构，<strong>OopMap 记录了栈上本地变量到堆上对象的引用关系</strong>，也就是说，GC 的时候就不用遍历整个栈只遍历每个栈的 OopMap 就行了</p>
<p>在 OopMap 的帮助下，HotSpot 可以快速准确的完成 GC 枚举了，不过，OopMap 也不是万年不变的，它也是需要被更新的，当内存中的对象间的引用关系发生变化时，就需要改变 OopMap 中的相应内容。可是能导致引用关系发生变化的指令非常之多，如果我们执行完一条指令就改下 OopMap，这 GC 成本实在太高了</p>
<p>因此，HotSpot 采用了一种在 “安全点” 更新 OopMap 的方法，安全点的选取既不能让 GC 等待的时间过长，也不能过于频繁增加运行负担，也就是说，我们既要让程序运行一段时间，又不能让这个时间太长。我们知道，JVM 中每条指令执行的是很快的，所以一个超级长的指令流也可能很快就执行完了，所以 <strong>真正会出现 “长时间执行” 的一般是指令的复用，例如：方法调用、循环跳转、异常跳转等</strong>，虚拟机一般会将这些地方设置为安全点更新 OopMap 并判断是否需要进行 GC 操作</p>
<p>此外，在进行枚举根节点的这个操作时，为了保证准确性，我们需要在一段时间内 “冻结” 整个应用，即 Stop The World（传说中的 GC 停顿），因为如果在我们分析可达性的过程中，对象的引用关系还在变来变去，那是不可能得到正确的分析结果的。即便是在号称几乎不会发生停顿的 CMS 垃圾收集器中，枚举根节点时也是必须要停顿的。这里就涉及到了一个问题</p>
<h1 id="📔安全点SafePoint"><a href="#📔安全点SafePoint" class="headerlink" title="📔安全点SafePoint"></a><strong>📔安全点SafePoint</strong></h1><p><strong>Safepoint是垃圾回收器执行停顿的触发点，用于确保所有线程都处于安全状态，以执行特定的操作（如栈帧的扫描和对象标记）</strong></p>
<p><strong>跟引用的变化有关系, 尽量不影响引用的情况下发生安全点</strong></p>
<p><strong>原理:记录一个标记位,业务线程不断的轮训检测这个标志位,发现信号之后,就要跑到最近的一个安全点把自己挂起,等待GC线程执行, 该标记位尽量是引用不太发生变化的位置</strong></p>
<ul>
<li>safepoint不能太少，否则GC等待的时间会很久（无法有效的触发停顿，导致垃圾回收停顿时间增加）</li>
<li>safepoint不能太多，否则将增加运行GC的负担</li>
</ul>
<p><strong>安全点常见的位置:</strong></p>
<ol>
<li><strong>循环的末尾</strong></li>
<li><strong>方法临返回前&#x2F;调用方法的call指令后</strong></li>
<li><strong>可能抛异常的位置</strong></li>
</ol>
<p>之所以选择这些位置作为safepoint的插入点，主要的考虑是“避免程序长时间运行而不进入safepoint”，比如GC的时候必须要等到Java线程都进入到safepoint的时候VMThread才能开始执行GC，如果程序长时间运行而没有进入safepoint，那么GC也无法开始，JVM可能进入到Freezen假死状态</p>
<p>JVM在很多场景下使用到safepoint, 最常见的场景就是GC的时候。对一个Java线程来说，它要么处在safepoint,要么不在safepoint</p>
<h1 id="安全区"><a href="#安全区" class="headerlink" title="安全区"></a>安全区</h1><p><strong>安全点触发不了,就会触发安全区的模式</strong></p>
<p>业务线程如果sleep了，业务线程就检测不到是否需要挂起，不能GC线程工作了;这个时候会风险，这段代码，有那一段是安全的，不会发生引用变化的，<strong>那么在这个区域内,都是可以发生GC的</strong></p>
<p><img src="/images/posts/jvm_gc_info/img-4.png" alt="JVM垃圾回收"></p>
<h1 id="方法区回收"><a href="#方法区回收" class="headerlink" title="方法区回收"></a>方法区回收</h1><p>  <strong>方法区主要存放比较经常使用的对象，所以永久代对象的回收率比新生代差很多(经常用的数据,并且不怎么被回收,所以称之为永久代)</strong>,  主要是对常量池的回收和对类的卸载,因为方法比较经常被使用到,想收回也不好回收</p>
<p>类的卸载条件很多，需要满足以下三个条件，并且满足了也不一定会被卸载：</p>
<ol>
<li>该类所有的实例都已经被回收，也就是 Java 堆中不存在该类的任何实例</li>
<li>加载该类的 ClassLoader 已经被回收</li>
<li>该类对应的 java.lang.Class 对象没有在任何地方被引用，也就无法在任何地方通过反射访问该类方法</li>
</ol>
<p>可以通过 -Xnoclassgc 参数来控制是否对类进行卸载</p>
<p>在大量使用反射、动态代理、CGLib 等 ByteCode 框架、动态生成 JSP 以及 OSGo 这类频繁自定义 ClassLoader 的场景都需要虚拟机具备类卸载功能，以保证不会出现内存溢出</p>
<p>在 Java 8 中，方法区仍然需要进行垃圾回收（GC），不过与永久代不同的是，方法区的垃圾回收使用的是基于内存分配担保的方式，即当方法区无法分配新的内存时，会触发垃圾回收来释放一些无用的类信息等元数据，从而腾出更多的空间来存储新的类定义信息和方法信息</p>
<h1 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h1><h2 id="复制算法"><a href="#复制算法" class="headerlink" title="复制算法"></a>复制算法</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>实现简单，运行高效，无需标记和清理操作，只需要复制即可</li>
<li>没有内存碎片</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>需要两倍的内存空间</li>
<li>只能使用一半的空间来存储数据，不能达到内存的最大使用率</li>
</ul>
<p>复制算法常用于新生代的垃圾回收，因为新生代中的对象生命周期短，且对象数量少，适合使用复制算法。</p>
<p><img src="/images/posts/jvm_gc_info/img-5.png" alt="JVM垃圾回收"></p>
<h2 id="标记-清除算法"><a href="#标记-清除算法" class="headerlink" title="标记-清除算法"></a>标记-清除算法</h2><h3 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h3><ul>
<li>实现简单</li>
</ul>
<h3 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>标记和清除的过程效率都不高</li>
<li>标记清除之后会产生大量的内存碎片，不利于后续的内存分配，也会导致内存的使用率降低</li>
</ul>
<p>标记-清除算法常用于老年代的垃圾回收，因为老年代中的对象生命周期长，且对象数量多，适合使用标记-清除算法。</p>
<p><img src="/images/posts/jvm_gc_info/img-6.png" alt="JVM垃圾回收"></p>
<h2 id="标记-整理算法"><a href="#标记-整理算法" class="headerlink" title="标记-整理算法"></a>标记-整理算法</h2><h3 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h3><ul>
<li>可以避免标记-清除算法产生的内存碎片问题</li>
</ul>
<h3 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>标记-整理算法比较耗时</li>
<li>需要移动对象，如果存活对象比较多，移动的成本就会比较高</li>
</ul>
<p>标记-整理算法常用于老年代的垃圾回收，因为老年代中的对象生命周期长，且对象数量多，适合使用标记-整理算法。</p>
<p><img src="/images/posts/jvm_gc_info/img-7.png" alt="JVM垃圾回收"></p>
<h2 id="分代回收"><a href="#分代回收" class="headerlink" title="分代回收"></a>分代回收</h2><p>分代收集是一种垃圾回收算法，将虚拟机中的内存分为年轻代（Young Generation）、老年代（Old Generation）和永久代（Permanent Generation，也称方法区）。这种算法是基于一个事实：不同年龄段的对象有不同的生命周期。</p>
<p>年轻代中存放新生的对象，在这里进行短时间存活的对象都可以被看做是不用收集的垃圾对象，而那些经过多次垃圾回收后仍然存活下来的对象，就可以被视为是生命周期比较长的对象。同样的，老年代中存放着生命周期比较长的对象，而永久代中存放着一些不会被垃圾回收的对象，例如Java类的定义等。</p>
<p>分代收集的优点：</p>
<ul>
<li>年轻代中的对象生命周期短，容易被回收，因此可以使用复制算法进行垃圾回收，这样的效率比较高。</li>
<li>老年代中的对象生命周期长，可以使用标记-整理或标记-清除算法进行垃圾回收，这样的效率比较高。</li>
<li>分代收集可以很好地处理不同对象的生命周期，提高垃圾回收的效率。</li>
</ul>
<p>分代收集的缺点：</p>
<ul>
<li>分代收集算法比标记-清除或标记-整理算法更复杂，需要对不同年龄段的对象进行管理，增加了算法的复杂度。</li>
<li>在分代收集算法中，需要将内存分为不同年龄段，这样的分配可能会导致一些内存浪费。</li>
</ul>
<h2 id="卡表-提供效率-空间换时间"><a href="#卡表-提供效率-空间换时间" class="headerlink" title="卡表(提供效率,空间换时间)"></a>卡表(提供效率,空间换时间)</h2><h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>假如我们进行MinorGC，会不会有对象被老年代引用着？进行OldGC会不会又有对象被年轻代引用着？如果是的话，那我们进行MinorGC的时候不光要管GC Roots，还有再去遍历老年代，这个性能问题就很大了，该如何解决效率问题？</p>
<p><strong>存在的问题是:</strong></p>
<p>由于这种情况是极少数的,如果连同老年代一起扫描,老年代比年轻代大2倍,扫描的时间也比单纯的只扫描年轻代多了2倍,而且还可能扫完了并不存在跨代引用的情况,白花了那么多时间</p>
<p><strong>解决方案:</strong></p>
<p>我们不用去扫描整个老年代了，只要在年轻代建立一个数据结构，叫做记忆集Remembered Set，他把老年代划分为N个区域，标志出哪个区域会存在跨代引用。</p>
<p>以后在进行MinorGC的时候，只要把这些包含了跨代引用的内存区域加入GC Roots一起扫描就行了</p>
<p><img src="/images/posts/jvm_gc_info/img-8.png" alt="JVM垃圾回收"></p>
<p><img src="/images/posts/jvm_gc_info/img-9.png" alt="JVM垃圾回收"></p>
<h1 id="三色标记法"><a href="#三色标记法" class="headerlink" title="三色标记法"></a>三色标记法</h1><details><summary>**在并发标记阶段使用的标记算法**</summary>

</details>

<p><img src="/images/posts/jvm_gc_info/img-10.png" alt="JVM垃圾回收"></p>
<h2 id="多浮动-垃圾"><a href="#多浮动-垃圾" class="headerlink" title="多浮动-垃圾"></a>多浮动-垃圾</h2><p>在并发标记过程中，如果由于方法运行结束导致部分局部变量(gcroot)被销毁，这个gcroot引用的对象之前又被扫描过<br>(被标记为非垃圾对象)，那么本轮GC不会回收这部分的内存.这部分本应该回收的但是没有回到到的内存，被称之为”浮动垃圾”。浮动垃圾并不会影响垃圾回收的正确性，只需要等待下一轮垃圾回收中才被清除</p>
<p>针对并发标记（并发清理）开始产生的新对象，通常的做法是直接全部当成黑色，本轮不会进行清除。这部分对象期间可能会变成垃圾，这也是浮动垃圾的一部分</p>
<h2 id="漏标问题"><a href="#漏标问题" class="headerlink" title="漏标问题"></a>漏标问题</h2><p>导致被引用的对象被当成垃圾误删除,严重bug</p>
<details><summary>处理方式</summary>

</details>

<p>以上都是通过写屏障来实现的;所谓写屏障，其实就是在赋值操作前后，加一些处理(参考AOP)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"> /**</span><br><span class="line"> * @param field 某对象的成员变量，如 a.b.d</span><br><span class="line"> * @param new_value 新值，如 null</span><br><span class="line"> */</span><br><span class="line"> void oop_field_store(oop* field, oop new_value) &#123;</span><br><span class="line"> *field = new_value; // 赋值操作</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">void oop_field_store(oop* field, oop new_value) &#123;</span><br><span class="line">   pre_write_barrier(field); // 写屏障‐写前操作</span><br><span class="line">   *field = new_value;</span><br><span class="line">   post_write_barrier(field, value); // 写屏障‐写后操作</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="写屏障实现SATB"><a href="#写屏障实现SATB" class="headerlink" title="写屏障实现SATB"></a>写屏障实现SATB</h3><p>当对象B的成员变量的引用发生变化时，比如引用消失(a.b.d &#x3D; null),我们可以利用写屏障，将B原来成员变量的引用对象D记录下来</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">void pre_write_barrier(oop* field) &#123;</span><br><span class="line">  oop old_value = *field; // 获取旧值</span><br><span class="line">  remark_set.add(old_value); // 记录原来的引用对象</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="写屏障实现增量更新"><a href="#写屏障实现增量更新" class="headerlink" title="写屏障实现增量更新"></a>写屏障实现增量更新</h3><p>当对象A的成员变量的引用发生变化时，比如新增(a.d &#x3D; d),我们可以利用写屏障，将A新的成员变量引用对象D记录下来</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">void post_write_barrier(oop* field, oop new_value) &#123;</span><br><span class="line"> remark_set.add(new_value); // 记录新引用的对象</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="读屏障"><a href="#读屏障" class="headerlink" title="读屏障"></a>读屏障</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">oop oop_field_load(oop* field) &#123;</span><br><span class="line">  pre_load_barrier(field); // 读屏障‐读取前操作</span><br><span class="line">  <span class="built_in">return</span> *field;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">// 读屏障是直接针对第一步：D d = a.b.d，当读取成员变量时，一律记录下来：</span><br><span class="line">void pre_load_barrier(oop* field) &#123;</span><br><span class="line">  oop old_value = *field;</span><br><span class="line">  remark_set.add(old_value); // 记录读取到的对象</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<details><summary>**以Java HotSpot VM为例，其并发标记时对漏标的处理方案如下**</summary>

</details>

<p>SATB相对增量更新效率会高(当然STAB可能造成更多的浮动垃圾),因为不需要在重新标记阶段再次扫描被删除引用对象,而CMS增量引用的根对象会做深度描扫，G1因为很多对象都位于不同的region，CMS就一块老年代区域，重新深度描扫对象的话，G1的代价会比CMS高,所以G1选择SATB不深度扫描对象，只是简单标记，等到下一轮GC再深度描扫</p>
<p>B → D 消失, A再指向D,D现在不是垃圾被当成了垃圾</p>
<p><img src="/images/posts/jvm_gc_info/img-11.png" alt="JVM垃圾回收"></p>
<p>CMS的处理方式： 通过写屏障(钩子程序,回调这个函数),就是发现黑色对象有新的引用的引用的时候(也就是发现有白色的对象指向了黑色),会加一段操作，把黑色变成了灰色</p>
<p><img src="/images/posts/jvm_gc_info/img-12.png" alt="JVM垃圾回收"></p>
<p><img src="/images/posts/jvm_gc_info/img-13.png" alt="JVM垃圾回收"></p>
<p>多线程下不安全:</p>
<p>M1垃圾线程 标记了 1 还没标记2，  这个时候M1的角度是灰色<br>M2业务线程开始了，M2开始的时候 发生了B-D无了， A指向了D， 这个时候会将A变成灰<br>M1垃圾线程回来了， 将2标完 A就变成黑色了，  此时A是黑色的。<br>M2业务线程开始了，本来是灰色的，现在被M1改成了黑色， D就漏标了</p>
<p><img src="/images/posts/jvm_gc_info/img-14.png" alt="JVM垃圾回收"></p>
<h1 id="垃圾收集器"><a href="#垃圾收集器" class="headerlink" title="垃圾收集器"></a>垃圾收集器</h1><ol>
<li><p>JDK 1.8默认是 PS+PO</p>
</li>
<li><ol>
<li>左边都是分代， 右边是分区</li>
</ol>
</li>
</ol>
<p><img src="/images/posts/jvm_gc_info/img-15.png" alt="JVM垃圾回收"></p>
<h2 id="Serial收集器"><a href="#Serial收集器" class="headerlink" title="Serial收集器"></a>Serial收集器</h2><p>单线程收集器,不仅意味着只会使用一个线程进行垃圾收集工作,更重要的是它在进行垃圾收集时，必须暂停其它的线程,往往造成过程的等待时间</p>
<p>它的优点是简单高效，对于单个 CPU 环境来说，由于没有线程交互的开销，因此拥有最高的单线程收集效率</p>
<p><img src="/images/posts/jvm_gc_info/img-16.png" alt="JVM垃圾回收"></p>
<h2 id="ParNew收集器"><a href="#ParNew收集器" class="headerlink" title="ParNew收集器"></a>ParNew收集器</h2><p>Serial收集器的多线程版本</p>
<p>是 Server 模式下的虚拟机首选新生代收集器，除了性能原因外，主要是因为除了 Serial 收集器，<strong>只有它能与 CMS 收集器配合工作</strong></p>
<p>默认开始的线程数量与 CPU 数量相同，可以使用 -XX:ParallelGCThreads 参数来设置线程数</p>
<p><img src="/images/posts/jvm_gc_info/img-17.png" alt="JVM垃圾回收"></p>
<h2 id="Parallel-Scavenge收集器"><a href="#Parallel-Scavenge收集器" class="headerlink" title="Parallel Scavenge收集器"></a>Parallel Scavenge收集器</h2><p>是并行的多线程收集器。 其它收集器关注点是尽可能缩短垃圾收集时用户线程的停顿时间，而它的目标是达到一个可控制的吞吐量，它被称 为“吞吐量优先”收集器。<br>这里的吞吐量指 CPU 用于运行用户代码的时间占总时间的比值。 停顿时间越短就越适合需要与用户交互的程序，良好的响应速度能提升用户体验。而高吞吐量则可以高效率地利用 CPU 时间，尽快完成程序的运算任务，主要适合在后台运算而不需要太多交互的任务。<br>提供了两个参数用于精确控制吞吐量，分别是控制最大垃圾收集停顿时间 -XX:MaxGCPauseMillis 参数以及直接 设置吞吐量大小的 -XX:GCTimeRatio 参数（值为大于 0 且小于 100 的整数）。缩短停顿时间是以牺牲吞吐量 和新生代空间来换取的：新生代空间变小，垃圾回收变得频繁，导致吞吐量下降。<br>还提供了一个参数 -XX:+UseAdaptiveSizePolicy，这是一个开关参数，打开参数后，就不需要手工指定新生代 的大小（-Xmn）、Eden 和 Survivor 区的比例（-XX:SurvivorRatio）、晋升老年代对象年龄（- XX:PretenureSizeThreshold）等细节参数了，虚拟机会根据当前系统的运行情况收集性能监控信息，动态调整 这些参数以提供最合适的停顿时间或者最大的吞吐量，这种方式称为 GC 自适应的调节策略（GC Ergonomics）。 自适应调节策略也是它与 ParNew 收集器的一个重要区别</p>
<p><img src="/images/posts/jvm_gc_info/img-18.png" alt="JVM垃圾回收"></p>
<h2 id="Serial-Old收集器"><a href="#Serial-Old收集器" class="headerlink" title="Serial Old收集器"></a>Serial Old收集器</h2><p>Serial Old 是 Serial 收集器的老年代版本,也是Client模式下的虚拟机使用</p>
<p>如果用在 Server 模式 下，它有两大用途：</p>
<ol>
<li>在 JDK 1.5 以及之前版本（Parallel Old 诞生以前）中与 Parallel Scavenge 收集器搭配使用。</li>
<li>作为 CMS 收集器的后备预案，在并发收集发生 Concurrent Mode Failure 时使用</li>
</ol>
<p><img src="/images/posts/jvm_gc_info/img-19.png" alt="JVM垃圾回收"></p>
<h2 id="Parallel-Old收集器"><a href="#Parallel-Old收集器" class="headerlink" title="Parallel Old收集器"></a>Parallel Old收集器</h2><p>是 Parallel Scavenge 收集器的老年代版本。<br>在注重吞吐量以及 CPU 资源敏感的场合，都可以优先考虑 Parallel Scavenge 加 Parallel Old 收集器</p>
<p><img src="/images/posts/jvm_gc_info/img-20.png" alt="JVM垃圾回收"></p>
<h2 id="CSM收集器"><a href="#CSM收集器" class="headerlink" title="CSM收集器"></a>CSM收集器</h2><p><img src="/images/posts/jvm_gc_info/img-21.png" alt="JVM垃圾回收"></p>
<p><img src="/images/posts/jvm_gc_info/img-22.png" alt="JVM垃圾回收"></p>
<h3 id="初始标记"><a href="#初始标记" class="headerlink" title="初始标记"></a>初始标记</h3><p><strong>仅仅只是标记一下 GC Roots 能直接关联到的对象，速度很快，需要停顿</strong></p>
<h3 id="并发标记"><a href="#并发标记" class="headerlink" title="并发标记"></a>并发标记</h3><p>进行 GC Roots Tracing 的过程，它在整个回收过程中耗时最长，不需要停顿。 (会产生脏页问题)</p>
<h3 id="预清理阶段"><a href="#预清理阶段" class="headerlink" title="预清理阶段"></a>预清理阶段</h3><p>此阶段标记从<strong>新生代晋升的对象</strong>、<strong>新分配到老年代的对象</strong>以及在<strong>并发阶段被修改了的对象</strong>。(扫描引用放生了变化的对象, 黑对象会重新变成灰色,重新扫描),是不可中断的, 并发标记时，如果某个对象的引用发生了变化，就标记该对象所在的块为 <strong>dirty card</strong>。并发预清理阶段就会重新扫描该块，将该对象引用的对象标识为可达</p>
<h3 id="并发可中断预处理"><a href="#并发可中断预处理" class="headerlink" title="并发可中断预处理"></a>并发可中断预处理</h3><p>新生代中对象的特点是“朝生夕灭”，这样如果Remark前执行一次Minor GC，大部分对象就会被回收。这样,老年代回收的时候,就会更少的扫描到年轻代的引用指向老年代, CMS就采用了这样的方式，在Remark前增加了一个可中断的并发预清理（CMS-concurrent-abortable-preclean），该阶段主要工作仍然是并发标记对象是否存活，只是这个过程可被中断。此阶段在Eden区使用超过2M时启动，当然2M是默认的阈值，可以通过参数修改。如果此阶段执行时等到了Minor GC，那么上述灰色对象将被回收，Reamark阶段需要扫描的对象就少</p>
<details><summary>CMS 有两个参数：**CMSScheduleRemarkEdenSizeThreshold**、**CMSScheduleRemarkEdenPenetration**，默认值分别是2M、50%。两个参数组合起来的意思是预清理后，eden空间使用超过2M时启动可中断的并发预清理（CMS-concurrent-abortable-preclean），直到eden空间使用率达到50%时中断，进入remark阶段</summary>

</details>

<h3 id="重新标记"><a href="#重新标记" class="headerlink" title="重新标记"></a>重新标记</h3><p>重新标记阶段就是为了修正并发标记期间因为用户程序继续运行而导致标记产生变动的那一部分对象的标记记录，这个阶段的停顿时间一般会比初始标记阶段的时间稍长，远远比并发标记阶段时间短。主要用到了三色标记里的增量更新算法做重新标记</p>
<h3 id="并发清理"><a href="#并发清理" class="headerlink" title="并发清理"></a>并发清理</h3><p>开启用户线程，同时GC线程开始对未标记的区域做清扫。这个阶段如果有新增对象会被标记为黑色不做任何处理</p>
<h3 id="并发重置"><a href="#并发重置" class="headerlink" title="并发重置"></a>并发重置</h3><p>重置本次GC过程中的标记数据</p>
<h2 id="G1收集器"><a href="#G1收集器" class="headerlink" title="G1收集器"></a>G1收集器</h2><p>G1将Java堆划分为多个大小相等的独立区域(Region),JVM最多可以有2048个Region</p>
<p>一般Region大小等于堆大小除以2048，比如堆大小为4096M，则Region大小为2M，当然也可以用参数”-XX:G1HeapRegionSize”手动指定Region大小，但是推荐默认的计算方式</p>
<p>G1保留了年轻代和老年代的概念，但不再是屋里隔阂了，它们都是(可以不连续)Region的集合。</p>
<p>默认年轻代对堆内存的占比是5%，如果堆大小为4096M，那么年轻代占据200MB左右的内存，对应大概是100个Region，可以通过“-XX:G1NewSizePercent”设置新生代初始占比，在系统运行中，JVM会不停的给年轻代增加更多的Region，但是最多新生代的占比不会超过60%，可以通过”-XX:G1MaxNewSizePercent”调整.年轻代中的Eden和Survivor对应的region也跟之前一样，默认8:1:1,假设年轻代现在有1000个region，eden区对应800个,s0对应100个,s1对应100个</p>
<p>G1垃圾收集器对于对象什么时候会转移到老年代跟之前讲过的原则一样，<strong>唯一不同的是对大对象的处理</strong>，G1有专门分配大对象的Reigon叫Humongous区，而不是让大对象直接进入到老年代的Region中。</p>
<p>在G1中，大对象的判定规则就是一个大对象超过了一个Region大小的50%，比如按照上面算的，每个Region是2M，只要一个大对象超过了1M，就会被放<br>入Humongous中，而且一个大对象如果太大，可能会横跨多个Region来存放</p>
<p><img src="/images/posts/jvm_gc_info/img-23.png" alt="JVM垃圾回收"></p>
<p><img src="/images/posts/jvm_gc_info/img-24.png" alt="JVM垃圾回收"></p>
<h3 id="初始标记-1"><a href="#初始标记-1" class="headerlink" title="初始标记"></a>初始标记</h3><p>暂停所有的其他线程，并记录下gc roots直接能引用的对象，速度很快</p>
<h3 id="并发标记-1"><a href="#并发标记-1" class="headerlink" title="并发标记"></a>并发标记</h3><p>同CMS的并发标记</p>
<h3 id="最终标记"><a href="#最终标记" class="headerlink" title="最终标记"></a>最终标记</h3><p>对用户线程做短暂暂停，处理并发阶段结束后仍遗留下来的少量 SATB 记录</p>
<p>为了修正在并发标记期间因用户程序继续运作而导致标记产生变动的那一部分标记记录，虚拟机将这 段时间对象变化记录在线程的 Remembered Set Logs 里面，最终标记阶段需要把 Remembered Set Logs 的数据合并到 Remembered Set 中。这阶段需要停顿线程，但是可并行执行。 （儿子找爸爸就比较好找， 如果不是G1的话，儿子找爸爸可能会白找，找了半天根本没人指向它）</p>
<h3 id="筛选回收"><a href="#筛选回收" class="headerlink" title="筛选回收"></a>筛选回收</h3><p>对各 Region 的回收价值<a href="https://www.notion.so/c9f7a5cd5c3a43a28ab9b11fa74dbc05?v=0a184c51c60b4ea980d3808644935289&p=ea7ee928726847a28b26a8120746543f&pm=s">排序</a>，根据用户期望停顿时间制定回收计划。必须暂停用户线程，由多条收集线程并行完成</p>
<h2 id="G1缺点"><a href="#G1缺点" class="headerlink" title="G1缺点"></a>G1缺点</h2><p>浪费内存空间(TAMS)：因为每一块区域里面 总有5-15%的空间（Rset）记录有哪些区域的引用指向我（方便反向扫描） - 空间换时间</p>
<h2 id="RSet-记忆跨代引用的集合"><a href="#RSet-记忆跨代引用的集合" class="headerlink" title="RSet(记忆跨代引用的集合)"></a>RSet(记忆跨代引用的集合)</h2><p>存跨代引用(谁指向了我，CSet是划分区域). G1有多少个Eden区，就有多少个，CMS只有一个</p>
<p>有了RSet，标记就不需要全盘扫描，只扫描扫对象以及引用的region集合就可以了</p>
<p><img src="/images/posts/jvm_gc_info/img-25.png" alt="JVM垃圾回收"></p>
<h1 id="ZGC"><a href="#ZGC" class="headerlink" title="ZGC"></a>ZGC</h1><p>垃圾能不能回收，关键看他的引用; 染色指针，内存不超过4TB;短暂的STW,只与GC ROOT大小有关，与堆空间大小无关，所以能保存不管多大的空间都能保证很小的STW时间</p>
<p>特点：不随内存的增大而增加扫描时间(因为根本不扫描内存，之前的GC都在内存堆上处理)</p>
<p>标记在对象头上，<br>2的42次方 4T内存左右<br>32和64位是什么意思：能够访问到最大的地址是多少<br>Object  o  &#x3D; new Object<br>比如o是2位的， 他可以通过 00 01 10 11 4种方式访问，  2的32位就是 4个G， 64位就是18446744073709551616<br>ZGC支持最大的内存数  2的48次方 16T</p>
<p><img src="/images/posts/jvm_gc_info/img-26.png" alt="JVM垃圾回收"></p>
<h1 id="七种垃圾收集器比较"><a href="#七种垃圾收集器比较" class="headerlink" title="七种垃圾收集器比较"></a>七种垃圾收集器比较</h1><p><img src="/images/posts/jvm_gc_info/img-27.png" alt="JVM垃圾回收"></p>
<h1 id="Full-GC触发条件"><a href="#Full-GC触发条件" class="headerlink" title="Full GC触发条件"></a>Full GC触发条件</h1><p>对于 Minor GC，其触发条件非常简单，当 Eden 区空间满时，就将触发一次 Minor GC。而 Full GC 则相对复杂,如果要触发fullGC,则</p>
<details><summary>调用 System.gc()</summary>

</details>

<details><summary>老年代空间不足</summary>

</details>

<details><summary>空间分配担保失败</summary>

</details>

<details><summary>JDK 1.7 及以前的永久代空间不足</summary>

</details>

<details><summary>Concurrent Mode Failure</summary>

</details>

<h1 id="Minor-GC、Major-GC、FULL-GC、mixed-gc"><a href="#Minor-GC、Major-GC、FULL-GC、mixed-gc" class="headerlink" title="Minor GC、Major GC、FULL GC、mixed gc"></a><strong>Minor GC、Major GC、FULL GC、mixed gc</strong></h1><h2 id="1-Minor-GC"><a href="#1-Minor-GC" class="headerlink" title="1 Minor GC"></a>1 Minor GC</h2><p>在年轻代<code>Young space</code>(包括Eden区和Survivor区)中的垃圾回收称之为 Minor GC,Minor GC只会清理年轻代</p>
<h2 id="2-Major-GC"><a href="#2-Major-GC" class="headerlink" title="2 Major GC"></a>2 Major GC</h2><p>Major GC清理老年代(old GC)，但是通常也可以指和Full GC是等价，因为收集老年代的时候往往也会伴随着升级年轻代，收集整个Java堆。所以有人问的时候需问清楚它指的是full GC还是old GC</p>
<h2 id="3-Full-GC"><a href="#3-Full-GC" class="headerlink" title="3 Full GC"></a>3 Full GC</h2><p>full gc是对新生代、老年代、永久代【jdk1.8后没有这个概念了】统一的回收。</p>
<p>【知乎R大的回答:收集整个堆，包括young gen、old gen、perm gen（如果存在的话)、元空间(1.8及以上)等所有部分的模式】。</p>
<h2 id="4-mixed-GC【g1特有】"><a href="#4-mixed-GC【g1特有】" class="headerlink" title="4 mixed GC【g1特有】"></a>4 mixed GC【g1特有】</h2><p>混合GC</p>
<p>收集整个young gen以及部分old gen的GC。只有G1有这个模式</p>
<h1 id="GC是怎么判断对象是被标记的"><a href="#GC是怎么判断对象是被标记的" class="headerlink" title="GC是怎么判断对象是被标记的"></a>GC是怎么判断对象是被标记的</h1><p>通过枚举根节点的方式，通过jvm提供的一种oopMap的数据结构，简单来说就是不要再通过去遍历内存里的东西，而是通过OOPMap的数据结构去记录该记录的信息,比如说它可以不用去遍历整个栈，而是扫描栈上面引用的信息并记录下来。</p>
<p>总结:通过OOPMap把栈上代表引用的位置全部记录下来，避免全栈扫描，加快枚举根节点的速度，除此之外还有一个极为重要的作用，可以帮HotSpot实现准确式GC【这边的准确关键就是类型，可以根据给定位置的某块数据知道它的准确类型，HotSpot是通过oopMap外部记录下这些信息，存成映射表一样的东西】</p>
<h1 id="📔什么时候触发GC"><a href="#📔什么时候触发GC" class="headerlink" title="📔什么时候触发GC"></a><strong>📔什么时候触发GC</strong></h1><p>minor GC(young GC):当年轻代中eden区分配满的时候触发[值得一提的是因为young GC后部分存活的对象会已到老年代(比如对象熬过15轮)，所以过后old gen的占用量通常会变高]</p>
<p>Full GC:</p>
<p>①手动调用System.gc()方法 [增加了full GC频率，不建议使用而是让jvm自己管理内存，可以设置-XX:+ DisableExplicitGC来禁止RMI调用System.gc]</p>
<p>②发现perm gen（如果存在永久代的话)需分配空间但已经没有足够空间</p>
<p>③老年代空间不足，比如说新生代的大对象大数组晋升到老年代就可能导致老年代空间不足。</p>
<p>④CMS GC时出现Promotion Faield</p>
<p>⑤统计得到的Minor GC晋升到旧生代的平均大小大于老年代的剩余空间。</p>
<p>这个比较难理解，这是HotSpot为了避免由于新生代晋升到老年代导致老年代空间不足而触发的FUll GC。</p>
<p>比如程序第一次触发Minor GC后，有5m的对象晋升到老年代，姑且现在平均算5m，那么下次Minor GC发生时，先判断现在老年代剩余空间大小是否超过5m，如果小于5m，则HotSpot则会触发full GC(这点挺智能的)</p>
<h1 id="📔为什么minor-gc-比full-gc-快？"><a href="#📔为什么minor-gc-比full-gc-快？" class="headerlink" title="📔为什么minor gc 比full gc 快？"></a><strong>📔为什么minor gc 比full gc 快？</strong></h1><p>minor gc 只针对 young 区， full gc 针对所有区，包括young gen、old gen、perm gen.<br>minor gc 和 full gc 都是从 gc root 开始扫描的.<br>minor gc root 是指：当前线程stack+ Dirty cards.<br>full gc root 是指：当前线程stack+ Perm Gen .<br>full gc 扫描stack 时候会递归扫描整个所有对象以及他们引用，是全量扫描。<br>minor gc 扫描对象时候和full gc 类似，只不过当遍历的对象是old 区的对象就停止进一步遍历了，这样就就跳过了所有old 对象扫描，扫描数量大大减少(只是young 区的)，但这样会产生一个问题：<br>就是如果young 区的某个对象只被old 应用，那么该对象就扫描不到成了需要被回收的。这时候Dirty cards 排上了用场，dirty card 里面记录了old 区所有引用了young 区的对象，所以扫描<br>一次dirty card 问题就解决了。</p>
<p>总结一下：就是minor gc 需要扫描的对象很少，扫描后需要复制有效对象也很少，所以速度很快。full gc 需要做全量扫描标记清除，很耗时</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://www.cnblogs.com/lzmrex/articles/12887198.html">https://www.cnblogs.com/lzmrex/articles/12887198.html</a></li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM的JIT优化</title>
    <url>//article/jvm_jit/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>JVM（Java虚拟机）中的JIT（即时编译器）是一种优化技术，它将字节码实时编译成本地机器码，以提高Java应用程序的执行性能</p>
</blockquote>
<h1 id="📝-逃逸分析"><a href="#📝-逃逸分析" class="headerlink" title="📝 逃逸分析"></a>📝 逃逸分析</h1><p><strong>逃逸分析并不是直接的优化手段</strong>，而是一个代码分析，通过动态分析对象的作用域，为其它优化手段如栈上分配、标量替换和同步消除等提供依据，发生逃逸行为的情况有两种：方法逃逸和线程逃逸</p>
<ol>
<li><strong>方法逃逸</strong>：当一个对象在方法中定义之后，作为参数传递到其它方法中；</li>
<li><strong>线程逃逸</strong>：如类变量或实例变量，可能被其它线程访问到；</li>
</ol>
<details><summary>如果**不存在**逃逸行为，则可以对该对象进行如下优化：</summary>

</details>

<p><img src="/images/posts/jvm_jit/img-1.png" alt="JVM的JIT优化"></p>
<h1 id="📔标量替换"><a href="#📔标量替换" class="headerlink" title="📔标量替换"></a><strong>📔标量替换</strong></h1><ol>
<li>标量是指不可分割的量，如java中基本数据类型和reference类型，相对的一个数据可以继续分解，称为聚合量；</li>
<li>如果把一个对象拆散，将其成员变量恢复到基本类型来访问就叫做标量替换；</li>
<li>如果逃逸分析发现一个对象不会被外部访问，并且该对象可以被拆散，那么经过优化之后，并不直接生成该对象，而是在栈上创建若干个成员变量；</li>
<li>通过<code>-XX:+EliminateAllocations</code>可以开启标量替换， </li>
<li><code>-XX:+PrintEliminateAllocations</code>查看标量替换情况;</li>
</ol>
<p>本来是对象.name, 对象.age, 直接转化成 String name , int age, 标量替换,不需要去new对象了</p>
<p><img src="/images/posts/jvm_jit/img-2.png" alt="JVM的JIT优化"></p>
<h1 id="📔栈上分配"><a href="#📔栈上分配" class="headerlink" title="📔栈上分配"></a><strong>📔栈上分配</strong></h1><p>在栈上分配对象，其实目前Hotspot并没有实现真正意义上的栈上分配，实际上是标量替换</p>
<p><img src="/images/posts/jvm_jit/img-3.png" alt="JVM的JIT优化"></p>
<p>User对象的作用域局限在方法fn中，可以使用标量替换的优化手段在栈上分配对象的成员变量，这样就不会生成User对象，大大减轻GC的压力</p>
<p>分层编译和逃逸分析在1.8中是默认是开启的，例子中fn方法被执行了200w次，按理说应该在Java堆生成200w个User对象,实际堆中产生的对象没有200W个, 但是还是会产生一部分数据,栈上分配不能绝对100%分配</p>
<p><img src="/images/posts/jvm_jit/img-4.png" alt="JVM的JIT优化"></p>
<h1 id="📔同步消除"><a href="#📔同步消除" class="headerlink" title="📔同步消除"></a><strong>📔同步消除</strong></h1><p>线程同步本身比较消费性能(要抢锁,释放锁,期间还可能发生上下文切换问题)，如果确定一个对象不会逃逸出线程，无法被其它线程访问到，那该对象的读写就不会存在竞争，则可以消除对该对象的同步锁，通过<code>-XX:+EliminateLocks</code>可以开启同步消除(也就是锁消除,清除了管程,不需要管程来控制并发问题)</p>
<h1 id="📔内联优化（方法内联）"><a href="#📔内联优化（方法内联）" class="headerlink" title="📔内联优化（方法内联）"></a><strong>📔内联优化（方法内联）</strong></h1><p>内联就是把目标方法的代码复制到调用方法之中, <strong>就不用新生成一个栈帧做多余的损耗了</strong></p>
<p>将热点方法的代码直接插入到调用处，避免了方法调用的开销，提供了执行速度</p>
<p><code>addNumbers</code> 方法调用了 <code>multiplyNumbers</code> 方法，而 <code>multiplyNumbers</code> 方法只是简单地执行了乘法运算。当 JIT 编译器优化代码时，它可以选择将 <code>multiplyNumbers</code> 方法内联到 <code>addNumbers</code> 方法中。这样，调用 <code>addNumbers</code> 方法的地方就直接执行了乘法运算，而不再需要额外的方法调用开销</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MethodInliningExample</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> addNumbers(<span class="number">5</span>, <span class="number">10</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;Result: &quot;</span> + result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">addNumbers</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> multiplyNumbers(a, b);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">multiplyNumbers</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> x * y;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="📔循环优化"><a href="#📔循环优化" class="headerlink" title="📔循环优化"></a><strong>📔循环优化</strong></h1><p>JIT会对循环进行优化，如循环展开（Loop Unrolling）、循环变量消除（Loop Variable Elimination）和循环不变代码外提（Loop-Invariant Code Motion），以减少循环的开销和提高执行效率</p>
<h1 id="📔内联缓存"><a href="#📔内联缓存" class="headerlink" title="📔内联缓存"></a><strong>📔内联缓存</strong></h1><details><summary>动态调用和静态调用的区别</summary>

</details>

<p>内联缓存通过缓存动态调用的方法地址，将动态调用转换为静态调用，从而避免了重复的查找和分派操作，提高了方法的执行效率</p>
<p>具体展开内联缓存的流程如下：</p>
<ol>
<li>初始阶段：在第一次动态调用时，JVM会记录下方法的接收者类型和对应的方法地址，将其存储在内联缓存中。</li>
<li>缓存命中：在后续的动态调用中，如果接收者类型与之前缓存的类型匹配，那么就可以直接从内联缓存中获取对应的方法地址，进行静态调用。</li>
<li>缓存未命中：如果接收者类型与内联缓存中存储的类型不匹配，就需要进行查找和分派操作，以确定方法的地址。在此过程中，JVM会更新内联缓存，将新的接收者类型和方法地址存储在缓存中，以便下次调用时使用。</li>
</ol>
<details><summary>如果动态调用的类型多样性较高或者存在频繁的类型变化，内联缓存的效果可能会降低。</summary>

</details>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>思考</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM内存结构</title>
    <url>//article/jvm_memory_structure/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-主旨"><a href="#📝-主旨" class="headerlink" title="📝 主旨"></a>📝 主旨</h1><p><img src="/images/posts/jvm_memory_structure/img-1.png" alt="JVM内存结构"></p>
<p><img src="/images/posts/jvm_memory_structure/img-2.png" alt="JVM内存结构"></p>
<h1 id="📔-程序计数器"><a href="#📔-程序计数器" class="headerlink" title="📔 程序计数器"></a><strong>📔 程序计数器</strong></h1><ul>
<li><strong>唯一一个无OOM的地方,类比操作系统中的 PC</strong></li>
<li><strong>记录正在执行的虚拟机字节码指令的行号</strong>（如果正在执行的是本地方法则为空）</li>
<li>CPU是轮转分片执行的, 为了防止CPU切换的时候返回忘记之前执行到哪一步,需要程序计数器来记录走到具体哪一行指令了(<strong>依赖指令前的序号定位</strong>)</li>
<li>class文件会看到一串增序的数字就是指令执行的顺序,这一串增序可能不是连续的,比如下面1-3之间的2消失了,因为机器本身要执行其他的操作,这部分操作是不希望你看到的,可能和安全有关系就没展示了.(计算机硬件封装的操作没必要全部给你展示)</li>
</ul>
<p><strong>为什么被设定为私有的</strong></p>
<p>因为CPU需要不停的切换各个线程，这时候切换回来以后，就得知道接着从哪开始继续执行,如果是共享的话,就混乱了不好辨别</p>
<p><img src="/images/posts/jvm_memory_structure/img-3.png" alt="JVM内存结构"></p>
<h1 id="📔-Java-虚拟机栈"><a href="#📔-Java-虚拟机栈" class="headerlink" title="📔 Java 虚拟机栈"></a><strong>📔 Java 虚拟机栈</strong></h1><p><strong>栈是解决程序的运行问题，即程序如何执行，或者说程序如何处理数据  (顺序是: 先进后出)</strong></p>
<ul>
<li>方法在执行的同时会创建一个栈帧用于存储局部变量表、操作数栈、动态链接、方法出口等信息。每一个方法从调用直至执行完成的过程，就对应着一个栈帧在 Java 虚拟机栈中入栈和出栈的过程</li>
<li>入栈表示被调用，出栈表示执行完毕或者返回异常</li>
<li>当前CPU调度的那个线程叫做活动线程；一个栈帧对应一个方法，活动线程的虚拟机栈里最顶部的栈帧代表了当前正在执行的方法，<strong>而这个栈帧也被叫做 当前栈帧,(也就是正在执行的方法在栈顶)</strong></li>
<li>可以通过 <strong>Xss</strong> 这个虚拟机参数来指定一个程序的 Java 虚拟机栈内存大小java -Xss&#x3D;512M HackTheJava</li>
</ul>
<ol>
<li>当线程请求的栈深度超过最大值，会抛出 StackOverflowError 异常； </li>
<li>栈进行动态扩展时如果无法申请到足够内存，会抛出 OutOfMemoryError 异常 </li>
<li><strong>基本数据类型</strong>变量和对象的引用都是在栈分配的</li>
<li>默认大小是<strong>1M</strong></li>
</ol>
<p><strong>Java 只有值传递,  引用类型也是值传递,只不过值是引用地址</strong></p>
<p><img src="/images/posts/jvm_memory_structure/img-4.png" alt="JVM内存结构"></p>
<h2 id="局部变量表"><a href="#局部变量表" class="headerlink" title="局部变量表"></a>局部变量表</h2><ul>
<li><strong>存的是对象的引用, 或者基本数据类型</strong></li>
<li>一片连续的内存空间，用来存放方法参数，以及方法内定义的局部变量，存放着编译期间已知的数据类型(<strong>八大基本类型和对象引用(reference类型</strong>)</li>
<li>它的最小的局部变量表空间单位为Slot，虚拟机没有指明Slot的大小，但在jvm中，long和double类型数据明确规定为64位，这两个类型占2个Slot，其它基本类型固定占用1个Slot</li>
<li>局部变量表所需要的内存空间在编译期完成分配，当进入一个方法时，这个方法在栈中需要分配多大的局部变量空间是完全确定的，在方法运行期间不会改变局部变量表大小</li>
</ul>
<p><strong>相当于OS 中的cache</strong></p>
<p>class文件如下:</p>
<p><img src="/images/posts/jvm_memory_structure/img-5.png" alt="JVM内存结构"></p>
<p><img src="/images/posts/jvm_memory_structure/img-6.png" alt="JVM内存结构"></p>
<h2 id=""><a href="#" class="headerlink" title=""></a></h2><h2 id="操作数栈"><a href="#操作数栈" class="headerlink" title="操作数栈"></a>操作数栈</h2><p><strong>主要用于保存计算过程的中间结果，同时作为计算过程中变量临时的存储空间</strong></p>
<ul>
<li><p>是执行引擎的工作区,(存储中间态的数据),执行引擎不保存数据,数据需要有地方放.就放操作数栈, <strong>相当于OS 中的寄存器</strong></p>
</li>
<li><p>如下代码</p>
<p>将1,2,10 压入操作数栈,执行引擎计算x+y,也就是(1+2)得到3,再取出10,与3计算得到30,将30压入操作数栈,随后取出 赋值给变量Z</p>
</li>
</ul>
<p><img src="/images/posts/jvm_memory_structure/img-7.png" alt="JVM内存结构"></p>
<h3 id="栈顶缓存技术"><a href="#栈顶缓存技术" class="headerlink" title="栈顶缓存技术"></a>栈顶缓存技术</h3><p>我们的执行引擎执行命令，以及我们的局部变量表存储对象，都需要通过操作数栈来实现，<strong>只会和操作数栈交互</strong><strong>，<strong>明白了这点，就知道</strong>提升操作数栈的运行效率有多么的重要</strong>，因此引入了栈顶缓存技术</p>
<ol>
<li>前面提过，基于栈式架构的虚拟机所使用的零地址指令更加紧凑，但完成一项操作的时候必然需要使用更多的入栈和出栈指令，这同时也就意味着将需要更多的指令分派（instruction dispatch）次数（也就是你会发现指令很多）和导致内存读&#x2F;写次数多，效率不高。</li>
<li>由于操作数是存储在内存中的，因此频繁地执行内存读&#x2F;写操作必然会影响执行速度。为了解决这个问题，HotSpot JVM的设计者们提出了栈顶缓存（Tos，Top-of-Stack Cashing）技术，<strong>将栈顶元素全部缓存在物理CPU的寄存器中，以此降低对内存的读&#x2F;写次数，提升执行引擎的执行效率</strong><strong>。(栈顶就是当前运行的方法)</strong></li>
<li>寄存器的主要优点：指令更少，执行速度快，但是指令集（也就是指令种类）很多</li>
</ol>
<h2 id="动态链接"><a href="#动态链接" class="headerlink" title="动态链接"></a>动态链接</h2><p>可以理解为直接引用，每个栈帧包含一个指向运行时常量池中该栈帧所属方法引用，持有这个引用是为了支持方法调用过程中的动态链接</p>
<p>为了支持方法的动态的调用过程(java是多态的,调用的时候可能才知道具体是设谁在调用,方法可能是重写的);</p>
<p>给到我们的方法一个<strong>确定</strong>的值，符号引用转变成直接引用</p>
<p>A-B B多态 C&#x2F;D，加载过程是不知道B是调用C还是D的， 要实际调用的时候才知道</p>
<p><img src="/images/posts/jvm_memory_structure/img-8.png" alt="JVM内存结构"></p>
<h2 id="方法返回地址"><a href="#方法返回地址" class="headerlink" title="方法返回地址"></a>方法返回地址</h2><p>方法退出后都返回到该方法被调用的位置。方法正常退出时，</p>
<p><strong>调用者的pc计数器的值作为返回地址，即调用该方法的指令的下一条指令的地址。</strong></p>
<p><strong>而通过异常退出的，返回地址是要通过异常表来确定</strong></p>
<p>本质上，方法的退出就是当前栈帧出栈的过程。此时，需要恢复上层方法的局部变量表、操作数栈、将返回值压入调用者栈帧的操作数栈、设置PC寄存器值等，让调用者方法继续执行下去</p>
<ul>
<li>正常退出 - 程序计数器的行号</li>
<li>异常退出 - <strong>异常处理表</strong> try catch</li>
</ul>
<h3 id="异常处理表"><a href="#异常处理表" class="headerlink" title="异常处理表"></a>异常处理表</h3><ul>
<li>反编译字节码文件，可得到 Exception table</li>
<li>from ：字节码指令起始地址</li>
<li>to ：字节码指令结束地址</li>
<li>target ：出现异常跳转至地址为 11 的指令执行</li>
<li>type ：捕获异常的类型</li>
</ul>
<p><img src="/images/posts/jvm_memory_structure/img-9.png" alt="JVM内存结构"></p>
<h2 id="方法调用"><a href="#方法调用" class="headerlink" title="方法调用"></a>方法调用</h2><p>Java 的方法的执行分为两个部分：</p>
<ul>
<li>方法调用：确定被调用的方法是哪一个；</li>
<li>基于栈的解释执行：真正的执行方法的字节码。</li>
</ul>
<h3 id="调用解析"><a href="#调用解析" class="headerlink" title="调用解析"></a>调用解析</h3><p>  解析调用，也叫静态解析，就是 <strong>在类加载的解析阶段，就确定了方法的调用版本</strong> 。我们知道类加载的解析阶段会将一部分符号引用转化为直接引用，这一过程就叫做解析调用。因为是在程序真正运行前就确定了要调用哪一个方法，所以 <strong>解析调用能成立的前提就是：方法在程序真正运行前就有一个明确的调用版本了，并且这个调用版本不会在运行期发生改变。</strong></p>
<h2 id="分派调用"><a href="#分派调用" class="headerlink" title="分派调用"></a>分派调用</h2><p>分派调用,也叫动态解析，我们先来介绍一下 Java 所具备的面向对象的 3 个基本特征：封装，继承，多态。</p>
<p>其中多态最基本的体现就是重载和重写了，重载和重写的一个重要特征就是方法名相同，其他各种不同：</p>
<ul>
<li>重载：发生在同一个类中，入参必须不同，返回类型、访问修饰符、抛出的异常都可以不同；</li>
<li>重写：发生在子父类中，入参和返回类型必须相同，访问修饰符大于等于被重写的方法，不能抛出新的异常。</li>
</ul>
<p>相同的方法名实际上给虚拟机的调用带来了困惑，因为虚拟机需要判断，它到底应该调用哪个方法，而这个过程会在分派调用中体现出来。其中：</p>
<ul>
<li>方法重载 —— 静态分派</li>
<li>方法重写 —— 动态分派</li>
</ul>
<h3 id="静态分派（方法重载）"><a href="#静态分派（方法重载）" class="headerlink" title="静态分派（方法重载）"></a><strong>静态分派（方法重载）</strong></h3><p>通俗的讲，静态分派就是通过方法的参数（类型 &amp; 个数 &amp; 顺序）这种静态的东西来判断到底调用哪个方法的过程。</p>
<h3 id="动态分派（方法重写）"><a href="#动态分派（方法重写）" class="headerlink" title="动态分派（方法重写）"></a><strong>动态分派（方法重写）</strong></h3><p>动态分派就是在运行时，根据实际类型确定方法执行版本的分派过程。</p>
<p>动态分派在虚拟机种执行的非常频繁，而且方法查找的过程要在类的方法元数据中搜索合适的目标，从性能上考虑，不太可能进行如此频繁的搜索，需要进行性能上的优化。</p>
<p>**常用优化手段：**在类的方法区中建立一个虚方法表</p>
<ul>
<li>虚方法表中存放着各个方法的实际入口地址，如果某个方法没有被子类方法重写，那子类方法表中该方法的入口地址 &#x3D; 父类方法表中该方法的入口地址；</li>
<li>使用这个方法表索引代替在元数据中查找；</li>
<li>该方法表会在类加载的连接阶段初始化好。</li>
</ul>
<p><strong>通俗的讲，动态分派就是通过方法的接收者这种动态的东西来判断到底调用哪个方法的过程。</strong></p>
<h2 id="栈帧复用优化技术"><a href="#栈帧复用优化技术" class="headerlink" title="栈帧复用优化技术"></a>栈帧复用优化技术</h2><p>如果存在一个方法调用另一个方法,局部变量表的参数会共用一部分,减少空间的使用,<strong>复用数据</strong></p>
<p><img src="/images/posts/jvm_memory_structure/img-10.png" alt="JVM内存结构"></p>
<h1 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a>本地方法栈</h1><p>本地方法不是用 Java 实现，C++做的,本地方法栈就是给本地native方法使用的,没有程序计数器,因为执行引擎改不到(不是java写的方法),所以单独搞一片空间<strong>给native方法用</strong></p>
<h1 id="堆空间"><a href="#堆空间" class="headerlink" title="堆空间"></a>堆空间</h1><p><strong>堆解决的问题是数据存储的问题，即数据怎么放，数据放哪儿的问题。</strong></p>
<ul>
<li>大部分对象实例都在 **堆 **分配内存。</li>
<li>是垃圾收集的主要区域，现代的垃圾收集器基本都是采用分代收集算法，该算法的思想是针对不同的对 象采取不同的垃圾回收算法，因此虚拟机把 Java 堆分成以下2块</li>
</ul>
<ol>
<li><strong>新生代（Young Generation）</strong></li>
<li><strong>老年代（Old Generation）</strong></li>
</ol>
<p>当一个对象被创建时，它首先进入新生代，之后有可能被转移到老年代中。<strong>新生代存放着大量的生命很短的对象，因此新生代在三个区域中垃圾回收的频率最高。为了更高效地进行垃圾回收，把新生代继续划分成以下三个空间</strong><strong>：</strong></p>
<ol>
<li>Eden 先分配到eden区</li>
<li>Survivor 每次垃圾回收,会将eden区的存活对象放到S0中,</li>
<li>To Survivor 每次垃圾回收,会将S0区的存活对象通过复制算法放到S1中,然后清空另外一个S区</li>
</ol>
<p>可以通过  <strong>-Xms</strong>  和  <strong>-Xmx</strong>**  **两个虚拟机参数来指定一个程序的 Java 堆内存大小，第一个参数设置初始值，第二个参 数设置最大值 ,<strong>一般设置最初始值和最大值相等, 这样JVM就不用多次更改大小消耗性能</strong></p>
<p>java -Xms&#x3D;1M -Xmx&#x3D;2M</p>
<p>虚拟机分配的总内存是电脑内存的1&#x2F;4，初始化的内存是电脑内存的1&#x2F;64</p>
<p><img src="/images/posts/jvm_memory_structure/img-11.png" alt="JVM内存结构"></p>
<p>🤖<strong>为什么要有2个S区,如果只有一个的话, 第二次清理之后,对象要么直接进入老年代, 要么不放入老年代,</strong></p>
<ul>
<li><strong>放入, 由于大部分对象是朝生夕死,直接进入老年代代价太大,</strong></li>
<li><strong>不放入,S区就会有垃圾碎片的情况,两个S区就可以用复制算法,把对象复制copy整理到另一个S区</strong></li>
</ul>
<p>🤖<strong>为什么要分代</strong></p>
<p>优化GC的性能，如果没有分代的话，所有的对象都在一起，在GC的时候，就会对整堆进行全局扫描，然后很多对象都是朝生夕死的;如果分代的话，就把这些对象聚集在一起,GC先回收这部分，就会节省很多空间</p>
<h1 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h1><ul>
<li>用于存放已被加载的<strong>类信息</strong>(方法信息)、<strong>常量</strong>、<strong>静态变量</strong>、即时编译器编译后的代码等数据 (CGLIB的class文件)</li>
<li>和 Java 堆一样不需要连续的内存，并且可以动态扩展，动态扩展失败一样会抛出 OutOfMemoryError 异常。 对这块区域进行垃圾回收的主要目标是对常量池的回收和对类的卸载，但是一般比较难实现，HotSpot 虚拟机把它当 成永久代来进行垃圾回收</li>
</ul>
<h3 id="HotSpot方法区的变化"><a href="#HotSpot方法区的变化" class="headerlink" title="HotSpot方法区的变化"></a><strong>HotSpot方法区的变化</strong></h3><ul>
<li>JDK1.6之前 用永久代（permanent generation） 静态变量存放在永久代上</li>
<li>JDK1.7 有永久代，但是已经逐步“去永久代”，字符串常量池、静态变量移除，保存在堆中</li>
<li>JDK1.8之后 无永久代。类型信息、字段、方法、常量保存在本地内存的元空间，但是字符串常量池、静态变量仍在堆</li>
</ul>
<h1 id="常量池"><a href="#常量池" class="headerlink" title="常量池"></a>常量池</h1><h2 id="静态常量池"><a href="#静态常量池" class="headerlink" title="静态常量池"></a>静态常量池</h2><p>加载的时候,把class里面的常量池加载进方法区<br>字面量(String a &#x3D;”啊”, “啊”就是字面量)<br>符号引用(方法A的名字)等信息,(编译的时候可能方法A还没被分配空间,只能先用符号引用代替,后面再根据符号引用去找真正的内存地址)</p>
<h2 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h2><p>直接引用(运行的时候才有直接引用)</p>
<h2 id="字符串常量池"><a href="#字符串常量池" class="headerlink" title="字符串常量池"></a>字符串常量池</h2><p>String 类的 intern()。这部分常量也会被放入运行时常量池。<br>字符串常量池的重心是要解决String是使用效率问题；因为String用的太多了</p>
<h1 id="元空间"><a href="#元空间" class="headerlink" title="元空间"></a>元空间</h1><ul>
<li>JDK1.8之后,将方法区的实现永久代,更改为元空间</li>
<li>方法区存储的数据是类相关,静态变量,常量相关信息,设置大小比较难控制,因为多了会占空整个JVM其他区域的比重,少了又会报出OOM影响程序</li>
<li>JDK1.8之后,方法区改成元空间,元空间直接使用本地内存,(OS的内存),不占用JVM其他区域资源,但是OS内存不够的时候也会报OOM</li>
<li>元空间使用的是本地内存,所以垃圾回收的效率也更快了,不需要再扫描元空间这一片区域.减少垃圾回收的范围</li>
</ul>
<p><strong>正常来说 256M就够了.参考编译后的target文件大小</strong></p>
<h1 id="各数据处于JVM内部的位置"><a href="#各数据处于JVM内部的位置" class="headerlink" title="各数据处于JVM内部的位置"></a>各数据处于JVM内部的位置</h1><p><img src="/images/posts/jvm_memory_structure/img-12.png" alt="JVM内存结构"></p>
<h1 id="一个对象在JVM分布的情况"><a href="#一个对象在JVM分布的情况" class="headerlink" title="一个对象在JVM分布的情况"></a>一个对象在JVM分布的情况</h1><p><img src="/images/posts/jvm_memory_structure/img-13.png" alt="JVM内存结构"></p>
<h1 id="对象的分配"><a href="#对象的分配" class="headerlink" title="对象的分配"></a>对象的分配</h1><p><img src="/images/posts/jvm_memory_structure/img-14.png" alt="JVM内存结构"></p>
<p>当java虚拟机遇到了一条字节码new指令时,首先会去检查这个指令的参数是否能在常量池中定位到一个类的符号引用,并且检查这个符号引用代表的类是否已被加载,解析和初始化.如果没有,就执行相应的类加载过程.<br>类加载检查通过后,<br>为对象分配空间的任务实际上便等同于把一块确定大小的内存快从java堆中划分出来.</p>
<ol>
<li>指针碰撞(Bump The Pointer):   假设java堆中内存是绝对规整的,所有被使用过的内存都被放在一边,空闲的内存被放在另一边,中间放着一个指针作为分界点的指示器,那所分配内存就仅仅是把那个指针向空闲空间方向挪动一段与对象大小相等的距离.</li>
<li>空闲列表(Free List): 如果java堆内存并不是规整的,已使用内存和空闲内存相互交错在一起,那就没有办法简单的进行指针碰撞. 虚拟机需要维护一个列表,记录上那些内存块是可用的,在分配的时候,从列表中找到一块足够大的空间划分给对象实例,并更新列表上的记录.<br>当使用 serial,parNew等带压缩整理过程的收集器时,系统采用的分配算法是指针碰撞,既简单也高效</li>
</ol>
<p>当使用CMS这种基于清除(sweep)算法的收集器时,理论上只能采用<br>并发分配:<br>对象创建在虚拟机中是非常频繁的行为,即使仅仅修改一个指针所指向的位置,在并发的情况下也是不安全的.<br>方案一: 对分配内存空间的动作进行同步处理,实际上虚拟机采用的CAS;<br>另外方案是:   把内存分配的动作按照线程划分在不同的空间之中进行,即每个线程在java堆中预先分配一小快内存,成为本地线程分配缓冲(TLAB),那个线程要分配内存,就在哪个线程的本地缓冲区中分配,只有本地缓冲区用完了,分配新的缓冲区时才需要同步锁定. 虚拟机是否使用 TLAB,可以通过 -XX: +&#x2F;-UserTLAB参数来设定</p>
<h1 id="对象的访问方式"><a href="#对象的访问方式" class="headerlink" title="对象的访问方式"></a>对象的访问方式</h1><p>Object obj &#x3D; new Object();</p>
<p>Object obj 这部分的语义将会反映到Java 栈的本地变量表中，作为一个reference 类型数据出现</p>
<p>new Object() 这部分的语义将会反映到Java 堆中，形成一块存储了Object 类型所有实例数据值</p>
<p>reference 类型在Java 虚拟机规范里面只规定了一个指向对象的引用，并没有定义这个引用应该通过哪种方式去定位，以及访问到Java 堆中的对象的具体位置，因此不同虚拟机实现的对象访问方式会有所不同，主流的访问方式有两种：使用句柄和直接指针</p>
<h2 id="直接访问"><a href="#直接访问" class="headerlink" title="直接访问"></a>直接访问</h2><p><strong>指针-对象的引用地址 (Hotspot用的方式)</strong></p>
<p>优点:更有效率,直接指向地址,访问速度更快</p>
<p>缺点:如果我的对象消失了,我需要对引用做修改</p>
<h2 id="句柄访问"><a href="#句柄访问" class="headerlink" title="句柄访问"></a>句柄访问</h2><p>句柄-特殊的指针 , 由系统管理的标识,该标识可以重新被定位一个新的内存地址上</p>
<p>优点:使用句柄访问方式的最大好处就是reference 中存储的是稳定的句柄地址，在对象被移动（垃圾收集时移动对象是非常普遍的行为）时只会改变句柄中的实例数据指针，而reference 本身不需要被修改</p>
<p>缺点是:效率低,要先通过句柄池,再找具体的地址</p>
<p><strong>安全在哪:</strong></p>
<p>句柄是指向内存池的一片区域</p>
<p>可以理解为指针的指针</p>
<p>那既然内存池的区域已经分配了,</p>
<p>那么里面是不是为空,这个指针都不可能是空</p>
<p><img src="/images/posts/jvm_memory_structure/img-15.png" alt="JVM内存结构"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://www.processon.com/view/link/5eea141cf346fb1ae56a44e7">https://www.processon.com/view/link/5eea141cf346fb1ae56a44e7</a></li>
<li></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>ArrayDeque源码阅读记录</title>
    <url>/2021/11/03/Java/ArrayDeque%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>ArrayDeque 在我目前做的项目中,使用是比较少的,基本都没有地方用到。可能是我太low了,也可能是业务没有一定要用到队列的情况. 但是这不影响我们对其进行源码阅读。</p>
<h3 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h3><p>结构还是可以看到, 使用一个Object的数组, 二个int类型的变量来记录头和尾(从单词的意思)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">transient Object[] elements; // non-private to simplify nested class access</span><br><span class="line"></span><br><span class="line">transient int head;</span><br><span class="line"></span><br><span class="line">transient int tail;</span><br></pre></td></tr></table></figure>

<h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><ul>
<li><p>构造函数</p>
<p>无参构造函数. 可以看到无参构造函数,默认是对数据进行初始化大小为16的操作.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public ArrayDeque() &#123;</span><br><span class="line">    elements = new Object[16];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>有参构造函数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**传递int类型的构造函数,最后是调用到了calculateSize方法返回值来初始化数组大小 */</span><br><span class="line">public ArrayDeque(int numElements) &#123;</span><br><span class="line">    allocateElements(numElements);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">private void allocateElements(int numElements) &#123;</span><br><span class="line">        elements = new Object[calculateSize(numElements)];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/** 获取变量MIN_INITIAL_CAPACITY的值,如果传入进来的值是大于这个值,就会进行下面的运算操作,然后返回这个值出去.   */</span><br><span class="line">private static int calculateSize(int numElements) &#123;</span><br><span class="line">        int initialCapacity = MIN_INITIAL_CAPACITY;</span><br><span class="line">        // Find the best power of two to hold elements.</span><br><span class="line">        // Tests &quot;&lt;=&quot; because arrays aren&#x27;t kept full.</span><br><span class="line">        if (numElements &gt;= initialCapacity) &#123;</span><br><span class="line">            initialCapacity = numElements;</span><br><span class="line">            initialCapacity |= (initialCapacity &gt;&gt;&gt;  1);</span><br><span class="line">            initialCapacity |= (initialCapacity &gt;&gt;&gt;  2);</span><br><span class="line">            initialCapacity |= (initialCapacity &gt;&gt;&gt;  4);</span><br><span class="line">            initialCapacity |= (initialCapacity &gt;&gt;&gt;  8);</span><br><span class="line">            initialCapacity |= (initialCapacity &gt;&gt;&gt; 16);</span><br><span class="line">            initialCapacity++;</span><br><span class="line"></span><br><span class="line">            if (initialCapacity &lt; 0)   // Too many elements, must back off</span><br><span class="line">                initialCapacity &gt;&gt;&gt;= 1;// Good luck allocating 2 ^ 30 elements</span><br><span class="line">        &#125;</span><br><span class="line">        return initialCapacity;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">   传递集合的构造函数</span><br><span class="line">   使用传递进来的集合的长度来初始化数组的长度.</span><br><span class="line">   然后调用addAll方法,这里说明下 addAll 是在其 AbstractCollection 里面,也就是子类调用父类的方法.然后add方法是在ArrayDeque里面调用</span><br><span class="line">*/</span><br><span class="line">public ArrayDeque(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">        allocateElements(c.size());</span><br><span class="line">        addAll(c);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private void allocateElements(int numElements) &#123;</span><br><span class="line">        elements = new Object[calculateSize(numElements)];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/** 这里可以看到,定义给变量,迭代集合c,依次调用add方法,如果add方法返回的是true,变量modified就会变为true.最后addAll就会返回变量modified回去. */</span><br><span class="line">public boolean addAll(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">        boolean modified = false;</span><br><span class="line">        for (E e : c)</span><br><span class="line">            if (add(e))</span><br><span class="line">                modified = true;</span><br><span class="line">        return modified;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 往下调用</span><br><span class="line">public boolean add(E e) &#123;</span><br><span class="line">        addLast(e);</span><br><span class="line">        return true;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这里的值是不能传入null进来的,否则的话就会报NPE的异常.</span><br><span class="line">	然后使用下标tail直接插入到最后,if 里面是对 tail 的值进行新赋值操作,如果满足条件就会调用doubleCapacity方法,目测这个方法就是进行扩容的方法.</span><br><span class="line">	这里就是看下  (tail = (tail + 1) &amp; (elements.length - 1)) 这个赋值操作,就是给tail进行新的赋值。</span><br><span class="line">*/</span><br><span class="line">public void addLast(E e) &#123;</span><br><span class="line">        if (e == null)</span><br><span class="line">            throw new NullPointerException();</span><br><span class="line">        elements[tail] = e;</span><br><span class="line">        if ( (tail = (tail + 1) &amp; (elements.length - 1)) == head)</span><br><span class="line">            doubleCapacity();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加方法</p>
<p>add(E e) 这里添加调用的方法,我们主要看下 doubleCapacity 这个方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public boolean add(E e) &#123;</span><br><span class="line">    addLast(e);</span><br><span class="line">    return true;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public void addLast(E e) &#123;</span><br><span class="line">        if (e == null)</span><br><span class="line">            throw new NullPointerException();</span><br><span class="line">        elements[tail] = e;</span><br><span class="line">        if ( (tail = (tail + 1) &amp; (elements.length - 1)) == head)</span><br><span class="line">            doubleCapacity();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这里对 head / elements的长度都进行值存储操作, 一 是便于后面使用System.arraycopy 来进行copy数组的值,好从对应下标开始复制值. 二是 head /  tail 等赋值.</span><br><span class="line">	可以看到扩容后的的大小,来new了一个新的数组,后面调用System.arraycopy来进行复制.</span><br><span class="line">*/</span><br><span class="line">private void doubleCapacity() &#123;</span><br><span class="line">        assert head == tail;</span><br><span class="line">        int p = head;</span><br><span class="line">        int n = elements.length;</span><br><span class="line">        int r = n - p; // number of elements to the right of p</span><br><span class="line">        int newCapacity = n &lt;&lt; 1;</span><br><span class="line">        if (newCapacity &lt; 0)</span><br><span class="line">            throw new IllegalStateException(&quot;Sorry, deque too big&quot;);</span><br><span class="line">        Object[] a = new Object[newCapacity];</span><br><span class="line">        System.arraycopy(elements, p, a, 0, r);</span><br><span class="line">        System.arraycopy(elements, 0, a, r, p);</span><br><span class="line">        elements = a;</span><br><span class="line">        head = 0;</span><br><span class="line">        tail = n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>addFirst 从头部插入 , 可以看到 使用 head &#x3D; (head - 1) &amp; (elements.length - 1) 是计算出头部下标的位置,并且对值进行覆盖. 如果 head 与 tail 是相等的话,就会调用 doubleCapacity来进行扩容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public void addFirst(E e) &#123;</span><br><span class="line">    if (e == null)</span><br><span class="line">        throw new NullPointerException();</span><br><span class="line">    elements[head = (head - 1) &amp; (elements.length - 1)] = e;</span><br><span class="line">    if (head == tail)</span><br><span class="line">        doubleCapacity();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>addLast 尾部插入, 这个方法上面都是有提到的。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public void addLast(E e) &#123;</span><br><span class="line">    if (e == null)</span><br><span class="line">        throw new NullPointerException();</span><br><span class="line">    elements[tail] = e;</span><br><span class="line">    if ( (tail = (tail + 1) &amp; (elements.length - 1)) == head)</span><br><span class="line">        doubleCapacity();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">offerLast / offerFirst  内部都是分别调用到了 addLast / addFirst 方法</span><br></pre></td></tr></table></figure>

<p> push 方法也是调用的 addFirst 方法</p>
<ul>
<li><p>get 获取值方法</p>
<p>getFirst &#x2F; getLast 方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/** 从head对应的数组中直接获取出值,如果值是null的话,就会抛出一个异常,否则就会返回*/</span><br><span class="line">public E getFirst() &#123;</span><br><span class="line">    @SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="line">    E result = (E) elements[head];</span><br><span class="line">    if (result == null)</span><br><span class="line">        throw new NoSuchElementException();</span><br><span class="line">    return result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	(tail - 1) &amp; (elements.length - 1) 得出尾部元素的下标位置,然后用数组下标返回对应的值.</span><br><span class="line">*/</span><br><span class="line">public E getLast() &#123;</span><br><span class="line">        @SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="line">        E result = (E) elements[(tail - 1) &amp; (elements.length - 1)];</span><br><span class="line">        if (result == null)</span><br><span class="line">            throw new NoSuchElementException();</span><br><span class="line">        return result;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>peekFirst &#x2F; peekLast 中的操作,是与 getFirst &#x2F; getLast 是一样的</p>
<p>peek 方法里面是走的 peekFirst 方法</p>
<ul>
<li><p>remove 方法</p>
<p>removeFirst 方法, 走的是 pollFirst 方法</p>
<p>removeLast 方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public E removeFirst() &#123;</span><br><span class="line">    E x = pollFirst();</span><br><span class="line">    if (x == null)</span><br><span class="line">        throw new NoSuchElementException();</span><br><span class="line">    return x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> 头节点 head 使用变量 h 来记录, 直接elements[h]下标来获取值,如果值是null的话,就执行返回(这里直接返回的逻辑处理是,初始化一个集合,但是没任何值,就调用removeFirst方法,这个时候数组里面是没有值的,于是就直接返回即可).</span><br><span class="line"> 如果不是null的话,就会走下面的,将h的下标值设置为null,也就是进行删除,然后重新计算出 head 的值.</span><br><span class="line">*/</span><br><span class="line">public E pollFirst() &#123;</span><br><span class="line">        int h = head;</span><br><span class="line">        @SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="line">        E result = (E) elements[h];</span><br><span class="line">        // Element is null if deque empty</span><br><span class="line">        if (result == null)</span><br><span class="line">            return null;</span><br><span class="line">        elements[h] = null;     // Must null out slot</span><br><span class="line">        head = (h + 1) &amp; (elements.length - 1);</span><br><span class="line">        return result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">public E removeLast() &#123;</span><br><span class="line">        E x = pollLast();</span><br><span class="line">        if (x == null)</span><br><span class="line">            throw new NoSuchElementException();</span><br><span class="line">        return x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	先计算出尾节点的下标,然后用值result来进行记录.如果是null的话,就直接方法(这里想法和上面一样).将t的下标的值重置为null进行删除,然后tail的值就是等t的值.</span><br><span class="line">*/</span><br><span class="line">public E pollLast() &#123;</span><br><span class="line">        int t = (tail - 1) &amp; (elements.length - 1);</span><br><span class="line">        @SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="line">        E result = (E) elements[t];</span><br><span class="line">        if (result == null)</span><br><span class="line">            return null;</span><br><span class="line">        elements[t] = null;</span><br><span class="line">        tail = t;</span><br><span class="line">        return result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>remove 方法是直接调用的 removeFirst 方法.</p>
<ul>
<li>removeFirstOccurrence &#x2F; removeLastOccurrence TODO 后续更新</li>
</ul>
<hr>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>ArrayDeque 队列， 队列的特性就是对数据是先进先出。 而栈的特性是先进后出(比如枪打出去的子弹).</p>
<p>这里的理解就是, 使用二个变量，然后每次进行 add &#x2F; get &#x2F; remove 都是利用这二个变量来进行 添加 &#x2F; 删除 &#x2F; 获取等操作.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>java集合</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java集合</tag>
      </tags>
  </entry>
  <entry>
    <title>PriorityQueue源码阅读记录</title>
    <url>/2021/11/03/Java/PriorityQueue%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>PriorityQueue : 中文是优先队列 , 队列的特点就是数据 先进先出, 但是这个优先队列的特别是什么呢？ 首先肯定是有队列的基本特点，也就是有先进先出。 如果是先进先出的话,那么就和普通的有什么区别？优先二字又是体现在什么地方呢？ 优先级队列的元素按照其自然顺序进行排序, 或者根据 构造队列时提供的 Comparator 进行排序.</p>
<hr>
<h4 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h4><p> 结构就是 PriorityQueue这个类的 全局变量参数, 因为这些参数是存储数据的, 所以只要理解了这些参数,就明白了这个 PriorityQueue这个是对数据是怎么样进行存储的, 还是比较好理解的.</p>
<p>这里可以看到 priorityQueue的数据结构还是很简单的, 一眼扫过去没什么需要特别的理解</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 存储数据的数组</span><br><span class="line">transient Object[] queue;</span><br><span class="line"></span><br><span class="line">// 记录 priorityQueue的长度</span><br><span class="line">private int size = 0;</span><br><span class="line"></span><br><span class="line">// 这个就是之前说提到的  可以根据 Comparator 进行排序</span><br><span class="line">private final Comparator&lt;? super E&gt; comparator;</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h4><p> priorityQueue的构造方法相对于其他的集合的构造方法可能是比较多的.</p>
<p> 这里列举出来, 可以看到构造方法还是比较多的.</p>
<p> 对构造函数的初始化赋值等操作还是很好理解的,并没有什么特别难理解的。 主要还是对数组&#x2F;长度&#x2F;或者传入进来的数组进行赋值操作.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 1   </span><br><span class="line">// 这里是走到 4 的构造方法去了</span><br><span class="line">public PriorityQueue() &#123;</span><br><span class="line">    this(DEFAULT_INITIAL_CAPACITY, null);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 2</span><br><span class="line">// 这里是走到 4 的构造方法去了</span><br><span class="line">public PriorityQueue(int initialCapacity) &#123;</span><br><span class="line">        this(initialCapacity, null);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 3</span><br><span class="line">// 这里是走到 4 的构造方法去了</span><br><span class="line">public PriorityQueue(Comparator&lt;? super E&gt; comparator) &#123;</span><br><span class="line">        this(DEFAULT_INITIAL_CAPACITY, comparator);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 4 </span><br><span class="line">/**  可以看到前面的三个都是走到了这里来了,</span><br><span class="line">	 长度如果是小于1的话,就会报错.  </span><br><span class="line">     this.queue 的数组长度就是 initialCapacity</span><br><span class="line">     comparator 排序方法就是传入进来的</span><br><span class="line">*/</span><br><span class="line">public PriorityQueue(int initialCapacity,</span><br><span class="line">                         Comparator&lt;? super E&gt; comparator) &#123;</span><br><span class="line">        // Note: This restriction of at least one is not actually needed,</span><br><span class="line">        // but continues for 1.5 compatibility</span><br><span class="line">        if (initialCapacity &lt; 1)</span><br><span class="line">            throw new IllegalArgumentException();</span><br><span class="line">        this.queue = new Object[initialCapacity];</span><br><span class="line">        this.comparator = comparator;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 5</span><br><span class="line">/**  对传入进来的集合进行判断. 分为   SortedSet  或者  PriorityQueue  或者其他</span><br><span class="line">     如果是 SortedSet 的话, 对 comparator 的值赋值为 传入进来集合的排序方式,然后走 initElementsFromCollection() 方法, 这里应该是对集合进行赋值操作.</span><br><span class="line">     如果是 PriorityQueue , comparator 处理方式是和 SortedSet一样,然后走 initFromPriorityQueue 方法. </span><br><span class="line">     否则就不上面的二种, comparator 复置为 null ,走 initFromCollection 方法.</span><br><span class="line">     这里总结的话,就是传入进来不同的集合,走的方法也是不一样的,这个还是很好理解的.</span><br><span class="line">	</span><br><span class="line">*/</span><br><span class="line">public PriorityQueue(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">        if (c instanceof SortedSet&lt;?&gt;) &#123;</span><br><span class="line">            SortedSet&lt;? extends E&gt; ss = (SortedSet&lt;? extends E&gt;) c;</span><br><span class="line">            this.comparator = (Comparator&lt;? super E&gt;) ss.comparator();</span><br><span class="line">            initElementsFromCollection(ss);</span><br><span class="line">        &#125;</span><br><span class="line">        else if (c instanceof PriorityQueue&lt;?&gt;) &#123;</span><br><span class="line">            PriorityQueue&lt;? extends E&gt; pq = (PriorityQueue&lt;? extends E&gt;) c;</span><br><span class="line">            this.comparator = (Comparator&lt;? super E&gt;) pq.comparator();</span><br><span class="line">            initFromPriorityQueue(pq);</span><br><span class="line">        &#125;</span><br><span class="line">        else &#123;</span><br><span class="line">            this.comparator = null;</span><br><span class="line">            initFromCollection(c);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 6  这个对应上面的, 如果是传入进来 PriorityQueue 的处理方法</span><br><span class="line">public PriorityQueue(PriorityQueue&lt;? extends E&gt; c) &#123;</span><br><span class="line">        this.comparator = (Comparator&lt;? super E&gt;) c.comparator();</span><br><span class="line">        initFromPriorityQueue(c);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 7  这个也是对应上面的 SortedSet 处理方法</span><br><span class="line">public PriorityQueue(SortedSet&lt;? extends E&gt; c) &#123;</span><br><span class="line">        this.comparator = (Comparator&lt;? super E&gt;) c.comparator();</span><br><span class="line">        initElementsFromCollection(c);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">----------------------   华丽分割线   --------------------</span><br><span class="line">上面的if else 里面提到的走不同的方法,还是有必要取看看的. </span><br><span class="line">    </span><br><span class="line">/**</span><br><span class="line">  对传入进来是  PriorityQueue  进行处理, 先判断确认 class是PriorityQueue ,是的话,调用toArray() 将数组赋值给 queue , 并且长度也进行复置给size.</span><br><span class="line">  否则就走  initFromCollection 方法, 这个 if else 还是比较严谨的.进行多次判断处理</span><br><span class="line">*/    </span><br><span class="line">private void initFromPriorityQueue(PriorityQueue&lt;? extends E&gt; c) &#123;</span><br><span class="line">        if (c.getClass() == PriorityQueue.class) &#123;</span><br><span class="line">            this.queue = c.toArray();</span><br><span class="line">            this.size = c.size();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            initFromCollection(c);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;    </span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">   这个对传入进来的集合, 将值转化为 数组a (Object []), 如果 comparator不是null的话,就会根据comparator来进行排序. 也就是对a进行排序,并且这个的值不可以为null的,如果出现了null的话,就会有空指针的异常出现.</span><br><span class="line">   然后将数组a赋值给queue,长度也是调用 a.length 复置给size</span><br><span class="line">*/</span><br><span class="line">private void initElementsFromCollection(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">        Object[] a = c.toArray();</span><br><span class="line">        // If c.toArray incorrectly doesn&#x27;t return Object[], copy it.</span><br><span class="line">        if (a.getClass() != Object[].class)</span><br><span class="line">            a = Arrays.copyOf(a, a.length, Object[].class);</span><br><span class="line">        int len = a.length;</span><br><span class="line">        if (len == 1 || this.comparator != null)</span><br><span class="line">            for (int i = 0; i &lt; len; i++)</span><br><span class="line">                if (a[i] == null)</span><br><span class="line">                    throw new NullPointerException();</span><br><span class="line">        this.queue = a;</span><br><span class="line">        this.size = a.length;</span><br><span class="line">&#125;	</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> 可以看到这个方法是走了  initElementsFromCollection 这个方法, 然后再走 headify 方法</span><br><span class="line">*/</span><br><span class="line">private void initFromCollection(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">        initElementsFromCollection(c);</span><br><span class="line">        heapify();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h4><p> 添加元素方法 ：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public boolean add(E e) &#123;</span><br><span class="line">    return offer(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这里可以看到,如果值是null的话,就会抛出NPE的异常.</span><br><span class="line">	如果size的大小比 queue数组的长度还大的话,就会进行扩容.</span><br><span class="line">	然后size长度+1,如何i是0的话,就说明是第一个元素,不需要任何拍寻处理,直接赋值给第一个即可.</span><br><span class="line">	如果不是第一个的话,就会走siftUp方法</span><br><span class="line">*/</span><br><span class="line">public boolean offer(E e) &#123;</span><br><span class="line">        if (e == null)</span><br><span class="line">            throw new NullPointerException();</span><br><span class="line">        modCount++;</span><br><span class="line">        int i = size;</span><br><span class="line">        if (i &gt;= queue.length)</span><br><span class="line">            grow(i + 1);</span><br><span class="line">        size = i + 1;</span><br><span class="line">        if (i == 0)</span><br><span class="line">            queue[0] = e;</span><br><span class="line">        else</span><br><span class="line">            siftUp(i, e);</span><br><span class="line">        return true;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	如果 comparator是null的话,就走  siftUpUsingComparator 方法.</span><br><span class="line">	否则就会走 siftUpComparable 方法</span><br><span class="line">*/</span><br><span class="line">private void siftUp(int k, E x) &#123;</span><br><span class="line">        if (comparator != null)</span><br><span class="line">            siftUpUsingComparator(k, x);</span><br><span class="line">        else</span><br><span class="line">            siftUpComparable(k, x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这里先判断k是大于0的,也就是不是第一个的意思.</span><br><span class="line">	然后通过 (k - 1) &gt;&gt;&gt; 1 计算出来下标位置,下标是parent的值,调用 comparator.compare(x,e)来进行比较,如果是大于0的话,就不需要做任何处理。</span><br><span class="line">	否则的话,就会 queue[k] = e ; k = parent; 来进行下标数值的替换处理.</span><br><span class="line">	最后queue[k] = x 的值</span><br><span class="line">*/</span><br><span class="line">private void siftUpUsingComparator(int k, E x) &#123;</span><br><span class="line">        while (k &gt; 0) &#123;</span><br><span class="line">            int parent = (k - 1) &gt;&gt;&gt; 1;</span><br><span class="line">            Object e = queue[parent];</span><br><span class="line">            if (comparator.compare(x, (E) e) &gt;= 0)</span><br><span class="line">                break;</span><br><span class="line">            queue[k] = e;</span><br><span class="line">            k = parent;</span><br><span class="line">        &#125;</span><br><span class="line">        queue[k] = x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这个方法其实也是和上面的处理方式是类似的,通过比较值来进行处理.</span><br><span class="line">*/</span><br><span class="line">private void siftUpComparable(int k, E x) &#123;</span><br><span class="line">        Comparable&lt;? super E&gt; key = (Comparable&lt;? super E&gt;) x;</span><br><span class="line">        while (k &gt; 0) &#123;</span><br><span class="line">            int parent = (k - 1) &gt;&gt;&gt; 1;</span><br><span class="line">            Object e = queue[parent];</span><br><span class="line">            if (key.compareTo((E) e) &gt;= 0)</span><br><span class="line">                break;</span><br><span class="line">            queue[k] = e;</span><br><span class="line">            k = parent;</span><br><span class="line">        &#125;</span><br><span class="line">        queue[k] = key;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	这里顺路看下 grow 扩容方法吧。 </span><br><span class="line">	肯定是根据 queue的长度来进行扩容,如果值太小了的话,就会进行 二倍扩容.  否则的话,就是1.5倍扩容.</span><br><span class="line">	最后调用 Arrays.copyOf() 来进行扩容数组操作</span><br><span class="line">	这个扩容还是想对比较简单的</span><br><span class="line">*/</span><br><span class="line">private void grow(int minCapacity) &#123;</span><br><span class="line">        int oldCapacity = queue.length;</span><br><span class="line">        // Double size if small; else grow by 50%</span><br><span class="line">        int newCapacity = oldCapacity + ((oldCapacity &lt; 64) ?</span><br><span class="line">                                         (oldCapacity + 2) :</span><br><span class="line">                                         (oldCapacity &gt;&gt; 1));</span><br><span class="line">        // overflow-conscious code</span><br><span class="line">        if (newCapacity - MAX_ARRAY_SIZE &gt; 0)</span><br><span class="line">            newCapacity = hugeCapacity(minCapacity);</span><br><span class="line">        queue = Arrays.copyOf(queue, newCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>peek 方法</p>
<p>这里可以看清楚的看到，出队列方法的值,就是默认的第一个嘛，这么一眼看下去就是很清楚明了的.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public E peek() &#123;</span><br><span class="line">    return (size == 0) ? null : (E) queue[0];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>remove 方法 :</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line">	删除元素的方法。</span><br><span class="line">	indexOf 如果返回的不是-1的话,就说明是有值得,就会走到 removeAt 方法</span><br><span class="line">*/</span><br><span class="line">public boolean remove(Object o) &#123;</span><br><span class="line">    int i = indexOf(o);</span><br><span class="line">    if (i == -1)</span><br><span class="line">        return false;</span><br><span class="line">    else &#123;</span><br><span class="line">        removeAt(i);</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	indexOf 这个方法就是判断在这个集合里面有没有 o 这个值, 如果有的话就会返回对应的下标,如果不存在的话,就会返回-1的值</span><br><span class="line">*/</span><br><span class="line">private int indexOf(Object o) &#123;</span><br><span class="line">        if (o != null) &#123;</span><br><span class="line">            for (int i = 0; i &lt; size; i++)</span><br><span class="line">                if (o.equals(queue[i]))</span><br><span class="line">                    return i;</span><br><span class="line">        &#125;</span><br><span class="line">        return -1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	removeAt 方法就会将传入进来的i的下标的值重置为null,这是满足 i == --size 的情况下.</span><br><span class="line">	然后会将要删除的下标 i 和 对应的值 moved 传入到 siftDown 这个方法中.</span><br><span class="line">	siftUp()是在前面有讲解到的.</span><br><span class="line">	这里还是很明显的看到, priorityQueue是一直在维护这排序的关系。</span><br><span class="line">*/</span><br><span class="line">private E removeAt(int i) &#123;</span><br><span class="line">        // assert i &gt;= 0 &amp;&amp; i &lt; size;</span><br><span class="line">        modCount++;</span><br><span class="line">        int s = --size;</span><br><span class="line">        if (s == i) // removed last element</span><br><span class="line">            queue[i] = null;</span><br><span class="line">        else &#123;</span><br><span class="line">            E moved = (E) queue[s];</span><br><span class="line">            queue[s] = null;</span><br><span class="line">            siftDown(i, moved);</span><br><span class="line">            if (queue[i] == moved) &#123;</span><br><span class="line">                siftUp(i, moved);</span><br><span class="line">                if (queue[i] != moved)</span><br><span class="line">                    return moved;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return null;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">	根据comparator走不同的方法</span><br><span class="line">	可以看到走的二个方法，其中的区别是 comparator.compare 和 comparator.compareTo 调用的api是不一样的</span><br><span class="line">*/</span><br><span class="line">private void siftDown(int k, E x) &#123;</span><br><span class="line">        if (comparator != null)</span><br><span class="line">            siftDownUsingComparator(k, x);</span><br><span class="line">        else</span><br><span class="line">            siftDownComparable(k, x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private void siftDownUsingComparator(int k, E x) &#123;</span><br><span class="line">        int half = size &gt;&gt;&gt; 1;</span><br><span class="line">        while (k &lt; half) &#123;</span><br><span class="line">            int child = (k &lt;&lt; 1) + 1;</span><br><span class="line">            Object c = queue[child];</span><br><span class="line">            int right = child + 1;</span><br><span class="line">            if (right &lt; size &amp;&amp;</span><br><span class="line">                comparator.compare((E) c, (E) queue[right]) &gt; 0)</span><br><span class="line">                c = queue[child = right];</span><br><span class="line">            if (comparator.compare(x, (E) c) &lt;= 0)</span><br><span class="line">                break;</span><br><span class="line">            queue[k] = c;</span><br><span class="line">            k = child;</span><br><span class="line">        &#125;</span><br><span class="line">        queue[k] = x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">private void siftDownComparable(int k, E x) &#123;</span><br><span class="line">        Comparable&lt;? super E&gt; key = (Comparable&lt;? super E&gt;)x;</span><br><span class="line">        int half = size &gt;&gt;&gt; 1;        // loop while a non-leaf</span><br><span class="line">        while (k &lt; half) &#123;</span><br><span class="line">            int child = (k &lt;&lt; 1) + 1; // assume left child is least</span><br><span class="line">            Object c = queue[child];</span><br><span class="line">            int right = child + 1;</span><br><span class="line">            if (right &lt; size &amp;&amp;</span><br><span class="line">                ((Comparable&lt;? super E&gt;) c).compareTo((E) queue[right]) &gt; 0)</span><br><span class="line">                c = queue[child = right];</span><br><span class="line">            if (key.compareTo((E) c) &lt;= 0)</span><br><span class="line">                break;</span><br><span class="line">            queue[k] = c;</span><br><span class="line">            k = child;</span><br><span class="line">        &#125;</span><br><span class="line">        queue[k] = key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p> PriorityQueue 的存储数据结构是采用一个数据来进行存储,也就是一直在操作这个数组，只是每次都对数据进行了维护排序的关系。</p>
<p> PriorityQueue 是线程不安全的队列，这里还是提一下吧,因为添加元素和删除元素的方法都是没有进行加锁处理，当然了,如果不使用作为全局变量的话，自然是没有任何问题的,在局部变量里面.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>java集合</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java集合</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot动态添加接口</title>
    <url>/2021/11/03/Java/SpringBoot%E5%8A%A8%E6%80%81%E6%B7%BB%E5%8A%A0%E6%8E%A5%E5%8F%A3/</url>
    <content><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>最近看了公司的产品,有一个这样的动态,就是根据input输入的内容,进行动态接口的添加.</p>
<p>比如 我在没有添加之前,访问 <a href="http://localhost:8080/test">http://localhost:8080/test</a> 该地址是404, 于是通过input的输入创建,就相当于动态添加了一个接口,于是就可以访问了.</p>
<p>实现的方式有很多种, 具体的想要的效果肯定是需要根据自己的业务角度来.</p>
<h2 id="实现思路"><a href="#实现思路" class="headerlink" title="实现思路"></a>实现思路</h2><p>这里是说下个人的实现思路 :</p>
<ul>
<li>方案一 : 先定义好类,然后使用反射根据定义的类,创建好一个对象出来,然后将这个对象给注入到Spring容器中来,这里不仅仅是注入进来这么简单,还是需要经过 MVC(也就是Controller等注解的匹配操作)来实现</li>
<li>方案二 : 基于请求404的拦截来现实. 比如你新添加的接口访问肯定是404,于是我们可以定义一个拦截,然后获取出请求的路径,根据提前定好的一些设置,进行逻辑的处理.</li>
</ul>
<p>这里肯定还会有很多好的实现思路,并且基于每个框架都是不一样的,这里更多的是基于 SpringBoot框架来实现这个思路的.</p>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><h3 id="基于反射创建bean注入代码实现"><a href="#基于反射创建bean注入代码实现" class="headerlink" title="基于反射创建bean注入代码实现"></a>基于反射创建bean注入代码实现</h3><p>定义一个类的模板</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">String</span> <span class="variable">templateValue</span> <span class="operator">=</span> <span class="string">&quot;import org.springframework.web.bind.annotation.GetMapping;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;import org.springframework.web.bind.annotation.RequestMapping;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;import org.springframework.web.bind.annotation.RestController;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;@RestController\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;@RequestMapping(\&quot;/test\&quot;)\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;public class TestController &#123;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;    @GetMapping(\&quot;/test\&quot;)\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;    public String test()&#123;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;        return \&quot;测试Test接口\&quot;;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;    &#125;\n&quot;</span> +</span><br><span class="line">        <span class="string">&quot;&#125;&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>提供一个接口来反射创建对象并且注入到spring容器中来:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;/create&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">createTemplate</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">        Map&lt;String, <span class="type">byte</span>[]&gt; bytecode = DynamicLoader.compile(<span class="string">&quot;TestController.java&quot;</span>, templateValue);</span><br><span class="line">        <span class="type">MemoryClassLoader</span> <span class="variable">classLoader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MemoryClassLoader</span>(bytecode);</span><br><span class="line">        <span class="type">Class</span> <span class="variable">clazz</span> <span class="operator">=</span> classLoader.loadClass(<span class="string">&quot;TestController&quot;</span>);</span><br><span class="line">        <span class="type">Object</span> <span class="variable">object</span> <span class="operator">=</span> clazz.newInstance();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 注入 bean 容器的代码 : 容器中是存在这个 bean 对象的,但是Controller却没有访问到.</span></span><br><span class="line">        <span class="type">BeanDefinitionBuilder</span> <span class="variable">builder</span> <span class="operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(object.getClass());</span><br><span class="line">        <span class="type">ConfigurableApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> springUtils.getContext();</span><br><span class="line">        <span class="type">DefaultListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> (DefaultListableBeanFactory) context.getBeanFactory();</span><br><span class="line">        beanFactory.registerBeanDefinition(<span class="string">&quot;testController&quot;</span>,builder.getBeanDefinition());</span><br><span class="line"></span><br><span class="line">        <span class="type">RequestMappingHandlerMapping</span> <span class="variable">mappingHandlerMapping</span> <span class="operator">=</span> context.getBean(RequestMappingHandlerMapping.class);</span><br><span class="line">        <span class="type">Object</span> <span class="variable">oj</span> <span class="operator">=</span> context.getBean(<span class="string">&quot;testController&quot;</span>);</span><br><span class="line">        Map&lt;Method, RequestMappingInfo&gt; methods = MethodIntrospector.selectMethods(oj.getClass(),(MethodIntrospector.MetadataLookup&lt;RequestMappingInfo&gt;) method -&gt;&#123;</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                <span class="type">RequestMapping</span> <span class="variable">requestMapping</span> <span class="operator">=</span> AnnotatedElementUtils.findMergedAnnotation(method, RequestMapping.class);</span><br><span class="line">                RequestMappingInfo.<span class="type">Builder</span> <span class="variable">mappping</span> <span class="operator">=</span> RequestMappingInfo.paths(requestMapping.path())</span><br><span class="line">                                  .methods(requestMapping.method())</span><br><span class="line">                                  .params(requestMapping.params())</span><br><span class="line">                                  .headers(requestMapping.headers())</span><br><span class="line">                                  .consumes(requestMapping.consumes())</span><br><span class="line">                                  .produces(requestMapping.produces())</span><br><span class="line">                                  .mappingName(requestMapping.name());</span><br><span class="line">                <span class="keyword">return</span> mappping.build();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e)&#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="type">Method</span> <span class="variable">rmhmMethod</span> <span class="operator">=</span> mappingHandlerMapping.getClass()</span><br><span class="line">                            .getDeclaredMethod(<span class="string">&quot;registerHandlerMethod&quot;</span>,<span class="keyword">new</span> <span class="title class_">Class</span>[]&#123;Object.class, Method.class, Object.class&#125;);</span><br><span class="line"></span><br><span class="line">        rmhmMethod.setAccessible(<span class="literal">true</span>);</span><br><span class="line">        methods.forEach((method,mapping) -&gt; &#123;</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                rmhmMethod.invoke(mappingHandlerMapping,<span class="keyword">new</span> <span class="title class_">Object</span>[]&#123;oj,method,mapping&#125; );</span><br><span class="line">            &#125;<span class="keyword">catch</span> (Exception e)&#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>Spring工具类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpringUtils</span> <span class="keyword">implements</span> <span class="title class_">ApplicationContextAware</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> ApplicationContext context;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setApplicationContext</span><span class="params">(ApplicationContext applicationContext)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        context = applicationContext;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> ConfigurableApplicationContext <span class="title function_">getContext</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> (ConfigurableApplicationContext)context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setContext</span><span class="params">(ApplicationContext context)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.context = context;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>到此,就可以动态的创建出一个接口来了。</p>
<p>当然了,这里的代码都是写固定在代码里面的,可以提供 template 或者 通过页面定义给添加进来, 然后调用 反射&#x2F;注入到Spring容器中等操作即可.</p>
<h3 id="基于-404-拦截请求"><a href="#基于-404-拦截请求" class="headerlink" title="基于 404 拦截请求"></a>基于 404 拦截请求</h3><p>这是 SpringBoot 本来就有的 404 拦截实现, 如果不做什么处理的话,那么进入到这里来的url地址就会是&#x2F;error,是无法满足实现的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SelfErrorController</span> <span class="keyword">implements</span> <span class="title class_">ErrorController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="type">String</span> <span class="variable">ERROR_PATH</span> <span class="operator">=</span> <span class="string">&quot;/error&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Supports the HTML Error View</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> request</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@RequestMapping(value = ERROR_PATH)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">errorHtml</span><span class="params">(HttpServletRequest request)</span> &#123;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">statusCode</span> <span class="operator">=</span> (Integer) request.getAttribute(<span class="string">&quot;javax.servlet.error.status_code&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">requestURI</span> <span class="operator">=</span> request.getRequestURI();</span><br><span class="line">        log.info(<span class="string">&quot;在SelfErrorController中请求的路径 : &#123;&#125; &quot;</span> ,requestURI);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 拿到路径后就可以执行相应的代码逻辑.</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">realUrlName</span> <span class="operator">=</span> request.getAttribute(<span class="string">&quot;realName&quot;</span>).toString();</span><br><span class="line">        log.info(<span class="string">&quot;在SelfErrorController中真实存在的请求路径是 : --&gt; &#123;&#125; &quot;</span> , realUrlName);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(statusCode == <span class="number">401</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span>  <span class="string">&quot;&#123; \&quot;code\&quot;: \&quot;401\&quot;&#125;&quot;</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(statusCode == <span class="number">404</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;&#123; \&quot;code\&quot;: \&quot;404\&quot;&#125;&quot;</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(statusCode == <span class="number">403</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;&#123; \&quot;code\&quot;: \&quot;403\&quot;&#125;&quot;</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;&#123; \&quot;code\&quot;: \&quot;500\&quot;&#125;&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getErrorPath</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为上面无法满足的前提下,所以我们可以使用拦截来配置原来的路径.</p>
<p>配置类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyWebAppConfigurer</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> &#123;</span><br><span class="line">        registry.addInterceptor(<span class="keyword">new</span> <span class="title class_">SelfInterceptor</span>()).addPathPatterns(<span class="string">&quot;/**&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>自定义拦截器:</p>
<p>这里可以看到将原有的路径给set字段realName了.</p>
<p>所以在上面的error接口,我们调用这个realName字段就可以获取到了原有的路径.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SelfInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span><br><span class="line"><span class="params">                             Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 前置拦截器</span></span><br><span class="line">        log.info(<span class="string">&quot;前置拦截器调用 : com.iyang.hello.boot.config.SelfInterceptor.preHandle 中来.&quot;</span> );</span><br><span class="line">        <span class="type">String</span> <span class="variable">requestURI</span> <span class="operator">=</span> request.getRequestURI();</span><br><span class="line">        log.info(<span class="string">&quot;在preHandle方法中捕捉到的请求路径 : ---&gt; &#123;&#125; &quot;</span> , requestURI);</span><br><span class="line">        <span class="comment">// handler 是 ResourceHttpRequestHandler</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span><br><span class="line"><span class="params">                           Object handler, ModelAndView modelAndView)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">requestURI</span> <span class="operator">=</span> request.getRequestURI();</span><br><span class="line">        request.setAttribute(<span class="string">&quot;realName&quot;</span>,requestURI);</span><br><span class="line">        <span class="keyword">if</span>(response.getStatus() == <span class="number">404</span>)&#123;</span><br><span class="line">            log.info(<span class="string">&quot;状态是404正常操作&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        log.info(<span class="string">&quot;请求的url路径是 ---&gt; &#123;&#125; &quot;</span> , requestURI);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span><br><span class="line"><span class="params">                                Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>该技能看到比较有意思,具体的实现和设计思路其实是有很多种的,具体得看项目的业务是不是需要使用.</p>
<p>并且实现的思路并不是只有这一种,肯定是还有很多种的.</p>
<p>适合自己的业务是最好的.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>springBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>springBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>ArrayList源码分析</title>
    <url>//article/arrayList_code/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-ArrayList源码分析"><a href="#📝-ArrayList源码分析" class="headerlink" title="📝 ArrayList源码分析"></a>📝 <strong>ArrayList源码分析</strong></h1><p>ArrayList 是代码中使用非常频繁的,所以看底层的代码时非常有必须的.</p>
<h2 id="数据结构和参数"><a href="#数据结构和参数" class="headerlink" title="数据结构和参数"></a>数据结构和参数</h2><p>DEFAULT_CAPACITY : 默认集合大小;默认值是10</p>
<p>EMPTY_ELEMENTDATA : 空的Object数组,用于后续的初始化和赋值</p>
<p>DEFAULTCAPACTITY_EMPTY_ELEMENTDATA: 默认空的Object数组,也是用于后续的判断和赋值</p>
<p>elementData: 存放ArrayList数据的数组</p>
<p>size: 记录ArrayList中元素的个数;size不一定是elementData的长度</p>
<p> ArrayList 是一个由 Object [] 的数组来实现的;transient Object[] elementData ,这个变量就是存放数据的</p>
<p> 长度是用 size 这个变量来记录的,而不是直接调用的数组的长度</p>
<p> 如果ArrayList list &#x3D; new ArrayList(); 只是仅仅new一个集合的话,数组的大小是没有初始化为10的,而是在add()中,进行判断。 如果数组的是为空的数组的话,就会使用 <strong>DEFAULT_CAPACITY</strong> 来进行初始化。也就是要调用add方法才行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;</span><br><span class="line">    <span class="keyword">return</span> Math.max(DEFAULT_CAPACITY, minCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h2><h3 id="add方法"><a href="#add方法" class="headerlink" title="add方法"></a>add方法</h3><p>add 里面是走了三个方法, size 没有赋值的情况下,就是0</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">add</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    ensureCapacityInternal(size + <span class="number">1</span>);  <span class="comment">// Increments modCount!!</span></span><br><span class="line">    elementData[size++] = e;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 确认容量 , 打个比方我们没有对size进行赋值,那么size + 1 传入到这个里面的值也就是1,那么 elementData 对应的也就是一个空数组</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">ensureCapacityInternal</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">     ensureExplicitCapacity(calculateCapacity(elementData, minCapacity));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  满足是空数组的话,就会使用默认的值 10 于 minCapcacity 来进行对比,这里返回的10</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">calculateCapacity</span><span class="params">(Object[] elementData, <span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;</span><br><span class="line">            <span class="keyword">return</span> Math.max(DEFAULT_CAPACITY, minCapacity);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> minCapacity;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  如果 minCapacity  减去 数组的长度是大于0的,就会调用grow来进行扩容</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">ensureExplicitCapacity</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">        modCount++;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// overflow-conscious code</span></span><br><span class="line">        <span class="keyword">if</span> (minCapacity - elementData.length &gt; <span class="number">0</span>)</span><br><span class="line">            grow(minCapacity);</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里可以看到先对数组的值进行,然后对保存出来的值进行1.5倍扩容,与传入进来的值进行对比,满足条件赋值.这里就要看到 Arrays.copyOf(elementDate,newCapacity); 这才是真正的对数组进行扩容的方法,也就是直接调用Arrays的API. Arrays.copyOf() 里面最后也是调用了 System.arraycopy()的方法</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">grow</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">        <span class="comment">// overflow-conscious code</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">oldCapacity</span> <span class="operator">=</span> elementData.length;</span><br><span class="line">        <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> oldCapacity + (oldCapacity &gt;&gt; <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span> (newCapacity - minCapacity &lt; <span class="number">0</span>)</span><br><span class="line">            newCapacity = minCapacity;</span><br><span class="line">        <span class="keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="number">0</span>)</span><br><span class="line">            newCapacity = hugeCapacity(minCapacity);</span><br><span class="line">        <span class="comment">// minCapacity is usually close to size, so this is a win:</span></span><br><span class="line">        elementData = Arrays.copyOf(elementData, newCapacity);</span><br><span class="line">    &#125;</span><br><span class="line">到这里 ensureCapacityInternal 方法也就是走完了</span><br><span class="line">---------------------------------------------</span><br><span class="line">后面就是使用 数组下标来进行赋值并且返回<span class="literal">true</span>。</span><br></pre></td></tr></table></figure>

<p>所以add方法的总体流程如下</p>
<p><img src="/images/posts/arrayList_code/img-1.png" alt="ArrayList源码分析"></p>
<h3 id="add根据index添加"><a href="#add根据index添加" class="headerlink" title="add根据index添加"></a>add根据index添加</h3><p>在上述的add逻辑之前,进行index是否符合规则的校验</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> index, E element)</span> &#123;</span><br><span class="line">    rangeCheckForAdd(index);</span><br><span class="line"></span><br><span class="line">    ensureCapacityInternal(size + <span class="number">1</span>);  <span class="comment">// Increments modCount!!</span></span><br><span class="line">    System.arraycopy(elementData, index, elementData, index + <span class="number">1</span>,</span><br><span class="line">                     size - index);</span><br><span class="line">    elementData[index] = element;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查下标是否越界 , ensureCapacityInternal 方法和上面一样</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">rangeCheckForAdd</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (index &gt; size || index &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IndexOutOfBoundsException</span>(outOfBoundsMsg(index));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  System.arraycopy() 从 elementDate 的 index处开始复制, 复制给后面的elementDate数组的值,从index + 1 开始复制,也就是说 index 相当于修改了 index + 1, 然后index位置就是没有值了,所以elementDate[index] = element的值,size ++.</span></span><br></pre></td></tr></table></figure>

<p>根据下标添加元素的逻辑流程图</p>
<p><img src="/images/posts/arrayList_code/img-2.png" alt="ArrayList源码分析"></p>
<h3 id="set方法"><a href="#set方法" class="headerlink" title="set方法"></a>set方法</h3><p>根据下标来对久的值进行一种替换,取出对应下标的值,然后下标对应的位置赋值给新值,最后返回旧值回去即可</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先检查下标是否越界,如果越界就会抛出异常</span></span><br><span class="line"><span class="keyword">public</span> E <span class="title function_">set</span><span class="params">(<span class="type">int</span> index, E element)</span> &#123;</span><br><span class="line">    rangeCheck(index);</span><br><span class="line"></span><br><span class="line">    <span class="type">E</span> <span class="variable">oldValue</span> <span class="operator">=</span> elementData(index);</span><br><span class="line">    elementData[index] = element;</span><br><span class="line">    <span class="keyword">return</span> oldValue;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 取出对应下标的值</span></span><br><span class="line">    <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">    E <span class="title function_">elementData</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> (E) elementData[index];</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>set方法逻辑</p>
<p><img src="/images/posts/arrayList_code/img-3.png" alt="ArrayList源码分析"></p>
<h3 id="remove方法"><a href="#remove方法" class="headerlink" title="remove方法"></a>remove方法</h3><p>根据传入进来的值进行删除</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 分为 null 和 不是 null 的情况来进行删除.满足条件的话,最后都会调用到 fastRemove方法中来</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">remove</span><span class="params">(Object o)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (o == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> <span class="number">0</span>; index &lt; size; index++)</span><br><span class="line">            <span class="keyword">if</span> (elementData[index] == <span class="literal">null</span>) &#123;</span><br><span class="line">                fastRemove(index);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> <span class="number">0</span>; index &lt; size; index++)</span><br><span class="line">            <span class="keyword">if</span> (o.equals(elementData[index])) &#123;</span><br><span class="line">                fastRemove(index);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  根据传入进来的 下标来删除数据,System.arraycopy 这个方法并不默认,根据下标的位置来进行复制数组。</span></span><br><span class="line"><span class="comment">//  可以看到最后有一个 将值设置为null的操作,从注释上看是help GC, 帮助GC</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">fastRemove</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        modCount++;</span><br><span class="line">        <span class="type">int</span> <span class="variable">numMoved</span> <span class="operator">=</span> size - index - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (numMoved &gt; <span class="number">0</span>)</span><br><span class="line">            System.arraycopy(elementData, index+<span class="number">1</span>, elementData, index,</span><br><span class="line">                             numMoved);</span><br><span class="line">        elementData[--size] = <span class="literal">null</span>; <span class="comment">// clear to let GC do its work</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="remove根据下标删除"><a href="#remove根据下标删除" class="headerlink" title="remove根据下标删除"></a>remove根据下标删除</h3><p>可以看到根据下标删除的话，会先判断传入进来的下标是否满足条件,就是没有出现越界的情况</p>
<p>然后取出旧值,接下来的代码就是非常的熟悉了,就是fastRemove() 里面的代码了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">remove</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        rangeCheck(index);</span><br><span class="line"></span><br><span class="line">        modCount++;</span><br><span class="line">        <span class="type">E</span> <span class="variable">oldValue</span> <span class="operator">=</span> elementData(index);</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">numMoved</span> <span class="operator">=</span> size - index - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (numMoved &gt; <span class="number">0</span>)</span><br><span class="line">            System.arraycopy(elementData, index+<span class="number">1</span>, elementData, index,</span><br><span class="line">                             numMoved);</span><br><span class="line">        elementData[--size] = <span class="literal">null</span>; <span class="comment">// clear to let GC do its work</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> oldValue;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>根据下标移除元素逻辑</p>
<p><img src="/images/posts/arrayList_code/img-4.png" alt="ArrayList源码分析"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>大致就是看 ArrayList 是如何添加数据的,对数据是怎么保存的,是如何删除数据的,是怎么样进行扩容的,大致弄明白这些就是对ArrayList有一个大致的了解</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Class加载</title>
    <url>//article/classLoad/</url>
    <content><![CDATA[<blockquote>
<p>😀 class加载过程</p>
</blockquote>
<h1 id="📝-类加载"><a href="#📝-类加载" class="headerlink" title="📝 类加载"></a>📝 类加载</h1><h2 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a><strong>生命周期</strong></h2><ol>
<li>加载(Loading)</li>
<li>验证(Verification)</li>
<li>准备(Preparetion)</li>
<li>解析(Resolution)</li>
<li>初始化(Initialization)</li>
<li>使用(Using)</li>
<li>卸载(Unloading)</li>
</ol>
<p>其中 , 验证&#x2F;准备&#x2F;解析 三个部分统称为 连接(Linking)</p>
<h2 id="加载"><a href="#加载" class="headerlink" title="加载"></a><strong>加载</strong></h2><h3 id="何时加载"><a href="#何时加载" class="headerlink" title="何时加载"></a>何时加载</h3><ol>
<li>遇到 new , getstatic, putstatic 和 invokestatic 这四条字节码指令时,如果类型没有进行过初始化,则需要先触发其初始化阶段.</li>
</ol>
<ul>
<li>使用 new 关键字实例化对象</li>
<li>读取或者设置一个类型的静态字段(被final修饰,已在编译期把结果放入常量池的静态字段除外)的时候</li>
<li>调用一个类型的静态方法的时候</li>
</ul>
<ol>
<li>使用 java.lang.reflect包的时候对类型进行反射调用的时候,如果类型没有进行初始化,则需要触发其初始化</li>
<li>当初始化类的时候，如果发现其子类还没有进行初始化，则需要先触发其父类的初始化</li>
<li>当虚拟机启动时，用户需要指定一个启动类（main方法），虚拟机会先初始化这个主类</li>
<li>当使用JDK 7新加入的动态语言支持时，如果一个java.lang.invoke.MethodHandle实例最后的解析结果为REF_getStatic、REF_putStatic、REF_invokeStatic、REF_newInvokeSpecial四种类型的方法句柄，并且这个方法句柄对应的类没有进行过初始化，则需要先触发其初始化</li>
<li>当一个接口定义了default关键字修饰的接口方法时,如果有这个接口的实现类发生了初始化，那该接口要在其之前初始化</li>
</ol>
<p><strong>注意: 常量传播优化,讲静态常量直接存储在 使用类 的常量池中,实际都被转化为 类 对自身的常量池的引用。</strong></p>
<h3 id="案例说明"><a href="#案例说明" class="headerlink" title="案例说明"></a><strong>案例说明</strong></h3><p>比如我们经常写的一个类的main启动, 首先是需要通过类加载器把主类加载到JVM中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">class</span> <span class="title class_">ClassLoadMain</span> &#123;</span><br><span class="line"></span><br><span class="line">    public <span class="keyword">static</span> final <span class="title class_">String</span> name = <span class="string">&quot;BaoYang&quot;</span>;</span><br><span class="line">    public <span class="keyword">static</span> final int age = <span class="number">24</span>;</span><br><span class="line"></span><br><span class="line">    <span class="title class_">Teacher</span> teacher = <span class="keyword">new</span> <span class="title class_">Teacher</span>();</span><br><span class="line"></span><br><span class="line">    public int <span class="title function_">add</span>(<span class="params"></span>)&#123;</span><br><span class="line"></span><br><span class="line">        int a = <span class="number">1</span>, b =<span class="number">1</span> ;</span><br><span class="line">        int c = a + b;</span><br><span class="line">        <span class="title class_">System</span>.<span class="property">out</span>.<span class="title function_">println</span>(<span class="string">&quot;c的结果是: &quot;</span> + c);</span><br><span class="line">        <span class="keyword">return</span> c;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span>(<span class="params"><span class="built_in">String</span>[] args</span>) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="title class_">ClassLoadMain</span> classLoadMain = <span class="keyword">new</span> <span class="title class_">ClassLoadMain</span>();</span><br><span class="line">        classLoadMain.<span class="title function_">add</span>();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过执行流程如下</p>
<p><img src="/images/posts/classLoad/img-1.png" alt="Class加载"></p>
<p>其中 loadClass 的类加载过程有如下几步:</p>
<p>加载 &gt;&gt; 验证 &gt;&gt; 准备 &gt;&gt; 解析 &gt;&gt; 初始化 &gt;&gt; 使用 &gt;&gt; 卸载</p>
<ul>
<li>加载: 从磁盘上查找并通过io读入字节码,使用到类时才会加载，例如调用类的main()方法，new对象等等，在加载阶段会在内存中生成一个代表这个类的java.lang.class对象，作为方法区这个类的各种数据的访问入口。<ul>
<li>通过一个类的全限定名来获取定义此类的二进制字节流</li>
<li>将这个字节流所代表的静态存储结构转化为方法区的运行时数据结构</li>
<li>在内存中生成一个代表这个类 java.lang.Class 对象,作为方法区 这个类的各自数据的访问入口</li>
</ul>
</li>
<li>验证: 检验字节码文件的准确性<ol>
<li>文件格式验证</li>
</ol>
<ul>
<li>是否魔数 0xCAFEBABE开头</li>
<li>主次版本号是否在当前java虚拟机接受范围之内</li>
<li>常量池的常量中是否有不被支持的常量类型</li>
<li>指向常量的各种索引值中是否有指向不存在的常量或不符合类型的常量。</li>
<li>CONSTANT_Utf8_info型的常量中是否有不符合UTF-8编码的数据。</li>
<li>Class文件中各个部分及文件本身是否有被删除的或附加的其他信息。</li>
<li>等等。。。</li>
</ul>
<ol>
<li>元数据验证(字节码描述的信息进行语义分析)</li>
</ol>
<ul>
<li>这个类是否有父类(除了java.lang.Object之外,所有的类都应当有父类)</li>
<li>这个类的父类是否继承了不允许被继承的类（被final修饰的类）。</li>
<li>如果这个类不是抽象类，是否实现了其父类或接口之中要求实现的所有方法。</li>
<li>类中的字段、方法是否与父类产生矛盾（例如覆盖了父类的final字段，或者出现不符合规则的方法重载，例如方法参数都一致，但返回值类型却不同等）。</li>
<li>….等等</li>
</ul>
<ol>
<li>字节码验证(数据流分析和控制流分析,确定程序语义是合法的,逻辑是符合的)</li>
<li>符号引用验证(对类自身外（常量池中的各种符号引用）的各类信息进行匹配校验)</li>
</ol>
<ul>
<li>通过字符串中描述的全限定名是否能找到对应的类</li>
<li>指定类中是否存在符合方法的字段描述以及简单名称所描述的字段和方法</li>
<li>符号引用中的类、字段、方法的可访问性（private、protected、public、）是否可被当前类访问</li>
</ul>
</li>
<li>准备: 将类的静态变量分配内存,并赋予默认值</li>
<li>解析: 将符号的引用替换为直接引用,该阶段会把一些静态方法(符号引用,比如main方法)替换为指向数据所存内存的指针或句柄等(直接引用),这是所谓的静态链接过程(类加载期间完成),动态替换是在程序运行期间完成将符号引用替换为直接引用.</li>
<li>初始化: 对类的静态变量初始化为指定的值,执行静态代码块\</li>
</ul>
<p><img src="/images/posts/classLoad/img-2.png" alt="Class加载"></p>
<p>类被加载到方法区中后主要包括 运行时常量池,类型信息,字段信息,方法信息,类加载器的引用,对应class实例的引用信息.</p>
<p>类加载器引用: 这个类到类加载器实例的引用</p>
<p>对应class引用: 类加载器在加载类信息放到方法区中后,会创建一个对应的class类型的对象放到heap中,作为开发人员访问方法区中类定义的入口和切入点</p>
<p>注意点: 主类在运行过程中如果使用到了其他类，会逐步加载这些类，jar或者war包里的类不是一次性全部加载的,是使用到时才会加载</p>
<h2 id="类加载和双亲委派机制"><a href="#类加载和双亲委派机制" class="headerlink" title="类加载和双亲委派机制"></a><strong>类加载和双亲委派机制</strong></h2><ul>
<li>引导类加载器： 负责加载支撑jvm运行的位于jre的lib目录下的核心类库,比如 rt.jar , charset.jar等</li>
<li>扩展包加载器： 负责加载支撑jvm运行的位于jre的lib包目录下的ext扩展目录中的jar类包</li>
<li>应用程序类加载器: 负责加载classPath路径下的类包，主要就是加载自己写的那些类</li>
<li>自定义加载器： 负责加载用户自定义的路径下的类包</li>
</ul>
<h2 id="类加载初始化过程"><a href="#类加载初始化过程" class="headerlink" title="类加载初始化过程"></a><strong>类加载初始化过程</strong></h2><p>JVM会启动实例sun.misc.Launcher</p>
<p>Launcher采用单例模式,保证jvm虚拟机内存中只有一个 Launcher 实例</p>
<p>在 Launcher 构造方法内部, 创建了两个类加载器,分别是 ExtClassLoader(扩展类加载器) 和 AppClassLoader(应用类加载器)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">Launcher</span><span class="params">()</span> &#123;</span><br><span class="line">        Launcher.ExtClassLoader var1;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 构造扩展类加载器,构造的过程中使用(ClassLoader)null将父类的加载器设置为null</span></span><br><span class="line">            <span class="comment">// super(getExtURLs(var1), (ClassLoader)null, Launcher.factory);</span></span><br><span class="line">            var1 = Launcher.ExtClassLoader.getExtClassLoader();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException var10) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InternalError</span>(<span class="string">&quot;Could not create extension class loader&quot;</span>, var10);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 构造应用类加载器,在构造的过程中将其父类加载器设置为 ExtClassLoader</span></span><br><span class="line">            <span class="comment">// Launcher的loader属性值是 AppClassLoader,我们一般都是用这个类加载器来加载我们自己写的应用程序</span></span><br><span class="line">            <span class="built_in">this</span>.loader = Launcher.AppClassLoader.getAppClassLoader(var1);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException var9) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InternalError</span>(<span class="string">&quot;Could not create application class loader&quot;</span>, var9);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Thread.currentThread().setContextClassLoader(<span class="built_in">this</span>.loader);</span><br><span class="line">        <span class="type">String</span> <span class="variable">var2</span> <span class="operator">=</span> System.getProperty(<span class="string">&quot;java.security.manager&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (var2 != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="type">SecurityManager</span> <span class="variable">var3</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">if</span> (!<span class="string">&quot;&quot;</span>.equals(var2) &amp;&amp; !<span class="string">&quot;default&quot;</span>.equals(var2)) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    var3 = (SecurityManager)<span class="built_in">this</span>.loader.loadClass(var2).newInstance();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (IllegalAccessException var5) &#123;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InstantiationException var6) &#123;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (ClassNotFoundException var7) &#123;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (ClassCastException var8) &#123;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                var3 = <span class="keyword">new</span> <span class="title class_">SecurityManager</span>();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (var3 == <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InternalError</span>(<span class="string">&quot;Could not create SecurityManager: &quot;</span> + var2);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            System.setSecurityManager(var3);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="双亲委派"><a href="#双亲委派" class="headerlink" title="双亲委派"></a><strong>双亲委派</strong></h2><p><img src="/images/posts/classLoad/img-3.png" alt="Class加载"></p>
<p>加载某个类的时,会先委托父加载器寻找目标类,找不到再委托上层父类加载器加载，如果所有的父类加载器在自己的加载类路径下都找不到目标类，则在自己的类加载路径中查找并载入目标类</p>
<p>双亲委派: 先找父类,不行再有子类自加载</p>
<h3 id="AppClassLoader的loadCLass"><a href="#AppClassLoader的loadCLass" class="headerlink" title="AppClassLoader的loadCLass"></a><strong>AppClassLoader的loadCLass</strong></h3><p>AppClassLoader中的loadCLass</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Class&lt;?&gt; loadClass(String var1, <span class="type">boolean</span> var2) <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">var3</span> <span class="operator">=</span> var1.lastIndexOf(<span class="number">46</span>);</span><br><span class="line">            <span class="keyword">if</span> (var3 != -<span class="number">1</span>) &#123;</span><br><span class="line">                <span class="type">SecurityManager</span> <span class="variable">var4</span> <span class="operator">=</span> System.getSecurityManager();</span><br><span class="line">                <span class="keyword">if</span> (var4 != <span class="literal">null</span>) &#123;</span><br><span class="line">                    var4.checkPackageAccess(var1.substring(<span class="number">0</span>, var3));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">this</span>.ucp.knownToNotExist(var1)) &#123;</span><br><span class="line">                <span class="type">Class</span> <span class="variable">var5</span> <span class="operator">=</span> <span class="built_in">this</span>.findLoadedClass(var1);</span><br><span class="line">                <span class="keyword">if</span> (var5 != <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (var2) &#123;</span><br><span class="line">                        <span class="built_in">this</span>.resolveClass(var5);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">return</span> var5;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ClassNotFoundException</span>(var1);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 调用ClassLoader的loadCLass方法</span></span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">super</span>.loadClass(var1, var2);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>ClassLoader的loadCLass方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">protected</span> Class&lt;?&gt; loadClass(String name, <span class="type">boolean</span> resolve)</span><br><span class="line">        <span class="keyword">throws</span> ClassNotFoundException</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 根据 类名字 来进行上锁</span></span><br><span class="line">        <span class="keyword">synchronized</span> (getClassLoadingLock(name)) &#123;</span><br><span class="line">            <span class="comment">// First, check if the class has already been loaded</span></span><br><span class="line">            <span class="comment">// 第一步,查找该类是否已经被加载</span></span><br><span class="line">            Class&lt;?&gt; c = findLoadedClass(name);</span><br><span class="line">            <span class="keyword">if</span> (c == <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="comment">// null则是没有加载</span></span><br><span class="line">                <span class="type">long</span> <span class="variable">t0</span> <span class="operator">=</span> System.nanoTime();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">                        <span class="comment">// 如果类加载器不是null，则委派给父加载器进行加载</span></span><br><span class="line">                        c = parent.loadClass(name, <span class="literal">false</span>);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 如果父加载器为null,则委托引导类加载器加载该类</span></span><br><span class="line">                        c = findBootstrapClassOrNull(name);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">                    <span class="comment">// ClassNotFoundException thrown if class not found</span></span><br><span class="line">                    <span class="comment">// from the non-null parent class loader</span></span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (c == <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="comment">// 如果上面还是全部没有加载到</span></span><br><span class="line">                    <span class="comment">// If still not found, then invoke findClass in order</span></span><br><span class="line">                    <span class="comment">// to find the class.</span></span><br><span class="line">                    <span class="type">long</span> <span class="variable">t1</span> <span class="operator">=</span> System.nanoTime();</span><br><span class="line">                    <span class="comment">// 都会调用 URLCLassLoader的findCLass方法在加载器的类路径里查找并加载该类</span></span><br><span class="line">                    c = findClass(name);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// this is the defining class loader; record the stats</span></span><br><span class="line">                    sun.misc.PerfCounter.getParentDelegationTime().addTime(t1 - t0);</span><br><span class="line">                    sun.misc.PerfCounter.getFindClassTime().addElapsedTimeFrom(t1);</span><br><span class="line">                    sun.misc.PerfCounter.getFindClasses().increment();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (resolve) &#123;</span><br><span class="line">                resolveClass(c);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> c;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="双亲委派机制好处"><a href="#双亲委派机制好处" class="headerlink" title="双亲委派机制好处"></a><strong>双亲委派机制好处</strong></h3><ol>
<li>沙箱安全机制: 自己写的jre核心类库不会被加载,这样便可以防止核心api库被篡改</li>
<li>避免类的重复加载, 当父类已经加载了该类时,就没必要子ClassLoader再加载一次,保证被加载类的唯一性</li>
<li>全盘委托机制: 是指一个ClassLoader装载一个类时，除非显式的使用宁外一个ClassLoader，该类所依赖及引用的类也由这个ClassLoader载入</li>
</ol>
<h3 id="自定义类加载器"><a href="#自定义类加载器" class="headerlink" title="自定义类加载器"></a><strong>自定义类加载器</strong></h3><p>只需要继承java.lang.ClassLoader类, 主要重写 findClass方法</p>
<ol>
<li>loadClass(String,boolean)</li>
<li>findClass : 默认实现是空方法</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Antlr4-解析入门一</title>
    <url>//article/altrl4one/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>为什么需要解析SQL？解析完SQL后又能做什么？<br>0. 校验SQL的语法是否合规</p>
<ol>
<li>数据安全需要解析SQL获取出对应的的操作</li>
<li>数据血缘的抽取需要解析SQL</li>
<li>类似Hive&#x2F;Spark等在将SQL转化为mr,执行计划等的时候,也是需要解析SQL的</li>
<li>等等;欢迎你来补充更多需要解析SQL的话题</li>
</ol>
</blockquote>
<h1 id="📝-解析的方法"><a href="#📝-解析的方法" class="headerlink" title="📝 解析的方法"></a>📝 解析的方法</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>项目名称</td>
<td>适合的数据库</td>
<td>项目地址</td>
<td>个人观点</td>
</tr>
<tr>
<td>JSqlParser</td>
<td><strong>RDBMS</strong></td>
<td>[嵌入对象]</td>
<td>目前只适合关系型的数据库，对于大数据很多领域的数据库目前还不是特别支持</td>
</tr>
<tr>
<td>calcite</td>
<td>Cassandra、Druid、Elasticsearch、MongoDB、Kafka,FlinkSQL等</td>
<td><a href="https://github.com/apache/calcite/tree/main">https://github.com/apache/calcite/tree/main</a></td>
<td>后续再补</td>
</tr>
<tr>
<td>antlr</td>
<td>可以借助 grammars-v4 这个项目提供,可以解析很多数据库以及编程语言等都可以解析的</td>
<td>antlr4项目:<a href="https://github.com/antlr/antlr4">https://github.com/antlr/antlr4</a><br><br>语法校验项目模块： [嵌入对象]</td>
<td>社区构建比较完善,比如Spark,Hive,Iceberg等都有.g4文件来满足antlr4对SQL的解析,并且你也可以借助开源项目提供的g4来解析</td>
</tr>
</tbody></table>
<p>类似阿里开源的druid这种也是有解析SQL的功能的，但是稍微看了下源码，其底层好像也是有利用到antlr</p>
<h1 id="antlr怎么用"><a href="#antlr怎么用" class="headerlink" title="antlr怎么用"></a>antlr怎么用</h1><h2 id="IDEA插件"><a href="#IDEA插件" class="headerlink" title="IDEA插件"></a>IDEA插件</h2><p>借助IDEA插件来进行安装,但是需要注意的是,项目中的antlr的版本最好是和idea中antlr的版本一致;如果不一致的话，需要使用antlr对应版本的jar来对g4文件生成对应的文件</p>
<p><img src="/images/posts/altrl4one/img-1.png" alt="Antlr4-解析入门一"></p>
<h2 id="手动JAR安装"><a href="#手动JAR安装" class="headerlink" title="手动JAR安装"></a>手动JAR安装</h2><p>对应版本下载地址: <a href="https://github.com/antlr/website-antlr4/tree/gh-pages/download">https://github.com/antlr/website-antlr4/tree/gh-pages/download</a></p>
<p>我们在此处的地址下载自己需要的对应版本;此处的anltr4如果你不想配置环境变量,就替换为 java jar antrlxxx.jar -o… 后面需要的参数</p>
<p>常用参数说明:</p>
<ul>
<li>-o : 生成 tokens,interp,java等路径</li>
<li>-listener: 生成监听者模式的参数</li>
<li>-visitor:  生成访问者模式的参数</li>
<li>-lib:      指定g4文件的路径</li>
</ul>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">antlr4 <span class="literal">-o</span> E:/coding_self/github_learn/grammars<span class="literal">-v4</span>/sql/hive/v3\gen </span><br><span class="line"><span class="literal">-listener</span> <span class="literal">-visitor</span> </span><br><span class="line"><span class="literal">-lib</span> E:/coding_self/github_learn/grammars<span class="literal">-v4</span>/sql/hive/v3 E:/coding_self/github_learn/grammars<span class="literal">-v4</span>/sql/hive/v3\HiveParser.g4 E:/coding_self/github_learn/grammars<span class="literal">-v4</span>/sql/hive/v3\HiveLexer.g4</span><br></pre></td></tr></table></figure>

<h2 id="其它方法"><a href="#其它方法" class="headerlink" title="其它方法"></a>其它方法</h2><p>官网介绍的方法传送门: <a href="https://github.com/antlr/antlr4/blob/master/doc/java-target.md">https://github.com/antlr/antlr4/blob/master/doc/java-target.md</a></p>
<h2 id="生成效果"><a href="#生成效果" class="headerlink" title="生成效果"></a>生成效果</h2><p>采用如上,最后生成的效果如下图;那我们就可以愉快的在代码中使用和调试了</p>
<p><img src="/images/posts/altrl4one/img-2.png" alt="Antlr4-解析入门一"></p>
<h1 id="使用流程"><a href="#使用流程" class="headerlink" title="使用流程"></a>使用流程</h1><p>我们按照上面的流程,都生成对应的文件之后,就可以导入到项目中进行使用了;假设导入后报错的话,仔细检查下是否包名的路径之类的问题,手动处理下就好了</p>
<p>我们这里基于访问者来进行介绍</p>
<h2 id="继承-BaseVisitor"><a href="#继承-BaseVisitor" class="headerlink" title="继承 BaseVisitor"></a>继承 <code>BaseVisitor</code></h2><p>比如我借助 grammars-v4 的hive4生成的访问者base类叫 <code>HiveParserBaseVisitor</code></p>
<pre><code> 借助 Spark的g4生成的 `SqlBaseBaseVisitor`
</code></pre>
<p>这里我已Hive的为准介绍,<code>HiveSqlAst</code> 来集成 <code>HiveParserBaseVisitor</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iyang.bootbasicio.sql.hive.utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.iyang.bootbasicio.sql.hive.HiveParser;</span><br><span class="line"><span class="keyword">import</span> com.iyang.bootbasicio.sql.hive.HiveParserBaseVisitor;</span><br><span class="line"><span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.HashMap;</span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="comment">/***</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span>: yang_bao</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span>: 2023/11/3</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@desc</span>:</span></span><br><span class="line"><span class="comment"> ***/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HiveSqlAst</span> <span class="keyword">extends</span> <span class="title class_">HiveParserBaseVisitor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Map&lt;String, Set&lt;String&gt;&gt; dnTableActionMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">HiveSqlAst</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">HiveSqlAst</span><span class="params">(Map&lt;String, Set&lt;String&gt;&gt; dnTableActionMap)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.dnTableActionMap = dnTableActionMap;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, Set&lt;String&gt;&gt; <span class="title function_">getDnTableActionMap</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> dnTableActionMap;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">visitStatement</span><span class="params">(HiveParser.StatementContext ctx)</span> &#123;</span><br><span class="line">        log.info(<span class="string">&quot; enter visitStatement function &quot;</span>);</span><br><span class="line"><span class="comment">/*        HiveParser.DdlStatementContext ddlStatementContext = ctx.execStatement().ddlStatement();</span></span><br><span class="line"><span class="comment">        HiveParser.TableNameContext nameContext = ddlStatementContext.alterStatement().tableName();</span></span><br><span class="line"><span class="comment">        String dbText = nameContext.db.getText();</span></span><br><span class="line"><span class="comment">        String tabText = nameContext.tab.getText();</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">        log.info(&quot;the db is &#123;&#125; , tab is &#123;&#125; , meta is &#123;&#125; &quot; , dbText, tabText,1);*/</span></span><br><span class="line">				 <span class="comment">// 这步链式调用是不能少的,如果你发现少了的话,下面的解析方法就不会进去了</span></span><br><span class="line">        <span class="built_in">super</span>.visitStatement(ctx);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="comment">// return visitChildren(ctx);</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 包含了 select 的查询</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitSelectStatement</span><span class="params">(HiveParser.SelectStatementContext ctx)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">        log.info(<span class="string">&quot;enter visitSelectStatement function &quot;</span>);</span><br><span class="line"><span class="comment">/*        HiveParser.AtomSelectStatementContext atomSelectStatementContext = ctx.atomSelectStatement();</span></span><br><span class="line"><span class="comment">        String tableName = atomSelectStatementContext.fromClause().fromSource().joinSource().atomjoinSource().tableSource().tabname.getText();</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">        log.info(&quot;the tableName is ---&gt; &#123;&#125; &quot; , tableName);*/</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">        System.out.println(ctx.getText());</span></span><br><span class="line"><span class="comment">       try &#123;</span></span><br><span class="line"><span class="comment">           SqlParser sqlParser = SqlParser.create(ctx.getText());</span></span><br><span class="line"><span class="comment">           SqlNode sqlNode = sqlParser.parseQuery();</span></span><br><span class="line"><span class="comment">           System.out.println(sqlNode);</span></span><br><span class="line"><span class="comment">       &#125; catch (Exception e)&#123;</span></span><br><span class="line"><span class="comment">           e.printStackTrace();</span></span><br><span class="line"><span class="comment">       &#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">/*        HiveParser.AtomSelectStatementContext atomCtx = ctx.atomSelectStatement();</span></span><br><span class="line"><span class="comment">        HiveParser.FromSourceContext fromSourceContext = atomCtx.fromClause().fromSource();</span></span><br><span class="line"><span class="comment">        HiveParser.AtomjoinSourceContext atomJoinSource = fromSourceContext.joinSource().atomjoinSource();</span></span><br><span class="line"><span class="comment">        String atomTableName = atomJoinSource.tableSource().getText();</span></span><br><span class="line"><span class="comment">        List&lt;HiveParser.JoinSourcePartContext&gt; partContexts = fromSourceContext.joinSource().joinSourcePart();</span></span><br><span class="line"><span class="comment">        for (HiveParser.JoinSourcePartContext sourcePartContext : partContexts) &#123;</span></span><br><span class="line"><span class="comment">            String partTableName = sourcePartContext.tableSource().getText();</span></span><br><span class="line"><span class="comment">            log.info(&quot;the partTableName value is ---&gt; &#123;&#125; &quot;, partTableName);</span></span><br><span class="line"><span class="comment">        &#125;</span></span><br><span class="line"><span class="comment">        log.info(&quot;the atomJoinTable value is ---&gt; &#123;&#125; &quot; , atomTableName);*/</span></span><br><span class="line">        <span class="comment">// String tabelName = ctx.atomSelectStatement().fromClause().fromSource().joinSource().atomjoinSource().tableSource().getText();</span></span><br><span class="line"></span><br><span class="line">        HiveParser.<span class="type">AtomSelectStatementContext</span> <span class="variable">atomSelectStatement</span> <span class="operator">=</span> ctx.atomSelectStatement();</span><br><span class="line"></span><br><span class="line">        parseSelectSql(atomSelectStatement);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 包含了 insert </span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitInsertClause</span><span class="params">(HiveParser.InsertClauseContext ctx)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">        HiveParser.<span class="type">TableOrPartitionContext</span> <span class="variable">tableOrPartition</span> <span class="operator">=</span> ctx.destination().tableOrPartition();</span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> tableOrPartition.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot;in visitInsertClause, the tableName is ---&gt; &#123;&#125; &quot;</span> , tableName);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitInsertClause(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// alter 语句</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitAlterStatement</span><span class="params">(HiveParser.AlterStatementContext ctx)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">        log.info(<span class="string">&quot; enter visitAlterStatement func &quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> ctx.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot;the tableName is &#123;&#125; &quot;</span> , tableName);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// tableName 可能会带上catalog的名称；如果需要的话，需要进行catalog的切割</span></span><br><span class="line">        <span class="comment">// ALTER TABLE HIVE_PROD.XIAOBAO_BIGSCREENT.T_ICCE_ENTERPRISE ADD COLUMNS ( SINK_TIME STRING COMMENT &#x27;WRITE DATA TIME&#x27;)</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitAlterStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// create 建表</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitCreateTableStatement</span><span class="params">(HiveParser.CreateTableStatementContext ctx)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> ctx.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot;in visitCreateTableStatement, the tableName is &#123;&#125; &quot;</span> , tableName);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitCreateTableStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改语句</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitUpdateStatement</span><span class="params">(HiveParser.UpdateStatementContext ctx)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> ctx.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot;in visitUpdateStatement, the tableName value is &#123;&#125; &quot;</span>, tableName);</span><br><span class="line">        <span class="comment">// where 中可能保存查询库或者表的条件</span></span><br><span class="line">        setActionToMapByDbTableName(tableName, HiveActionEnum.UPDATE.getAction());</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitUpdateStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// delete 删除语句</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitDeleteStatement</span><span class="params">(HiveParser.DeleteStatementContext ctx)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> ctx.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot;in visitDeleteStatement, the tableName value is &#123;&#125; &quot;</span> , tableName);</span><br><span class="line">        <span class="comment">// where 中可能保存查询库或者表的条件</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitDeleteStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// drop 删除表数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitDropTableStatement</span><span class="params">(HiveParser.DropTableStatementContext ctx)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> ctx.tableName().getText();</span><br><span class="line">        log.info(<span class="string">&quot; in visitDropTableStatement , the tableName is &#123;&#125; &quot;</span> , tableName);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitDropTableStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// drop删除库的数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">visitDropDatabaseStatement</span><span class="params">(HiveParser.DropDatabaseStatementContext ctx)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">dbName</span> <span class="operator">=</span> ctx.db_schema().getText();</span><br><span class="line">        log.info(<span class="string">&quot; in visitDropDatabaseStatement, the dbName is &#123;&#125; &quot;</span> , dbName);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.visitDropDatabaseStatement(ctx);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解析 select 查询的sql语句,获取对应的库和表信息</span></span><br><span class="line"><span class="comment">     * 对 join,子查询sql,以及子查询SQL等嵌套递归查询</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">parseSelectSql</span><span class="params">(HiveParser.AtomSelectStatementContext atomSelectStatement)</span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// atomSelectStatement 中存在需要解析的sql; fromSource,join,subQuery,</span></span><br><span class="line">        HiveParser.<span class="type">JoinSourceContext</span> <span class="variable">sourceContext</span> <span class="operator">=</span> atomSelectStatement.fromClause().fromSource().joinSource();</span><br><span class="line">        <span class="comment">// 获取传入进来的 atmo的表名字;判断非空即获取</span></span><br><span class="line">        <span class="keyword">if</span> (sourceContext.atomjoinSource().tableSource() != <span class="literal">null</span>)&#123;</span><br><span class="line">            HiveParser.<span class="type">TableNameContext</span> <span class="variable">tabname</span> <span class="operator">=</span> sourceContext.atomjoinSource().tableSource().tabname;</span><br><span class="line">            log.info(<span class="string">&quot;the tabname value us ---&gt; &#123;&#125; &quot;</span>, tabname.getText());</span><br><span class="line">            setActionToMapByDbTableName(tabname.getText(), HiveActionEnum.SELECT.getAction());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (HiveParser.JoinSourcePartContext sourcePartContext : sourceContext.joinSourcePart()) &#123;</span><br><span class="line">            <span class="comment">// 解析join部分自身获取的 库和表信息</span></span><br><span class="line">            <span class="keyword">if</span> (sourcePartContext.tableSource() != <span class="literal">null</span>) &#123;</span><br><span class="line">                HiveParser.<span class="type">TableNameContext</span> <span class="variable">sourcePartTableName</span> <span class="operator">=</span> sourcePartContext.tableSource().tabname;</span><br><span class="line">                setActionToMapByDbTableName(sourcePartTableName.getText(), HiveActionEnum.SELECT.getAction());</span><br><span class="line">                log.info(<span class="string">&quot;the sourcePartTableName value is ---&gt; &#123;&#125;&quot;</span> , sourcePartTableName.getText());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// join部分的sql也是存在子sql的情况下;如果存在子sql的话,就进行迭代处理</span></span><br><span class="line">            HiveParser.<span class="type">SubQuerySourceContext</span> <span class="variable">partSubQuery</span> <span class="operator">=</span> sourcePartContext.subQuerySource();</span><br><span class="line">            <span class="keyword">if</span> (partSubQuery != <span class="literal">null</span>) &#123;</span><br><span class="line">                HiveParser.<span class="type">AtomSelectStatementContext</span> <span class="variable">partSubQueryAtmo</span> <span class="operator">=</span> partSubQuery.queryStatementExpression().queryStatementExpressionBody().regularBody().selectStatement().atomSelectStatement();</span><br><span class="line">                parseSelectSql(partSubQueryAtmo);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 存在没有值的情况sql语句</span></span><br><span class="line">            <span class="comment">// HiveParser.TableNameContext sourcePartTableName = sourcePartContext.tableSource().tabname;</span></span><br><span class="line">            <span class="comment">// log.info(&quot;the sourcePartTableName value is ---&gt; &#123;&#125;&quot; , sourcePartTableName.getText());</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 子sql;如果子sql不是null</span></span><br><span class="line">        HiveParser.<span class="type">SubQuerySourceContext</span> <span class="variable">subQuerySourceContext</span> <span class="operator">=</span> sourceContext.atomjoinSource().subQuerySource();</span><br><span class="line">        <span class="keyword">if</span> (subQuerySourceContext != <span class="literal">null</span>) &#123;</span><br><span class="line">            HiveParser.<span class="type">AtomSelectStatementContext</span> <span class="variable">subQueryAtom</span> <span class="operator">=</span> subQuerySourceContext.queryStatementExpression().queryStatementExpressionBody().regularBody().selectStatement().atomSelectStatement();</span><br><span class="line">            parseSelectSql(subQueryAtom);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 封装 db table 对应的action到 map 集合中</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> dbTableName</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> action</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">setActionToMapByDbTableName</span><span class="params">(String dbTableName,String action)</span>&#123;</span><br><span class="line"></span><br><span class="line">        Set&lt;String&gt; dbTableActionSet = dnTableActionMap.getOrDefault(dbTableName, <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;());</span><br><span class="line">        dbTableActionSet.add(action);</span><br><span class="line">        dnTableActionMap.put(dbTableName, dbTableActionSet);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="解析使用"><a href="#解析使用" class="headerlink" title="解析使用"></a>解析使用</h2><p>创建一个来进行调用使用;大家可以使用下面的例子来进行校验使用</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.iyang.bootbasicio.sql.hive.utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.iyang.bootbasicio.sql.hive.HiveLexer;</span><br><span class="line"><span class="keyword">import</span> com.iyang.bootbasicio.sql.hive.HiveParser;</span><br><span class="line"><span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line"><span class="keyword">import</span> org.antlr.v4.runtime.CharStreams;</span><br><span class="line"><span class="keyword">import</span> org.antlr.v4.runtime.CommonTokenStream;</span><br><span class="line"><span class="keyword">import</span> org.antlr.v4.runtime.atn.PredictionMode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="comment">/***</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span>: yang_bao</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span>: 2023/11/3</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@desc</span>:</span></span><br><span class="line"><span class="comment"> ***/</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HiveSqlHelper</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">ALERT_SQL</span> <span class="operator">=</span> <span class="string">&quot;ALTER TABLE HIVE_PROD.XIAOBAO_BIGSCREENT.T_ICCE_ENTERPRISE ADD COLUMNS ( SINK_TIME STRING COMMENT &#x27;WRITE DATA TIME&#x27;)&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">SIMPLE_QUERY_SQL</span> <span class="operator">=</span> <span class="string">&quot;SELECT * FROM AAA.CCC&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">MANY_TABLE_SQL</span> <span class="operator">=</span> <span class="string">&quot;SELECT \n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    NULL                        AS   ID,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    CAST(TEMP_A.CREATE_DATE AS TIMESTAMP)          AS   TIMEC,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    TEMP_A.ENTERPRISE_NUM       AS   ENTERPRISE_NUM,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    TEMP_B.USER_NUM             AS   USER_NUM,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    TEMP_A.VIRTUAL_TENANT       AS   VTENANT,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    CAST(NOW() AS TIMESTAMP)    AS   ETL_LOAD_TS\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;FROM (\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    SELECT \n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        COUNT(DISTINCT A.ENTERPRISE_ID) AS ENTERPRISE_NUM,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        A.PLATFORM_VIRTUAL_TENANT       AS VIRTUAL_TENANT,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        A.CREATE_DATE                   AS CREATE_DATE\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    FROM (SELECT ENTERPRISE_ID,PLATFORM_VIRTUAL_TENANT,SUBSTRING(CREATE_DATE,1,10) AS CREATE_DATE FROM DIM_BIGSCREENT.DIM_ECENTERPRISE_LIST_DI) A GROUP BY PLATFORM_VIRTUAL_TENANT,CREATE_DATE\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;) TEMP_A LEFT JOIN\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;(\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    SELECT\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        COUNT(DISTINCT A.USER_ID) AS USER_NUM,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        A.PLATFORM_VIRTUAL_TENANT AS VIRTUAL_TENANT,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;        A.CREATE_DATE\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    FROM (SELECT USER_ID,PLATFORM_VIRTUAL_TENANT,SUBSTRING(CREATE_DATE,1,10) AS CREATE_DATE FROM DIM_BIGSCREENT.DIM_ECUSERENTERPRISE_RELATION_LIST_DI) A GROUP BY PLATFORM_VIRTUAL_TENANT,CREATE_DATE\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;) TEMP_B ON TEMP_A.VIRTUAL_TENANT = TEMP_B.VIRTUAL_TENANT AND TEMP_A.CREATE_DATE = TEMP_B.CREATE_DATE&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">JOIN_SQL</span> <span class="operator">=</span> <span class="string">&quot;select * from aaa.aa as a left join ccc.cc as c on a.id = c.id  left join ddd.dd as d on a.id = d.id&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">INSERT_QUERY_SQL</span> <span class="operator">=</span> <span class="string">&quot;INSERT OVERWRITE TABLE aaa.cc\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;select * from aaa.aa&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">CREATE_DDL_SQL</span> <span class="operator">=</span> <span class="string">&quot;create table if not exists  ads_bigscreent.ads_cloud_info_sum_df (\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tid                       bigint   comment &#x27;id(主键)&#x27;  \t\t\t\t\t\t,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tplatform_id\t         \t string   comment &#x27;平台ID&#x27;                          ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tplatform_name\t         string   comment &#x27;平台名称&#x27;                        ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tenterprise_num\t         bigint   comment &#x27;上云企业数量&#x27;                    ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tuser_num\t             bigint   comment &#x27;注册用户数&#x27;                      ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\ttransaction_num\t     \t bigint   comment &#x27;交易用户数&#x27;                      ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tdeviceconnection_num\t bigint   comment &#x27;设备连接数&#x27;                      ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tpartners_num\t         bigint   comment &#x27;生态伙伴数量&#x27;                    ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tapps_num\t             bigint   comment &#x27;工业app数量&#x27;                     ,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tvtenant\t\t\t\t\tstring \t\tcomment &#x27;租户&#x27;\t\t\t\t\t\t\t,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tlongitude\t    \t\tdecimal(25,10)  comment\t&#x27;经度&#x27;\t\t\t\t\t\t,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;    latitude\t    \t\tdecimal(25,10)  comment\t&#x27;纬度&#x27;\t\t\t\t\t\t,\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;\tetl_load_ts              timestamp comment &#x27;创建时间&#x27;\n&quot;</span> +</span><br><span class="line">            <span class="string">&quot;)  comment  &#x27;上云情况详情&#x27;&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">UPDATE_QUERY_SQL</span> <span class="operator">=</span> <span class="string">&quot;update ads_bigscreent.ads_cloud_info_sum_df set name = &#x27;123&#x27; where id in (select id from aaaa.aa) and &quot;</span> +</span><br><span class="line">            <span class="string">&quot; name in (select name from ads_bigscreent.ads_cloud_info_sum_df)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DELETE_QUERT_SQl</span> <span class="operator">=</span> <span class="string">&quot;delete from ads_bigscreent.ads_cloud_info_sum_df where id in (select id from aaaa.cc)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DROP_TABLE_SQL</span> <span class="operator">=</span> <span class="string">&quot;drop table ads_bigscreent.ads_cloud_info_sum_df&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DROP_DATABASE_SQL</span> <span class="operator">=</span> <span class="string">&quot;drop database ads_bigscreent&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">QUERY_SUB_SQl</span> <span class="operator">=</span> <span class="string">&quot;select * from aaa.a as a left join (select * from ccc.c) as c on a.id = c.id&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">HiveLexer</span> <span class="variable">lexer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveLexer</span>(CharStreams.fromString(UPDATE_QUERY_SQL));</span><br><span class="line">        <span class="type">CommonTokenStream</span> <span class="variable">tokenStream</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CommonTokenStream</span>(lexer);</span><br><span class="line">        <span class="type">HiveParser</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveParser</span>(tokenStream);</span><br><span class="line">        parser.getInterpreter().setPredictionMode(PredictionMode.SLL);</span><br><span class="line"></span><br><span class="line">        <span class="type">HiveSqlAst</span> <span class="variable">hiveSqlAst</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveSqlAst</span>();</span><br><span class="line">        hiveSqlAst.visit(parser.statement());</span><br><span class="line"></span><br><span class="line">        Map&lt;String, Set&lt;String&gt;&gt; dnTableActionMap = hiveSqlAst.getDnTableActionMap();</span><br><span class="line">        log.info(<span class="string">&quot;the dnTableActionMap value is &#123;&#125; &quot;</span>, dnTableActionMap);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用结果"><a href="#使用结果" class="headerlink" title="使用结果"></a>使用结果</h2><p>比如我们这里使用的一条update中嵌套一个select校验,看最后的结果都是查询出来的;如果只是库和表的校验的话,那么对应的功能就是可以满足了的</p>
<p><img src="/images/posts/altrl4one/img-3.png" alt="Antlr4-解析入门一"></p>
<h2 id="错误SQL解析的错误获取"><a href="#错误SQL解析的错误获取" class="headerlink" title="错误SQL解析的错误获取"></a>错误SQL解析的错误获取</h2><p>继承  <code>BaseErrorListener</code>; 我们这里借助传入list来收起错误的error</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HiveErrorListener</span> <span class="keyword">extends</span> <span class="title class_">BaseErrorListener</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> List&lt;String&gt; errorMsg ;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">HiveErrorListener</span><span class="params">(List&lt;String&gt; errorMsg)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.errorMsg = errorMsg;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">syntaxError</span><span class="params">(Recognizer&lt;?, ?&gt; recognizer, Object offendingSymbol, <span class="type">int</span> line,</span></span><br><span class="line"><span class="params">                            <span class="type">int</span> charPositionInLine, String msg, RecognitionException e)</span> &#123;</span><br><span class="line">        errorMsg.add(msg);</span><br><span class="line">        log.info(<span class="string">&quot;the HiveErrorListener(syntaxError) error info is &#123;&#125; &quot;</span> , msg);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们新addErrorListener和addErrorListener,注册上我们的错误监听器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">				 <span class="comment">// 收集错误的集合</span></span><br><span class="line">        List&lt;String&gt; errorList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">HiveLexer</span> <span class="variable">lexer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveLexer</span>(CharStreams.fromString(UPDATE_QUERY_SQL));</span><br><span class="line">        <span class="type">CommonTokenStream</span> <span class="variable">tokenStream</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CommonTokenStream</span>(lexer);</span><br><span class="line">        <span class="type">HiveParser</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveParser</span>(tokenStream);</span><br><span class="line">        parser.getInterpreter().setPredictionMode(PredictionMode.SLL);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 注册我们的错误监听</span></span><br><span class="line">        lexer.removeErrorListeners();</span><br><span class="line">        parser.removeErrorListeners();</span><br><span class="line">        lexer.addErrorListener(<span class="keyword">new</span> <span class="title class_">HiveErrorListener</span>(errorList));</span><br><span class="line">        parser.addErrorListener(<span class="keyword">new</span> <span class="title class_">HiveErrorListener</span>(errorList));</span><br><span class="line"></span><br><span class="line">        <span class="type">HiveSqlAst</span> <span class="variable">hiveSqlAst</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HiveSqlAst</span>();</span><br><span class="line">        hiveSqlAst.visit(parser.statement());</span><br><span class="line">        log.info(<span class="string">&quot;the error info is : &#123;&#125; &quot;</span> , errorList);</span><br><span class="line"></span><br><span class="line">        Map&lt;String, Set&lt;String&gt;&gt; dnTableActionMap = hiveSqlAst.getDnTableActionMap();</span><br><span class="line">        log.info(<span class="string">&quot;the dnTableActionMap value is &#123;&#125; &quot;</span>, dnTableActionMap);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>我们故意在sql前加个;号,就可以看到错误解析的具体位置在 ; 这里,还是非常的准确的</p>
<p><img src="/images/posts/altrl4one/img-4.png" alt="Antlr4-解析入门一"></p>
<h2 id="方法使用小结"><a href="#方法使用小结" class="headerlink" title="方法使用小结"></a>方法使用小结</h2><p>可以看到该接口还提供了很多的对用操作方法,那么我们在使用的时候,就根据对应的方法耐心的调试即可</p>
<p><img src="/images/posts/altrl4one/img-5.png" alt="Antlr4-解析入门一"></p>
<h2 id="idea方法小结"><a href="#idea方法小结" class="headerlink" title="idea方法小结"></a>idea方法小结</h2><p>上面不是提到我们安装了antlr的插件么？那么也得使用起来了吧</p>
<p>还是以grammer-v4为准来看; 点击 <code>statement</code> 会出现下面的Test Rule statement;然后我们就可以将我们SQL放入如下,就可以看到左下角的SQL语法树的结构</p>
<p>然后我们的代码就可以根据树的调用方法来进行精确的获取,并且同时也可以看到校验的SQL是否有问题</p>
<p><img src="/images/posts/altrl4one/img-6.png" alt="Antlr4-解析入门一"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>目前我这边更多的是解析出来SQL,然后对权限进行校验和拦截操作</p>
<p>对于多种不同类似的数据库,其语法是可能不同的,我们其实是可以借助一些设计模式来更好的帮助我们维护和扩展代码</p>
<p>最后,放一个我编写的demo的传送门: <a href="https://github.com/ruY9527/boot-case/blob/master/boot-base/boot-basic-io/src/main/java/com/iyang/bootbasicio/sql/hive/utils/HiveSqlHelper.java">https://github.com/ruY9527/boot-case/blob/master/boot-base/boot-basic-io/src/main/java/com/iyang/bootbasicio/sql/hive/utils/HiveSqlHelper.java</a></p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://makeyourchoice.cn/archives/591/">https://makeyourchoice.cn/archives/591/</a></li>
<li>[嵌入对象]</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Antlr</tag>
        <tag>Iceberg</tag>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap 源码阅读</title>
    <url>//article/hashMap_code/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-HashMap-源码阅读"><a href="#📝-HashMap-源码阅读" class="headerlink" title="📝 HashMap 源码阅读"></a>📝 <strong>HashMap 源码阅读</strong></h1><p>HashMap 这种Key,Value 的存储结构,是我们在写代码中经常使用到的.可以说使用是非常频繁的,不过现在使用JSONObject也是非常多的,二者都是实现了Map接口</p>
<p>所以看下HashMap源码是非常有必要的</p>
<h2 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h2><p>这里我们要看下 HashMap的内部类</p>
<p>这里的 Node 节点就是 HashMap存放数据的结构. hash 计算出来的哈希值,key就是HashMap中的key,value就是key对应的value的值. 这个 next 就是 key 不一样,计算出来的hash却是一样的,这样就有了hash冲突,所以就将节点存放在next里面了,从尾部插入进去. java8 后,如果next的长度是大于8的话,就会转化了红黑树来存储,那样获取值的速度变快了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;K,V&gt; <span class="keyword">implements</span> <span class="title class_">Map</span>.Entry&lt;K,V&gt; &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">int</span> hash;</span><br><span class="line">    <span class="keyword">final</span> K key;</span><br><span class="line">    V value;</span><br><span class="line">    Node&lt;K,V&gt; next;</span><br><span class="line"></span><br><span class="line">    Node(<span class="type">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">        <span class="built_in">this</span>.hash = hash;</span><br><span class="line">        <span class="built_in">this</span>.key = key;</span><br><span class="line">        <span class="built_in">this</span>.value = value;</span><br><span class="line">        <span class="built_in">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> K <span class="title function_">getKey</span><span class="params">()</span>        &#123; <span class="keyword">return</span> key; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> V <span class="title function_">getValue</span><span class="params">()</span>      &#123; <span class="keyword">return</span> value; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> String <span class="title function_">toString</span><span class="params">()</span> &#123; <span class="keyword">return</span> key + <span class="string">&quot;=&quot;</span> + value; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">hashCode</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Objects.hashCode(key) ^ Objects.hashCode(value);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> V <span class="title function_">setValue</span><span class="params">(V newValue)</span> &#123;</span><br><span class="line">        <span class="type">V</span> <span class="variable">oldValue</span> <span class="operator">=</span> value;</span><br><span class="line">        value = newValue;</span><br><span class="line">        <span class="keyword">return</span> oldValue;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object o)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="built_in">this</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">if</span> (o <span class="keyword">instanceof</span> Map.Entry) &#123;</span><br><span class="line">            Map.Entry&lt;?,?&gt; e = (Map.Entry&lt;?,?&gt;)o;</span><br><span class="line">            <span class="keyword">if</span> (Objects.equals(key, e.getKey()) &amp;&amp;</span><br><span class="line">                Objects.equals(value, e.getValue()))</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>TreeNode 这个内部类就是表示红黑树的. TODO 后续进行更新</p>
<p>参数, 可以看到 HashMap 是使用了一个数组来进行存储 Node节点</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">transient</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"></span><br><span class="line"><span class="keyword">transient</span> <span class="type">int</span> size;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEFAULT_INITIAL_CAPACITY</span> <span class="operator">=</span> <span class="number">1</span> &lt;&lt; <span class="number">4</span>; <span class="comment">// aka 16</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">MAXIMUM_CAPACITY</span> <span class="operator">=</span> <span class="number">1</span> &lt;&lt; <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">float</span> <span class="variable">DEFAULT_LOAD_FACTOR</span> <span class="operator">=</span> <span class="number">0.75f</span>;</span><br></pre></td></tr></table></figure>

<h2 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h2><h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Constructs an empty &lt;tt&gt;HashMap&lt;/tt&gt; with the default initial capacity</span></span><br><span class="line"><span class="comment"> * (16) and the default load factor (0.75).</span></span><br><span class="line"><span class="comment"> 	当使用无参构造函数的时候,只是对 loadFactor 进行了赋值操作</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">HashMap</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.loadFactor = DEFAULT_LOAD_FACTOR; <span class="comment">// all other fields defaulted</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 传递一个int类型的参数时候,就会计息往下调用构造函数</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">HashMap</span><span class="params">(<span class="type">int</span> initialCapacity)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>(initialCapacity, DEFAULT_LOAD_FACTOR);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	对值进行判断,怕你可能传入进来一个负数来测试玩玩哈哈哈。</span></span><br><span class="line"><span class="comment">	最后调用到了tableSizeFor方法,可以看到这个方法是对传入进来的参数,进行一连串的位运算.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">HashMap</span><span class="params">(<span class="type">int</span> initialCapacity, <span class="type">float</span> loadFactor)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Illegal initial capacity: &quot;</span> +</span><br><span class="line">                                               initialCapacity);</span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &gt; MAXIMUM_CAPACITY)</span><br><span class="line">            initialCapacity = MAXIMUM_CAPACITY;</span><br><span class="line">        <span class="keyword">if</span> (loadFactor &lt;= <span class="number">0</span> || Float.isNaN(loadFactor))</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Illegal load factor: &quot;</span> +</span><br><span class="line">                                               loadFactor);</span><br><span class="line">        <span class="built_in">this</span>.loadFactor = loadFactor;</span><br><span class="line">        <span class="built_in">this</span>.threshold = tableSizeFor(initialCapacity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">tableSizeFor</span><span class="params">(<span class="type">int</span> cap)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> cap - <span class="number">1</span>;</span><br><span class="line">        n |= n &gt;&gt;&gt; <span class="number">1</span>;</span><br><span class="line">        n |= n &gt;&gt;&gt; <span class="number">2</span>;</span><br><span class="line">        n |= n &gt;&gt;&gt; <span class="number">4</span>;</span><br><span class="line">        n |= n &gt;&gt;&gt; <span class="number">8</span>;</span><br><span class="line">        n |= n &gt;&gt;&gt; <span class="number">16</span>;</span><br><span class="line">        <span class="keyword">return</span> (n &lt; <span class="number">0</span>) ? <span class="number">1</span> : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 传入 Map 的实现类的话,就是往下继续调用 putMapEntries方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">HashMap</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.loadFactor = DEFAULT_LOAD_FACTOR;</span><br><span class="line">        putMapEntries(m, <span class="literal">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	传入 m.size()的长度,长度大于0就会走逻辑代码.最后可以看到 迭代了m,然后调用putVal来将值放入Map中</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">putMapEntries</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m, <span class="type">boolean</span> evict)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> m.size();</span><br><span class="line">        <span class="keyword">if</span> (s &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (table == <span class="literal">null</span>) &#123; <span class="comment">// pre-size</span></span><br><span class="line">                <span class="type">float</span> <span class="variable">ft</span> <span class="operator">=</span> ((<span class="type">float</span>)s / loadFactor) + <span class="number">1.0F</span>;</span><br><span class="line">                <span class="type">int</span> <span class="variable">t</span> <span class="operator">=</span> ((ft &lt; (<span class="type">float</span>)MAXIMUM_CAPACITY) ?</span><br><span class="line">                         (<span class="type">int</span>)ft : MAXIMUM_CAPACITY);</span><br><span class="line">                <span class="keyword">if</span> (t &gt; threshold)</span><br><span class="line">                    threshold = tableSizeFor(t);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (s &gt; threshold)</span><br><span class="line">                resize();</span><br><span class="line">            <span class="keyword">for</span> (Map.Entry&lt;? <span class="keyword">extends</span> <span class="title class_">K</span>, ? <span class="keyword">extends</span> <span class="title class_">V</span>&gt; e : m.entrySet()) &#123;</span><br><span class="line">                <span class="type">K</span> <span class="variable">key</span> <span class="operator">=</span> e.getKey();</span><br><span class="line">                <span class="type">V</span> <span class="variable">value</span> <span class="operator">=</span> e.getValue();</span><br><span class="line">                putVal(hash(key), key, value, <span class="literal">false</span>, evict);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="put方法"><a href="#put方法" class="headerlink" title="put方法"></a>put方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> V <span class="title function_">put</span><span class="params">(K key, V value)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> putVal(hash(key), key, value, <span class="literal">false</span>, <span class="literal">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据传入进来的key来计算对应的hash值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">hash</span><span class="params">(Object key)</span> &#123;</span><br><span class="line">        <span class="type">int</span> h;</span><br><span class="line">        <span class="keyword">return</span> (key == <span class="literal">null</span>) ? <span class="number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	putVal 就是 </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">final</span> V <span class="title function_">putVal</span><span class="params">(<span class="type">int</span> hash, K key, V value, <span class="type">boolean</span> onlyIfAbsent,</span></span><br><span class="line"><span class="params">                   <span class="type">boolean</span> evict)</span> &#123;</span><br><span class="line">        <span class="comment">// 定义一些变量</span></span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="type">int</span> n, i;</span><br><span class="line">    	<span class="comment">// table 赋值给 tab 并判断是否等于null 或者 tab的长度是否等于0,如果是的话，就会调用resize来进行扩容</span></span><br><span class="line">        <span class="keyword">if</span> ((tab = table) == <span class="literal">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">            n = (tab = resize()).length;</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="literal">null</span>)</span><br><span class="line">            tab[i] = newNode(hash, key, value, <span class="literal">null</span>);</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            Node&lt;K,V&gt; e; K k;</span><br><span class="line">            <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">                ((k = p.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                e = p;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="built_in">this</span>, tab, hash, key, value);</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">binCount</span> <span class="operator">=</span> <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">                    <span class="keyword">if</span> ((e = p.next) == <span class="literal">null</span>) &#123;</span><br><span class="line">                        p.next = newNode(hash, key, value, <span class="literal">null</span>);</span><br><span class="line">                        <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// -1 for 1st</span></span><br><span class="line">                            treeifyBin(tab, hash);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                        ((k = e.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    p = e;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (e != <span class="literal">null</span>) &#123; <span class="comment">// existing mapping for key</span></span><br><span class="line">                <span class="type">V</span> <span class="variable">oldValue</span> <span class="operator">=</span> e.value;</span><br><span class="line">                <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="literal">null</span>)</span><br><span class="line">                    e.value = value;</span><br><span class="line">                afterNodeAccess(e);</span><br><span class="line">                <span class="keyword">return</span> oldValue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        ++modCount;</span><br><span class="line">        <span class="keyword">if</span> (++size &gt; threshold)</span><br><span class="line">            resize();</span><br><span class="line">        afterNodeInsertion(evict);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	扩容方法</span></span><br><span class="line"><span class="comment">	table使用 oldTab来进行存储,拿出oldTab的长度(如果oldTab是null的话,对应的长度就是为0).</span></span><br><span class="line"><span class="comment">	oldThr 是 记录 threshold 之前的值, newCap / newThr就是需要扩容使用到的变量命名.</span></span><br><span class="line"><span class="comment">	这里分为 </span></span><br><span class="line"><span class="comment">	1 : oldCap 是大于0的。 如果比 MAXIMUM_CAPACITY 还是要大的话,就说明里面存储的元素是太多了,就直接返回oldTab.  还有一种就是 newCap等于oldCap的1.5倍并且小于MAXIMUM_CAPACITY和oldCap是大于默认16的,就会进行1.5倍的扩容</span></span><br><span class="line"><span class="comment">	2 : oldThr 大于 0, newCap(扩容新长度) 就是等于 oldThe的值.</span></span><br><span class="line"><span class="comment">	3 : 否则就是都使用默认的值大小</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">        Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">        <span class="type">int</span> <span class="variable">oldCap</span> <span class="operator">=</span> (oldTab == <span class="literal">null</span>) ? <span class="number">0</span> : oldTab.length;</span><br><span class="line">        <span class="type">int</span> <span class="variable">oldThr</span> <span class="operator">=</span> threshold;</span><br><span class="line">        <span class="type">int</span> newCap, newThr = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span> (oldCap &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">                threshold = Integer.MAX_VALUE;</span><br><span class="line">                <span class="keyword">return</span> oldTab;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp;</span><br><span class="line">                     oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">                newThr = oldThr &lt;&lt; <span class="number">1</span>; <span class="comment">// double threshold</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (oldThr &gt; <span class="number">0</span>) <span class="comment">// initial capacity was placed in threshold</span></span><br><span class="line">            newCap = oldThr;</span><br><span class="line">        <span class="keyword">else</span> &#123;               <span class="comment">// zero initial threshold signifies using defaults</span></span><br><span class="line">            newCap = DEFAULT_INITIAL_CAPACITY;</span><br><span class="line">            newThr = (<span class="type">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (newThr == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="type">float</span> <span class="variable">ft</span> <span class="operator">=</span> (<span class="type">float</span>)newCap * loadFactor;</span><br><span class="line">            newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="type">float</span>)MAXIMUM_CAPACITY ?</span><br><span class="line">                      (<span class="type">int</span>)ft : Integer.MAX_VALUE);</span><br><span class="line">        &#125;</span><br><span class="line">        threshold = newThr;</span><br><span class="line">    	</span><br><span class="line">    	<span class="comment">/**</span></span><br><span class="line"><span class="comment">    	使用扩容后的newCap来创建一个数组,oldTab不是null,然后就需要将老的值赋值到新的newTab里面来.</span></span><br><span class="line"><span class="comment">        使用下标来进行迭代,获取每个下标的Node节点的值,然oldTab[j]赋值给e后,然后将oldTab[j]重置为null.</span></span><br><span class="line"><span class="comment">        这里面的进行Node复制是有分为下面几种, Node的next节点是没有值得,next下面是由值,e节点转化为了红黑树.</span></span><br><span class="line"><span class="comment">        1 : 如果e.next是null,也就是没有值,newTab[e.hash &amp; (newCap - 1)] = e来赋值.</span></span><br><span class="line"><span class="comment">        2 : 如果e是TreeNode的话,就会调用((TreeNode&lt;K,V&gt;)e).split(this, newTab, j, oldCap)方法.</span></span><br><span class="line"><span class="comment">        3 : 然后可以看到 do while 循环里面, while 里面的条件是 e.next != null 才会进去,也就是next是由值得情况下才会进入到这里面来.然后可以看到一些系取节点啊,赋值给变量啊,然后赋值给新创建的Node数组下标然后将之前的node节点重置为null。 这里就需要读者对这些代码来慢慢消化了.仔细想看,就是对node节点的取值,赋值,重置等操作.</span></span><br><span class="line"><span class="comment">    	*/</span></span><br><span class="line">        <span class="meta">@SuppressWarnings(&#123;&quot;rawtypes&quot;,&quot;unchecked&quot;&#125;)</span></span><br><span class="line">        Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="keyword">new</span> <span class="title class_">Node</span>[newCap];</span><br><span class="line">        table = newTab;</span><br><span class="line">        <span class="keyword">if</span> (oldTab != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; oldCap; ++j) &#123;</span><br><span class="line">                Node&lt;K,V&gt; e;</span><br><span class="line">                <span class="keyword">if</span> ((e = oldTab[j]) != <span class="literal">null</span>) &#123;</span><br><span class="line">                    oldTab[j] = <span class="literal">null</span>;</span><br><span class="line">                    <span class="keyword">if</span> (e.next == <span class="literal">null</span>)</span><br><span class="line">                        newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                        ((TreeNode&lt;K,V&gt;)e).split(<span class="built_in">this</span>, newTab, j, oldCap);</span><br><span class="line">                    <span class="keyword">else</span> &#123; <span class="comment">// preserve order</span></span><br><span class="line">                        Node&lt;K,V&gt; loHead = <span class="literal">null</span>, loTail = <span class="literal">null</span>;</span><br><span class="line">                        Node&lt;K,V&gt; hiHead = <span class="literal">null</span>, hiTail = <span class="literal">null</span>;</span><br><span class="line">                        Node&lt;K,V&gt; next;</span><br><span class="line">                        <span class="keyword">do</span> &#123;</span><br><span class="line">                            next = e.next;</span><br><span class="line">                            <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">                                <span class="keyword">if</span> (loTail == <span class="literal">null</span>)</span><br><span class="line">                                    loHead = e;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    loTail.next = e;</span><br><span class="line">                                loTail = e;</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="keyword">else</span> &#123;</span><br><span class="line">                                <span class="keyword">if</span> (hiTail == <span class="literal">null</span>)</span><br><span class="line">                                    hiHead = e;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    hiTail.next = e;</span><br><span class="line">                                hiTail = e;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">while</span> ((e = next) != <span class="literal">null</span>);</span><br><span class="line">                        <span class="keyword">if</span> (loTail != <span class="literal">null</span>) &#123;</span><br><span class="line">                            loTail.next = <span class="literal">null</span>;</span><br><span class="line">                            newTab[j] = loHead;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">if</span> (hiTail != <span class="literal">null</span>) &#123;</span><br><span class="line">                            hiTail.next = <span class="literal">null</span>;</span><br><span class="line">                            newTab[j + oldCap] = hiHead;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> newTab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="get方法"><a href="#get方法" class="headerlink" title="get方法"></a>get方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> V <span class="title function_">get</span><span class="params">(Object key)</span> &#123;</span><br><span class="line">    Node&lt;K,V&gt; e;</span><br><span class="line">    <span class="keyword">return</span> (e = getNode(hash(key), key)) == <span class="literal">null</span> ? <span class="literal">null</span> : e.value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 传入key来计算出哈希值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">hash</span><span class="params">(Object key)</span> &#123;</span><br><span class="line">        <span class="type">int</span> h;</span><br><span class="line">        <span class="keyword">return</span> (key == <span class="literal">null</span>) ? <span class="number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	如果 table不是Null,并且长度是大于0的,能够根据 (n-1) &amp; hash 得出来的下标是在tab里面能获取到值得,才会进入逻辑代码,否则就是返回null.</span></span><br><span class="line"><span class="comment">	如果first的hash是于传入进来的hash相同,斌且给key的值也是相同的话,就会返回first节点.</span></span><br><span class="line"><span class="comment">	拿node的next节点,如果是TreeNode的类,就会走TreeNode对应的getTreeNode方法(链表的长度大于8就会转化为红黑树). 否则的话就就迭代这个Node,退出的条件就是 e.next == null,就说说明下面没有对应的节点了。</span></span><br><span class="line"><span class="comment">	这里拿值得逻辑,还是比较容易理解得。 先根据计算出来得hash值,去数组中是否可以获取到对应得值,如果有就先会对first进行判断,是否满足条件.如果不满足的话,就说明这个key的hash是由冲突的,也就是由二个不同的值,计算出来相同的hash值,这个时候就会用链表(Node)来进行存储,如果长度是大于8的话,就会转化为TreeNode的红黑树.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title function_">getNode</span><span class="params">(<span class="type">int</span> hash, Object key)</span> &#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="type">int</span> n; K k;</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != <span class="literal">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">            (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (first.hash == hash &amp;&amp; <span class="comment">// always check first node</span></span><br><span class="line">                ((k = first.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                <span class="keyword">return</span> first;</span><br><span class="line">            <span class="keyword">if</span> ((e = first.next) != <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                    <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                        ((k = e.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                        <span class="keyword">return</span> e;</span><br><span class="line">                &#125; <span class="keyword">while</span> ((e = e.next) != <span class="literal">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="isEmpty方法"><a href="#isEmpty方法" class="headerlink" title="isEmpty方法"></a>isEmpty方法</h3><p>isEmpty 方法,这里就直接使用 size &#x3D;&#x3D; 0 来进行判断,如果你的map是null的话,直接调用这个方法就会出现空指针</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isEmpty</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> size == <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>这里只是选用了 put 和 get方法来进行讲解,因为这二个是经常调用的,所以得明白是一个怎么样得大体流程走向才行</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>AbstractQueuedSynchronizer</title>
    <url>//article/java_aqs1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-基本介绍"><a href="#📝-基本介绍" class="headerlink" title="📝 基本介绍"></a>📝 <strong>基本介绍</strong></h1><p>AQS(队列同步器)，简称同步锁框架。</p>
<p>同步器是实现锁的关键，利用同步器将锁的语义实现，然后在锁的实现中聚合同步器。</p>
<p>AQS会将请求获取锁失败的线程放入一个队列的尾部：等待获取锁的线程全部处理阻塞状态。当前线程执行完毕（释放锁）后，会激活当前线程的后继节点。</p>
<ol>
<li>采用模板模式，tryacquire(), release()等方法需要子类覆写。实现了算法主体框架，供外部调用，里面会调用原语操作和钩子操作</li>
<li>原语操作：即定义的抽象方法，子类必须重写</li>
<li>钩子操作：与原语操作类似，也是供子类重写的。区别是钩子可以重写也可以不重写，如果不重写默认使用父类的</li>
<li>一般的lock类都实现Lock接口的，而内部会继承AQS，因为Lock是面向使用者，而AQS这个框架是锁的实现框架，使用者一般不会关注AQS</li>
</ol>
<h1 id="CLH队列"><a href="#CLH队列" class="headerlink" title="CLH队列"></a>CLH队列</h1><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>CLH锁其实就是一种基于队列(具体为单向链表)排队的自旋锁，由于是Craig、Landin和Hagersten三人一起发明的，因此被命名为CLH 锁，也叫CLH队列锁</p>
<p>简单的CLH锁可以基于单向链表实现，申请加锁的线程首先会通过 CAS操作在单向链表的尾部增加一个节点，之后该线程<strong>只需要在其前驱节点上进行普通自旋</strong>，等待前驱节点释放锁即可</p>
<p>由于CLH锁只有在节点入队时进行一下CAS的操作，在节点加入队列之后，抢锁线程不需要 进行CAS自旋，只需普通自旋即可。因此，在争用激烈的场景下，CLH 锁能大大减少CAS操作的数量，以避免CPU的总线风暴</p>
<p>声明一个node节点，locked是自身，myPred是前一个node节点的引用（组成一个单向链表），因为获取锁的线程，可能是多个，后节点会不断的自旋(<strong>普通自旋</strong>)自己的myPred指向的前一个node的Locked属性，当这个属性探测得到是false的时候，说明前驱节点已经释放了锁，自己应该去抢锁了</p>
<p><img src="/images/posts/java_aqs1/img-1.png" alt="AbstractQueuedSynchronizer"></p>
<h1 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h1><h2 id="重写方法"><a href="#重写方法" class="headerlink" title="重写方法"></a>重写方法</h2><table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>方法名称</td>
<td>描述</td>
</tr>
<tr>
<td>boolean tryAcquire(int arg)</td>
<td>独占式获取同步状态，成功返回true，失败返回false</td>
</tr>
<tr>
<td>boolean tryRelease(int arg)</td>
<td>独占式释放同步状态，成功返回true，失败返回false</td>
</tr>
<tr>
<td>int tryAcquireShared(int arg)</td>
<td>共享式获取同步状态，获取成功则返回值&gt;&#x3D;0</td>
</tr>
<tr>
<td>boolean tryReleaseShared(int arg)</td>
<td>共享式释放同步状态，成功返回true，失败返回false</td>
</tr>
<tr>
<td>boolean isHeldExclusively()</td>
<td>判断同步器是否在独占模式下被占用，一般用来表示同步器是否被当前线程占用</td>
</tr>
</tbody></table>
<h2 id="重写模板方法"><a href="#重写模板方法" class="headerlink" title="重写模板方法"></a>重写模板方法</h2><p>子类需要重写该方法，AQS中提供的模板方法才可以正常调用</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>方法名称</td>
<td>描述</td>
</tr>
<tr>
<td>void acquire(int arg)</td>
<td>独占式获取同步状态，该方法会调用子类重写的tryAcquire(int arg)，如果tryAcquire返回true则该方法直接返回，否则先将当前线程加入同步队列的尾部，然后阻塞当前线程</td>
</tr>
<tr>
<td>void acquireInterruptibly(int arg)</td>
<td>当线程获取同步状态失败被阻塞后，可以响应中断，收到中断后将会取消获取同步状态</td>
</tr>
<tr>
<td>boolean tryAcquireNanos(int arg, long nanosTimeout)</td>
<td>在acquireInterruptibly的基础上加了超时限制，如果在超时时间内获取到同步状态返回true，否则返回false</td>
</tr>
<tr>
<td>boolean release(int arg)</td>
<td>独占式释放同步状态，该方法会在释放同步状态后将第一个节点（对应刚刚释放同步状态的线程）的后继节点对应的线程唤醒</td>
</tr>
<tr>
<td>void acquireShared(int arg)</td>
<td>共享式获取同步状态，该方法会调用子类重写的tryAcquireShared(int arg)，如果tryAcquireShared返回true则该方法直接返回，否则先将当前线程加入同步队列的尾部，然后阻塞当前线程</td>
</tr>
<tr>
<td>void acquireSharedInterruptibly(int arg)</td>
<td>当线程获取同步状态失败被阻塞后，可以响应中断，收到中断后将会取消获取同步状态</td>
</tr>
<tr>
<td>boolean tryAcquireSharedNanos(int arg, long nanosTimeout)</td>
<td>在acquireSharedInterruptibly的基础上加了超时限制，如果在超时时间内获取到同步状态返回true，否则返回false</td>
</tr>
<tr>
<td>boolean releaseShared(int arg)</td>
<td>共享式的释放同步状态</td>
</tr>
</tbody></table>
<h2 id="State"><a href="#State" class="headerlink" title="State"></a>State</h2><p>volatile关键字进行修饰<br>0&#x3D;初始值，没有被线程占用<br>1&#x3D;锁已经被抢占（抢占之后调用unlock方法会释放锁，将state&#x3D;1重置为0</p>
<blockquote>
<p>1的情况说，说明锁被重入了。举例子：该值等于3，说明已经被重入了3次，最后递减state的时候也应该递减3次，重置为0的时候表示已经释放了锁</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* The synchronization state.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">long</span> state;</span><br></pre></td></tr></table></figure>

<h2 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">        <span class="comment">// 共享</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">SHARED</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>();</span><br><span class="line">        <span class="comment">//  独占</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">EXCLUSIVE</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="comment">//   当前节点被取消</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CANCELLED</span> <span class="operator">=</span>  <span class="number">1</span>;</span><br><span class="line">        <span class="comment">// 表示当前节点的后续节点将要或者已经被阻塞，在当前节点释放的时候，</span></span><br><span class="line">        <span class="comment">// 需要unpark唤醒后续节点</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">SIGNAL</span>    <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">        <span class="comment">// 表示当前节点在等待condition，即在condition队列中(在condition队列中)</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CONDITION</span> <span class="operator">=</span> -<span class="number">2</span>;</span><br><span class="line">        <span class="comment">// 表示releaseShared需要被传播给后续节点(仅在共享模式下使用)</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">PROPAGATE</span> <span class="operator">=</span> -<span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">    	<span class="comment">// 等待状态。默认0：无状态，表示当前节点在队列中等待获取锁</span></span><br><span class="line">        <span class="keyword">volatile</span> <span class="type">int</span> waitStatus;</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 上一个节点</span></span><br><span class="line">        <span class="keyword">volatile</span> Node prev;</span><br><span class="line"></span><br><span class="line">       <span class="comment">//  下一个节点</span></span><br><span class="line">        <span class="keyword">volatile</span> Node next;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 当前线程</span></span><br><span class="line">        <span class="keyword">volatile</span> Thread thread;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 存储 condition队列中的后续节点</span></span><br><span class="line">        Node nextWaiter;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="AQS的Node"><a href="#AQS的Node" class="headerlink" title="AQS的Node"></a>AQS的Node</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 头节点，指向队列的第一个节点</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node head;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 尾节点，指向队列的最后一个节点</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node tail;</span><br></pre></td></tr></table></figure>

<h1 id="FIFO队列"><a href="#FIFO队列" class="headerlink" title="FIFO队列"></a>FIFO队列</h1><p><img src="/images/posts/java_aqs1/img-2.png" alt="AbstractQueuedSynchronizer"></p>
<h2 id="AQS结构"><a href="#AQS结构" class="headerlink" title="AQS结构"></a>AQS结构</h2><p><img src="/images/posts/java_aqs1/img-3.png" alt="AbstractQueuedSynchronizer"></p>
<h2 id="AQS执行流程"><a href="#AQS执行流程" class="headerlink" title="AQS执行流程"></a><strong>AQS执行流程</strong></h2><p>node节点会通过CAS算法把自己变成读取到尾部node节点的下一个节点，不成功的继续自旋读取最新的尾节点再CAS</p>
<p><img src="/images/posts/java_aqs1/img-4.png" alt="AbstractQueuedSynchronizer"></p>
<p><img src="/images/posts/java_aqs1/img-5.png" alt="AbstractQueuedSynchronizer"></p>
<h2 id="acquire-方法"><a href="#acquire-方法" class="headerlink" title="acquire 方法"></a><strong>acquire 方法</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">acquire</span><span class="params">(<span class="type">long</span> arg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp;</span><br><span class="line">            acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">            selfInterrupt();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// tryAcquire 失败之后，addWaiter先快速加入尾部，</span></span><br><span class="line">   <span class="comment">// 失败的话进入enq方法，通过自旋的方式加入尾部</span></span><br><span class="line">    <span class="keyword">private</span> Node <span class="title function_">addWaiter</span><span class="params">(Node mode)</span> &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>(Thread.currentThread(), mode);</span><br><span class="line">        <span class="comment">// Try the fast path of enq; backup to full enq on failure</span></span><br><span class="line">        <span class="type">Node</span> <span class="variable">pred</span> <span class="operator">=</span> tail;</span><br><span class="line">        <span class="keyword">if</span> (pred != <span class="literal">null</span>) &#123;</span><br><span class="line">            node.prev = pred;</span><br><span class="line">            <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123;</span><br><span class="line">                pred.next = node;</span><br><span class="line">                <span class="keyword">return</span> node;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        enq(node);</span><br><span class="line">        <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 自旋，先看队列里有没有，没有就创建一个队列；有就通过cas加入队尾</span></span><br><span class="line">    <span class="keyword">private</span> Node <span class="title function_">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail;</span><br><span class="line">            <span class="keyword">if</span> (t == <span class="literal">null</span>) &#123; <span class="comment">// Must initialize</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> <span class="title class_">Node</span>()))</span><br><span class="line">                    tail = head;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                node.prev = t;</span><br><span class="line">                <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;</span><br><span class="line">                    t.next = node;</span><br><span class="line">                    <span class="keyword">return</span> t;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/java_aqs1/img-6.png" alt="AbstractQueuedSynchronizer"></p>
<h2 id="acquireQueued-方法"><a href="#acquireQueued-方法" class="headerlink" title="acquireQueued 方法"></a><strong>acquireQueued 方法</strong></h2><ol>
<li>node.predecessor 找到前一个节点，如果是head节点的话，就CAS获取锁（业务执行块的话，前面锁释放了，这时候尝试获取一些）</li>
<li>如果获取成功的话，当前节点设置头节点，将原来的head节点设置为null</li>
<li>如果1 和 2 不成功，走 shouldParkAfterFailedAcquire，检查自己是不是应该park，如果park之后，交给上一个线程去唤醒</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="type">long</span> arg)</span> &#123;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">failed</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">boolean</span> <span class="variable">interrupted</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">p</span> <span class="operator">=</span> node.predecessor();</span><br><span class="line">                <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</span><br><span class="line">                    setHead(node);</span><br><span class="line">                    p.next = <span class="literal">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    failed = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">return</span> interrupted;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                    parkAndCheckInterrupt())</span><br><span class="line">                    interrupted = <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (failed)</span><br><span class="line">                cancelAcquire(node);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="release-方法"><a href="#release-方法" class="headerlink" title="release 方法"></a>release 方法</h2><p>释放锁的时候，会唤起head节点的下一个节点，当后置节点拿到锁之后，会把head指向自己</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">release</span><span class="params">(<span class="type">long</span> arg)</span> &#123;</span><br><span class="line">        <span class="comment">// 释放锁</span></span><br><span class="line">        <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">            <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">            <span class="keyword">if</span> (h != <span class="literal">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">                <span class="comment">// 唤醒其它节点</span></span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//   释放锁后需要唤醒其它线程</span></span><br><span class="line"><span class="comment">//   Node node 传递进来是头节点</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">unparkSuccessor</span><span class="params">(Node node)</span> &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * If status is negative (i.e., possibly needing signal) try</span></span><br><span class="line"><span class="comment">         * to clear in anticipation of signalling.  It is OK if this</span></span><br><span class="line"><span class="comment">         * fails or if status is changed by waiting thread.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> node.waitStatus;</span><br><span class="line">        <span class="keyword">if</span> (ws &lt; <span class="number">0</span>)</span><br><span class="line">            compareAndSetWaitStatus(node, ws, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * Thread to unpark is held in successor, which is normally</span></span><br><span class="line"><span class="comment">         * just the next node.  But if cancelled or apparently null,</span></span><br><span class="line"><span class="comment">         * traverse backwards from tail to find the actual</span></span><br><span class="line"><span class="comment">         * non-cancelled successor.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="comment">// 判断头节点的下一个节点是否为空或者是取消状态，如果是，找其他节点</span></span><br><span class="line">        <span class="comment">// 如果不是, 直接唤醒下一个节点</span></span><br><span class="line">        <span class="type">Node</span> <span class="variable">s</span> <span class="operator">=</span> node.next;</span><br><span class="line">        <span class="keyword">if</span> (s == <span class="literal">null</span> || s.waitStatus &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            s = <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail; t != <span class="literal">null</span> &amp;&amp; t != node; t = t.prev)</span><br><span class="line">                <span class="keyword">if</span> (t.waitStatus &lt;= <span class="number">0</span>)</span><br><span class="line">                    s = t;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (s != <span class="literal">null</span>)</span><br><span class="line">            LockSupport.unpark(s.thread);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="http://ifeve.com/introduce-abstractqueuedsynchronizer/">http://ifeve.com/introduce-abstractqueuedsynchronizer/</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Java CAS理解</title>
    <url>//article/java_cas1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-介绍"><a href="#📝-介绍" class="headerlink" title="📝 介绍"></a>📝 <strong>介绍</strong></h1><p>锁在并发处理中占据一席之地，但是使用锁有一个不好的地方，就是当一个线程没有获取到锁的时候会被阻塞挂起来，这会导致线程上下文的切换和重新调度开销。<br>java提供了非阻塞volatile关键字来解决共享变量的可见性问题，这在一定程度上弥补了锁带来的开销问题，但是volatile只能保证共享可见的问题，不能解决读一改一写的原子性问题。CAS即CompareAndSwap（比较交换），即是JDK提供非阻塞原子性操作</p>
<h1 id="CAS机制"><a href="#CAS机制" class="headerlink" title="CAS机制"></a>CAS机制</h1><p>它包含3个参数CAS（V,E,N），V表示要更新变量的值，E表示预期值，N表示新值。仅当V值等于E值的时候，才会将V的值设置为N;如该V与E的值不一样，则说明已经有其他线程做了更新，则当前线程什么都做不了。最后，CAS返回当前V的真实值<br>比如看：AtomicInteger#getAndIncrement 的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">getAndIncrement</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> unsafe.getAndAddInt(<span class="built_in">this</span>, valueOffset, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>最后调用到 Unsafe 类中<br>var1:  AtomicInteger 这个对象a<br>var2:  偏移量(有效地址值)<br>var5:  AtomicInteger 这个对象a在地址var2上的期待的值<br>var5 +var4: 是值操作 + 1</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">getAndAddInt</span><span class="params">(Object var1, <span class="type">long</span> var2, <span class="type">int</span> var4)</span> &#123;</span><br><span class="line">        <span class="type">int</span> var5;</span><br><span class="line">        <span class="keyword">do</span> &#123;</span><br><span class="line">            var5 = <span class="built_in">this</span>.getIntVolatile(var1, var2);</span><br><span class="line">        &#125; <span class="keyword">while</span>(!<span class="built_in">this</span>.compareAndSwapInt(var1, var2, var5, var5 + var4));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> var5;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="底层原理"><a href="#底层原理" class="headerlink" title="底层原理"></a>底层原理</h1><p>接着上面的跟进,compareAndSwapInt方法:<br>var1: 操作对象<br>var2: 操作对象a的地址偏移量,有效地址<br>var4: 我们期待这个a是什么值<br>var5: 表示a的实际值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// native 原生方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="type">boolean</span> </span><br><span class="line">	<span class="title function_">compareAndSwapInt</span><span class="params">(Object var1, <span class="type">long</span> var2, <span class="type">int</span> var4, <span class="type">int</span> var5)</span>;</span><br></pre></td></tr></table></figure>

<p>再往下跟,C语言的代码<br>首先使用 jint 计算了 value 的地址值，然后根据这个地址，使用了 Atomic 的 cmpxchg 方法进行比较交换。将问题抛给了 cmpxchg</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">UNSAFE_ENTRY(jboolean, Unsafe_CompareAndSwapInt(JNIEnv *env, jobject unsafe, </span><br><span class="line">                                            jobject obj, jlong offset, jint e, jint x))</span><br><span class="line">  UnsafeWrapper(<span class="string">&quot;Unsafe_CompareAndSwapInt&quot;</span>);</span><br><span class="line">  <span class="type">oop</span> <span class="variable">p</span> <span class="operator">=</span> JNIHandles::resolve(obj);</span><br><span class="line">  <span class="comment">// 根据偏移量valueOffset，计算 value 的地址</span></span><br><span class="line">  jint* addr = (jint *) index_oop_from_field_offset_long(p, offset);</span><br><span class="line">  <span class="comment">// 调用 Atomic 中的函数 cmpxchg来进行比较交换</span></span><br><span class="line">  <span class="keyword">return</span> (jint)(Atomic::cmpxchg(x, addr, e)) == e;</span><br><span class="line">UNSAFE_END</span><br></pre></td></tr></table></figure>

<p>cmpxchg函数代码:<br>现在不同操作系统下会调用不同的 cmpxchg  重载函数</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">unsigned Atomic::cmpxchg(unsigned <span class="type">int</span> exchange_value,</span><br><span class="line">                         <span class="keyword">volatile</span> unsigned <span class="type">int</span>* dest, </span><br><span class="line">                         unsigned <span class="type">int</span> compare_value) &#123;</span><br><span class="line">    <span class="keyword">assert</span>(sizeof(unsigned <span class="type">int</span>) == sizeof(jint), <span class="string">&quot;more work to do&quot;</span>);</span><br><span class="line">  <span class="comment">/*</span></span><br><span class="line"><span class="comment">   * 根据操作系统类型调用不同平台下的重载函数，</span></span><br><span class="line"><span class="comment">     这个在预编译期间编译器会决定调用哪个平台下的重载函数</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line">    <span class="keyword">return</span> (unsigned <span class="type">int</span>)Atomic::cmpxchg((jint)exchange_value, </span><br><span class="line">                     (<span class="keyword">volatile</span> jint*)dest, (jint)compare_value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再往下跟<br>move 指令表示是将后面的值移动到前面的寄存器上，然后调用 LOCK_IF_MP 和 下面的 cmpxchg 汇编指令进行了比较交换。<br>cas指令最终由操作系统汇编指令完成</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">inline jint Atomic::cmpxchg (jint exchange_value, <span class="keyword">volatile</span> jint* dest, </span><br><span class="line">                            jint compare_value) &#123;</span><br><span class="line">  <span class="type">int</span> <span class="variable">mp</span> <span class="operator">=</span> os::is_MP();</span><br><span class="line">  __asm &#123;</span><br><span class="line">    mov edx, dest</span><br><span class="line">    mov ecx, exchange_value</span><br><span class="line">    mov eax, compare_value</span><br><span class="line">    <span class="title function_">LOCK_IF_MP</span><span class="params">(mp)</span></span><br><span class="line">    cmpxchg dword ptr [edx], ecx</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">inline jint Atomic::cmpxchg (jint exchange_value, </span><br><span class="line">                             <span class="keyword">volatile</span> jint* dest, jint compare_value) &#123;</span><br><span class="line">  <span class="comment">//1、 判断是否是多核 CPU</span></span><br><span class="line">  <span class="type">int</span> <span class="variable">mp</span> <span class="operator">=</span> os::is_MP();</span><br><span class="line">  __asm &#123;</span><br><span class="line">    <span class="comment">//2、 将参数值放入寄存器中</span></span><br><span class="line">    mov edx, dest   </span><br><span class="line">    mov ecx, exchange_value</span><br><span class="line">    mov eax, compare_value </span><br><span class="line">    <span class="comment">//3、LOCK_IF_MP指令</span></span><br><span class="line">    cmp mp, <span class="number">0</span></span><br><span class="line">    <span class="comment">//4、 如果 mp = 0，表明线程运行在单核CPU环境下。此时 je 会跳转到 L0 标记处，直接执行 cmpxchg 指令</span></span><br><span class="line">    je L0</span><br><span class="line">    _emit <span class="number">0xF0</span></span><br><span class="line"><span class="comment">//5、这里真正实现了比较交换</span></span><br><span class="line">    L0:</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 比较并交换。简单解释一下下面这条指令，熟悉汇编的朋友可以略过下面的解释:</span></span><br><span class="line"><span class="comment">     *   cmpxchg: 即“比较并交换”指令</span></span><br><span class="line"><span class="comment">     *   dword: 全称是 double word 表示两个字，一共四个字节</span></span><br><span class="line"><span class="comment">     *   ptr: 全称是 pointer，与前面的 dword 连起来使用，表明访问的内存单元是一个双字单元 </span></span><br><span class="line"><span class="comment">     * 这一条指令的意思就是：</span></span><br><span class="line"><span class="comment">            将 eax 寄存器中的值（compare_value）与 [edx] 双字内存单元中的值进行对比，</span></span><br><span class="line"><span class="comment">            如果相同，则将 ecx 寄存器中的值（exchange_value）存入 [edx] 内存单元中。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    cmpxchg dword ptr [edx], ecx</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="CAS优缺点"><a href="#CAS优缺点" class="headerlink" title="CAS优缺点"></a>CAS优缺点</h1><h1 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h1><p>cas是一种乐观锁，而且还是非阻塞的轻量级的乐观锁。</p>
<p>一个线程想要获取锁，对方会给一个回应表示这个锁能不能获得。</p>
<p>在资源竞争不激烈的情况下性能高，相对 synchronized 重量锁，synchronized会进行比较复杂的加锁，解锁和唤醒操作</p>
<h1 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h1><p>缺点也是一个非常重要的知识点，因为涉及到了一个非常著名的问题，叫做ABA问题。假设一个变量 A ，修改为 B之后又修改为 A，CAS 的机制是无法察觉的，但实际上已经被修改过了。这就是ABA问题，</p>
<p>ABA问题会带来大量的问题，比如说数据不一致的问题等等。我们可以举一个例子来解释说明。</p>
<p>你有一瓶水放在桌子上，别人把这瓶水喝完了，然后重新倒上去。你再去喝的时候发现水还是跟之前一样，就误以为是刚刚那杯水。如果你知道了真相，那是别人用过了你还会再用吗</p>
<h1 id="🤗-总结归纳s"><a href="#🤗-总结归纳s" class="headerlink" title="🤗 总结归纳s"></a>🤗 总结归纳s</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>源码分析</tag>
        <tag>使用入门</tag>
      </tags>
  </entry>
  <entry>
    <title>Java常见基础</title>
    <url>//article/java_common_basic/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Java基础巩固</p>
</blockquote>
<h1 id="📝-为什么重写-hashCode-方法也要重写-equals"><a href="#📝-为什么重写-hashCode-方法也要重写-equals" class="headerlink" title="📝 为什么重写 hashCode 方法也要重写 equals"></a>📝 为什么重写 hashCode 方法也要重写 equals</h1><p>在基于哈希实现的容器里，如果想使用某个类，不仅需要定义它的hashCode()方法，还需要定义equals()方法。这两个方法一起使用，才能实现对哈希容器的正确查找。</p>
<p>官方文档讲的很清楚，防止依赖hashcode的api出现错误，比如经典的set和map, 一般情况下是不需要的。</p>
<p>代码如下：</p>
<p>只要将 Person 对象中 hashCode 或者 equals 注释掉一个的话,set集合中的值就会是2</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">hashCode</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Objects.hash(name, age);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object o)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span> == o) <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 引用相等返回 true</span></span><br><span class="line">        <span class="comment">// 如果等于 null，或者对象类型不同返回 false</span></span><br><span class="line">        <span class="keyword">if</span> (o == <span class="literal">null</span> || getClass() != o.getClass()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="comment">// 强转为自定义 Persion 类型</span></span><br><span class="line">        <span class="type">Person</span> <span class="variable">persion</span> <span class="operator">=</span> (Person) o;</span><br><span class="line">        <span class="comment">// 如果 age 和 name 都相等，就返回 true</span></span><br><span class="line">        <span class="keyword">return</span> age == persion.age &amp;&amp;</span><br><span class="line">                Objects.equals(name, persion.name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PersonSet</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        Set&lt;Person&gt; personSet = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">Person</span> <span class="variable">baoyang</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;baoyang&quot;</span>, <span class="number">25</span>);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">baoyang1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;baoyang&quot;</span>, <span class="number">25</span>);</span><br><span class="line"></span><br><span class="line">        personSet.add(baoyang1);</span><br><span class="line">        personSet.add(baoyang);</span><br><span class="line"></span><br><span class="line">        System.out.println(personSet.size());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="📔-String"><a href="#📔-String" class="headerlink" title="📔 String"></a><strong>📔 String</strong></h1><h2 id="为什么String是final修饰"><a href="#为什么String是final修饰" class="headerlink" title="为什么String是final修饰"></a><strong>为什么String是final修饰</strong></h2><p><img src="/images/posts/java_common_basic/img-1.png" alt="Java常见基础"></p>
<ol>
<li>安全,如果是可变的,会被恶意去修改</li>
<li>hashCode也不会改变</li>
<li>这种设计可以去做字符串常量池</li>
</ol>
<ul>
<li>因为字符串常量池是共享的，多个地方可能会引用同一个字符串，如果string是可变的，一个地方把字符串改了，其他地方的引用就变成了被改之后的字符串了，这就出错了。</li>
</ul>
<h2 id="String的创建方法"><a href="#String的创建方法" class="headerlink" title="String的创建方法"></a>String的创建方法</h2><h3 id="string-str-“abc”"><a href="#string-str-“abc”" class="headerlink" title="string str &#x3D; “abc”"></a>s<strong>tring str &#x3D; “abc”</strong></h3><p>在字符串常量池里面存在的对象,直接拿来用就可以了,可能要创建多个的对象,变成只创建一个对象,更高效的使用内存,此时堆空间是没有对象的</p>
<p><img src="/images/posts/java_common_basic/img-2.png" alt="Java常见基础"></p>
<h3 id="String-str-new-“abc”"><a href="#String-str-new-“abc”" class="headerlink" title="String str &#x3D; new (“abc”);"></a><strong>String str &#x3D; new (“abc”);</strong></h3><p>这种创建方式会在堆中间会多一个对象,比较不划算</p>
<ul>
<li>如果没有abc 会创建两个对象，一个String 一个abc</li>
<li>如果有abc，只会创建一个对象</li>
</ul>
<p><img src="/images/posts/java_common_basic/img-3.png" alt="Java常见基础"></p>
<h3 id="象里面带String属性"><a href="#象里面带String属性" class="headerlink" title="象里面带String属性"></a><strong>象里面带String属性</strong></h3><p>直接放在对象里面，不会再常量池里面了</p>
<p><img src="/images/posts/java_common_basic/img-4.png" alt="Java常见基础"></p>
<h3 id="拼接式生成String"><a href="#拼接式生成String" class="headerlink" title="拼接式生成String"></a><strong>拼接式生成String</strong></h3><p>String是不可变的，不能拼接，只会生成多一个拼接后的数据存在常量池<br>相当于 str2 &#x3D; “abcdef” ;</p>
<p><img src="/images/posts/java_common_basic/img-5.png" alt="Java常见基础"></p>
<h3 id="for循环拼接"><a href="#for循环拼接" class="headerlink" title="for循环拼接"></a>for循环拼接</h3><p><img src="/images/posts/java_common_basic/img-6.png" alt="Java常见基础"></p>
<p>编译器会优化成如下图， 用StringBuilder.append();拼接</p>
<p><img src="/images/posts/java_common_basic/img-7.png" alt="Java常见基础"></p>
<h3 id="用Intern方法"><a href="#用Intern方法" class="headerlink" title="用Intern方法"></a><strong>用Intern方法</strong></h3><p>会检查这个字符串在常量池是否存在，如果存在的话直接返回<br>可以有效减少字符串的创建</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;baoyang&quot;</span>).intern();</span><br><span class="line">        <span class="type">String</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;baoyang&quot;</span>).intern();</span><br><span class="line"></span><br><span class="line">        System.out.println(a == b);</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">c</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;123&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">d</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;123&quot;</span>);</span><br><span class="line">        System.out.println(c == d);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="📔-代理"><a href="#📔-代理" class="headerlink" title="**📔**代理"></a>**📔**代理</h1><h3 id="Java静态代理"><a href="#Java静态代理" class="headerlink" title="Java静态代理"></a>Java静态代理</h3><p>代理对象和目标对象实现了相同的接口，目标对象作为代理对象的一个属性，具体接口实现中，代理对象可以在调用目标对象相应方法前后加上其他业务处理逻辑。<br>缺点：一个代理类只能代理一个业务类。如果业务类增加方法时，相应的代理类也要增加方法。</p>
<h2 id="Java动态代理"><a href="#Java动态代理" class="headerlink" title="Java动态代理"></a>Java动态代理</h2><p>Java动态代理是写一个类实现InvocationHandler接口，重写Invoke方法，在Invoke方法可以进行增强处理的逻辑的编写，这个公共代理类在运行的时候才能明确自己要代理的对象，同时可以实现该被代理类的方法的实现，然后在实现类方法的时候可以进行增强处理。<br>实际上：代理对象的方法 &#x3D; 增强处理 + 被代理对象的方法</p>
<h2 id="JDK和CGLIB生成动态代理类的区别："><a href="#JDK和CGLIB生成动态代理类的区别：" class="headerlink" title="JDK和CGLIB生成动态代理类的区别："></a><strong>JDK和CGLIB生成动态代理类的区别：</strong></h2><p>JDK动态代理只能针对实现了接口的类生成代理（实例化一个类）。此时代理对象和目标对象实现了相同的接口，目标对象作为代理对象的一个属性，具体接口实现中，可以在调用目标对象相应方法前后加上其他业务处理逻辑<br>CGLIB是针对类实现代理，主要是对指定的类生成一个子类（没有实例化一个类），覆盖其中的方法</p>
<h2 id="Spring-AOP应用场景"><a href="#Spring-AOP应用场景" class="headerlink" title="Spring AOP应用场景"></a><strong>Spring AOP应用场景</strong></h2><p>性能检测，访问控制，日志管理，事务等。<br>默认的策略是如果目标类实现接口，则使用JDK动态代理技术，如果目标对象没有实现接口，则默认会采用CGLIB代理</p>
<h1 id="静态代码块-非静态代码块"><a href="#静态代码块-非静态代码块" class="headerlink" title="静态代码块&#x2F;非静态代码块"></a>静态代码块&#x2F;非静态代码块</h1><h2 id="静态代码"><a href="#静态代码" class="headerlink" title="静态代码"></a>静态代码</h2><p>执行优先级高于非静态的初始化块，它会在类初始化的时候执行一次，它仅能初始化类变量，即<a href="https://so.csdn.net/so/search?q=static&spm=1001.2101.3001.7020">static</a>修饰的数据成员,  (<strong>随着类的加载而执行，而且只执行一次)</strong></p>
<h2 id="非静态代码"><a href="#非静态代码" class="headerlink" title="非静态代码"></a>非静态代码</h2><p>执行的时候如果有静态初始化块，先执行静态初始化块再执行非静态初始化块，</p>
<p><strong>在每个对象生成时都会被执行一次</strong>，它可以初始化类的实例变量。非静态初始化块会在构造函数执行时，在构造函数主体代码执行之前被运行</p>
<h2 id="执行顺序"><a href="#执行顺序" class="headerlink" title="执行顺序"></a>执行顺序</h2><p><strong>静态代码块 —–&gt; 非静态代码块 ——–&gt; 构造函数</strong></p>
<h1 id="📔-抽象和接口的区别"><a href="#📔-抽象和接口的区别" class="headerlink" title="**📔**抽象和接口的区别"></a>**📔**抽象和接口的区别</h1><h2 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h2><ol>
<li>可以有构造器</li>
<li>可以有抽象方法，和具体的方法</li>
<li>成员变量都是public的</li>
<li>抽象类可以定义成员变量</li>
<li>抽象类可以有静态方法</li>
<li>一个类只能继承一个抽象类</li>
</ol>
<h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><ol>
<li>不能有构造器</li>
<li>只能有抽象方法, 不能有实现方法</li>
<li>不能有静态方法</li>
<li>一个类可以实现多个接口</li>
<li>接口的成员变量都是常量</li>
</ol>
<h2 id="什么时候用抽象类，-什么时候用接口"><a href="#什么时候用抽象类，-什么时候用接口" class="headerlink" title="什么时候用抽象类， 什么时候用接口"></a><strong>什么时候用抽象类， 什么时候用接口</strong></h2><ol>
<li>抽象类是一个类，它的存在旨在被继承。它可以包含抽象方法、具体方法、实例变量等内容。</li>
<li>接口，共通事务之间的共通的特征&#x2F;功能，比如 fly，很多东西都会飞，能做出这些<strong>功能。接口是一组方法的集合，但是这些方法都没有具体的实现。接口通常用于定义一组事物需要什么，但是不关心它们需要用何种方式来完成。</strong></li>
</ol>
<h1 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>多态是在不同继承关系的类对象，去调用同一函数，产生了不同的行为。那么在继承中要构成多态的还有两个条件：</p>
<ol>
<li>必须通过基类的指针或者引用调用虚函数</li>
<li>被调用的函数必须是虚函数，且派生类必须对基类的虚函数进行重写</li>
</ol>
<p><img src="/images/posts/java_common_basic/img-8.png" alt="Java常见基础"></p>
<h2 id="虚函数"><a href="#虚函数" class="headerlink" title="虚函数"></a>虚函数</h2><p>在函数前面加上virtual关键字，变成虚函数，那么编译器在编译的时候就不能确定函数调用了</p>
<h2 id="多态分类"><a href="#多态分类" class="headerlink" title="多态分类"></a>多态分类</h2><ul>
<li>静态多态：函数重载和运算符重载属于静态多态，复用函数名</li>
<li>动态多态：派生类和虚函数实现运行时多态</li>
</ul>
<p><strong>调用虚函数时，根据对象的实际类型，在虚方法表中查找函数的地址，并进行调用。</strong></p>
<h2 id="多态底层实现"><a href="#多态底层实现" class="headerlink" title="多态底层实现"></a>多态底层实现</h2><p>父类中写了一个虚函数后，内部便有个一个虚函数指针vfptr,虚函数指针会指向一个虚函数表vftable，表的内部记录的是虚函数</p>
<p>当发生继承时（此处默认为public继承），子类将继承父类的所有元素。</p>
<p>因此，子类也就继承了父类中的虚函数指针，此时子类和父类完全相同，子类和父类中的虚函数指针指向的都是父类中虚函数地址。但是当子类重写了父类中的虚函数后，子类中的虚函数表内部会替换成子类的虚函数地址，父类中的虚函数指针不发生变化。当父类的指针或引用指向子类对象时，将发生多态。因此，当上述函数（即参数为父类指针或引用）的参数指向子类时，将从子类的虚函数表中找到虚函数的地址，此时读取的是子类中的虚函数。由此形成动态的运行中确定函数地址</p>
<h1 id="同步-异步"><a href="#同步-异步" class="headerlink" title="同步&#x2F;异步"></a>同步&#x2F;异步</h1><h2 id="同步"><a href="#同步" class="headerlink" title="同步"></a>同步</h2><p>应用程序开发非常简单；<strong>在阻塞等待数据期间，用户线程挂起，基本不会占用CPU资源</strong></p>
<p><strong>同步调用虽然让系统间只耦合于接口</strong>，而且实时性也会比异步调用要高</p>
<p>但是我们也需要知道同步调用会带来如下几个问题:</p>
<ol>
<li><strong>同步调用需要被调用方的吞吐不低于调用方的吞吐</strong>。否则会导致被调用方因为性能不足而拖死调用方。换句话说，整个同步调用链的性能会由最慢的那个服务所决定。</li>
<li><strong>同步调用会导致调用方一直在等待被调用方完成</strong>，<strong>如果一层接一层地同步调用下去，所有的参与方会有相同的等待时间</strong>。这会非常消耗调用方的资源。因为调用方需要保存现场（Context）等待远端返回，所以对于并发比较高的场景来说，这样的等待可能会极度消耗资源。</li>
<li><strong>同步调用只能是一对一的，很难做到一对多</strong>。</li>
<li><strong>同步调用最不好的是，如果被调用方有问题，那么其调用方就会跟着出问题</strong>，于是会出现多米诺骨牌效应，故障一下就蔓延开来</li>
</ol>
<h2 id="异步"><a href="#异步" class="headerlink" title="异步"></a>异步</h2><h3 id="请求响应式"><a href="#请求响应式" class="headerlink" title="请求响应式"></a>请求响应式</h3><p>在这种情况下，发送方（sender）会直接请求接收方（receiver），被请求方接收到请求后，直接返回——收到请求，正在处理。</p>
<ul>
<li>对于返回结果，有两种方法，一种是发送方时不时地去轮询一下，问一下干没干完。</li>
<li>另一种方式是发送方注册一个回调方法，也就是接收方处理完后回调请求方</li>
</ul>
<h3 id="通过订阅的方式"><a href="#通过订阅的方式" class="headerlink" title="通过订阅的方式"></a>通过订阅的方式</h3><p>这种情况下，接收方（receiver）会来订阅发送方（sender）的消息，发送方会把相关的消息或数据放到接收方所订阅的队列中，而接收方会从队列中获取数据。这种方式下，发送方并不关心订阅方的处理结果，它只是告诉订阅方有事要干，收完消息后给个 ACK 就好了，你干成啥样我不关心</p>
<h3 id="Broker的方式"><a href="#Broker的方式" class="headerlink" title="Broker的方式"></a>Broker的方式</h3><p>谓 Broker，就是一个中间人，发送方（sender）和接收方（receiver）都互相看不到对方，它们看得到的是一个 Broker，发送方向 Broker 发送消息，接收方向 Broker 订阅消息。如下图所示</p>
<p><img src="/images/posts/java_common_basic/img-9.png" alt="Java常见基础"></p>
<p>在 Broker 这种模式下，发送方的服务和接收方的服务最大程度地解耦。但是所有人都依赖于一个总线，所以这个总线就需要有如下的特性：</p>
<ol>
<li>必须是高可用的，因为它成了整个系统的关键；</li>
<li>必须是高性能而且是可以水平扩展的；</li>
<li>必须是可以持久化不丢数据的。</li>
</ol>
<h2 id="阻塞和非阻塞的区别"><a href="#阻塞和非阻塞的区别" class="headerlink" title="阻塞和非阻塞的区别"></a>阻塞和非阻塞的区别</h2><ol>
<li><strong>阻塞</strong>是指用户进程（或者线程）一直在等待，而不能做别的事情；</li>
<li><strong>非阻塞</strong>是指用户进程（或者线程）获得内核返回的状态值就返回自己的空间，可以去做别的事情。在Java中，非阻塞IO的socket被设置为NONBLOCK模式</li>
</ol>
<h1 id="Cookie和Session区别"><a href="#Cookie和Session区别" class="headerlink" title="Cookie和Session区别"></a>Cookie和Session区别</h1><h2 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h2><p>Cookie可以让服务端跟踪每个客户端的访问，但是每次客户端的访问都必须传回这些Cookie，如果Cookie很多，则无形的增加了客户端与服务端的数据传输量</p>
<h2 id="Session"><a href="#Session" class="headerlink" title="Session"></a>Session</h2><p>Session则很好地解决了这个问题，同一个客户端每次和服务端交互时，将数据存储通过Session到服务端，不需要每次都传回所有的Cookie值，而是传回一个ID，每个客户端第一次访问服务器生成的唯一的ID，客户端只要传回这个ID就行了，这个ID通常为NAME为JSESSIONID的一个Cookie。这样服务端就可以通过这个ID，来将存储到服务端的KV值取出了</p>
<h1 id="过滤器和拦截器区别"><a href="#过滤器和拦截器区别" class="headerlink" title="过滤器和拦截器区别"></a>过滤器和拦截器区别</h1><h2 id="过滤器"><a href="#过滤器" class="headerlink" title="过滤器"></a>过滤器</h2><p>用于在请求进入应用程序之前或响应离开应用程序之后对请求和响应进行处理的组件.过滤器通常用于实现对请求和响应进行预处理和后处理的逻辑，例如身份验证、日志记录、请求参数处理等。过滤器在请求的前后都可以进行操作，对整个请求进行处理</p>
<p>应用场景:</p>
<ol>
<li>身份验证和授权：验证用户身份，并根据权限授予访问权限。</li>
<li>请求日志记录：记录请求和响应的详细信息，以进行故障排查和监控。</li>
<li>请求参数处理：解析和处理请求参数，例如解码、转换等。</li>
<li>跨域资源共享（CORS）：实施跨域请求的安全策略。</li>
</ol>
<h2 id="拦截器"><a href="#拦截器" class="headerlink" title="拦截器"></a>拦截器</h2><p>用于拦截并处理请求和响应的组件</p>
<p>拦截器通常与特定框架或应用程序的执行流程相关联，用于在请求到达控制器（或处理程序）之前或离开控制器之后执行某些操作</p>
<p>应用场景:</p>
<ol>
<li>日志记录：记录请求和响应的关键信息，用于审计和故障排查。</li>
<li>事务管理：管理数据库事务的开始、提交和回滚。</li>
<li>缓存管理：在控制器执行前检查缓存，如果缓存中有相应的数据，则直接返回，提高性能。</li>
<li>性能监控：记录请求的执行时间和资源消耗，用于性能分析和优化。</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>使用入门</tag>
      </tags>
  </entry>
  <entry>
    <title>Java反射学习</title>
    <url>//article/java_reflect1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 <strong>题记</strong></h1><p>最近在看Spring源码的时候, 可以看到在加载类等的时候,都是大量使用的反射。估摸着MyBatis这种框架,其内部也是会大量的使用反射。所以看得出来反射在第三方的框架中使用是非常多的,于是说学习反射技术是很有必要的，不论是你写代码造轮子还是去理解第三方框架的底层实现.</p>
<p>话不多bb,直接上代码看看是个什么操作.</p>
<h1 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h1><p>我们这里写一个简单的pojo类,也就是我们经常使用的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">	<span class="keyword">public</span> String age;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">User</span><span class="params">()</span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">User</span><span class="params">(Integer id, String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> Integer <span class="title function_">getId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setId</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">say</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;User说&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后接着在写一个启动的Main类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserClazzMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">u</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        <span class="comment">// 获取类的所有的构造方法</span></span><br><span class="line">        Constructor&lt;?&gt;[] constructors = u.getClass().getConstructors();</span><br><span class="line">        System.out.println(Arrays.asList(constructors).toString());</span><br><span class="line">        <span class="comment">// 根据传入进去的参数类型,获取出类的构造方法.</span></span><br><span class="line">        Constructor&lt;? <span class="keyword">extends</span> <span class="title class_">User</span>&gt; constructor = u.getClass().getConstructor(Integer.class, String.class);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 打印结果:[public com.iyang.bootbasicio.pojo.User(), public com.iyang.bootbasicio.pojo.User(java.lang.Integer,java.lang.String)],</span></span><br><span class="line"><span class="comment">//可以看到这是一个获取类的构造方法的.</span></span><br></pre></td></tr></table></figure>

<p>调用反射获取方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserClazzMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">u</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        <span class="comment">// 可以看到获取出来的全部方法,不仅仅包含我们写的,还有Object中的notify等方法.</span></span><br><span class="line">        Method[] methods = u.getClass().getMethods();</span><br><span class="line">        System.out.println(Arrays.asList(methods).toString());</span><br><span class="line">        <span class="comment">// 根据方法的名字来过去我们特定的方法.</span></span><br><span class="line">        <span class="type">Method</span> <span class="variable">method</span> <span class="operator">=</span> u.getClass().getMethod(<span class="string">&quot;say&quot;</span>);</span><br><span class="line">        System.out.println(method.toString());</span><br><span class="line">        </span><br><span class="line">        <span class="comment">//getDeclaredMethods方法仅仅只获取除了我们这个类里面的方法,并没有获取全部的方法(也就是不包括Object的).</span></span><br><span class="line">        Method[] declaredMethods = u.getClass().getDeclaredMethods();</span><br><span class="line">        System.out.println(Arrays.asList(declaredMethods).toString());</span><br><span class="line">        <span class="comment">// 这里依然是获取我们自己写的方法</span></span><br><span class="line">        <span class="type">Method</span> <span class="variable">say</span> <span class="operator">=</span> u.getClass().getDeclaredMethod(<span class="string">&quot;say&quot;</span>);</span><br><span class="line">        System.out.println(say.toString());</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 这样我们就可以调用了user的say方法</span></span><br><span class="line">        <span class="type">Method</span> <span class="variable">say</span> <span class="operator">=</span> u.getClass().getDeclaredMethod(<span class="string">&quot;say&quot;</span>);</span><br><span class="line">        say.invoke(u);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">[<span class="keyword">public</span> <span class="keyword">void</span> com.iyang.bootbasicio.pojo.User.setId(java.lang.Integer), <span class="keyword">public</span> <span class="keyword">void</span> com.iyang.bootbasicio.pojo.User.say(), <span class="keyword">public</span> java.lang.String com.iyang.bootbasicio.pojo.User.getName(), <span class="keyword">public</span> <span class="keyword">void</span> com.iyang.bootbasicio.pojo.User.setName(java.lang.String), <span class="keyword">public</span> java.lang.Integer com.iyang.bootbasicio.pojo.User.getId(), <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> java.lang.Object.wait(<span class="type">long</span>,<span class="type">int</span>) <span class="keyword">throws</span> java.lang.InterruptedException, <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> java.lang.Object.wait(<span class="type">long</span>) <span class="keyword">throws</span> java.lang.InterruptedException, <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> java.lang.Object.wait() <span class="keyword">throws</span> java.lang.InterruptedException, <span class="keyword">public</span> <span class="type">boolean</span> java.lang.Object.equals(java.lang.Object), <span class="keyword">public</span> java.lang.String java.lang.Object.toString(), <span class="keyword">public</span> <span class="keyword">native</span> <span class="type">int</span> java.lang.Object.hashCode(), <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> java.lang.Class java.lang.Object.getClass(), <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> java.lang.Object.notify(), <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> java.lang.Object.notifyAll()]</span><br><span class="line">----------------------------</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> com.iyang.bootbasicio.pojo.User.say()</span><br></pre></td></tr></table></figure>

<p>调用获取字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserClazzMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">u</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        <span class="comment">//获取全部的字段,是public修饰的字段,private修饰的是获取不到的.</span></span><br><span class="line">        Field[] fields = u.getClass().getFields();</span><br><span class="line">        System.out.println(Arrays.asList(fields).toString());</span><br><span class="line">        <span class="comment">//获取字段,私有的不能获取,会抛出异常,只能获取public修饰的字段.</span></span><br><span class="line">        <span class="type">Field</span> <span class="variable">field</span> <span class="operator">=</span> u.getClass().getField(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">        System.out.println(field.toString());</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取全部的字段,private修饰的也是可以获取出来的</span></span><br><span class="line">        Field[] declaredFields = u.getClass().getDeclaredFields();</span><br><span class="line">        System.out.println(Arrays.asList(declaredFields).toString());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 根据字段的名字获取字段,不管什么修饰的,都是可以获取出来的.</span></span><br><span class="line">        <span class="type">Field</span> <span class="variable">name</span> <span class="operator">=</span> u.getClass().getDeclaredField(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        System.out.println(name.toString());</span><br><span class="line">       &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>获取注解:</p>
<p>我们先定义二个注解, 然后记得加在User类上. GavinYang 和 PeterWong 这二个注解是可以加在类上的, GavinYangFiledAnno是加在字段上面的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(&#123;ElementType.TYPE&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> GavinYang &#123;</span><br><span class="line"></span><br><span class="line">    String <span class="title function_">lwf</span><span class="params">()</span> <span class="keyword">default</span> <span class="string">&quot;lwf&quot;</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(&#123;ElementType.TYPE&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> PeterWong &#123;</span><br><span class="line"></span><br><span class="line">    String <span class="title function_">name</span><span class="params">()</span> <span class="keyword">default</span> <span class="string">&quot;peterWong&quot;</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(&#123;ElementType.FIELD&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> GavinYangFiledAnno &#123;</span><br><span class="line">    String <span class="title function_">desc</span><span class="params">()</span> <span class="keyword">default</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">// 从结果来看,获取注解还是蛮顺利的</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">u</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        <span class="comment">// 获取这个类上面的注解</span></span><br><span class="line">        Annotation[] annotations = u.getClass().getAnnotations();</span><br><span class="line">        System.out.println(Arrays.asList(annotations).toString());</span><br><span class="line">        <span class="comment">// 根据注解名字获取,可以看到返回的也直接是注解的Obejct了</span></span><br><span class="line">        <span class="type">GavinYang</span> <span class="variable">gavinYang</span> <span class="operator">=</span> u.getClass().getAnnotation(GavinYang.class);</span><br><span class="line">        System.out.println(gavinYang.toString());</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取字段上面使用的注解.</span></span><br><span class="line">        <span class="type">Field</span> <span class="variable">field</span> <span class="operator">=</span> u.getClass().getDeclaredField(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        Annotation[] fieldAnnotations = field.getAnnotations();</span><br><span class="line">        System.out.println(Arrays.asList(fieldAnnotations).toString());</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      </span><br><span class="line">[<span class="meta">@com</span>.iyang.bootbasicio.pojo.GavinYang(lwf=baoyang), <span class="meta">@com</span>.iyang.bootbasicio.pojo.PeterWong(name=gavinyang)]</span><br><span class="line"><span class="meta">@com</span>.iyang.bootbasicio.pojo.GavinYang(lwf=baoyang)     </span><br><span class="line">[<span class="meta">@com</span>.iyang.bootbasicio.pojo.GavinYangFiledAnno(desc=秒啊)]</span><br></pre></td></tr></table></figure>

<h1 id="小战一下"><a href="#小战一下" class="headerlink" title="小战一下"></a>小战一下</h1><p>UserService 无参构造方法 User说使用依赖注入完成一个简单的注入</p>
<p>认一下Spring写一个差不多的注入注解. 在写一个虚假的 UserService,当然了,我们这里先不使用扫描,就使用简单的UserServcie去操作即可.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(&#123;ElementType.FIELD&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> GavinYangAutowired &#123;</span><br><span class="line"></span><br><span class="line">    String <span class="title function_">alias</span><span class="params">()</span> <span class="keyword">default</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GavinYangAutowired</span></span><br><span class="line">    <span class="keyword">private</span> User user;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">UserService</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;UserService 无参构造方法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">()</span>&#123;</span><br><span class="line">        user.say();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>UserClassMain 类来启动发车 , 这里看结果是可以成功的启动 UserService 并且也是用hello方法来调用user的say,都是成功. 但是真实的框架复杂程度不是这几行就可以ok了的. 这只是一个简单易学的demo.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserClazzMain</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">UserService</span> <span class="variable">u</span>  <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserService</span>();</span><br><span class="line">        Field[] fields = u.getClass().getDeclaredFields();</span><br><span class="line">        <span class="keyword">for</span>(Field f : fields)&#123;</span><br><span class="line">            <span class="type">GavinYangAutowired</span> <span class="variable">autowired</span> <span class="operator">=</span> f.getDeclaredAnnotation(GavinYangAutowired.class);</span><br><span class="line">            <span class="keyword">if</span>(autowired != <span class="literal">null</span>)&#123;</span><br><span class="line">                Class&lt;?&gt; fType = f.getType();</span><br><span class="line">                Constructor&lt;?&gt; typeConstructor = fType.getConstructor();</span><br><span class="line">                <span class="type">Object</span> <span class="variable">instance</span> <span class="operator">=</span> typeConstructor.newInstance();</span><br><span class="line"></span><br><span class="line">                f.setAccessible(<span class="literal">true</span>);</span><br><span class="line">                f.set(u,instance);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        u.hello();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// UserService 无参构造方法</span></span><br><span class="line"><span class="comment">// User说</span></span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>可以看到反射的功能还是蛮强大的, 但是项目里面一般是CRUD,目前也没有什么特别的地方看到使用反射的情况比较多.就是最近一直看Spring源码中,是可以看到有大量使用反射的情况.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>使用入门</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedList源码分析</title>
    <url>//article/linkiedList_code/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-LinkedList源码分析"><a href="#📝-LinkedList源码分析" class="headerlink" title="📝 LinkedList源码分析"></a>📝 <strong>LinkedList源码分析</strong></h1><p>虽然一般都是使用ArrayList集合比使用LinkedList集合要多,但是这并不妨碍我们对LinkedList的源码研究和学习</p>
<h2 id="数据结构和参数"><a href="#数据结构和参数" class="headerlink" title="数据结构和参数"></a>数据结构和参数</h2><p>LinkedList 是一个双向链表的结构,这点可以直接看其内部内就可以非常明显的看出来. 静态私有的内部类,只提供一个构造函数</p>
<p>头节点的上一个指针是Null;尾节点的下一个指针是Null</p>
<p>如果头节点和尾节点都是null的话,则说明该LinkedList只有一个元素</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;E&gt; &#123;</span><br><span class="line">    E item;</span><br><span class="line">    Node&lt;E&gt; next;</span><br><span class="line">    Node&lt;E&gt; prev;</span><br><span class="line"></span><br><span class="line">    Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;</span><br><span class="line">        <span class="built_in">this</span>.item = element;</span><br><span class="line">        <span class="built_in">this</span>.next = next;</span><br><span class="line">        <span class="built_in">this</span>.prev = prev;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/linkiedList_code/img-1.png" alt="LinkedList源码分析"></p>
<p>然后我们看 LinkedList 自身的变量. size 肯定是记录这个链表的长度,不然到时候node.next.next….获取长度就很得不偿失了. 然后记录了一个头节点和尾节点，个人认为这是方便遍历。从头开始遍历就从first节点获取,从尾部开始遍历的话,就从last开始获取</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">transient</span> <span class="type">int</span> <span class="variable">size</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Pointer to first node.</span></span><br><span class="line"><span class="comment"> * Invariant: (first == null &amp;&amp; last == null) ||</span></span><br><span class="line"><span class="comment"> *            (first.prev == null &amp;&amp; first.item != null)</span></span><br><span class="line"><span class="comment"> *  记录头节点</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">transient</span> Node&lt;E&gt; first;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Pointer to last node.</span></span><br><span class="line"><span class="comment"> * Invariant: (first == null &amp;&amp; last == null) ||</span></span><br><span class="line"><span class="comment"> *            (last.next == null &amp;&amp; last.item != null)</span></span><br><span class="line"><span class="comment"> * 记录尾节点</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">transient</span> Node&lt;E&gt; last;</span><br></pre></td></tr></table></figure>

<h2 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h2><h3 id="add-E-e-方法"><a href="#add-E-e-方法" class="headerlink" title="add(E e)方法"></a>add(E e)方法</h3><p>add方法调用一个linkLast方法,然后就返回true了. 也就是说add(E e)就是默认从尾部开始插入元素进去</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">add</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    linkLast(e);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Links e as last element.</span></span><br><span class="line"><span class="comment">       翻译 : 链接e作为最后一个元素。</span></span><br><span class="line"><span class="comment">       先对last赋值给 Node&lt;E&gt; l , 然后调用new Node&lt;&gt;(l,e,null);传入进去的上个节点,也就是l,上次保存的尾部节点,也就是从倒数第一变为了倒数二,这样理解。然后此时的newNode就是尾节点了,然后赋值给last,因为last每次记录的都是尾节点.</span></span><br><span class="line"><span class="comment">       if else 中是对之前的尾节点进行判断,如果是null的话,说明此时就是添加的第一个元素,first也赋值给newNode,否则的话,l.next 和 尾节点进行关联。</span></span><br><span class="line"><span class="comment">       size 长度加一</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">linkLast</span><span class="params">(E e)</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; l = last;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(l, e, <span class="literal">null</span>);</span><br><span class="line">        last = newNode;</span><br><span class="line">        <span class="keyword">if</span> (l == <span class="literal">null</span>)</span><br><span class="line">            first = newNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            l.next = newNode;</span><br><span class="line">        size++;</span><br><span class="line">        modCount++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>方法解析流程如下</p>
<p><img src="/images/posts/linkiedList_code/img-2.png" alt="LinkedList源码分析"></p>
<h3 id="add-int-index-E-element-根据下标添加"><a href="#add-int-index-E-element-根据下标添加" class="headerlink" title="add(int index,E element)根据下标添加"></a>add(int index,E element)根据下标添加</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	1: 检查传入进来的下标是否越界了,如果下标越界的话,就会抛出 下标越界的异常</span></span><br><span class="line"><span class="comment">	2: 根据传入进来的下标值,判断是否和 size 相等,如果是相等的话,就说明是尾部插入,就不需要挨个迭代去获取对应的下标值对应的节点.满足条件,就会调用上面说到的 linkLast方法</span></span><br><span class="line"><span class="comment">	3: 不满足条件2的话,就会走lineBefore()方法,其中也调用到了.传入下标调用node方法.node会返回对应下标的值,根据返回的节点和当前的值调用lienkBefore方法.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> index, E element)</span> &#123;</span><br><span class="line">    checkPositionIndex(index);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (index == size)</span><br><span class="line">        linkLast(element);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        linkBefore(element, node(index));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">checkPositionIndex</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!isPositionIndex(index))</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IndexOutOfBoundsException</span>(outOfBoundsMsg(index));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">isPositionIndex</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> index &gt;= <span class="number">0</span> &amp;&amp; index &lt;= size;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	size &gt;&gt; 1 ; 是对 size 进行去半, 比如 6 &gt;&gt; 1 是 3， 5 &gt;&gt; 1 是2</span></span><br><span class="line"><span class="comment">	如果小于一半的话,就会从first节点开始遍历,也就是从头节点开始遍历,否则就是从尾节点开始遍历.</span></span><br><span class="line"><span class="comment">	这个方法可以看到,从头开始遍历的话,就是调用的next,如果尾部遍历的话,调用的就是prev。找到对应下标的节点并且返回回去.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">Node&lt;E&gt; <span class="title function_">node</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="comment">// assert isElementIndex(index);</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (index &lt; (size &gt;&gt; <span class="number">1</span>)) &#123;</span><br><span class="line">            Node&lt;E&gt; x = first;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; index; i++)</span><br><span class="line">                x = x.next;</span><br><span class="line">            <span class="keyword">return</span> x;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            Node&lt;E&gt; x = last;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> size - <span class="number">1</span>; i &gt; index; i--)</span><br><span class="line">                x = x.prev;</span><br><span class="line">            <span class="keyword">return</span> x;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	获取节点的上个节点赋值给pred，其实类似于pred这种,都是用于变量替换创建出来的.</span></span><br><span class="line"><span class="comment">	上一个节点,当前值e,succ节点来new一个新的节点出来.</span></span><br><span class="line"><span class="comment">	succ.prev 指向当前new出来的节点</span></span><br><span class="line"><span class="comment">	对pred判断是否是null,如果是null的话,就说明是第一个值,否则就是赋值上pred个节点的next</span></span><br><span class="line"><span class="comment">	,size ++ 就是对长度 ++ </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">linkBefore</span><span class="params">(E e, Node&lt;E&gt; succ)</span> &#123;</span><br><span class="line">        <span class="comment">// assert succ != null;</span></span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; pred = succ.prev;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(pred, e, succ);</span><br><span class="line">        succ.prev = newNode;</span><br><span class="line">        <span class="keyword">if</span> (pred == <span class="literal">null</span>)</span><br><span class="line">            first = newNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            pred.next = newNode;</span><br><span class="line">        size++;</span><br><span class="line">        modCount++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>代码逻辑如下图:</p>
<p><img src="/images/posts/linkiedList_code/img-3.png" alt="LinkedList源码分析"></p>
<h3 id="addFirst-E-e-头插入"><a href="#addFirst-E-e-头插入" class="headerlink" title="addFirst(E e)头插入"></a>addFirst(E e)头插入</h3><p>将值插入到头部</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    linkFirst(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	先将first 赋值给 f ,  根据传入进来的值e 和 下一个节点f(前一个头节点),new一个新的newNode节点出来,first指向newNode.如果f是null的话就说明是初始化,如果不是null的话,f的上一个节点指向newNode,刚刚程序newNode出来的.就完成了头节点的插入</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">linkFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(<span class="literal">null</span>, e, f);</span><br><span class="line">        first = newNode;</span><br><span class="line">        <span class="keyword">if</span> (f == <span class="literal">null</span>)</span><br><span class="line">            last = newNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            f.prev = newNode;</span><br><span class="line">        size++;</span><br><span class="line">        modCount++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>代码逻辑如下</p>
<p><img src="/images/posts/linkiedList_code/img-4.png" alt="LinkedList源码分析"></p>
<h3 id="addLast-E-尾插入"><a href="#addLast-E-尾插入" class="headerlink" title="addLast(E)尾插入"></a>addLast(E)尾插入</h3><p>与头节点相似，也是利用变量last来实现尾部插入</p>
<p>可参考上面的头插入图,也就是将头的判断和指针的指向倒过来,换成尾就是的了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addLast</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    linkLast(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">linkLast</span><span class="params">(E e)</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; l = last;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(l, e, <span class="literal">null</span>);</span><br><span class="line">        last = newNode;</span><br><span class="line">        <span class="comment">// 如果尾节点没有,则说明原的linkedList是没有元素的</span></span><br><span class="line">        <span class="keyword">if</span> (l == <span class="literal">null</span>)</span><br><span class="line">            first = newNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        <span class="comment">// 原来的last的next指向新new出来的node</span></span><br><span class="line">            l.next = newNode;</span><br><span class="line">        <span class="comment">// 长度和迭代的标记都+1</span></span><br><span class="line">        size++;</span><br><span class="line">        modCount++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="get方法"><a href="#get方法" class="headerlink" title="get方法"></a>get方法</h3><p>根据下标来获取出值 ,然后调用node方法获取出节点,node.item就是我们需要的值,然后对其进行返回即可</p>
<p>参考下标添加中的图,有个node(index)的方法,get也是走的node(index)去获取的node节点;再次之前,也会进行一次index是否越界的判断</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    checkElementIndex(index);</span><br><span class="line">    <span class="keyword">return</span> node(index).item;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果输入index是小于0和大于size的话,就会爆出下标越界的错误.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">checkElementIndex</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!isElementIndex(index))</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IndexOutOfBoundsException</span>(outOfBoundsMsg(index));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="getFirst-getLast方法"><a href="#getFirst-getLast方法" class="headerlink" title="getFirst &#x2F; getLast方法"></a>getFirst &#x2F; getLast方法</h3><p>可以看到first和 last都是直接从定义的变量中获取出对应的值;分别从头节点和尾节点的值获取</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">getFirst</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">    <span class="keyword">if</span> (f == <span class="literal">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NoSuchElementException</span>();</span><br><span class="line">    <span class="keyword">return</span> f.item;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> E <span class="title function_">getLast</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; l = last;</span><br><span class="line">        <span class="keyword">if</span> (l == <span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NoSuchElementException</span>();</span><br><span class="line">        <span class="keyword">return</span> l.item;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="peek方法"><a href="#peek方法" class="headerlink" title="peek方法"></a>peek方法</h3><p>使用first节点,如果是null的话就会返回null，否则就是f.item. 这里是没有删除first元素,poll是弹出元素并且删除</p>
<p>与 getFirst 逻辑一致,只是这里如果first是null的话就返回null,而getFirst是null的话,则会抛出异常</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">peek</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">    <span class="keyword">return</span> (f == <span class="literal">null</span>) ? <span class="literal">null</span> : f.item;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="peekFirst和peekLast方法"><a href="#peekFirst和peekLast方法" class="headerlink" title="peekFirst和peekLast方法"></a>peekFirst和peekLast方法</h3><p>peekFirst: 获取头节点元素;其实和peek逻辑是一致的</p>
<p>peekLast:  获取尾节点元素;从变量last获取,如果是null返回null;否则返回对应的值</p>
<h3 id="poll方法"><a href="#poll方法" class="headerlink" title="poll方法"></a>poll方法</h3><p>弹出第一个元素,并且将都一个元素的下个元素重置未第一个元素;如果next是null的话,则first也是null,说明当前linkedlist无元素了</p>
<p>这里主要看unlinkFirst方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">poll</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">    <span class="keyword">return</span> (f == <span class="literal">null</span>) ? <span class="literal">null</span> : unlinkFirst(f);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">   取出 f 的item,节点对应的值和 f的next个节点,如果下个节点是null的话,就说明是没有值的,如果不为null的话，说将next的上一个节点prev指向null,因为头节点的prev和尾节点的next都是null来进行区分。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">private</span> E <span class="title function_">unlinkFirst</span><span class="params">(Node&lt;E&gt; f)</span> &#123;</span><br><span class="line">        <span class="comment">// assert f == first &amp;&amp; f != null;</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">E</span> <span class="variable">element</span> <span class="operator">=</span> f.item;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; next = f.next;</span><br><span class="line">        f.item = <span class="literal">null</span>;</span><br><span class="line">        f.next = <span class="literal">null</span>; <span class="comment">// help GC</span></span><br><span class="line">        first = next;</span><br><span class="line">        <span class="keyword">if</span> (next == <span class="literal">null</span>)</span><br><span class="line">            last = <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            next.prev = <span class="literal">null</span>;</span><br><span class="line">        size--;</span><br><span class="line">        modCount++;</span><br><span class="line">        <span class="keyword">return</span> element;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="remove-方法"><a href="#remove-方法" class="headerlink" title="remove()方法"></a>remove()方法</h3><p>移出第一个元素,也就是和 poll 的逻辑是一致的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">removeFirst</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">        <span class="keyword">if</span> (f == <span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NoSuchElementException</span>();</span><br><span class="line">        <span class="keyword">return</span> unlinkFirst(f);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="remove-int-index-方法"><a href="#remove-int-index-方法" class="headerlink" title="remove(int index)方法"></a>remove(int index)方法</h3><p>根据下标进来remove方法, node(index) 也是在上面进行讲到的,就是根据下标获取对应的node节点信息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">remove</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    checkElementIndex(index);</span><br><span class="line">    <span class="keyword">return</span> unlink(node(index));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	这里获取出节点的 next 和 prev方法.</span></span><br><span class="line"><span class="comment">	该节点的上一个节点(prev)的next需要指向指向该节点的下个节点(next),该节点的下一个节点和prev的操作是相反的,因为这样的话,就删除了该节点,并且上一个节点和下一个节点关联起来了.	</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"> E <span class="title function_">unlink</span><span class="params">(Node&lt;E&gt; x)</span> &#123;</span><br><span class="line">        <span class="comment">// assert x != null;</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">E</span> <span class="variable">element</span> <span class="operator">=</span> x.item;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; next = x.next;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; prev = x.prev;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (prev == <span class="literal">null</span>) &#123;</span><br><span class="line">            first = next;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            prev.next = next;</span><br><span class="line">            x.prev = <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (next == <span class="literal">null</span>) &#123;</span><br><span class="line">            last = prev;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            next.prev = prev;</span><br><span class="line">            x.next = <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        x.item = <span class="literal">null</span>;</span><br><span class="line">        size--;</span><br><span class="line">        modCount++;</span><br><span class="line">        <span class="keyword">return</span> element;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="remove-Object-o"><a href="#remove-Object-o" class="headerlink" title="remove(Object o)"></a>remove(Object o)</h3><p>根据值来进行删除.这个可以看出来，如果有二个相同节点的值,调用一次这个方法是只可以删除一个,而不是二个</p>
<p>假设node节点1有二个,则你需要调用二次删除</p>
<p>当然你可以分别调用removeFirstOccurrence(Object o)和removeLastOccurrence(Object o)分别优先删除头和优先删除尾</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">remove</span><span class="params">(Object o)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (o == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="literal">null</span>; x = x.next) &#123;</span><br><span class="line">            <span class="keyword">if</span> (x.item == <span class="literal">null</span>) &#123;</span><br><span class="line">                unlink(x);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="literal">null</span>; x = x.next) &#123;</span><br><span class="line">            <span class="keyword">if</span> (o.equals(x.item)) &#123;</span><br><span class="line">                unlink(x);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>好啦,今天的知识内容就更新到这里,虽然文字描述到很难理解,但是主要去理解 Node 节点的 双向指向,并且每次添加节点和删除添加，都是靠Node的prev和next来进行指向. 所以说LinkedList是删除快，查询慢的原因</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>ReentrantLock源码笔记</title>
    <url>//article/reentrantLock_note/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-ReentrantLock源码笔记"><a href="#📝-ReentrantLock源码笔记" class="headerlink" title="📝 ReentrantLock源码笔记"></a>📝 <strong>ReentrantLock源码笔记</strong></h1><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>ReetrantLock 效果是和 synchronized 是一样的,只不过 synchronized 是内置锁,ReetrantLock是语法级别的锁, 相对于而言是比synchronized灵活性高些. 不过从我目前公司写代码角度来看,都是直接使用 synchronized . 但是不妨碍我们来看 ReetrantLock 里面的代码实现</p>
<p>使用代码 : 有lock方法就一定要有 unlock方法来释放锁. 一般代码中这样写即可.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ReentrantLockCaseMain</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">lockUseCase</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;执行业务代码逻辑&quot;</span>);</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="代码分析"><a href="#代码分析" class="headerlink" title="代码分析"></a>代码分析</h2><p> ReetrantLock 中是没有什么全局参数,相比于集合,就没有那么多全局参数.但是我们要看其里面的内,这里有三个类, Sync , NonfairSync , FairSync. NonfairSync和FairSync 都是有继承 Sync. 可以看到NonfairSync 是非公平锁 , FairSync是公平锁</p>
<p>Sync 又集成 AQS, 使用独占锁, 重写了 tryRelease 方法</p>
<h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><p>默认是使用的非公平锁,如果传入进来的是true就会使用公平锁,否则就会使用非公平锁</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Creates an instance of &#123;<span class="doctag">@code</span> ReentrantLock&#125;.</span></span><br><span class="line"><span class="comment">   * This is equivalent to using &#123;<span class="doctag">@code</span> ReentrantLock(false)&#125;.</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">ReentrantLock</span><span class="params">()</span> &#123;</span><br><span class="line">      sync = <span class="keyword">new</span> <span class="title class_">NonfairSync</span>();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Creates an instance of &#123;<span class="doctag">@code</span> ReentrantLock&#125; with the</span></span><br><span class="line"><span class="comment">   * given fairness policy.</span></span><br><span class="line"><span class="comment">   *</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> fair &#123;<span class="doctag">@code</span> true&#125; if this lock should use a fair ordering policy</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">ReentrantLock</span><span class="params">(<span class="type">boolean</span> fair)</span> &#123;</span><br><span class="line">      sync = fair ? <span class="keyword">new</span> <span class="title class_">FairSync</span>() : <span class="keyword">new</span> <span class="title class_">NonfairSync</span>();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h3 id="lock方法"><a href="#lock方法" class="headerlink" title="lock方法"></a>lock方法</h3><p>lock方法是调用的 Sync 的lock方法, 然后我们可以看到上锁的时候,走的Sync,然后根据FairSync&#x2F;NonfairSync取走各自的加锁方法,所以说公平锁和非公平锁是加锁的方式是不一样的</p>
<p>非公平锁获取锁的时候,会获取state这个状态标识,然后再去走对应的逻辑,这里多了比非公平锁多了一个从队列中获取信息和不能获取锁的线程就会被挂起进入队列中排队</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">lock</span><span class="params">()</span> &#123;    sync.lock();&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Sync </span></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">lock</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 公平锁</span></span><br><span class="line"><span class="comment">     * Sync object for fair locks</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">tatic <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">FairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> -<span class="number">3000897897090466540L</span>;</span><br><span class="line"></span><br><span class="line">    	<span class="comment">/* </span></span><br><span class="line"><span class="comment">    	acquire(1) 调用到AQS中,最后还是调用到下面的tryAcquire方法.</span></span><br><span class="line"><span class="comment">    	那些没有获取到锁的线程,就会按照队列的方式排队,满足先进先出的效果的,也就是先来的线程先执行,</span></span><br><span class="line"><span class="comment">    	果然这就很公平</span></span><br><span class="line"><span class="comment">    	**/</span></span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">lock</span><span class="params">()</span> &#123;</span><br><span class="line">            acquire(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * Fair version of tryAcquire.  Don&#x27;t grant access unless</span></span><br><span class="line"><span class="comment">         * recursive call or no waiters or is first.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryAcquire</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Thread</span> <span class="variable">current</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> getState();</span><br><span class="line">            <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">/**</span></span><br><span class="line"><span class="comment">                	hasQueuedPredecessors() 方法,先判断头结点和尾结点是不相等的,因为相等的话,就重复了,就是同一个. 然后在判断头结点的 thread是不是当前线程,如果不是当前的前程的话,那么就是在这个线程钱面还有一个等待获取锁时间更久的线程,于是就先抛弃这个线程,去执行那个等待更久的线程.</span></span><br><span class="line"><span class="comment">                	</span></span><br><span class="line"><span class="comment">                	compareAndSetState 就是用cas来获取锁的代码,如果获取成功的话,就会走setExclusiveOwnerThread方法,这里set进去的值是在释放锁的时候会用到.</span></span><br><span class="line"><span class="comment">                	最后返回true,说明获取锁成功了.</span></span><br><span class="line"><span class="comment">                */</span></span><br><span class="line">                <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;</span><br><span class="line">                    compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">                    setExclusiveOwnerThread(current);</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">            	获取从setExclusiveOwnerThread里面的thread,来判断是否与当前线程相等,如果相等的话,就说明重入了.</span></span><br><span class="line"><span class="comment">            */</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">nextc</span> <span class="operator">=</span> c + acquires;</span><br><span class="line">                <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Maximum lock count exceeded&quot;</span>);</span><br><span class="line">                setState(nextc);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;之前</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">hasQueuedPredecessors</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// The correctness of this depends on head being initialized</span></span><br><span class="line">        <span class="comment">// before tail and on head.next being accurate if the current</span></span><br><span class="line">        <span class="comment">// thread is first in queue.</span></span><br><span class="line">        <span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail; <span class="comment">// Read fields in reverse initialization order</span></span><br><span class="line">        <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">        Node s;</span><br><span class="line">        <span class="keyword">return</span> h != t &amp;&amp;</span><br><span class="line">            ((s = h.next) == <span class="literal">null</span> || s.thread != Thread.currentThread());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	非公平锁:</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">NonfairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">7316153563782823691L</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * Performs lock.  Try immediate barge, backing up to normal</span></span><br><span class="line"><span class="comment">         * acquire on failure.</span></span><br><span class="line"><span class="comment">         可以看到非公平锁是没有从队列中获取说明结点信息,而是直接获取锁的.</span></span><br><span class="line"><span class="comment">         获取成功了就会走 setExclusiveOwnerThread 方法</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">lock</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, <span class="number">1</span>))</span><br><span class="line">                setExclusiveOwnerThread(Thread.currentThread());</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                acquire(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryAcquire</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> nonfairTryAcquire(acquires);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	Sync类中</span></span><br><span class="line"><span class="comment">	这段代码的逻辑也是和 公平锁后来的处理一样的了. c如果是0的话,就会走获取锁的代码,如果不是0的话,就说明重入了,所以就++</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">nonfairTryAcquire</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Thread</span> <span class="variable">current</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> getState();</span><br><span class="line">            <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">                    setExclusiveOwnerThread(current);</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">nextc</span> <span class="operator">=</span> c + acquires;</span><br><span class="line">                <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Maximum lock count exceeded&quot;</span>);</span><br><span class="line">                setState(nextc);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="unlock方法"><a href="#unlock方法" class="headerlink" title="unlock方法"></a>unlock方法</h3><p>unlock是释放锁的方法. 可以看到释放锁是走的 Sync的release方法,所以不管公平锁还是非公平锁起走的释放锁方法是不一样的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">unlock</span><span class="params">()</span> &#123;    sync.release(<span class="number">1</span>);&#125;  </span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	Sync 中方法. </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">release</span><span class="params">(<span class="type">int</span> arg)</span> &#123;</span><br><span class="line">        <span class="comment">// tryRelease()方法返回true的话,就说明锁都释放完了.</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">            <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">            <span class="comment">// 恢复线程</span></span><br><span class="line">            <span class="keyword">if</span> (h != <span class="literal">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryRelease</span><span class="params">(<span class="type">int</span> releases)</span> &#123;</span><br><span class="line">            <span class="comment">// 释放锁</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> getState() - releases;</span><br><span class="line">            <span class="comment">// 如果当前线程不是自己的话,就会抛出异常.这里可以理解为,独占锁,肯定是自己.</span></span><br><span class="line">            <span class="comment">// 也就是说,如果不是独占锁的话,就会抛出异常.</span></span><br><span class="line">            <span class="keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalMonitorStateException</span>();</span><br><span class="line">            <span class="type">boolean</span> <span class="variable">free</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">            <span class="comment">// 如果你调用了一次lock的话,那么会加一,所以这个地方要等这个lock方法全部被释放掉.</span></span><br><span class="line">    		<span class="comment">// 也就是由于重入锁的原因.</span></span><br><span class="line">            <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">                free = <span class="literal">true</span>;</span><br><span class="line">                <span class="comment">// 释放完了,就设置了null.  </span></span><br><span class="line">                <span class="comment">// 然后AbstractOwnableSynchronizer中的thread标记也就是null,</span></span><br><span class="line">                <span class="comment">// 所以下个线程判断是null的话,就可以获取到执行权,也就是获取到锁.</span></span><br><span class="line">                setExclusiveOwnerThread(<span class="literal">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            setState(c);</span><br><span class="line">            <span class="keyword">return</span> free;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="isLocked-方法"><a href="#isLocked-方法" class="headerlink" title="isLocked 方法"></a>isLocked 方法</h3><p>判断这个线程是不是被锁了:调用Sync中isLock方法,如果不是0的话,就说明是被锁了,如果是0的话,就说明没有被锁</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Queries if this lock is held by any thread. This method is</span></span><br><span class="line"><span class="comment">     * designed for use in monitoring of the system state,</span></span><br><span class="line"><span class="comment">     * not for synchronization control.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> &#123;<span class="doctag">@code</span> true&#125; if any thread holds this lock and</span></span><br><span class="line"><span class="comment">     *         &#123;<span class="doctag">@code</span> false&#125; otherwise</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isLocked</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> sync.isLocked();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">isLocked</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> getState() != <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="其他方法"><a href="#其他方法" class="headerlink" title="其他方法"></a>其他方法</h3><p>hasQueuedThreads() : 是否有线程在等待队列中</p>
<p>hasQueuedThread(Thread thread) : 线程是否在等待队列中</p>
<p>getQueueLength() : 获取队列中线程个数</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>Sync &#x2F; NonfairSync &#x2F; FairSync 这个三个类就是 ReetrantLock中的三个类,都是围绕这这三个类在做文章</p>
<p>公平锁和非公平锁的获取锁方式不一样,但是释放方式是一样的. 公平锁获取锁的时候,如果有线程持有了的话,那么其他的会被挂起并且进入等待队列. 而非公平锁,直接获取锁,就是抢占式</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>记录奇怪的SQL</title>
    <url>//article/mysql_special_sql/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>记录不同篇章数据库的一些奇怪操作的SQL语句;会持续的记录</p>
</blockquote>
<h1 id="📝-MySql篇章"><a href="#📝-MySql篇章" class="headerlink" title="📝 MySql篇章"></a>📝 MySql篇章</h1><h2 id="去重排序后的SQL"><a href="#去重排序后的SQL" class="headerlink" title="去重排序后的SQL"></a>去重排序后的SQL</h2><p>简单的一个明细表</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> `t_directory_browse_detail_info` (</span><br><span class="line">  `id` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;主键id&#x27;</span>,</span><br><span class="line">  `user_id` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;用户id&#x27;</span>,</span><br><span class="line">  `dept_id` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;租户id&#x27;</span>,</span><br><span class="line">  `type_id` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;类型id&#x27;</span>,</span><br><span class="line">  `type_number` <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;类型对应的number编码&#x27;</span>,</span><br><span class="line">  `type_value` <span class="type">varchar</span>(<span class="number">32</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;类型type&#x27;</span>,</span><br><span class="line">  `browse_count` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;浏览次数&#x27;</span>,</span><br><span class="line">  `create_by` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;创建人id&#x27;</span>,</span><br><span class="line">  `update_by` <span class="type">bigint</span>(<span class="number">20</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;更新人id&#x27;</span>,</span><br><span class="line">  `create_time` datetime <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;创建时间&#x27;</span>,</span><br><span class="line">  `update_time` datetime <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;修改时间&#x27;</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`id`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB AUTO_INCREMENT<span class="operator">=</span><span class="number">81</span> <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;浏览表明细表&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="Q1-根据type-number-type-value去重取最新的数据-并且根据update-time排序取最新的"><a href="#Q1-根据type-number-type-value去重取最新的数据-并且根据update-time排序取最新的" class="headerlink" title="Q1: 根据type_number,type_value去重取最新的数据,并且根据update_time排序取最新的"></a>Q1: 根据type_number,type_value去重取最新的数据,并且根据update_time排序取最新的</h3><p>错误SQL编写1: 可以看到提示的信息</p>
<p>Expression #1 of ORDER BY clause is not in SELECT list, references column ‘t_directory_browse_detail_info.update_time’ which is not in SELECT list; this is incompatible with DISTINCT</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span>  <span class="keyword">DISTINCT</span> type_id,type_value  <span class="keyword">from</span> t_directory_browse_detail_info <span class="keyword">order</span> <span class="keyword">by</span> update_time <span class="keyword">desc</span>;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>错误SQL编写2:我们先排序出一张表，然后根据这张表去做去重处理<br>问题: 你会发现查询出来的数据不是你想要的数据</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span>  <span class="keyword">DISTINCT</span> type_id,type_value <span class="keyword">from</span> </span><br><span class="line"> (</span><br><span class="line"> <span class="keyword">SELECT</span> </span><br><span class="line"> type_id,type_value</span><br><span class="line"><span class="keyword">from</span> t_directory_browse_detail_info <span class="keyword">order</span> <span class="keyword">by</span> update_time <span class="keyword">desc</span></span><br><span class="line"> ) a</span><br></pre></td></tr></table></figure>

<p>正确SQL1:采用分组,然后取最大的update_time或者取最大的id的值</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	type_id <span class="keyword">as</span> typeId ,</span><br><span class="line">	type_value <span class="keyword">as</span> typeValue</span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">	t_directory_browse_detail_info</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span></span><br><span class="line">	type_id,</span><br><span class="line">	type_value</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span></span><br><span class="line">	<span class="built_in">max</span>(id) <span class="keyword">desc</span></span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>mysql</tag>
        <tag>问题记录</tag>
      </tags>
  </entry>
  <entry>
    <title>spring创建bean_one</title>
    <url>/2021/11/03/Java/spring%E5%88%9B%E5%BB%BAbean-one/</url>
    <content><![CDATA[<h4 id="前提"><a href="#前提" class="headerlink" title="前提"></a>前提</h4><p>我们在创建 Spring Bean 的时候，是可以通过很多种方式来创建的. 但是这么多种方式,又是怎么加载的？是不是又有顺序呢？ 所以对 Spring 的 Bean 创建还是很有必要的.</p>
<h4 id="创建方式"><a href="#创建方式" class="headerlink" title="创建方式"></a>创建方式</h4><p>我们可以通过自己创建 bd , 然后调用 registerBeanDefinition 方法给注册到 Spring 中来.</p>
<p>那么创建bd的怎么创建的呢？可以看到下面的二种创建方式.</p>
<p>这是通过 bd 来的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanDefinitionCreateAndRegister</span> &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1 : 通过 BeanDefinitionBuilder 来创建 bd</span></span><br><span class="line">        <span class="type">BeanDefinitionBuilder</span> <span class="variable">beanDefinitionBuilder</span> <span class="operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(Person.class);</span><br><span class="line">        beanDefinitionBuilder.addPropertyValue(<span class="string">&quot;id&quot;</span>,<span class="number">9527</span>).addPropertyValue(<span class="string">&quot;name&quot;</span>,<span class="string">&quot;GavinYang&quot;</span>);</span><br><span class="line">        <span class="type">BeanDefinition</span> <span class="variable">beanDefinition</span> <span class="operator">=</span> beanDefinitionBuilder.getBeanDefinition();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2 : 通过 new GenericBeanDefinition 来创建 bd.</span></span><br><span class="line">        <span class="type">GenericBeanDefinition</span> <span class="variable">genericBeanDefinition</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GenericBeanDefinition</span>();</span><br><span class="line">        genericBeanDefinition.setBeanClass(Person.class);</span><br><span class="line">        <span class="type">MutablePropertyValues</span> <span class="variable">mutablePropertyValues</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MutablePropertyValues</span>();</span><br><span class="line">        mutablePropertyValues.add(<span class="string">&quot;id&quot;</span>,<span class="number">1</span>).add(<span class="string">&quot;name&quot;</span>,<span class="string">&quot;Peterwong&quot;</span>);</span><br><span class="line">        genericBeanDefinition.setPropertyValues(mutablePropertyValues);</span><br><span class="line"></span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这里是给 bd 给注册到 Spring 容器里面来.</span></span><br><span class="line">        <span class="comment">// context.registerBeanDefinition(&quot;person&quot;,beanDefinition);</span></span><br><span class="line">        context.registerBeanDefinition(<span class="string">&quot;peterwong&quot;</span>,genericBeanDefinition);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果这里不调用 refresh 是会有错误的.</span></span><br><span class="line">        context.refresh();</span><br><span class="line"></span><br><span class="line">        <span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> context.getBean(Person.class);</span><br><span class="line">        person.say();</span><br><span class="line">        System.out.println(person.toString());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过我们常用的注解</p>
<p>这里主要是 @Import&#x2F;@Bean&#x2F;@Component+@ComponentScan 方式来注入对象到 Spring 容器中来.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Import(ImportBeanConfigMain.ImportConfig.class)</span></span><br><span class="line"><span class="meta">@ComponentScan(basePackages = &quot;com.iyang.bean.bd&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ImportBeanConfigMain</span> &#123;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ImportBeanConfigMain</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;ImportBeanConfigMain 无参数构造函数&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>();</span><br><span class="line">        context.register(ImportBeanConfigMain.class);</span><br><span class="line">        context.refresh();</span><br><span class="line"></span><br><span class="line">        <span class="type">ImportConfig</span> <span class="variable">importConfig</span> <span class="operator">=</span> context.getBean(ImportConfig.class);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> context.getBean(Person.class);</span><br><span class="line">        <span class="type">AnnotConfig</span> <span class="variable">annotConfig</span> <span class="operator">=</span> context.getBean(AnnotConfig.class);</span><br><span class="line">        <span class="type">ExternalConfig</span> <span class="variable">externalConfig</span> <span class="operator">=</span> context.getBean(ExternalConfig.class);</span><br><span class="line"></span><br><span class="line">        System.out.println(importConfig);</span><br><span class="line">        System.out.println(person);</span><br><span class="line">        System.out.println(annotConfig);</span><br><span class="line">        System.out.println(externalConfig);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 通过 <span class="doctag">@Import</span> 导入进来.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ImportConfig</span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">importMe</span><span class="params">()</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;这是导入自己的方法&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;ImportConfig 的 toString 方法&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">ImportConfig</span><span class="params">()</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;ImportConfig无参数构造函数&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 使用 <span class="doctag">@Bean</span> 注解 注入 Bean 进来.</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="meta">@Bean</span></span><br><span class="line">        <span class="keyword">public</span> Person <span class="title function_">importPerson</span><span class="params">()</span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="number">9527</span>,<span class="string">&quot;GavinYang&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Component</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">AnnotConfig</span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">AnnotConfig</span><span class="params">()</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;AnnotConfig无参数构造函数&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;使用注解来注入bean进来.&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ExternalConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ExternalConfig</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;externalConfig 无参构造函数&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;externalConfig 打印 toString() 方法&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">----------------------------------------------</span><br><span class="line">ImportBeanConfigMain 无参数构造函数    </span><br><span class="line">externalConfig 无参构造函数</span><br><span class="line">AnnotConfig无参数构造函数</span><br><span class="line">ImportConfig无参数构造函数</span><br><span class="line">person 有参数构造函数</span><br><span class="line">ImportConfig 的 toString 方法</span><br><span class="line">Person&#123;id=<span class="number">9527</span>, name=<span class="string">&#x27;GavinYang&#x27;</span>&#125;</span><br><span class="line">使用注解来注入bean进来.</span><br><span class="line">externalConfig 打印 toString() 方法</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 这里可以看到new出来的对象打印顺序.</span></span><br></pre></td></tr></table></figure>

<p>如果是基于创建 bd 的方式的话，是说明下是可以通过这种方式来将我们自己创建的对象给注入到Spring容器中来.我们主要来分析第二种,是做了什么事情.</p>
<h4 id="Import-Bean-Component-ComponentScan-分析"><a href="#Import-Bean-Component-ComponentScan-分析" class="headerlink" title="@Import&#x2F;@Bean&#x2F;@Component+@ComponentScan 分析"></a>@Import&#x2F;@Bean&#x2F;@Component+@ComponentScan 分析</h4><p>在分析之前，我们看下我们的 beanClass 是怎么先注册到 Spring中来的,也就是在org.springframework.beans.factory.support.DefaultListableBeanFactory#beanDefinitionMap和org.springframework.beans.factory.support.DefaultListableBeanFactory#beanDefinitionNames中,可以看到一个是Map类型的,一个是集合类型的.</p>
<p>我们把断点打在 org.springframework.beans.factory.support.DefaultListableBeanFactory#registerBeanDefinition 进来的方法上就可以看到,然后看堆栈信息,就可以看到怎么一步一步给添加进来的.</p>
<h5 id="注册-Spring-中来走的方法"><a href="#注册-Spring-中来走的方法" class="headerlink" title="注册 Spring 中来走的方法"></a>注册 Spring 中来走的方法</h5><p>这里只用关注我们自己自己定义的，Spring内部的就不需要管了。</p>
<p><strong>ImportBeanConfigMain</strong></p>
<p>org.springframework.context.annotation.AnnotatedBeanDefinitionReader#registerBean(java.lang.Class&lt;?&gt;) —-&gt; org.springframework.beans.factory.support.BeanDefinitionReaderUtils#registerBeanDefinition —-&gt; org.springframework.context.support.GenericApplicationContext#registerBeanDefinition</p>
<p><strong>externalConfig</strong></p>
<p>org.springframework.context.support.AbstractApplicationContext#invokeBeanFactoryPostProcessors —-&gt;</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanDefinitionRegistryPostProcessors —–&gt; org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions —-&gt; org.springframework.context.annotation.ConfigurationClassParser#parse(org.springframework.core.type.AnnotationMetadata, java.lang.String) —&gt; org.springframework.context.annotation.ConfigurationClassParser#doProcessConfigurationClass —-&gt;</p>
<p>org.springframework.context.annotation.ClassPathBeanDefinitionScanner#doScan —&gt; org.springframework.context.annotation.ClassPathBeanDefinitionScanner#registerBeanDefinition</p>
<p><strong>importBeanConfigMain.AnnotConfig</strong></p>
<p>org.springframework.context.support.AbstractApplicationContext#invokeBeanFactoryPostProcessors —-&gt;org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;) —-&gt; org.springframework.context.annotation.ConfigurationClassPostProcessor#postProcessBeanDefinitionRegistry —-&gt; org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions —&gt; org.springframework.context.annotation.ConfigurationClassParser#parse(org.springframework.core.type.AnnotationMetadata, java.lang.String) —&gt; org.springframework.context.annotation.ConfigurationClassParser#doProcessConfigurationClass —&gt; org.springframework.context.annotation.ClassPathBeanDefinitionScanner#doScan —&gt; org.springframework.context.annotation.ClassPathBeanDefinitionScanner#registerBeanDefinition</p>
<p><strong>com.iyang.bean.bd.ImportBeanConfigMain$ImportConfig</strong></p>
<p>org.springframework.context.support.AbstractApplicationContext#invokeBeanFactoryPostProcessors —&gt; org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanDefinitionRegistryPostProcessors —&gt; org.springframework.context.annotation.ConfigurationClassPostProcessor#postProcessBeanDefinitionRegistry —-&gt; org.springframework.context.annotation.ConfigurationClassBeanDefinitionReader#loadBeanDefinitions —&gt; org.springframework.context.annotation.ConfigurationClassBeanDefinitionReader#registerBeanDefinitionForImportedConfigurationClass</p>
<p><strong>importPerson</strong></p>
<p>org.springframework.context.support.AbstractApplicationContext#invokeBeanFactoryPostProcessors —&gt; org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanDefinitionRegistryPostProcessors —&gt; org.springframework.context.annotation.ConfigurationClassPostProcessor#postProcessBeanDefinitionRegistry —&gt; org.springframework.context.annotation.ConfigurationClassBeanDefinitionReader#loadBeanDefinitions —&gt; org.springframework.context.annotation.ConfigurationClassBeanDefinitionReader#loadBeanDefinitionsForBeanMethod</p>
<p>可以看到除了 ImportBeanConfigMain 在扫描的时候就被注册到 spring 容器里面来，后面的都是走的 AbstractApplicationContext#invokeBeanFactoryPostProcessors 方法给注册到 Spring 容器中来了. 是不是应该详细分析下 invokeBeanFactoryPostProcessors 方法到了做了什么或者说用了什么,将我们定义的对象给注册到 Spring 容器中来了呢？</p>
<h5 id="invokeBeanFactoryPostProcessors-方法解析"><a href="#invokeBeanFactoryPostProcessors-方法解析" class="headerlink" title="invokeBeanFactoryPostProcessors 方法解析"></a>invokeBeanFactoryPostProcessors 方法解析</h5><p>从上面来看，这个方法并不是我们想象中那么简单的.</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;) 委托到这里来进行解析的,所以我们直接深度分析这个方法即可.</p>
<p>上面可以看到都是走的 PostProcessorRegistrationDelegate 这个类,但是我们并没有在这个方法中找到这个类.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">      ConfigurableListableBeanFactory beanFactory, List&lt;BeanFactoryPostProcessor&gt; beanFactoryPostProcessors)</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Invoke BeanDefinitionRegistryPostProcessors first, if any.</span></span><br><span class="line">   Set&lt;String&gt; processedBeans = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (beanFactory <span class="keyword">instanceof</span> BeanDefinitionRegistry) &#123;</span><br><span class="line">      <span class="type">BeanDefinitionRegistry</span> <span class="variable">registry</span> <span class="operator">=</span> (BeanDefinitionRegistry) beanFactory;</span><br><span class="line">      List&lt;BeanFactoryPostProcessor&gt; regularPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">      List&lt;BeanDefinitionRegistryPostProcessor&gt; registryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">      <span class="keyword">for</span> (BeanFactoryPostProcessor postProcessor : beanFactoryPostProcessors) &#123;</span><br><span class="line">         <span class="keyword">if</span> (postProcessor <span class="keyword">instanceof</span> BeanDefinitionRegistryPostProcessor) &#123;</span><br><span class="line">            <span class="type">BeanDefinitionRegistryPostProcessor</span> <span class="variable">registryProcessor</span> <span class="operator">=</span></span><br><span class="line">                  (BeanDefinitionRegistryPostProcessor) postProcessor;</span><br><span class="line">            registryProcessor.postProcessBeanDefinitionRegistry(registry);</span><br><span class="line">            registryProcessors.add(registryProcessor);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            regularPostProcessors.add(postProcessor);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">      <span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line">      <span class="comment">// Separate between BeanDefinitionRegistryPostProcessors that implement</span></span><br><span class="line">      <span class="comment">// PriorityOrdered, Ordered, and the rest.</span></span><br><span class="line">      List&lt;BeanDefinitionRegistryPostProcessor&gt; currentRegistryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// First, invoke the BeanDefinitionRegistryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">      String[] postProcessorNames =</span><br><span class="line">            beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">      <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">         <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">            currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">            processedBeans.add(ppName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">      registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line"><span class="comment">// Note : 我们根据 debug 是可以跟进到这里的, 我们直接在这里打上断点,再来仔细看看这个方法做了什么事情.   // currentRegistryProcessors : org.springframework.context.annotation.ConfigurationClassPostProcessor      </span></span><br><span class="line"><span class="comment">// registry :  DefaultableListFactory </span></span><br><span class="line"><span class="comment">// 走完这个方法,我们的bean信息都注册到 Spring 的 DefaultLitableFactory中来了.      </span></span><br><span class="line">      invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">      currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Next, invoke the BeanDefinitionRegistryPostProcessors that implement Ordered.</span></span><br><span class="line">      postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">      <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">         <span class="keyword">if</span> (!processedBeans.contains(ppName) &amp;&amp; beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">            currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">            processedBeans.add(ppName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">      registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">      invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">      currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Finally, invoke all other BeanDefinitionRegistryPostProcessors until no further ones appear.</span></span><br><span class="line">      <span class="type">boolean</span> <span class="variable">reiterate</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">      <span class="keyword">while</span> (reiterate) &#123;</span><br><span class="line">         reiterate = <span class="literal">false</span>;</span><br><span class="line">         postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">         <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!processedBeans.contains(ppName)) &#123;</span><br><span class="line">               currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">               processedBeans.add(ppName);</span><br><span class="line">               reiterate = <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">         registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">         invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">         currentRegistryProcessors.clear();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Now, invoke the postProcessBeanFactory callback of all processors handled so far.</span></span><br><span class="line">      invokeBeanFactoryPostProcessors(registryProcessors, beanFactory);</span><br><span class="line">      invokeBeanFactoryPostProcessors(regularPostProcessors, beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Invoke factory processors registered with the context instance.</span></span><br><span class="line">      invokeBeanFactoryPostProcessors(beanFactoryPostProcessors, beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">   <span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line">   String[] postProcessorNames =</span><br><span class="line">         beanFactory.getBeanNamesForType(BeanFactoryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Separate between BeanFactoryPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">   <span class="comment">// Ordered, and the rest.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">      <span class="keyword">if</span> (processedBeans.contains(ppName)) &#123;</span><br><span class="line">         <span class="comment">// skip - already processed in first phase above</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">         priorityOrderedPostProcessors.add(beanFactory.getBean(ppName, BeanFactoryPostProcessor.class));</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">         orderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// First, invoke the BeanFactoryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">   sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">   invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Next, invoke the BeanFactoryPostProcessors that implement Ordered.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String postProcessorName : orderedPostProcessorNames) &#123;</span><br><span class="line">      orderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line">   sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">   invokeBeanFactoryPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Finally, invoke all other BeanFactoryPostProcessors.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String postProcessorName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">      nonOrderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line">   invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Clear cached merged bean definitions since the post-processors might have</span></span><br><span class="line">   <span class="comment">// modified the original metadata, e.g. replacing placeholders in values...</span></span><br><span class="line">   beanFactory.clearMetadataCache();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="org-springframework-context-annotation-ConfigurationClassPostProcessor-processConfigBeanDefinitions-方法"><a href="#org-springframework-context-annotation-ConfigurationClassPostProcessor-processConfigBeanDefinitions-方法" class="headerlink" title="org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions 方法"></a>org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions 方法</h6><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Build and validate a configuration model based on the registry of</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> Configuration&#125; classes.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">processConfigBeanDefinitions</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">   List&lt;BeanDefinitionHolder&gt; configCandidates = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   String[] candidateNames = registry.getBeanDefinitionNames();</span><br><span class="line"></span><br><span class="line">   <span class="keyword">for</span> (String beanName : candidateNames) &#123;</span><br><span class="line">      <span class="type">BeanDefinition</span> <span class="variable">beanDef</span> <span class="operator">=</span> registry.getBeanDefinition(beanName);</span><br><span class="line">      <span class="keyword">if</span> (beanDef.getAttribute(ConfigurationClassUtils.CONFIGURATION_CLASS_ATTRIBUTE) != <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">            logger.debug(<span class="string">&quot;Bean definition has already been processed as a configuration class: &quot;</span> + beanDef);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 对是否满足配置类进行检查, 这里我们的bean是importBeanConfigMain,满足条件的,具体可以看下面该方法的分析.然后会构建一个 bdHolder,添加到集合中来.</span></span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (ConfigurationClassUtils.checkConfigurationClassCandidate(beanDef, <span class="built_in">this</span>.metadataReaderFactory)) &#123;</span><br><span class="line">         configCandidates.add(<span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(beanDef, beanName));</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Return immediately if no @Configuration classes were found</span></span><br><span class="line">   <span class="keyword">if</span> (configCandidates.isEmpty()) &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Sort by previously determined @Order value, if applicable</span></span><br><span class="line"><span class="comment">// 这里会根据 @Order 来进行排序下.</span></span><br><span class="line"><span class="comment">// 从 Integer.compare(i1, i2) 来分析，应该是从小到大的排序,也就是说,越小的话,优先级就约高. </span></span><br><span class="line">   configCandidates.sort((bd1, bd2) -&gt; &#123;</span><br><span class="line">      <span class="type">int</span> <span class="variable">i1</span> <span class="operator">=</span> ConfigurationClassUtils.getOrder(bd1.getBeanDefinition());</span><br><span class="line">      <span class="type">int</span> <span class="variable">i2</span> <span class="operator">=</span> ConfigurationClassUtils.getOrder(bd2.getBeanDefinition());</span><br><span class="line">      <span class="keyword">return</span> Integer.compare(i1, i2);</span><br><span class="line">   &#125;);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect any custom bean name generation strategy supplied through the enclosing application context</span></span><br><span class="line">   <span class="type">SingletonBeanRegistry</span> <span class="variable">sbr</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (registry <span class="keyword">instanceof</span> SingletonBeanRegistry) &#123;</span><br><span class="line"><span class="comment">// 满足类型条件强转下.       </span></span><br><span class="line">      sbr = (SingletonBeanRegistry) registry;</span><br><span class="line">      <span class="keyword">if</span> (!<span class="built_in">this</span>.localBeanNameGeneratorSet) &#123;</span><br><span class="line"><span class="comment">// 这里不包含,所以返回的就是null.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#getSingleton(java.lang.String, boolean)          </span></span><br><span class="line">         <span class="type">BeanNameGenerator</span> <span class="variable">generator</span> <span class="operator">=</span> (BeanNameGenerator) sbr.getSingleton(</span><br><span class="line">               AnnotationConfigUtils.CONFIGURATION_BEAN_NAME_GENERATOR);</span><br><span class="line">         <span class="keyword">if</span> (generator != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="built_in">this</span>.componentScanBeanNameGenerator = generator;</span><br><span class="line">            <span class="built_in">this</span>.importBeanNameGenerator = generator;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// 确保environment不是null.</span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.environment == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="built_in">this</span>.environment = <span class="keyword">new</span> <span class="title class_">StandardEnvironment</span>();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Parse each @Configuration class</span></span><br><span class="line"><span class="comment">// 创建一个解析 @Configuration 的对象.</span></span><br><span class="line"><span class="comment">// 在创建ConfigurationClassParser的这个有参构造函数里面,是可以看到又new了二个对象的,一个是ComponentScanAnnotationParser,一个是ConditionEvaluator.</span></span><br><span class="line"><span class="comment">// ComponentScanAnnotationParser 这个从名字上看,可以理解为@ComponentScan注解的解析.  </span></span><br><span class="line">   <span class="type">ConfigurationClassParser</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ConfigurationClassParser</span>(</span><br><span class="line">         <span class="built_in">this</span>.metadataReaderFactory, <span class="built_in">this</span>.problemReporter, <span class="built_in">this</span>.environment,</span><br><span class="line">         <span class="built_in">this</span>.resourceLoader, <span class="built_in">this</span>.componentScanBeanNameGenerator, registry);</span><br><span class="line"></span><br><span class="line">   Set&lt;BeanDefinitionHolder&gt; candidates = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(configCandidates);</span><br><span class="line">   Set&lt;ConfigurationClass&gt; alreadyParsed = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;(configCandidates.size());</span><br><span class="line">   <span class="keyword">do</span> &#123;</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ConfigurationClassParser#processConfigurationClass这里走到这里,主要看这个方法中的doProcessConfigurationClass方法.       </span></span><br><span class="line">      parser.parse(candidates);</span><br><span class="line"><span class="comment">// 这里对我们上面解析出来的bean进行valiate,如果validate失败的话,那么最后是会抛出一个异常来的.	       </span></span><br><span class="line">      parser.validate();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 装有我们解析出来的bean信息       </span></span><br><span class="line">      Set&lt;ConfigurationClass&gt; configClasses = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(parser.getConfigurationClasses());</span><br><span class="line"><span class="comment">// 移除已经解析过了的.       </span></span><br><span class="line">      configClasses.removeAll(alreadyParsed);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Read the model and create bean definitions based on its content</span></span><br><span class="line"><span class="comment">//如果this.reader是null的话,就会new一个ConfigurationClassBeanDefinitionReader出来.       </span></span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.reader == <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="built_in">this</span>.reader = <span class="keyword">new</span> <span class="title class_">ConfigurationClassBeanDefinitionReader</span>(</span><br><span class="line">               registry, <span class="built_in">this</span>.sourceExtractor, <span class="built_in">this</span>.resourceLoader, <span class="built_in">this</span>.environment,</span><br><span class="line">               <span class="built_in">this</span>.importBeanNameGenerator, parser.getImportRegistry());</span><br><span class="line">      &#125;</span><br><span class="line"> <span class="comment">// 这里对我们获取的 bean 再进行一个 load.      </span></span><br><span class="line">      <span class="built_in">this</span>.reader.loadBeanDefinitions(configClasses);</span><br><span class="line"><span class="comment">// 解析过了的bean放入到 alreadyParsed 中来.       </span></span><br><span class="line">      alreadyParsed.addAll(configClasses);</span><br><span class="line"></span><br><span class="line">      candidates.clear();</span><br><span class="line"><span class="comment">// 扫描获取出来的bean个数大于 初始化传入进来的个数.       </span></span><br><span class="line">      <span class="keyword">if</span> (registry.getBeanDefinitionCount() &gt; candidateNames.length) &#123;</span><br><span class="line">        <span class="comment">// 获取出新扫描的bean信息.  </span></span><br><span class="line">         String[] newCandidateNames = registry.getBeanDefinitionNames();</span><br><span class="line">        <span class="comment">// 旧的bean信息  </span></span><br><span class="line">         Set&lt;String&gt; oldCandidateNames = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;(Arrays.asList(candidateNames));</span><br><span class="line">        <span class="comment">// 表示已经注册过了的  </span></span><br><span class="line">         Set&lt;String&gt; alreadyParsedClasses = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line">       <span class="comment">// 将外面的 alreadyParsed 中的元素的 metadata的className给放入到alreadyParsedClasses集合中来.    </span></span><br><span class="line">         <span class="keyword">for</span> (ConfigurationClass configurationClass : alreadyParsed) &#123;</span><br><span class="line">            alreadyParsedClasses.add(configurationClass.getMetadata().getClassName());</span><br><span class="line">         &#125;</span><br><span class="line"> <span class="comment">// 对new的集合中元素进行迭代         </span></span><br><span class="line">         <span class="keyword">for</span> (String candidateName : newCandidateNames) &#123;</span><br><span class="line">       <span class="comment">// 老的集合中不包含      </span></span><br><span class="line">            <span class="keyword">if</span> (!oldCandidateNames.contains(candidateName)) &#123;</span><br><span class="line">               <span class="type">BeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> registry.getBeanDefinition(candidateName);</span><br><span class="line">                </span><br><span class="line">       <span class="comment">// alreadyParsedClasses 中不包含并且检验出需要配置的,比如有一些@Configuration等特殊注解，这个方法在之前是有提到的.         </span></span><br><span class="line">               <span class="keyword">if</span> (ConfigurationClassUtils.checkConfigurationClassCandidate(bd, <span class="built_in">this</span>.metadataReaderFactory) &amp;&amp;</span><br><span class="line">                     !alreadyParsedClasses.contains(bd.getBeanClassName())) &#123;</span><br><span class="line">           <span class="comment">// 满足上面这些条件就会放入到candidates集合中来.         </span></span><br><span class="line">                  candidates.add(<span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(bd, candidateName));</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         candidateNames = newCandidateNames;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// candidates 是 empty 就跳出while循环,否则就认为还有bean需要解析.    </span></span><br><span class="line">   <span class="keyword">while</span> (!candidates.isEmpty());</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register the ImportRegistry as a bean in order to support ImportAware @Configuration classes</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ConfigurationClassPostProcessor.importRegistry sbr不包含importRegistry的话,就会注册一个进去.   </span></span><br><span class="line">   <span class="keyword">if</span> (sbr != <span class="literal">null</span> &amp;&amp; !sbr.containsSingleton(IMPORT_REGISTRY_BEAN_NAME)) &#123;</span><br><span class="line">      sbr.registerSingleton(IMPORT_REGISTRY_BEAN_NAME, parser.getImportRegistry());</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.metadataReaderFactory <span class="keyword">instanceof</span> CachingMetadataReaderFactory) &#123;</span><br><span class="line">      <span class="comment">// Clear cache in externally provided MetadataReaderFactory; this is a no-op</span></span><br><span class="line">      <span class="comment">// for a shared cache since it&#x27;ll be cleared by the ApplicationContext.</span></span><br><span class="line"> <span class="comment">// 这里是清除缓存,也是清除一些集合.      </span></span><br><span class="line">      ((CachingMetadataReaderFactory) <span class="built_in">this</span>.metadataReaderFactory).clearCache();</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>走完这个方法,如果是debug模式的话,就可以在 registry(也就是DefaultListableBeanFactory)的 beanDefintionMap和beanDefinitionNames这二个集合中是可以看到我们的bean名字已经bean对应的class信息的.</strong></p>
<h6 id="org-springframework-context-annotation-ConfigurationClassParser-doProcessConfigurationClass方法"><a href="#org-springframework-context-annotation-ConfigurationClassParser-doProcessConfigurationClass方法" class="headerlink" title="org.springframework.context.annotation.ConfigurationClassParser#doProcessConfigurationClass方法"></a>org.springframework.context.annotation.ConfigurationClassParser#doProcessConfigurationClass方法</h6><p>可以看到这个方法就是对 configuration 类进行处理的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Apply processing and build a complete &#123;<span class="doctag">@link</span> ConfigurationClass&#125; by reading the</span></span><br><span class="line"><span class="comment"> * annotations, members and methods from the source class. This method can be called</span></span><br><span class="line"><span class="comment"> * multiple times as relevant sources are discovered.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> configClass the configuration class being build</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> sourceClass a source class</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> the superclass, or &#123;<span class="doctag">@code</span> null&#125; if none found or previously processed</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">final</span> SourceClass <span class="title function_">doProcessConfigurationClass</span><span class="params">(ConfigurationClass configClass, SourceClass sourceClass)</span></span><br><span class="line">      <span class="keyword">throws</span> IOException &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断是不是有 @Component 注解.  </span></span><br><span class="line">   <span class="keyword">if</span> (configClass.getMetadata().isAnnotated(Component.class.getName())) &#123;</span><br><span class="line">      <span class="comment">// Recursively process any member (nested) classes first</span></span><br><span class="line">      processMemberClasses(configClass, sourceClass);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process any @PropertySource annotations</span></span><br><span class="line"><span class="comment">// 接着再处理 @PropertySources 注解. 可以看到这个注解貌似是和 Environment 有关系.   </span></span><br><span class="line">   <span class="keyword">for</span> (AnnotationAttributes propertySource : AnnotationConfigUtils.attributesForRepeatable(</span><br><span class="line">         sourceClass.getMetadata(), PropertySources.class,</span><br><span class="line">         org.springframework.context.annotation.PropertySource.class)) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.environment <span class="keyword">instanceof</span> ConfigurableEnvironment) &#123;</span><br><span class="line">         processPropertySource(propertySource);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         logger.info(<span class="string">&quot;Ignoring @PropertySource annotation on [&quot;</span> + sourceClass.getMetadata().getClassName() +</span><br><span class="line">               <span class="string">&quot;]. Reason: Environment must implement ConfigurableEnvironment&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process any @ComponentScan annotations</span></span><br><span class="line"><span class="comment">// 获取@ComponentScan 注解,我们这里是有的.    </span></span><br><span class="line">   Set&lt;AnnotationAttributes&gt; componentScans = AnnotationConfigUtils.attributesForRepeatable(</span><br><span class="line">         sourceClass.getMetadata(), ComponentScans.class, ComponentScan.class);</span><br><span class="line">   <span class="keyword">if</span> (!componentScans.isEmpty() &amp;&amp;</span><br><span class="line">         !<span class="built_in">this</span>.conditionEvaluator.shouldSkip(sourceClass.getMetadata(), ConfigurationPhase.REGISTER_BEAN)) &#123;</span><br><span class="line">      <span class="keyword">for</span> (AnnotationAttributes componentScan : componentScans) &#123;</span><br><span class="line">         <span class="comment">// The config class is annotated with @ComponentScan -&gt; perform the scan immediately</span></span><br><span class="line">          </span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ComponentScanAnnotationParser#parse</span></span><br><span class="line"><span class="comment">// parse 方法内部是使用 ClassPathBeanDefinitionScanner 扫描器的,对resourcePattern/includeFilters/excludeFilters/lazyInit 是否有进行处理.</span></span><br><span class="line"><span class="comment">// 获取注解上的属性 basePackages/basePackageClasses的值,添加一个AbstractTypeHierarchyTraversingFilter,这个是ExcludeFilter</span></span><br><span class="line"><span class="comment">//最后来org.springframework.context.annotation.ClassPathBeanDefinitionScanner#doScan做扫描操作.</span></span><br><span class="line"><span class="comment">//doScan做了什么事情呢? 显示通过传入进来的包,调用findCandidateComponents获取出bd的集合来,ScopeMetadata设置也是默认的,用beanNameGenerator生成bean对应的beanName</span></span><br><span class="line"><span class="comment">//如果bd是AbstractBeanDefinition,再走一下postProcessBeanDefinition方法</span></span><br><span class="line"><span class="comment">//如果bd是AnnotatedBeanDefinition,会走AnnotationConfigUtils.processCommonDefinitionAnnotations()方法,也是对一些注解的属性进行设置值操作. 走个checkCandidat检查方法,确保bd再registry中不存在的,如果存在的话,那就说明是已经注册过了的.     //如果是不存在的话,就会new一个BeanDefinitionHolder来,然后走registerBeanDefinition给注册到Spring容器中来. 最后返回扫描获取到的bdHolder集合来.     </span></span><br><span class="line">         Set&lt;BeanDefinitionHolder&gt; scannedBeanDefinitions =</span><br><span class="line">               <span class="built_in">this</span>.componentScanParser.parse(componentScan, sourceClass.getMetadata().getClassName());</span><br><span class="line">         <span class="comment">// Check the set of scanned definitions for any further config classes and parse recursively if needed</span></span><br><span class="line">         <span class="keyword">for</span> (BeanDefinitionHolder holder : scannedBeanDefinitions) &#123;</span><br><span class="line">            <span class="type">BeanDefinition</span> <span class="variable">bdCand</span> <span class="operator">=</span> holder.getBeanDefinition().getOriginatingBeanDefinition();</span><br><span class="line">            <span class="keyword">if</span> (bdCand == <span class="literal">null</span>) &#123;</span><br><span class="line">               bdCand = holder.getBeanDefinition();</span><br><span class="line">            &#125;</span><br><span class="line"><span class="comment">// 可以看到这里, 我们在最初进入到processConfigBeanDefinitions来的时候,其实就已经是调用了这个方法,那么我们这里扫描获取的bean在此调用这个方法. 也就是确保,扫描获取的bean,也是有一些配置的注解并且也是需要解析的.           </span></span><br><span class="line">            <span class="keyword">if</span> (ConfigurationClassUtils.checkConfigurationClassCandidate(bdCand, <span class="built_in">this</span>.metadataReaderFactory)) &#123;</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ConfigurationClassParser#processConfigurationClass这里最后也是走到这里了.</span></span><br><span class="line"><span class="comment">// 最初我们是从parse.parse() 进来的,也是走的ConfigurationClassParser#processConfigurationClas,这里又走到了该方法.</span></span><br><span class="line"><span class="comment">// 也就说我们是调用这个方法,只要满足条件的话,就会一直调用这个方法,直到不满足条件为止.                </span></span><br><span class="line">               parse(bdCand.getBeanClassName(), holder.getBeanName());</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process any @Import annotations</span></span><br><span class="line"><span class="comment">// 这里是对 @Import 注解进行处理. 该方法是有利用 importStack 来控制,</span></span><br><span class="line"><span class="comment">// 其内部又分为 @ImportSelector/@ImportBeanDefinitionRegistrar/无注解这三种情况.</span></span><br><span class="line"><span class="comment">// 获取完 bean 信息后,就又走到了org.springframework.context.annotation.ConfigurationClassParser#processConfigurationClass方法来.</span></span><br><span class="line"><span class="comment">// 最后importStack 调用 pop 给数据给弹出来.    </span></span><br><span class="line">   processImports(configClass, sourceClass, getImports(sourceClass), <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process any @ImportResource annotations</span></span><br><span class="line"><span class="comment">// 对@ImportResource是否有进行判断.    </span></span><br><span class="line">   <span class="type">AnnotationAttributes</span> <span class="variable">importResource</span> <span class="operator">=</span></span><br><span class="line">         AnnotationConfigUtils.attributesFor(sourceClass.getMetadata(), ImportResource.class);</span><br><span class="line">   <span class="keyword">if</span> (importResource != <span class="literal">null</span>) &#123;</span><br><span class="line">      String[] resources = importResource.getStringArray(<span class="string">&quot;locations&quot;</span>);</span><br><span class="line">      Class&lt;? <span class="keyword">extends</span> <span class="title class_">BeanDefinitionReader</span>&gt; readerClass = importResource.getClass(<span class="string">&quot;reader&quot;</span>);</span><br><span class="line">      <span class="keyword">for</span> (String resource : resources) &#123;</span><br><span class="line">         <span class="type">String</span> <span class="variable">resolvedResource</span> <span class="operator">=</span> <span class="built_in">this</span>.environment.resolveRequiredPlaceholders(resource);</span><br><span class="line">         configClass.addImportedResource(resolvedResource, readerClass);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process individual @Bean methods</span></span><br><span class="line"><span class="comment">// @Bean 注解处理.</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.ConfigurationClassParser#retrieveBeanMethodMetadata , </span></span><br><span class="line"><span class="comment">// 这里对于主入口类进来,是没有这个配置的.    </span></span><br><span class="line">   Set&lt;MethodMetadata&gt; beanMethods = retrieveBeanMethodMetadata(sourceClass);</span><br><span class="line">   <span class="keyword">for</span> (MethodMetadata methodMetadata : beanMethods) &#123;</span><br><span class="line">      configClass.addBeanMethod(<span class="keyword">new</span> <span class="title class_">BeanMethod</span>(methodMetadata, configClass));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process default methods on interfaces</span></span><br><span class="line"><span class="comment">// 对接口的进行处理. 这里目前也是没有的.    </span></span><br><span class="line">   processInterfaces(configClass, sourceClass);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Process superclass, if any</span></span><br><span class="line"><span class="comment">// 先是判断是不是有父类.    </span></span><br><span class="line">   <span class="keyword">if</span> (sourceClass.getMetadata().hasSuperClass()) &#123;</span><br><span class="line"><span class="comment">// 获取出父类信息       </span></span><br><span class="line">      <span class="type">String</span> <span class="variable">superclass</span> <span class="operator">=</span> sourceClass.getMetadata().getSuperClassName();</span><br><span class="line"><span class="comment">// 父类不是null,不是java开头并且knownSuperclasses中不存在,就满满足条件.       </span></span><br><span class="line">      <span class="keyword">if</span> (superclass != <span class="literal">null</span> &amp;&amp; !superclass.startsWith(<span class="string">&quot;java&quot;</span>) &amp;&amp;</span><br><span class="line">            !<span class="built_in">this</span>.knownSuperclasses.containsKey(superclass)) &#123;</span><br><span class="line">         <span class="built_in">this</span>.knownSuperclasses.put(superclass, configClass);</span><br><span class="line">         <span class="comment">// Superclass found, return its annotation metadata and recurse</span></span><br><span class="line">         <span class="keyword">return</span> sourceClass.getSuperClass();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// No superclass -&gt; processing is complete</span></span><br><span class="line">   <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>这里可以看到 doProcessConfigurationClass方法,是传入进来主类入口进行解析, 然后没满足一个条件的bean,都会在走一遍解析的方法,直到都走到没满足条件的.</strong></p>
<h6 id="org-springframework-context-annotation-ConfigurationClassUtils-checkConfigurationClassCandidate方法"><a href="#org-springframework-context-annotation-ConfigurationClassUtils-checkConfigurationClassCandidate方法" class="headerlink" title="org.springframework.context.annotation.ConfigurationClassUtils#checkConfigurationClassCandidate方法"></a>org.springframework.context.annotation.ConfigurationClassUtils#checkConfigurationClassCandidate方法</h6><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Check whether the given bean definition is a candidate for a configuration class</span></span><br><span class="line"><span class="comment"> * (or a nested component class declared within a configuration/component class,</span></span><br><span class="line"><span class="comment"> * to be auto-registered as well), and mark it accordingly.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanDef the bean definition to check</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> metadataReaderFactory the current factory in use by the caller</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> whether the candidate qualifies as (any kind of) configuration class</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">checkConfigurationClassCandidate</span><span class="params">(</span></span><br><span class="line"><span class="params">      BeanDefinition beanDef, MetadataReaderFactory metadataReaderFactory)</span> &#123;</span><br><span class="line"><span class="comment">// 先获取 beanName 出来</span></span><br><span class="line">   <span class="type">String</span> <span class="variable">className</span> <span class="operator">=</span> beanDef.getBeanClassName();</span><br><span class="line">   <span class="keyword">if</span> (className == <span class="literal">null</span> || beanDef.getFactoryMethodName() != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   AnnotationMetadata metadata;</span><br><span class="line"><span class="comment">// 判断 bd 是不是AnnotatedBeanDefinition 并且 确认 beanName是不是与前面获取出来的classsName是一样的.    </span></span><br><span class="line">   <span class="keyword">if</span> (beanDef <span class="keyword">instanceof</span> AnnotatedBeanDefinition &amp;&amp;</span><br><span class="line">         className.equals(((AnnotatedBeanDefinition) beanDef).getMetadata().getClassName())) &#123;</span><br><span class="line">      <span class="comment">// Can reuse the pre-parsed metadata from the given BeanDefinition...</span></span><br><span class="line"><span class="comment">// 获取类上的注解.我们这里获取出来的是 @Import 和 @ComponentScan       </span></span><br><span class="line">      metadata = ((AnnotatedBeanDefinition) beanDef).getMetadata();</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (beanDef <span class="keyword">instanceof</span> AbstractBeanDefinition &amp;&amp; ((AbstractBeanDefinition) beanDef).hasBeanClass()) &#123;</span><br><span class="line">      <span class="comment">// Check already loaded Class if present...</span></span><br><span class="line">      <span class="comment">// since we possibly can&#x27;t even load the class file for this Class.</span></span><br><span class="line">      Class&lt;?&gt; beanClass = ((AbstractBeanDefinition) beanDef).getBeanClass();</span><br><span class="line">      <span class="keyword">if</span> (BeanFactoryPostProcessor.class.isAssignableFrom(beanClass) ||</span><br><span class="line">            BeanPostProcessor.class.isAssignableFrom(beanClass) ||</span><br><span class="line">            AopInfrastructureBean.class.isAssignableFrom(beanClass) ||</span><br><span class="line">            EventListenerFactory.class.isAssignableFrom(beanClass)) &#123;</span><br><span class="line">         <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      metadata = AnnotationMetadata.introspect(beanClass);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="type">MetadataReader</span> <span class="variable">metadataReader</span> <span class="operator">=</span> metadataReaderFactory.getMetadataReader(className);</span><br><span class="line">         metadata = metadataReader.getAnnotationMetadata();</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">            logger.debug(<span class="string">&quot;Could not find class file for introspecting configuration annotations: &quot;</span> +</span><br><span class="line">                  className, ex);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 获取@Configuration,我们这里没有,所以获取出来的null.   </span></span><br><span class="line">   Map&lt;String, Object&gt; config = metadata.getAnnotationAttributes(Configuration.class.getName());</span><br><span class="line">   <span class="keyword">if</span> (config != <span class="literal">null</span> &amp;&amp; !Boolean.FALSE.equals(config.get(<span class="string">&quot;proxyBeanMethods&quot;</span>))) &#123;</span><br><span class="line">      beanDef.setAttribute(CONFIGURATION_CLASS_ATTRIBUTE, CONFIGURATION_CLASS_FULL);</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// 注意这里的 isConfigurationCandidate方法,org.springframework.context.annotation.ConfigurationClassUtils#isConfigurationCandidate</span></span><br><span class="line"><span class="comment">// @Component/@ComponentScan/@Import/@ImportResource,只要有其中的一种的话，那么返回的就是true. </span></span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (config != <span class="literal">null</span> || isConfigurationCandidate(metadata)) &#123;</span><br><span class="line"><span class="comment">// CONFIGURATION_CLASS_ATTRIBUTE 对应的值是org.springframework.context.annotation.ConfigurationClassPostProcessor.configurationClass       </span></span><br><span class="line">      beanDef.setAttribute(CONFIGURATION_CLASS_ATTRIBUTE, CONFIGURATION_CLASS_LITE);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// It&#x27;s a full or lite configuration candidate... Let&#x27;s determine the order value, if any.</span></span><br><span class="line"><span class="comment">// 获取 order,如果有的话,就会set进去.    </span></span><br><span class="line">   <span class="type">Integer</span> <span class="variable">order</span> <span class="operator">=</span> getOrder(metadata);</span><br><span class="line">   <span class="keyword">if</span> (order != <span class="literal">null</span>) &#123;</span><br><span class="line">      beanDef.setAttribute(ORDER_ATTRIBUTE, order);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>可以看到这个方法最主的就是对一些类上是否有注解进行判断, 如果满足 @Configuration&#x2F;@Component&#x2F;@ComponentScan&#x2F;@Import&#x2F;@ImportResource,那么返回的就是会true,同时也会set一个CONFIGURATION_CLASS_ATTRIBUTE属性到bd里面来.</strong></p>
<h4 id="getBean方法分析"><a href="#getBean方法分析" class="headerlink" title="getBean方法分析"></a>getBean方法分析</h4><p> getBean 不仅仅是获取bean的效果,更是创建bean的，可以看到getBean最后走到了createBean方法来.</p>
<p> org.springframework.beans.factory.support.DefaultListableBeanFactory#preInstantiateSingletons : 这里我们直接定位到这个方法,来看下是怎么调用的,调用之前&#x2F;实例化bean等过程,又做了什么事情？</p>
<h5 id="preInstantiateSingletons-方法"><a href="#preInstantiateSingletons-方法" class="headerlink" title="preInstantiateSingletons 方法"></a>preInstantiateSingletons 方法</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">preInstantiateSingletons</span><span class="params">()</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">      logger.trace(<span class="string">&quot;Pre-instantiating singletons in &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Iterate over a copy to allow for init methods which in turn register new bean definitions.</span></span><br><span class="line">   <span class="comment">// While this may not be part of the regular factory bootstrap, it does otherwise work fine.</span></span><br><span class="line"><span class="comment">// 从 beanDefinitionNames 中获取出 beanName的集合.</span></span><br><span class="line"><span class="comment">// 这里获取出来的 beanNameList 不仅仅有Spring内部的,还有我们自己的.    </span></span><br><span class="line">   List&lt;String&gt; beanNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(<span class="built_in">this</span>.beanDefinitionNames);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Trigger initialization of all non-lazy singleton beans...</span></span><br><span class="line">   <span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line"><span class="comment">// bd不是抽象的,是单列的,不是赖加载的,就进入到这里来.       </span></span><br><span class="line">      <span class="keyword">if</span> (!bd.isAbstract() &amp;&amp; bd.isSingleton() &amp;&amp; !bd.isLazyInit()) &#123;</span><br><span class="line">    <span class="comment">// 判断是不是 FactoryBean      </span></span><br><span class="line">         <span class="keyword">if</span> (isFactoryBean(beanName)) &#123;</span><br><span class="line">            <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> getBean(FACTORY_BEAN_PREFIX + beanName);</span><br><span class="line">            <span class="keyword">if</span> (bean <span class="keyword">instanceof</span> FactoryBean) &#123;</span><br><span class="line">               <span class="keyword">final</span> FactoryBean&lt;?&gt; factory = (FactoryBean&lt;?&gt;) bean;</span><br><span class="line">               <span class="type">boolean</span> isEagerInit;</span><br><span class="line">               <span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span> &amp;&amp; factory <span class="keyword">instanceof</span> SmartFactoryBean) &#123;</span><br><span class="line">                  isEagerInit = AccessController.doPrivileged((PrivilegedAction&lt;Boolean&gt;)</span><br><span class="line">                              ((SmartFactoryBean&lt;?&gt;) factory)::isEagerInit,</span><br><span class="line">                        getAccessControlContext());</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">else</span> &#123;</span><br><span class="line">                  isEagerInit = (factory <span class="keyword">instanceof</span> SmartFactoryBean &amp;&amp;</span><br><span class="line">                        ((SmartFactoryBean&lt;?&gt;) factory).isEagerInit());</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">if</span> (isEagerInit) &#123;</span><br><span class="line">                  getBean(beanName);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// 如果不是 FactroyBean的话,就直接走 getBean方法.       </span></span><br><span class="line">            getBean(beanName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Trigger post-initialization callback for all applicable beans...</span></span><br><span class="line"><span class="comment">// 根据 beanNames 来进行迭代.    </span></span><br><span class="line">   <span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">   <span class="comment">// 根据 beanName 来获取对象.    </span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#getSingleton(java.lang.String, boolean)       </span></span><br><span class="line">      <span class="type">Object</span> <span class="variable">singletonInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">  <span class="comment">// 满足是 SmartInitializingSingleton 接口的子类. 最后就都会调用 afterSingletonsInstantiated 方法, 这个也算是bean自身实现SmartInitializingSingleton接口来做的一种扩展.  </span></span><br><span class="line">      <span class="keyword">if</span> (singletonInstance <span class="keyword">instanceof</span> SmartInitializingSingleton) &#123;</span><br><span class="line">         <span class="keyword">final</span> <span class="type">SmartInitializingSingleton</span> <span class="variable">smartSingleton</span> <span class="operator">=</span> (SmartInitializingSingleton) singletonInstance;</span><br><span class="line">         <span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span>) &#123;</span><br><span class="line">            AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">               smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">               <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            &#125;, getAccessControlContext());</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 preInstanitateSingletons方法，根据beanDefinitionNames中注册过的beanName集合,调用getBean方法来创建这个bean. 当创建完所有的bean后,判断是不是有实现 SmartInitializingSingleton 接口的bean,如果有的话, 就会调用这个bean 的afterSingletonsInstantiated方法.</p>
<h6 id="doGetBean-方法"><a href="#doGetBean-方法" class="headerlink" title="doGetBean() 方法"></a>doGetBean() 方法</h6><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Return an instance, which may be shared or independent, of the specified bean.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> name the name of the bean to retrieve</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> requiredType the required type of the bean to retrieve</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> args arguments to use when creating a bean instance using explicit arguments</span></span><br><span class="line"><span class="comment"> * (only applied when creating a new instance as opposed to retrieving an existing one)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> typeCheckOnly whether the instance is obtained for a type check,</span></span><br><span class="line"><span class="comment"> * not for actual use</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> an instance of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeansException if the bean could not be created</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line"><span class="keyword">protected</span> &lt;T&gt; T <span class="title function_">doGetBean</span><span class="params">(<span class="keyword">final</span> String name, <span class="meta">@Nullable</span> <span class="keyword">final</span> Class&lt;T&gt; requiredType,</span></span><br><span class="line"><span class="params">      <span class="meta">@Nullable</span> <span class="keyword">final</span> Object[] args, <span class="type">boolean</span> typeCheckOnly)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"><span class="comment">// 获取beanName</span></span><br><span class="line">   <span class="keyword">final</span> <span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> transformedBeanName(name);</span><br><span class="line">   Object bean;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Eagerly check singleton cache for manually registered singletons.</span></span><br><span class="line"> <span class="comment">//   这里是判断是不是手动给添加到单例池里面去的.</span></span><br><span class="line">   <span class="type">Object</span> <span class="variable">sharedInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">   <span class="keyword">if</span> (sharedInstance != <span class="literal">null</span> &amp;&amp; args == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         <span class="keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">            logger.trace(<span class="string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName +</span><br><span class="line">                  <span class="string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            logger.trace(<span class="string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 如果是从单例池里面获取出来的,就走这个方法.    </span></span><br><span class="line">      bean = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="literal">null</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Fail if we&#x27;re already creating this bean instance:</span></span><br><span class="line">      <span class="comment">// We&#x27;re assumably within a circular reference.</span></span><br><span class="line">  <span class="comment">// 判断这个bean当前是不是已经在注册了,如果是的话,就会抛出异常来.  </span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.AbstractBeanFactory#prototypesCurrentlyInCreation,利用ThreadLocal来记录值,如果beanName是相同的话就会返回ture,否则就返回flase,这里返回的是false.       </span></span><br><span class="line">      <span class="keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Check if bean definition exists in this factory.</span></span><br><span class="line"> <span class="comment">//  org.springframework.beans.factory.support.AbstractBeanFactory#getParentBeanFactory获取父工厂,这里返回的是null,也就是说是没有的.所以下面的if条件也就不会进去.     </span></span><br><span class="line">      <span class="type">BeanFactory</span> <span class="variable">parentBeanFactory</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">      <span class="keyword">if</span> (parentBeanFactory != <span class="literal">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">         <span class="comment">// Not found -&gt; check parent.</span></span><br><span class="line">         <span class="type">String</span> <span class="variable">nameToLookup</span> <span class="operator">=</span> originalBeanName(name);</span><br><span class="line">         <span class="keyword">if</span> (parentBeanFactory <span class="keyword">instanceof</span> AbstractBeanFactory) &#123;</span><br><span class="line">            <span class="keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(</span><br><span class="line">                  nameToLookup, requiredType, args, typeCheckOnly);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> <span class="keyword">if</span> (args != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Delegation to parent with explicit args.</span></span><br><span class="line">            <span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> <span class="keyword">if</span> (requiredType != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// No args -&gt; delegate to standard getBean method.</span></span><br><span class="line">            <span class="keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// typeCheckOnly 在此处的值是 false.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.AbstractBeanFactory#alreadyCreated,利用Set集合来标记是否创建,可以看到往alreadyCreated中添加元素进去的时候,还使用了synchronized来加锁判断并且使用了双重if,可以看到我们在接触单例模式的时候，也是有使用  synchronized + 双重if的.      </span></span><br><span class="line">      <span class="keyword">if</span> (!typeCheckOnly) &#123;</span><br><span class="line">         markBeanAsCreated(beanName);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 获取出 bd 来,org.springframework.beans.factory.support.AbstractBeanFactory#mergedBeanDefinitions,从这个ConcurrentHashMap中获取出来,也就是说这个mergedBeanDefinitions Map 中,key就是beanName,value就是对应的bd.          </span></span><br><span class="line">         <span class="keyword">final</span> <span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line">          </span><br><span class="line"><span class="comment">// 对 bd 进行检查,如果是抽象的话,就会抛出异常来.          </span></span><br><span class="line">         checkMergedBeanDefinition(mbd, beanName, args);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Guarantee initialization of beans that the current bean depends on.</span></span><br><span class="line"> <span class="comment">// 获取 @DependsOn 注解.并且对 @Depends进行处理.         </span></span><br><span class="line">         String[] dependsOn = mbd.getDependsOn();</span><br><span class="line">         <span class="keyword">if</span> (dependsOn != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (String dep : dependsOn) &#123;</span><br><span class="line">               <span class="keyword">if</span> (isDependent(beanName, dep)) &#123;</span><br><span class="line">                  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                        <span class="string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">               &#125;</span><br><span class="line">               registerDependentBean(dep, beanName);</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                  getBean(dep);</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;</span><br><span class="line">                  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                        <span class="string">&quot;&#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Create bean instance.</span></span><br><span class="line"> <span class="comment">// 确保 bd 是单例的.     </span></span><br><span class="line">         <span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line">            sharedInstance = getSingleton(beanName, () -&gt; &#123;</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                  <span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">                  <span class="comment">// Explicitly remove instance from singleton cache: It might have been put there</span></span><br><span class="line">                  <span class="comment">// eagerly by the creation process, to allow for circular reference resolution.</span></span><br><span class="line">                  <span class="comment">// Also remove any beans that received a temporary reference to the bean.</span></span><br><span class="line">                  destroySingleton(beanName);</span><br><span class="line">                  <span class="keyword">throw</span> ex;</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">             </span><br><span class="line"><span class="comment">// 获取bean实例             </span></span><br><span class="line">            bean = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);</span><br><span class="line">         &#125;</span><br><span class="line"><span class="comment">// 这里是实例化一个 多列的 bean</span></span><br><span class="line">         <span class="keyword">else</span> <span class="keyword">if</span> (mbd.isPrototype()) &#123;</span><br><span class="line">            <span class="comment">// It&#x27;s a prototype -&gt; create a new instance.</span></span><br><span class="line">            <span class="type">Object</span> <span class="variable">prototypeInstance</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">               beforePrototypeCreation(beanName);</span><br><span class="line">               prototypeInstance = createBean(beanName, mbd, args);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">finally</span> &#123;</span><br><span class="line">               afterPrototypeCreation(beanName);</span><br><span class="line">            &#125;</span><br><span class="line">            bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 这里操作的,不仅单列也不是多列.</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">scopeName</span> <span class="operator">=</span> mbd.getScope();</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Scope</span> <span class="variable">scope</span> <span class="operator">=</span> <span class="built_in">this</span>.scopes.get(scopeName);</span><br><span class="line">            <span class="keyword">if</span> (scope == <span class="literal">null</span>) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">               <span class="type">Object</span> <span class="variable">scopedInstance</span> <span class="operator">=</span> scope.get(beanName, () -&gt; &#123;</span><br><span class="line">                  beforePrototypeCreation(beanName);</span><br><span class="line">                  <span class="keyword">try</span> &#123;</span><br><span class="line">                     <span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">                  &#125;</span><br><span class="line">                  <span class="keyword">finally</span> &#123;</span><br><span class="line">                     afterPrototypeCreation(beanName);</span><br><span class="line">                  &#125;</span><br><span class="line">               &#125;);</span><br><span class="line">               bean = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">catch</span> (IllegalStateException ex) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName,</span><br><span class="line">                     <span class="string">&quot;Scope &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27; is not active for the current thread; consider &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;defining a scoped proxy for this bean if you intend to refer to it from a singleton&quot;</span>,</span><br><span class="line">                     ex);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         cleanupAfterBeanCreationFailure(beanName);</span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Check if required type matches the type of the actual bean instance.</span></span><br><span class="line"> <span class="comment">// 不满足条件,所以没进入到这里.   </span></span><br><span class="line">   <span class="keyword">if</span> (requiredType != <span class="literal">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="type">T</span> <span class="variable">convertedBean</span> <span class="operator">=</span> getTypeConverter().convertIfNecessary(bean, requiredType);</span><br><span class="line">         <span class="keyword">if</span> (convertedBean == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> convertedBean;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (TypeMismatchException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">            logger.trace(<span class="string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="string">&quot;&#x27; to required type &#x27;&quot;</span> +</span><br><span class="line">                  ClassUtils.getQualifiedName(requiredType) + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接返回了 bean 信息.    </span></span><br><span class="line">   <span class="keyword">return</span> (T) bean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>doGetBean方法 : 可以看到该方法主要是对 bean 分为三种类型来进行初始化 , 分别是 mbd.isSingleton&#x2F;mbd.isPrototype()&#x2F;非前二者 这三种情况. 在分这三种情况之前,还对@DependsOn 注解来进行分析,也就说当你初始化这个bean的时候,如果它依赖了一个宁外的bean,就会先去初始化宁外一个bean,也就是调用了 getBean 方法, 而getBean方法就是走的 doGetBean() —&gt; createBean() 也就是走到了自身这里,是一种递归调用.</p>
<p>然后我们这里是单例的,自然就往下走了 createBean 方法.</p>
<h6 id="createBean-方法"><a href="#createBean-方法" class="headerlink" title="createBean() 方法"></a>createBean() 方法</h6><p>从名字来看,还是可以很很闲的感受到,是创建bean的方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Central method of this class: creates a bean instance,</span></span><br><span class="line"><span class="comment"> * populates the bean instance, applies post-processors, etc.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #doCreateBean</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> Object <span class="title function_">createBean</span><span class="params">(String beanName, RootBeanDefinition mbd, <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">      <span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">      logger.trace(<span class="string">&quot;Creating instance of bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="type">RootBeanDefinition</span> <span class="variable">mbdToUse</span> <span class="operator">=</span> mbd;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Make sure bean class is actually resolved at this point, and</span></span><br><span class="line">   <span class="comment">// clone the bean definition in case of a dynamically resolved Class</span></span><br><span class="line">   <span class="comment">// which cannot be stored in the shared merged bean definition.</span></span><br><span class="line"><span class="comment">// 确定bean的class, 如果bd有beanClass的信息,就会直接返回.    </span></span><br><span class="line">   Class&lt;?&gt; resolvedClass = resolveBeanClass(mbd, beanName);</span><br><span class="line"><span class="comment">//如果这里从bd获取出来的class是有值的,然后bd是没有beanCalss,获取出来的beanClassName也是null的话,那么这里就会重新来构建出一个bd,并且设置上 beanClass信息.    </span></span><br><span class="line">   <span class="keyword">if</span> (resolvedClass != <span class="literal">null</span> &amp;&amp; !mbd.hasBeanClass() &amp;&amp; mbd.getBeanClassName() != <span class="literal">null</span>) &#123;</span><br><span class="line">      mbdToUse = <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(mbd);</span><br><span class="line">      mbdToUse.setBeanClass(resolvedClass);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Prepare method overrides.</span></span><br><span class="line"><span class="comment">// 准备重写的方法信息,先判断是不是有重写的方法,    </span></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      mbdToUse.prepareMethodOverrides();</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionStoreException</span>(mbdToUse.getResourceDescription(),</span><br><span class="line">            beanName, <span class="string">&quot;Validation of method overrides failed&quot;</span>, ex);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 这是 Spring 系统默认的后置处理器,是有六个的.       </span></span><br><span class="line"><span class="comment">// ApplicationContextAwareProcessor ,  ConfigurationClassPostProcessor$ImportAwareBeanPostProcessor , PostProcessorRegistrationDelegate$BeanPostProcessorCheck , CommonAnnotationBeanPostProcessor ,  AutowiredAnnotationBeanPostProcessor ,  ApplicationListenerDetector ,        </span></span><br><span class="line">       </span><br><span class="line">      <span class="comment">// Give BeanPostProcessors a chance to return a proxy instead of the target bean instance.</span></span><br><span class="line"><span class="comment">// Apply before-instantiation post-processors, resolving whether there is a before-instantiation shortcut for the specified bean. 可以看到这里有个应用实例化前的处理器,</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#resolveBeforeInstantiation,可以看到这个方法里面,满足条件的话,会调用applyBeanPostProcessorsBeforeInstantiation() / applyBeanPostProcessorsAfterInitialization() 这二个方法的.</span></span><br><span class="line"><span class="comment">// 走完 applyBeanPostProcessorsBeforeInstantiation 方法,如果前置处理器能够返回bean回来并且不是null的话,就会继续走applyBeanPostProcessorsAfterInitialization方法.</span></span><br><span class="line"><span class="comment">// 我们这里返回的 bean 是null,如果不是null的话,就会直接返回的.       </span></span><br><span class="line">      <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> resolveBeforeInstantiation(beanName, mbdToUse);</span><br><span class="line">      <span class="keyword">if</span> (bean != <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="keyword">return</span> bean;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbdToUse.getResourceDescription(), beanName,</span><br><span class="line">            <span class="string">&quot;BeanPostProcessor before instantiation of bean failed&quot;</span>, ex);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 上面的前置处理器applyBeanPostProcessorsBeforeInstantiation返回的bean是null的话,就会接着这个下面继续往下走.  于是就有了走 doCreateBean 方法.   </span></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="type">Object</span> <span class="variable">beanInstance</span> <span class="operator">=</span> doCreateBean(beanName, mbdToUse, args);</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;Finished creating instance of bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line"> <span class="comment">// 返回 bean 对象回去.      </span></span><br><span class="line">      <span class="keyword">return</span> beanInstance;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (BeanCreationException | ImplicitlyAppearedSingletonException ex) &#123;</span><br><span class="line">      <span class="comment">// A previously detected exception with proper bean creation context already,</span></span><br><span class="line">      <span class="comment">// or illegal singleton state to be communicated up to DefaultSingletonBeanRegistry.</span></span><br><span class="line">      <span class="keyword">throw</span> ex;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">            mbdToUse.getResourceDescription(), beanName, <span class="string">&quot;Unexpected exception during bean creation&quot;</span>, ex);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>createBean 方法 : 可以看到createBean在创建之前走了前置处理器,如果前置处理器返回的bean不是null,那么也就没有下面的doCreateBean什么事情了. 如果返回的bean是null的话,那么就会走到下面的doCreateBean方法,可以理解为这个方法才是真正调用反射去获取 bean 对象实例的方法 , 并且其返回值 beanInstance 是直接返回返回去了,也没有做什么其他的处理.</p>
<h6 id="doCreateBean-方法"><a href="#doCreateBean-方法" class="headerlink" title="doCreateBean() 方法"></a>doCreateBean() 方法</h6><p>可以感觉到 doCreateBean 就是真正实例化bean的方法, 是不是Spring 加上了 do 开头的方法,才是真正干活的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Actually create the specified bean. Pre-creation processing has already happened</span></span><br><span class="line"><span class="comment"> * at this point, e.g. checking &#123;<span class="doctag">@code</span> postProcessBeforeInstantiation&#125; callbacks.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Differentiates between default bean instantiation, use of a</span></span><br><span class="line"><span class="comment"> * factory method, and autowiring a constructor.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> mbd the merged bean definition for the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> args explicit arguments to use for constructor or factory method invocation</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> a new instance of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeanCreationException if the bean could not be created</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #instantiateBean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #instantiateUsingFactoryMethod</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #autowireConstructor</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> Object <span class="title function_">doCreateBean</span><span class="params">(<span class="keyword">final</span> String beanName, <span class="keyword">final</span> RootBeanDefinition mbd, <span class="keyword">final</span> <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">      <span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Instantiate the bean.</span></span><br><span class="line">   <span class="type">BeanWrapper</span> <span class="variable">instanceWrapper</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="comment">// 确定是单例,    </span></span><br><span class="line">   <span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line"><span class="comment">// 从org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#factoryBeanInstanceCache缓存中remove掉.       </span></span><br><span class="line">      instanceWrapper = <span class="built_in">this</span>.factoryBeanInstanceCache.remove(beanName);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (instanceWrapper == <span class="literal">null</span>) &#123;</span><br><span class="line"> <span class="comment">// 创建 bean 的实例对象.</span></span><br><span class="line"> <span class="comment">// 先根据 bd 获取出 beanClass,根据beanClass获取出如果是null并且不是public并且无参数构造函数不是public的话,就会抛出一个BeanCreationException异常来. </span></span><br><span class="line"><span class="comment">// 从bd获取出实例提供者信息,这里获取出来的是Null,所以也就不会往下走.</span></span><br><span class="line"><span class="comment">// 获取 mbd.getFactoryMethodName() 操作</span></span><br><span class="line"><span class="comment">// 用变量resolved/autowireNecessary布尔类型的来控制一些流程,  用传入进来的args参数来决定是走无参构造函数还是在有参构造函数,如果args是null的话,就走无参数构造函数.</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#determineConstructorsFromBeanPostProcessors, 该方法是获取出全部的 后置处理器,如果后置处理器是继承了SmartInstantiationAwareBeanPostProcessor的话,就会走到后置处理器的determineCandidateConstructors方法来,  根据 Constructor&lt;?&gt;[] ctors = ibp.determineCandidateConstructors(beanClass, beanName) 可以看到,最后返回的是一个构造方法,可以看到org.springframework.beans.factory.annotation.AutowiredAnnotationBeanPostProcessor#determineCandidateConstructors这个地方来. 这里目测是对@Autowired注解注入的对象进行操作.</span></span><br><span class="line"><span class="comment">//最后,看到这个方法:org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#instantiateBean   ----&gt;   org.springframework.beans.factory.support.SimpleInstantiationStrategy#instantiate(org.springframework.beans.factory.support.RootBeanDefinition, java.lang.String, org.springframework.beans.factory.BeanFactory) 走到这里, 先判断没有重写的方法,接着判断如果是接口的话,就会抛出异常来,用constructorToUse = clazz.getDeclaredConstructor();获取出构造方法,最后用BeanUtils.instantiateClass(constructorToUse)来实例化对象,可以看到这行代码走完,我们在无参构造函数中的输出语句就可以打印出来了.  将我们实例化出来的对象beanInstance用BeanWrapperImpl包装下,所以这里最后返回的就是   BeanWrapperImpl , 是对我们的目标对象进行一层包装过了的.     </span></span><br><span class="line">      instanceWrapper = createBeanInstance(beanName, mbd, args);</span><br><span class="line">   &#125;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 从包装了beanInstance的BeanWrapperImpl中获取出来bean和beanType来,    </span></span><br><span class="line">   <span class="keyword">final</span> <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> instanceWrapper.getWrappedInstance();</span><br><span class="line">   Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 赋值beanType给mbd.resolvedTargetType    </span></span><br><span class="line">   <span class="keyword">if</span> (beanType != NullBean.class) &#123;</span><br><span class="line">      mbd.resolvedTargetType = beanType;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow post-processors to modify the merged bean definition.</span></span><br><span class="line">   <span class="keyword">synchronized</span> (mbd.postProcessingLock) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!mbd.postProcessed) &#123;</span><br><span class="line">         <span class="keyword">try</span> &#123;</span><br><span class="line"> <span class="comment">// 这里后走了一个调用后置处理器的方法,是MergedBeanDefinitionPostProcessor接口的子类,就会调用到后置处理器的postProcessMergedBeanDefinition方法.从名字上看,是对bd进行合并的处理操作.	            </span></span><br><span class="line">            applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                  <span class="string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);</span><br><span class="line">         &#125;</span><br><span class="line">         mbd.postProcessed = <span class="literal">true</span>;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Eagerly cache singletons to be able to resolve circular references</span></span><br><span class="line">   <span class="comment">// even when triggered by lifecycle interfaces like BeanFactoryAware.</span></span><br><span class="line"><span class="comment">//  bd是单例的并且是循环引用的并且单例是创建的,就满足这个条件,这里是处理循环依赖问题?还是用于实现BeanFactoryAware这种来避免循环依赖?    </span></span><br><span class="line">   <span class="type">boolean</span> <span class="variable">earlySingletonExposure</span> <span class="operator">=</span> (mbd.isSingleton() &amp;&amp; <span class="built_in">this</span>.allowCircularReferences &amp;&amp;</span><br><span class="line">         isSingletonCurrentlyInCreation(beanName));</span><br><span class="line">   <span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +</span><br><span class="line">               <span class="string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">//  添加到 org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#registeredSingletons 中来.       </span></span><br><span class="line">      addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize the bean instance.</span></span><br><span class="line">   <span class="type">Object</span> <span class="variable">exposedObject</span> <span class="operator">=</span> bean;</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 构建bean,这里走了 InstantiationAwareBeanPostProcessor 接口的实现类的后置处理器,如果满足条件就会走处理器的postProcessAfterInstantiation方法,该方法会返回一个布尔类型的值,如果是false的话,就会跳出循环来的.</span></span><br><span class="line"><span class="comment">// 下面还会走一个InstantiationAwareBeanPostProcessor接口的子类的后置处理器,满足条件就会走后置处理器的postProcessProperties方法,如果获取出来的PropertyValues pvsToUse是null的话,会继续走后置处理器的postProcessPropertyValues方法.       </span></span><br><span class="line">      populateBean(beanName, mbd, instanceWrapper);</span><br><span class="line"><span class="comment">//  这里调用每个后置处理器的 postProcessBeforeInitialization 方法,</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#invokeInitMethods 该方法可以看到(InitializingBean) bean).afterPropertiesSet();对于afterPropertiesSet方法还是有点熟悉的.</span></span><br><span class="line"><span class="comment">//  接着就是调用每个后置处理器的postProcessAfterInitialization方法,       </span></span><br><span class="line">      exposedObject = initializeBean(beanName, exposedObject, mbd);</span><br><span class="line">       </span><br><span class="line"><span class="comment">// 可以看到这二个方法都是在调用后置处理器来进行扩展.       </span></span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      <span class="keyword">if</span> (ex <span class="keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;</span><br><span class="line">         <span class="keyword">throw</span> (BeanCreationException) ex;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">               mbd.getResourceDescription(), beanName, <span class="string">&quot;Initialization of bean failed&quot;</span>, ex);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">//  earlySingletonExposure is true.  </span></span><br><span class="line">   <span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line"><span class="comment">// 从单例池中根据 beanName 来获取对象.       </span></span><br><span class="line">      <span class="type">Object</span> <span class="variable">earlySingletonReference</span> <span class="operator">=</span> getSingleton(beanName, <span class="literal">false</span>);</span><br><span class="line">       </span><br><span class="line"><span class="comment">// 获取出来的对象不是null的话,就会进入到这里来.       </span></span><br><span class="line">      <span class="keyword">if</span> (earlySingletonReference != <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="keyword">if</span> (exposedObject == bean) &#123;</span><br><span class="line">            exposedObject = earlySingletonReference;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;</span><br><span class="line">            String[] dependentBeans = getDependentBeans(beanName);</span><br><span class="line">            Set&lt;String&gt; actualDependentBeans = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(dependentBeans.length);</span><br><span class="line">            <span class="keyword">for</span> (String dependentBean : dependentBeans) &#123;</span><br><span class="line">               <span class="keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;</span><br><span class="line">                  actualDependentBeans.add(dependentBean);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName,</span><br><span class="line">                     <span class="string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; has been injected into other beans [&quot;</span> +</span><br><span class="line">                     StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +</span><br><span class="line">                     <span class="string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;&#x27;getBeanNamesOfType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register bean as disposable.</span></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line"> <span class="comment">// 如果有必要的话,注册任意bean信息.      </span></span><br><span class="line">      registerDisposableBeanIfNecessary(beanName, bean, mbd);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">            mbd.getResourceDescription(), beanName, <span class="string">&quot;Invalid destruction signature&quot;</span>, ex);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> exposedObject;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>doCreateBean() 方法 : 该方法才是正在去走反射来实例化bean的. 并且在实例化这个bean之前和之后,都是有调用许多后置处理器的,也就是这个bean进行一些增强或者其他的处理. 从现在来看,都是Spring内置的处理器.我们后面可以跟着Spring里面的写法,来做相同的扩展处理.</p>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p> 其实可以看到,我们通过这种方式给我们定义的 bean 给注入到 Spring 容器中, 先是通过我们定义的 @ComponentScan(basePackages &#x3D; “com.iyang.bean.bd”) 来扫描，然后将扫描得到的信息给添加到Spring的信息池里面,也就是添加到集合中来了. 最后在getBean 方法中, 通过扫描获取到的beanNames集合进行迭代，然后挨个调用getBean()方法来实例化bean, getBean() 方法中又走了 doGetBean () —-&gt; createBean() —&gt; doCreateBean() 方法， 然后每个方法有各自要做的事情，并且也会走相应的后置处理器.</p>
<p> 最后，这是一个比较详细的getBean分析，但是还有更深入的 , 比如 : @Autowired &#x2F; @DependsOn &#x2F; 循环依赖等注入，需要扩展来讲.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>spring初始化(三)</title>
    <url>/2021/11/03/Java/spring%E5%88%9D%E5%A7%8B%E5%8C%96-%E4%B8%89/</url>
    <content><![CDATA[<h4 id="题记"><a href="#题记" class="headerlink" title="题记"></a>题记</h4><p> 前面讲到了这么多的什么BeanPostProcessor,事件什么的. 如果不写几下代码这里怕是很难弄清楚是个怎么回事. 所以只有看到代码跑,就大致可以看到其效果还是很明显的.</p>
<h4 id="BeanDefinitionRegistryPostProcessor"><a href="#BeanDefinitionRegistryPostProcessor" class="headerlink" title="BeanDefinitionRegistryPostProcessor"></a>BeanDefinitionRegistryPostProcessor</h4><p> 先写一个类简单实现下 BeanDefinitionRegistryPostProcessor 这个接口 :</p>
<p> 运行后的结果是可以很明显的看到我们的打印数据输出结果的.</p>
<p> 程序是怎么运行到这个地方来的呢？</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;) 走到这个方法的</p>
<p>beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class…..),从这个类中获取出来的postProcessorNames,就有包含我们的自己定义的一个.</p>
<p>由于我们自己扩展的这个类,是没有实现 PriorityOrdered&#x2F;Ordered的,所以就放到最后来处理了.</p>
<p>也就是在这个方法,while (reiterate) { … invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);}</p>
<p>走的这个方法的时候,才会去走到我们定义的类。</p>
<p>那是因为这个地方 Spring是有处理顺序的。 先处理 PriorityOrdered.class , 再处理 Ordered.class , 最后处理既没有PriorityOrdered,也没有Ordered的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangRegistryPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanDefinitionRegistryPostProcessor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">GavinYangRegistryPostProcessor</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;GavinYangRegistryPostProcessor 构造方法执行&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanDefinitionRegistry</span><span class="params">(BeanDefinitionRegistry registry)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;调用到 GavinYangRegistryPostProcessor#postProcessBeanDefinitionRegistry方法&quot;</span>);</span><br><span class="line">        String[] beanDefinitionNames = registry.getBeanDefinitionNames();</span><br><span class="line">        <span class="type">String</span> <span class="variable">bdNamesString</span> <span class="operator">=</span> Arrays.toString(beanDefinitionNames);</span><br><span class="line">        System.out.println(<span class="string">&quot;GavinYangRegistryPostProcessor 类中&quot;</span> + bdNamesString);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="BeanFactoryPostProcessor"><a href="#BeanFactoryPostProcessor" class="headerlink" title="BeanFactoryPostProcessor"></a>BeanFactoryPostProcessor</h4><p> 简单实现一下 BeanFactoryPostProcessor 这个接口来看一下效果.</p>
<p> 先写一个类 : 然后实现一下这个接口 BeanFactoryPostProcessor , 我们这里就获取下beanFactory中的所有beanDefinitionNames的数组, 然后打印出来看下效果. 打印结果也是贴在下面的. 接着我们看下其源码是一个怎么样的走向.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangBeanFactoryPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanFactoryPostProcessor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        String[] beanDefinitionNames = beanFactory.getBeanDefinitionNames();</span><br><span class="line">        System.out.println(Arrays.toString(beanDefinitionNames));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//[org.springframework.context.annotation.internalConfigurationAnnotationProcessor, org.springframework.context.annotation.internalAutowiredAnnotationProcessor, org.springframework.context.annotation.internalCommonAnnotationProcessor, org.springframework.context.event.internalEventListenerProcessor, org.springframework.context.event.internalEventListenerFactory, yangBeanOne, gavinYangBeanFactoryPostProcessor, yangBeanPostProcessor, gavinYangLifeImpl]</span></span><br></pre></td></tr></table></figure>

<p>直接看这个类的这个方法，我这里只截取了一部分代码,也就是和 BeanFactoryPostProcessor 有关的代码.</p>
<p>和它没关系的代码,这里就没有去截取了.</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;) .</p>
<p>看源码看是如何调用到这个方法的,还是比较好理解。先是根据 BeanFactoryPostProcessor.class获取出beanName的集合,然后老规矩进行一些特定的排序,当然我这里什么都没做,也就是最后处理哦。</p>
<p>然后跟到invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);这个方法里面,可以看到它迭代这个集合的元素,当然了,我们只需要关注我们自己定义的哪个就可以了,然后就会走其postProcessBeanFactory方法,也就是走到了我们定义的类的这个方法上来了.</p>
<p>OK啦。大致流程就是这个样子的,还是比较好理解的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line"><span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line"><span class="comment">// 这个地方获取出来的数组里面的值,就有我们想看到的.</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerProcessor</span></span><br><span class="line"><span class="comment">//gavinYangBeanFactoryPostProcessor</span></span><br><span class="line"><span class="comment">//当看到第三个是不是非常的熟悉.没错,这就是我们自己定义的.</span></span><br><span class="line">String[] postProcessorNames =</span><br><span class="line">      beanFactory.getBeanNamesForType(BeanFactoryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Separate between BeanFactoryPostProcessors that implement PriorityOrdered,</span></span><br><span class="line"><span class="comment">// Ordered, and the rest.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// 然后先经过一轮排序,就可以看到 我们自己定义的就放入到了nonOrderedPostProcessorNames这个集合中,</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">   <span class="keyword">if</span> (processedBeans.contains(ppName)) &#123;</span><br><span class="line">      <span class="comment">// skip - already processed in first phase above</span></span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">      priorityOrderedPostProcessors.add(beanFactory.getBean(ppName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">      orderedPostProcessorNames.add(ppName);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// First, invoke the BeanFactoryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Next, invoke the BeanFactoryPostProcessors that implement Ordered.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line"><span class="keyword">for</span> (String postProcessorName : orderedPostProcessorNames) &#123;</span><br><span class="line">   orderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">&#125;</span><br><span class="line">sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">invokeBeanFactoryPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Finally, invoke all other BeanFactoryPostProcessors.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line"><span class="keyword">for</span> (String postProcessorName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">   nonOrderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 然后我们直接看到处理nonOrderedPostProcessors这个集合的方法,</span></span><br><span class="line"><span class="comment">//因为这个方法也会走到我们自己定义的类中去</span></span><br><span class="line"><span class="comment">//方法里面对nonOrderedPostProcessors进行迭代,然后一次调用其postProcessBeanFactory方法,同时也传入了beanFactory到里面去.</span></span><br><span class="line">invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);</span><br></pre></td></tr></table></figure>

<h4 id="BeanPostProcessor"><a href="#BeanPostProcessor" class="headerlink" title="BeanPostProcessor"></a>BeanPostProcessor</h4><p>我们先写一个 Bean. 然后可以里面写一个属性,方便标识.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangBeanOne</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 无参构造函数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">YangBeanOne</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;YangBeanOne无参数构造函数&quot;</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后我们自定义一个BeanPostProcessor,其实现了BeanPostProcessor接口.</p>
<p>可以看到的是,我们在if中做了beanName的判断,如果是的话,那么我们就会强转,然后给name字段赋值上GavinYang的值.</p>
<p>我们给断点打到 if这里，然后看进来的堆栈信息,发现其是在初始化bean中,然后调用beanPostProcessor的postProcessAfterInitialization的方法才会走到这里,也就是在doCreateBean这个方法里面.</p>
<p>然后这个 YangBeanPostProcessor 是什么时候给添加到 beanFactory中去的呢？</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#registerBeanPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, org.springframework.context.support.AbstractApplicationContext)在这个方法打上断点,然后你会发现其get出来的postProcessorNames数组,就有我们的这个YangBeanPostProcessor，然后走registerBeanPostProcessors方法的时候,就会给添加到beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangBeanPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanPostProcessor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">&quot;yangBeanOne&quot;</span>.equalsIgnoreCase(beanName))&#123;</span><br><span class="line">            ((YangBeanOne) bean).setName(<span class="string">&quot;GavinYang&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动类 :</p>
<p>这就打印出来的结果就可以看到的是GavinYang,也就是说我们初始化这个bean之后,然后给其name属性赋值上了GavinYang这个值.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpringStartMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>(<span class="string">&quot;com.iyang.spring&quot;</span>);</span><br><span class="line">        <span class="type">YangBeanOne</span> <span class="variable">yangBeanOne</span> <span class="operator">=</span> context.getBean(YangBeanOne.class);</span><br><span class="line">        System.out.println(yangBeanOne.getName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="Lifecycle-扩展"><a href="#Lifecycle-扩展" class="headerlink" title="Lifecycle 扩展"></a>Lifecycle 扩展</h4><p>Lifecycle 扩展要比起 BeanPostProcessor要好理解得多,因为你只用去实现这个接口(SmartLifecycle),然后在org.springframework.context.support.AbstractApplicationContext#finishRefresh到这个方法的时候,就会去调用实现这个接口的对用的方法.</p>
<p>这里我们自己写一个类,然后实现 SmartLifecycle 接口即可。</p>
<p>可以看到下面的打印参数.还是很清楚的明白.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangLifeImpl</span> <span class="keyword">implements</span> <span class="title class_">SmartLifecycle</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getPhase</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Integer.MAX_VALUE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">start</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;调用到了GavinYangLifeImpl.start()方法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">stop</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isRunning</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//YangBeanOne无参数构造函数</span></span><br><span class="line"><span class="comment">//调用到了GavinYangLifeImpl.start()方法</span></span><br><span class="line"><span class="comment">//GavinYang</span></span><br></pre></td></tr></table></figure>

<p>然后我们再看下源码,为什么要实现 SmartLifecycle 这个接口呢？</p>
<p>org.springframework.context.support.DefaultLifecycleProcessor</p>
<p>这里我们先看到 phases ,也就是最底下的代码, 这个集合是有值的情况下先排序,然后再迭代,然后调用到 start()方法, 这个start方法是不是在我们的实现类中可以看到,是不是非常的熟悉感觉.</p>
<p>然后我们在看下,怎么样让这个集合能有值呢？</p>
<p>phases.put(phase, group); 可以看到 put方法这里, autoStartupOnly 是false 或者bean是SmartLifecycle的子类,并且其isAutoStartup方法的是true. 点到SmartLifecycle源码中去看,可以发现这个方法默认是返回的true.</p>
<p>接着在调用getPhase方法, 该方法也就是判断.最最关键的phases集合来了,先从里面get出数据,然后判断数据是不是null,如果是null的话,就先new一个Group出来,然后调用phases的put方法,也就是放入到这个集合中去了</p>
<p>所以这里就是我们为什么要实现 SmartLifecycle 这个接口,就会有启动的效果了的原因.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Internal helpers</span></span><br><span class="line"><span class="comment">// 传入进来的  autoStartupOnly 参数是true.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">startBeans</span><span class="params">(<span class="type">boolean</span> autoStartupOnly)</span> &#123;</span><br><span class="line">   Map&lt;String, Lifecycle&gt; lifecycleBeans = getLifecycleBeans();</span><br><span class="line">   Map&lt;Integer, LifecycleGroup&gt; phases = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">   lifecycleBeans.forEach((beanName, bean) -&gt; &#123;</span><br><span class="line">      <span class="keyword">if</span> (!autoStartupOnly || (bean <span class="keyword">instanceof</span> SmartLifecycle &amp;&amp; ((SmartLifecycle) bean).isAutoStartup())) &#123;</span><br><span class="line">         <span class="type">int</span> <span class="variable">phase</span> <span class="operator">=</span> getPhase(bean);</span><br><span class="line">         <span class="type">LifecycleGroup</span> <span class="variable">group</span> <span class="operator">=</span> phases.get(phase);</span><br><span class="line">         <span class="keyword">if</span> (group == <span class="literal">null</span>) &#123;</span><br><span class="line">            group = <span class="keyword">new</span> <span class="title class_">LifecycleGroup</span>(phase, <span class="built_in">this</span>.timeoutPerShutdownPhase, lifecycleBeans, autoStartupOnly);</span><br><span class="line">            phases.put(phase, group);</span><br><span class="line">         &#125;</span><br><span class="line">         group.add(beanName, bean);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;);</span><br><span class="line">   <span class="keyword">if</span> (!phases.isEmpty()) &#123;</span><br><span class="line">      List&lt;Integer&gt; keys = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(phases.keySet());</span><br><span class="line">      Collections.sort(keys);</span><br><span class="line">      <span class="keyword">for</span> (Integer key : keys) &#123;</span><br><span class="line">         phases.get(key).start();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>可以看到我们基于接口扩展的,还是很好理解的。 跟着源码中的初始化一些类,看哪个类是怎么写的，然后我们跟着写一个。 前提是,你要弄得明白 Spring 这个执行过程的. 不然你跟着写,不是很明白的话,就可能不是很容易看明白或者看懂你这个效果的.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>spring初始化(一)</title>
    <url>/2021/11/03/Java/spring%E5%88%9D%E5%A7%8B%E5%8C%96-%E4%B8%80/</url>
    <content><![CDATA[<h4 id="阅读方法"><a href="#阅读方法" class="headerlink" title="阅读方法"></a>阅读方法</h4><p>最简单的阅读方法,就是创建一个maven项目,让引入Spring的依赖. 然后写上一个main方法,来读取包下的内容,然后写一个bean,即可. 这个bean要在你扫描的包下. 于是我们直接在new的地方打上断点跟进去即可.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;dependencies&gt;</span><br><span class="line"></span><br><span class="line">    &lt;dependency&gt;</span><br><span class="line">        &lt;groupId&gt;org.springframework&lt;/groupId&gt;</span><br><span class="line">        &lt;artifactId&gt;spring-context&lt;/artifactId&gt;</span><br><span class="line">        &lt;version&gt;<span class="number">5.2</span><span class="number">.0</span>.RELEASE&lt;/version&gt;</span><br><span class="line">    &lt;/dependency&gt;</span><br><span class="line"></span><br><span class="line">&lt;/dependencies&gt;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpringStartMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>(<span class="string">&quot;com.iyang.spring&quot;</span>);</span><br><span class="line">        <span class="type">YangBeanOne</span> <span class="variable">yangBeanOne</span> <span class="operator">=</span> context.getBean(YangBeanOne.class);</span><br><span class="line">        System.out.println(yangBeanOne.getClass().toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还有一种就是你去github上clone一个Spring的源码,然后倒入idea,当然你需要gradle环境来构建.然后成功的build一下, 如果成功了的话,就在源码的目录创建一个模块(x项目),然后像上面一样。这样做的好处是,你可以随便修改源码的代码, 你觉得它的哪个地方的代码写到不够好的话,也是可以去修改的.</p>
<h4 id="Debug阅读"><a href="#Debug阅读" class="headerlink" title="Debug阅读"></a>Debug阅读</h4><p>开始debug进行代码的阅读 :</p>
<p>debug就会进入到这个构造函数中, 这里我们先对 this() 和 scan(basePackages) 这二个方法进行阅读, refresh()里面涉及到内容比较多(BeanPostprocess,Aware,event等),不是一下子就能看明白的,是需要大量的时间去仔细阅读的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Create a new AnnotationConfigApplicationContext, scanning for components</span></span><br><span class="line"><span class="comment"> * in the given packages, registering bean definitions for those components,</span></span><br><span class="line"><span class="comment"> * and automatically refreshing the context.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> basePackages the packages to scan for component classes</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">(String... basePackages)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>();</span><br><span class="line">   scan(basePackages);</span><br><span class="line">   refresh();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>this () 方法</p>
<p>可以看到this方法,基本是在做一些对环境初始化的操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Create a new AnnotationConfigApplicationContext that needs to be populated</span></span><br><span class="line"><span class="comment"> * through &#123;<span class="doctag">@link</span> #register&#125; calls and then manually &#123;<span class="doctag">@linkplain</span> #refresh refreshed&#125;.</span></span><br><span class="line"><span class="comment"> 同时还会走到 : org.springframework.context.support.GenericApplicationContext#GenericApplicationContext()这个方法里面来.  this.beanFactory = new DefaultListableBeanFactory(); 可以看到这里是new了一个beanFactory的,也就是我们后面的refresh()方法,可以看到DefaultListableBeanFactroy这个类.</span></span><br><span class="line"><span class="comment"> 再往父类走 : org.springframework.context.support.AbstractApplicationContext#AbstractApplicationContext()就会走到这个类的这个方法来, this.resourcePatternResolver = getResourcePatternResolver(); 这里可以看到是初始化了 resourcePatternResolver.当然了,肯定还有一些new的全局变量的初始化也会进行初始化的.</span></span><br><span class="line"><span class="comment"> </span></span><br><span class="line"><span class="comment"> </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>.reader = <span class="keyword">new</span> <span class="title class_">AnnotatedBeanDefinitionReader</span>(<span class="built_in">this</span>);</span><br><span class="line"><span class="comment">//这个方法对registry,environment和resourceLoader进行赋值,然后根据filter是true,添加了三个filter过滤器.可以看到这个方法虽然带了scanner名字,但是看每个走的方法,好像是没有扫描任何东西,都是对全局参数进行赋值等操作.</span></span><br><span class="line">   <span class="built_in">this</span>.scanner = <span class="keyword">new</span> <span class="title class_">ClassPathBeanDefinitionScanner</span>(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">---------------------------------</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@code</span> AnnotatedBeanDefinitionReader&#125; for the given registry.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;If the registry is &#123;<span class="doctag">@link</span> EnvironmentCapable&#125;, e.g. is an &#123;<span class="doctag">@code</span> ApplicationContext&#125;,</span></span><br><span class="line"><span class="comment">	 * the &#123;<span class="doctag">@link</span> Environment&#125; will be inherited, otherwise a new</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@link</span> StandardEnvironment&#125; will be created and used.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> registry the &#123;<span class="doctag">@code</span> BeanFactory&#125; to load bean definitions into,</span></span><br><span class="line"><span class="comment">	 * in the form of a &#123;<span class="doctag">@code</span> BeanDefinitionRegistry&#125;</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #AnnotatedBeanDefinitionReader(BeanDefinitionRegistry, Environment)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #setEnvironment(Environment)</span></span><br><span class="line"><span class="comment">这里可以看到传入进来的registry是 this,也就是传入了AnnotationConfigApplicationContext它自己. </span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">可以看到这个方法主要做的事情是,初始化Environment,然后new一个ConditionEvaluator对象,其保存了五个信息. 最后就分别添加五个 Processor到beanFactroy的beanDefinitionMap中来.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">AnnotatedBeanDefinitionReader</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">        <span class="comment">// getOrCreateEnvironment()方法,先对registry进行非null的判断,如果是Null的话,就会抛出对应的异常.最后是new StandardEnvironment()了一个对象返回来. registry是满足EnvironmentCapable</span></span><br><span class="line"><span class="comment">// this()方法:先对传入进来的registry和environemnt进行非null的判断.this.registry = registry; 紧着new了一个ConditionEvaluator对象,其构造函数中,初始化了registry,beanFactory,environment,resourceLoader和classLoader这五个参数,是在内部类ConditionContextImpl中. 最后往beanFactory的beanDefinitionMap中添加了五个值,分别是:</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalCommonAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerFactory  </span></span><br><span class="line"><span class="comment">//添加完,new AnnotatedBeanDefinitionReader()这个方法就走完了.        </span></span><br><span class="line">		<span class="built_in">this</span>(registry, getOrCreateEnvironment(registry));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">----------------------------------</span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@code</span> ClassPathBeanDefinitionScanner&#125; for the given bean factory.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> registry the &#123;<span class="doctag">@code</span> BeanFactory&#125; to load bean definitions into, in the form</span></span><br><span class="line"><span class="comment">	 * of a &#123;<span class="doctag">@code</span> BeanDefinitionRegistry&#125;</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>(registry, <span class="literal">true</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// </span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters)</span> &#123;</span><br><span class="line"><span class="comment">//这里也有getOrCreateEnvironment()方法来获取环境,在上一步已经做了,所以这步是直接获取上一步的结果即可.      </span></span><br><span class="line">		<span class="built_in">this</span>(registry, useDefaultFilters, getOrCreateEnvironment(registry));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 先对传入进来的 registry 进行非null的判断,</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters,</span></span><br><span class="line"><span class="params">			Environment environment, <span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line"></span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		<span class="built_in">this</span>.registry = registry;</span><br><span class="line">		<span class="comment">// 这里传入的是ture,也就是会走到这个if里面来.</span></span><br><span class="line">		<span class="keyword">if</span> (useDefaultFilters) &#123;</span><br><span class="line"><span class="comment">//org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider#registerDefaultFilters, 往includeFilters 集合中添加Filter,这些添加的filter,点进去看即可.            </span></span><br><span class="line">			registerDefaultFilters();</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// this.environment赋上传入进来的environment值.        </span></span><br><span class="line">		setEnvironment(environment);</span><br><span class="line"><span class="comment">// org.springframework.core.io.support.ResourcePatternUtils#getResourcePatternResolver进行判断,这里由于是ResourcePatternResolver,所以在第一个if就返回了.</span></span><br><span class="line"><span class="comment">//接着new一个CachingMetadataReaderFactory,传入进去resourceLoader,new这个类的内部也是可以看,就是对参数进行赋值,并没有做其他的什么事情了.      </span></span><br><span class="line"><span class="comment">//this.componentsIndex = CandidateComponentsIndexLoader.loadIndex(this.resourcePatternResolver.getClassLoader());获取出来的值Null.        </span></span><br><span class="line">		setResourceLoader(resourceLoader);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>scan(basePackages) 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对传入进来的参数进行一个校验.</span></span><br><span class="line"><span class="comment">//scanner也是上面那步this.scanner = new ClassPathBeanDefinitionScanner(this)给new出来的.</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">scan</span><span class="params">(String... basePackages)</span> &#123;</span><br><span class="line">   Assert.notEmpty(basePackages, <span class="string">&quot;At least one base package must be specified&quot;</span>);</span><br><span class="line">   <span class="built_in">this</span>.scanner.scan(basePackages);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Perform a scan within the specified base packages.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> basePackages the packages to check for annotated classes</span></span><br><span class="line"><span class="comment">* <span class="doctag">@return</span> number of beans registered</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">scan</span><span class="params">(String... basePackages)</span> &#123;</span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#getBeanDefinitionCount 走到这步来获取个数,还记得new AnnotatedBeanDefinitionReader(this)这个方法里面添加了五个processor吗?所以这里获取出来的beanCountAtScanStart大小就是5(默认对初始化做任何改动的情况下).</span></span><br><span class="line">	<span class="type">int</span> <span class="variable">beanCountAtScanStart</span> <span class="operator">=</span> <span class="built_in">this</span>.registry.getBeanDefinitionCount();</span><br><span class="line"><span class="comment">//从名字上看,这个方法是真正的做扫描的.其实Spring中,scan都算不做事的,doScan才是真的做事的。到后面还有getBean不是做事的,doGetBean才是做事的,createBean也是的. </span></span><br><span class="line"><span class="comment">//doScan做的事情可以看到,读取包下的类,然后根据filter条件来过滤,满足条件的话,就会封装成ScannedGenericBeanDefinition,最后是一个集合包装的该包下全部满足条件的. 然后就是接着对 sbd进行注解的处理,比如有些打入了Lazy等注解的,都要读取出来,存入bd的信息中.最后再检查一遍db,如果没问题的话,就会根据beanName和bd,new一个BeanDefinitionHolder出来,最后注册到beanFactory中去,也就是放入BeanFactory的beanDeifitionMap中去.    </span></span><br><span class="line">	doScan(basePackages);</span><br><span class="line">	<span class="comment">// Register annotation config processors, if necessary.</span></span><br><span class="line">	<span class="keyword">if</span> (<span class="built_in">this</span>.includeAnnotationConfig) &#123;</span><br><span class="line"><span class="comment">// 先获取出beanFactory,先调用beanFactory的getDependencyComparator和getAutowireCandidateResolver方法,如果满足条件的话,就会有对应的set方法.然后紧接着就是判断beanFactory中是否包含一些bd,如果是不包含的话,这里就会添加进去. 这里判断的值,再最初new reader()的时候已经有添加到BeanFactory的beanDifitionMap中去.        </span></span><br><span class="line">		AnnotationConfigUtils.registerAnnotationConfigProcessors(<span class="built_in">this</span>.registry);</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">// 这里返回int参数,但是 this.scanner.scan(basePackages); 好像并没有使用到返回值.</span></span><br><span class="line">	<span class="keyword">return</span> (<span class="built_in">this</span>.registry.getBeanDefinitionCount() - beanCountAtScanStart);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Perform a scan within the specified base packages,</span></span><br><span class="line"><span class="comment">* returning the registered bean definitions.</span></span><br><span class="line"><span class="comment">* &lt;p&gt;This method does &lt;i&gt;not&lt;/i&gt; register an annotation config processor</span></span><br><span class="line"><span class="comment">* but rather leaves this up to the caller.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> basePackages the packages to check for annotated classes</span></span><br><span class="line"><span class="comment">* <span class="doctag">@return</span> set of beans registered if any for tooling registration purposes (never &#123;<span class="doctag">@code</span> null&#125;)</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line"><span class="keyword">protected</span> Set&lt;BeanDefinitionHolder&gt; <span class="title function_">doScan</span><span class="params">(String... basePackages)</span> &#123;</span><br><span class="line">    <span class="comment">// 先是对传入进来的参数进行检验.</span></span><br><span class="line">	Assert.notEmpty(basePackages, <span class="string">&quot;At least one base package must be specified&quot;</span>);</span><br><span class="line">    <span class="comment">// 存BeanDefinitionHolder的集合,也是最后要返回的.</span></span><br><span class="line">	Set&lt;BeanDefinitionHolder&gt; beanDefinitions = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;();</span><br><span class="line">	<span class="keyword">for</span> (String basePackage : basePackages) &#123;</span><br><span class="line"><span class="comment">//根据传入进去的包名字,读取出包名字下的所有文件,然后迭代这些文件,这些文件要有能读的权限,再走isCandidateComponent(metadataReader)这个方法,其中就有使用 excludeFilters和includeFilters,这二个filter来过滤进行一些判断操作. 返回ture,就会往下走,new一个ScannedGenericBeanDefinition,其中beanClass就是这个类的全限定名字.比如这里(com.iyang.spring.bean.YangBeanOne),我们的是这个.</span></span><br><span class="line"><span class="comment">//这就是这个方法,扫描,然后根据特定filter,如果是满足条件的话,就会new一个sbd,然后放入Set集合中,返回. </span></span><br><span class="line">		Set&lt;BeanDefinition&gt; candidates = findCandidateComponents(basePackage);</span><br><span class="line">        </span><br><span class="line">		<span class="keyword">for</span> (BeanDefinition candidate : candidates) &#123;</span><br><span class="line"><span class="comment">//org.springframework.context.annotation.AnnotationScopeMetadataResolver#resolveScopeMetadata,走的这个方法,因AnnotationConfigUtils.attributesFor(...)方法返回的是null,所以这个里面就仅仅只是new了一个ScopeMetadata对象返回了           </span></span><br><span class="line">			<span class="type">ScopeMetadata</span> <span class="variable">scopeMetadata</span> <span class="operator">=</span> <span class="built_in">this</span>.scopeMetadataResolver.resolveScopeMetadata(candidate);</span><br><span class="line"><span class="comment">//scope的值是singletone.这不就是我们熟悉的单例嘛.            </span></span><br><span class="line">			candidate.setScope(scopeMetadata.getScopeName());</span><br><span class="line"><span class="comment">//获取出这个bean的名字            </span></span><br><span class="line">			<span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> <span class="built_in">this</span>.beanNameGenerator.generateBeanName(candidate, <span class="built_in">this</span>.registry);</span><br><span class="line"><span class="comment">// db是 AbstractBeanDefinition的话,这里肯定是,从AbstractBeanDefinition这个名字上看,是一个抽象的，也就是应该是父类.            </span></span><br><span class="line">			<span class="keyword">if</span> (candidate <span class="keyword">instanceof</span> AbstractBeanDefinition) &#123;</span><br><span class="line"><span class="comment">//beanDefinition.applyDefaults(this.beanDefinitionDefaults)该方法是对一些参数进行赋值操作. </span></span><br><span class="line"><span class="comment">//                </span></span><br><span class="line">				postProcessBeanDefinition((AbstractBeanDefinition) candidate, beanName);</span><br><span class="line">			&#125;</span><br><span class="line"><span class="comment">// bd是AnnotatedBeanDefinition或者其子类.            </span></span><br><span class="line">			<span class="keyword">if</span> (candidate <span class="keyword">instanceof</span> AnnotatedBeanDefinition) &#123;</span><br><span class="line"><span class="comment">// 该方是对bd的Lazy.calss,Primary.class,DependsOn.class,Role.class,Description.class这些注解进行获取,如果有的话,就会调用bd对应的set方法给值set进去. 当然我们这里的bean没有这些属性.这里可自行加入一些注入,然后debug到这个地方进行看.</span></span><br><span class="line">                AnnotationConfigUtils.processCommonDefinitionAnnotations((AnnotatedBeanDefinition) candidate);</span><br><span class="line">				&#125;</span><br><span class="line"><span class="comment">// 检查registry中是否含有这个beanName,如果没包含的话,就直接返回ture.            </span></span><br><span class="line">			<span class="keyword">if</span> (checkCandidate(beanName, candidate)) &#123;</span><br><span class="line"><span class="comment">// 传入bean和beanName, new一个bean的Holder出来,也就是bean的持有者的意思.其实个人觉得这里是对bean进行一层封装,Holder更抽象地理解点.                </span></span><br><span class="line">				<span class="type">BeanDefinitionHolder</span> <span class="variable">definitionHolder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(candidate, beanName);</span><br><span class="line"><span class="comment">//scopeMetadata中获取出getScopedProxyMode,如果是No的话,就直接返回definitionHolder         </span></span><br><span class="line">				definitionHolder =</span><br><span class="line">							AnnotationConfigUtils.applyScopedProxyMode(scopeMetadata, definitionHolder, <span class="built_in">this</span>.registry);</span><br><span class="line"><span class="comment">// 添加到最外层的集合中                </span></span><br><span class="line">				beanDefinitions.add(definitionHolder);</span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultListableBeanFactory#registerBeanDefinition,最后走到了这里,该方法会先对传入进来的参数进行非null的判断,如果bd是AbstractBeanDefinition的话,就会强转调用其validate()方法,进行检验. 在从this.beanDefinitionMap中获取,根据beanName,第一次肯定是获取不到的,走到else.else中在判断hasBeanCreationStarted(),这里返回的是flase,也就是走到了else的else中去了,根据beanName和bean存入到this.beanDefinitionMap中,然后beanName添加到beanDefinitionNames集合中.  这就是这步根据beanName和bean放入beanFactory的beanDefinitionMap集合中.               </span></span><br><span class="line">				registerBeanDefinition(definitionHolder, <span class="built_in">this</span>.registry);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> beanDefinitions;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ul>
<li>this() 方法 : 该方法中主要是初始化了 this.reader 和 this.scanner这个参数,当然了,其中还有一些环境等信息 的初始化. this.reader的时候,是有往beanFactroy中添加五个默认要添加的bd,也就是添加到了 BeanFactory的BeanDefitionMap中. this.scanner 也是对一些环境等信息初始化 , 然后下接来的方法就是使用 this.scanner来进扫描 class,然后满足条件的,就封装成bd,注册到beanFactory中去.</li>
<li>register() 方法里面调用 this.reader.register(componentClasses); 该方法就是读取包下的class信息,然后满足条件的就封装成bd,同时还会对注解@Lazy等也会读取,如果是有这些注解的话,就会调用bd对应的set方法,给赋值进去,最后将bd给注册到BeanFactory的beanDefitionMap中去即可.</li>
</ul>
<p>可以看到 this() 方法 和 register()方法,主要是对环境的初始化和根据传入进来的包名来进行扫描获取class信息,满足条件的class信息就会转化为bd,然后注册到beanFactory中去.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>spring初始化(二)</title>
    <url>/2021/11/03/Java/spring%E5%88%9D%E5%A7%8B%E5%8C%96-%E4%BA%8C/</url>
    <content><![CDATA[<h4 id="题记"><a href="#题记" class="headerlink" title="题记"></a>题记</h4><p> 昨天记录了this()和 register() 这二个方法, 这二个方法都是为后面的做铺垫,也就是提前初始化了一些环境和读取class文件. refresh() 这个方法才是最重要的,其中包含的内容是非常多的. 所以这里慢慢进行更新其方法的内容.</p>
<h4 id="refresh-方法"><a href="#refresh-方法" class="headerlink" title="refresh 方法"></a>refresh 方法</h4><p>这里可以看到的是, refresh()该方法里面,基本都是走了很多方法的. 所以挨个看方法,有些方法是留给子类的,也就是进行扩展的. 从synchronized这个关键字来看,这里只容许一次只有一个线程来执行这个方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException &#123;</span><br><span class="line">   <span class="keyword">synchronized</span> (<span class="built_in">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">      <span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">      prepareRefresh();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line"></span><br><span class="line">      <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">      <span class="comment">/**</span></span><br><span class="line"><span class="comment">       * org.springframework.beans.factory.support.DefaultListableBeanFactory</span></span><br><span class="line"><span class="comment">       *</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">      prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">         postProcessBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">         invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">         registerBeanPostProcessors(beanFactory);</span><br><span class="line">         <span class="comment">// Initialize message source for this context.</span></span><br><span class="line">         initMessageSource();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">         initApplicationEventMulticaster();</span><br><span class="line">         <span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">         onRefresh();</span><br><span class="line">         <span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">         registerListeners();</span><br><span class="line">         <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">         <span class="comment">/**</span></span><br><span class="line"><span class="comment">          */</span></span><br><span class="line">         finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">         finishRefresh();</span><br><span class="line">      &#125; <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">            logger.warn(<span class="string">&quot;Exception encountered during context initialization - &quot;</span> +</span><br><span class="line">                  <span class="string">&quot;cancelling refresh attempt: &quot;</span> + ex);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">         destroyBeans();</span><br><span class="line">         <span class="comment">// Reset &#x27;active&#x27; flag.</span></span><br><span class="line">         cancelRefresh(ex);</span><br><span class="line">         <span class="comment">// Propagate exception to caller.</span></span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">         <span class="comment">// Reset common introspection caches in Spring&#x27;s core, since we</span></span><br><span class="line">         <span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">         resetCommonCaches();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-prepareRefresh-方法"><a href="#refresh-prepareRefresh-方法" class="headerlink" title="refresh.prepareRefresh() 方法"></a>refresh.prepareRefresh() 方法</h4><p>prepareRefresh() 方法: 可以看到该方法先是对closed&#x2F;active参数进行设置,然后对Enviornment进行调用检验方法,接着判断this.earlyApplicationListeners是否有值来操作this.applicationListeners. 最后初始化earlyApplicationEvents这个集合. 这里大概还是进行一些初始化操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Prepare this context for refreshing, setting its startup date and</span></span><br><span class="line"><span class="comment"> * active flag as well as performing any initialization of property sources.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Switch to active.</span></span><br><span class="line">   <span class="built_in">this</span>.startupDate = System.currentTimeMillis();</span><br><span class="line">  <span class="comment">// closed设置为false,active设置为true.  </span></span><br><span class="line">   <span class="built_in">this</span>.closed.set(<span class="literal">false</span>);</span><br><span class="line">   <span class="built_in">this</span>.active.set(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 根据log级别来进行输出 </span></span><br><span class="line">   <span class="keyword">if</span> (logger.isInfoEnabled()) &#123;</span><br><span class="line">      logger.info(<span class="string">&quot;Refreshing &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize any placeholder property sources in the context environment.</span></span><br><span class="line">   <span class="comment">// 目前该方法没有调用;目前没有做任何事情. 目测是应该留给子类之类的进行扩展的.</span></span><br><span class="line">   initPropertySources();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Validate that all properties marked as required are resolvable:</span></span><br><span class="line">   <span class="comment">// see ConfigurablePropertyResolver#setRequiredProperties</span></span><br><span class="line"><span class="comment">//先调用getEnvironment()获取this()方法中创建出来的Environment来,然后走validateRequiredProperties方法来进行一些检验,</span></span><br><span class="line"><span class="comment">//org.springframework.core.env.AbstractPropertyResolver#validateRequiredProperties</span></span><br><span class="line"><span class="comment">//最后是走到了这个方法,如果this.requiredProperties中是有值的话,那么这里就会抛出一个异常来    </span></span><br><span class="line">   getEnvironment().validateRequiredProperties();</span><br><span class="line">    </span><br><span class="line">   <span class="comment">// Store pre-refresh ApplicationListeners...</span></span><br><span class="line"><span class="comment">// 这里是对 earlyApplicationListeners 进行判断,如果有值的话,就先会clear掉,然后再addAll</span></span><br><span class="line"><span class="comment">//如果是没有值的话,就会new一个集合,然后赋值给this.earlyApplicationListeners参数   </span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.earlyApplicationListeners == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="built_in">this</span>.earlyApplicationListeners = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="built_in">this</span>.applicationListeners);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Reset local application listeners to pre-refresh state.</span></span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.clear();</span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.addAll(<span class="built_in">this</span>.earlyApplicationListeners);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for the collection of early ApplicationEvents,</span></span><br><span class="line">   <span class="comment">// to be published once the multicaster is available...</span></span><br><span class="line"><span class="comment">// 最后初始化一下 this.earlyApplicationEvents 这个参数</span></span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-obtainFreshBeanFactory-方法"><a href="#refresh-obtainFreshBeanFactory-方法" class="headerlink" title="refresh.obtainFreshBeanFactory()方法"></a>refresh.obtainFreshBeanFactory()方法</h4><p>这个方法是有方法一个BeanFactory回去的.</p>
<p>该方法对beanFactory进行SerializationId,然后获取BeanFactory,最后返回这个BeanFactory.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line"><span class="comment"> *  告诉子类刷新内部Bean工厂</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> the fresh BeanFactory instance</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #refreshBeanFactory()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #getBeanFactory()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> ConfigurableListableBeanFactory <span class="title function_">obtainFreshBeanFactory</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="comment">//this.refreshed.compareAndSet(false, true)该方法如果返回的是false的话,就会有异常给抛出来</span></span><br><span class="line"><span class="comment">//不是false的话,接着就是对beanFactory设置SerializationId    </span></span><br><span class="line">   refreshBeanFactory();</span><br><span class="line"><span class="comment">// org.springframework.context.support.GenericApplicationContext#getBeanFactory</span></span><br><span class="line"><span class="comment">//该方法直接返回DefaultListableBeanFactory    </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line">   <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">      logger.debug(<span class="string">&quot;Bean factory for &quot;</span> + getDisplayName() + <span class="string">&quot;: &quot;</span> + beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">//返回获取的beanFactory.    </span></span><br><span class="line">   <span class="keyword">return</span> beanFactory;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-prepareBeanFactory-方法"><a href="#refresh-prepareBeanFactory-方法" class="headerlink" title="refresh.prepareBeanFactory() 方法"></a>refresh.prepareBeanFactory() 方法</h4><p>从这个方法来看,是对BeanFactory的准备.</p>
<p>该方法可以先是对classLoader,expressionResolver,propertyEditorRegistrar添加到beanFactory中去. 然后添加ApplicationContextAwareProcessor(BeanPostProcessor)到BeanFactory,然后忽略到一些接口的注入到beanFactory中去.</p>
<p>设置 BeanFactory , ResourceLoader , ApplicationEventPublisher, ApplicationContext等bean到BeanFactory中去.</p>
<p>最后就是一些environment,systemProperties,systemEnvironment等注入到BeanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Configure the factory&#x27;s standard context characteristics,</span></span><br><span class="line"><span class="comment"> * such as the context&#x27;s ClassLoader and post-processors.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the BeanFactory to configure</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Tell the internal bean factory to use the context&#x27;s class loader etc.</span></span><br><span class="line"><span class="comment">//给beanFactory设置classLoader(加载bean) </span></span><br><span class="line">   beanFactory.setBeanClassLoader(getClassLoader());</span><br><span class="line"><span class="comment">//这里根据classLoader来获取解析器,然后set到BeanFactory中去.(解析bean定义的表达式)</span></span><br><span class="line">   beanFactory.setBeanExpressionResolver(<span class="keyword">new</span> <span class="title class_">StandardBeanExpressionResolver</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line"><span class="comment">//属性编辑注册器,set到BeanFactory中</span></span><br><span class="line">   beanFactory.addPropertyEditorRegistrar(<span class="keyword">new</span> <span class="title class_">ResourceEditorRegistrar</span>(<span class="built_in">this</span>, getEnvironment()));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Configure the bean factory with context callbacks.</span></span><br><span class="line"><span class="comment">//添加ApplicationContextAwareProcessor到BeanFactory中.该类是有实现BeanPostProcessor的</span></span><br><span class="line"><span class="comment">//BeanPostProcessor是在bean初始化完后,调用BeanPostProcessor进行扩展.</span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationContextAwareProcessor</span>(<span class="built_in">this</span>));</span><br><span class="line"><span class="comment">//忽略掉EnvironmentAware/EmbeddedValueResolverAware....ApplicationContextAware</span></span><br><span class="line"><span class="comment">//这六个接口的注入(依赖). 因为ApplicationContextAwareProcessor中有做了这些事</span></span><br><span class="line">   beanFactory.ignoreDependencyInterface(EnvironmentAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(EmbeddedValueResolverAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ResourceLoaderAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationEventPublisherAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(MessageSourceAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationContextAware.class);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// BeanFactory interface not registered as resolvable type in a plain factory.</span></span><br><span class="line">   <span class="comment">// MessageSource registered (and found for autowiring) as a bean.</span></span><br><span class="line"><span class="comment">// BeanFactory,ResourceLoader,ApplicationEventPublisher,ApplicationContext这四个接口</span></span><br><span class="line"><span class="comment">//对应的bean都set到beanFactory中去.    </span></span><br><span class="line">   beanFactory.registerResolvableDependency(BeanFactory.class, beanFactory);</span><br><span class="line">   beanFactory.registerResolvableDependency(ResourceLoader.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationEventPublisher.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationContext.class, <span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register early post-processor for detecting inner beans as ApplicationListeners.</span></span><br><span class="line"><span class="comment">//添加ApplicationListenerDetector(BeanPostProcessor)到beanFactory中去.</span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found.</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      <span class="comment">// Set a temporary ClassLoader for type matching.</span></span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register default environment beans.</span></span><br><span class="line"><span class="comment">//如果beanFactory中没有ENVIRONMENT_BEAN_NAME这个bean的话,就注入一个进去</span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(ENVIRONMENT_BEAN_NAME, getEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// SYSTEM_PROPERTIES_BEAN_NAME也是一样,注入到beanFactory中去</span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_PROPERTIES_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_PROPERTIES_BEAN_NAME, getEnvironment().getSystemProperties());</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">//SYSTEM_ENVIRONMENT_BEAN_NAME同上    </span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_ENVIRONMENT_BEAN_NAME, getEnvironment().getSystemEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-postProcessBeanFactory-方法"><a href="#refresh-postProcessBeanFactory-方法" class="headerlink" title="refresh.postProcessBeanFactory() 方法"></a>refresh.postProcessBeanFactory() 方法</h4><p>该方法目前在单个 Spring中是没有做任何事情的。 等到看SpringBoot源码的时候,这里就会有代码走进来,是进行根据包来扫描来获取class等信息的. 满足条件的class,就会当为bd给注册到beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Modify the application context&#x27;s internal bean factory after its standard</span></span><br><span class="line"><span class="comment"> * initialization. All bean definitions will have been loaded, but no beans</span></span><br><span class="line"><span class="comment"> * will have been instantiated yet. This allows for registering special</span></span><br><span class="line"><span class="comment"> * BeanPostProcessors etc in certain ApplicationContext implementations.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the bean factory used by the application context</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-invokeBeanFactoryPostProcessors-方法"><a href="#refresh-invokeBeanFactoryPostProcessors-方法" class="headerlink" title="refresh.invokeBeanFactoryPostProcessors() 方法"></a>refresh.invokeBeanFactoryPostProcessors() 方法</h4><p>可以看到这个方法,借助PostProcessorRegistrationDelegate来对PostProcessor进行处理。</p>
<p>先是对BeanDefinitionRegistryPostProcessor进行从beanFactory中获取出相应的名字数组,然后迭代这个数组,然后处理PriorityOrdered—&gt;Ordered—&gt; 没有,这个顺序,最后还有一个while循环迭代来检查BeanDefinitionRegistryPostProcessor是否都处理完了.</p>
<p>再接着就是处理BeanFactoryPostProcessor,处理方式是和BeanDefinitionRegistryPostProcessor一样的,顺序也是一样的.</p>
<p>最后就是调用beanFactory.clearMetadataCache()清除.</p>
<p>当然,这个里面有些上面 PostProcessor等待阅读SpringBoot的时候给补上来,因为到时候SpringBoot这里会有很多PostProcessor,这里目前是没有的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Instantiate and invoke all registered BeanFactoryPostProcessor beans,</span></span><br><span class="line"><span class="comment"> * respecting explicit order if given.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Must be called before singleton instantiation.</span></span><br><span class="line"><span class="comment">BeanFactoryPostProcessor: 用来修改Spring容器中已经存在的bean定义.</span></span><br><span class="line"><span class="comment">BeanDefinitionRegistryPostProcessor: 是BeanFactoryPostProcessor的子类,作用和父类是一样的,不同的是,该使用的是BeanDefinitionRegistry对bean进行处理</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line"><span class="comment">//org.springframework.context.support.AbstractApplicationContext#getBeanFactoryPostProcessors,由于这里只是启动了单个Spring,返回的集合是没有值的.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; postProcessorsList = getBeanFactoryPostProcessors();</span><br><span class="line">   <span class="comment">//System.out.println(&quot;postProcessorsList value ---&gt; &quot; + postProcessorsList);</span></span><br><span class="line">   <span class="comment">// System.out.println(&quot;beanFactory  value 111111 ---&gt; &quot; + beanFactory);</span></span><br><span class="line"><span class="comment">//借助PostProcessorRegistrationDelegate来处理PostProcessors.</span></span><br><span class="line"><span class="comment">//对传入postProcessorsList进行迭代,如果PostProcessor是BeanDefinitionRegistryPostProcessor的话,就会强转然后调用postProcessBeanDefinitionRegistry方法(传入参数是beanFacotry),添加到registryProcessors集合中.如果不是的话,就会添加到regularPostProcessors集合中.</span></span><br><span class="line"><span class="comment">//根据BeanDefinitionRegistryPostProcessor,从beanFactory中获取postProcessorNames,</span></span><br><span class="line"><span class="comment">//进行迭代,如果是有PriorityOrdered接口的子类的话,就会从beanFactory中根据bean名字,类.class来获取BeanDefinitionRegistryPostProcessor,并且添加到currentRegistryProcessors集合中,ppName(名字的值)也会添加到processedBeans该集合中</span></span><br><span class="line"><span class="comment">//对currentRegistryProcessors进行排序,全部添加到registryProcessors集合中,invokeBeanDefinitionRegistryPostProcessors()该方法是调用BeanDefinitionRegistryPostProcessors的,调用完了然后清空currentRegistryProcessors这个集合.</span></span><br><span class="line"><span class="comment">//同样方法获取postProcessorNames,processedBeans集合中不包含并且是Ordered的子类,然后添加到currentRegistryProcessors集合中,ppName也会添加到processedBeans集合中,同样的排序方式,添加到registryProcessors中,再调用invokeBeanDefinitionRegistryPostProcessors()方法,currentRegistryProcessors清空该集合.</span></span><br><span class="line"><span class="comment">// 也就是到这里,可以看出来,处理的顺序,先是处理PriorityOrdered,再处理Ordered.</span></span><br><span class="line"><span class="comment">// 然后使用一个while循环,继续获取BeanDefinitionRegistryPostProcessor对应的postProcessorNames,这个地方是为了防止有些没有调用到的,并且是processedBeans集合中不包含的,然后就会放入到currentRegistryProcessors这个集合中,排序currentRegistryProcessors集合,全部添加到registryProcessors中,调用invokeBeanDefinitionRegistryPostProcessors,也就是调用具体的PostProcessors.</span></span><br><span class="line"><span class="comment">//invokeBeanFactoryPostProcessors(registryProcessors, beanFactory);</span></span><br><span class="line"><span class="comment">//invokeBeanFactoryPostProcessors(regularPostProcessors, beanFactory);</span></span><br><span class="line"><span class="comment">// 之前的二个集合,registryProcessors和regularPostProcessors,在这里还是会继续调用.</span></span><br><span class="line"><span class="comment">//然后根据BeanFactoryPostProcessor.class获取postProcessorNames数组,与上面的也是同样的方法,</span></span><br><span class="line"><span class="comment">//对postProcessorNames进行迭代,如果是processedBeans(上面装的名字)如果包含了,就会跳过.</span></span><br><span class="line"><span class="comment">/** 如果ppName,也就是迭代的值,是有PriorityOrdered的子类的话,就会从走beanFactory.getBean(ppName, BeanFactoryPostProcessor.class)获取出BeanFactoryPostProcessor放入到priorityOrderedPostProcessors集合中.  如果是Ordered的子类,就将名字放入到orderedPostProcessorNames集合中,如果上面三种都不满足的话,就会放入到nonOrderedPostProcessorNames集合中.</span></span><br><span class="line"><span class="comment">然后先排序priorityOrderedPostProcessors,再走invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span></span><br><span class="line"><span class="comment">接着迭代orderedPostProcessorNames集合,然后从beanFactory中获取BeanFactoryPostProcessor,再就做与priorityOrderedPostProcessors一样的操作.</span></span><br><span class="line"><span class="comment">最后在做nonOrderedPostProcessors这个集合的,操作是与orderedPostProcessorNames一摸一样的.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">最后在调用一个beanFactory的clearMetadataCache方法.</span></span><br><span class="line"><span class="comment">可以看到这个方法是先对BeanDefinitionRegistryPostProcessor.class进行处理,然后根据顺序PriorityOrdered--&gt;Ordered---&gt;没有, 这样的顺序执行的.</span></span><br><span class="line"><span class="comment">然后再处理BeanFactoryPostProcessor.class,处理方式是和BeanDefinitionRegistryPostProcessor.class也是一样的,根据顺序来进行处理.</span></span><br><span class="line"><span class="comment">*/</span>    </span><br><span class="line">   PostProcessorRegistrationDelegate.invokeBeanFactoryPostProcessors(beanFactory, postProcessorsList);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found in the meantime</span></span><br><span class="line">   <span class="comment">// (e.g. through an @Bean method registered by ConfigurationClassPostProcessor)</span></span><br><span class="line"><span class="comment">// 获取beanFactory的tempClassLoader加载,并且beanFactory是包含了loadTimeWeaver这个bean的,</span></span><br><span class="line"><span class="comment">//就会走if方法,可以看到是添加LoadTimeWeaverAwareProcessor到beanFactory的postProcessor中,</span></span><br><span class="line"><span class="comment">//然后添加一个ClassLoader到beanFactory中   </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.getTempClassLoader() == <span class="literal">null</span> &amp;&amp; beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-registerBeanPostProcessors-方法"><a href="#refresh-registerBeanPostProcessors-方法" class="headerlink" title="refresh.registerBeanPostProcessors() 方法"></a>refresh.registerBeanPostProcessors() 方法</h4><p>仔细看中这个方法,其实和上一个方法走的逻辑好像是有点类似的. 也是借助PostProcessorRegistrationDelegate来完成其逻辑的.</p>
<p>先是从BeanFactory中获取BeanPostProcessor对用的postProcessorNames数组。</p>
<p>然后分为 PriorityOrdered –&gt; Ordered –&gt; 既不是PriorityOrdered ,也不是Ordered –&gt; MergedBeanDefinitionPostProcessor子类, 这样的先后顺序,走registerBeanPostProcessors,这个是将PostProcessros注册到Spring的beanFactory中(Spring容器).</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Instantiate and register all BeanPostProcessor beans,</span></span><br><span class="line"><span class="comment"> * respecting explicit order if given.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Must be called before any instantiation of application beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   PostProcessorRegistrationDelegate.registerBeanPostProcessors(beanFactory, <span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-------------------</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">			ConfigurableListableBeanFactory beanFactory, AbstractApplicationContext applicationContext)</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 先是根据BeanPostProcessor获取出postProcessorNames数组,这个根据和上面的方法很相似.    </span></span><br><span class="line">		String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor.class,</span><br><span class="line">				<span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Register BeanPostProcessorChecker that logs an info message when</span></span><br><span class="line">		<span class="comment">// a bean is created during BeanPostProcessor instantiation, i.e. when</span></span><br><span class="line">		<span class="comment">// a bean is not eligible for getting processed by all BeanPostProcessors.</span></span><br><span class="line"><span class="comment">//然后从beanFactory中获取出个数 + postProcessorNames数组长度再加上一个1.     </span></span><br><span class="line">		<span class="type">int</span> <span class="variable">beanProcessorTargetCount</span> <span class="operator">=</span> beanFactory.getBeanPostProcessorCount() + <span class="number">1</span> + postProcessorNames.length;</span><br><span class="line"><span class="comment">//添加一个BeanPostProcessorChecker到beanFactory中.从名字上来,这个PostProcessor应该是进行检查的操作.</span></span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">BeanPostProcessorChecker</span>(beanFactory, beanProcessorTargetCount));</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Separate between BeanPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">		<span class="comment">// Ordered, and the rest.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;BeanPostProcessor&gt; internalPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 对 postProcessorNames 进行遍历;同时使用不同类型的集合来存储数据</span></span><br><span class="line"><span class="comment">//主要是根据是否是PriorityOrdered的子类,是的话就会放入到priorityOrderedPostProcessors集合中,接着在判断是否是MergedBeanDefinitionPostProcessor,如果是的话,就会放入到internalPostProcessors集合中</span></span><br><span class="line"><span class="comment">//是不是orderd的子类,是的话,就会放入到orderedPostProcessorNames集合中,</span></span><br><span class="line"><span class="comment">//如果上面二者都不的话,就会放入到nonOrderedPostProcessorNames集合中  </span></span><br><span class="line">		<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">			<span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">				<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">				priorityOrderedPostProcessors.add(pp);</span><br><span class="line">				<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">					internalPostProcessors.add(pp);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125; <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">				orderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">				nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// First, register the BeanPostProcessors that implement PriorityOrdered.</span></span><br><span class="line"><span class="comment">//先处理priorityOrderedPostProcessors这个集合中的数据.先排序,然后调用registerBeanPostPtocessors方法.</span></span><br><span class="line">		sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, priorityOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Next, register the BeanPostProcessors that implement Ordered.</span></span><br><span class="line"><span class="comment">//在处理orderedPostProcessorNames集合中的数据,发现如果也是MergedBeanDefinitionPostProcessor或者其子类的话,也就放入到internalPostProcessors集合中,也就是这里先不处理.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : orderedPostProcessorNames) &#123;</span><br><span class="line">			<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">			orderedPostProcessors.add(pp);</span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">//排序,处理上面不是MergedBeanDefinitionPostProcessor的或其子类,并且是 orderedPostProcessorNames集合中的数据</span></span><br><span class="line">		sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, orderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Now, register all regular BeanPostProcessors.</span></span><br><span class="line"><span class="comment">//最后就处理既不是PriorityOrdered,也不是Ordered的,如果也是MergedBeanDefinitionPostProcessor或者其子类的话,这里也会放入到internalPostProcessors集合中 </span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">			<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">			nonOrderedPostProcessors.add(pp);</span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">//这里先处理nonOrderedPostProcessorNames中的数据并且不是 MergedBeanDefinitionPostProcessor的子类.</span></span><br><span class="line">		registerBeanPostProcessors(beanFactory, nonOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Finally, re-register all internal BeanPostProcessors.</span></span><br><span class="line"><span class="comment">//最后排序下 MergedBeanDefinitionPostProcessor子类的集合,调用registerBeanPostProcessors方法,注册到BeanFactory中去.   </span></span><br><span class="line">		sortPostProcessors(internalPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, internalPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Re-register post-processor for detecting inner beans as ApplicationListeners,</span></span><br><span class="line">		<span class="comment">// moving it to the end of the processor chain (for picking up proxies etc).</span></span><br><span class="line"><span class="comment">//最后添加一个ApplicationListenerDetector到beanFactory中去,并且ApplicationListenerDetector是有实现MergedBeanDefinitionPostProcessor接口的.</span></span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(applicationContext));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refersh-initMessageSource-方法"><a href="#refersh-initMessageSource-方法" class="headerlink" title="refersh.initMessageSource() 方法"></a>refersh.initMessageSource() 方法</h4><p>这个方法主要是对 MESSAGE_SOURCE_BEAN_NAME 是否在beanFactory中进行判断.如果已经在了的话,就会判断是不是HierarchicalMessageSource类型,继续判断其ParentMessageSource是不是null,如果是null的话,就会getInternalParentMessageSource调用初始化获取一些值给赋值进去.</p>
<p>如果beanFactory中没有的话,就会先new一个,然后也会setParentMessageSource值进去,最后注册到beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the MessageSource.</span></span><br><span class="line"><span class="comment"> * Use parent&#x27;s if none defined in this context.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initMessageSource</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="comment">//先获取BeanFactory.  </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line"><span class="comment">// beanFactory中包含MESSAGE_SOURCE_BEAN_NAME</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(MESSAGE_SOURCE_BEAN_NAME)) &#123;</span><br><span class="line"> <span class="comment">//获取出出来的bean赋值给this.messageSource      </span></span><br><span class="line">      <span class="built_in">this</span>.messageSource = beanFactory.getBean(MESSAGE_SOURCE_BEAN_NAME, MessageSource.class);</span><br><span class="line">      <span class="comment">// Make MessageSource aware of parent MessageSource.</span></span><br><span class="line"><span class="comment">//this.parent不是null并且bean是HierarchicalMessageSource</span></span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.parent != <span class="literal">null</span> &amp;&amp; <span class="built_in">this</span>.messageSource <span class="keyword">instanceof</span> HierarchicalMessageSource) &#123;</span><br><span class="line">        <span class="comment">//强转  </span></span><br><span class="line">         <span class="type">HierarchicalMessageSource</span> <span class="variable">hms</span> <span class="operator">=</span> (HierarchicalMessageSource) <span class="built_in">this</span>.messageSource;</span><br><span class="line">  <span class="comment">// hms获取出来的parentMessageSource是null情况下,getInternalParentMessageSource()返回的值赋值给hms的ParentMessageSource属性  </span></span><br><span class="line">         <span class="keyword">if</span> (hms.getParentMessageSource() == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Only set parent context as parent MessageSource if no parent MessageSource</span></span><br><span class="line">            <span class="comment">// registered already.</span></span><br><span class="line">            hms.setParentMessageSource(getInternalParentMessageSource());</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Using MessageSource [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// beanFactory中不包含MESSAGE_SOURCE_BEAN_NAME    </span></span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Use empty MessageSource to be able to accept getMessage calls.</span></span><br><span class="line"><span class="comment">//自己new一个DelegatingMessageSource,dms    </span></span><br><span class="line">      <span class="type">DelegatingMessageSource</span> <span class="variable">dms</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DelegatingMessageSource</span>();</span><br><span class="line"><span class="comment">//调用getInternalParentMessageSource()方法的返回值给set进去.  </span></span><br><span class="line">      dms.setParentMessageSource(getInternalParentMessageSource());</span><br><span class="line">      <span class="built_in">this</span>.messageSource = dms;</span><br><span class="line"><span class="comment">// 注入到 beanFactroy中去       </span></span><br><span class="line">      beanFactory.registerSingleton(MESSAGE_SOURCE_BEAN_NAME, <span class="built_in">this</span>.messageSource);</span><br><span class="line"><span class="comment">// 根据log的级别来打印.       </span></span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Unable to locate MessageSource with name &#x27;&quot;</span> + MESSAGE_SOURCE_BEAN_NAME +</span><br><span class="line">               <span class="string">&quot;&#x27;: using default [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-initApplicationEventMulticaster-方法"><a href="#refresh-initApplicationEventMulticaster-方法" class="headerlink" title="refresh.initApplicationEventMulticaster() 方法"></a>refresh.initApplicationEventMulticaster() 方法</h4><p>该方法可以看到也是对APPLICATION_EVENT_MULTICASTER_BEAN_NAME是否在bean的判断，如果有的话,就会get出来,没有的话,就会new一个出来,然后注册到beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the ApplicationEventMulticaster.</span></span><br><span class="line"><span class="comment"> * Uses SimpleApplicationEventMulticaster if none defined in the context.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> org.springframework.context.event.SimpleApplicationEventMulticaster</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initApplicationEventMulticaster</span><span class="params">()</span> &#123;</span><br><span class="line"> <span class="comment">// 先获取beanFactory   </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line"> <span class="comment">//判断beanFactory是不是有APPLICATION_EVENT_MULTICASTER_BEAN_NAME这个bean,</span></span><br><span class="line"> <span class="comment">//如果是有的话,就会获取出来.然后进行log的级别,判断要不要打印</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster =</span><br><span class="line">            beanFactory.getBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, ApplicationEventMulticaster.class);</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Using ApplicationEventMulticaster [&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">//如果beanFactory是不包含的话,那么久new一个SimpleApplicationEventMulticaster出来,</span></span><br><span class="line"> <span class="comment">//然后注册到beanFactory中去,最后根据log的级别来判断打印</span></span><br><span class="line">  </span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster = <span class="keyword">new</span> <span class="title class_">SimpleApplicationEventMulticaster</span>(beanFactory);</span><br><span class="line">      beanFactory.registerSingleton(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, <span class="built_in">this</span>.applicationEventMulticaster);</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Unable to locate ApplicationEventMulticaster with name &#x27;&quot;</span> +</span><br><span class="line">               APPLICATION_EVENT_MULTICASTER_BEAN_NAME +</span><br><span class="line">               <span class="string">&quot;&#x27;: using default [&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-onRefresh-方法"><a href="#refresh-onRefresh-方法" class="headerlink" title="refresh.onRefresh() 方法"></a>refresh.onRefresh() 方法</h4><p>该方法时留给子类的。 如果是SpringBoot启动的话,这里就会去new Tomcat,然后启动web相应的环境.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Template method which can be overridden to add context-specific refresh work.</span></span><br><span class="line"><span class="comment"> * Called on initialization of special beans, before instantiation of singletons.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;This implementation is empty.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeansException in case of errors</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #refresh()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onRefresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="comment">// For subclasses: do nothing by default.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-registerListeners-方法"><a href="#refresh-registerListeners-方法" class="headerlink" title="refresh.registerListeners() 方法"></a>refresh.registerListeners() 方法</h4><p>该方法是先获取 ApplicationListeners,如果是有值的话,就会添加到AbstractApplicationEventMulticaster的ListenerRetriever的applicationListeners集合中去.</p>
<p>根据ApplicationListener.class获取对应的bean信息,然后迭代,最后会添加到AbstractApplicationEventMulticaster的ListenerRetriever的applicationListenerBeans属性中去</p>
<p>最后是对this.earlyApplicationEvents中的事件进行发布</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Add beans that implement ApplicationListener as listeners.</span></span><br><span class="line"><span class="comment"> * Doesn&#x27;t affect other listeners, which can be added without being beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerListeners</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Register statically specified listeners first.</span></span><br><span class="line"><span class="comment">//getApplicationListeners()获取AbstractApplicationContext中的applicationListeners</span></span><br><span class="line"><span class="comment">//getApplicationEventMulticaster()方法获取的applicationEventMulticaster,是在</span></span><br><span class="line"><span class="comment">//initApplicationEventMulticaster方法中有初始化的.    </span></span><br><span class="line"><span class="comment">//org.springframework.context.event.AbstractApplicationEventMulticaster#addApplicationListener,最后是走到了这里, </span></span><br><span class="line"><span class="comment">//this.defaultRetriever.applicationListeners.add(listener);最后listener是添加到</span></span><br><span class="line"><span class="comment">//其内部内ListenerRetriever的applicationListeners参数中去了.    </span></span><br><span class="line">   <span class="keyword">for</span> (ApplicationListener&lt;?&gt; listener : getApplicationListeners()) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListener(listener);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">   <span class="comment">// uninitialized to let post-processors apply to them!</span></span><br><span class="line"><span class="comment">//根据ApplicationListener获取相应的beanNames数组,这里可以看到和之前获取PostProcessor是一样的</span></span><br><span class="line">   String[] listenerBeanNames = getBeanNamesForType(ApplicationListener.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"><span class="comment">//然后迭代, getApplicationListenerBean是走到了</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.AbstractApplicationEventMulticaster#addApplicationListenerBean,也就是添加到了其内部类ListenerRetriever的applicationListenerBeans属性里面    </span></span><br><span class="line">   <span class="keyword">for</span> (String listenerBeanName : listenerBeanNames) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListenerBean(listenerBeanName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish early application events now that we finally have a multicaster...</span></span><br><span class="line"><span class="comment">//使用this.earlyApplicationEvents的集合的值,赋值给变量earlyEventsToProcess,</span></span><br><span class="line"><span class="comment">//然后给this.earlyApplicationEvents重置为null   </span></span><br><span class="line">   Set&lt;ApplicationEvent&gt; earlyEventsToProcess = <span class="built_in">this</span>.earlyApplicationEvents;</span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="literal">null</span>;</span><br><span class="line"> <span class="comment">//集合不是null并且是有值的话,   </span></span><br><span class="line">   <span class="keyword">if</span> (earlyEventsToProcess != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">for</span> (ApplicationEvent earlyEvent : earlyEventsToProcess) &#123;</span><br><span class="line">  <span class="comment">//org.springframework.context.event.SimpleApplicationEventMulticaster#invokeListener,这里是走到了这里,可以看到是对这个事件进行发布.</span></span><br><span class="line"> <span class="comment">// 然后会根据ApplicationListener去走onApplicationEvent方法         </span></span><br><span class="line">         getApplicationEventMulticaster().multicastEvent(earlyEvent);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-finishBeanFactoryInitialization-方法"><a href="#refresh-finishBeanFactoryInitialization-方法" class="headerlink" title="refresh.finishBeanFactoryInitialization() 方法"></a>refresh.finishBeanFactoryInitialization() 方法</h4><p>该方法从名字上来,就是结束beanFactory的初始化,也就是我们前面准备的bd,postProcessor等信息,在这里都会使用到的.</p>
<p>可以看到该方法就是真正的实例化bean的方法。 大致就是getBean往下走,getBean如果是没有的话,就会走createBean,也就是没有就去创建嘛，就是这个意思。然后其创建的条件,是走各种beanPostProcessors来进行扩展bean.</p>
<p>beanFactory.preInstantiateSingletons() 是需要去阅读很多遍的. 不是一遍或者简单的几遍就ok了的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the initialization of this context&#x27;s bean factory,</span></span><br><span class="line"><span class="comment"> * initializing all remaining singleton beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishBeanFactoryInitialization</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Initialize conversion service for this context.</span></span><br><span class="line"><span class="comment">//如果beanFactory包含CONVERSION_SERVICE_BEAN_NAME,并且该CONVERSION_SERVICE_BEAN_NAME是</span></span><br><span class="line"><span class="comment">//ConversionService的子类的话,久满足条件,然后先从beanFactory中获取出bean,set给beanFactory中的conversionService属性    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(CONVERSION_SERVICE_BEAN_NAME) &amp;&amp;</span><br><span class="line">         beanFactory.isTypeMatch(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class)) &#123;</span><br><span class="line">      beanFactory.setConversionService(</span><br><span class="line">            beanFactory.getBean(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register a default embedded value resolver if no bean post-processor</span></span><br><span class="line">   <span class="comment">// (such as a PropertyPlaceholderConfigurer bean) registered any before:</span></span><br><span class="line">   <span class="comment">// at this point, primarily for resolution in annotation attribute values.</span></span><br><span class="line"><span class="comment">// beanFactory中没有EmbeddedValueResolver,也就是该方法返回的是false,然后就从environment中获取出来一个给add到beanFactory中去.    </span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.hasEmbeddedValueResolver()) &#123;</span><br><span class="line">      beanFactory.addEmbeddedValueResolver(strVal -&gt; getEnvironment().resolvePlaceholders(strVal));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize LoadTimeWeaverAware beans early to allow for registering their transformers early.</span></span><br><span class="line"><span class="comment">//根据LoadTimeWeaverAware获取出对用的names数组</span></span><br><span class="line">   String[] weaverAwareNames = beanFactory.getBeanNamesForType(LoadTimeWeaverAware.class,</span><br><span class="line">         <span class="literal">false</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//然后迭代上面获取出来的数组,挨个调用getBean方法    </span></span><br><span class="line">   <span class="keyword">for</span> (String weaverAwareName : weaverAwareNames) &#123;      </span><br><span class="line">      getBean(weaverAwareName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Stop using the temporary ClassLoader for type matching.</span></span><br><span class="line"><span class="comment">// tempClassLoader,temp的ClassLoader设置为null    </span></span><br><span class="line">   beanFactory.setTempClassLoader(<span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for caching all bean definition metadata, not expecting further changes.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultListableBeanFactory#freezeConfiguration,该方法时走的这里. 其中可以看到是给configurationFrozen设置为true,然后beanName的集合转化为数组,并且赋值给this.frozenBeanDefinitionNames这个数组    </span></span><br><span class="line">   beanFactory.freezeConfiguration();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line"><span class="comment">//这里面初始化bean,简单说一下逻辑. org.springframework.beans.factory.support.AbstractBeanFactory#getBean(java.lang.String)</span></span><br><span class="line"><span class="comment">//getBean() ---&gt; doGetBean() ---&gt;   createBean() ---&gt;  doCreateBean() </span></span><br><span class="line"><span class="comment">//然后再createBean和doCreateBean()方法之中,会根据条件上面的,获取BeanPostProcessors,然后判断走哦不走其各种BeanPostProceesors提供的方法.满足条件就会走,不满足也就自然不会走了.</span></span><br><span class="line"><span class="comment">//当然了这个方法的复杂程度是比较高的，是需要好好理解的。不是这个简简单单的几句话,还需要自己去读.</span></span><br><span class="line"><span class="comment">//起大致打代码走向就是这样,然后其中会走很多调用bean扩展的BeanPostProcessors，还有实现Init...接口后提供的afterS...等方法.    </span></span><br><span class="line">   beanFactory.preInstantiateSingletons();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id=""><a href="#" class="headerlink" title=""></a></h4><h4 id="refresh-finishRefresh-方法"><a href="#refresh-finishRefresh-方法" class="headerlink" title="refresh.finishRefresh() 方法"></a>refresh.finishRefresh() 方法</h4><p>可以看到这个方法是清除了资源缓存, 然后 实现Lifecycle接口的子类,这里就会启动其start方法</p>
<p>发送一个ContextRefreshedEvent事件出去</p>
<p>最后将当前的 AbstractApplicationContext 添加到 LiveBeansView的applicationContexts集合中来</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the refresh of this context, invoking the LifecycleProcessor&#x27;s</span></span><br><span class="line"><span class="comment"> * onRefresh() method and publishing the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> org.springframework.context.event.ContextRefreshedEvent&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Clear context-level resource caches (such as ASM metadata from scanning).</span></span><br><span class="line">  <span class="comment">//清除资源缓存  </span></span><br><span class="line">   clearResourceCaches();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize lifecycle processor for this context.</span></span><br><span class="line"><span class="comment">// 这个方法就会调用实现了 Lifecycle 接口的子类,并且执行其start方法    </span></span><br><span class="line">   initLifecycleProcessor();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Propagate refresh to lifecycle processor first.</span></span><br><span class="line">   getLifecycleProcessor().onRefresh();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish the final event.</span></span><br><span class="line"> <span class="comment">//发送一个刷新上下文的Event出去   </span></span><br><span class="line">   publishEvent(<span class="keyword">new</span> <span class="title class_">ContextRefreshedEvent</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Participate in LiveBeansView MBean, if active.</span></span><br><span class="line"><span class="comment">//org.springframework.context.support.LiveBeansView#applicationContexts</span></span><br><span class="line"><span class="comment">//将AbstractApplicationContext添加到liveBean的applicationContexts集合中    </span></span><br><span class="line">   LiveBeansView.registerApplicationContext(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="refresh-resetCommonCaches"><a href="#refresh-resetCommonCaches" class="headerlink" title="refresh.resetCommonCaches()"></a>refresh.resetCommonCaches()</h4><p>可以看到这个方法才是真正的清除各种集合缓存啥的操作. 是在finally代码快中,也就是说是必须要执行的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Reset Spring&#x27;s common reflection metadata caches, in particular the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> ReflectionUtils&#125;, &#123;<span class="doctag">@link</span> AnnotationUtils&#125;, &#123;<span class="doctag">@link</span> ResolvableType&#125;</span></span><br><span class="line"><span class="comment"> * and &#123;<span class="doctag">@link</span> CachedIntrospectionResults&#125; caches.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 4.2</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ReflectionUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> AnnotationUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ResolvableType#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> CachedIntrospectionResults#clearClassLoader(ClassLoader)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">resetCommonCaches</span><span class="params">()</span> &#123;</span><br><span class="line">   ReflectionUtils.clearCache();</span><br><span class="line">   AnnotationUtils.clearCache();</span><br><span class="line">   ResolvableType.clearCache();</span><br><span class="line">   CachedIntrospectionResults.clearClassLoader(getClassLoader());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java</category>
        <category>spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>spring的refresh方法阅读</title>
    <url>/2021/11/03/Java/spring%E7%9A%84refresh%E6%96%B9%E6%B3%95%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<h4 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h4><p>这里是相对上一次再次的阅读和记录,比上次有了更深入的理解.</p>
<p>这里是再次整理的阅读 Spring 的源码, 相对比上次的阅读，我希望这次可以更清晰&amp;更深刻的理解Spring,也不仅仅会从一个案例来进行分析，会结合多方面的知识来进行整理分析.</p>
<p> 这里放上之前阅读的比例 : <a href="https://github.com/baoyang23/source-notes/tree/master/java/spring_bean">https://github.com/baoyang23/source-notes/tree/master/java/spring_bean</a></p>
<p> 该目录下面有 : bean&#x2F;get&#x2F;extend 三个主要地方的分析.</p>
<p> 此模块还是讲述 整体的 flow,后面会对单个进行分析&amp;Spring提供怎么样的扩展方式来进行增强扩展等.</p>
<p>案例入门操作的话,可以参考之前的博客.</p>
<h4 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h4><p> 这里我们先不忙这其他类型的bean分析, 就对我们作为 config 的 bean 进行分析. 先单个分析容易理解些.</p>
<p> 入口类 :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">InitWorkFlowSpring</span> &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span></span><br><span class="line">                <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>(YangBeanScannerConfig.class);</span><br><span class="line">        <span class="type">YangBeanScannerConfig</span> <span class="variable">yangBeanScannerConfig</span> <span class="operator">=</span> context.getBean(YangBeanScannerConfig.class);</span><br><span class="line">        yangBeanScannerConfig.say();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 配置类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ComponentScan(basePackages = &quot;com.iyang.spring&quot;)</span></span><br><span class="line"><span class="meta">@Description(value = &quot;This is GavinYang DemoWorld.&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangBeanScannerConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">YangBeanScannerConfig</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;配置扫描初始化打印&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">say</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;我是从Spring容器中获取出来的&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，当我们启动 main 方法的时候，是可以看到 YangBeanScannerConfig 中构造函数打印的内容和调用say方法打印出来的内容.</p>
<p>基于这个基础上,我们debug一层一层的走进去看,Spring做了什么事情.</p>
<p>先进入到我们new出来的AnnotationConfigApplicationContext中来</p>
<p>调用自身的无参构造函数</p>
<p>调用 register 注册方法</p>
<p>最后调用一个 refresh, refresh 方法中是做了很多事的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">(Class&lt;?&gt;... componentClasses)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>();</span><br><span class="line">   register(componentClasses);</span><br><span class="line">   refresh();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么有了入口，我们就根据这些方法来一个一个的分析.</p>
<h4 id="this-方法-—-org-springframework-context-annotation-AnnotationConfigApplicationContext-AnnotationConfigApplicationContext"><a href="#this-方法-—-org-springframework-context-annotation-AnnotationConfigApplicationContext-AnnotationConfigApplicationContext" class="headerlink" title="this() 方法 —&gt; org.springframework.context.annotation.AnnotationConfigApplicationContext#AnnotationConfigApplicationContext()"></a>this() 方法 —&gt; org.springframework.context.annotation.AnnotationConfigApplicationContext#AnnotationConfigApplicationContext()</h4><p>先来看 this 方法做了什么事情.</p>
<p>创建了二个对象，分别是 注解bd读取&#x2F;类路口db扫描.</p>
<p>比如有意思的是,传入this(AnnotationConfigApplicationContext), 然后返回来的reader&#x2F;scanner又属于this.也是相互之间各自都持有各自的引用.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>.reader = <span class="keyword">new</span> <span class="title class_">AnnotatedBeanDefinitionReader</span>(<span class="built_in">this</span>);</span><br><span class="line">   <span class="built_in">this</span>.scanner = <span class="keyword">new</span> <span class="title class_">ClassPathBeanDefinitionScanner</span>(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="new-AnnotatedBeanDefinitionReader"><a href="#new-AnnotatedBeanDefinitionReader" class="headerlink" title="new AnnotatedBeanDefinitionReader"></a>new AnnotatedBeanDefinitionReader</h5><p>来，看下new一个对象做了什么事情.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotatedBeanDefinitionReader</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line"><span class="comment">// 这里的 getOrCreateEnvironment 方法中,AnnotationConfigApplicationContext是EnvironmentCapable的子类,</span></span><br><span class="line"><span class="comment">// 所以Environment也是从AnnotationConfigApplicationContext中获取出来的.    </span></span><br><span class="line">   <span class="built_in">this</span>(registry, getOrCreateEnvironment(registry));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">--------------</span><br><span class="line">    </span><br><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotatedBeanDefinitionReader</span><span class="params">(BeanDefinitionRegistry registry, Environment environment)</span> &#123;</span><br><span class="line"> <span class="comment">// 检验 registry/environment都不能为null.   </span></span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		Assert.notNull(environment, <span class="string">&quot;Environment must not be null&quot;</span>);</span><br><span class="line">		<span class="built_in">this</span>.registry = registry;</span><br><span class="line"><span class="comment">// 这里将 registry/environment 给传入构造到 org.springframework.context.annotation.ConditionEvaluator 中来.</span></span><br><span class="line"><span class="comment">// ConditionEvaluator又借助org.springframework.context.annotation.ConditionEvaluator.ConditionContextImpl#ConditionContextImpl 来存储这些信息,所以这里最后的信息是在ConditionContextImpl中来了.    </span></span><br><span class="line">		<span class="built_in">this</span>.conditionEvaluator = <span class="keyword">new</span> <span class="title class_">ConditionEvaluator</span>(registry, environment, <span class="literal">null</span>);</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.AnnotationConfigUtils#registerAnnotationConfigProcessors(org.springframework.beans.factory.support.BeanDefinitionRegistry, java.lang.Object)</span></span><br><span class="line"><span class="comment">// 从该方法的名字上看,是对注册注解配置进行处理.    </span></span><br><span class="line">		AnnotationConfigUtils.registerAnnotationConfigProcessors(<span class="built_in">this</span>.registry);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="org-springframework-context-annotation-AnnotationConfigUtils-registerAnnotationConfigProcessors-org-springframework-beans-factory-support-BeanDefinitionRegistry-java-lang-Object-分析"><a href="#org-springframework-context-annotation-AnnotationConfigUtils-registerAnnotationConfigProcessors-org-springframework-beans-factory-support-BeanDefinitionRegistry-java-lang-Object-分析" class="headerlink" title="org.springframework.context.annotation.AnnotationConfigUtils#registerAnnotationConfigProcessors(org.springframework.beans.factory.support.BeanDefinitionRegistry, java.lang.Object) 分析"></a>org.springframework.context.annotation.AnnotationConfigUtils#registerAnnotationConfigProcessors(org.springframework.beans.factory.support.BeanDefinitionRegistry, java.lang.Object) 分析</h6><p>这里根据我们的案列，传入进来的source是null.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Set&lt;BeanDefinitionHolder&gt; <span class="title function_">registerAnnotationConfigProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">      BeanDefinitionRegistry registry, <span class="meta">@Nullable</span> Object source)</span> &#123;</span><br><span class="line"><span class="comment">// 根据 registry 的类型来获取 DefaultListableBeanFactory.</span></span><br><span class="line"><span class="comment">// 这里的registry属于GenericApplicationContext,调用其getDefaultListableBeanFactory来获取.    </span></span><br><span class="line">   <span class="type">DefaultListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> unwrapDefaultListableBeanFactory(registry);</span><br><span class="line">   <span class="keyword">if</span> (beanFactory != <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// beanFactory.getDependencyComparator() 返回的是null,满足条件.       </span></span><br><span class="line">      <span class="keyword">if</span> (!(beanFactory.getDependencyComparator() <span class="keyword">instanceof</span> AnnotationAwareOrderComparator)) &#123;</span><br><span class="line"><span class="comment">// 设置 AnnotationAwareOrderComparator 到beanFactory中来          </span></span><br><span class="line">         beanFactory.setDependencyComparator(AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// get方法获取出来的是SimpleAutowireCandidateResolver,       </span></span><br><span class="line">      <span class="keyword">if</span> (!(beanFactory.getAutowireCandidateResolver() <span class="keyword">instanceof</span> ContextAnnotationAutowireCandidateResolver)) &#123;</span><br><span class="line"><span class="comment">// 设置ContextAnnotationAutowireCandidateResolver到beanFactory中来.          </span></span><br><span class="line">         beanFactory.setAutowireCandidateResolver(<span class="keyword">new</span> <span class="title class_">ContextAnnotationAutowireCandidateResolver</span>());</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   Set&lt;BeanDefinitionHolder&gt; beanDefs = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以看到每个都有 internal 来特意表明内部的意思.    </span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalConfigurationAnnotationProcessor ---&gt;  ConfigurationClassPostProcessor</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalAutowiredAnnotationProcessor  --&gt; AutowiredAnnotationBeanPostProcessor</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalCommonAnnotationProcessor   ---&gt; CommonAnnotationBeanPostProcessor </span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalPersistenceAnnotationProcessor  ---&gt; PersistenceAnnotationBeanPostProcessor</span></span><br><span class="line"><span class="comment">// org.springframework.context.event.internalEventListenerProcessor   ---&gt; EventListenerMethodProcessor</span></span><br><span class="line"><span class="comment">// org.springframework.context.event.internalEventListenerFactory  --- &gt; DefaultEventListenerFactory</span></span><br><span class="line">   <span class="keyword">if</span> (!registry.containsBeanDefinition(CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(ConfigurationClassPostProcessor.class);</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (!registry.containsBeanDefinition(AUTOWIRED_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(AutowiredAnnotationBeanPostProcessor.class);</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, AUTOWIRED_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Check for JSR-250 support, and if present add the CommonAnnotationBeanPostProcessor.</span></span><br><span class="line">   <span class="keyword">if</span> (jsr250Present &amp;&amp; !registry.containsBeanDefinition(COMMON_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(CommonAnnotationBeanPostProcessor.class);</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, COMMON_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Check for JPA support, and if present add the PersistenceAnnotationBeanPostProcessor.</span></span><br><span class="line">   <span class="keyword">if</span> (jpaPresent &amp;&amp; !registry.containsBeanDefinition(PERSISTENCE_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>();</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         def.setBeanClass(ClassUtils.forName(PERSISTENCE_ANNOTATION_PROCESSOR_CLASS_NAME,</span><br><span class="line">               AnnotationConfigUtils.class.getClassLoader()));</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (ClassNotFoundException ex) &#123;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(</span><br><span class="line">               <span class="string">&quot;Cannot load optional framework class: &quot;</span> + PERSISTENCE_ANNOTATION_PROCESSOR_CLASS_NAME, ex);</span><br><span class="line">      &#125;</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, PERSISTENCE_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (!registry.containsBeanDefinition(EVENT_LISTENER_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(EventListenerMethodProcessor.class);</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, EVENT_LISTENER_PROCESSOR_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (!registry.containsBeanDefinition(EVENT_LISTENER_FACTORY_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(DefaultEventListenerFactory.class);</span><br><span class="line">      def.setSource(source);</span><br><span class="line">      beanDefs.add(registerPostProcessor(registry, def, EVENT_LISTENER_FACTORY_BEAN_NAME));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> beanDefs;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里都是先判断这些内部的bean,是不是已经在 registry 中已经存在了,如果没有存在的话，就会利用类信息来构造出一个RootBeanDefinition来,接着就是调用 registerPostProcessor 方法给注册到 registry 中来.</p>
<p>最后返回一个注册过的 bean 的 Set 集合回去.</p>
<p>总结下这里就是为了给spring容器中注册一些内部的 bean 进去. 这些注册进去的bean,都是在后面初始化bean&amp;解析bean等情况有使用到的.</p>
<h5 id="new-ClassPathBeanDefinitionScanner-方法"><a href="#new-ClassPathBeanDefinitionScanner-方法" class="headerlink" title="new ClassPathBeanDefinitionScanner() 方法"></a>new ClassPathBeanDefinitionScanner() 方法</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>(registry, useDefaultFilters, getOrCreateEnvironment(registry));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-----------------------------------------------------</span><br><span class="line"><span class="comment">// 最后走到 org.springframework.context.annotation.ClassPathBeanDefinitionScanner#ClassPathBeanDefinitionScanner(org.springframework.beans.factory.support.BeanDefinitionRegistry, boolean, org.springframework.core.env.Environment, org.springframework.core.io.ResourceLoader) 构造函数来.    </span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters,</span></span><br><span class="line"><span class="params">			Environment environment, <span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line"></span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line"><span class="comment">// 赋值 registry 来.    </span></span><br><span class="line">		<span class="built_in">this</span>.registry = registry;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (useDefaultFilters) &#123;</span><br><span class="line"><span class="comment">// 添加 filter 到 includeFilters 中来.</span></span><br><span class="line"><span class="comment">// AnnotationTypeFilter(Component.class)</span></span><br><span class="line"><span class="comment">// AnnotationTypeFilter(((Class&lt;? extends Annotation&gt;) ClassUtils.forName(&quot;javax.annotation.ManagedBean&quot;, cl)     </span></span><br><span class="line"><span class="comment">// 等信息进来      </span></span><br><span class="line">			registerDefaultFilters();</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider#setEnvironment</span></span><br><span class="line"><span class="comment">// 设置 enviornment到父类中来.    </span></span><br><span class="line">		setEnvironment(environment);</span><br><span class="line"><span class="comment">// 这里也是这是到父类来了.</span></span><br><span class="line"><span class="comment">// 返回的resourcePatternResolver是AnnotationConfigApplicationContext.</span></span><br><span class="line"><span class="comment">// metadataReaderFactory 是 CachingMetadataReaderFactory 对象来.</span></span><br><span class="line"><span class="comment">// componentsIndex 是 null.    </span></span><br><span class="line">		setResourceLoader(resourceLoader);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该方法可以看到,添加了三个 filter 到 includeFilters 中来.</p>
<p>设置environment &#x2F; resource 到 其父类org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider 中来.</p>
<p>也就是setXXX方法是调用的父类.</p>
<h4 id="register-componentClasses-方法"><a href="#register-componentClasses-方法" class="headerlink" title="register(componentClasses) 方法"></a>register(componentClasses) 方法</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">register</span><span class="params">(Class&lt;?&gt;... componentClasses)</span> &#123;</span><br><span class="line">   <span class="comment">//  检验传入进来的 comonpentClasses是一定要有值的. </span></span><br><span class="line">   Assert.notEmpty(componentClasses, <span class="string">&quot;At least one component class must be specified&quot;</span>);</span><br><span class="line">   <span class="built_in">this</span>.reader.register(componentClasses);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-------------------------</span><br><span class="line"><span class="comment">// 这里从名字上就可以很容易看出是注册 bean 的    </span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">register</span><span class="params">(Class&lt;?&gt;... componentClasses)</span> &#123;</span><br><span class="line">		<span class="keyword">for</span> (Class&lt;?&gt; componentClass : componentClasses) &#123;</span><br><span class="line">			registerBean(componentClass);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-----------------------------</span><br><span class="line"><span class="keyword">private</span> &lt;T&gt; <span class="keyword">void</span> <span class="title function_">doRegisterBean</span><span class="params">(Class&lt;T&gt; beanClass, <span class="meta">@Nullable</span> String name,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> Class&lt;? extends Annotation&gt;[] qualifiers, <span class="meta">@Nullable</span> Supplier&lt;T&gt; supplier,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> BeanDefinitionCustomizer[] customizers)</span> &#123;</span><br><span class="line"><span class="comment">// new 一个 bd 出来.</span></span><br><span class="line">		<span class="type">AnnotatedGenericBeanDefinition</span> <span class="variable">abd</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotatedGenericBeanDefinition</span>(beanClass);</span><br><span class="line"><span class="comment">// 这里没有 @Conditional 注解和 metadata 是 null 就会直接返回 false 来.    </span></span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">this</span>.conditionEvaluator.shouldSkip(abd.getMetadata())) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		abd.setInstanceSupplier(supplier);</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 在对象上获取 @Scope 注解,这里没有,所以就不会往下走.</span></span><br><span class="line"><span class="comment">// 这里返回的 ScopeMetadata应该是默认的,scopeName是singleton,scopedProxyMode是No/1    </span></span><br><span class="line">		<span class="type">ScopeMetadata</span> <span class="variable">scopeMetadata</span> <span class="operator">=</span> <span class="built_in">this</span>.scopeMetadataResolver.resolveScopeMetadata(abd);</span><br><span class="line">		abd.setScope(scopeMetadata.getScopeName());</span><br><span class="line"><span class="comment">// 获取 beanName 来    </span></span><br><span class="line">		<span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> (name != <span class="literal">null</span> ? name : <span class="built_in">this</span>.beanNameGenerator.generateBeanName(abd, <span class="built_in">this</span>.registry));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对一些注解的处理.</span></span><br><span class="line"><span class="comment">// @Lazy , @Primary , @DependsOn , @Role , @Description 如果有这些注解的话,就会进行处理.</span></span><br><span class="line"><span class="comment">// 根据注解的名字,来调用相应的set方法.    </span></span><br><span class="line">		AnnotationConfigUtils.processCommonDefinitionAnnotations(abd);</span><br><span class="line">    </span><br><span class="line"> <span class="comment">// 这里是对是否有 @Primary / @Lazy /   @Qualifier 注解进行判断.</span></span><br><span class="line">		<span class="keyword">if</span> (qualifiers != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">for</span> (Class&lt;? <span class="keyword">extends</span> <span class="title class_">Annotation</span>&gt; qualifier : qualifiers) &#123;</span><br><span class="line">				<span class="keyword">if</span> (Primary.class == qualifier) &#123;</span><br><span class="line">					abd.setPrimary(<span class="literal">true</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (Lazy.class == qualifier) &#123;</span><br><span class="line">					abd.setLazyInit(<span class="literal">true</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					abd.addQualifier(<span class="keyword">new</span> <span class="title class_">AutowireCandidateQualifier</span>(qualifier));</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">//这里 BeanDefinitionCustomizer[] customizers 数组如果有值的话,</span></span><br><span class="line"><span class="comment">// 会调用 customizer 的 customize 方法传入 bd.</span></span><br><span class="line"><span class="comment">// TODO , 这里由于没有具体的值,也不是很清楚做了什么事情.    </span></span><br><span class="line">		<span class="keyword">if</span> (customizers != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">for</span> (BeanDefinitionCustomizer customizer : customizers) &#123;</span><br><span class="line">				customizer.customize(abd);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用 bd 和 bean的名字，创建出一个 bd 的持有者.    </span></span><br><span class="line">		<span class="type">BeanDefinitionHolder</span> <span class="variable">definitionHolder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(abd, beanName);</span><br><span class="line"><span class="comment">// 这里由于传入进来的 scopeMetadata的值是NO,所以就直接返回bdHolder的持有者了.</span></span><br><span class="line"><span class="comment">// 可以看到返回下面的代码,是满足一个增强类的概念的.    </span></span><br><span class="line">		definitionHolder = AnnotationConfigUtils.applyScopedProxyMode(scopeMetadata, definitionHolder, <span class="built_in">this</span>.registry); </span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#registerBeanDefinition</span></span><br><span class="line"><span class="comment">//走到beanFactory中的registerBeanDefinition方法来,先是对bd进行校验,然后利用org.springframework.beans.factory.support.DefaultListableBeanFactory#beanDefinitionMap+beaName来判断是不是已经包含了该bean</span></span><br><span class="line"><span class="comment">// 此时如果你是debug的话,你会发现有五个内置的bean已经在该beanDefinitionMap中了.这也是对应了AnnotatedBeanDefinitionReader中处理的内置的bean.</span></span><br><span class="line"><span class="comment">//如果beanDefinitionMap中没有的话,就分为是不是已经开始创建bean了.</span></span><br><span class="line"><span class="comment">//如果没有已经开始创建了,就添加到beanDefinitionMap中来,beanName也会添加到beanDefinitionNames,其实这里有个问题, beanDefinitionMap的key集合就已经是beanName集合了,为什么还单独使用一个集合来维护呢？</span></span><br><span class="line"><span class="comment">// 这样这个bean的信息和bd就放入到 BeanFactory中来了.    </span></span><br><span class="line"><span class="comment">// 如果有别名的注解或者配置的话,就会走到registry.registerAlias(beanName, alias);来进行别名的注册. </span></span><br><span class="line">		BeanDefinitionReaderUtils.registerBeanDefinition(definitionHolder, <span class="built_in">this</span>.registry);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>这里可以总结下看到 register 方法就是对我们的配置类进行扫描, 然后对是否有一些注解进行判断等. 最后使用 BeanDefinitionReaderUtils 工具类的方法将 bd 给 注册到 Spring 容器中来, 注意这时候是没有实例化我们的 YangBeanScannerConfig,只是封装成 bd + beanName 给注册到 BeanFactory 的 beanDefinitionMap 中来了.</p>
<h4 id="refresh-方法"><a href="#refresh-方法" class="headerlink" title="refresh() 方法"></a>refresh() 方法</h4><p> 更新方法，可以看到这个方法内部是走了很多方法,其逻辑也是比较绕的. 不过没事，我们一个一个方法的来看.</p>
<p>org.springframework.context.support.AbstractApplicationContext#refresh()</p>
<p>可以看到其内部的每个方法上面都是有一行注释的.</p>
<p>于是我们挨个方法来debug进来分析.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 利用 Object 来当锁对象,避免多个线程同时调用到 refresh 方法来.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Object</span> <span class="variable">startupShutdownMonitor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException &#123;</span><br><span class="line">   <span class="keyword">synchronized</span> (<span class="built_in">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">      <span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">  </span><br><span class="line">      prepareRefresh();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line">      <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">      prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">         postProcessBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">         invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">         registerBeanPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize message source for this context.</span></span><br><span class="line">         initMessageSource();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">         initApplicationEventMulticaster();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">         onRefresh();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">         registerListeners();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">         finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">         finishRefresh();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">            logger.warn(<span class="string">&quot;Exception encountered during context initialization - &quot;</span> +</span><br><span class="line">                  <span class="string">&quot;cancelling refresh attempt: &quot;</span> + ex);</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">         destroyBeans();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Reset &#x27;active&#x27; flag.</span></span><br><span class="line">         cancelRefresh(ex);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Propagate exception to caller.</span></span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">finally</span> &#123;</span><br><span class="line">         <span class="comment">// Reset common introspection caches in Spring&#x27;s core, since we</span></span><br><span class="line">         <span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">         resetCommonCaches();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="prepareRefresh-方法"><a href="#prepareRefresh-方法" class="headerlink" title="prepareRefresh() 方法"></a>prepareRefresh() 方法</h5><p>从注释来看, 设置startup数据 &amp; 标识active来表示状态,同时也会初始化一些资源.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Prepare this context for refreshing, setting its startup date and</span></span><br><span class="line"><span class="comment"> * active flag as well as performing any initialization of property sources.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Switch to active.</span></span><br><span class="line">   <span class="built_in">this</span>.startupDate = System.currentTimeMillis();</span><br><span class="line"><span class="comment">// 对状态标识的设置.    </span></span><br><span class="line">   <span class="built_in">this</span>.closed.set(<span class="literal">false</span>);</span><br><span class="line">   <span class="built_in">this</span>.active.set(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;Refreshing &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Refreshing &quot;</span> + getDisplayName());</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize any placeholder property sources in the context environment.</span></span><br><span class="line"><span class="comment">// 这里暂时没有实现来做事情.    </span></span><br><span class="line">   initPropertySources();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Validate that all properties marked as required are resolvable:</span></span><br><span class="line">   <span class="comment">// see ConfigurablePropertyResolver#setRequiredProperties</span></span><br><span class="line"><span class="comment">// org.springframework.core.env.AbstractEnvironment#validateRequiredProperties</span></span><br><span class="line"><span class="comment">// 对 org.springframework.core.env.AbstractPropertyResolver#requiredProperties 进行检验,如果检验到有问题的话,就会抛出异常来.</span></span><br><span class="line"><span class="comment">// 这里是对 properties 进行检验.    </span></span><br><span class="line">   getEnvironment().validateRequiredProperties();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Store pre-refresh ApplicationListeners...</span></span><br><span class="line"><span class="comment">// earlyApplicationListeners是null的话,利用applicationListeners来初始化.   </span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.earlyApplicationListeners == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="built_in">this</span>.earlyApplicationListeners = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="built_in">this</span>.applicationListeners);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 如果已经存在值了,就对  applicationListeners 清空，然后全部添加applicationListeners来.     </span></span><br><span class="line">      <span class="comment">// Reset local application listeners to pre-refresh state.</span></span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.clear();</span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.addAll(<span class="built_in">this</span>.earlyApplicationListeners);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for the collection of early ApplicationEvents,</span></span><br><span class="line">   <span class="comment">// to be published once the multicaster is available...</span></span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到该方法的话,对状态标识进行设置. 接着地 propertySources 资源来进行初始化, 于是就对property来进行检验. 接下来是对 earlyApplicationListeners&#x2F;earlyApplicationEvents根据条件来初始化操作.</p>
<h5 id="obtainFreshBeanFactroy"><a href="#obtainFreshBeanFactroy" class="headerlink" title="obtainFreshBeanFactroy()"></a>obtainFreshBeanFactroy()</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">protected ConfigurableListableBeanFactory obtainFreshBeanFactory() &#123;</span><br><span class="line">   refreshBeanFactory();</span><br><span class="line">   return getBeanFactory();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">---------------------------</span><br><span class="line">org.springframework.context.support.GenericApplicationContext#refreshBeanFactory</span><br><span class="line">  </span><br><span class="line">// 看到 compareAndSet 有点cas 的味道.    </span><br><span class="line">protected final void refreshBeanFactory() throws IllegalStateException &#123;    </span><br><span class="line">		if (!this.refreshed.compareAndSet(false, true)) &#123;</span><br><span class="line">			throw new IllegalStateException(</span><br><span class="line">					&quot;GenericApplicationContext does not support multiple refresh attempts: just call &#x27;refresh&#x27; once&quot;);</span><br><span class="line">		&#125;</span><br><span class="line">// private String id = ObjectUtils.identityToString(this);</span><br><span class="line">// 这里获取出来的id在这个类被new或者子类调用父类的super()构造方法的时候,就已经被初始化值了的.    </span><br><span class="line">		this.beanFactory.setSerializationId(getId());</span><br><span class="line">	&#125;    </span><br><span class="line">    </span><br><span class="line">-----------------</span><br><span class="line">org.springframework.context.support.GenericApplicationContext#getBeanFactory</span><br><span class="line"></span><br><span class="line">// 这里就直接返回了 DefaultListableBeanFactory.   </span><br><span class="line">	@Override</span><br><span class="line">	public final ConfigurableListableBeanFactory getBeanFactory() &#123;</span><br><span class="line">		return this.beanFactory;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>该方法 设置了一个 SerializationId 到 beanFactory 中来. 最后也是返回了一个 DefaultListableBeanFactory 来.</p>
<h5 id="prepareBeanFactory-方法"><a href="#prepareBeanFactory-方法" class="headerlink" title="prepareBeanFactory() 方法"></a>prepareBeanFactory() 方法</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Configure the factory&#x27;s standard context characteristics,</span></span><br><span class="line"><span class="comment"> * such as the context&#x27;s ClassLoader and post-processors.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the BeanFactory to configure</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Tell the internal bean factory to use the context&#x27;s class loader etc.</span></span><br><span class="line"><span class="comment">// org.springframework.core.io.DefaultResourceLoader#getClassLoader   </span></span><br><span class="line"><span class="comment">// 设置 class 加载器&amp;赋值进去.    </span></span><br><span class="line">   beanFactory.setBeanClassLoader(getClassLoader());</span><br><span class="line"><span class="comment">// 将 beanClassLoader放入SpelParserConfiguration中来,SpelExpressionParser中有含有SpelParserConfiguration作为configuration,StandardBeanExpressionResolver属性又含有SpelExpressionParser. 这也就可以理解为beanClassLoader最后是放入到SpelParserConfiguration来.</span></span><br><span class="line">   beanFactory.setBeanExpressionResolver(<span class="keyword">new</span> <span class="title class_">StandardBeanExpressionResolver</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 传入applicationContext和environment到ResourceEditorRegistrar对象来.</span></span><br><span class="line"><span class="comment">//然后添加到beanFactory中来.    </span></span><br><span class="line">   beanFactory.addPropertyEditorRegistrar(<span class="keyword">new</span> <span class="title class_">ResourceEditorRegistrar</span>(<span class="built_in">this</span>, getEnvironment()));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Configure the bean factory with context callbacks.</span></span><br><span class="line"><span class="comment">// 添加ApplicationContextAwareProcessor后置处理器到org.springframework.beans.factory.support.AbstractBeanFactory#beanPostProcessors中来.</span></span><br><span class="line"><span class="comment">// 在添加后置处理器到Spring容器之前,会判断这个后置处理起是不是InstantiationAwareBeanPostProcessor/DestructionAwareBeanPostProcessor 这二种情况.</span></span><br><span class="line"><span class="comment">// 最后添加到 beanPostProcessors 中来.    </span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationContextAwareProcessor</span>(<span class="built_in">this</span>));</span><br><span class="line"><span class="comment">// 然后这里忽略了六种情况的接口. 为什么要忽略呢? 看一个地方.</span></span><br><span class="line"><span class="comment">// org.springframework.context.support.ApplicationContextAwareProcessor#postProcessBeforeInitialization &amp;  org.springframework.context.support.ApplicationContextAwareProcessor#invokeAwareInterfaces 结合这二个方法来看,是已经对这六种情况的接口做了处理的.    </span></span><br><span class="line">   beanFactory.ignoreDependencyInterface(EnvironmentAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(EmbeddedValueResolverAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ResourceLoaderAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationEventPublisherAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(MessageSourceAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationContextAware.class);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// BeanFactory interface not registered as resolvable type in a plain factory.</span></span><br><span class="line">   <span class="comment">// MessageSource registered (and found for autowiring) as a bean.</span></span><br><span class="line"><span class="comment">// private final Map&lt;Class&lt;?&gt;, Object&gt; resolvableDependencies = new ConcurrentHashMap&lt;&gt;(16);    </span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#resolvableDependencies,这里将 BeanFactory.class和beanFactory给添加到 resolvableDependencies中来了,这里可以看到resolvableDependencies的key是一个Class类型.</span></span><br><span class="line">   beanFactory.registerResolvableDependency(BeanFactory.class, beanFactory);</span><br><span class="line">   beanFactory.registerResolvableDependency(ResourceLoader.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationEventPublisher.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationContext.class, <span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register early post-processor for detecting inner beans as ApplicationListeners.</span></span><br><span class="line"><span class="comment">// 这里又添加了一个后置处理器.</span></span><br><span class="line"><span class="comment">// 传入一个 ApplicationContext 给后置处理器,然后添加到BeanFactory中来.</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractBeanFactory#beanPostProcessors,也即是添加到专门存放 后置处理器的集合中来了.    </span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found.</span></span><br><span class="line"><span class="comment">// beanFactory如果有loadTimeWeaver,那么就添加 LoadTimeWeaverAwareProcessor 后置处理器进来   </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      <span class="comment">// Set a temporary ClassLoader for type matching.</span></span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register default environment beans.</span></span><br><span class="line"><span class="comment">// 不包含environment/systemProperties/systemEnvironment，就会添加到org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#singletonObjects中来.    </span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(ENVIRONMENT_BEAN_NAME, getEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_PROPERTIES_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_PROPERTIES_BEAN_NAME, getEnvironment().getSystemProperties());</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_ENVIRONMENT_BEAN_NAME, getEnvironment().getSystemEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里可以看到,prepareBeanFactory 中做了这些事情 : 添加了 beanClassLoader,添加了二个后置处理器,然后注册了四个 BeanFactory&#x2F;ResourceLoader&#x2F;ApplicationEventPublisher&#x2F;ApplicationContext 到DefaultListableBeanFactory#resolvableDependencies中来了.</p>
<p>最后判断beanFactory是不是不包含一些关于环境的bean,如果是的话,那就调用registerSingleton方法给注册进来.</p>
<p>还是可以看到，这里都是在为环境做准备工作.</p>
<h5 id="postProcessBeanFactory-方法"><a href="#postProcessBeanFactory-方法" class="headerlink" title="postProcessBeanFactory() 方法"></a>postProcessBeanFactory() 方法</h5><p>略略略, 该方法暂无实现类来搞事情…..</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Modify the application context&#x27;s internal bean factory after its standard</span></span><br><span class="line"><span class="comment"> * initialization. All bean definitions will have been loaded, but no beans</span></span><br><span class="line"><span class="comment"> * will have been instantiated yet. This allows for registering special</span></span><br><span class="line"><span class="comment"> * BeanPostProcessors etc in certain ApplicationContext implementations.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the bean factory used by the application context</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="invokeBeanFactoryPostProcessors-方法"><a href="#invokeBeanFactoryPostProcessors-方法" class="headerlink" title="invokeBeanFactoryPostProcessors 方法"></a>invokeBeanFactoryPostProcessors 方法</h5><p>这些是对beanFactoryPostProcessors进行处理. 是借用了 PostProcessorRegistrationDelegate.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Instantiate and invoke all registered BeanFactoryPostProcessor beans,</span></span><br><span class="line"><span class="comment"> * respecting explicit order if given.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Must be called before singleton instantiation.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line"><span class="comment">// getBeanFactoryPostProcessors() 获取出来的是空集合.    </span></span><br><span class="line">   PostProcessorRegistrationDelegate.invokeBeanFactoryPostProcessors(beanFactory, getBeanFactoryPostProcessors());</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found in the meantime</span></span><br><span class="line">   <span class="comment">// (e.g. through an @Bean method registered by ConfigurationClassPostProcessor)</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.getTempClassLoader() == <span class="literal">null</span> &amp;&amp; beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="org-springframework-context-support-PostProcessorRegistrationDelegate-invokeBeanFactoryPostProcessors-org-springframework-beans-factory-config-ConfigurableListableBeanFactory-java-util-List"><a href="#org-springframework-context-support-PostProcessorRegistrationDelegate-invokeBeanFactoryPostProcessors-org-springframework-beans-factory-config-ConfigurableListableBeanFactory-java-util-List" class="headerlink" title="org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;)"></a>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;)</h6><p>该方法从代码上来看,还是做了蛮多的事情.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">      ConfigurableListableBeanFactory beanFactory, List&lt;BeanFactoryPostProcessor&gt; beanFactoryPostProcessors)</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Invoke BeanDefinitionRegistryPostProcessors first, if any.</span></span><br><span class="line">   Set&lt;String&gt; processedBeans = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里是满足条件的    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory <span class="keyword">instanceof</span> BeanDefinitionRegistry) &#123;</span><br><span class="line">      <span class="type">BeanDefinitionRegistry</span> <span class="variable">registry</span> <span class="operator">=</span> (BeanDefinitionRegistry) beanFactory;</span><br><span class="line">      List&lt;BeanFactoryPostProcessor&gt; regularPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">      List&lt;BeanDefinitionRegistryPostProcessor&gt; registryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对 beanFactory的后置处理器进行迭代处理操作.       </span></span><br><span class="line">      <span class="keyword">for</span> (BeanFactoryPostProcessor postProcessor : beanFactoryPostProcessors) &#123;</span><br><span class="line">         <span class="keyword">if</span> (postProcessor <span class="keyword">instanceof</span> BeanDefinitionRegistryPostProcessor) &#123;</span><br><span class="line">            <span class="type">BeanDefinitionRegistryPostProcessor</span> <span class="variable">registryProcessor</span> <span class="operator">=</span></span><br><span class="line">                  (BeanDefinitionRegistryPostProcessor) postProcessor;</span><br><span class="line">            registryProcessor.postProcessBeanDefinitionRegistry(registry);</span><br><span class="line">            registryProcessors.add(registryProcessor);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            regularPostProcessors.add(postProcessor);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">      <span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line">      <span class="comment">// Separate between BeanDefinitionRegistryPostProcessors that implement</span></span><br><span class="line">      <span class="comment">// PriorityOrdered, Ordered, and the rest.</span></span><br><span class="line">      List&lt;BeanDefinitionRegistryPostProcessor&gt; currentRegistryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// First, invoke the BeanDefinitionRegistryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line"><span class="comment">// 根据 BeanDefinitionRegistryPostProcessor.class 来获取beanNames数组,</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalConfigurationAnnotationProcessor 这里是获取到了一个内置的BeanFactroyPostProcessor.      </span></span><br><span class="line">      String[] postProcessorNames =</span><br><span class="line">            beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">      <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line"> <span class="comment">// 判断是不是有PriorityOrdered,         </span></span><br><span class="line">         <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line"> <span class="comment">// 这里的getBean就已经对bean进行初始化，是真正的走反射构造函数拿出来的实例对象.</span></span><br><span class="line"> <span class="comment">// getBean需要仔细分析下，因为其内部在 createBean是走了很多后置处理起来进行增强的. </span></span><br><span class="line"><span class="comment">// ConfigurationClassPostProcessor 给添加进来.             </span></span><br><span class="line">            currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line"><span class="comment">// beanName 添加到 processedBeans集合中来了.             </span></span><br><span class="line">            processedBeans.add(ppName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 对集合进行排序,从beanFactory中获取出dependencyComparator来,如果没有的话,就用OrderComparator.INSTANCE默认的</span></span><br><span class="line">      sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line"><span class="comment">// 全部添加到 registryProcessors 中来.       </span></span><br><span class="line">      registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ConfigurationClassPostProcessor#postProcessBeanDefinitionRegistry</span></span><br><span class="line"><span class="comment">//这里是进入到ConfigurationClassPostProcessor中来了,可以看到其接口 BeanDefinitionRegistryPostProcessor,是重写了接口的方法. </span></span><br><span class="line"><span class="comment">// ConfigurationClassPostProcessor#postProcessBeanDefinitionRegistry 做了什么事情呢?</span></span><br><span class="line"><span class="comment">// 用System.identityHashCode(registry);计算出registryId来,如果在org.springframework.context.annotation.ConfigurationClassPostProcessor#registriesPostProcessed/factoriesPostProcessed(二个集合)中已经包含了的话,就会抛出已经被调用过的异常来.如果没有的话,就会添加到registriesPostProcessed中来</span></span><br><span class="line"><span class="comment">// 继续看 org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions 方法,</span></span><br><span class="line"><span class="comment">//先从registry中获取beanNames来,这其中就有Spring内置的和我们自己定义的yangBeanScannerConfig</span></span><br><span class="line"><span class="comment">//对beanNames迭代处理,接着就用ConfigurationClassUtils.checkConfigurationClassCandidate(beanDef, this.metadataReaderFactory)来判断要不要添加到List&lt;BeanDefinitionHolder&gt; configCandidates集合中来,最后是我们定义的beanName给添加进来了.</span></span><br><span class="line"><span class="comment">// 对configCandidates集合进行排序,</span></span><br><span class="line"><span class="comment">// 创建一个ConfigurationClassParser对象来解析每个@Configuration注解类.调用其parse和validate方法,解析完后就是一个ConfigurationClass的Set集合,接着就是new了一个ConfigurationClassBeanDefinitionReader对象来,</span></span><br><span class="line"><span class="comment">// this.reader.loadBeanDefinitions(configClasses); 这行代码有点根据Config去解析bean的意思.    </span></span><br><span class="line"><span class="comment">// 具体要等到后面深度解析再反过来定位每行代码的意思.</span></span><br><span class="line"><span class="comment">// 最后再清除下缓存.       </span></span><br><span class="line">      invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line"><span class="comment">// 清空 currentRegistryProcessors 集合      </span></span><br><span class="line">      currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Next, invoke the BeanDefinitionRegistryPostProcessors that implement Ordered.</span></span><br><span class="line">      postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">      <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line"><span class="comment">// 如果processedBeans集合中不包含并且type是Ordered.class才满足进来的条件.          </span></span><br><span class="line">         <span class="keyword">if</span> (!processedBeans.contains(ppName) &amp;&amp; beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">            currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">            processedBeans.add(ppName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 所以这里的currentRegistryProcessors集合是空集合.       </span></span><br><span class="line">      sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">      registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">      invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">      currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Finally, invoke all other BeanDefinitionRegistryPostProcessors until no further ones appear.</span></span><br><span class="line"><span class="comment">// 这里用 while 循环来最后解析,判断从getBeanNamesForType获取出来的bean是不是被解析过了的. </span></span><br><span class="line"><span class="comment">// 也是用 processedBeans 集合来进行控制的. </span></span><br><span class="line">      <span class="type">boolean</span> <span class="variable">reiterate</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">      <span class="keyword">while</span> (reiterate) &#123;</span><br><span class="line">         reiterate = <span class="literal">false</span>;</span><br><span class="line">         postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">         <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!processedBeans.contains(ppName)) &#123;</span><br><span class="line">               currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">               processedBeans.add(ppName);</span><br><span class="line">               reiterate = <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">         registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">         invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">         currentRegistryProcessors.clear();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Now, invoke the postProcessBeanFactory callback of all processors handled so far.</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ConfigurationClassPostProcessor#postProcessBeanFactory,这里是走到了postProcessBeanFactory回调方法来了.用org.springframework.context.annotation.ConfigurationClassPostProcessor#factoriesPostProcessed集合来控制是否解析过了.用registriesPostProcessed集合来判断上次是否进入到postProcessBeanDefinitionRegistry方法中来. 如果没有的话,就会再走一边processConfigBeanDefinitions,可以看到 postProcessBeanDefinitionRegistry 方法最后也是走到了processConfigBeanDefinitions中来了.</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.ConfigurationClassPostProcessor#enhanceConfigurationClasses 该方法判断是不是需要代理来增强,这里是没有的,所以就直接return掉了.</span></span><br><span class="line"><span class="comment">// 最后添加一个 ImportAwareBeanPostProcessor 后置处理器进来.       </span></span><br><span class="line">      invokeBeanFactoryPostProcessors(registryProcessors, beanFactory);</span><br><span class="line"><span class="comment">// 这里的regularPostProcessors 集合是empty.       </span></span><br><span class="line">      invokeBeanFactoryPostProcessors(regularPostProcessors, beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Invoke factory processors registered with the context instance.</span></span><br><span class="line">      invokeBeanFactoryPostProcessors(beanFactoryPostProcessors, beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">   <span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line"> <span class="comment">// org.springframework.context.annotation.internalConfigurationAnnotationProcessor和org.springframework.context.event.internalEventListenerProcessor这里获取出来的是二个.   </span></span><br><span class="line">   String[] postProcessorNames =</span><br><span class="line">         beanFactory.getBeanNamesForType(BeanFactoryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Separate between BeanFactoryPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">   <span class="comment">// Ordered, and the rest.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line"> <span class="comment">// 这里是对上面已经处理过了的进行过滤处理.      </span></span><br><span class="line">      <span class="keyword">if</span> (processedBeans.contains(ppName)) &#123;</span><br><span class="line">         <span class="comment">// skip - already processed in first phase above</span></span><br><span class="line">      &#125;</span><br><span class="line">       </span><br><span class="line"><span class="comment">// 这里分为 PriorityOrdered&amp;Ordered&amp;非前二者,分这三种情况分别放入到三个不同的集合中.</span></span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">         priorityOrderedPostProcessors.add(beanFactory.getBean(ppName, BeanFactoryPostProcessor.class));</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">         orderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里是可以看到先是对PriorityOrdered进行处理,再对Ordered处理,最后对非前二者进行处理.    </span></span><br><span class="line">   <span class="comment">// First, invoke the BeanFactoryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">   sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">   invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Next, invoke the BeanFactoryPostProcessors that implement Ordered.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String postProcessorName : orderedPostProcessorNames) &#123;</span><br><span class="line"> <span class="comment">// 注意这里是调用了getBean方法.      </span></span><br><span class="line">      orderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// 排序    </span></span><br><span class="line">   sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">   invokeBeanFactoryPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Finally, invoke all other BeanFactoryPostProcessors.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String postProcessorName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line"> <span class="comment">// 注意这里也是调用了 getBean 方法的.      </span></span><br><span class="line">      nonOrderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// org.springframework.context.event.EventListenerMethodProcessor#postProcessBeanFactory</span></span><br><span class="line"><span class="comment">// 这里由于只有一个EventListenerMethodProcessor处理器,所以对应起来的走到其postProcessBeanFactory方法中来.</span></span><br><span class="line"><span class="comment">// 这里也是调用 postProcessBeanFactory 方法的意思,也就是回调方法.    </span></span><br><span class="line">   invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Clear cached merged bean definitions since the post-processors might have</span></span><br><span class="line">   <span class="comment">// modified the original metadata, e.g. replacing placeholders in values...</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#clearMetadataCache</span></span><br><span class="line"><span class="comment">// 对 一些集合等进行清除.    </span></span><br><span class="line">   beanFactory.clearMetadataCache();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至此就可以看到,该方法主要是对BeanDefinitionRegistryPostProcessor.class和BeanFactoryPostProcessor.class来进行处理.</p>
<p>BeanDefinitionRegistryPostProcessor 又是先处理PriorityOrdered,然后会将处理过的放入processedBeans集合中做一个总的记录，再处理非再processedBeans集合记录中的和是Ordered的,最后用while循环来再确认一遍是不是有还没处理的,这个时候控制条件也是通过 processedBeans来控制是不是处理过了的. 这里注意, 实例化是通过调用 getBean方法来实现的,所以你会发现再调用invokeBeanDefinitionRegistryPostProcessors方法之前,都是会有调用getBean方法的.</p>
<p>BeanFactoryPostProcessor 的处理,这里是一次获取出,然后分为 PriorityOrdered&#x2F;Ordered&#x2F;非前二者，分别放入三个集合中进行处理,前提是都没再 processedBeans 集合中. 这里可以看到,如果是PriorityOrdered类型的话，那么在分类的时候就已经调用getBean方法来实例化这个对象了，其他二者都是最后迭代遍历的时候调用getBean方法的. 最后都是sortPostProcessors走下排序，然后调用invokeBeanFactoryPostProcessors方法，这个方法的意思，也就是调用 重写的 postProcessBeanFactory 的方法.</p>
<h5 id="registerBeanPostProcessors-方法"><a href="#registerBeanPostProcessors-方法" class="headerlink" title="registerBeanPostProcessors 方法"></a>registerBeanPostProcessors 方法</h5><p>该方法传入 beanFactory进来,然后直接借助 PostProcessorRegistrationDelegate 来实现.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   PostProcessorRegistrationDelegate.registerBeanPostProcessors(beanFactory, <span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="registerBeanPostProcessors-方法-1"><a href="#registerBeanPostProcessors-方法-1" class="headerlink" title="registerBeanPostProcessors 方法"></a>registerBeanPostProcessors 方法</h6><p>从名字上不难理解，注册 Bean的后置处理器进来.</p>
<p>这里传入进来的 beanFactory 是 DefaultListableBeanFactory , applicationContext是AnnotationConfigApplicationContext</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">      ConfigurableListableBeanFactory beanFactory, AbstractApplicationContext applicationContext)</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取出 BeanPostProcessor 的名字.</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.internalCommonAnnotationProcessor</span></span><br><span class="line"><span class="comment">// 这里获取出来的是二个内部的后置处理器,因为我这里并没有扩展,只是简单的进行说明了下,后面会详细分析。</span></span><br><span class="line"><span class="comment">// 就是这行代码获取的是什么.    </span></span><br><span class="line">   String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register BeanPostProcessorChecker that logs an info message when</span></span><br><span class="line">   <span class="comment">// a bean is created during BeanPostProcessor instantiation, i.e. when</span></span><br><span class="line">   <span class="comment">// a bean is not eligible for getting processed by all BeanPostProcessors.</span></span><br><span class="line"> <span class="comment">// 6   </span></span><br><span class="line">   <span class="type">int</span> <span class="variable">beanProcessorTargetCount</span> <span class="operator">=</span> beanFactory.getBeanPostProcessorCount() + <span class="number">1</span> + postProcessorNames.length;</span><br><span class="line"><span class="comment">// 传入beanFactory和个数,创建出一个检查bean的后置处理器来.</span></span><br><span class="line"><span class="comment">// org.springframework.context.support.PostProcessorRegistrationDelegate.BeanPostProcessorChecker</span></span><br><span class="line"><span class="comment">// 有兴趣的同学可以看到该后置处理器重写的方法做了什么事情.</span></span><br><span class="line"><span class="comment">// 最后添加到 beanFactory 中来.    </span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">BeanPostProcessorChecker</span>(beanFactory, beanProcessorTargetCount));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Separate between BeanPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">   <span class="comment">// Ordered, and the rest.</span></span><br><span class="line">   List&lt;BeanPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;BeanPostProcessor&gt; internalPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    </span><br><span class="line"> <span class="comment">// 对后置处理器进行迭代   </span></span><br><span class="line">   <span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">      <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">   <span class="comment">// 注意这里调用 getBean 方法是已经实例化这个后置处理起了.</span></span><br><span class="line"><span class="comment">// AutowiredAnnotationBeanPostProcessor</span></span><br><span class="line"><span class="comment">// CommonAnnotationBeanPostProcessor</span></span><br><span class="line"> <span class="comment">// 这里实例化的是Spring内置的二个         </span></span><br><span class="line">         <span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">         priorityOrderedPostProcessors.add(pp);</span><br><span class="line">      <span class="comment">// 内部的二个后置处理器都是有实现   MergedBeanDefinitionPostProcessor 的. </span></span><br><span class="line">         <span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">            internalPostProcessors.add(pp);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">         orderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// First, register the BeanPostProcessors that implement PriorityOrdered.</span></span><br><span class="line"><span class="comment">// 排序    </span></span><br><span class="line">   sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"><span class="comment">// 添加到 org.springframework.beans.factory.support.AbstractBeanFactory#beanPostProcessors,也就是添加到Spring的BanFactory中来.    </span></span><br><span class="line">   registerBeanPostProcessors(beanFactory, priorityOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Next, register the BeanPostProcessors that implement Ordered.</span></span><br><span class="line"><span class="comment">// 这里是对实现了 Ordered 类型的处理，很显然我这里是没有的.    </span></span><br><span class="line">   List&lt;BeanPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String ppName : orderedPostProcessorNames) &#123;</span><br><span class="line">      <span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">      orderedPostProcessors.add(pp);</span><br><span class="line">      <span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">         internalPostProcessors.add(pp);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">   registerBeanPostProcessors(beanFactory, orderedPostProcessors);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Now, register all regular BeanPostProcessors.</span></span><br><span class="line"><span class="comment">// 最后是对非 PriorityOrdered和Ordered的处理，    </span></span><br><span class="line">   List&lt;BeanPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line">   <span class="keyword">for</span> (String ppName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">      <span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">      nonOrderedPostProcessors.add(pp);</span><br><span class="line">      <span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">         internalPostProcessors.add(pp);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   registerBeanPostProcessors(beanFactory, nonOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Finally, re-register all internal BeanPostProcessors.</span></span><br><span class="line"><span class="comment">// 这里可以看到,最后对内部的后置处理器又重新注册了一遍.    </span></span><br><span class="line">   sortPostProcessors(internalPostProcessors, beanFactory);</span><br><span class="line">   registerBeanPostProcessors(beanFactory, internalPostProcessors);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Re-register post-processor for detecting inner beans as ApplicationListeners,</span></span><br><span class="line">   <span class="comment">// moving it to the end of the processor chain (for picking up proxies etc).</span></span><br><span class="line"><span class="comment">// ApplicationListenerDetector 这里也是对  ApplicationListenerDetector 也是重新注册一遍.   </span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(applicationContext));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该方法 借助 org.springframework.context.support.PostProcessorRegistrationDelegate#registerBeanPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, org.springframework.context.support.AbstractApplicationContext) 来，获取BeanPostProcessor的后置处理器,也是分为 PriorityOrdered &#x2F; Ordered&#x2F; 前二者都没有，在 PriorityOrdered 分类的时候，就已经调用了 getBean方法来获取出 bean 对象来(这里依然是分为了三个集合来装数据&amp;处理). 然后调用getBean方法后,就调用registerBeanPostProcessors方法，将后置处理器给注册到 Spring 的BeanFactory 中来.</p>
<p>最后还会最内部的 BeanPost后置处理器 &amp; ApplicationListenerDetector 再重新注册一遍.</p>
<p>可能会比较好奇这个后置处理器是干什么用的 ？ 在后面实例化 bean 的时候，就可以看到是有走很多后置处理器的.</p>
<p>所以该方法是对 beanPost的后置处理器进行实例化并且注册到 Spring 的 BeanFactory 中来的.</p>
<h5 id="initMessageSource-方法"><a href="#initMessageSource-方法" class="headerlink" title="initMessageSource () 方法"></a>initMessageSource () 方法</h5><p>初始化 messageSource .</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the MessageSource.</span></span><br><span class="line"><span class="comment"> * Use parent&#x27;s if none defined in this context.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initMessageSource</span><span class="params">()</span> &#123;</span><br><span class="line"> <span class="comment">// 获取出 beanFactory   </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line"><span class="comment">// 如果 beanFactory 包含了名字是messageSource的本地bean.    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(MESSAGE_SOURCE_BEAN_NAME)) &#123;</span><br><span class="line">  <span class="comment">// 从 beanFactory 中获取出来.     </span></span><br><span class="line">      <span class="built_in">this</span>.messageSource = beanFactory.getBean(MESSAGE_SOURCE_BEAN_NAME, MessageSource.class);</span><br><span class="line">      <span class="comment">// Make MessageSource aware of parent MessageSource.</span></span><br><span class="line"><span class="comment">// this.parent不是null并且   messageSource是   HierarchicalMessageSource类型 </span></span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.parent != <span class="literal">null</span> &amp;&amp; <span class="built_in">this</span>.messageSource <span class="keyword">instanceof</span> HierarchicalMessageSource) &#123;</span><br><span class="line">  <span class="comment">// 强转,判断  getParentMessageSource 是不是null,如果是null的话,就调用 getInternalParentMessageSource() 将获取出来的值给set进去.     </span></span><br><span class="line">         <span class="type">HierarchicalMessageSource</span> <span class="variable">hms</span> <span class="operator">=</span> (HierarchicalMessageSource) <span class="built_in">this</span>.messageSource;</span><br><span class="line">         <span class="keyword">if</span> (hms.getParentMessageSource() == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Only set parent context as parent MessageSource if no parent MessageSource</span></span><br><span class="line">            <span class="comment">// registered already.</span></span><br><span class="line">            hms.setParentMessageSource(getInternalParentMessageSource());</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;Using MessageSource [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 这里是不包含的情况.       </span></span><br><span class="line">      <span class="comment">// Use empty MessageSource to be able to accept getMessage calls.</span></span><br><span class="line">      <span class="type">DelegatingMessageSource</span> <span class="variable">dms</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DelegatingMessageSource</span>();</span><br><span class="line"><span class="comment">// getInternalParentMessageSource() 返回的是null       </span></span><br><span class="line">      dms.setParentMessageSource(getInternalParentMessageSource());</span><br><span class="line">      <span class="built_in">this</span>.messageSource = dms;</span><br><span class="line"><span class="comment">// 注册到 beanFactory 中来       </span></span><br><span class="line">      beanFactory.registerSingleton(MESSAGE_SOURCE_BEAN_NAME, <span class="built_in">this</span>.messageSource);</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;No &#x27;&quot;</span> + MESSAGE_SOURCE_BEAN_NAME + <span class="string">&quot;&#x27; bean, using [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该方法可以看到是对 messageSource 的初始化进行操作.</p>
<h5 id="initApplicationEventMulticaster-方法"><a href="#initApplicationEventMulticaster-方法" class="headerlink" title="initApplicationEventMulticaster 方法"></a>initApplicationEventMulticaster 方法</h5><p>这里如果了解过 Spring 的Event 机制的话,是可以比较清晰的感觉到,是对 ApplicationEventMulticaster 的初始化.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the ApplicationEventMulticaster.</span></span><br><span class="line"><span class="comment"> * Uses SimpleApplicationEventMulticaster if none defined in the context.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> org.springframework.context.event.SimpleApplicationEventMulticaster</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initApplicationEventMulticaster</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="comment">// 获取出 beanFactory 来.  </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line"><span class="comment">// 判断 beanFactory 是否包含  applicationEventMulticaster    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME)) &#123;</span><br><span class="line"><span class="comment">// 如果包含的话，就直接从beanFactroy中获取出来,并且赋值给  applicationEventMulticaster  </span></span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster =</span><br><span class="line">            beanFactory.getBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, ApplicationEventMulticaster.class);</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;Using ApplicationEventMulticaster [&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">// 如果不包含的话,传入beanFactory接着就是new一个SimpleApplicationEventMulticaster出来      </span></span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster = <span class="keyword">new</span> <span class="title class_">SimpleApplicationEventMulticaster</span>(beanFactory);</span><br><span class="line"><span class="comment">// 然后注册到 beanFactory 中来.      </span></span><br><span class="line">      beanFactory.registerSingleton(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, <span class="built_in">this</span>.applicationEventMulticaster);</span><br><span class="line">      <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(<span class="string">&quot;No &#x27;&quot;</span> + APPLICATION_EVENT_MULTICASTER_BEAN_NAME + <span class="string">&quot;&#x27; bean, using &quot;</span> +</span><br><span class="line">               <span class="string">&quot;[&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster.getClass().getSimpleName() + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里可以看到, 主要是对 applicationEventMulticaster 的初始化.</p>
<p>如果beanFactory有的话，就从其中拿，如果没有就自己new一个,最后注册到beanFactory中来.</p>
<h5 id="onRefresh-方法"><a href="#onRefresh-方法" class="headerlink" title="onRefresh() 方法"></a>onRefresh() 方法</h5><p>这里是没有做任何事情的，如果是SpringBoot的源码的，这里就是启动tomcat的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Template method which can be overridden to add context-specific refresh work.</span></span><br><span class="line"><span class="comment"> * Called on initialization of special beans, before instantiation of singletons.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;This implementation is empty.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeansException in case of errors</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #refresh()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onRefresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="comment">// For subclasses: do nothing by default.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="registerListeners-方法"><a href="#registerListeners-方法" class="headerlink" title="registerListeners() 方法"></a>registerListeners() 方法</h5><p>从名字来看,这里是注册监听器的意思.</p>
<p>org.springframework.context.event.AbstractApplicationEventMulticaster.ListenerRetriever#applicationListeners 这里是存放监听器的地方。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Add beans that implement ApplicationListener as listeners.</span></span><br><span class="line"><span class="comment"> * Doesn&#x27;t affect other listeners, which can be added without being beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerListeners</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Register statically specified listeners first.</span></span><br><span class="line"><span class="comment">// getApplicationListeners() 获取出来的是空集合.    </span></span><br><span class="line">   <span class="keyword">for</span> (ApplicationListener&lt;?&gt; listener : getApplicationListeners()) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListener(listener);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">   <span class="comment">// uninitialized to let post-processors apply to them!</span></span><br><span class="line"><span class="comment">// 根据ApplicationListener来获取出监听器，这也也是没有的.     </span></span><br><span class="line">   String[] listenerBeanNames = getBeanNamesForType(ApplicationListener.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">   <span class="keyword">for</span> (String listenerBeanName : listenerBeanNames) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListenerBean(listenerBeanName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish early application events now that we finally have a multicaster...</span></span><br><span class="line"><span class="comment">// 这里也是获取早初始的 ApplicationEvent.    </span></span><br><span class="line">   Set&lt;ApplicationEvent&gt; earlyEventsToProcess = <span class="built_in">this</span>.earlyApplicationEvents;</span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="literal">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (earlyEventsToProcess != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">for</span> (ApplicationEvent earlyEvent : earlyEventsToProcess) &#123;</span><br><span class="line">         getApplicationEventMulticaster().multicastEvent(earlyEvent);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>org.springframework.context.event.SimpleApplicationEventMulticaster#multicastEvent(org.springframework.context.ApplicationEvent, org.springframework.core.ResolvableType) 可以看下这个方法或者后续我们再详细的看，Spring是如何发送event的，以及那些监听器是怎么获取到 event 的.</p>
<p>TODO : 这里后面是有待详细的讲解的.</p>
<h5 id="finishBeanFactoryInitialization-方法"><a href="#finishBeanFactoryInitialization-方法" class="headerlink" title="finishBeanFactoryInitialization() 方法"></a>finishBeanFactoryInitialization() 方法</h5><p>从名字理解上,这里是对 beanFactory的初始化结束.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the initialization of this context&#x27;s bean factory,</span></span><br><span class="line"><span class="comment"> * initializing all remaining singleton beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishBeanFactoryInitialization</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Initialize conversion service for this context.</span></span><br><span class="line"> <span class="comment">// 如果beanFactroy包含conversionService并且type是ConversionService.class的话，</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(CONVERSION_SERVICE_BEAN_NAME) &amp;&amp;</span><br><span class="line">         beanFactory.isTypeMatch(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class)) &#123;</span><br><span class="line"><span class="comment">// 就会从beanFactory中获取出对象设置到beanFactory的ConversionService来.       </span></span><br><span class="line">      beanFactory.setConversionService(</span><br><span class="line">            beanFactory.getBean(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register a default embedded value resolver if no bean post-processor</span></span><br><span class="line">   <span class="comment">// (such as a PropertyPlaceholderConfigurer bean) registered any before:</span></span><br><span class="line">   <span class="comment">// at this point, primarily for resolution in annotation attribute values.</span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.hasEmbeddedValueResolver()) &#123;</span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.AbstractBeanFactory#addEmbeddedValueResolver //添加到org.springframework.beans.factory.support.AbstractBeanFactory#embeddedValueResolvers中来.      </span></span><br><span class="line">      beanFactory.addEmbeddedValueResolver(strVal -&gt; getEnvironment().resolvePlaceholders(strVal));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize LoadTimeWeaverAware beans early to allow for registering their transformers early.</span></span><br><span class="line"><span class="comment">// 根据  LoadTimeWeaverAware.class 来获取信息.   </span></span><br><span class="line"><span class="comment">// 很明显这里我们是没有配置的,所以也就是没有的.    </span></span><br><span class="line">   String[] weaverAwareNames = beanFactory.getBeanNamesForType(LoadTimeWeaverAware.class, <span class="literal">false</span>, <span class="literal">false</span>);</span><br><span class="line">   <span class="keyword">for</span> (String weaverAwareName : weaverAwareNames) &#123;</span><br><span class="line">      getBean(weaverAwareName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Stop using the temporary ClassLoader for type matching.</span></span><br><span class="line">   beanFactory.setTempClassLoader(<span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for caching all bean definition metadata, not expecting further changes.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultListableBeanFactory#freezeConfiguration</span></span><br><span class="line"><span class="comment">// 设置configurationFrozen是true,</span></span><br><span class="line"><span class="comment">// 将beanDefinitionNames集合转哈为String类型的数组. StringUtils.toStringArray(this.beanDefinitionNames);使用这个方法即可.    </span></span><br><span class="line">   beanFactory.freezeConfiguration();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">   beanFactory.preInstantiateSingletons();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="preInstantiateSingletons-方法"><a href="#preInstantiateSingletons-方法" class="headerlink" title="preInstantiateSingletons 方法"></a>preInstantiateSingletons 方法</h6><p>这里就是对 单例池 里面的对象进行初始化,可以看到是有 getBean 方法的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">preInstantiateSingletons</span><span class="params">()</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">      logger.trace(<span class="string">&quot;Pre-instantiating singletons in &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Iterate over a copy to allow for init methods which in turn register new bean definitions.</span></span><br><span class="line">   <span class="comment">// While this may not be part of the regular factory bootstrap, it does otherwise work fine.</span></span><br><span class="line"><span class="comment">// 这里获取出来的 beanNames 是有6个的,其中五个是包含了内部的</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalCommonAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerFactory</span></span><br><span class="line"><span class="comment">//yangBeanScannerConfig    </span></span><br><span class="line">   List&lt;String&gt; beanNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(<span class="built_in">this</span>.beanDefinitionNames);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Trigger initialization of all non-lazy singleton beans...</span></span><br><span class="line">   <span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">      <span class="type">RootBeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line">       </span><br><span class="line">  <span class="comment">// bd 不是抽象的&amp;是单例的&amp;不是赖加载的     </span></span><br><span class="line">      <span class="keyword">if</span> (!bd.isAbstract() &amp;&amp; bd.isSingleton() &amp;&amp; !bd.isLazyInit()) &#123;</span><br><span class="line">  <span class="comment">// 判断是不是 FactroyBean        </span></span><br><span class="line">         <span class="keyword">if</span> (isFactoryBean(beanName)) &#123;</span><br><span class="line">            <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> getBean(FACTORY_BEAN_PREFIX + beanName);</span><br><span class="line">            <span class="keyword">if</span> (bean <span class="keyword">instanceof</span> FactoryBean) &#123;</span><br><span class="line">               <span class="keyword">final</span> FactoryBean&lt;?&gt; factory = (FactoryBean&lt;?&gt;) bean;</span><br><span class="line">               <span class="type">boolean</span> isEagerInit;</span><br><span class="line">               <span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span> &amp;&amp; factory <span class="keyword">instanceof</span> SmartFactoryBean) &#123;</span><br><span class="line">                  isEagerInit = AccessController.doPrivileged((PrivilegedAction&lt;Boolean&gt;)</span><br><span class="line">                              ((SmartFactoryBean&lt;?&gt;) factory)::isEagerInit,</span><br><span class="line">                        getAccessControlContext());</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">else</span> &#123;</span><br><span class="line">                  isEagerInit = (factory <span class="keyword">instanceof</span> SmartFactoryBean &amp;&amp;</span><br><span class="line">                        ((SmartFactoryBean&lt;?&gt;) factory).isEagerInit());</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">if</span> (isEagerInit) &#123;</span><br><span class="line">                  getBean(beanName);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 这里不是 FactoryBean  </span></span><br><span class="line"><span class="comment">// 可以看到当我走到yangBeanScannerConfig,我们定义的类的时候,走完这个方法，就可以看到com.iyang.spring.config.YangBeanScannerConfig#YangBeanScannerConfig中打印的语句了,也就是说走完这里，我们定义的bean就已经被Spring被实例化了.             </span></span><br><span class="line">            getBean(beanName);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Trigger post-initialization callback for all applicable beans...</span></span><br><span class="line"><span class="comment">// 这里再对 beanNames 进行迭代,如果是 SmartInitializingSingleton 的话，就会再调用    afterSingletonsInstantiated 方法.</span></span><br><span class="line">   <span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">      <span class="type">Object</span> <span class="variable">singletonInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">      <span class="keyword">if</span> (singletonInstance <span class="keyword">instanceof</span> SmartInitializingSingleton) &#123;</span><br><span class="line">         <span class="keyword">final</span> <span class="type">SmartInitializingSingleton</span> <span class="variable">smartSingleton</span> <span class="operator">=</span> (SmartInitializingSingleton) singletonInstance;</span><br><span class="line">         <span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span>) &#123;</span><br><span class="line">            AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">               smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">               <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            &#125;, getAccessControlContext());</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">            smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以对我们定义的 bean 进行实例化，最后是调用了 getBean 方法， getBean 方法表面看上去是获取，其实如果没有的话，调用的是createBean方法, 也就是会实例化我们的bean。当然它肯定不会很简单的去调用反射就实例化完一个我们的bean,肯定是有一系列的走Spring内置的或者我们自己定义的后置处理器等操作.</p>
<p>getBean 方法需要后面专门领出来分析，不能简单的过，这里对 Spring 容器进行大致的flow过,所以还是比较轻描淡写的写过去.</p>
<h5 id="finishRefresh-方法"><a href="#finishRefresh-方法" class="headerlink" title="finishRefresh 方法"></a>finishRefresh 方法</h5><p>中文式的英语 : 结束刷新方法.</p>
<p>显示清除缓存,再是init了LifecycleProcessor,调用其onRefresh()方法,接近就是发送一个ContextRefreshedEvent事件出来.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the refresh of this context, invoking the LifecycleProcessor&#x27;s</span></span><br><span class="line"><span class="comment"> * onRefresh() method and publishing the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> org.springframework.context.event.ContextRefreshedEvent&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Clear context-level resource caches (such as ASM metadata from scanning).</span></span><br><span class="line"><span class="comment">//对org.springframework.core.io.DefaultResourceLoader#resourceCaches进行清除.    </span></span><br><span class="line">   clearResourceCaches();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize lifecycle processor for this context.</span></span><br><span class="line">   initLifecycleProcessor();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Propagate refresh to lifecycle processor first.</span></span><br><span class="line"><span class="comment">//org.springframework.context.support.DefaultLifecycleProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.support.DefaultLifecycleProcessor#startBeans</span></span><br><span class="line">    </span><br><span class="line">   getLifecycleProcessor().onRefresh();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish the final event.</span></span><br><span class="line"><span class="comment">// 推送Event,这里的Event是 ContextRefreshedEvent.    </span></span><br><span class="line">   publishEvent(<span class="keyword">new</span> <span class="title class_">ContextRefreshedEvent</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Participate in LiveBeansView MBean, if active.</span></span><br><span class="line"><span class="comment">//org.springframework.context.support.LiveBeansView#registerApplicationContext</span></span><br><span class="line"><span class="comment">//先根据key:spring.liveBeansView.mbeanDomain获取value,这里获取出来的是null,</span></span><br><span class="line"><span class="comment">// 所以也就是没有下文了.    </span></span><br><span class="line">   LiveBeansView.registerApplicationContext(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="initLifecycleProcessor-方法"><a href="#initLifecycleProcessor-方法" class="headerlink" title="initLifecycleProcessor 方法 ()"></a>initLifecycleProcessor 方法 ()</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * Initialize the LifecycleProcessor.</span><br><span class="line"> * Uses DefaultLifecycleProcessor if none defined in the context.</span><br><span class="line"> * @see org.springframework.context.support.DefaultLifecycleProcessor</span><br><span class="line"> */</span><br><span class="line">protected void initLifecycleProcessor() &#123;</span><br><span class="line">  // 获取出 beanFactory 来.  </span><br><span class="line">   ConfigurableListableBeanFactory beanFactory = getBeanFactory();</span><br><span class="line">// 判断beanFactory中是否包含lifecycleProcessor    </span><br><span class="line">   if (beanFactory.containsLocalBean(LIFECYCLE_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">// 包含的话,就会获取出来,指向this.lifecycleProcessor       </span><br><span class="line">      this.lifecycleProcessor =</span><br><span class="line">            beanFactory.getBean(LIFECYCLE_PROCESSOR_BEAN_NAME, LifecycleProcessor.class);</span><br><span class="line">      if (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(&quot;Using LifecycleProcessor [&quot; + this.lifecycleProcessor + &quot;]&quot;);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   else &#123;</span><br><span class="line">   // 如果不包含的话，就自己new一个,然后注册到Spring容器中来.    </span><br><span class="line">      DefaultLifecycleProcessor defaultProcessor = new DefaultLifecycleProcessor();</span><br><span class="line">      defaultProcessor.setBeanFactory(beanFactory);</span><br><span class="line">      this.lifecycleProcessor = defaultProcessor;</span><br><span class="line">      beanFactory.registerSingleton(LIFECYCLE_PROCESSOR_BEAN_NAME, this.lifecycleProcessor);</span><br><span class="line">      if (logger.isTraceEnabled()) &#123;</span><br><span class="line">         logger.trace(&quot;No &#x27;&quot; + LIFECYCLE_PROCESSOR_BEAN_NAME + &quot;&#x27; bean, using &quot; +</span><br><span class="line">               &quot;[&quot; + this.lifecycleProcessor.getClass().getSimpleName() + &quot;]&quot;);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="publishEvent-方法"><a href="#publishEvent-方法" class="headerlink" title="publishEvent 方法"></a>publishEvent 方法</h6><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Publish the given event to all listeners.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> event the event to publish (may be an &#123;<span class="doctag">@link</span> ApplicationEvent&#125;</span></span><br><span class="line"><span class="comment"> * or a payload object to be turned into a &#123;<span class="doctag">@link</span> PayloadApplicationEvent&#125;)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> eventType the resolved event type, if known</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 4.2</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">publishEvent</span><span class="params">(Object event, <span class="meta">@Nullable</span> ResolvableType eventType)</span> &#123;</span><br><span class="line">   Assert.notNull(event, <span class="string">&quot;Event must not be null&quot;</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Decorate event as an ApplicationEvent if necessary</span></span><br><span class="line">   ApplicationEvent applicationEvent;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 对传入进来的 event 进行类型的判断.    </span></span><br><span class="line">   <span class="keyword">if</span> (event <span class="keyword">instanceof</span> ApplicationEvent) &#123;</span><br><span class="line">      applicationEvent = (ApplicationEvent) event;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      applicationEvent = <span class="keyword">new</span> <span class="title class_">PayloadApplicationEvent</span>&lt;&gt;(<span class="built_in">this</span>, event);</span><br><span class="line">      <span class="keyword">if</span> (eventType == <span class="literal">null</span>) &#123;</span><br><span class="line">         eventType = ((PayloadApplicationEvent&lt;?&gt;) applicationEvent).getResolvableType();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Multicast right now if possible - or lazily once the multicaster is initialized</span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.earlyApplicationEvents != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="built_in">this</span>.earlyApplicationEvents.add(applicationEvent);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">//org.springframework.context.event.SimpleApplicationEventMulticaster#multicastEvent(org.springframework.context.ApplicationEvent, org.springframework.core.ResolvableType)</span></span><br><span class="line"><span class="comment">//走到了这里来发送event的,       </span></span><br><span class="line">      getApplicationEventMulticaster().multicastEvent(applicationEvent, eventType);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish event via parent context as well...</span></span><br><span class="line"><span class="comment">// 这里的 parent是null.    </span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.parent != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.parent <span class="keyword">instanceof</span> AbstractApplicationContext) &#123;</span><br><span class="line">         ((AbstractApplicationContext) <span class="built_in">this</span>.parent).publishEvent(event, eventType);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         <span class="built_in">this</span>.parent.publishEvent(event);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里是发送ContextRefreshedEvent事件出来.</p>
<h5 id="resetCommonCaches-方法"><a href="#resetCommonCaches-方法" class="headerlink" title="resetCommonCaches 方法()"></a>resetCommonCaches 方法()</h5><p>可以看到 finally 代码块中是疯狂的清除各种缓存.</p>
<p>可以大家可以点进去详细的看下，具体就不仔细描述了.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Reset Spring&#x27;s common reflection metadata caches, in particular the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> ReflectionUtils&#125;, &#123;<span class="doctag">@link</span> AnnotationUtils&#125;, &#123;<span class="doctag">@link</span> ResolvableType&#125;</span></span><br><span class="line"><span class="comment"> * and &#123;<span class="doctag">@link</span> CachedIntrospectionResults&#125; caches.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 4.2</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ReflectionUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> AnnotationUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ResolvableType#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> CachedIntrospectionResults#clearClassLoader(ClassLoader)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">resetCommonCaches</span><span class="params">()</span> &#123;</span><br><span class="line">   ReflectionUtils.clearCache();</span><br><span class="line">   AnnotationUtils.clearCache();</span><br><span class="line">   ResolvableType.clearCache();</span><br><span class="line">   CachedIntrospectionResults.clearClassLoader(getClassLoader());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>最后总结下,Spring在加载 bean &amp; 处理内置的一些配置 &amp; 内部处理器的时候,是下了很多的功夫。可以看着这些方法一步一步的分析下去,理解起来，个人感觉这里还不是特别深入的跟进去了代码，只是一个简单的大概描述，更深入的知识需要更加详细的理解等了.</p>
<p>这里只是简单的对这个整个flow来进行描述，还不是特别有详细的那种.</p>
]]></content>
      <categories>
        <category>java</category>
        <category>spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>synchronized笔记</title>
    <url>//article/synchronized_note/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-synchronized"><a href="#📝-synchronized" class="headerlink" title="📝 synchronized"></a>📝 <strong>synchronized</strong></h1><h2 id="前置条件"><a href="#前置条件" class="headerlink" title="前置条件"></a><strong>前置条件</strong></h2><h3 id="临界区"><a href="#临界区" class="headerlink" title="临界区"></a>临界区</h3><ul>
<li>一个程序运行多线程本身没有问题</li>
<li>多个线程访问共享资源</li>
</ul>
<ol>
<li>多个线程访问共享资源也没问题</li>
<li>多个线程对共享资源读写操作时发生指令交错,就会出现问题</li>
</ol>
<ul>
<li>一段代码块内如果存在对共享资源的多线程读写操作，称这段代码块为临界区，其共享资源为临界资源</li>
</ul>
<h3 id="竞态条件"><a href="#竞态条件" class="headerlink" title="竞态条件"></a>竞态条件</h3><p>多个线程在临界区内执行，由于代码顺序序列不同而导致结果无法预测，称之为发生了竟态条件</p>
<ul>
<li>阻塞解决方法： Synchronized，Lock</li>
<li>非阻塞解决方法： 原子变量</li>
</ul>
<h2 id="synchronized"><a href="#synchronized" class="headerlink" title="synchronized"></a><strong>synchronized</strong></h2><p>synchronized 同步快是 java 提供的一种原子性内置锁，java中的对个对象都可以当作一个同步锁来使用，这些java内置的使用者看不到等锁被称为内置锁，也叫做监视器 基于 Monitor机制实现，依赖底层操作系统的互斥原语Mutex（互斥量）, 它是一个重量级锁，性能较低。 1.5之后进行了优化，锁粗化，锁消除，轻量级锁，偏向锁，自适应自旋等技术来减少锁操作的开销 同步方法是通过 access_flags 中设置 ACC_SYNCHRONIZED 标志来实现;同步代码快是通过 monitorenter 和 monitorexit来实现。两个指令等的执行是JVM通过调用操作系统的斥原语mutex来实现，被阻塞的线程会被挂起来，等待重新调度，会导致 “用户态和内核态” 两个态之间来回切换，对性能影响较大</p>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<h3 id="原子性"><a href="#原子性" class="headerlink" title="原子性"></a>原子性</h3><p>synchronized 保证同一时间只有一个线程拿到锁，能够进入同步代码快</p>
<h3 id="可见性"><a href="#可见性" class="headerlink" title="可见性"></a>可见性</h3><p>执行 synchronized 时，会对应lock原子操作回刷新共享变量的值</p>
<h3 id="有序性"><a href="#有序性" class="headerlink" title="有序性"></a>有序性</h3><p>as-if-serial: 不管编译器和CPU如何重拍序，必须保证在单线程情况下程序结果是正确的，还有就是数据依赖的也是不能重拍序的</p>
<h3 id="可重入锁"><a href="#可重入锁" class="headerlink" title="可重入锁"></a>可重入锁</h3><p>synchronized锁对象的时候有个计数器，他会记录下线程获取锁的次数，在执行完对应的代码块之后，计数器就会-1，直到计数器清零，就释放锁了</p>
<h3 id="不可中断"><a href="#不可中断" class="headerlink" title="不可中断"></a>不可中断</h3><p>一个线程获取锁之后，另外一个线程处于阻塞或者等待状态，前一个不释放，后一个会一直阻塞或者等待，不可被中断</p>
<h2 id="Monitor-管程-监视器"><a href="#Monitor-管程-监视器" class="headerlink" title="Monitor(管程&#x2F;监视器)"></a><strong>Monitor(管程&#x2F;监视器)</strong></h2><p>管程是指管理共享变量以及对共享变量操作的过程，让它们支持并发</p>
<h2 id="MESA模型"><a href="#MESA模型" class="headerlink" title="MESA模型"></a>MESA模型</h2><p>唤醒的时间和获取到锁继续执行的时间是不一致的，被唤醒的线程再次执行时可能条件又不满足了，所以循环检验条件。MESA模型的wait方法有一个超时参数，为了避免线程进入等待队列永久阻塞</p>
<p><img src="/images/posts/synchronized_note/img-1.png" alt="synchronized笔记"></p>
<h3 id="notify和notifyAll"><a href="#notify和notifyAll" class="headerlink" title="notify和notifyAll"></a><strong>notify和notifyAll</strong></h3><p>满足以下三个条件时，可以使用notify，其余情况尽量使用notifyall</p>
<ol>
<li>所有等待线程拥有相同的等待条件</li>
<li>所有等待线程被唤醒后，执行相同的操作</li>
<li>只需要唤醒一个线程</li>
</ol>
<h2 id="Monitor机制java实现"><a href="#Monitor机制java实现" class="headerlink" title="Monitor机制java实现"></a><strong>Monitor机制java实现</strong></h2><p>ObjectMonitor 数据结构如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ObjectMonitor() &#123;</span><br><span class="line">    _header       = NULL; <span class="comment">//对象头  markOop</span></span><br><span class="line">    _count        = <span class="number">0</span>;  </span><br><span class="line">    _waiters      = <span class="number">0</span>,   </span><br><span class="line">    _recursions   = <span class="number">0</span>;   <span class="comment">// 锁的重入次数 </span></span><br><span class="line">    _object       = NULL;  <span class="comment">//存储锁对象</span></span><br><span class="line">    _owner        = NULL;  <span class="comment">// 标识拥有该monitor的线程（当前获取锁的线程） </span></span><br><span class="line">    _WaitSet      = NULL;  <span class="comment">// 等待线程（调用wait）组成的双向循环链表，_WaitSet是第一个节点</span></span><br><span class="line">    _WaitSetLock  = <span class="number">0</span> ;    </span><br><span class="line">    _Responsible  = NULL ;</span><br><span class="line">    _succ         = NULL ;</span><br><span class="line">    _cxq          = NULL ; <span class="comment">//多线程竞争锁会先存到这个单向链表中 （FILO栈结构）</span></span><br><span class="line">    FreeNext      = NULL ;</span><br><span class="line">    _EntryList    = NULL ; <span class="comment">//存放在进入或重新进入时被阻塞(blocked)的线程 (也是存竞争锁失败的线程)</span></span><br><span class="line">    _SpinFreq     = <span class="number">0</span> ;</span><br><span class="line">    _SpinClock    = <span class="number">0</span> ;</span><br><span class="line">    OwnerIsThread = <span class="number">0</span> ;</span><br><span class="line">    _previous_owner_tid = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<h2 id="对象内存布局"><a href="#对象内存布局" class="headerlink" title="对象内存布局"></a>对象内存布局</h2><p>对象在内存中存储等布局可以分为三块区域： 对象头(Header)，实例数据(Instance Data)和对齐填充(Padding)</p>
<ul>
<li>对象头：hash码，对象所属年代，锁状态标志，偏向锁（线程），偏向时间，数组长度（数组对象才有）</li>
<li>实例数据：存放类的属性数据信息，包括父类的属性信息</li>
<li>对齐填充：由于虚拟机要求对象起始地址必须是8字节的倍数，填充数据不是必须存在等，仅仅是为了字节对齐</li>
</ul>
<p><img src="/images/posts/synchronized_note/img-2.png" alt="synchronized笔记"></p>
<h3 id="对象头详解"><a href="#对象头详解" class="headerlink" title="对象头详解"></a><strong>对象头详解</strong></h3><ul>
<li>Mark Word：用于存储对象自身的运行时数据，如哈希码（HashCode）、GC分代年龄、锁状态标志、线程持有的锁、偏向线程ID、偏向时间戳等，这部分数据的长度在32位和64位的虚拟机中分别为32bit和64bit，官方称它为“Mark Word”</li>
<li>Klass Pointer：对象头的另外一部分是klass类型指针，即对象指向它的类元数据的指针，虚拟机通过这个指针来确定这个对象是那个类的实例。</li>
<li>数组长度：如果对象是一个数组, 那在对象头中还必须有一块数据用于记录数组长度</li>
</ul>
<h2 id="用户态和内核态"><a href="#用户态和内核态" class="headerlink" title="用户态和内核态"></a><strong>用户态和内核态</strong></h2><p>我们所有的程序都是用户空间运行，进入用户运行状态也是(用户态)，但是很多操作可能涉及内核运行，比如IO，就会进入内核运行状态（内核态）</p>
<ol>
<li>用户态把一些数据放到寄存器，或者创建对应的堆栈，表明需要操作系统提供的服务</li>
<li>用户态执行系统调用</li>
<li>CPU切换到内核态，跳转到对应的内存指定的位置执行指令</li>
<li>系统调用处理器去读取我们先前放到内存的数据参数，并执行下个指令</li>
<li>调用完成，操作系统重置CPU为用户态返回结果，并执行下个指令</li>
</ol>
<h2 id="锁升级场景"><a href="#锁升级场景" class="headerlink" title="锁升级场景"></a>锁升级场景</h2><p>32位JVM对象结构</p>
<p><img src="/images/posts/synchronized_note/img-3.png" alt="synchronized笔记"></p>
<p>64位JVM 对象结构</p>
<p><img src="/images/posts/synchronized_note/img-4.png" alt="synchronized笔记"></p>
<p>锁标识位置</p>
<p><img src="/images/posts/synchronized_note/img-5.png" alt="synchronized笔记"></p>
<p>锁状态变化</p>
<p><img src="/images/posts/synchronized_note/img-6.png" alt="synchronized笔记"></p>
<h3 id="偏向锁批量偏向-批量撤销"><a href="#偏向锁批量偏向-批量撤销" class="headerlink" title="偏向锁批量偏向&amp;批量撤销"></a><strong>偏向锁批量偏向&amp;批量撤销</strong></h3><p>从偏向锁的加锁解锁过程中可以看出，当只有一个线程反复进入一个同步快时，偏向锁带来的性能开销基本可以忽略，但是当有其他线程尝试获取锁时，就需要等于safe point时，再将偏向锁撤销为无锁状态或者升级为轻量锁，会消耗一定的性能。所以在多线程的情况下，偏向锁不仅不能提高性能，还会导致性能下降。于是就有了批量偏向锁与批量撤销的机制</p>
<h3 id="锁粗化"><a href="#锁粗化" class="headerlink" title="锁粗化"></a>锁粗化</h3><p>假设一系列的连续操作都会对同一个对象反复加锁以及解锁，甚至加锁操作是出现在循环中的，即使没有出现线程的竞争，频繁的进行互斥同步操作也会导致不必要等性能损耗。如果JVM检测到有一连串零碎的操作都是同一个对象的加锁，将会扩大加锁同步的范围（即锁粗化）到整个操作序列等外部</p>
<h3 id="锁消除"><a href="#锁消除" class="headerlink" title="锁消除"></a>锁消除</h3><p>锁消除即删除不必要的加锁操作。锁消除是在java虚拟机在JIT编译期间，通过运行上下文的扫描，去除不可能存在共享资源竞争的锁，通过锁消除，可以节省毫无意义的请求锁时间</p>
<h3 id="逃逸分析"><a href="#逃逸分析" class="headerlink" title="逃逸分析"></a>逃逸分析</h3><ul>
<li>逃逸分析，是一种可以有效减少java程序中同步负载和内存堆分配压力的跨函数全局数据流分析算法。通过逃逸分析，java hotspot编译器能够分析出一个新的对象的引用的使用范围从而决定是否将这个对象分配到堆上。逃逸分析的基本行为就是分析对象动态作用域。</li>
<li>方法逃逸：当一个对象在方法中被定义后，它可能被外部方法所引用，例如作为调用参数传递到其他地方中</li>
<li>线程逃避：这个对象甚至可能被其他线程访问到，例如赋值给类变量或可以在其他线程中访问的实例变量</li>
</ul>
<p>使用逃逸分析，编译器可以对代码优化：</p>
<ol>
<li>同步省略或者锁消除。如果一个对象被发现只能从一个线程被访问到，那么对于这个对象的操作是不考虑同步的</li>
<li>将堆分配转化为栈分配。如果一个对象在子程序中被分配，要使指向该对象的指针永远不会逃逸，对象可能是栈分配的候选，而不是堆分配</li>
<li>分离对象 或 标量替换。有的对象可能不需要连续的内存结构存在也可以被访问到，那么对象的部分（或全部）可以不存储在内存，而是存储在CPU寄存器中</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">-XX:+DoEscapeAnalysis  <span class="comment">//表示开启逃逸分析 (jdk1.8默认开启）</span></span><br><span class="line">-XX:-DoEscapeAnalysis <span class="comment">//表示关闭逃逸分析。</span></span><br><span class="line">-XX:+EliminateAllocations   <span class="comment">//开启标量替换(默认打开)</span></span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>ThreadLocal源码阅读</title>
    <url>//article/threadLocal_note/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-ThreadLocal源码阅读"><a href="#📝-ThreadLocal源码阅读" class="headerlink" title="📝 ThreadLocal源码阅读"></a>📝 <strong>ThreadLocal源码阅读</strong></h1><p>ThreadLocal 是来这个公司有过使用一次的感受,所以就学习阅读下源码。 其实Thread 这个里面,就有一个 Map(这里是用ThreadLocal内部类中实现的) , 里面的key就是 ThreadLocal, value 就是存储的值,所以一个Thread是有多个 ThreadLocal</p>
<h2 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">threadLocalHashCode</span> <span class="operator">=</span> nextHashCode();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The next hash code to be given out. Updated atomically. Starts at</span></span><br><span class="line"><span class="comment"> * zero.</span></span><br><span class="line"><span class="comment">   AtomicInteger 是一个线程安全的,实现原理是采用了cas.	</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">AtomicInteger</span> <span class="variable">nextHashCode</span> <span class="operator">=</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">AtomicInteger</span>();</span><br></pre></td></tr></table></figure>

<h2 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h2><h3 id="set-赋值"><a href="#set-赋值" class="headerlink" title="set 赋值"></a>set 赋值</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">*	首先获取当前线程.</span></span><br><span class="line"><span class="comment">	调用 getMap 方法, 直接调用 t.trheadLocals来获取 ThreadLocalMap。(ThreadLocalMap这里是ThreadLocal内部自己实现的类)</span></span><br><span class="line"><span class="comment">	如果map不是null的话,就进行set值,这里可以看到 set 的key是this,也就是ThreadLocal它自己.</span></span><br><span class="line"><span class="comment">	否则就是调用createMap方法,走这个方法是可以确认 currentThread中的threadLocals的值是null,所以直接new了一个进行赋值即可.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(T value)</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="literal">null</span>)</span><br><span class="line">        map.set(<span class="built_in">this</span>, value);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        createMap(t, value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ThreadLocalMap <span class="title function_">getMap</span><span class="params">(Thread t)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> t.threadLocals;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">createMap</span><span class="params">(Thread t, T firstValue)</span> &#123;</span><br><span class="line">        t.threadLocals = <span class="keyword">new</span> <span class="title class_">ThreadLocalMap</span>(<span class="built_in">this</span>, firstValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="get方法"><a href="#get方法" class="headerlink" title="get方法"></a>get方法</h3><p>get 获取值方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	这里可以看到,获取ThreadLocalMap,如果ThreadLocalMap的是null的话,就会走setInitialValue方法。</span></span><br><span class="line"><span class="comment">	如果有值的话,就会进行获取值并且返回.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> T <span class="title function_">get</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="literal">null</span>) &#123;</span><br><span class="line">        ThreadLocalMap.<span class="type">Entry</span> <span class="variable">e</span> <span class="operator">=</span> map.getEntry(<span class="built_in">this</span>);</span><br><span class="line">        <span class="keyword">if</span> (e != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">            <span class="type">T</span> <span class="variable">result</span> <span class="operator">=</span> (T)e.value;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> setInitialValue();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	如果获取出来的ThreadLocalMap 不是null的话,就会进行set,这个时候set进去的值,value就是null了.</span></span><br><span class="line"><span class="comment">	如果获取出来是nulld</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">private</span> T <span class="title function_">setInitialValue</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">T</span> <span class="variable">value</span> <span class="operator">=</span> initialValue();</span><br><span class="line">        <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">        <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">        <span class="keyword">if</span> (map != <span class="literal">null</span>)</span><br><span class="line">            map.set(<span class="built_in">this</span>, value);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            createMap(t, value);</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> T <span class="title function_">initialValue</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="remove方法"><a href="#remove方法" class="headerlink" title="remove方法"></a>remove方法</h3><p>remove 方法就是获取map,如果map不是null的话,就调用m.remove(this)，根据当前this来删除</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">remove</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">m</span> <span class="operator">=</span> getMap(Thread.currentThread());</span><br><span class="line">    <span class="keyword">if</span> (m != <span class="literal">null</span>)</span><br><span class="line">        m.remove(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="📔为什么要将key设计成ThreadLocal的弱引用？"><a href="#📔为什么要将key设计成ThreadLocal的弱引用？" class="headerlink" title="📔为什么要将key设计成ThreadLocal的弱引用？"></a><strong>📔为什么要将key设计成ThreadLocal的弱引用？</strong></h1><p>如果 ThreadLocal 的 Key 是强引用，同样会发生内存泄漏的。如果 ThreadLocal的Key是强引用，引用的ThreadLocal的对象被回收了，但是ThreadLocalMap还持有ThreadLocal的强引用，如果没有手动删除，ThreadLocal不会被回收，发生内存泄露，用强引用100%发生内存泄漏</p>
<p>如果是弱引用的话，引用的<code>ThreadLocal</code>的对象被回收了，即使没有手动删除,<code>ThreadLocal</code>也会被回收.<code>value</code>也会在<code>ThreadLocalMap</code>调用 <code>set()</code>、<code>get()</code>、</p>
<p><code>remove()</code> 的时候会被清除.</p>
<p>所以两种方案比较下来，还是<code>ThreadLoacl</code>的<code>key</code>为弱引用好一些</p>
<p><img src="/images/posts/threadLocal_note/img-1.png" alt="ThreadLocal源码阅读"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>ThreadLocal里面的方法也比较少,还是比较好理解的。只要弄清楚ThreadLocal和Thread是怎么在存储的,就很好的理解了</p>
<p>注意 : 使用ThreadLocal一定要进行remove,否则容易出现内存泄漏，从而导致内存溢出</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Unsafe类</title>
    <url>//article/unsafe_read1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-主旨内容"><a href="#📝-主旨内容" class="headerlink" title="📝 主旨内容"></a>📝 主旨内容</h1><p><img src="/images/posts/unsafe_read1/img-1.png" alt="Unsafe类"></p>
<h1 id="堆外内存"><a href="#堆外内存" class="headerlink" title="堆外内存"></a><strong>堆外内存</strong></h1><ul>
<li>对垃圾会搜停顿的改善,由于堆外内存是直接受操作系统管理而不是JVM,所以当我们使用堆外内存的时候,即可保持较小的堆内内存规模.从而在GC时减少回收停顿对于应用的影响</li>
<li>提升程序I&#x2F;O操作的性能,通常在I&#x2F;O通信过程中,会存在堆内内存到堆外内存的数据拷贝,对于需要频繁进行内存间数据拷贝且生命周期较短的暂存数据,都建议存储到堆外内存.</li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://tech.meituan.com/2019/02/14/talk-about-java-magic-class-unsafe.html">https://tech.meituan.com/2019/02/14/talk-about-java-magic-class-unsafe.html</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>volatile笔记</title>
    <url>//article/volatile_note/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-volatile笔记"><a href="#📝-volatile笔记" class="headerlink" title="📝 volatile笔记"></a>📝 volatile笔记</h1><h2 id="前置条件"><a href="#前置条件" class="headerlink" title="前置条件"></a>前置条件</h2><p><img src="/images/posts/volatile_note/img-1.png" alt="volatile笔记"></p>
<p>所有的数据处理都在CPU内进行，因此保存在内存中的程序在执行前需要被复制到cpu中。程序指令复制到CPU中，可以一次复制一条，也可以一次复制多条。CPU和物理内存之间的通信，CPU的处理速度远大于物理内存，所有CPU就了自己的内存，所以此时 CPU和主内存是不一致的</p>
<h2 id="缓存一致性"><a href="#缓存一致性" class="headerlink" title="缓存一致性"></a>缓存一致性</h2><h3 id="操作系统提供"><a href="#操作系统提供" class="headerlink" title="操作系统提供"></a>操作系统提供</h3><p>操作系统提供总线锁定的机制，前端总线(也叫CPU总线，Front Side Bus）)是所有CPU与芯片组连接的主干道，<strong>负责CPU与外界所有部件的通信</strong>，包括高速缓存、内存、北桥，其控制总线向各个部件发送控制信号、通过地址总线发送地址信号指定其要访问的部件、通过数据总线双向传输。在CPU1要做 i++操作的时候，其在总线上发出一个LOCK#信号，其他处理器就不能操作缓存了该共享变量内存地址的缓存，也就是阻塞了其他CPU，使该处理器可以独享此共享内存。 但我们只需要对此共享变量的操作是原子就可以了，而总线锁定把CPU和内存的通信给锁住了，使得在锁定期间，其他处理器不能操作其他内存地址的数据，从而开销较大，所以后来的CPU都提供了缓存一致性机制，Intel的奔腾486之后就提供了这种优化。 总线锁定等颗粒度就大了，性能损失</p>
<h3 id="缓存一致性-1"><a href="#缓存一致性-1" class="headerlink" title="缓存一致性"></a>缓存一致性</h3><p>缓存一致性机制整体来说，就当某快CPU对缓存中等数据进行操作了之后，就通知其他CPU放弃存储在它们内部的缓存，或者从主线程中重新读取</p>
<ol>
<li>写传播（<strong>Write Propagation</strong>）： 对任何缓存中的数据的更改都必须传播到对等缓存中的其他副本(该缓存行的副本)</li>
<li>事务串行化：对单个内存位置的读&#x2F;写必须被所有处理器以相同的顺序看到，理论上，一致性可以在加载&#x2F;存储粒度上执行</li>
<li>一致性机制：确保一致性的两种最常见等机制是 窥探机制 和 基于目录的机制。 窥探机制：每个请求必须广播到系统中的所有节点，这意味随着系统越大，（逻辑或物理）总线大小及其宽带也必须增加。 目录机制：往往有更长的延迟（3跳 请求&#x2F;转发&#x2F;响应），但是使用宽带更少，因为消息都是点对点的</li>
</ol>
<h2 id="MESI协议"><a href="#MESI协议" class="headerlink" title="MESI协议"></a>MESI协议</h2><p>是以缓存行(缓存的基本数据单位，一般64字节)的几个状态来命名的（Modified,Exclusive,Share or Invalid）。 M: 被修改的。处于这一状态的数据，只在CPU中有缓存数据，而其他CPU中没有。同时状态相对其他内存中的值来说，是已经被修改的，且没有更新到内存中。如果别的CPU内核要读主内存这块数据，该缓存行必须写回到主存，状态修改为S E: 独占的。处于这一状态的数据，只有在本CPU中有缓存，且其数据没有修改，即与内存中一致 S: 共享的。处于这一状态的数据在多个CPU中都有缓存，且与内存一致。缓存行可以在任意时刻抛弃 I: 无效的。本CPU中的这份缓存已经无效</p>
<h2 id="内存屏障"><a href="#内存屏障" class="headerlink" title="内存屏障"></a>内存屏障</h2><p>编译器和CPU在不影响结果的情况下对指令重排序，使性能得到优化，但是实际情况里面有些指令虽然没有前后依赖关系，但是重排序之后影响到输出结果，这是很插入一个 内存屏障， 相当于告诉了CPU和编译器限于这个命令的必须执行，后于这个命令的必须后执行。 内存屏障的另外一个作用是强制更新一次不同的CPU的缓存，这意味着如果你对一个volatile字段进行操作，你必须知道</p>
<ol>
<li>一旦你完成写入，任何访问这个字段的线程将会得到最新的值</li>
<li>在你写入之前，会保证所有之前发生的事已经发生了，并且任何更新过的数据值也是可见的，因为内存屏障会把之前写入值都刷新到缓存</li>
</ol>
<h2 id="伪共享"><a href="#伪共享" class="headerlink" title="伪共享"></a>伪共享</h2><p>如果多个核的线程在操作同一个缓存行中的不同变量数据，那么就出现频繁的缓存失效，即使在代码层面看这两个线程操作的数据之间完全没有关系。这在不合理的资源竞争情况就是伪共享(False Sharing)</p>
<h2 id="可见性和顺序性"><a href="#可见性和顺序性" class="headerlink" title="可见性和顺序性"></a>可见性和顺序性</h2><p>可见性： 每个线程操作数据的时候会把数据从主内存读取到自己的工作内存，根据MESI思想，如果某个线程修改数据写回主内存，其他线程能通过嗅探检测到本地数据无效，然后从主内存读取到自己的工作内存 顺序性：通过指令间加入内存屏障来防止指令重排序</p>
<h2 id="嗅探"><a href="#嗅探" class="headerlink" title="嗅探"></a>嗅探</h2><p>每个处理器通过嗅探在总线上传播的数据来检测自己的缓存数据是不是过期了，当处理器发现自己缓存数据对应的内存地址被修改，就会将当前处理器的缓存数据设置成无效状态，当处理器对这个数据进行修改操作的时候，会重新从系统内存中把数据读到处理器缓存</p>
<h2 id="总线风暴"><a href="#总线风暴" class="headerlink" title="总线风暴"></a>总线风暴</h2><p>java中使用unsafe实现cas，而其底层有C++调用汇编指令实现的，如果多核cpu使用lock cmpxchg，单核cpu使用cmpxch指令 如果在段时间内产生大量的cas操作再加上volatile的嗅探机制则会不断地占用总线宽带，导致总线流量激增，就会产生总线风暴</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://blog.csdn.net/Anenan/article/details/114955578">https://blog.csdn.net/Anenan/article/details/114955578</a> <a href="https://www.cnblogs.com/Courage129/p/14401680.html">https://www.cnblogs.com/Courage129/p/14401680.html</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>线程池源码笔记</title>
    <url>//article/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-线程池"><a href="#📝-线程池" class="headerlink" title="📝 线程池"></a>📝 线程池</h1><h2 id="使用线程池的好处"><a href="#使用线程池的好处" class="headerlink" title="使用线程池的好处"></a><strong>使用线程池的好处</strong></h2><p>核心问题就是资源管理问题</p>
<ol>
<li>频繁申请&#x2F;销毁资源和调度资源，将带来额外的消耗，可能会非常巨大</li>
<li>对资源无限申请缺少抑制手段，容易引发系统资源耗尽的风险</li>
<li>系统无法合理管理内部的资源分布，会降低系统的稳定性</li>
</ol>
<p>注意: 在 hotspot模型下，java的线程会一对一的映射内核线程，意味着每次申请和销毁都要转换到内核去操作，内核转换这个操作是十分消耗性能的，可能这个线程的时间还没 消耗+申请 加起来久</p>
<p>线程池的优势</p>
<ol>
<li>提供资源的利用性： 通过池化可以重复利用已创建的线程，空闲线程可以处理新提交的任务，从而降低了创建和销毁的资源开销</li>
<li>提高线程的管理性： 在一个线程中管理执行任务的线程，对线程可以进行统一的创建，销毁以及监控等，对线程数做控制，防止线程无限制创建，避免线程数量的急剧上升而导致CPU过度等问题</li>
<li>提高程序的响应性：提交任务后，有空闲线程可以直接去执行任务，无需新建</li>
<li>提高系统的可扩展性：利用线程池可以更好的扩展一些功能，比如定时线程池可以实现系统的定时任务</li>
<li>懒惰性：先创建线程池的时候不会有任何线程，要先有第一个任务进来，才会创建线程</li>
</ol>
<h2 id="线程池状态"><a href="#线程池状态" class="headerlink" title="线程池状态"></a>线程池状态</h2><p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<p>ctl: 对线程池的运行状态 和 线程池中有效线程的数量进行控制的一个字段</p>
<p>高3位保存runState，低29位保存workerCount，两个变量之间互不干扰。<strong>用一个变量去存储两个值，可避免在做相关决策时，出现不一致的情况，不必为了维护两者的一致，而占用锁资源</strong>。通过阅读线程池源代码也可以发现，经常出现要同时判断线程池运行状态和线程数量的情况。线程池也提供了若干方法去供用户获得线程池当前的运行状态、线程个数。这里都使用的是位运算的方式，相比于基本运算，速度也会快很多 利用低29位表示线程池中线程数，高3位表示线程池的运行状态</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 得到线程池状态</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">runStateOf</span><span class="params">(<span class="type">int</span> c)</span>     &#123; <span class="keyword">return</span> c &amp; ~CAPACITY; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 得带工作线程数量</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">workerCountOf</span><span class="params">(<span class="type">int</span> c)</span>  &#123; <span class="keyword">return</span> c &amp; CAPACITY; &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">AtomicInteger</span> <span class="variable">ctl</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicInteger</span>(ctlOf(RUNNING, <span class="number">0</span>));</span><br></pre></td></tr></table></figure>

<p>提交优先级</p>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<p>执行优先级</p>
<p><img src="/images/posts/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E7%AC%94%E8%AE%B0/img-1.png" alt="线程池源码笔记"></p>
<h2 id="线程池参数"><a href="#线程池参数" class="headerlink" title="线程池参数"></a>线程池参数</h2><ol>
<li>corePoolSize: 核心线程数，线程池事先创建的线程，当有任务进来的时候第一时间去执行任务，空闲了也不会被回收，会一直重复这些线程</li>
</ol>
<p>默认情况下，即使是核⼼线程也只能在新任务到达时才创建和启动。但是我们可以使⽤ prestartCoreThread(启动⼀个核⼼线程)或prestartAllCoreThreads(启动全部核⼼线程)⽅法来提前启动 核⼼线程</p>
<ol>
<li>maximumPoolSize： 最大线程数，当核心线程数处于繁忙并且队列满了的时候，会向操作系统申请额外的线程来消费新进来的任务，允许在这个池子里面最大的线程数量</li>
<li>keepAliveTime： 除了核心线程之外的线程，当执行完任务后，存活空闲下来的时间，超过这个时间就会被回收</li>
<li>unit： 空闲时间的单位，默认是秒</li>
<li>workQueue：工作队列,阻塞队列.分为有界&#x2F;无界,工作完的线程(不管是core,还是max线程)都会去拉取队列的任务.</li>
<li>threadFactory：线程工厂，设置了一些标识，比如名字</li>
<li>RejectedExecutionHandler：拒绝策略</li>
</ol>
<ul>
<li>默认为Abort, 直接抛出异常（AbortPolicy）</li>
<li>什么都不做，也不抛出一场（CallerRunsPolicy）</li>
<li>抛出头部的任务，加入新提交的任务（DiscardOldestPolicy）</li>
<li>谁提交的谁执行（DiscardPolicy）</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">ThreadPoolExecutor</span><span class="params">(<span class="type">int</span> corePoolSize,</span></span><br><span class="line"><span class="params">                              <span class="type">int</span> maximumPoolSize,</span></span><br><span class="line"><span class="params">                              <span class="type">long</span> keepAliveTime,</span></span><br><span class="line"><span class="params">                              TimeUnit unit,</span></span><br><span class="line"><span class="params">                              BlockingQueue&lt;Runnable&gt; workQueue,</span></span><br><span class="line"><span class="params">                              ThreadFactory threadFactory,</span></span><br><span class="line"><span class="params">                              RejectedExecutionHandler handler)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (corePoolSize &lt; <span class="number">0</span> ||</span><br><span class="line">            maximumPoolSize &lt;= <span class="number">0</span> ||</span><br><span class="line">            maximumPoolSize &lt; corePoolSize ||</span><br><span class="line">            keepAliveTime &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>();</span><br><span class="line">        <span class="keyword">if</span> (workQueue == <span class="literal">null</span> || threadFactory == <span class="literal">null</span> || handler == <span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">        <span class="built_in">this</span>.corePoolSize = corePoolSize;</span><br><span class="line">        <span class="built_in">this</span>.maximumPoolSize = maximumPoolSize;</span><br><span class="line">        <span class="built_in">this</span>.workQueue = workQueue;</span><br><span class="line">        <span class="built_in">this</span>.keepAliveTime = unit.toNanos(keepAliveTime);</span><br><span class="line">        <span class="built_in">this</span>.threadFactory = threadFactory;</span><br><span class="line">        <span class="built_in">this</span>.handler = handler;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="线程池工作流程"><a href="#线程池工作流程" class="headerlink" title="线程池工作流程"></a>线程池工作流程</h2><ol>
<li>如果当前工作线程数量小于核心线程数量，执行器总是优先 创建一个任务线程，而不是从线程队列中获取一个空闲线程</li>
<li>如果线程池中总的任务数量大于核心线程池数量，新接受的任务将会加入阻塞队列中，一直到阻塞队列已满。在核心线程池数量已经用完，阻塞队列没有满的场景下，线程池不会为新任务创建一个新线程</li>
<li>当完成一个任务执行完时，执行器总是优先从阻塞队列中获取下一个任务，并开始执行，一直到阻塞队列为空，其中所有的缓存任务被取光</li>
<li>在核心线程池数量已经用完、阻塞队列也已经满了的场景下，如果线程池接收到新的任务，将会为新任务创建一个线程（非核心线程），并且立即开始执行新任务</li>
<li>在核心线程都用完、阻塞队列已满的情况下，一直会创建新线程去执行新任务，直到池内的线程总数超出maximumPoolSize。如果线程池的线程总数超过maximumPoolSize，线程池就会拒绝接收任务， 当新任务过来时，会为新任务执行拒绝策略</li>
</ol>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<p><img src="/images/posts/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E7%AC%94%E8%AE%B0/img-2.png" alt="线程池源码笔记"></p>
<h2 id="ThreadPoolExecutor执行任务"><a href="#ThreadPoolExecutor执行任务" class="headerlink" title="ThreadPoolExecutor执行任务"></a><strong>ThreadPoolExecutor执行任务</strong></h2><p>有 submit 和 execute 二种方式</p>
<h3 id="submit-方法"><a href="#submit-方法" class="headerlink" title="submit 方法"></a>submit 方法</h3><p>使用 FutureTask 包裹一层 Runnable，返回 FutureTask 回去，中间调用 execute 方法</p>
<h3 id="execute方法"><a href="#execute方法" class="headerlink" title="execute方法"></a>execute方法</h3><ol>
<li>如果当前正在执行的worker数量比corePoolSize小，直接创建一个新的worker执行任务，调用addWorker方法</li>
<li>如果当前正在执行的worker数量大于等于corePoolSize，将任务放到阻塞队列里，等待空闲线程来执行</li>
<li>若队列的任务数达到上限，且当前运行线程数小于 maximumPoolSize ，任务入队列失败，新创建worker执行任务</li>
<li>若创建线程也失败(队列任务达到上限 且 当前线程数达到了 maximumPoolSize)，对于新加入的任务，就会调用reject进行拒绝策略</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Future&lt;?&gt; submit(Runnable task) &#123;</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="literal">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">        RunnableFuture&lt;Void&gt; ftask = newTaskFor(task, <span class="literal">null</span>);</span><br><span class="line">        execute(ftask);</span><br><span class="line">        <span class="keyword">return</span> ftask;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">(Runnable command)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (command == <span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * Proceed in 3 steps:</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * 1. If fewer than corePoolSize threads are running, try to</span></span><br><span class="line"><span class="comment">         * start a new thread with the given command as its first</span></span><br><span class="line"><span class="comment">         * task.  The call to addWorker atomically checks runState and</span></span><br><span class="line"><span class="comment">         * workerCount, and so prevents false alarms that would add</span></span><br><span class="line"><span class="comment">         * threads when it shouldn&#x27;t, by returning false.</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * 2. If a task can be successfully queued, then we still need</span></span><br><span class="line"><span class="comment">         * to double-check whether we should have added a thread</span></span><br><span class="line"><span class="comment">         * (because existing ones died since last checking) or that</span></span><br><span class="line"><span class="comment">         * the pool shut down since entry into this method. So we</span></span><br><span class="line"><span class="comment">         * recheck state and if necessary roll back the enqueuing if</span></span><br><span class="line"><span class="comment">         * stopped, or start a new thread if there are none.</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * 3. If we cannot queue task, then we try to add a new</span></span><br><span class="line"><span class="comment">         * thread.  If it fails, we know we are shut down or saturated</span></span><br><span class="line"><span class="comment">         * and so reject the task.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">        <span class="comment">// 第一步，满足线程池中的线程大小比核心线程数小</span></span><br><span class="line">        <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">            <span class="comment">// addWorker 返回ture 表示添加运行线程成功。</span></span><br><span class="line">            <span class="comment">// 此处的第二个参数true表示使用核心线程数</span></span><br><span class="line">            <span class="keyword">if</span> (addWorker(command, <span class="literal">true</span>))</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            <span class="comment">// 如果核心线程中没有添加成功的话，再次获取当前线程池的状态</span></span><br><span class="line">            c = ctl.get();</span><br><span class="line">        &#125;</span><br><span class="line"><span class="comment">// 第二步，线程池还在运行，但是线程池数比核心线程数大，阻塞队列还没满，放入阻塞队列中</span></span><br><span class="line">        <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">recheck</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">            <span class="comment">// 判断线程池是不是突然关闭了，如果关闭则从队列中移除</span></span><br><span class="line">            <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">                <span class="comment">// 执行对应等拒绝策略</span></span><br><span class="line">                reject(command);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">                <span class="comment">// 当前线程数为空，使用最大线程数创建一个线程</span></span><br><span class="line">                addWorker(<span class="literal">null</span>, <span class="literal">false</span>);</span><br><span class="line">            <span class="keyword">else</span> : 仅仅加入阻塞队列中</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//第三步，当前线程为非运行状态并且尝试新建线程，若失败则执行拒绝策略</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="literal">false</span>))</span><br><span class="line">            reject(command);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="addWorker方法"><a href="#addWorker方法" class="headerlink" title="addWorker方法"></a>addWorker方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// firstTask 需要运行的任务 ， core 代表是否使用核心线程数</span></span><br><span class="line"><span class="comment">// 返回值： true表示新任务被接受并且执行了;反之false</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">addWorker</span><span class="params">(Runnable firstTask, <span class="type">boolean</span> core)</span> &#123;</span><br><span class="line">        retry:</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">            <span class="comment">// 获取当前线程池状态</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">rs</span> <span class="operator">=</span> runStateOf(c);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Check if queue empty only if necessary.</span></span><br><span class="line"><span class="comment">// 1.线程池不在running的状态并且状态是 STOP,TIDYING,TERMINATED中一种</span></span><br><span class="line"><span class="comment">// 2.线程池不在running状态，线程池接受了新的任务</span></span><br><span class="line"><span class="comment">// 3.线程池不在running状态，阻塞队列为空。</span></span><br><span class="line"><span class="comment">// 满足这三点的情况下，拒绝执行任务</span></span><br><span class="line">            <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp;</span><br><span class="line">                ! (rs == SHUTDOWN &amp;&amp;</span><br><span class="line">                   firstTask == <span class="literal">null</span> &amp;&amp;</span><br><span class="line">                   ! workQueue.isEmpty()))</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">// 线程池数量</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">wc</span> <span class="operator">=</span> workerCountOf(c);</span><br><span class="line">                <span class="comment">// 1.如果线程池数量超过线程池最大容量</span></span><br><span class="line">                <span class="comment">// 2.或者根据 core 来判断是否超过 核心线程数或者最大线程数</span></span><br><span class="line">                <span class="comment">// 满足以上二点直接返回false</span></span><br><span class="line">                <span class="keyword">if</span> (wc &gt;= CAPACITY ||</span><br><span class="line">                    wc &gt;= (core ? corePoolSize : maximumPoolSize))</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">                <span class="comment">//cas操作线程池数量+1,如果称该就break跳出自旋</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndIncrementWorkerCount(c))</span><br><span class="line">                    <span class="keyword">break</span> retry;</span><br><span class="line">                <span class="comment">// 重新获取状态</span></span><br><span class="line">                c = ctl.get();  <span class="comment">// Re-read ctl</span></span><br><span class="line">                <span class="comment">// 如果状态变更了，重新循环操作</span></span><br><span class="line">                <span class="comment">//  runStateOf(c) 与 int rs = runStateOf(c) 最初进行对比</span></span><br><span class="line">                <span class="keyword">if</span> (runStateOf(c) != rs)</span><br><span class="line">                    <span class="keyword">continue</span> retry;</span><br><span class="line">                <span class="comment">// else CAS failed due to workerCount change; retry inner loop</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 到这里说明cas成功，线程池数量+1</span></span><br><span class="line">    <span class="comment">// 任务是否启动成功</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">workerStarted</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">	<span class="comment">// 任务是否添加成功</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">workerAdded</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="type">Worker</span> <span class="variable">w</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            w = <span class="keyword">new</span> <span class="title class_">Worker</span>(firstTask);</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> w.thread;</span><br><span class="line">            <span class="keyword">if</span> (t != <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="comment">// 得到线程池的可重入锁，lock防止并发</span></span><br><span class="line">                <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">                mainLock.lock();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// Recheck while holding lock.</span></span><br><span class="line">                    <span class="comment">// Back out on ThreadFactory failure or if</span></span><br><span class="line">                    <span class="comment">// shut down before lock acquired.</span></span><br><span class="line">                    <span class="comment">// 再次获取状态</span></span><br><span class="line">                    <span class="type">int</span> <span class="variable">rs</span> <span class="operator">=</span> runStateOf(ctl.get());</span><br><span class="line">                    <span class="comment">// 线程池是running状态 或者 线程池是shutdown并且任务是null</span></span><br><span class="line">                    <span class="keyword">if</span> (rs &lt; SHUTDOWN ||</span><br><span class="line">                        (rs == SHUTDOWN &amp;&amp; firstTask == <span class="literal">null</span>)) &#123;</span><br><span class="line">                        <span class="comment">// 线程是否存在，线程已经启动并且还没死掉</span></span><br><span class="line">                        <span class="keyword">if</span> (t.isAlive()) <span class="comment">// precheck that t is startable</span></span><br><span class="line">                            <span class="comment">// 启动并且还没死掉，抛出异常</span></span><br><span class="line">                            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalThreadStateException</span>();</span><br><span class="line">                        <span class="comment">//  添加worker到 HashSet 中</span></span><br><span class="line">                        workers.add(w);</span><br><span class="line">                        <span class="comment">// 线程池中的线程数</span></span><br><span class="line">                        <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> workers.size();</span><br><span class="line">                        <span class="comment">// 如果线程数比最大还大，就赋值给最大值是线程数</span></span><br><span class="line">                        <span class="keyword">if</span> (s &gt; largestPoolSize)</span><br><span class="line">                            largestPoolSize = s;</span><br><span class="line">                        <span class="comment">// 标识任务已经添加成功</span></span><br><span class="line">                        workerAdded = <span class="literal">true</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    <span class="comment">// 释放锁</span></span><br><span class="line">                    mainLock.unlock();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 添加成功</span></span><br><span class="line">                <span class="keyword">if</span> (workerAdded) &#123;</span><br><span class="line">                    <span class="comment">// 启动线程，调用thread的start方法</span></span><br><span class="line">                    t.start();</span><br><span class="line">                    <span class="comment">// 标识已经启动的状态</span></span><br><span class="line">                    workerStarted = <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 如果已经启动状态不是true，就是添加失败</span></span><br><span class="line">            <span class="keyword">if</span> (! workerStarted)</span><br><span class="line">                <span class="comment">// 从 worker中剔除</span></span><br><span class="line">                <span class="comment">// ctl 使用cas减1</span></span><br><span class="line">                addWorkerFailed(w);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> workerStarted;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="runWorker方法"><a href="#runWorker方法" class="headerlink" title="runWorker方法"></a>runWorker方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">runWorker</span><span class="params">(Worker w)</span> &#123;</span><br><span class="line">        <span class="type">Thread</span> <span class="variable">wt</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    	<span class="comment">// 得到worker的 任务task</span></span><br><span class="line">        <span class="type">Runnable</span> <span class="variable">task</span> <span class="operator">=</span> w.firstTask;</span><br><span class="line">    	<span class="comment">// worker任务置空</span></span><br><span class="line">        w.firstTask = <span class="literal">null</span>;</span><br><span class="line">        w.unlock(); <span class="comment">// allow interrupts</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">completedAbruptly</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 如果 worker中的任务不为空，继续知否，否则使用getTask获得任务。</span></span><br><span class="line">            <span class="comment">// 一直循环，除非得到的任务是空</span></span><br><span class="line">            <span class="keyword">while</span> (task != <span class="literal">null</span> || (task = getTask()) != <span class="literal">null</span>) &#123;</span><br><span class="line">                w.lock();</span><br><span class="line">                <span class="comment">// If pool is stopping, ensure thread is interrupted;</span></span><br><span class="line">                <span class="comment">// if not, ensure thread is not interrupted.  This</span></span><br><span class="line">                <span class="comment">// requires a recheck in second case to deal with</span></span><br><span class="line">                <span class="comment">// shutdownNow race while clearing interrupt</span></span><br><span class="line">                <span class="comment">// 1. 线程池已经处于STOP状态并且当前线程没有被中断，中断线程</span></span><br><span class="line">                <span class="comment">// 2. 如果线程池处于RUNNING或SHUTDOWN状态，并且当前线程已经被中断，</span></span><br><span class="line">                <span class="comment">// 重新检查一个线程池状态，如果处于STOP状态并且没有被中断，那么中断线程</span></span><br><span class="line">                <span class="keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||</span><br><span class="line">                     (Thread.interrupted() &amp;&amp;</span><br><span class="line">                      runStateAtLeast(ctl.get(), STOP))) &amp;&amp;</span><br><span class="line">                    !wt.isInterrupted())</span><br><span class="line">                    wt.interrupt();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 执行任务之前的钩子</span></span><br><span class="line">                    beforeExecute(wt, task);</span><br><span class="line">                    <span class="type">Throwable</span> <span class="variable">thrown</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="comment">// 真正开始执行任务，调用的是run方法，而不是start方法</span></span><br><span class="line">                        <span class="comment">// 这里run的时候可能会被中断，比如线程池调用SHUTDOWN方法</span></span><br><span class="line">                        task.run();</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (RuntimeException x) &#123;</span><br><span class="line">                        thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (Error x) &#123;</span><br><span class="line">                        thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (Throwable x) &#123;</span><br><span class="line">                        thrown = x; <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(x);</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        <span class="comment">// 任务结束后的钩子函数</span></span><br><span class="line">                        afterExecute(task, thrown);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    task = <span class="literal">null</span>;</span><br><span class="line">                    <span class="comment">// 记录执行任务的个数</span></span><br><span class="line">                    w.completedTasks++;</span><br><span class="line">                    w.unlock();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            completedAbruptly = <span class="literal">false</span>;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 回收worker方法</span></span><br><span class="line">            processWorkerExit(w, completedAbruptly);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="getTask方法"><a href="#getTask方法" class="headerlink" title="getTask方法"></a>getTask方法</h3><p>如果发生了下面事中的一个，那么worker需要被回收：</p>
<ol>
<li>worker个数比线程池最大的还要大</li>
<li>线程池处于STOP状态</li>
<li>线程池处于SHUTDOWN状态并且阻塞队列为空</li>
<li>使用超时时间从阻塞队列里拿数据，并且超时之后没有拿到数据（allowCoreThreadTimeOut || wc &gt; corePoolSize）</li>
</ol>
<p>如果 getTask 返回的是null，那说明阻塞队列已经没有任务并且当前调用getTask的Worker需要被回收，那么会调用processWorkerExit方法进行回收</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Runnable <span class="title function_">getTask</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">timedOut</span> <span class="operator">=</span> <span class="literal">false</span>; <span class="comment">// Did the last poll() time out?</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="comment">// c: ctl的值</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">            <span class="comment">// 高3位获取运行状态</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">rs</span> <span class="operator">=</span> runStateOf(c);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Check if queue empty only if necessary.</span></span><br><span class="line">            <span class="comment">// 如果线程池是SHUTDOWN状态并且阻塞队列是空的话，worker数量减一，直接返回null</span></span><br><span class="line">            <span class="comment">// SHUTDOWN 状态还是会处理阻塞队列任务，但是阻塞任务为空的话就结束了</span></span><br><span class="line">            <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp; (rs &gt;= STOP || workQueue.isEmpty())) &#123;</span><br><span class="line">                decrementWorkerCount();</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 低29位获取线程池数量</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">wc</span> <span class="operator">=</span> workerCountOf(c);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Are workers subject to culling?</span></span><br><span class="line">            <span class="comment">// allowCoreThreadTimeOut 核心线程是否可以超时</span></span><br><span class="line">            <span class="comment">// 线程池数量 大于 核心线程数</span></span><br><span class="line">            <span class="type">boolean</span> <span class="variable">timed</span> <span class="operator">=</span> allowCoreThreadTimeOut || wc &gt; corePoolSize;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 线程池数量 大于 最大线程数 或者 timed 和 timedOut(进来看是false) 是ture</span></span><br><span class="line">            <span class="comment">// 并且 线程池数量大于1 或者阻塞队列是空的</span></span><br><span class="line">            <span class="keyword">if</span> ((wc &gt; maximumPoolSize || (timed &amp;&amp; timedOut))</span><br><span class="line">                &amp;&amp; (wc &gt; <span class="number">1</span> || workQueue.isEmpty())) &#123;</span><br><span class="line">                <span class="comment">// workers数量减一，直接返回null</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndDecrementWorkerCount(c))</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 如该设置了超时，使用 poll 方法;否则take会一直阻塞队列进数据</span></span><br><span class="line">                <span class="type">Runnable</span> <span class="variable">r</span> <span class="operator">=</span> timed ?</span><br><span class="line">                    workQueue.poll(keepAliveTime, TimeUnit.NANOSECONDS) :</span><br><span class="line">                    workQueue.take();</span><br><span class="line">                <span class="keyword">if</span> (r != <span class="literal">null</span>)</span><br><span class="line">                    <span class="keyword">return</span> r;</span><br><span class="line">                timedOut = <span class="literal">true</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException retry) &#123;</span><br><span class="line">                <span class="comment">// 闲置 worker 被中断</span></span><br><span class="line">                timedOut = <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="processWorkerExit方法"><a href="#processWorkerExit方法" class="headerlink" title="processWorkerExit方法"></a><strong>processWorkerExit方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">processWorkerExit</span><span class="params">(Worker w, <span class="type">boolean</span> completedAbruptly)</span> &#123;</span><br><span class="line">        <span class="comment">// 如该worker没有正常结束流程调用 processWorkerExit的话，worker减一</span></span><br><span class="line">        <span class="comment">// 如果worker正常结束的话，在worker里就减一了</span></span><br><span class="line">        <span class="keyword">if</span> (completedAbruptly) <span class="comment">// If abrupt, then workerCount wasn&#x27;t adjusted</span></span><br><span class="line">            decrementWorkerCount();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取锁</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 记录总的完成任务数</span></span><br><span class="line">            completedTaskCount += w.completedTasks;</span><br><span class="line">            <span class="comment">// 线程池的worker集合删除掉需要回收的worker</span></span><br><span class="line">            workers.remove(w);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 释放锁</span></span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 尝试结束线程池</span></span><br><span class="line">        tryTerminate();</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">        <span class="comment">// 如果线程池处于RUNNING 或者 SHUTDOWN状态</span></span><br><span class="line">        <span class="keyword">if</span> (runStateLessThan(c, STOP)) &#123;</span><br><span class="line">            <span class="comment">// worker是否正常结束流程</span></span><br><span class="line">            <span class="keyword">if</span> (!completedAbruptly) &#123;</span><br><span class="line">                <span class="comment">// 如果有超时设置,就是0;否则就是核心线程数</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">min</span> <span class="operator">=</span> allowCoreThreadTimeOut ? <span class="number">0</span> : corePoolSize;</span><br><span class="line">                <span class="keyword">if</span> (min == <span class="number">0</span> &amp;&amp; ! workQueue.isEmpty())</span><br><span class="line">                    min = <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">if</span> (workerCountOf(c) &gt;= min)</span><br><span class="line">                    <span class="keyword">return</span>; <span class="comment">// replacement not needed</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 新开一个worker代理原先的worker</span></span><br><span class="line">            <span class="comment">// 1. 用户执行的任务发生了异常</span></span><br><span class="line">            <span class="comment">// 2. Worker数量比 核心线程数 大小要小</span></span><br><span class="line">            <span class="comment">// 3. 阻塞队列不为空但是没有没有任务worker在工作</span></span><br><span class="line">            addWorker(<span class="literal">null</span>, <span class="literal">false</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="tryTerminate方法"><a href="#tryTerminate方法" class="headerlink" title="tryTerminate方法"></a><strong>tryTerminate方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">tryTerminate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">            <span class="comment">// 1. 线程池还在运行，不能终止</span></span><br><span class="line">            <span class="comment">// 2. 线程池处于TIDYING或TERMINATED状态，说明已经在关闭了，不许继续处理</span></span><br><span class="line">            <span class="comment">// 3. 线程池处于 SHUTDOWN 状态并且阻塞队列不为空，</span></span><br><span class="line">            <span class="comment">// 这时候需要处理阻塞队列的任务，不能终止线程池</span></span><br><span class="line">            <span class="keyword">if</span> (isRunning(c) ||</span><br><span class="line">                runStateAtLeast(c, TIDYING) ||</span><br><span class="line">                (runStateOf(c) == SHUTDOWN &amp;&amp; ! workQueue.isEmpty()))</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            <span class="comment">// 这里说明线程池已经不在运行了，阻塞队列已经没有任务</span></span><br><span class="line">            <span class="comment">// 但是还要回收正在运行的worker</span></span><br><span class="line">            <span class="keyword">if</span> (workerCountOf(c) != <span class="number">0</span>) &#123; <span class="comment">// Eligible to terminate</span></span><br><span class="line">             </span><br><span class="line">                interruptIdleWorkers(ONLY_ONE);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 到这里，说明线程池已经不在运行，阻塞队列已经没有任务</span></span><br><span class="line">            <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">            mainLock.lock();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// cas操作，将线程池状态修改为TIDYING</span></span><br><span class="line">                <span class="keyword">if</span> (ctl.compareAndSet(c, ctlOf(TIDYING, <span class="number">0</span>))) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        terminated();</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        <span class="comment">// terminated调用完，线程池状态修改为TERMINATED</span></span><br><span class="line">                        ctl.set(ctlOf(TERMINATED, <span class="number">0</span>));</span><br><span class="line">                        termination.signalAll();</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                mainLock.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// else retry on failed CAS</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="ThreadPoolExecutor关闭"><a href="#ThreadPoolExecutor关闭" class="headerlink" title="ThreadPoolExecutor关闭"></a><strong>ThreadPoolExecutor关闭</strong></h2><p>线程池关闭主要有 shutdown 和 shutdownNow 方法 shutdown方法会更新状态到SHUTDOWN，不会影响阻塞队列里任务的执行，但是不会执行新进来的任务。同时也会回收闲置的worker shutdownNow方法会更新状态到STOP，会影响阻塞队列的任务执行，也不会执行新进来的任务</p>
<h3 id="shutdown方法"><a href="#shutdown方法" class="headerlink" title="shutdown方法"></a>shutdown方法</h3><p>shutdown 方法，关闭线程池，关闭之后阻塞队列里的任务不受影响，会继续被worker处理，但是新的任务不会被接受</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">shutdown</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 关闭时，进行上锁</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 检查关闭线程池的权限</span></span><br><span class="line">            checkShutdownAccess();</span><br><span class="line">            <span class="comment">// 把线程池状态更新到SHUTDOWN</span></span><br><span class="line">            advanceRunState(SHUTDOWN);</span><br><span class="line">            <span class="comment">// 中断闲置的worker</span></span><br><span class="line">            interruptIdleWorkers();</span><br><span class="line">            <span class="comment">// 钩子函数</span></span><br><span class="line">            onShutdown(); <span class="comment">// hook for ScheduledThreadPoolExecutor</span></span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 释放锁</span></span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 尝试结束线程池</span></span><br><span class="line">        tryTerminate();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="interruptIdleWorkers-方法"><a href="#interruptIdleWorkers-方法" class="headerlink" title="interruptIdleWorkers()方法"></a><strong>interruptIdleWorkers()方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">interruptIdleWorkers</span><span class="params">()</span> &#123;</span><br><span class="line">        interruptIdleWorkers(<span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="interruptIdleWorkers-boolean-onlyOne-方法"><a href="#interruptIdleWorkers-boolean-onlyOne-方法" class="headerlink" title="interruptIdleWorkers(boolean onlyOne)方法"></a><strong>interruptIdleWorkers(boolean onlyOne)方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">interruptIdleWorkers</span><span class="params">(<span class="type">boolean</span> onlyOne)</span> &#123;</span><br><span class="line">        <span class="comment">// 上锁，防止并发</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">for</span> (Worker w : workers) &#123;</span><br><span class="line">                <span class="comment">// 获取到worker的线程</span></span><br><span class="line">                <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> w.thread;</span><br><span class="line">                <span class="comment">// 线程没有被中断 并且 worker获取锁</span></span><br><span class="line">                <span class="keyword">if</span> (!t.isInterrupted() &amp;&amp; w.tryLock()) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        t.interrupt();</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (SecurityException ignore) &#123;</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        <span class="comment">// worker释放锁</span></span><br><span class="line">                        w.unlock();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (onlyOne)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="shutdownNow方法"><a href="#shutdownNow方法" class="headerlink" title="shutdownNow方法"></a><strong>shutdownNow方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> List&lt;Runnable&gt; <span class="title function_">shutdownNow</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;Runnable&gt; tasks;</span><br><span class="line">        <span class="comment">// 上锁，防止并发</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 检查关闭线程池的权限</span></span><br><span class="line">            checkShutdownAccess();</span><br><span class="line">            <span class="comment">// 把线程池的状态更新到STOP</span></span><br><span class="line">            advanceRunState(STOP);</span><br><span class="line">            <span class="comment">// 中断worker运行</span></span><br><span class="line">            interruptWorkers();</span><br><span class="line">            tasks = drainQueue();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 尝试关闭线程池</span></span><br><span class="line">        tryTerminate();</span><br><span class="line">        <span class="keyword">return</span> tasks;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="interruptWorkers-方法"><a href="#interruptWorkers-方法" class="headerlink" title="interruptWorkers() 方法"></a><strong>interruptWorkers() 方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">interruptWorkers</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="built_in">this</span>.mainLock;</span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (Worker w : workers)</span><br><span class="line">                <span class="comment">// 中断worker执行</span></span><br><span class="line">                w.interruptIfStarted();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="interruptIfStarted-方法"><a href="#interruptIfStarted-方法" class="headerlink" title="interruptIfStarted()方法"></a><strong>interruptIfStarted()方法</strong></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">interruptIfStarted</span><span class="params">()</span> &#123;</span><br><span class="line">            Thread t;</span><br><span class="line">            <span class="comment">// getState 代表 worker是否持有锁 并且 没有被中断，那就中断worker</span></span><br><span class="line">            <span class="keyword">if</span> (getState() &gt;= <span class="number">0</span> &amp;&amp; (t = thread) != <span class="literal">null</span> &amp;&amp; !t.isInterrupted()) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    t.interrupt();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SecurityException ignore) &#123;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h2 id="常用线程池"><a href="#常用线程池" class="headerlink" title="常用线程池"></a>常用线程池</h2><h3 id="newSingleThreadPool"><a href="#newSingleThreadPool" class="headerlink" title="newSingleThreadPool"></a>newSingleThreadPool</h3><p>core&#x3D;1,单线程，max&#x3D;1,最大只有一条线程，keepAliveTime&#x3D;0是不回收的，无界队列，一直复用同一个线程，好处是不用一直创建线程，一条线程一直复用 如果异常了，会new一条新线程，能保证顺序执行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">newSingleThreadExecutor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">FinalizableDelegatedExecutorService</span></span><br><span class="line">            (<span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(<span class="number">1</span>, <span class="number">1</span>,</span><br><span class="line">                                    <span class="number">0L</span>, TimeUnit.MILLISECONDS,</span><br><span class="line">                                    <span class="keyword">new</span> <span class="title class_">LinkedBlockingQueue</span>&lt;Runnable&gt;()));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="newCachedThreadPool"><a href="#newCachedThreadPool" class="headerlink" title="newCachedThreadPool"></a>newCachedThreadPool</h3><p>core&#x3D;0,max是Integer的最大值，阻塞队列是一个 SynchronousQueue（容量为0的队列，不能放任何东西，并且是一个阻塞队列，作用阻塞任务，让缓存任务队列无效，所有的任务都是走非核心线程，效率非常高），没有池化思想了，一个任务创建一个线程，回收时间为 60 s，当没任务执行&#x2F;提交的时候，线程池内无线程</p>
<ul>
<li>时效性高，新任务进来就会有线程去执行任务</li>
<li>不用管理线程的回收，因为线程池管理了线程的回收(keepAliveTime的时间为60s)</li>
<li>资源损耗严重</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">newCachedThreadPool</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(<span class="number">0</span>, Integer.MAX_VALUE,</span><br><span class="line">                                      <span class="number">60L</span>, TimeUnit.SECONDS,</span><br><span class="line">                                      <span class="keyword">new</span> <span class="title class_">SynchronousQueue</span>&lt;Runnable&gt;());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="newFixedThreadPool"><a href="#newFixedThreadPool" class="headerlink" title="newFixedThreadPool"></a>newFixedThreadPool</h3><p>core 和 max 的值都是相等，不会额外的创建新线程，keepAliveTime&#x3D;0,也不会回收空闲线程，无界队列，任务都由核心线程数去执行，保证线程的复用 特点: 适用于,已知任务数量,但是比较耗时的任务 注意: newFixedThreadPool的阻塞队列大小是没有大小限制的，如果队列堆积数据太多会造成资源消耗</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">newFixedThreadPool</span><span class="params">(<span class="type">int</span> nThreads)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(nThreads, nThreads,</span><br><span class="line">                                      <span class="number">0L</span>, TimeUnit.MILLISECONDS,</span><br><span class="line">                                      <span class="keyword">new</span> <span class="title class_">LinkedBlockingQueue</span>&lt;Runnable&gt;());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="newScheduledThreadPool"><a href="#newScheduledThreadPool" class="headerlink" title="newScheduledThreadPool"></a>newScheduledThreadPool</h3><p>线程池支持定时任务及周期性执行任务，创建一个 corePoolSize 传入，最大线程数为整形的最大数的线程池</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ScheduledExecutorService <span class="title function_">newScheduledThreadPool</span><span class="params">(<span class="type">int</span> corePoolSize)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ScheduledThreadPoolExecutor</span>(corePoolSize);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ScheduledThreadPoolExecutor</span><span class="params">(<span class="type">int</span> corePoolSize)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(corePoolSize, Integer.MAX_VALUE, <span class="number">0</span>, NANOSECONDS,</span><br><span class="line">              <span class="keyword">new</span> <span class="title class_">DelayedWorkQueue</span>());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="newWorkStealingPool"><a href="#newWorkStealingPool" class="headerlink" title="newWorkStealingPool"></a>newWorkStealingPool</h3><p>建持有足够线程的线程池来达到快速运算的目的，在内部通过使用多个 队列 来减少各个线程调度产生的竞争。这里所说的有足够的线程 JDK 根据 当前线程的运行需求向操作系统申请足够的线程，以保障线程的快速执行，并很大程度 地使用系统资源，提高并发计算的效率，省去用户根据 CPU 资源估算并行度的过程 然，如果开发者想自己定义线程的并发数，则也可以将其作为参数传入</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">newWorkStealingPool</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ForkJoinPool</span></span><br><span class="line">            (Runtime.getRuntime().availableProcessors(),</span><br><span class="line">             ForkJoinPool.defaultForkJoinWorkerThreadFactory,</span><br><span class="line">             <span class="literal">null</span>, <span class="literal">true</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="动态线程池"><a href="#动态线程池" class="headerlink" title="动态线程池"></a>动态线程池</h2><p>动态化线程池的核心设计包括如下：</p>
<ol>
<li>简化线程池的配置：线程池构造参数有8个，但是最核心的是3个，corePoolSize,maximumPoolSize,workQueue,它们最大程度的决定了线程池的任务分配和线程的分配策略。考虑到在实际应用中我们获取并发行的场景</li>
</ol>
<ul>
<li>并行执行子任务，提高响应速度。这种情况下应该使用同步队列，没有什么任务应该被缓下来，而是应该立即执行</li>
<li>并行执行大批次任务，提高吞吐量。这种情况下，使用使用有界队列，使用队列取缓冲大批量的任务，队列容量必须声明，防止任务无限制堆积。所以线程池只需要提供这三个关键参数的配置，并且提供两种队列的选择，就可以满足绝大多数的业务需求，Less is More</li>
</ul>
<ol>
<li>参数可动态的修改：为了解决参数不好配，修改参数成本高等问题，在Java线程池留有高扩展性的基础上，封装线程池，允许线程池监听同步外部的消息，根据消息进行修改配置。将线程池的配置放置在平台侧，允许开发同学简单的查看、修改线程池配置。</li>
<li>增加线程池监控，对某事物缺乏状态的观测，就对其改进无从下手。在线程池执行任务的生命周期添加监控能力，帮助开发同学了解线程池状态</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka生产者发送消息记录</title>
    <url>//article/kafka_problems1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 题记</h1><p> 在公司使用kafka也有一段时间了,都是停留在最简单的使用上. 配置好ip&#x2F;consumer&#x2F;topic等信息,然后就有数据就开始消费数据(也就是开始处理业务逻辑).</p>
<p> 然后这里记录下,使用kafkaTemplate 来发送消息.</p>
<p> 为什么要记录呢？ 因为我发现，存在丢失数据的情况哈哈哈哈。</p>
<p> 怪就怪在只有一个kafka,然后宕机,就丢了.</p>
<p> 但是是可以在发送端用回调来记录数据的.</p>
<h1 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h1><p> 话不多少, 可以上代码.</p>
<p> 这里主要看 future.addCallback() 这个方法,这里可以看到,如果是是失败了的话,就会走到 onFailure方法中来,这里你就对data数据进行处理,并且throwable也是有的,你也可以自定义异常抛出来,交给上游处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yang.bootkafkacase.kafka;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.slf4j.Logger;</span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Value;</span><br><span class="line"><span class="keyword">import</span> org.springframework.kafka.core.KafkaTemplate;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"><span class="keyword">import</span> org.springframework.util.concurrent.ListenableFuture;</span><br><span class="line"><span class="keyword">import</span> org.springframework.util.concurrent.ListenableFutureCallback;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by Yang on 2020/10/12 22:14</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 生产数据: 失败可以走失败的回调,成功可以走成功的回调.</span></span><br><span class="line"><span class="comment"> * 如果下游的kafka是宕机的话,没有该回调的话,数据就有可能会丢失,这对于数据严格的情况,肯定是不容许发生的现象.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinGroupProvider</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">LOGGER</span> <span class="operator">=</span> LoggerFactory.getLogger(GavinGroupProvider.class);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value(&quot;$&#123;gavin.test.topic:gavin.test.topic&#125;&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String topicName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> KafkaTemplate kafkaTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sendMessage</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">data</span> <span class="operator">=</span> <span class="string">&quot;GavinYang is big brother.&quot;</span>;</span><br><span class="line">        <span class="type">ListenableFuture</span> <span class="variable">future</span> <span class="operator">=</span> kafkaTemplate.send(topicName, data);</span><br><span class="line">        future.addCallback(<span class="keyword">new</span> <span class="title class_">ListenableFutureCallback</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onFailure</span><span class="params">(Throwable throwable)</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 如果失败了</span></span><br><span class="line">                LOGGER.error(<span class="string">&quot;The failed data is : &#123;&#125; &quot;</span> , data);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onSuccess</span><span class="params">(Object data)</span> &#123;</span><br><span class="line"></span><br><span class="line">                LOGGER.debug(<span class="string">&quot;The success data is : &#123;&#125; &quot;</span> , data);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还有这种的话,就是再次调用get方法, 如果发送失败的话，调用get方法就会抛出异常来，当然我这里是丢出去了一个总的Exception, 这里也可以自己catch处理，也可以抛给调用方法的地方处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sendMessage</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">data</span> <span class="operator">=</span> <span class="string">&quot;GavinYang is big brother.&quot;</span>;</span><br><span class="line">    <span class="type">ListenableFuture</span> <span class="variable">future</span> <span class="operator">=</span> kafkaTemplate.send(topicName, data);</span><br><span class="line"></span><br><span class="line">    future.get();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p> 当时因为说是丢数据那段时间,kafka宕机了,然后关掉kafka，直接调用发送，再重启kafka，发现果然在kafka里面是没有数据的。</p>
<p> 然后使用了这种后,可以根据自己的业务需开看,是抛出异常出去,还是在 onFailure 中进行异常处理操作.</p>
<p>个人感觉: 感觉像这种kafka的中间件,在往其发送数据的时候,特别是单个节点的时候,一定要考虑下如果该节点宕机了的话,其会产生什么问题? 如果看到问题现象，该怎么处理.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Kafka</category>
      </categories>
      <tags>
        <tag>问题记录</tag>
        <tag>kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka消费者数据配置问题记录</title>
    <url>//article/kafka_problems2/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-问题"><a href="#📝-问题" class="headerlink" title="📝 问题"></a>📝 <strong>问题</strong></h1><p>最近测试一些bug, 因为有些数据到canal, 然后canal写到kafka, 我们自己的服务消费kafka的数据.</p>
<p>由于一些参数的配置,导致kafka频繁的出现这个问题. 然后你如果这些关键字去百度的话,那么这个问题,你估计是很那处理了. 因为百度出来的处理方式是牛鬼蛇神,各种花样都有,但是就是没有一个有用的.</p>
<p>最后还是去 github上看到一个人提的这个问题,然后跟着他弄下,就ok了. 但是具体的参数配置,还是得仔细去测试下.不然总是会出现各种你想不到的情况发生,奇奇怪怪的问题.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">35</span>:<span class="number">05.450</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=<span class="number">126</span>)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">35</span>:<span class="number">35.707</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">36</span>:<span class="number">05.969</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">36</span>:<span class="number">36.202</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">37</span>:<span class="number">06.452</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">37</span>:<span class="number">36.713</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br><span class="line"><span class="number">2020</span>-<span class="number">06</span>-<span class="number">30</span> <span class="number">14</span>:<span class="number">38</span>:<span class="number">06.946</span> INFO <span class="number">26662</span> --- [ntainer#<span class="number">0</span>-<span class="number">0</span>-C-<span class="number">1</span>] o.a.kafka.clients.FetchSessionHandler : [Consumer clientId=consumer-<span class="number">2</span>, groupId=gavin_localhost] Error sending fetch <span class="title function_">request</span> <span class="params">(sessionId=<span class="number">1083316257</span>, epoch=INITIAL)</span> to node <span class="number">0</span>: org.apache.kafka.common.errors.DisconnectException.</span><br></pre></td></tr></table></figure>

<h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><p>这是最初的配置. 然后kafka那头,就有蛮多数据堆积在那里的. 接着就出现了上面的那个error, 但是没有抛异常出来,就是log里面打印了info的信息,说里面有error.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">consumerConfigs</span><span class="params">(String groupId)</span> &#123;    Map&lt;String, Object&gt; props = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(<span class="number">16</span>);    props.put(ConsumerConfig.GROUP_ID_CONFIG,groupId);    props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, <span class="string">&quot;127.0.0.1:9092&quot;</span>);    props.put(ConsumerConfig.ENABLE_AUTO_COMMIT_CONFIG, <span class="literal">false</span>);    props.put(ConsumerConfig.SESSION_TIMEOUT_MS_CONFIG, <span class="number">30000</span>);    props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);    props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);    props.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG,<span class="string">&quot;earliest&quot;</span>);     props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG,<span class="number">100</span>);    props.put(ConsumerConfig.MAX_PARTITION_FETCH_BYTES_CONFIG,<span class="number">31457280</span>);    props.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG,<span class="number">300000</span>);    <span class="keyword">return</span> props;&#125;</span><br></pre></td></tr></table></figure>

<p>处理方案: [嵌入对象]</p>
<p>可以看着别人的配置跟着配置下,但是最好要自己理解下你配置这些参数的值大小,代表着什么意思. 因为你获取这些数据的多少,是直接与你的业务代码和服务器内存进行挂钩的.</p>
<p>比如你的业务代码,拿到了kafka的数据,然后根据kafka的数据去db中query(假设你查询的数据很多), 那么就一定要注意内存的使用等问题了.</p>
<p>最后的配置样子. 当然了,这里只是我进行测试一些bug的配置,具体的上线代码配置,肯定还要再仔细看下.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">consumerConfigs</span><span class="params">(String groupId)</span> &#123;</span><br><span class="line">        Map&lt;String, Object&gt; props = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(<span class="number">16</span>);</span><br><span class="line">        props.put(ConsumerConfig.GROUP_ID_CONFIG,groupId);</span><br><span class="line">        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, <span class="string">&quot;127.0.0.1:9092&quot;</span>);</span><br><span class="line">        props.put(ConsumerConfig.ENABLE_AUTO_COMMIT_CONFIG, <span class="literal">false</span>);</span><br><span class="line">        props.put(ConsumerConfig.SESSION_TIMEOUT_MS_CONFIG, <span class="number">30000</span>);</span><br><span class="line">        props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);</span><br><span class="line">        props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);</span><br><span class="line">        props.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG,<span class="string">&quot;earliest&quot;</span>);</span><br><span class="line">        props.put(ConsumerConfig.FETCH_MAX_WAIT_MS_CONFIG,<span class="number">60000</span>);</span><br><span class="line">        props.put(ConsumerConfig.REQUEST_TIMEOUT_MS_CONFIG,<span class="number">70000</span>);</span><br><span class="line">        props.put(<span class="string">&quot;spring.kafka.listener.poll-timeout&quot;</span>,<span class="number">60000</span>);</span><br><span class="line"></span><br><span class="line">        props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG,<span class="number">100</span>);</span><br><span class="line">        props.put(ConsumerConfig.MAX_PARTITION_FETCH_BYTES_CONFIG,<span class="number">31457280</span>);</span><br><span class="line">        props.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG,<span class="number">300000</span>);</span><br><span class="line">        <span class="keyword">return</span> props;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>可以看到类似kafka这种中间件,其配置的参数还是相当的重要,所以弄明白是很重要的. 不然你配置的参数,可能刚刚好适应你的电脑,但是对服务器就不适应了(这样的话,不排除内存使用的问题). 还是就是数据量的不同, 分别是kafka没有堆积数据,有数据过来就消费,这样的话,肯定是没啥问题的. 如果kafka那头堆积的几百万呢? 那你的这个配置会不会问题? 消费了数据后,有去db做查询的操作,查询获取出来的数据多少? 这些都是需要注意的.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Kafka</category>
      </categories>
      <tags>
        <tag>问题记录</tag>
        <tag>kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>linux上压缩文件</title>
    <url>//article/linux_compress/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Linux上压缩以及解压文件</p>
</blockquote>
<h1 id="📝-压缩-tar-文件"><a href="#📝-压缩-tar-文件" class="headerlink" title="📝 压缩 .tar 文件"></a>📝 压缩 .tar 文件</h1><p>filename: 替换成你需要的名字</p>
<p>&#x2F;path&#x2F;to&#x2F;directory-or-file:  替换成你要压缩的文件夹路径</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -cvf filename.tar /path/to/directory-or-file</span><br></pre></td></tr></table></figure>

<h1 id="📝-压缩-tar-gz-文件"><a href="#📝-压缩-tar-gz-文件" class="headerlink" title="📝 压缩 .tar.gz 文件"></a>📝 压缩 .tar.gz 文件</h1><p>filename: 替换成你需要的名字</p>
<p>&#x2F;path&#x2F;to&#x2F;directory-or-file:  替换成你要压缩的文件夹路径</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -czvf filename.tar.gz /path/to/directory-or-file</span><br></pre></td></tr></table></figure>

<h1 id="📝-压缩-zip-文件"><a href="#📝-压缩-zip-文件" class="headerlink" title="📝 压缩 .zip 文件"></a>📝 压缩 .zip 文件</h1><p>filename: 替换成你需要的名字</p>
<p>&#x2F;path&#x2F;to&#x2F;directory-or-file:  替换成你要压缩的文件夹路径</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zip -r filename.zip /path/to/directory-or-file</span><br></pre></td></tr></table></figure>

<h1 id="📝-重启nginx"><a href="#📝-重启nginx" class="headerlink" title="📝 重启nginx"></a>📝 重启nginx</h1><p>注意此处的 &#x2F;usr&#x2F;local&#x2F;nginx-web&#x2F;sbin 以及  .&#x2F;nginx-web  ; 请根据你实际的 nginx 路径 以及 指令来进行重启</p>
<p>如何查看 nginx 的进程和路径：</p>
<p>查看进程:  ps -ef | grep nginx</p>
<p>查看路径： whereis nginx</p>
<p>查看历史记录nginx:  history | grep nginx</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/local/nginx-web/sbin</span><br><span class="line">./nginx-web -p ../</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>mybatis中config-xml代码阅读</title>
    <url>//article/mybatis_config_xml_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 <strong>题记</strong></h1><p>对于配置文件的解析, 还是相对比较好理解的, 就是读取配置文件, 然后在代码需要的地方给使用到.</p>
<p>这里,可以扩展下, Spring &#x2F; SpringBoot 等是怎么读取配置文件呢 ? 并且配置文件还是有 xml &#x2F; properties&#x2F;yaml 等格式的 ， 其读取代码是怎么写的 ? 然后基于 阿波罗(携程开源) 的配置中心 , 其实现配置又是怎么实现的呢 ? 然后这里，看了 Mybatis 读取配置文件, 后续再出 Spring 配置文件的时候，如果二者读取配置进行对比, 你个人更倾向使用代码呢 ?</p>
<p>所以,这里就开启读取 Mybatis 是如何解析配置文件的操作.</p>
<h1 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h1><p>这里的配置文件解读,是根据 MyBatis官网来一步一步的解析阅读. 如果有官网没有涉及到的,发现了也会在后续加上去的. 解析多行代码, 才能理解 何为优秀.</p>
<h2 id="标签一-properties"><a href="#标签一-properties" class="headerlink" title="标签一 : properties"></a><strong>标签一 : properties</strong></h2><p>org.apache.ibatis.builder.xml.XMLConfigBuilder#parseConfiguration —&gt; propertiesElement(root.evalNode(“properties”)) 方法中来</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里传入进来的 XNode 的值,就是我们写的 properties 标签.</span></span><br><span class="line"><span class="comment">// 可以看到 XNode的属性,name标签的名字,attributes就是key/value属性</span></span><br><span class="line"><span class="comment">// 比如这里: key 就是 resource , value 就是 ./db.properties.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">propertiesElement</span><span class="params">(XNode context)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (context != <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// 这里调用的node.getChildNodes(),如果有点话,会遍历挨个解析,最后封装成为key/value结构.      </span></span><br><span class="line">    <span class="type">Properties</span> <span class="variable">defaults</span> <span class="operator">=</span> context.getChildrenAsProperties();</span><br><span class="line"><span class="comment">// 获取 resource / url 二者的值.      </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">resource</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resource&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;url&quot;</span>);</span><br><span class="line"><span class="comment">// 如果二者都是null,就会抛出异常来.      </span></span><br><span class="line">    <span class="keyword">if</span> (resource != <span class="literal">null</span> &amp;&amp; url != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;The properties element cannot specify both a URL and a resource based property file reference.  Please specify one or the other.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line"> <span class="comment">// 这里先处理resource,再处理url,也就是有可能url会覆盖掉resource的内容.</span></span><br><span class="line"> <span class="comment">// 二者读取的方式不一样,前者是根据 resource开始读,url是根据绝对路径开始读.</span></span><br><span class="line"> <span class="comment">// 最后 defaults 里面放入的全部是 key/value 对应的键值对</span></span><br><span class="line"> <span class="comment">// 也就是db.properties中的 key / value 相对应i起来.     </span></span><br><span class="line">    <span class="keyword">if</span> (resource != <span class="literal">null</span>) &#123;</span><br><span class="line">      defaults.putAll(Resources.getResourceAsProperties(resource));</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (url != <span class="literal">null</span>) &#123;</span><br><span class="line">      defaults.putAll(Resources.getUrlAsProperties(url));</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 这里看的是 xml 里面是不是直接有 porperties 配置.     </span></span><br><span class="line"><span class="comment">// 如果有的话,就会putAll进去.      </span></span><br><span class="line">    <span class="type">Properties</span> <span class="variable">vars</span> <span class="operator">=</span> configuration.getVariables();</span><br><span class="line">    <span class="keyword">if</span> (vars != <span class="literal">null</span>) &#123;</span><br><span class="line">      defaults.putAll(vars);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 最后吧 defaults,也就是properties给放入到 BaseBuilder 和 Confifuration中去.      </span></span><br><span class="line">    parser.setVariables(defaults);</span><br><span class="line">    configuration.setVariables(defaults);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-----------------------------</span><br><span class="line"><span class="comment">//  如何让 Properties vars = configuration.getVariables(); 有值呢 ?</span></span><br><span class="line"><span class="comment">//  如果只是单个的 MyBatis 项目的话, 就自己手动new一个properties对象</span></span><br><span class="line"><span class="comment">//  然后key输入自己要覆盖掉的key就可以了</span></span><br><span class="line">        <span class="type">Properties</span> <span class="variable">dbConfigProperties</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Properties</span>();</span><br><span class="line">        dbConfigProperties.setProperty(<span class="string">&quot;jdbc.password&quot;</span>,<span class="string">&quot;GavinYang&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream,dbConfigProperties);</span><br></pre></td></tr></table></figure>

<h2 id="标签二-settings"><a href="#标签二-settings" class="headerlink" title="标签二 : settings"></a><strong>标签二 : settings</strong></h2><p>这是 MyBatis对 settings 的操作.</p>
<p>具体的 settings 中每项配置参考官网链接 : <a href="https://mybatis.org/mybatis-3/configuration.html#properties">https://mybatis.org/mybatis-3/configuration.html#properties</a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 解析 setting ---&gt; 转化为 key /value</span></span><br><span class="line"><span class="type">Properties</span> <span class="variable">settings</span> <span class="operator">=</span> settingsAsProperties(root.evalNode(<span class="string">&quot;settings&quot;</span>));</span><br><span class="line"><span class="comment">// </span></span><br><span class="line">loadCustomVfs(settings);</span><br><span class="line">loadCustomLogImpl(settings);</span><br></pre></td></tr></table></figure>

<p>settingsAsProperties 方法</p>
<p>可以看到, 该方法就是进行加载,转化为key&#x2F;value键值对类型, 然后对其key检验是否在</p>
<p>Configuration 中都有 set 方法.</p>
<p>Notes : 为了验证下, 我们加上一个没有的标签, 可以看到下面的异常. 所以我们看到这种异常的时候，是可以去检查下是不是名字什么有问题.</p>
<p><strong>Cause: org.apache.ibatis.builder.BuilderException: Error parsing SQL Mapper Configuration. Cause: org.apache.ibatis.builder.BuilderException: The setting nnnnn is not known. Make sure you spelled it correctly (case sensitive).</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Properties <span class="title function_">settingsAsProperties</span><span class="params">(XNode context)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (context == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Properties</span>();</span><br><span class="line">  &#125;</span><br><span class="line"> <span class="comment">// 对 settings 下的 setting 进行解析 并且 转化为 key / value 操作.   </span></span><br><span class="line">  <span class="type">Properties</span> <span class="variable">props</span> <span class="operator">=</span> context.getChildrenAsProperties();</span><br><span class="line">  <span class="comment">// Check that all settings are known to the configuration class</span></span><br><span class="line"> <span class="comment">// 对 Configuration 进行校验, 确认上面的 props 中的key 在 Configuration</span></span><br><span class="line"><span class="comment">// 中是都有set 方法的,目测是后面反射需要使用到.    </span></span><br><span class="line">  <span class="type">MetaClass</span> <span class="variable">metaConfig</span> <span class="operator">=</span> MetaClass.forClass(Configuration.class, localReflectorFactory);</span><br><span class="line">  <span class="keyword">for</span> (Object key : props.keySet()) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!metaConfig.hasSetter(String.valueOf(key))) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;The setting &quot;</span> + key + <span class="string">&quot; is not known.  Make sure you spelled it correctly (case sensitive).&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> props;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>loadCustomVfs(settings) 方法</p>
<p>该方法,主要就是读取 vfsImpl 对用的value,切割下,然后用 classForName 来获取 class,</p>
<p>最后赋值到 configuration 中去. 这里算是对 vfs 的一种自定义的扩展,虽然目前还不太清楚vfs具体作用.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">loadCustomVfs</span><span class="params">(Properties props)</span> <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line">  <span class="comment">// 获取 vfsImpl 的 value.  </span></span><br><span class="line">  <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> props.getProperty(<span class="string">&quot;vfsImpl&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (value != <span class="literal">null</span>) &#123;</span><br><span class="line">   <span class="comment">// 根据 , 进行切割.   </span></span><br><span class="line">    String[] clazzes = value.split(<span class="string">&quot;,&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (String clazz : clazzes) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!clazz.isEmpty()) &#123;</span><br><span class="line">        <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">        <span class="comment">// 反射,获取出 Class , 最后赋值到 configuration 中去.  </span></span><br><span class="line">        Class&lt;? <span class="keyword">extends</span> <span class="title class_">VFS</span>&gt; vfsImpl = (Class&lt;? <span class="keyword">extends</span> <span class="title class_">VFS</span>&gt;)Resources.classForName(clazz);</span><br><span class="line">        configuration.setVfsImpl(vfsImpl);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>loadCustomLogImpl(settings) 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">loadCustomLogImpl</span><span class="params">(Properties props)</span> &#123;</span><br><span class="line">  Class&lt;? <span class="keyword">extends</span> <span class="title class_">Log</span>&gt; logImpl = resolveClass(props.getProperty(<span class="string">&quot;logImpl&quot;</span>));</span><br><span class="line">  <span class="comment">// 将 log set 到 configuration 中去.  </span></span><br><span class="line">  configuration.setLogImpl(logImpl);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-----------------------</span><br><span class="line"><span class="comment">// resolve 最后如果不是 null 的话,</span></span><br><span class="line">org.apache.ibatis.type.TypeAliasRegistry#resolveAlias</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 就会走到这里,这里可以看先是在 typeAliases(HashMap) 中判断下,如果存在就直接获取</span></span><br><span class="line"><span class="comment">// 如果不存在就用 Resources.ClassForName来操作</span></span><br><span class="line"><span class="comment">// 这里的 HashMap就类似于,记录之前是否已经加载了或者预热.</span></span><br><span class="line"><span class="comment">// 如果是用来做cache的话, 那就应该最后会在 return 之前继续把值给放入进去.    </span></span><br><span class="line">  <span class="keyword">public</span> &lt;T&gt; Class&lt;T&gt; <span class="title function_">resolveAlias</span><span class="params">(String string)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (string == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// issue #748</span></span><br><span class="line">      <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> string.toLowerCase(Locale.ENGLISH);</span><br><span class="line">      Class&lt;T&gt; value;</span><br><span class="line">      <span class="keyword">if</span> (typeAliases.containsKey(key)) &#123;</span><br><span class="line">        value = (Class&lt;T&gt;) typeAliases.get(key);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        value = (Class&lt;T&gt;) Resources.classForName(string);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> value;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">TypeException</span>(<span class="string">&quot;Could not resolve type alias &#x27;&quot;</span> + string + <span class="string">&quot;&#x27;.  Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line">------------</span><br><span class="line"><span class="comment">// 如果我们在配置文件中没有定义的话,这里默认是null,也就是说不会set进去.    </span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setLogImpl</span><span class="params">(Class&lt;? extends Log&gt; logImpl)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (logImpl != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="built_in">this</span>.logImpl = logImpl;</span><br><span class="line">      LogFactory.useCustomLogging(<span class="built_in">this</span>.logImpl);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="标签三-关于别名的配置"><a href="#标签三-关于别名的配置" class="headerlink" title="标签三 : 关于别名的配置."></a><strong>标签三 : 关于别名的配置.</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">typeAliasesElement(root.evalNode(<span class="string">&quot;typeAliases&quot;</span>));</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">typeAliasesElement</span><span class="params">(XNode parent)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">   <span class="comment">// 对 typeAliases 下的子标签进行迭代.</span></span><br><span class="line">   <span class="comment">// 分为是 package 和非 package   </span></span><br><span class="line">    <span class="keyword">for</span> (XNode child : parent.getChildren()) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="string">&quot;package&quot;</span>.equals(child.getName())) &#123;</span><br><span class="line">       <span class="comment">// 获取你输入的包   </span></span><br><span class="line">        <span class="type">String</span> <span class="variable">typeAliasPackage</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        configuration.getTypeAliasRegistry().registerAliases(typeAliasPackage);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">// &lt;typeAlias type=&quot;com.iyang.mybatis.pojo.TbBlog&quot; alias=&quot;TbBlog&quot; /&gt;</span></span><br><span class="line"> <span class="comment">// 这里就是对这种进行解析的         </span></span><br><span class="line">        <span class="type">String</span> <span class="variable">alias</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;alias&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">type</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;type&quot;</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          Class&lt;?&gt; clazz = Resources.classForName(type);</span><br><span class="line">   <span class="comment">// 如果没写别名,就只传入 clazz.         </span></span><br><span class="line">          <span class="keyword">if</span> (alias == <span class="literal">null</span>) &#123;</span><br><span class="line">            typeAliasRegistry.registerAlias(clazz);</span><br><span class="line">          &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">   <span class="comment">// 写了别名,就别名和clazz一起传入进来.           </span></span><br><span class="line">            typeAliasRegistry.registerAlias(alias, clazz);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error registering typeAlias for &#x27;&quot;</span> + alias + <span class="string">&quot;&#x27;. Cause: &quot;</span> + e, e);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">----------------</span><br><span class="line"><span class="comment">// 这里可以看到是根据 packageName 来 register进来的.    </span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerAliases</span><span class="params">(String packageName, Class&lt;?&gt; superType)</span> &#123;</span><br><span class="line">    <span class="comment">// new 一个解析器工具类</span></span><br><span class="line">    ResolverUtil&lt;Class&lt;?&gt;&gt; resolverUtil = <span class="keyword">new</span> <span class="title class_">ResolverUtil</span>&lt;&gt;();</span><br><span class="line">    <span class="comment">// 获取包的path,然后获取该包下的文件,如果文件是.class结尾的话</span></span><br><span class="line">    <span class="comment">// 最后在 ResolverUtil 中matchess是有该包下的全名称.</span></span><br><span class="line">    resolverUtil.find(<span class="keyword">new</span> <span class="title class_">ResolverUtil</span>.IsA(superType), packageName);</span><br><span class="line">    <span class="comment">// 这里返回的是上一步说的 matches</span></span><br><span class="line">    Set&lt;Class&lt;? <span class="keyword">extends</span> <span class="title class_">Class</span>&lt;?&gt;&gt;&gt; typeSet = resolverUtil.getClasses();</span><br><span class="line">    <span class="keyword">for</span> (Class&lt;?&gt; type : typeSet) &#123;</span><br><span class="line">      <span class="comment">// Ignore inner classes and interfaces (including package-info.java)</span></span><br><span class="line">      <span class="comment">// Skip also inner classes. See issue #6</span></span><br><span class="line">      <span class="comment">// 如果不是接口,不是内部类等条件的话,就走  registerAlias 方法</span></span><br><span class="line">      <span class="keyword">if</span> (!type.isAnonymousClass() &amp;&amp; !type.isInterface() &amp;&amp; !type.isMemberClass()) &#123;</span><br><span class="line"> <span class="comment">// 先获取类名字,判断该类上有没有 @Alias 注解,如果有注解的话,就用注解的值作为缩写的.</span></span><br><span class="line"> <span class="comment">// 最后判断是不是null,是null就会抛出异常来.最后将上面获取出来的缩写名字,转化为大写.</span></span><br><span class="line"> <span class="comment">// 如果此时 typeAliases 是已经有了该值的话,就会抛出异常来.否则就放入到typeAliases来</span></span><br><span class="line"> <span class="comment">// private final Map&lt;String, Class&lt;?&gt;&gt; typeAliases = new HashMap&lt;&gt;();</span></span><br><span class="line"> <span class="comment">// 可以看到 typeAliases 是一个HashMap,并且其存储的Key/Value还是蛮明显的.         </span></span><br><span class="line">        registerAlias(type);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="标签四：扩展"><a href="#标签四：扩展" class="headerlink" title="标签四：扩展"></a>标签四：扩展</h2><p>扩展的 demo 可以参考 MyBatis官网 : <a href="https://mybatis.org/mybatis-3/configuration.html">https://mybatis.org/mybatis-3/configuration.html</a></p>
<p>然后看 MyBatis 是如何将插件给利用上的呢 ?</p>
<p>首先在 mybatis-config.xml 中配置好我们自己定义的 plugin</p>
<p>这里以我配置了二个插件</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;plugins&gt;</span><br><span class="line">    &lt;plugin interceptor=<span class="string">&quot;com.iyang.mybatis.plugins.ExamplePlugin&quot;</span>&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;name&quot;</span> value=<span class="string">&quot;GavinYang&quot;</span>/&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;age&quot;</span> value=<span class="string">&quot;22&quot;</span>/&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;hobby&quot;</span> value=<span class="string">&quot;lwf&quot;</span>/&gt;</span><br><span class="line">    &lt;/plugin&gt;</span><br><span class="line"></span><br><span class="line">    &lt;plugin interceptor=<span class="string">&quot;com.iyang.mybatis.plugins.QuerySqlPlugin&quot;</span>&gt;</span><br><span class="line"></span><br><span class="line">        &lt;property name=<span class="string">&quot;name&quot;</span> value=<span class="string">&quot;GavinYang&quot;</span>/&gt;</span><br><span class="line">    &lt;/plugin&gt;</span><br><span class="line">&lt;/plugins&gt;</span><br></pre></td></tr></table></figure>

<p>&#x2F;&#x2F; 处理 plugin 的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">pluginElement</span><span class="params">(XNode parent)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="comment">// 这里传入进来的就是 &lt;plugins&gt;整个标签内容.  </span></span><br><span class="line">  <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">   <span class="comment">// 获取 &lt;plugins&gt; 下的 &lt;plugin&gt; 集合,进行迭代处理.   </span></span><br><span class="line">    <span class="keyword">for</span> (XNode child : parent.getChildren()) &#123;</span><br><span class="line">     <span class="comment">// 获取插件的 全限定名字.   </span></span><br><span class="line">      <span class="type">String</span> <span class="variable">interceptor</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;interceptor&quot;</span>);</span><br><span class="line">     <span class="comment">// 获取我们定义在 plugin 下的 properties.   </span></span><br><span class="line">      <span class="type">Properties</span> <span class="variable">properties</span> <span class="operator">=</span> child.getChildrenAsProperties();</span><br><span class="line"><span class="comment">// resolveClass是最后注册到typeAliasRegistry来.    </span></span><br><span class="line"><span class="comment">// 实例化,这里就可以看到我们在定义的Plugin中,无参构造函数打印出来的内容了.        </span></span><br><span class="line">      <span class="type">Interceptor</span> <span class="variable">interceptorInstance</span> <span class="operator">=</span> (Interceptor) resolveClass(interceptor).getDeclaredConstructor().newInstance();</span><br><span class="line"><span class="comment">// 将 properties 赋值给  interceptorInstance</span></span><br><span class="line"><span class="comment">// 也就是放入到 interceptorInstance 来.        </span></span><br><span class="line">      interceptorInstance.setProperties(properties);</span><br><span class="line"><span class="comment">// org.apache.ibatis.plugin.InterceptorChain</span></span><br><span class="line"><span class="comment">// 这是是将interceptorInstance添加到InterceptorChain的interceptors中来.        </span></span><br><span class="line">      configuration.addInterceptor(interceptorInstance);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 MyBatis在加载plugin的时候,是利用了反射来new出一个对象来,并且注册到 typeAliasRegistry 中来. 这里主要是解析 plugin 的配置, 后面在执行sql的时候,都是如何使用到这些 plugin 的呢 ? 肯定是有一个从InterceptorChain中获取interceptors来,然后进行处理.</p>
<h2 id="标签五-objectFactory"><a href="#标签五-objectFactory" class="headerlink" title="标签五 : &lt; objectFactory &gt;"></a><strong>标签五 : &lt; objectFactory &gt;</strong></h2><p>objectFactory 的处理方式是和 标签四相似的,只是最后在使用场景是有点不同的.</p>
<p>代码上的操作也是类似的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">objectFactoryElement</span><span class="params">(XNode context)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (context != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">type</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;type&quot;</span>);</span><br><span class="line">    <span class="type">Properties</span> <span class="variable">properties</span> <span class="operator">=</span> context.getChildrenAsProperties();</span><br><span class="line">    <span class="type">ObjectFactory</span> <span class="variable">factory</span> <span class="operator">=</span> (ObjectFactory) resolveClass(type).getDeclaredConstructor().newInstance();</span><br><span class="line">    factory.setProperties(properties);</span><br><span class="line">    configuration.setObjectFactory(factory);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该标签在 MyBatis 官网是没有demo, 我是根据代码来顺藤摸瓜写的一个.</p>
<p>参考 : org.apache.ibatis.reflection.wrapper.DefaultObjectWrapperFactory 这个源码,来模仿写的一个.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">objectWrapperFactoryElement(root.evalNode(<span class="string">&quot;objectWrapperFactory&quot;</span>));</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">objectWrapperFactoryElement</span><span class="params">(XNode context)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (context != <span class="literal">null</span>) &#123;</span><br><span class="line">   <span class="comment">// 获取 配置文件中的type值   </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">type</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;type&quot;</span>);</span><br><span class="line"> <span class="comment">// 先注册到  typeAliasRegistry 来,然后实例化这个类.</span></span><br><span class="line"> <span class="comment">// 我们在自己定义的类中,写一个无参构造函数,就可以看到我们打印的内容了.     </span></span><br><span class="line">    <span class="type">ObjectWrapperFactory</span> <span class="variable">factory</span> <span class="operator">=</span> (ObjectWrapperFactory) resolveClass(type).getDeclaredConstructor().newInstance();</span><br><span class="line"><span class="comment">// 最后赋值到 confifuration 中来.      </span></span><br><span class="line">    configuration.setObjectWrapperFactory(factory);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="标签六-reflectorFactory"><a href="#标签六-reflectorFactory" class="headerlink" title="标签六 : &lt; reflectorFactory &gt;"></a><strong>标签六 : &lt; reflectorFactory &gt;</strong></h2><p>处理方式和上面类似.</p>
<p>这里我们自己写一个 com.iyang.mybatis.factory.GavinReflectorFactory 来继承DefaultReflectorFactory,在无参数构造函数中打印下内容, 然后debug跟进.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">reflectorFactoryElement</span><span class="params">(XNode context)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (context != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">type</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;type&quot;</span>);</span><br><span class="line">    <span class="type">ReflectorFactory</span> <span class="variable">factory</span> <span class="operator">=</span> (ReflectorFactory) resolveClass(type).getDeclaredConstructor().newInstance();</span><br><span class="line">    configuration.setReflectorFactory(factory);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="标签七：environments"><a href="#标签七：environments" class="headerlink" title="标签七：environments"></a>标签七：environments</h2><p>environments 标签都是放入一些 db 的配置信息等.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;environments <span class="keyword">default</span>=<span class="string">&quot;development&quot;</span>&gt;</span><br><span class="line">    &lt;environment id=<span class="string">&quot;development&quot;</span>&gt;</span><br><span class="line">        &lt;!-- 事务 --&gt;</span><br><span class="line">        &lt;transactionManager type=<span class="string">&quot;JDBC&quot;</span>/&gt;</span><br><span class="line">        </span><br><span class="line">        &lt;!-- DB 连接配置 --&gt;</span><br><span class="line">        &lt;dataSource type=<span class="string">&quot;POOLED&quot;</span>&gt;</span><br><span class="line">            &lt;property name=<span class="string">&quot;driver&quot;</span> value=<span class="string">&quot;$&#123;jdbc.driver&#125;&quot;</span> /&gt;</span><br><span class="line">            &lt;property name=<span class="string">&quot;url&quot;</span> value=<span class="string">&quot;$&#123;jdbc.url&#125;&quot;</span> /&gt;</span><br><span class="line">            &lt;property name=<span class="string">&quot;username&quot;</span> value = <span class="string">&quot;$&#123;jdbc.username&#125;&quot;</span> /&gt;</span><br><span class="line">            &lt;property name=<span class="string">&quot;password&quot;</span> value=<span class="string">&quot;$&#123;jdbc.password&#125;&quot;</span> /&gt;</span><br><span class="line">        &lt;/dataSource&gt;</span><br><span class="line">    &lt;/environment&gt;</span><br><span class="line">&lt;/environments&gt;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">environmentsElement</span><span class="params">(XNode context)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (context != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (environment == <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// 获取 default 对应字段的值         </span></span><br><span class="line">      environment = context.getStringAttribute(<span class="string">&quot;default&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 这里的 getChildren 获取的是 &lt;environments&gt; --&gt; &lt;environment&gt;下的子标签      </span></span><br><span class="line">    <span class="keyword">for</span> (XNode child : context.getChildren()) &#123;</span><br><span class="line">      <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;id&quot;</span>);</span><br><span class="line"><span class="comment">// 确保 id 和  上一步的environment 的值是相同的,就会返回true.      </span></span><br><span class="line">      <span class="keyword">if</span> (isSpecifiedEnvironment(id)) &#123;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">*  获取出 transactionManager 对应的标签.</span></span><br><span class="line"><span class="comment">*  然后根据 JBDC(配置文件中的值),然后从 typeAliasRegistry中获取出来，</span></span><br><span class="line"><span class="comment">*  调用反射来 实例化 这个对象. </span></span><br><span class="line"><span class="comment">*  最后还是可以配置 properties,会被set到txFactory中去的.</span></span><br><span class="line"><span class="comment">*  但是 JdbcTransactionFactory 好像没有重写 setProperties 方法.</span></span><br><span class="line"><span class="comment">*/</span>          </span><br><span class="line">        <span class="type">TransactionFactory</span> <span class="variable">txFactory</span> <span class="operator">=</span> transactionManagerElement(child.evalNode(<span class="string">&quot;transactionManager&quot;</span>));</span><br><span class="line"><span class="comment">// 先获取  dataSource 字段</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">*  先获取type的值,然后再获取 properties的标签字段值.</span></span><br><span class="line"><span class="comment">*  根据我们的配置 : org.apache.ibatis.datasource.pooled.PooledDataSourceFactory,应该会获取出这个对象.该对象其内部是有一个,org.apache.ibatis.datasource.pooled.PooledDataSource的,里面有部分默认值的.</span></span><br><span class="line"><span class="comment">*最后将  properties 调用 org.apache.ibatis.datasource.unpooled.UnpooledDataSourceFactory#setProperties方法,</span></span><br><span class="line"><span class="comment">最后是将 properties 里面的key/value 都设置到 MetaObject metaDataSource = SystemMetaObject.forObject(dataSource);来了.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">        <span class="type">DataSourceFactory</span> <span class="variable">dsFactory</span> <span class="operator">=</span> dataSourceElement(child.evalNode(<span class="string">&quot;dataSource&quot;</span>));</span><br><span class="line"><span class="comment">// 从  PooledDataSourceFactory 中获取 datasource 属性.         </span></span><br><span class="line">        <span class="type">DataSource</span> <span class="variable">dataSource</span> <span class="operator">=</span> dsFactory.getDataSource();</span><br><span class="line"><span class="comment">// 这里采用链式编程,也就是将id/txFactory/dataSource 都给set到 Environment.Builder来了.         </span></span><br><span class="line">        Environment.<span class="type">Builder</span> <span class="variable">environmentBuilder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Environment</span>.Builder(id)</span><br><span class="line">            .transactionFactory(txFactory)</span><br><span class="line">            .dataSource(dataSource);</span><br><span class="line">  <span class="comment">//    environmentBuilder.build() 也就是new 了一个 Environment </span></span><br><span class="line">  <span class="comment">// 最后 赋值到 configuration 中来了.        </span></span><br><span class="line">        configuration.setEnvironment(environmentBuilder.build());</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>解析 environments ,利用 typeAliasRegistry 中已经注册好了的信息,然后根据名字缩写(比如JDBC)这种,来获取class对象, 用 反射来 new 一波对象出来,真是美滋滋. 接着就是解析 事务&#x2F;JDBC连接配置信息等, 最后将信息保存到 DataSource 中来. 反手再来一波 链式编程 来new对象出来, 最后就是一个 Environment 对象出来,给set 到 configuration 中来.</p>
<h2 id="标签八：-handler"><a href="#标签八：-handler" class="headerlink" title="标签八： handler"></a><strong>标签八： handler</strong></h2><p>到这里,可以看到对xml的解析操作. 先解析 标签 的值出来,然后根据值进行分类处理或者根据自己的需求来进行处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">typeHandlerElement</span><span class="params">(XNode parent)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">for</span> (XNode child : parent.getChildren()) &#123;</span><br><span class="line">      <span class="comment">// 如果子标签是 package   </span></span><br><span class="line">      <span class="keyword">if</span> (<span class="string">&quot;package&quot;</span>.equals(child.getName())) &#123;</span><br><span class="line">       <span class="comment">// 获取出 name 对应的值.   </span></span><br><span class="line">        <span class="type">String</span> <span class="variable">typeHandlerPackage</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">      <span class="comment">// 注册到   typeHandlerRegistry 中来.  </span></span><br><span class="line">        typeHandlerRegistry.register(typeHandlerPackage);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">// 这里获取出三种值来,   javaType/jdbcType/  handler    </span></span><br><span class="line">        <span class="type">String</span> <span class="variable">javaTypeName</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;javaType&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">jdbcTypeName</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;jdbcType&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">handlerTypeName</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;handler&quot;</span>);</span><br><span class="line">        Class&lt;?&gt; javaTypeClass = resolveClass(javaTypeName);</span><br><span class="line">        <span class="type">JdbcType</span> <span class="variable">jdbcType</span> <span class="operator">=</span> resolveJdbcType(jdbcTypeName);</span><br><span class="line">        Class&lt;?&gt; typeHandlerClass = resolveClass(handlerTypeName);</span><br><span class="line">  <span class="comment">// 分为  javaTypeClass 是不是 null 的情况       </span></span><br><span class="line">        <span class="keyword">if</span> (javaTypeClass != <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="comment">// 基于 javaTypeClass 是不是 null的情况,再判断 jdbcType 是不是null  </span></span><br><span class="line">          <span class="keyword">if</span> (jdbcType == <span class="literal">null</span>) &#123;</span><br><span class="line">            typeHandlerRegistry.register(javaTypeClass, typeHandlerClass);</span><br><span class="line">          &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            typeHandlerRegistry.register(javaTypeClass, jdbcType, typeHandlerClass);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 这是根据   handlerTypeName 注册到 typeHandlerRegistry 中来.           </span></span><br><span class="line">          typeHandlerRegistry.register(typeHandlerClass);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="标签九"><a href="#标签九" class="headerlink" title="标签九"></a><strong>标签九</strong></h2><p>该标签是对我们对应的对象,其sql语句存放的地址. 也就是里面放入的是于mapper接口对应的方法,查询的sql语句.</p>
<p>接下来看下 MyBatis 是对 mappers 标签的内容进行了说明解析和处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">mapperElement</span><span class="params">(XNode parent)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">  <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">      </span><br><span class="line"> <span class="comment">// getChildren 获取的是 mappers 下的 mapper 标签</span></span><br><span class="line">    <span class="keyword">for</span> (XNode child : parent.getChildren()) &#123;</span><br><span class="line"><span class="comment">// 如果配置的是 package.        </span></span><br><span class="line">      <span class="keyword">if</span> (<span class="string">&quot;package&quot;</span>.equals(child.getName())) &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">mapperPackage</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        configuration.addMappers(mapperPackage);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 获取出    resource/url/class 这三类的值.       </span></span><br><span class="line">        <span class="type">String</span> <span class="variable">resource</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;resource&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;url&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">mapperClass</span> <span class="operator">=</span> child.getStringAttribute(<span class="string">&quot;class&quot;</span>);</span><br><span class="line"> <span class="comment">// 对 resource 处理         </span></span><br><span class="line">        <span class="keyword">if</span> (resource != <span class="literal">null</span> &amp;&amp; url == <span class="literal">null</span> &amp;&amp; mapperClass == <span class="literal">null</span>) &#123;</span><br><span class="line">          <span class="comment">// 将 resource 赋值给 ErrorContext 中  </span></span><br><span class="line">          ErrorContext.instance().resource(resource);</span><br><span class="line">     <span class="comment">// 读取文件.       </span></span><br><span class="line">          <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(resource);</span><br><span class="line"><span class="comment">// 使用 XMLMapperBuilder 来对解析xml内容.            </span></span><br><span class="line">          <span class="type">XMLMapperBuilder</span> <span class="variable">mapperParser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLMapperBuilder</span>(inputStream, configuration, resource, configuration.getSqlFragments());</span><br><span class="line">          mapperParser.parse();</span><br><span class="line"><span class="comment">// url 处理            </span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (resource == <span class="literal">null</span> &amp;&amp; url != <span class="literal">null</span> &amp;&amp; mapperClass == <span class="literal">null</span>) &#123;</span><br><span class="line">          ErrorContext.instance().resource(url);</span><br><span class="line">          <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> Resources.getUrlAsStream(url);</span><br><span class="line">          <span class="type">XMLMapperBuilder</span> <span class="variable">mapperParser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLMapperBuilder</span>(inputStream, configuration, url, configuration.getSqlFragments());</span><br><span class="line">          mapperParser.parse();</span><br><span class="line"><span class="comment">// mapperClass 处理            </span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (resource == <span class="literal">null</span> &amp;&amp; url == <span class="literal">null</span> &amp;&amp; mapperClass != <span class="literal">null</span>) &#123;</span><br><span class="line">          Class&lt;?&gt; mapperInterface = Resources.classForName(mapperClass);</span><br><span class="line">          configuration.addMapper(mapperInterface);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;A mapper element may only specify a url, resource or class, but not more than one.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-----------------------</span><br><span class="line"><span class="comment">// 这里我们跟进 mapperParser.parse() 方法来</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.builder.xml.XMLMapperBuilder</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">parse</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="comment">// 判断 configuration 的 loadedResources 是否含有该值,如果不含有的话,就会去解析.  </span></span><br><span class="line">    <span class="keyword">if</span> (!configuration.isResourceLoaded(resource)) &#123;</span><br><span class="line"><span class="comment">// 对mapper 标签进行解析        </span></span><br><span class="line">      configurationElement(parser.evalNode(<span class="string">&quot;/mapper&quot;</span>));</span><br><span class="line">      configuration.addLoadedResource(resource);</span><br><span class="line">      bindMapperForNamespace();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    parsePendingResultMaps();</span><br><span class="line">    parsePendingCacheRefs();</span><br><span class="line">    parsePendingStatements();</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line">-----------------------------------</span><br><span class="line"><span class="comment">//   configurationElement 方法</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">configurationElement</span><span class="params">(XNode context)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"> <span class="comment">// 获取 namespace       </span></span><br><span class="line">      <span class="type">String</span> <span class="variable">namespace</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;namespace&quot;</span>);</span><br><span class="line">      <span class="keyword">if</span> (namespace == <span class="literal">null</span> || namespace.equals(<span class="string">&quot;&quot;</span>)) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Mapper&#x27;s namespace cannot be empty&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">//  MapperBuilderAssistant 将 namespace 绑定到该类的参数中来.        </span></span><br><span class="line">      builderAssistant.setCurrentNamespace(namespace);</span><br><span class="line">  </span><br><span class="line">   <span class="comment">// 这里的 cache-ref / cache 都是暂时没有配置的.     </span></span><br><span class="line">      cacheRefElement(context.evalNode(<span class="string">&quot;cache-ref&quot;</span>));</span><br><span class="line">      cacheElement(context.evalNode(<span class="string">&quot;cache&quot;</span>));</span><br><span class="line">        </span><br><span class="line"> <span class="comment">//  /mapper/parameterMap 也是暂时没有配置的  </span></span><br><span class="line">      parameterMapElement(context.evalNodes(<span class="string">&quot;/mapper/parameterMap&quot;</span>));</span><br><span class="line">        </span><br><span class="line"><span class="comment">// resultMap 是对对象字段的映射</span></span><br><span class="line"><span class="comment">// mapper/sql 是对一些公用的sql进行抽取</span></span><br><span class="line"><span class="comment">// 二者暂时都没有配置        </span></span><br><span class="line">      resultMapElements(context.evalNodes(<span class="string">&quot;/mapper/resultMap&quot;</span>));</span><br><span class="line">      sqlElement(context.evalNodes(<span class="string">&quot;/mapper/sql&quot;</span>));</span><br><span class="line"><span class="comment">// 获取 select / insert / update / delete 等 标签.        </span></span><br><span class="line">      buildStatementFromContext(context.evalNodes(<span class="string">&quot;select|insert|update|delete&quot;</span>));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error parsing Mapper XML. The XML location is &#x27;&quot;</span> + resource + <span class="string">&quot;&#x27;. Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">// 往下跟方法</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">buildStatementFromContext</span><span class="params">(List&lt;XNode&gt; list, String requiredDatabaseId)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (XNode context : list) &#123;</span><br><span class="line">      <span class="keyword">final</span> <span class="type">XMLStatementBuilder</span> <span class="variable">statementParser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLStatementBuilder</span>(configuration, builderAssistant, context, requiredDatabaseId);</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        statementParser.parseStatementNode();</span><br><span class="line">      &#125; <span class="keyword">catch</span> (IncompleteElementException e) &#123;</span><br><span class="line">        configuration.addIncompleteStatement(statementParser);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>mybatis本地缓存阅读</title>
    <url>//article/mybatis_localtion_cache1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 <strong>题记</strong></h1><h3 id="缓存这个知识点在许多地方都有的，利用到好的话，对系统的很多地方查询是有很大的提升的-可以看到-MyBatis-也是有-cache-的，那MyBatis-是怎么利用这个缓存的呢？-在-INSERT-UPDATE-DELETE-SELECT中-是不是只有SELECT的时候用到了缓存，如果是-INSERT-UPDATE-DELETE-是否会对缓存有影响？"><a href="#缓存这个知识点在许多地方都有的，利用到好的话，对系统的很多地方查询是有很大的提升的-可以看到-MyBatis-也是有-cache-的，那MyBatis-是怎么利用这个缓存的呢？-在-INSERT-UPDATE-DELETE-SELECT中-是不是只有SELECT的时候用到了缓存，如果是-INSERT-UPDATE-DELETE-是否会对缓存有影响？" class="headerlink" title="缓存这个知识点在许多地方都有的，利用到好的话，对系统的很多地方查询是有很大的提升的. 可以看到,MyBatis 也是有 cache 的，那MyBatis 是怎么利用这个缓存的呢？ 在 INSERT&#x2F;UPDATE&#x2F;DELETE&#x2F;SELECT中,是不是只有SELECT的时候用到了缓存，如果是 INSERT&#x2F;UPDATE&#x2F;DELETE 是否会对缓存有影响？"></a>缓存这个知识点在许多地方都有的，利用到好的话，对系统的很多地方查询是有很大的提升的. 可以看到,MyBatis 也是有 cache 的，那MyBatis 是怎么利用这个缓存的呢？ 在 INSERT&#x2F;UPDATE&#x2F;DELETE&#x2F;SELECT中,是不是只有SELECT的时候用到了缓存，如果是 INSERT&#x2F;UPDATE&#x2F;DELETE 是否会对缓存有影响？</h3><p>可以看结果来分析，然后跟进源码来仔细分析.</p>
<p>MyBatis 是分为 一级缓存 和 二级缓存的. 那么，我们就先从一级缓存开始.</p>
<h1 id="一级缓存"><a href="#一级缓存" class="headerlink" title="一级缓存"></a><strong>一级缓存</strong></h1><p>案例代码 :</p>
<p>这里我们是打印的查询sql的语句，再第二次再查询的时候，是</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line"><span class="type">SqlSession</span> <span class="variable">session</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"><span class="type">BlogMapper</span> <span class="variable">blogMapper</span> <span class="operator">=</span> session.getMapper(BlogMapper.class);</span><br><span class="line"><span class="type">TbBlog</span> <span class="variable">tbBlog</span> <span class="operator">=</span> blogMapper.selectBlog(<span class="number">1</span>);</span><br><span class="line">System.out.println(blogMapper.selectBlog(<span class="number">1</span>));</span><br><span class="line">System.out.println(tbBlog);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果可以看到,第二次并没有再打印出 sql 语句来.</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>案例二 : 我们再第二次查询之前 加入 一个add 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line"><span class="type">SqlSession</span> <span class="variable">session</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"><span class="type">BlogMapper</span> <span class="variable">blogMapper</span> <span class="operator">=</span> session.getMapper(BlogMapper.class);</span><br><span class="line"><span class="type">TbBlog</span> <span class="variable">tbBlog</span> <span class="operator">=</span> blogMapper.selectBlog(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(blogMapper.addBlog(<span class="string">&quot;GavinYang&quot;</span>));</span><br><span class="line">System.out.println(blogMapper.selectBlog(<span class="number">1</span>));</span><br><span class="line">System.out.println(tbBlog);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 看结果,可以看到当中间穿插一个 insert 的sql语句,那么在第二次查询的时候,就会执行sql语句.</span></span><br><span class="line"><span class="comment">// 那么也就说，这个时候缓存是失效了.</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">==&gt;  Preparing: insert into <span class="title function_">tb_blog</span> <span class="params">(name)</span> values(?) </span><br><span class="line">==&gt; Parameters: GavinYang(String)</span><br><span class="line">&lt;==    Updates: <span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>案例三 : 使用二个 SqlSession 案例</p>
<p>可以很明显的看到 , 在第二次的时候还出现了脏数据.</p>
<p>这里也可以看到一级缓存是只在 SqlSession 中存在的,也就是数据库会话内部共享的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line"><span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line"></span><br><span class="line"><span class="type">SqlSession</span> <span class="variable">openSession1</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"><span class="type">SqlSession</span> <span class="variable">openSession2</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"><span class="type">BlogMapper</span> <span class="variable">blogMapper1</span> <span class="operator">=</span> openSession1.getMapper(BlogMapper.class);</span><br><span class="line"><span class="type">BlogMapper</span> <span class="variable">blogMapper2</span> <span class="operator">=</span> openSession2.getMapper(BlogMapper.class);</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;blogMapper1 读取数据 &quot;</span> + blogMapper1.selectBlog(<span class="number">1</span>));</span><br><span class="line">System.out.println(<span class="string">&quot;blogMapper2 读取数据&quot;</span> + blogMapper2.selectBlog(<span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">System.out.println(blogMapper1.updateHashCode(<span class="string">&quot;PeterWong&quot;</span>));</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;blogMapper1 读取数据 &quot;</span> + blogMapper1.selectBlog(<span class="number">1</span>));</span><br><span class="line">System.out.println(<span class="string">&quot;blogMapper2 读取数据&quot;</span> + blogMapper2.selectBlog(<span class="number">1</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 然后我们可以看到 log 打印出来的内容</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper1 读取数据 TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">Created connection <span class="number">433287555.</span></span><br><span class="line">Setting autocommit to <span class="literal">false</span> on JDBC Connection [com.mysql.jdbc.JDBC4Connection@19d37183]</span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper2 读取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">==&gt;  Preparing: update tb_blog <span class="type">set</span> <span class="variable">name</span> <span class="operator">=</span> ? <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> <span class="number">1</span>; </span><br><span class="line">==&gt; Parameters: PeterWong(String)</span><br><span class="line">&lt;==    Updates: <span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, PeterWong</span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper1 读取数据 TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;PeterWong&#x27;</span>&#125;</span><br><span class="line">blogMapper2 读取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们说了三面的这三种情况, 具体的执行流程可以我们可以现在 案例一里面对第二次 query 进行 debug 分析操作. 当我们debug到 org.apache.ibatis.executor.BaseExecutor#query(org.apache.ibatis.mapping.MappedStatement, java.lang.Object, org.apache.ibatis.session.RowBounds, org.apache.ibatis.session.ResultHandler, org.apache.ibatis.cache.CacheKey, org.apache.ibatis.mapping.BoundSql) 的时候，可以看到 org.apache.ibatis.executor.BaseExecutor#localCache 只有一个 缓存的值的 ， 根据 getObject 方法可以跟进到 org.apache.ibatis.cache.impl.PerpetualCache#cache 中来,</p>
<p>传入进来的 key 值是 : -1896651191:1062027004:com.iyang.mybatis.mapper.BlogMapper.selectBlog:0:2147483647:select * from tb_blog where id &#x3D; ?:1:development 然后从 cache 中获取出值来, 所以这里就没有走 query 的查询语句.</p>
<p>这是命中缓存的情况.</p>
<p>下面我们来看下, 在第二次 query 之前如果执行了一个 add 方法，为什么就命中不了了呢？</p>
<p>这里可以大致猜测下，在执行完 add 方法后，是不是给 cache 给清除掉了，然后再去查询的时候，就查询不到了.</p>
<p>于是我们在 add 方法上进行 debug 查看下 :</p>
<p>最后我们 debug 跟进到这里 : org.apache.ibatis.executor.BaseExecutor#clearLocalCache 就可以发现</p>
<p>这里是有二个 clear 方法，也就是清除方法.</p>
<p>localCache.clear() —-&gt; org.apache.ibatis.cache.impl.PerpetualCache#clear 对应的就是这里的清楚方法，直接调用 HashMap 的clear 方法进行清除.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">localCache.clear();</span><br><span class="line">localOutputParameterCache.clear();</span><br></pre></td></tr></table></figure>

<p>所以这里可以看出在第二次调用 query 之前，如果是有 insert&#x2F;update&#x2F;delete 等方法的话，就会去重置这二个地方的缓存的.</p>
<p>MyBatis 的一级缓存的是跟随 SqlSession 的，这里是可以根据简单的案例效果看出来的.</p>
<p>一级缓存只是使用了一个 HashMap , 最后清除缓存的时候，也是调用 HashMap 的clear 方法</p>
<p>最后从案例三可以看出来，当多个 SqlSession 的时候，由于各自有存有各自的缓存，所以是很容易引起脏数据的, 将缓存级别设置为 Statement.</p>
<h1 id="二级缓存"><a href="#二级缓存" class="headerlink" title="二级缓存"></a><strong>二级缓存</strong></h1><p>可以看到一级缓存的话，是局限于 SqlSession . 如果要多个 sqlSession 之间共享缓存的话，就需要开启二级缓存. 开启的话,我们在 MyBatis 配置文件中加上:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;settings&gt;</span><br><span class="line">    &lt;setting name=<span class="string">&quot;logImpl&quot;</span> value=<span class="string">&quot;STDOUT_LOGGING&quot;</span>/&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 开启二级缓存 --&gt;</span><br><span class="line">    &lt;setting name=<span class="string">&quot;cacheEnabled&quot;</span> value=<span class="string">&quot;true&quot;</span>/&gt;</span><br><span class="line">&lt;/settings&gt;</span><br></pre></td></tr></table></figure>

<p><strong>案例一 : 是否提交事务</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line">    <span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">sqlSession1</span> <span class="operator">=</span> sqlSessionFactory.openSession(<span class="literal">true</span>);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">sqlSession2</span> <span class="operator">=</span> sqlSessionFactory.openSession(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">BlogMapper</span> <span class="variable">blogMapper1</span> <span class="operator">=</span> sqlSession1.getMapper(BlogMapper.class);</span><br><span class="line">    <span class="type">BlogMapper</span> <span class="variable">blogMapper2</span> <span class="operator">=</span> sqlSession2.getMapper(BlogMapper.class);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;blogMapper1 获取数据&quot;</span> + blogMapper1.selectBlog(<span class="number">1</span>));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// sqlSession1.commit();</span></span><br><span class="line">    </span><br><span class="line">    System.out.println(<span class="string">&quot;blogMapper2 获取数据&quot;</span> + blogMapper2.selectBlog(<span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//   ----------------   true结果   -----------------------</span></span><br><span class="line"></span><br><span class="line">Created connection <span class="number">492079624.</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper1 获取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">Opening JDBC Connection</span><br><span class="line">    </span><br><span class="line">Created connection <span class="number">433287555.</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper2 获取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;    </span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"><span class="comment">// ------------   加上commit()方法结果   ---------------</span></span><br><span class="line"></span><br><span class="line">Created connection <span class="number">630074945.</span></span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line">blogMapper1 获取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">Cache Hit Ratio [com.iyang.mybatis.mapper.BlogMapper]: <span class="number">0.5</span></span><br><span class="line">blogMapper2 获取数据TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>

<p><strong>案例二 : 中间穿插更新语句</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span>  <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line">    <span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">sqlSession1</span> <span class="operator">=</span> sqlSessionFactory.openSession(<span class="literal">false</span>);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">sqlSession2</span> <span class="operator">=</span> sqlSessionFactory.openSession(<span class="literal">false</span>);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">sqlSession3</span> <span class="operator">=</span> sqlSessionFactory.openSession(<span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">BlogMapper</span> <span class="variable">blogMapper1</span> <span class="operator">=</span> sqlSession1.getMapper(BlogMapper.class);</span><br><span class="line">    <span class="type">BlogMapper</span> <span class="variable">blogMapper2</span> <span class="operator">=</span> sqlSession2.getMapper(BlogMapper.class);</span><br><span class="line">    <span class="type">BlogMapper</span> <span class="variable">blogMapper3</span> <span class="operator">=</span> sqlSession3.getMapper(BlogMapper.class);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot; blogMapper1 查询出来的数据 : &quot;</span> + blogMapper1.selectBlog(<span class="number">1</span>));</span><br><span class="line">    sqlSession1.commit();</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot; blogMapper2 查询出来的结果 : &quot;</span> + blogMapper2.selectBlog(<span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">    System.out.println(blogMapper3.updateHashCode(<span class="string">&quot;GavinYang&quot;</span>));</span><br><span class="line">    sqlSession3.commit();</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot; blogMapper2 查询出来的结果 : &quot;</span> + blogMapper2.selectBlog(<span class="number">1</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  ------------------  打印结果 ------</span></span><br><span class="line"></span><br><span class="line">Created connection <span class="number">630074945.</span></span><br><span class="line">Setting autocommit to <span class="literal">false</span> on JDBC Connection [com.mysql.jdbc.JDBC4Connection@258e2e41]</span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, <span class="number">6565</span></span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line"> blogMapper1 查询出来的数据 : TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line">Cache Hit Ratio [com.iyang.mybatis.mapper.BlogMapper]: <span class="number">0.5</span></span><br><span class="line"> blogMapper2 查询出来的结果 : TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;6565&#x27;</span>&#125;</span><br><span class="line"></span><br><span class="line">Created connection <span class="number">603443293.</span></span><br><span class="line">Setting autocommit to <span class="literal">false</span> on JDBC Connection [com.mysql.jdbc.JDBC4Connection@23f7d05d]</span><br><span class="line">==&gt;  Preparing: update tb_blog <span class="type">set</span> <span class="variable">name</span> <span class="operator">=</span> ? <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> <span class="number">1</span>; </span><br><span class="line">==&gt; Parameters: GavinYang(String)</span><br><span class="line">&lt;==    Updates: <span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line">Committing JDBC Connection [com.mysql.jdbc.JDBC4Connection@23f7d05d]</span><br><span class="line">Cache Hit Ratio [com.iyang.mybatis.mapper.BlogMapper]: <span class="number">0.3333333333333333</span></span><br><span class="line">Opening JDBC Connection</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">Created connection <span class="number">707976812.</span></span><br><span class="line">Setting autocommit to <span class="literal">false</span> on JDBC Connection [com.mysql.jdbc.JDBC4Connection@2a32de6c]</span><br><span class="line">==&gt;  Preparing: select * from tb_blog <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> ? </span><br><span class="line">==&gt; Parameters: <span class="number">1</span>(Integer)</span><br><span class="line">&lt;==    Columns: id, name</span><br><span class="line">&lt;==        Row: <span class="number">1</span>, GavinYang</span><br><span class="line">&lt;==      Total: <span class="number">1</span></span><br><span class="line"> blogMapper2 查询出来的结果 : TbBlog&#123;id=<span class="number">1</span>, name=<span class="string">&#x27;GavinYang&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>这里是可以看到在更新之后并且 commit 了事务之后，后面紧跟的 sql 是去查询 数据库了的. 所以这里是可以看出来，update等操作是会去 清空对应的缓存的。</p>
<p>这里我们根据 案例一 的情况来分析，在开启了 二级缓存 的时候，是从哪里获取出来的数据的呢？</p>
<p>debug 跟进来 : org.apache.ibatis.executor.CachingExecutor#query(org.apache.ibatis.mapping.MappedStatement, java.lang.Object, org.apache.ibatis.session.RowBounds, org.apache.ibatis.session.ResultHandler, org.apache.ibatis.cache.CacheKey, org.apache.ibatis.mapping.BoundSql)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> &lt;E&gt; List&lt;E&gt; <span class="title function_">query</span><span class="params">(MappedStatement ms, Object parameterObject, RowBounds rowBounds, ResultHandler resultHandler, CacheKey key, BoundSql boundSql)</span></span><br><span class="line">    <span class="keyword">throws</span> SQLException &#123;</span><br><span class="line">  <span class="type">Cache</span> <span class="variable">cache</span> <span class="operator">=</span> ms.getCache();</span><br><span class="line">  <span class="keyword">if</span> (cache != <span class="literal">null</span>) &#123;</span><br><span class="line">    flushCacheIfRequired(ms);</span><br><span class="line">    <span class="keyword">if</span> (ms.isUseCache() &amp;&amp; resultHandler == <span class="literal">null</span>) &#123;</span><br><span class="line">      ensureNoOutParams(ms, boundSql);</span><br><span class="line">      <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line"><span class="comment">// debug 到这里，可以看到,就已经返回了我们需要的数据.        </span></span><br><span class="line">      List&lt;E&gt; list = (List&lt;E&gt;) tcm.getObject(cache, key);</span><br><span class="line">      <span class="keyword">if</span> (list == <span class="literal">null</span>) &#123;</span><br><span class="line">        list = delegate.query(ms, parameterObject, rowBounds, resultHandler, key, boundSql);</span><br><span class="line">        tcm.putObject(cache, key, list); <span class="comment">// issue #578 and #116</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> list;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> delegate.query(ms, parameterObject, rowBounds, resultHandler, key, boundSql);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>org.apache.ibatis.executor.CachingExecutor#tcm 调用这个对象的 getObject 方法获取到了我们需要的值, 跟进来又从 org.apache.ibatis.cache.decorators.TransactionalCache 的 getObject 获取出我们的值, 最后从 org.apache.ibatis.cache.decorators.TransactionalCache#delegate 获取出值, 返回回来的.</p>
<p>org.apache.ibatis.cache.decorators.TransactionalCache#getObject</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Object <span class="title function_">getObject</span><span class="params">(Object key)</span> &#123;</span><br><span class="line">  <span class="comment">// issue #116</span></span><br><span class="line"><span class="comment">// 从缓存中获取出值.    </span></span><br><span class="line">  <span class="type">Object</span> <span class="variable">object</span> <span class="operator">=</span> delegate.getObject(key);</span><br><span class="line">  <span class="keyword">if</span> (object == <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// 如果获取出来是null,也就是缓存中没有的话,org.apache.ibatis.cache.decorators.TransactionalCache#entriesMissedInCache 就添加到这个集合中来.      </span></span><br><span class="line">    entriesMissedInCache.add(key);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// issue #146</span></span><br><span class="line"><span class="comment">// commit 后需要 clear 的话，就会返回 null.</span></span><br><span class="line"><span class="comment">// 这里想下这个变量会不会和我门案例二中的 update 操作有关系呢？</span></span><br><span class="line"><span class="comment">// 这里再 update后再 debug 发现,  delegate 中获取出来的是 null ,也就是确实是获取不到缓存了</span></span><br><span class="line"><span class="comment">// 和这个参数没关系.    </span></span><br><span class="line">  <span class="keyword">if</span> (clearOnCommit) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> object;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>MyBatis 二级缓存不适应于配置文件中存在多表查询的情况. 一般我们是单表的 cache, 由于 mybatis 的二级缓存是基于 namespace 的, 多表查询语句所在的 namespace 无法感应到其他的 namespace 中的语句对多表中设计修改，就会引发脏数据. 这个时候，可以采用 cache-ref 来做处理，但是这样的话,缓存的颗粒度就变粗了.</p>
<p>执行流程 : 如果开启了二级缓存的话， MyBatis 会先走二级缓存，如果二级缓存没有的话，就会去一级缓存看看，如果都没有的话，就去查询数据库.</p>
<p>二级缓存 : 用 org.apache.ibatis.executor.CachingExecutor 装饰了 org.apache.ibatis.executor.BaseExecutor 的子类, 委托具体职责给 delegate 之前，实现了二级缓存的查询和写入功能.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>最后看 一级缓存和二级缓存，都是利用的 HashMap 这种来做到本地缓存， 只是二级缓存的作用范围比起一级缓存的话，是要大的，并且也利用了一些 装饰者 等设计模式来设计二级缓存的.</p>
<p>如果是部署的分布式项目的话，那么还是 得切换到 redis 这种缓存来了， 本地利用 HashMap 这种缓存满足不了的.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://tech.meituan.com/2018/01/19/mybatis-cache.html">https://tech.meituan.com/2018/01/19/mybatis-cache.html</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>mybatis中mapper文件阅读</title>
    <url>//article/mybatis_mapper_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 <strong>题记</strong></h1><p>MyBatis是如何对 Mapper 文件中的sql进行处理呢？ 虽然上篇解析 mybatis-config.xml 是有进行说明的, 但是应该拿出来单独仔细解析下. 因为这个里面涉及到动态sql, 加上mapper文件自身也有很多标签内容,然后MyBatis是怎么读取出这些内容的呢？读取出来后,又是做了怎么样的处理, 然后达到了sql那种执行效果的呢？</p>
<p>意思也就是,Mapper + 动态sql , 内容还是有点多的, 并且也很重要, 是非常有必要拿出来单独的仔细讲解下的.</p>
<h1 id="Target"><a href="#Target" class="headerlink" title="Target"></a><strong>Target</strong></h1><p>在之前对标签的进行解析的时候,是有对 标签进行一个初步的解析. 然后里面其实是很多内容还没填补很详细,所以特意记录下对 详细操作的. 那么，下文就开始操作吧.</p>
<h2 id="org-apache-ibatis-builder-xml-XMLMapperBuilder-parse"><a href="#org-apache-ibatis-builder-xml-XMLMapperBuilder-parse" class="headerlink" title="org.apache.ibatis.builder.xml.XMLMapperBuilder#parse"></a><strong>org.apache.ibatis.builder.xml.XMLMapperBuilder#parse</strong></h2><p>主要来看这段解析的代码 :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">parse</span><span class="params">()</span> &#123;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 利用 org.apache.ibatis.session.Configuration 的 loadedResources</span></span><br><span class="line"><span class="comment">// 来判断是不是已经加载过了的.    </span></span><br><span class="line">  <span class="keyword">if</span> (!configuration.isResourceLoaded(resource)) &#123;</span><br><span class="line">    configurationElement(parser.evalNode(<span class="string">&quot;/mapper&quot;</span>));</span><br><span class="line"><span class="comment">// 这里添加到 loadedResources 中来,也就是用来控制是不是已经解析过了的.      </span></span><br><span class="line">    configuration.addLoadedResource(resource);</span><br><span class="line">    bindMapperForNamespace();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这三个方法给我一种好像解析那种没有还没解析完的 ? 这个地方有待完善.    </span></span><br><span class="line">  parsePendingResultMaps();</span><br><span class="line">  parsePendingCacheRefs();</span><br><span class="line">  parsePendingStatements();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// configurationElement 方法,</span></span><br><span class="line"><span class="comment">// 可以看到这个方法中,很多标签(namespace/parameterMa/resultMap/sql)</span></span><br><span class="line"><span class="comment">// 还有下面的select/insert/update/delete</span></span><br><span class="line"><span class="comment">// 这些熟悉的标签</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">configurationElement</span><span class="params">(XNode context)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="type">String</span> <span class="variable">namespace</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;namespace&quot;</span>);</span><br><span class="line">      <span class="keyword">if</span> (namespace == <span class="literal">null</span> || namespace.equals(<span class="string">&quot;&quot;</span>)) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Mapper&#x27;s namespace cannot be empty&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 将 namespace 赋值进去,也就是当前正在解析的 namespace.        </span></span><br><span class="line">      builderAssistant.setCurrentNamespace(namespace);</span><br><span class="line">        </span><br><span class="line"><span class="comment">// 这里是对缓存标签进行解析.        </span></span><br><span class="line">      cacheRefElement(context.evalNode(<span class="string">&quot;cache-ref&quot;</span>));</span><br><span class="line">      cacheElement(context.evalNode(<span class="string">&quot;cache&quot;</span>));</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 解析 parameterMap标签        </span></span><br><span class="line">      parameterMapElement(context.evalNodes(<span class="string">&quot;/mapper/parameterMap&quot;</span>));</span><br><span class="line">        </span><br><span class="line"><span class="comment">//         </span></span><br><span class="line">      resultMapElements(context.evalNodes(<span class="string">&quot;/mapper/resultMap&quot;</span>));</span><br><span class="line">      sqlElement(context.evalNodes(<span class="string">&quot;/mapper/sql&quot;</span>));</span><br><span class="line">      buildStatementFromContext(context.evalNodes(<span class="string">&quot;select|insert|update|delete&quot;</span>));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error parsing Mapper XML. The XML location is &#x27;&quot;</span> + resource + <span class="string">&quot;&#x27;. Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="resultMapElements-方法"><a href="#resultMapElements-方法" class="headerlink" title="resultMapElements 方法 :"></a><strong>resultMapElements 方法 :</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里的 list 是 xml 文件中的所有 &lt;resultMap&gt; 标签文件.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">resultMapElements</span><span class="params">(List&lt;XNode&gt; list)</span> &#123;</span><br><span class="line">  <span class="keyword">for</span> (XNode resultMapNode : list) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">//  有点好奇,该方法返回的 ResultMap 这边好像并没有参数,有点尴尬.</span></span><br><span class="line"><span class="comment">//  不过是已经存储在 org.apache.ibatis.session.Configuration#resultMaps 中.       </span></span><br><span class="line">      resultMapElement(resultMapNode);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IncompleteElementException e) &#123;</span><br><span class="line">      <span class="comment">// ignore, it will be retried</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">----------</span><br><span class="line"><span class="comment">// 最后跟进到这个方法中来.</span></span><br><span class="line">  <span class="keyword">private</span> ResultMap <span class="title function_">resultMapElement</span><span class="params">(XNode resultMapNode, List&lt;ResultMapping&gt; additionalResultMappings, Class&lt;?&gt; enclosingType)</span> &#123;</span><br><span class="line">    ErrorContext.instance().activity(<span class="string">&quot;processing &quot;</span> + resultMapNode.getValueBasedIdentifier());</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 获取出 type , 这里我们获取出来的 type 是 TbBlog.    </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">type</span> <span class="operator">=</span> resultMapNode.getStringAttribute(<span class="string">&quot;type&quot;</span>,</span><br><span class="line">        resultMapNode.getStringAttribute(<span class="string">&quot;ofType&quot;</span>,</span><br><span class="line">            resultMapNode.getStringAttribute(<span class="string">&quot;resultType&quot;</span>,</span><br><span class="line">                resultMapNode.getStringAttribute(<span class="string">&quot;javaType&quot;</span>))));</span><br><span class="line"><span class="comment">// 先判断 org.apache.ibatis.type.TypeAliasRegistry#typeAliases 中有没有,</span></span><br><span class="line"><span class="comment">// 如果没有的话,就会自己new一个出来.    </span></span><br><span class="line">    Class&lt;?&gt; typeClass = resolveClass(type);</span><br><span class="line">    <span class="keyword">if</span> (typeClass == <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// TODO,如果没有话?        </span></span><br><span class="line">      typeClass = inheritEnclosingType(resultMapNode, enclosingType);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">Discriminator</span> <span class="variable">discriminator</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    List&lt;ResultMapping&gt; resultMappings = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(additionalResultMappings);</span><br><span class="line">    </span><br><span class="line"> <span class="comment">// 获取该 &lt;resultMap&gt; 下的子标签</span></span><br><span class="line"><span class="comment">// 那么这里也就是获取 &lt;id&gt; 和 &lt;result&gt; 这二个.    </span></span><br><span class="line">    List&lt;XNode&gt; resultChildren = resultMapNode.getChildren();</span><br><span class="line">    <span class="keyword">for</span> (XNode resultChild : resultChildren) &#123;</span><br><span class="line"><span class="comment">// 分为 constructor / discriminator / 其他 这三类情况        </span></span><br><span class="line">      <span class="keyword">if</span> (<span class="string">&quot;constructor&quot;</span>.equals(resultChild.getName())) &#123;</span><br><span class="line">        processConstructorElement(resultChild, typeClass, resultMappings);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;discriminator&quot;</span>.equals(resultChild.getName())) &#123;</span><br><span class="line">        discriminator = processDiscriminatorElement(resultChild, typeClass, resultMappings);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">// 非前二者情况.         </span></span><br><span class="line">        List&lt;ResultFlag&gt; flags = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">&quot;id&quot;</span>.equals(resultChild.getName())) &#123;</span><br><span class="line">         <span class="comment">// 如果标签是id的话,就会给flags添加ResultFlag.ID.</span></span><br><span class="line">          flags.add(ResultFlag.ID);</span><br><span class="line">        &#125;</span><br><span class="line">  <span class="comment">//  将返回回来的 ResultMapping 添加进来.       </span></span><br><span class="line">        resultMappings.add(buildResultMappingFromContext(resultChild, typeClass, flags));</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 这里获取的是 &lt;resultMap&gt; 标签的 id 字段.    </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> resultMapNode.getStringAttribute(<span class="string">&quot;id&quot;</span>,</span><br><span class="line">            resultMapNode.getValueBasedIdentifier());</span><br><span class="line"><span class="comment">// 这里还可以使用 extends 属性, 不是看到这里, 都好奇还有这种标签.    </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">extend</span> <span class="operator">=</span> resultMapNode.getStringAttribute(<span class="string">&quot;extends&quot;</span>);</span><br><span class="line">    <span class="type">Boolean</span> <span class="variable">autoMapping</span> <span class="operator">=</span> resultMapNode.getBooleanAttribute(<span class="string">&quot;autoMapping&quot;</span>);</span><br><span class="line"><span class="comment">// 这里 new 了一个 ResultMapResolver 对象.   </span></span><br><span class="line">    <span class="type">ResultMapResolver</span> <span class="variable">resultMapResolver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ResultMapResolver</span>(builderAssistant, id, typeClass, extend, discriminator, resultMappings, autoMapping);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 这里最后就是 new 了一个 ResultMap 对象, 该对象的 id 是 namespace + 方法ID 拼接.</span></span><br><span class="line"><span class="comment">// 然后将该对象给添加到  org.apache.ibatis.session.Configuration#resultMaps 中来,</span></span><br><span class="line"><span class="comment">// key 就是其id, 最后就是根据 local / global 来分别进行二种情况检查.        </span></span><br><span class="line">      <span class="keyword">return</span> resultMapResolver.resolve();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IncompleteElementException  e) &#123;</span><br><span class="line">      configuration.addIncompleteResultMap(resultMapResolver);</span><br><span class="line">      <span class="keyword">throw</span> e;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">//  buildResultMappingFromContext 方法</span></span><br><span class="line"><span class="comment">// 该方法是对 resultMap 中的字段进行解析.</span></span><br><span class="line">  <span class="keyword">private</span> ResultMapping <span class="title function_">buildResultMappingFromContext</span><span class="params">(XNode context, Class&lt;?&gt; resultType, List&lt;ResultFlag&gt; flags)</span> &#123;</span><br><span class="line">    String property;</span><br><span class="line">    <span class="keyword">if</span> (flags.contains(ResultFlag.CONSTRUCTOR)) &#123;</span><br><span class="line">      property = context.getStringAttribute(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      property = context.getStringAttribute(<span class="string">&quot;property&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">String</span> <span class="variable">column</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;column&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">javaType</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;javaType&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">jdbcType</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;jdbcType&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">nestedSelect</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;select&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">nestedResultMap</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultMap&quot;</span>, () -&gt;</span><br><span class="line">      processNestedResultMappings(context, Collections.emptyList(), resultType));</span><br><span class="line">    <span class="type">String</span> <span class="variable">notNullColumn</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;notNullColumn&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">columnPrefix</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;columnPrefix&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">typeHandler</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;typeHandler&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">resultSet</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultSet&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">foreignColumn</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;foreignColumn&quot;</span>);</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">lazy</span> <span class="operator">=</span> <span class="string">&quot;lazy&quot;</span>.equals(context.getStringAttribute(<span class="string">&quot;fetchType&quot;</span>, configuration.isLazyLoadingEnabled() ? <span class="string">&quot;lazy&quot;</span> : <span class="string">&quot;eager&quot;</span>));</span><br><span class="line">      </span><br><span class="line"><span class="comment">// 获取 javaType , typeHandler , jdbcType 等对象.      </span></span><br><span class="line">    Class&lt;?&gt; javaTypeClass = resolveClass(javaType);</span><br><span class="line">    Class&lt;? <span class="keyword">extends</span> <span class="title class_">TypeHandler</span>&lt;?&gt;&gt; typeHandlerClass = resolveClass(typeHandler);</span><br><span class="line">    <span class="type">JdbcType</span> <span class="variable">jdbcTypeEnum</span> <span class="operator">=</span> resolveJdbcType(jdbcType);</span><br><span class="line"><span class="comment">// org.apache.ibatis.builder.MapperBuilderAssistant#buildResultMapping(java.lang.Class&lt;?&gt;, java.lang.String, java.lang.String, java.lang.Class&lt;?&gt;, org.apache.ibatis.type.JdbcType, java.lang.String, java.lang.String, java.lang.String, java.lang.String, java.lang.Class&lt;? extends org.apache.ibatis.type.TypeHandler&lt;?&gt;&gt;, java.util.List&lt;org.apache.ibatis.mapping.ResultFlag&gt;, java.lang.String, java.lang.String, boolean)</span></span><br><span class="line"><span class="comment">// 可以看到这里传递进来的参数还是很多的.</span></span><br><span class="line"><span class="comment">// 最后返回 ResultMapping 对象,也就是说这么多参数&amp;buildResultMapping方法中的参数,</span></span><br><span class="line"><span class="comment">//都设置到该对象中来了.     </span></span><br><span class="line">    <span class="keyword">return</span> builderAssistant.buildResultMapping(resultType, property, column, javaTypeClass, jdbcTypeEnum, nestedSelect, nestedResultMap, notNullColumn, columnPrefix, typeHandlerClass, flags, resultSet, foreignColumn, lazy);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="SqlElement-方法"><a href="#SqlElement-方法" class="headerlink" title="SqlElement 方法"></a><strong>SqlElement 方法</strong></h2><p>该方法可以很明显的感受到是对 标签进行解析的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">sqlElement</span><span class="params">(List&lt;XNode&gt; list)</span> &#123;</span><br><span class="line"> <span class="comment">// configuration 获取出来 dataBaseId是null,跳过此方法  </span></span><br><span class="line">  <span class="keyword">if</span> (configuration.getDatabaseId() != <span class="literal">null</span>) &#123;</span><br><span class="line">    sqlElement(list, configuration.getDatabaseId());</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">//    </span></span><br><span class="line">  sqlElement(list, <span class="literal">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有点好奇写代码风格:  sqlElement(list,configuration.getDatabaseId());</span></span><br><span class="line">------------------------</span><br><span class="line"></span><br><span class="line"><span class="comment">//  sqlElement 方法</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">sqlElement</span><span class="params">(List&lt;XNode&gt; list, String requiredDatabaseId)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (XNode context : list) &#123;</span><br><span class="line">        </span><br><span class="line"> <span class="comment">// 获取 databaseId 和 id 这二个属性的值.       </span></span><br><span class="line">      <span class="type">String</span> <span class="variable">databaseId</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;databaseId&quot;</span>);</span><br><span class="line">      <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;id&quot;</span>);</span><br><span class="line"> <span class="comment">//  org.apache.ibatis.builder.MapperBuilderAssistant#applyCurrentNamespace   </span></span><br><span class="line"> <span class="comment">// 该方法最后返回的id的值是: namespace + id       </span></span><br><span class="line">      id = builderAssistant.applyCurrentNamespace(id, <span class="literal">false</span>);</span><br><span class="line"><span class="comment">//sqlFragments 不包含该id就返回true,也就说该Map是确定是否已经解析过了的.         </span></span><br><span class="line">      <span class="keyword">if</span> (databaseIdMatchesCurrent(id, databaseId, requiredDatabaseId)) &#123;</span><br><span class="line"><span class="comment">// 添加到 org.apache.ibatis.builder.xml.XMLMapperBuilder#sqlFragments 中来.          </span></span><br><span class="line">        sqlFragments.put(id, context);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>最后 解析后的值,是使用 namespace + id 存放在 org.apache.ibatis.builder.xml.XMLMapperBuilder#sqlFragments 的属性中的.</p>
<h2 id="buildStatementFromContext-方法"><a href="#buildStatementFromContext-方法" class="headerlink" title="buildStatementFromContext() 方法 :"></a><strong>buildStatementFromContext() 方法 :</strong></h2><p>这里是对 select &#x2F; insert &#x2F; update &#x2F; delete 标签进行解析.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">buildStatementFromContext(context.evalNodes(<span class="string">&quot;select|insert|update|delete&quot;</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以看到 databaseId 的获取与 sql 标签是一样的操作</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">buildStatementFromContext</span><span class="params">(List&lt;XNode&gt; list)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (configuration.getDatabaseId() != <span class="literal">null</span>) &#123;</span><br><span class="line">    buildStatementFromContext(list, configuration.getDatabaseId());</span><br><span class="line">  &#125;</span><br><span class="line">  buildStatementFromContext(list, <span class="literal">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 所以我们可以跟进到这个方法来.</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">buildStatementFromContext</span><span class="params">(List&lt;XNode&gt; list, String requiredDatabaseId)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (XNode context : list) &#123;</span><br><span class="line"><span class="comment">// 先 new 了一个 XMLStatementBuilder 对象, 紧接着就调用该对象的 解析 方法.        </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">XMLStatementBuilder</span> <span class="variable">statementParser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLStatementBuilder</span>(configuration, builderAssistant, context, requiredDatabaseId);</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        statementParser.parseStatementNode();</span><br><span class="line">      &#125; <span class="keyword">catch</span> (IncompleteElementException e) &#123;</span><br><span class="line">        configuration.addIncompleteStatement(statementParser);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 解析方法</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">parseStatementNode</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;id&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">databaseId</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;databaseId&quot;</span>);</span><br><span class="line"><span class="comment">// 用 namespace + id 组合为 id</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.session.Configuration#mappedStatements</span></span><br><span class="line"><span class="comment">// 接着就是判断在 mappedStatements 中是不是有该id,如果不存在就返回ture,</span></span><br><span class="line"><span class="comment">// 存在就返回false,这里也就会直接return出去,也就是不会往后面执行了.      </span></span><br><span class="line">    <span class="keyword">if</span> (!databaseIdMatchesCurrent(id, databaseId, <span class="built_in">this</span>.requiredDatabaseId)) &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 获取标签名字,  select / insert/ update /delete.</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">nodeName</span> <span class="operator">=</span> context.getNode().getNodeName();</span><br><span class="line"><span class="comment">// 转化为大写的 SELECT      </span></span><br><span class="line">    <span class="type">SqlCommandType</span> <span class="variable">sqlCommandType</span> <span class="operator">=</span> SqlCommandType.valueOf(nodeName.toUpperCase(Locale.ENGLISH));</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">isSelect</span> <span class="operator">=</span> sqlCommandType == SqlCommandType.SELECT;</span><br><span class="line"><span class="comment">// 是否刷新 cache,也就是select是不刷新的,那么其他的就应该是要刷新的.      </span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">flushCache</span> <span class="operator">=</span> context.getBooleanAttribute(<span class="string">&quot;flushCache&quot;</span>, !isSelect);</span><br><span class="line"><span class="comment">// 使用使用 cache,这里应该是一级缓存，默认开启的.      </span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">useCache</span> <span class="operator">=</span> context.getBooleanAttribute(<span class="string">&quot;useCache&quot;</span>, isSelect);</span><br><span class="line"><span class="comment">// 结果排序, 如果没有配置的话,默认就是false.      </span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">resultOrdered</span> <span class="operator">=</span> context.getBooleanAttribute(<span class="string">&quot;resultOrdered&quot;</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Include Fragments before parsing</span></span><br><span class="line"><span class="comment">// 创建了一个 XMLIncludeTransformer 对象,该对象应该是进行转化的.      </span></span><br><span class="line">    <span class="type">XMLIncludeTransformer</span> <span class="variable">includeParser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLIncludeTransformer</span>(configuration, builderAssistant);</span><br><span class="line"> <span class="comment">//  TODO ? 该方法有待更新     </span></span><br><span class="line">    includeParser.applyIncludes(context.getNode());</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取 parameterType 属性,如果有的话,也会获取出该属性对应的 Class.    </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">parameterType</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;parameterType&quot;</span>);</span><br><span class="line">    Class&lt;?&gt; parameterTypeClass = resolveClass(parameterType);</span><br><span class="line"></span><br><span class="line"><span class="comment">// lang : null,这里是没有设置的.      </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">lang</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;lang&quot;</span>);</span><br><span class="line">    <span class="type">LanguageDriver</span> <span class="variable">langDriver</span> <span class="operator">=</span> getLanguageDriver(lang);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Parse selectKey after includes and remove them.</span></span><br><span class="line"><span class="comment">// 这里对是否有 selectKey 进行处理.我们这里目前没有使用 selectKey</span></span><br><span class="line">    processSelectKeyNodes(id, parameterTypeClass, langDriver);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Parse the SQL (pre: &lt;selectKey&gt; and &lt;include&gt; were parsed and removed)</span></span><br><span class="line">    KeyGenerator keyGenerator;</span><br><span class="line"> <span class="comment">// selectBlog!selectKey     </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">keyStatementId</span> <span class="operator">=</span> id + SelectKeyGenerator.SELECT_KEY_SUFFIX;</span><br><span class="line"><span class="comment">// 这里拼接上 namespace :  com.iyang.mybatis.mapper.BlogMapper.selectBlog!selectKey      </span></span><br><span class="line">    keyStatementId = builderAssistant.applyCurrentNamespace(keyStatementId, <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里是判断是否有 主键自动生成. 这里是查询语句,应该是没有的. </span></span><br><span class="line">    <span class="keyword">if</span> (configuration.hasKeyGenerator(keyStatementId)) &#123;</span><br><span class="line">      keyGenerator = configuration.getKeyGenerator(keyStatementId);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      keyGenerator = context.getBooleanAttribute(<span class="string">&quot;useGeneratedKeys&quot;</span>,</span><br><span class="line">          configuration.isUseGeneratedKeys() &amp;&amp; SqlCommandType.INSERT.equals(sqlCommandType))</span><br><span class="line">          ? Jdbc3KeyGenerator.INSTANCE : NoKeyGenerator.INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建一个 XMLScriptBuilder 对象,使用该对象的parseScriptNode方法来解析</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.scripting.xmltags.XMLScriptBuilder#parseScriptNode</span></span><br><span class="line"><span class="comment">// 获取出sql, 有个 isDynamic 参数,来判断是不是动态sql语句.</span></span><br><span class="line"><span class="comment">// 这里不是动态sql,最后new了一个RawSqlSource.</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.builder.SqlSourceBuilder#parse,我们的#&#123;id&#125; 替换成 ? 就是在</span></span><br><span class="line"><span class="comment">// 这里进行替换的.</span></span><br><span class="line"><span class="comment">// 如果是动态 sql 的话,就会创建出 DynamicSqlSource 该对象来.</span></span><br><span class="line"><span class="comment">// 可以看到 SqlSource 下面是有 四个实现类的.</span></span><br><span class="line"><span class="comment">// 这里返回的 SqlSource里面有sql语句的,和返回类型的.      </span></span><br><span class="line">    <span class="type">SqlSource</span> <span class="variable">sqlSource</span> <span class="operator">=</span> langDriver.createSqlSource(configuration, context, parameterTypeClass);</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 获取属性.      </span></span><br><span class="line">    <span class="type">StatementType</span> <span class="variable">statementType</span> <span class="operator">=</span> StatementType.valueOf(context.getStringAttribute(<span class="string">&quot;statementType&quot;</span>, StatementType.PREPARED.toString()));</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">fetchSize</span> <span class="operator">=</span> context.getIntAttribute(<span class="string">&quot;fetchSize&quot;</span>);</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">timeout</span> <span class="operator">=</span> context.getIntAttribute(<span class="string">&quot;timeout&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">parameterMap</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;parameterMap&quot;</span>);</span><br><span class="line"><span class="comment">// 获取返回类型. 获取出来的 resultTypeClass 是 class com.iyang.mybatis.pojo.TbBlog      </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">resultType</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultType&quot;</span>);</span><br><span class="line">    Class&lt;?&gt; resultTypeClass = resolveClass(resultType);</span><br><span class="line">    <span class="type">String</span> <span class="variable">resultMap</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultMap&quot;</span>);      </span><br><span class="line">    <span class="type">String</span> <span class="variable">resultSetType</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultSetType&quot;</span>);</span><br><span class="line">    <span class="type">ResultSetType</span> <span class="variable">resultSetTypeEnum</span> <span class="operator">=</span> resolveResultSetType(resultSetType);</span><br><span class="line">    <span class="keyword">if</span> (resultSetTypeEnum == <span class="literal">null</span>) &#123;</span><br><span class="line">      resultSetTypeEnum = configuration.getDefaultResultSetType();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 获取属性的值      </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">keyProperty</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;keyProperty&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">keyColumn</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;keyColumn&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">resultSets</span> <span class="operator">=</span> context.getStringAttribute(<span class="string">&quot;resultSets&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建一个 MappedStatement.Builder 对象出来.</span></span><br><span class="line"><span class="comment">// 再通过 builder 构建出一个 MappedStatement 对象来.</span></span><br><span class="line"><span class="comment">// 最后放入到 org.apache.ibatis.session.Configuration#mappedStatements 中来.      </span></span><br><span class="line">    builderAssistant.addMappedStatement(id, sqlSource, statementType, sqlCommandType,</span><br><span class="line">        fetchSize, timeout, parameterMap, parameterTypeClass, resultMap, resultTypeClass,</span><br><span class="line">        resultSetTypeEnum, flushCache, useCache, resultOrdered,</span><br><span class="line">        keyGenerator, keyProperty, keyColumn, databaseId, langDriver, resultSets);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">processSelectKeyNodes</span><span class="params">(String id, Class&lt;?&gt; parameterTypeClass, LanguageDriver langDriver)</span> &#123;</span><br><span class="line">    List&lt;XNode&gt; selectKeyNodes = context.evalNodes(<span class="string">&quot;selectKey&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (configuration.getDatabaseId() != <span class="literal">null</span>) &#123;</span><br><span class="line">      parseSelectKeyNodes(id, selectKeyNodes, parameterTypeClass, langDriver, configuration.getDatabaseId());</span><br><span class="line">    &#125;</span><br><span class="line">    parseSelectKeyNodes(id, selectKeyNodes, parameterTypeClass, langDriver, <span class="literal">null</span>);</span><br><span class="line">    removeSelectKeyNodes(selectKeyNodes);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结下 MyBatis 解析 Mapper的xml 文件流程。 可以感受到,对于Mybatis处理Mapper,对其字段属性都是挨个解析的,还是下了很大的功夫.</p>
<p>先是有一个集合来控制是否已经解析过了,算是一种是否解析的开关配置. 可以看到其先后的解析顺序,</p>
<p>namespace –&gt; cache-ref –&gt; cache —&gt; mapper&#x2F;parameterMap —&gt; mapper&#x2F;resultMap —&gt; mapper&#x2F;sql —&gt; select&#x2F;insert&#x2F;update&#x2F;detele.</p>
<p>当解析这些标签的时候, 又会对标签里面的属性进行解析. 这里,主要看下我们平常使用到最多的标签, MyBatis 对这些标签解析了后,其后有是怎么利用的呢？可以看到目前MyBatis是存放在一些configuration等类信息里面,那么等到真正去查询sql语句的时候, MyBatis 又是怎么用上的呢？ 这里目前只讲了如何解析.</p>
<p>解析完了，没异常，那就是解析都ok了，剩下的就是看当 MyBatis 去查询的时候, 是怎么利用上这些资源的呢？所以看接下来的更新.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis源码阅读搭建</title>
    <url>//article/mybatis_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-MyBatis"><a href="#📝-MyBatis" class="headerlink" title="📝 MyBatis"></a>📝 MyBatis</h1><p> 作为一名 “优秀的” CRUD 工程师,如果不看MyBatis是个怎么大概的执行流程的话,那还真是有点可惜. 所以阅读一下其源码和执行流程,是很有帮助的. 同时,看下其代码(设计模式等对成长也是很有帮助的).</p>
<h1 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h1><p>github 源码地址:<a href="https://github.com/mybatis/mybatis-3">https://github.com/mybatis/mybatis-3</a></p>
<p>官网文档地址： <a href="https://mybatis.org/mybatis-3/zh/configuration.html#properties">https://mybatis.org/mybatis-3/zh/configuration.html#properties</a></p>
<p> 先将github上的代码clone下来,导入到本地的idea中. 然后按照官方文档的demo搭建就可以了. 这里自行创建一个 resources 目录即可,也就是放置一些配置文件信息的</p>
<h2 id="实体类"><a href="#实体类" class="headerlink" title="实体类"></a>实体类</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Phone</span> &#123;</span><br><span class="line">  <span class="keyword">private</span> Integer id;</span><br><span class="line">  <span class="keyword">private</span> String phone;</span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">Phone</span><span class="params">()</span> &#123;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">Phone</span><span class="params">(Integer id, String phone)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.id = id;</span><br><span class="line">    <span class="built_in">this</span>.phone = phone;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> Integer <span class="title function_">getId</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> id;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setId</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.id = id;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> String <span class="title function_">getPhone</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> phone;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setPhone</span><span class="params">(String phone)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.phone = phone;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Phone&#123;&quot;</span> + <span class="string">&quot;id=&quot;</span> + id + <span class="string">&quot;, phone=&#x27;&quot;</span> + phone + <span class="string">&#x27;\&#x27;&#x27;</span> + <span class="string">&#x27;&#125;&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><p>再写一个Mapper的接口. 可以看到我这里使用的方法都没修改,不能遗忘 Ctrl + C&#x2F;V大师功能</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">PhoneMapper</span> &#123;  Phone <span class="title function_">selectBlog</span><span class="params">(Integer id)</span>;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><p>MyBatis的 xml 配置文件来一份: resouces下创建:mybatis-config.xml</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="line">&lt;!DOCTYPE configuration</span><br><span class="line">  PUBLIC <span class="string">&quot;-//mybatis.org//DTD Config 3.0//EN&quot;</span></span><br><span class="line">  <span class="string">&quot;http://mybatis.org/dtd/mybatis-3-config.dtd&quot;</span>&gt;</span><br><span class="line">&lt;configuration&gt;</span><br><span class="line">  &lt;environments <span class="keyword">default</span>=<span class="string">&quot;development&quot;</span>&gt;</span><br><span class="line">    &lt;environment id=<span class="string">&quot;development&quot;</span>&gt;</span><br><span class="line">      &lt;transactionManager type=<span class="string">&quot;JDBC&quot;</span>/&gt;</span><br><span class="line">      &lt;dataSource type=<span class="string">&quot;POOLED&quot;</span>&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;driver&quot;</span> value=<span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>/&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;url&quot;</span> value=<span class="string">&quot;jdbc:mysql://127.0.0.1:3306/two&quot;</span>/&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;username&quot;</span> value=<span class="string">&quot;root&quot;</span>/&gt;</span><br><span class="line">        &lt;property name=<span class="string">&quot;password&quot;</span> value=<span class="string">&quot;root&quot;</span>/&gt;</span><br><span class="line">      &lt;/dataSource&gt;</span><br><span class="line">    &lt;/environment&gt;</span><br><span class="line">  &lt;/environments&gt;</span><br><span class="line">  &lt;mappers&gt;</span><br><span class="line">    &lt;mapper resource=<span class="string">&quot;pojo/PhoneMapper.xml&quot;</span>/&gt;</span><br><span class="line">  &lt;/mappers&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>

<h2 id="实体对应的xml文件"><a href="#实体对应的xml文件" class="headerlink" title="实体对应的xml文件"></a>实体对应的xml文件</h2><p>对用的pojo的Mapper的xml也给来一份 : pojo&#x2F;PhoneMapper.xml , 这里是在 resouces下的pojo文件夹中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="line">&lt;!DOCTYPE mapper</span><br><span class="line">  PUBLIC <span class="string">&quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;</span></span><br><span class="line">  <span class="string">&quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;</span>&gt;</span><br><span class="line">&lt;mapper namespace=<span class="string">&quot;org.apache.ibatis.mapper.PhoneMapper&quot;</span>&gt;</span><br><span class="line">  &lt;select id=<span class="string">&quot;selectBlog&quot;</span> resultType=<span class="string">&quot;org.apache.ibatis.pojo.Phone&quot;</span>&gt;</span><br><span class="line">    select * from phone <span class="type">where</span> <span class="variable">id</span> <span class="operator">=</span> #&#123;id&#125;</span><br><span class="line">  &lt;/select&gt;</span><br><span class="line">&lt;/mapper&gt;</span><br></pre></td></tr></table></figure>

<h2 id="启动类"><a href="#启动类" class="headerlink" title="启动类"></a>启动类</h2><p>最后写一个启动类即可: StartMain</p>
<p>这里先运行一下,看下能不能获取到我们想要的结果,也就是有没有错误或者异常等信息给报出来. 如果没有的话,就说明是ok的. 然后就可以开始我们愉快的debug旅行了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StartMain</span> &#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">resouce</span> <span class="operator">=</span> <span class="string">&quot;mybatis-config.xml&quot;</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 读取 mybatis文件,返回的是 InputStream流</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(resouce);</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 使用 SqlSessionFactoryBuilder 来构建出一个 SqlSessionFactory来.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(inputStream);</span><br><span class="line">    <span class="type">SqlSession</span> <span class="variable">session</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line">    <span class="type">PhoneMapper</span> <span class="variable">phoneMapper</span> <span class="operator">=</span> session.getMapper(PhoneMapper.class);</span><br><span class="line">    System.out.println(phoneMapper.selectBlog(<span class="number">1</span>).toString());</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="debug"><a href="#debug" class="headerlink" title="debug"></a>debug</h2><p>这里我们只需要跟着 StartMain 中的方法,一步一步的走下去, 弄明白每个方法是说明意思,然后在其内部是怎么走的</p>
<p>Resources.getResourceAsStream(resouce) 方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Returns a resource on the classpath as a Stream object</span></span><br><span class="line"><span class="comment">   *</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> loader   The classloader used to fetch the resource</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> resource The resource to find</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@return</span> The resource</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@throws</span> java.io.IOException If the resource cannot be found or read</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> InputStream <span class="title function_">getResourceAsStream</span><span class="params">(ClassLoader loader, String resource)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line"><span class="comment">// in 最后是从  getResourceAsStream() 方法返回回来的. </span></span><br><span class="line"><span class="comment">// ClassLoaderWrapper 是 Resources的全局变量,并且最初就已经实例化了,所以这里就直接使用了.      </span></span><br><span class="line">    <span class="type">InputStream</span> <span class="variable">in</span> <span class="operator">=</span> classLoaderWrapper.getResourceAsStream(resource, loader);</span><br><span class="line"><span class="comment">// 如果inputStream是null的话,就会抛出异常来.      </span></span><br><span class="line">    <span class="keyword">if</span> (in == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IOException</span>(<span class="string">&quot;Could not find resource &quot;</span> + resource);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> in;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  </span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Try to get a resource from a group of classloaders</span></span><br><span class="line"><span class="comment">   *</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> resource    - the resource to get</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> classLoader - the classloaders to examine</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@return</span> the resource or null</span></span><br><span class="line"><span class="comment">这里的classLoader集合,分别是 </span></span><br><span class="line"><span class="comment">new ClassLoader[]</span></span><br><span class="line"><span class="comment">&#123;classLoader,defaultClassLoader,</span></span><br><span class="line"><span class="comment">Thread.currentThread().getContextClassLoader(),</span></span><br><span class="line"><span class="comment">getClass.getClassLoader(),systemClassLoader&#125; </span></span><br><span class="line"><span class="comment">分别是这五个.   </span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  InputStream <span class="title function_">getResourceAsStream</span><span class="params">(String resource, ClassLoader[] classLoader)</span> &#123;</span><br><span class="line"><span class="comment">// 对传入进来的classLoader数组进行迭代,跳过classLoader是null的,</span></span><br><span class="line"><span class="comment">//如果 classLoader的getResourceAsStream方法,</span></span><br><span class="line"><span class="comment">//传入进去的是 resource,返回的InputStream不是null的话,就会返回.      </span></span><br><span class="line">    <span class="keyword">for</span> (ClassLoader cl : classLoader) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="literal">null</span> != cl) &#123;</span><br><span class="line">        <span class="comment">// try to find the resource as passed</span></span><br><span class="line">        <span class="type">InputStream</span> <span class="variable">returnValue</span> <span class="operator">=</span> cl.getResourceAsStream(resource);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// now, some class loaders want this leading &quot;/&quot;, so we&#x27;ll add it and try again if we didn&#x27;t find the resource</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="literal">null</span> == returnValue) &#123;</span><br><span class="line">          returnValue = cl.getResourceAsStream(<span class="string">&quot;/&quot;</span> + resource);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="literal">null</span> != returnValue) &#123;</span><br><span class="line">          <span class="keyword">return</span> returnValue;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>new SqlSessionFactoryBuilder().build(inputStream) 方法: 该方法可以看其返回的结果,是返回了一个SqlSessionFactroy的回话工厂.</p>
<p>org.apache.ibatis.session.SqlSessionFactoryBuilder#build(java.io.InputStream, java.lang.String, java.util.Properties)</p>
<p>该方法最后是走到了这里.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> SqlSessionFactory <span class="title function_">build</span><span class="params">(InputStream inputStream, String environment, Properties properties)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;       </span><br><span class="line">      <span class="type">XMLConfigBuilder</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLConfigBuilder</span>(inputStream, environment, properties);</span><br><span class="line">      <span class="keyword">return</span> build(parser.parse());</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> ExceptionFactory.wrapException(<span class="string">&quot;Error building SqlSession.&quot;</span>, e);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      ErrorContext.instance().reset();</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="comment">//  最后给流关一下   </span></span><br><span class="line">        inputStream.close();</span><br><span class="line">      &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        <span class="comment">// Intentionally ignore. Prefer previous error.</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">---------------------------------</span><br><span class="line"><span class="keyword">new</span> <span class="title class_">XMLConfigBuilder</span>构造方法</span><br><span class="line"><span class="comment">/**该构造方法,其先new一个XMLMapperEntityResolver,然后最后在new一个XPathParser,</span></span><br><span class="line"><span class="comment">最后走this()构造方法.  </span></span><br><span class="line"><span class="comment">new XPathParser构造方法: </span></span><br><span class="line"><span class="comment">	先是给 validation/entityResolver/variables/xpath进行赋值.</span></span><br><span class="line"><span class="comment">	然后给inputStream给包装成 inputSource.</span></span><br><span class="line"><span class="comment">	掉用createDocument方法: 先弄一个 DocumentBuilderFactory出来,然后对其属性进行设置操作.</span></span><br><span class="line"><span class="comment">	  再调用parse方法, 最后返回的 this.document的类是 DeferredDocumentImpl这个类.	</span></span><br><span class="line"><span class="comment">this()构造方法: new Configuration()可以看到该类,给typeAliasRegistry和languageRegistry里面进行设值等操作,同时其全局参数也会一同会被实例化的,可以看到其全局参数还是蛮多的.  </span></span><br><span class="line"><span class="comment">   	super()方法,也就是给configuration给赋值给BaseBuilder这个类的全局参数.</span></span><br><span class="line"><span class="comment">   	给parsed设置为false  /  parser 设置为 parser.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">XMLConfigBuilder</span><span class="params">(InputStream inputStream, String environment, Properties props)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>(<span class="keyword">new</span> <span class="title class_">XPathParser</span>(inputStream, <span class="literal">true</span>, props, <span class="keyword">new</span> <span class="title class_">XMLMapperEntityResolver</span>()), environment, props);</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">----------------------------------</span><br><span class="line">org.apache.ibatis.builder.xml.XMLConfigBuilder#parse  (parser.parse()方法)</span><br><span class="line">    </span><br><span class="line">  <span class="keyword">public</span> Configuration <span class="title function_">parse</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 定义了一个参数parsed，如果是true就说明已经解析过了,只可以解析一次</span></span><br><span class="line">    <span class="comment">// 所以这里如果是true的话,就会抛出异常来的.</span></span><br><span class="line">    <span class="keyword">if</span> (parsed) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Each XMLConfigBuilder can only be used once.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 设置为true.</span></span><br><span class="line">    parsed = <span class="literal">true</span>;</span><br><span class="line">    <span class="comment">// 从configuration开始解析 </span></span><br><span class="line">    parseConfiguration(parser.evalNode(<span class="string">&quot;/configuration&quot;</span>));</span><br><span class="line">    <span class="keyword">return</span> configuration;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里可以看到 mybatis对于标签的解析顺序，从执行的顺序来看,就是其解析标签的顺序.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">parseConfiguration</span><span class="params">(XNode root)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">//issue #117 read properties first</span></span><br><span class="line"><span class="comment">//从root.evalNode(&quot;properties&quot;)中获取出来的值不是null的话,该方法才会走.</span></span><br><span class="line"><span class="comment">//获取其子节点,并且还分别获取resource/url,如果二者都有值的话,就会抛出异常.</span></span><br><span class="line"><span class="comment">//只可以有一种方式来读取. 要么是resource，要么是url. </span></span><br><span class="line"><span class="comment">//最后获取出来的值,都会调用Properties的putAll给放入进去,最后set到parser和configuration中去.      </span></span><br><span class="line">      propertiesElement(root.evalNode(<span class="string">&quot;properties&quot;</span>));</span><br><span class="line"><span class="comment">//获取setting标签,然后走settingsAsProperties方法.</span></span><br><span class="line"><span class="comment">// settingsAsProperties:如果解析出来的值是null的话,就会new一个Properties直接返回.</span></span><br><span class="line"><span class="comment">//否则的话,就会先获取出context.getChildrenAsProperties(),</span></span><br><span class="line"><span class="comment">//然后从MetaClass metaConfig = MetaClass.forClass(Configuration.class, localReflectorFactory);然后迭代context这个集合,然后metaConfig.hasSetter是false的话,就会抛出异常来.如果没抛异常的话,最后就返回props回去.        </span></span><br><span class="line">      <span class="type">Properties</span> <span class="variable">settings</span> <span class="operator">=</span> settingsAsProperties(root.evalNode(<span class="string">&quot;settings&quot;</span>));</span><br><span class="line">      loadCustomVfs(settings);</span><br><span class="line"> <span class="comment">//从setting中获取logImpl的值,也就是log,获取出来的logImpl的值最后设置到configuration中去.</span></span><br><span class="line"> <span class="comment">//也就是配置下你使用那个log的框架.       </span></span><br><span class="line">      loadCustomLogImpl(settings);</span><br><span class="line"> <span class="comment">//获取别名的标签值. parent</span></span><br><span class="line"> <span class="comment">//parent不为null,然后进行迭代,这里别名配置是可以是多个的.</span></span><br><span class="line"> <span class="comment">//分别是 package/alias(type)这二种,如果是package的话,就会放入到configuration的typeAliasRegistry中.  如果是alisa这种形式的话,alisa是null的话,就给class注册进去.</span></span><br><span class="line"><span class="comment">//否则就根据alisa+class给注册进去. 这里也就是放入到TypeAliasRegistry中的typeAliases中来了.</span></span><br><span class="line">      typeAliasesElement(root.evalNode(<span class="string">&quot;typeAliases&quot;</span>));</span><br><span class="line"><span class="comment">//获取plugins下面的值,然后再获取其childRen,也即是其下的标签,进行迭代.获取出interceptor的值,</span></span><br><span class="line"><span class="comment">//使用反射来将其实例化,并且给下面的property标签给set进去.        </span></span><br><span class="line">      pluginElement(root.evalNode(<span class="string">&quot;plugins&quot;</span>));</span><br><span class="line"><span class="comment">//获取objectFactory标签的值,然后判断其获取出来的值不为null,</span></span><br><span class="line"><span class="comment">//获取type对应的值,然后利用反射对其进行实例化,最后也将其properties子标签给设置进去.</span></span><br><span class="line"><span class="comment">//最后将ObjectFactory给设置到configuration中去.        </span></span><br><span class="line">      objectFactoryElement(root.evalNode(<span class="string">&quot;objectFactory&quot;</span>));</span><br><span class="line"><span class="comment">// 与ObjectFactory的处理方式是一样的        </span></span><br><span class="line">      objectWrapperFactoryElement(root.evalNode(<span class="string">&quot;objectWrapperFactory&quot;</span>));</span><br><span class="line"><span class="comment">// 与objectFactory的处理方式一样        </span></span><br><span class="line">      reflectorFactoryElement(root.evalNode(<span class="string">&quot;reflectorFactory&quot;</span>));</span><br><span class="line"><span class="comment">// 将setting标签的值,给设置到configuration中去,如果没的话,就会使用默认值.        </span></span><br><span class="line">      settingsElement(settings);</span><br><span class="line">      <span class="comment">// read it after objectFactory and objectWrapperFactory issue #631</span></span><br><span class="line"><span class="comment">//读取environments标签对用的值,如果其内部的environment是null的话,就使用默认的去获取</span></span><br><span class="line"><span class="comment">//获取id标签,然后判断id标签的值和default的值是不是一样的,如果不是一样的话,就不进行后续set</span></span><br><span class="line"><span class="comment">//transactionManager和dataSource等操作了.</span></span><br><span class="line"><span class="comment">//如果是一样的话,先是读取transactionManager，再读取dataSource.</span></span><br><span class="line"><span class="comment">//这里根据 POOLED 获取出来的是:PooledDataSourceFactory，然后将其下层的标签的值给set进去.</span></span><br><span class="line"><span class="comment">//最后返回回去.  如果context是null的话,这里就会直接抛出BuilderException这个异常来.</span></span><br><span class="line"><span class="comment">//接着从上面的 PooledDataSourceFactory 获取出 DataSource来, </span></span><br><span class="line"><span class="comment">//dataSource中就有一些关于db的配置信息     </span></span><br><span class="line"><span class="comment">//最后new一个Environment.Builder出来,并且将上面的transactionManager和dataSource给放入到Environment.Builder中去. 最后 Environment.Builder 是会被放入到configuration中去.</span></span><br><span class="line">      environmentsElement(root.evalNode(<span class="string">&quot;environments&quot;</span>));</span><br><span class="line"><span class="comment">//设置 dataBaseId 到 configuration中去.        </span></span><br><span class="line">      databaseIdProviderElement(root.evalNode(<span class="string">&quot;databaseIdProvider&quot;</span>));</span><br><span class="line"><span class="comment">// 根据package或者javaType(jdbcType,handler) 来分为二种给注册到typeHandlerRegistry中去.</span></span><br><span class="line"><span class="comment">//如果是javaType(jdbcType,handler)等情况的话,又会根据javaTypeClass等是否有值,</span></span><br><span class="line"><span class="comment">//走不通的重载方法给注册到typeHandlerRegistry中去.        </span></span><br><span class="line">      typeHandlerElement(root.evalNode(<span class="string">&quot;typeHandlers&quot;</span>));</span><br><span class="line"><span class="comment">// 然后mappers这个标签的话,机会分为是否用package和resouce(url/class)二种方式给解析</span></span><br><span class="line"><span class="comment">//如果是package的话,就直接添加到configuration中去.</span></span><br><span class="line"><span class="comment">// resource/url/class 就会分为三种情况进行处理:</span></span><br><span class="line"><span class="comment">// ①resource有值,url和class是没有值的,  ②url有值,resouce和class是没有值的,  </span></span><br><span class="line"><span class="comment">// ③class是有值的,url和resouces是没有值的.  </span></span><br><span class="line"><span class="comment">// ① 和 ②的情况都会在new一个XMLMapperBuilder,然后调用其parse方法进行解析.</span></span><br><span class="line"><span class="comment">// 如果是③的话,就会使用Resouce.classForName(mapperClass)返回的</span></span><br><span class="line"><span class="comment">// 	class给添加到configuration中去. (最后是放入到MapperRegistry.knownMappers这个集合中) </span></span><br><span class="line"><span class="comment">// 最后主要就是看 XMLMapperBuilder.parse() 这个解析的方法,该方法就是解析xml的信息的         </span></span><br><span class="line">      mapperElement(root.evalNode(<span class="string">&quot;mappers&quot;</span>));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error parsing SQL Mapper Configuration. Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>sqlSessionFactory.openSession() 方法:</p>
<p>org.apache.ibatis.session.defaults.DefaultSqlSessionFactory#openSessionFromDataSource</p>
<p>该方法主要就是获取一个SqlSession会话.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> SqlSession <span class="title function_">openSessionFromDataSource</span><span class="params">(ExecutorType execType, TransactionIsolationLevel level, <span class="type">boolean</span> autoCommit)</span> &#123;</span><br><span class="line">    <span class="type">Transaction</span> <span class="variable">tx</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 先获取出 Environment        </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">Environment</span> <span class="variable">environment</span> <span class="operator">=</span> configuration.getEnvironment();</span><br><span class="line"> <span class="comment">//从environment中获取出 TransactionFactory       </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">TransactionFactory</span> <span class="variable">transactionFactory</span> <span class="operator">=</span> getTransactionFactoryFromEnvironment(environment);</span><br><span class="line"> <span class="comment">// new 一个Transaction出来.       </span></span><br><span class="line">      tx = transactionFactory.newTransaction(environment.getDataSource(), level, autoCommit);</span><br><span class="line"> <span class="comment">// 根据 executorType来判断使用那种 Executor. </span></span><br><span class="line"> <span class="comment">// 这里看 BaseExecutor下,是有SimpleExectour/ReuseExecutor/BatchExecutor三种,</span></span><br><span class="line"> <span class="comment">// 一般情况下都是使用 SimpleExecutor这种的</span></span><br><span class="line"> <span class="comment">//如果cacheEnable是true的话,接着就会替换成CachingExecutor这种.</span></span><br><span class="line"> <span class="comment">//接着走org.apache.ibatis.plugin.InterceptorChain#pluginAll,这个可以看到是之前plugins标签进行扩展的配置. 将Executor给返回回来.      </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">Executor</span> <span class="variable">executor</span> <span class="operator">=</span> configuration.newExecutor(tx, execType);</span><br><span class="line"><span class="comment">//最后是new一个DefaultSqlSession这个session回去,然后初始化其 configuration/executor/dirty/autoCommit等参数配置.        </span></span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">DefaultSqlSession</span>(configuration, executor, autoCommit);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      closeTransaction(tx); <span class="comment">// may have fetched a connection so lets call close()</span></span><br><span class="line">      <span class="keyword">throw</span> ExceptionFactory.wrapException(<span class="string">&quot;Error opening session.  Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">     <span class="comment">// 最后重置一下.   </span></span><br><span class="line">      ErrorContext.instance().reset();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>session.getMapper(PhoneMapper.class); 获取Mapper的方法</p>
<p>org.apache.ibatis.session.defaults.DefaultSqlSession#getMapper</p>
<p>这里如果细心的话,可以看到其debug的这个类的值或者使用输出给值打印出来:System.out.println(phoneMapper.getClass().toString()); –&gt;class com.sun.proxy.$Proxy0,就可以很明显的看到这是一个代理类了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PhoneMapper phoneMapper = session.getMapper(PhoneMapper.class); </span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getMapper</span><span class="params">(Class&lt;T&gt; type)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> configuration.getMapper(type, <span class="built_in">this</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getMapper</span><span class="params">(Class&lt;T&gt; type, SqlSession sqlSession)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> mapperRegistry.getMapper(type, sqlSession);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">---------------------</span><br><span class="line"><span class="comment">// org.apache.ibatis.binding.MapperRegistry#getMapper    </span></span><br><span class="line">  <span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getMapper</span><span class="params">(Class&lt;T&gt; type, SqlSession sqlSession)</span> &#123;</span><br><span class="line"> <span class="comment">// 从knownMappers这个集合中获取出对应的Mapper,根据类名字.   </span></span><br><span class="line">    <span class="keyword">final</span> MapperProxyFactory&lt;T&gt; mapperProxyFactory = (MapperProxyFactory&lt;T&gt;) knownMappers.get(type);</span><br><span class="line"> <span class="comment">//如果不能根据类名字从该集合中获取出来的话,就会抛出异常来.   </span></span><br><span class="line">    <span class="keyword">if</span> (mapperProxyFactory == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Type &quot;</span> + type + <span class="string">&quot; is not known to the MapperRegistry.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 先new一个MapperProxy类,传入其构造方法的参数是</span></span><br><span class="line"><span class="comment">//sqlSession/mapperInterface(也就是我们的接口)/ methodCache</span></span><br><span class="line"> <span class="comment">// 最后使用Proxy.newProxyInstance()来创建出一个对象(这里也就是使用了反射).       </span></span><br><span class="line">      <span class="keyword">return</span> mapperProxyFactory.newInstance(sqlSession);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Error getting mapper instance. Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>mybatis与springboot整合阅读</title>
    <url>//article/mybatis_springBoot_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-前提"><a href="#📝-前提" class="headerlink" title="📝 前提"></a>📝 <strong>前提</strong></h1><p>MyBatis 与 SpringBoot 整合操作. 在这次整合的过程中,再次明白自己毫无疑问的是一个比较手残的同学了.</p>
<p>这里我们是基于 sql 语句写在 xml 里面进行整合的操作.</p>
<h1 id="入门"><a href="#入门" class="headerlink" title="入门"></a>入门</h1><p>这里说下创建一个 入门 项目的大致流程.</p>
<p>先创建一个 SpringBoot 项目 , 引入依赖 : <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/pom.xml">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/pom.xml</a></p>
<p>创建 MyBatis 的配置文件信息 : <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/resources/mybatis-config.xml">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/resources/mybatis-config.xml</a></p>
<p>创建查询的 sql 语句，也就是我们的 mapper 文件 : <a href="https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-boot-hello/src/main/resources/mapper">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-boot-hello/src/main/resources/mapper</a></p>
<p>application.properties : <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/resources/application.properties">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/resources/application.properties</a></p>
<p>扫描 mapper 接口 : @MapperScan(basePackages &#x3D; {“com.iyang.mybatis.springboot.hello.mapper”}) <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/java/com/iyang/mybatis/springboot/hello/MybatisSpringBootHelloApplication.java">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-boot-hello/src/main/java/com/iyang/mybatis/springboot/hello/MybatisSpringBootHelloApplication.java</a></p>
<p>这里是没有引入 web 依赖的 , 直接启动 main 方法 , 然后就可以看到我们查询出来的结果了.</p>
<p>如果你熟悉 SpringBoot 源码的话，就会晓得有一个自动装配的操作.</p>
<p>如果不熟悉的话，那么就只能通过 @MapperScan(basePackages &#x3D; {“com.iyang.mybatis.springboot.hello.mapper”}) 去看 , 这样有些是依赖自动装配（spring.factories） 中的配置加载的, 所以这里建议在看之前，如果是有一点 SpringBoot 扩展的知识了解是很好的。如果没有怎么办呢？没有就来看我接下来的内容。</p>
<p>其实这个地方你仔细想下，在 MyBatis 与 Spring 整合的时候，通过 xml 的方式给 MyBatis 的bean 已经 mybatis-spring 中自己写的扫描类，最后将扫描出来的 bd 在还没初始化之前，将bd 的beanClass 替换为我们的代理类.</p>
<p>那么，SpringBoot 与 MyBatis 整合的时候，最后要做的事情是不是也是将 MyBatis 的信息注入到 SpringBoot 来呢？只不过，SpringBoot 就不像 Spring 一样了，还将 bean 的信息配置到 xml 文件中.</p>
<p>于是，接下来跟我的阅读&amp;分析来一步一步的往下看.</p>
<h1 id="方法分析"><a href="#方法分析" class="headerlink" title="方法分析"></a>方法分析</h1><p><strong>关注点一</strong> : 这里我们点入到 org.mybatis.spring.annotation.MapperScan 注解里面来，可以看到有一个 @Import(MapperScannerRegistrar.class) , 于是我们顺手跟进来 : org.mybatis.spring.annotation.MapperScannerRegistrar , 从名字上来，这个类就做了一个扫描mapper并且将mapper注入到Spring容器中来的事情.</p>
<p><strong>关注点二</strong> : 我们从引入进来的依赖来看, mybatis-spring-boot-starter-2.1.2.jar 跟进到 这个包来，可以看到这个包也是引入一些进来. mybatis&#x2F;mybatis-spring&#x2F;spring-boot-starter-jdbc 这三个依赖我们应该不是很陌生的，mybatis-spring-boot-autoconfigure主要来看这个。 spring.factories 的作用大家可以去了解下，SpringBoot很多 EnableAutoConfiguration 的配置都是放入在这个里面的，在启动的时候，会去一层一层的去读取 spring.factories 文件的内容。 这里我们主要来看 : org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration 这个类皆可.</p>
<p>MyBatis 在 properties 中的配置文件读取 : org.mybatis.spring.boot.autoconfigure.MybatisProperties</p>
<p>可以看到该类上是有: @ConfigurationProperties(prefix &#x3D; MybatisProperties.MYBATIS_PREFIX)</p>
<p>于是我们一下子就多了二个关注点, 这里我们可以采用之前的 笨方法， 当你对整合流程执行不是很熟悉的话，可以在这二个关注点的重写方法上都打算断点，看下其执行顺序是怎么执行的. 弄清楚了执行流程,就可以跟着流程来一步一步的分析. 从我们打上 debug 开始，往下的执行流程就是一步一步来的，那么就跟着我们debug 的方法来一步一步的分析.</p>
<p>org.mybatis.spring.annotation.MapperScannerRegistrar#registerBeanDefinitions() —&gt; org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#MybatisAutoConfiguration —&gt; org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#afterPropertiesSet —-&gt; org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionFactory —&gt; org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionTemplate() —-&gt;</p>
<h2 id="org-mybatis-spring-annotation-MapperScannerRegistrar-registerBeanDefinitions-方法"><a href="#org-mybatis-spring-annotation-MapperScannerRegistrar-registerBeanDefinitions-方法" class="headerlink" title="org.mybatis.spring.annotation.MapperScannerRegistrar#registerBeanDefinitions() 方法 :"></a><strong>org.mybatis.spring.annotation.MapperScannerRegistrar#registerBeanDefinitions() 方法</strong> :</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@inheritDoc</span>&#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerBeanDefinitions</span><span class="params">(AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line"><span class="comment">/***</span></span><br><span class="line"><span class="comment">*  这里是获取出了注解里面属性的值. </span></span><br><span class="line"><span class="comment">*/</span>   </span><br><span class="line">  <span class="type">AnnotationAttributes</span> <span class="variable">mapperScanAttrs</span> <span class="operator">=</span> </span><br><span class="line">  AnnotationAttributes.fromMap(importingClassMetadata.getAnnotationAttributes(MapperScan.class.getName()));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 能获取到有注解,不是null,就会走到下面的代码中来.    </span></span><br><span class="line">  <span class="keyword">if</span> (mapperScanAttrs != <span class="literal">null</span>) &#123;</span><br><span class="line">    registerBeanDefinitions(importingClassMetadata, mapperScanAttrs, registry,</span><br><span class="line">        generateBaseBeanName(importingClassMetadata, <span class="number">0</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">registerBeanDefinitions</span><span class="params">(AnnotationMetadata annoMeta, AnnotationAttributes annoAttrs,</span></span><br><span class="line"><span class="params">      BeanDefinitionRegistry registry, String beanName)</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 利用 BeanDefinitionBuilder 构造者,传入了一个 MapperScannerConfigurer.class</span></span><br><span class="line"><span class="comment">// 这里的 builder里面是有一个 bd 的,里面的beanClass就是 MapperScannerConfigurer      </span></span><br><span class="line">    <span class="type">BeanDefinitionBuilder</span> <span class="variable">builder</span> <span class="operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(MapperScannerConfigurer.class);</span><br><span class="line">    builder.addPropertyValue(<span class="string">&quot;processPropertyPlaceHolders&quot;</span>, <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里获取 @MapperScan 注解的属性, 如果属性是有值的话,就会设置到 builder 中来. </span></span><br><span class="line">    Class&lt;? <span class="keyword">extends</span> <span class="title class_">Annotation</span>&gt; annotationClass = annoAttrs.getClass(<span class="string">&quot;annotationClass&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!Annotation.class.equals(annotationClass)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;annotationClass&quot;</span>, annotationClass);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Class&lt;?&gt; markerInterface = annoAttrs.getClass(<span class="string">&quot;markerInterface&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!Class.class.equals(markerInterface)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;markerInterface&quot;</span>, markerInterface);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Class&lt;? <span class="keyword">extends</span> <span class="title class_">BeanNameGenerator</span>&gt; generatorClass = annoAttrs.getClass(<span class="string">&quot;nameGenerator&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!BeanNameGenerator.class.equals(generatorClass)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;nameGenerator&quot;</span>, BeanUtils.instantiateClass(generatorClass));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Class&lt;? <span class="keyword">extends</span> <span class="title class_">MapperFactoryBean</span>&gt; mapperFactoryBeanClass = annoAttrs.getClass(<span class="string">&quot;factoryBean&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!MapperFactoryBean.class.equals(mapperFactoryBeanClass)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;mapperFactoryBeanClass&quot;</span>, mapperFactoryBeanClass);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">sqlSessionTemplateRef</span> <span class="operator">=</span> annoAttrs.getString(<span class="string">&quot;sqlSessionTemplateRef&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(sqlSessionTemplateRef)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;sqlSessionTemplateBeanName&quot;</span>, annoAttrs.getString(<span class="string">&quot;sqlSessionTemplateRef&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">sqlSessionFactoryRef</span> <span class="operator">=</span> annoAttrs.getString(<span class="string">&quot;sqlSessionFactoryRef&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(sqlSessionFactoryRef)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;sqlSessionFactoryBeanName&quot;</span>, annoAttrs.getString(<span class="string">&quot;sqlSessionFactoryRef&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下面是根据 value/basePackages/basePackageClasses 来获取包的信息,</span></span><br><span class="line"><span class="comment">// 这里也就说, 我们可以跟着这三个属性来配置包信息.      </span></span><br><span class="line">    List&lt;String&gt; basePackages = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    basePackages.addAll(</span><br><span class="line">        Arrays.stream(annoAttrs.getStringArray(<span class="string">&quot;value&quot;</span>)).filter(StringUtils::hasText).collect(Collectors.toList()));</span><br><span class="line"></span><br><span class="line">    basePackages.addAll(Arrays.stream(annoAttrs.getStringArray(<span class="string">&quot;basePackages&quot;</span>)).filter(StringUtils::hasText)</span><br><span class="line">        .collect(Collectors.toList()));</span><br><span class="line"></span><br><span class="line">    basePackages.addAll(Arrays.stream(annoAttrs.getClassArray(<span class="string">&quot;basePackageClasses&quot;</span>)).map(ClassUtils::getPackageName)</span><br><span class="line">        .collect(Collectors.toList()));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果没有获取到包的信息,那就根据注解所在的路径来获取默认的路径.      </span></span><br><span class="line">    <span class="keyword">if</span> (basePackages.isEmpty()) &#123;</span><br><span class="line">      basePackages.add(getDefaultBasePackage(annoMeta));</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 如果有lazyInitialization属性的值,就设置到 builder 中来. </span></span><br><span class="line">    <span class="type">String</span> <span class="variable">lazyInitialization</span> <span class="operator">=</span> annoAttrs.getString(<span class="string">&quot;lazyInitialization&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(lazyInitialization)) &#123;</span><br><span class="line">      builder.addPropertyValue(<span class="string">&quot;lazyInitialization&quot;</span>, lazyInitialization);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 添加包的属性</span></span><br><span class="line">    builder.addPropertyValue(<span class="string">&quot;basePackage&quot;</span>, StringUtils.collectionToCommaDelimitedString(basePackages));</span><br><span class="line"></span><br><span class="line"><span class="comment">//  getBeanDefinition() 在返回 bd 之前，会走一个 validate 方法.</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#registerBeanDefinition</span></span><br><span class="line"><span class="comment">// 走这个方法来将 bd 给注入到 Spring 容器中来.</span></span><br><span class="line"><span class="comment">// 这里注入进去的 beanName 的值是 :  com.iyang.mybatis.springboot.hello.MybatisSpringBootHelloApplication#MapperScannerRegistrar#0</span></span><br><span class="line"><span class="comment">// 注入进去的 bd 的 beanClass : class org.mybatis.spring.mapper.MapperScannerConfigurer </span></span><br><span class="line">    registry.registerBeanDefinition(beanName, builder.getBeanDefinition());</span><br><span class="line"></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><strong>这里可以总结下 registerBeanDefinitions 方法，该方法就是将 @MapperScan 的注解属性的值给到 : BeanDefinitionBuilder builder, 该builder 里面有bd,bd的beanClass是MapperScannerConfigurer，最后将MapperScannerConfigurer注入到 Spring 容器中来.</strong></p>
<h2 id="MyBatisAutoConfiguration-有参构造函数"><a href="#MyBatisAutoConfiguration-有参构造函数" class="headerlink" title="MyBatisAutoConfiguration() 有参构造函数"></a><strong>MyBatisAutoConfiguration() 有参构造函数</strong></h2><p>这里我们在 MybatisAutoConfiguration 构造函数上打上断点, 可以根据 断点来分析，走完</p>
<p><img src="/images/posts/mybatis_springBoot_source1/img-1.png" alt="mybatis与springboot整合阅读"></p>
<p>面的方法，然后我们点击走到下一个断点来，就会走到 这个 有参构造函数.</p>
<p>如果好奇的话，可以跟踪debug 的堆栈信息，是怎么走到这步来的. 走到这个方法来 : finishBeanFactoryInitialization(beanFactory) 这是最初的入口.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">MybatisAutoConfiguration</span><span class="params">(MybatisProperties properties, ObjectProvider&lt;Interceptor[]&gt; interceptorsProvider,</span></span><br><span class="line"><span class="params">    ObjectProvider&lt;TypeHandler[]&gt; typeHandlersProvider, ObjectProvider&lt;LanguageDriver[]&gt; languageDriversProvider,</span></span><br><span class="line"><span class="params">    ResourceLoader resourceLoader, ObjectProvider&lt;DatabaseIdProvider&gt; databaseIdProvider,</span></span><br><span class="line"><span class="params">    ObjectProvider&lt;List&lt;ConfigurationCustomizer&gt;&gt; configurationCustomizersProvider)</span> &#123;</span><br><span class="line"><span class="comment">// 这里都是赋值    </span></span><br><span class="line">  <span class="built_in">this</span>.properties = properties;</span><br><span class="line">  <span class="built_in">this</span>.interceptors = interceptorsProvider.getIfAvailable();</span><br><span class="line">  <span class="built_in">this</span>.typeHandlers = typeHandlersProvider.getIfAvailable();</span><br><span class="line">  <span class="built_in">this</span>.languageDrivers = languageDriversProvider.getIfAvailable();</span><br><span class="line">  <span class="built_in">this</span>.resourceLoader = resourceLoader;</span><br><span class="line">  <span class="built_in">this</span>.databaseIdProvider = databaseIdProvider.getIfAvailable();</span><br><span class="line">  <span class="built_in">this</span>.configurationCustomizers = configurationCustomizersProvider.getIfAvailable();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-afterPropertiesSet-方法"><a href="#org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-afterPropertiesSet-方法" class="headerlink" title="org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#afterPropertiesSet()方法"></a><strong>org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#afterPropertiesSet()方法</strong></h2><p>这里可以看到是对配置文件是否存在进行检验.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> &#123;</span><br><span class="line">  checkConfigFileExists();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 检验配置文件是否存在</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">checkConfigFileExists</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.properties.isCheckConfigLocation() &amp;&amp; StringUtils.hasText(<span class="built_in">this</span>.properties.getConfigLocation())) &#123;</span><br><span class="line">      <span class="type">Resource</span> <span class="variable">resource</span> <span class="operator">=</span> <span class="built_in">this</span>.resourceLoader.getResource(<span class="built_in">this</span>.properties.getConfigLocation());</span><br><span class="line">      Assert.state(resource.exists(),</span><br><span class="line">          <span class="string">&quot;Cannot find config location: &quot;</span> + resource + <span class="string">&quot; (please add config file or check your Mybatis configuration)&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-sqlSessionFactory-方法"><a href="#org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-sqlSessionFactory-方法" class="headerlink" title="org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionFactory 方法"></a><strong>org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionFactory 方法</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里说下 @ConditionalOnMissingBean 的作用,当bean不存在的时候，则实例化这个bean.</span></span><br><span class="line"><span class="comment">// 这里会传入 dataSource 进来.</span></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span></span><br><span class="line"><span class="keyword">public</span> SqlSessionFactory <span class="title function_">sqlSessionFactory</span><span class="params">(DataSource dataSource)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"><span class="comment">// 创建 sqlSessionBean 对象.    </span></span><br><span class="line">  <span class="type">SqlSessionFactoryBean</span> <span class="variable">factory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBean</span>();</span><br><span class="line"><span class="comment">// 设置 dataSource &amp; SpringBootVFS.class      </span></span><br><span class="line">  factory.setDataSource(dataSource);</span><br><span class="line">  factory.setVfs(SpringBootVFS.class);</span><br><span class="line"><span class="comment">// 获取到 MyBatis 的配置文件属性,如果有的话,就会设置到 configLocation属性来.    </span></span><br><span class="line">  <span class="keyword">if</span> (StringUtils.hasText(<span class="built_in">this</span>.properties.getConfigLocation())) &#123;</span><br><span class="line">    factory.setConfigLocation(<span class="built_in">this</span>.resourceLoader.getResource(<span class="built_in">this</span>.properties.getConfigLocation()));</span><br><span class="line">  &#125;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 这里从 properties 中获取 configuration,没有值就会是null.    </span></span><br><span class="line">  applyConfiguration(factory);</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.properties.getConfigurationProperties() != <span class="literal">null</span>) &#123;</span><br><span class="line">    factory.setConfigurationProperties(<span class="built_in">this</span>.properties.getConfigurationProperties());</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果有插件,就会设置插件.    </span></span><br><span class="line">  <span class="keyword">if</span> (!ObjectUtils.isEmpty(<span class="built_in">this</span>.interceptors)) &#123;</span><br><span class="line">    factory.setPlugins(<span class="built_in">this</span>.interceptors);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.databaseIdProvider != <span class="literal">null</span>) &#123;</span><br><span class="line">    factory.setDatabaseIdProvider(<span class="built_in">this</span>.databaseIdProvider);</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// 包的名别设置    </span></span><br><span class="line">  <span class="keyword">if</span> (StringUtils.hasLength(<span class="built_in">this</span>.properties.getTypeAliasesPackage())) &#123;</span><br><span class="line">    factory.setTypeAliasesPackage(<span class="built_in">this</span>.properties.getTypeAliasesPackage());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.properties.getTypeAliasesSuperType() != <span class="literal">null</span>) &#123;</span><br><span class="line">    factory.setTypeAliasesSuperType(<span class="built_in">this</span>.properties.getTypeAliasesSuperType());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (StringUtils.hasLength(<span class="built_in">this</span>.properties.getTypeHandlersPackage())) &#123;</span><br><span class="line">    factory.setTypeHandlersPackage(<span class="built_in">this</span>.properties.getTypeHandlersPackage());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (!ObjectUtils.isEmpty(<span class="built_in">this</span>.typeHandlers)) &#123;</span><br><span class="line">    factory.setTypeHandlers(<span class="built_in">this</span>.typeHandlers);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (!ObjectUtils.isEmpty(<span class="built_in">this</span>.properties.resolveMapperLocations())) &#123;</span><br><span class="line">    factory.setMapperLocations(<span class="built_in">this</span>.properties.resolveMapperLocations());</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// 这里都是配置属性的设置.    </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取 propert 字段属性的名字.    </span></span><br><span class="line">  Set&lt;String&gt; factoryPropertyNames = Stream</span><br><span class="line">      .of(<span class="keyword">new</span> <span class="title class_">BeanWrapperImpl</span>(SqlSessionFactoryBean.class).getPropertyDescriptors()).map(PropertyDescriptor::getName)</span><br><span class="line">      .collect(Collectors.toSet());</span><br><span class="line">  Class&lt;? <span class="keyword">extends</span> <span class="title class_">LanguageDriver</span>&gt; defaultLanguageDriver = <span class="built_in">this</span>.properties.getDefaultScriptingLanguageDriver();</span><br><span class="line">  <span class="keyword">if</span> (factoryPropertyNames.contains(<span class="string">&quot;scriptingLanguageDrivers&quot;</span>) &amp;&amp; !ObjectUtils.isEmpty(<span class="built_in">this</span>.languageDrivers)) &#123;</span><br><span class="line">    <span class="comment">// Need to mybatis-spring 2.0.2+</span></span><br><span class="line">    factory.setScriptingLanguageDrivers(<span class="built_in">this</span>.languageDrivers);</span><br><span class="line">    <span class="keyword">if</span> (defaultLanguageDriver == <span class="literal">null</span> &amp;&amp; <span class="built_in">this</span>.languageDrivers.length == <span class="number">1</span>) &#123;</span><br><span class="line">      defaultLanguageDriver = <span class="built_in">this</span>.languageDrivers[<span class="number">0</span>].getClass();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// 设置默认的脚本语言解析器. 这里没有,设置的是默认的null.    </span></span><br><span class="line">  <span class="keyword">if</span> (factoryPropertyNames.contains(<span class="string">&quot;defaultScriptingLanguageDriver&quot;</span>)) &#123;</span><br><span class="line">    <span class="comment">// Need to mybatis-spring 2.0.2+</span></span><br><span class="line">    factory.setDefaultScriptingLanguageDriver(defaultLanguageDriver);</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// org.mybatis.spring.SqlSessionFactoryBean#getObject,这里走到了 SqlSessionBean.</span></span><br><span class="line"><span class="comment">// 这个SqlSessionFactoryBean是在有之前 mybatis和Spring 整合分析有提过到的,可以参考getObject方法: https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello    </span></span><br><span class="line"><span class="comment">// 这里会走 org.mybatis.spring.SqlSessionFactoryBean#getObject 的 afterPropertiesSet 方法来创建一个 SqlSessionFactory , 这里返回的 SqlSessionFactory 就注入到 Spring 容器中来.    </span></span><br><span class="line">  <span class="keyword">return</span> factory.getObject();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>所以这个方法 ： 先是new了一个SqlSessionFactoryBean对象，如果你仔细看的话，你会发现这个对象在之前 mybatis-spring 整合的时候，我们通过 xml 配置文件配置进来的，并且同时通过标签给赋值了datasource等信息， 而这里是通过代码，if等判断，来对 SqlSessionFactoryBean 的属性进行set值的. 最后也是创建出一个 SqlSessionFactory 给注入到 Spring 容器中来.</strong></p>
<h2 id="org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-sqlSessionTemplate-方法"><a href="#org-mybatis-spring-boot-autoconfigure-MybatisAutoConfiguration-sqlSessionTemplate-方法" class="headerlink" title="org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionTemplate 方法"></a><strong>org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionTemplate 方法</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span></span><br><span class="line"><span class="keyword">public</span> SqlSessionTemplate <span class="title function_">sqlSessionTemplate</span><span class="params">(SqlSessionFactory sqlSessionFactory)</span> &#123;</span><br><span class="line"><span class="comment">// 这里根据 executorType 是否有值来判断要走的构造函数方法.    </span></span><br><span class="line">  <span class="type">ExecutorType</span> <span class="variable">executorType</span> <span class="operator">=</span> <span class="built_in">this</span>.properties.getExecutorType();</span><br><span class="line">  <span class="keyword">if</span> (executorType != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">SqlSessionTemplate</span>(sqlSessionFactory, executorType);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 这里默认的获取是 SIMPLE 这个 ExecutorType.      </span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">SqlSessionTemplate</span>(sqlSessionFactory);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.mybatis.spring.SqlSessionTemplate#SqlSessionTemplate(org.apache.ibatis.session.SqlSessionFactory, org.apache.ibatis.session.ExecutorType, org.springframework.dao.support.PersistenceExceptionTranslator),最后可以跟进到这个方法中来.</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">SqlSessionTemplate</span><span class="params">(SqlSessionFactory sqlSessionFactory, ExecutorType executorType,</span></span><br><span class="line"><span class="params">      PersistenceExceptionTranslator exceptionTranslator)</span> &#123;</span><br><span class="line"><span class="comment">// 对sqlSessionFactory 和 executorType 进行校验</span></span><br><span class="line">    notNull(sqlSessionFactory, <span class="string">&quot;Property &#x27;sqlSessionFactory&#x27; is required&quot;</span>);</span><br><span class="line">    notNull(executorType, <span class="string">&quot;Property &#x27;executorType&#x27; is required&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">this</span>.sqlSessionFactory = sqlSessionFactory;</span><br><span class="line">    <span class="built_in">this</span>.executorType = executorType;</span><br><span class="line">    <span class="built_in">this</span>.exceptionTranslator = exceptionTranslator;</span><br><span class="line"><span class="comment">// 这里通过 JDK 的代码来生成了一个 sqlSessionProxy 代理的对象.      </span></span><br><span class="line">    <span class="built_in">this</span>.sqlSessionProxy = (SqlSession) newProxyInstance(SqlSessionFactory.class.getClassLoader(),</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Class</span>[] &#123; SqlSession.class &#125;, <span class="keyword">new</span> <span class="title class_">SqlSessionInterceptor</span>());</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>该方法是将 SqlSessionTemplate 给注入到 Spring 容器中啦.</p>
<h1 id="疑惑点"><a href="#疑惑点" class="headerlink" title="疑惑点"></a>疑惑点</h1><p>大家有没有疑惑我们定义的 mapper 接口 好像从这个流程分析下来，并没有提到 ，那么是在上面时候被注入到 Spring 容器中来的呢？</p>
<p>registerBeanDefinitions() 这个方法 , 注入了MapperScannerConfigurer 到 Spring 容器中来了，可以回顾下之前 mybatis 整合 Spring 的时候，我们是通过 xml 配置了这个对象注入到 spring 容器中来的。 那么注入进来的,回调到 org.mybatis.spring.mapper.MapperScannerConfigurer#postProcessBeanDefinitionRegistry 这个方法的时候，就会将扫描并且将我们的mapper接口文件，给注入到 Spring 容器中来的. 然后扫描的包，是根据@MapperScan 解析注解的时候，是有对扫描的包进行解析的.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<p>其实 SpringBoot 整合 MyBatis , 我们从二个切入点来分析是怎么整合进来的. 一是 @MapperScan 注解中的 @Import(MapperScannerRegistrar.class) 将 MapperScannerRegistrar 给导入到 Spring 容器中来, 然后MapperSacnnerRegistrar 来讲 org.mybatis.spring.mapper.MapperScannerConfigurer 给注入到 Spring中来，替换了我们之前用 Spring 整合 Mybatis 的时候，通过xml配置文件整合进来.</p>
<p>二是利用 SpringBoot 提供的 spring-boot-autoconfigure + spring.factories() 来 配置自动注入, 这里注入了 MybatisAutoConfiguration 配置类. 然后注入进来的 MyBatis 配置类做了什么事情呢？ 可以看到这个类中是有做: 注入了 SqlSessionFactory. SqlSessionFactory 又是怎么注入进来的呢？ 可以看到 org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration#sqlSessionFactory 方法是有先创建一个 org.mybatis.spring.SqlSessionFactoryBean 的， 看到 SqlSessionFactoryBean 这个对象，我们就不难想起 Spring + Mybatis 里面的 beans.xml 是将该对象注入到 Spring 容器中来. 这里是直接new的，然后将一些配置属性并满足条件,给set到 SqlSessionFactoryBean 中来，最后调用 org.mybatis.spring.SqlSessionFactoryBean#getObject 方法来获取 SqlSessionFactory.</p>
<p>spring.factories 文件内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Auto Configure</span><br><span class="line">org.springframework.boot.autoconfigure.EnableAutoConfiguration=\</span><br><span class="line">org.mybatis.spring.boot.autoconfigure.MybatisLanguageDriverAutoConfiguration,\</span><br><span class="line">org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration</span><br></pre></td></tr></table></figure>

<p>最后从写的案例里面看, MyBatis 整合 SpringBoot 其实都是在 mybatis —&gt; MyBatis + Spring 等一步一步推导上来的，所以这里不难理解，好的技术都是在有需要和时间的沉淀下一步一步成长起来的.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis工作流程分析</title>
    <url>//article/mybatis_work_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-MyBatis-的工程流程分析"><a href="#📝-MyBatis-的工程流程分析" class="headerlink" title="📝 MyBatis 的工程流程分析"></a>📝 <strong>MyBatis 的工程流程分析</strong></h1><p>MyBatis 是我们在学习Java框架，也就是学习完JavaWeb的知识后,要学习到的一个ORM的框架. 我也是学习&amp;使用过后，再次对源码进行阅读的. 所以这篇文章记录 MyBatis 的一个 work flow.</p>
<p>先放上项目地址 : <a href="https://github.com/ruY9527/mybtatis-analysis/tree/master/mybatis-work-flow">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-work-flow</a></p>
<p>有兴趣的同学,可以clone下来看看.</p>
<h1 id="代码案例"><a href="#代码案例" class="headerlink" title="代码案例"></a>代码案例</h1><p>先放上案列的代码, 然后我们可以挨个的分析</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">InitHelloMyBatis</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="comment">// 读取配置文件.</span></span><br><span class="line">        <span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br><span class="line">        <span class="comment">// 传入读取配置文件的流,使用SqlSessionFactoryBuilder来</span></span><br><span class="line">        <span class="comment">// 构建 SqlSessionFactory.</span></span><br><span class="line">        <span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br><span class="line">        <span class="comment">// 从 SqlSessionFactory 中获取SqlSession会话.</span></span><br><span class="line">        <span class="type">SqlSession</span> <span class="variable">session</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从会话中获取 Mapper.</span></span><br><span class="line">        <span class="type">BlogMapper</span> <span class="variable">blogMapper</span> <span class="operator">=</span> session.getMapper(BlogMapper.class);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 调用查询方法.</span></span><br><span class="line">        <span class="type">TbBlog</span> <span class="variable">tbBlog</span> <span class="operator">=</span> blogMapper.selectBlog(<span class="number">1</span>);</span><br><span class="line">        System.out.println(tbBlog);</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里说下大致流程:</p>
<ul>
<li>使用 Resources 来读取 mybatis-config.xml配置文件, 如果该文件不存在或者读取出来 InputStream 是 null 的话,程序就会抛出 IOException 的错误来.</li>
<li>读取配置没有问题,来到 new SqlSessionFactoryBuilder().build(io) 来构建出一个 SqlSessionFactory 来, 这里构建出来的 SqlSessionFactory 肯定是有已经讲配置文件给全部加载进去了的.</li>
<li>SqlSessionFactory.openSession() 从 SqlSessionFactory 中获取一次会话, 然后可以从会话中获取出接口(BlogMapper)来,这里是不是有点好奇,明明这就是一个接口,也没有实现类,怎么就可以get出一个接口对象来?获取出接口来,然后就可以调用接口中的方法, 根据id查询出数据来.</li>
</ul>
<p>可以看到,根据从官网写的一个列子,从表面来看,代码量并不是很多. 所以接下来点去源码,去跟进源码中的每个方法,到底做了些什么事情.</p>
<h2 id="读取配置文件"><a href="#读取配置文件" class="headerlink" title="读取配置文件"></a>读取配置文件</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">InputStream</span> <span class="variable">mybatisInputStream</span> <span class="operator">=</span> Resources.getResourceAsStream(<span class="string">&quot;mybatis-config.xml&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>org.apache.ibatis.io.Resources (Class).</p>
<p>可以看到MyBatis源码还写了一个 ClassLoader的包装类，通过ClassLoaderWrapper包装类来讲配置文件转化为InputSream.</p>
<p>如果返回的InputStream是null，就会抛出IOException来.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Returns a resource on the classpath as a Stream object</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> loader   The classloader used to fetch the resource</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> resource The resource to find</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> The resource</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> java.io.IOException If the resource cannot be found or read</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">ClassLoaderWrapper</span> <span class="variable">classLoaderWrapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ClassLoaderWrapper</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> InputStream <span class="title function_">getResourceAsStream</span><span class="params">(ClassLoader loader, String resource)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">  <span class="comment">// 利用 ClasssLoaderWrapper.  </span></span><br><span class="line">  <span class="type">InputStream</span> <span class="variable">in</span> <span class="operator">=</span> classLoaderWrapper.getResourceAsStream(resource, loader);</span><br><span class="line">  <span class="keyword">if</span> (in == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IOException</span>(<span class="string">&quot;Could not find resource &quot;</span> + resource);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> in;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>于是我们接着看 ClassLoaderWrapper 是怎么 读取配置文件 &amp; 转化为 InputStream 流的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里返回的是 ClassLoader的数组,如果对ClassLoader不是很了解的话,可以先去百度了解下.</span></span><br><span class="line">ClassLoader[] getClassLoaders(ClassLoader classLoader) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ClassLoader</span>[]&#123;</span><br><span class="line">      <span class="comment">// 传递进来的 </span></span><br><span class="line">      classLoader,</span><br><span class="line">      <span class="comment">// 默认的 ClassLoader</span></span><br><span class="line">      defaultClassLoader,</span><br><span class="line">      <span class="comment">// 根据当前线程获取出来的</span></span><br><span class="line">      Thread.currentThread().getContextClassLoader(),</span><br><span class="line">      <span class="comment">// 根据当前 Class 获取出来的.</span></span><br><span class="line">      getClass().getClassLoader(),</span><br><span class="line">      <span class="comment">// 系统的ClassLoader.</span></span><br><span class="line">      systemClassLoader&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取到了 classLoader的数组,然后对其进行迭代.</span></span><br><span class="line"><span class="comment">// 也就是使用 ClassLoader的  getResourceAsStream 方法,来讲 mybatis-config.xml</span></span><br><span class="line"><span class="comment">// 配置文件转化为 InputStream.</span></span><br><span class="line"><span class="comment">// 最后如果获取到InputStream都是null的话,那么返回的也就是null了.</span></span><br><span class="line"><span class="comment">// 根据上面的说法,返回的如果是null的话,就会出 IOException来.</span></span><br><span class="line">InputStream <span class="title function_">getResourceAsStream</span><span class="params">(String resource, ClassLoader[] classLoader)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (ClassLoader cl : classLoader) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="literal">null</span> != cl) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// try to find the resource as passed</span></span><br><span class="line">        <span class="type">InputStream</span> <span class="variable">returnValue</span> <span class="operator">=</span> cl.getResourceAsStream(resource);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// now, some class loaders want this leading &quot;/&quot;, so we&#x27;ll add it and try again if we didn&#x27;t find the resource</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="literal">null</span> == returnValue) &#123;</span><br><span class="line">          returnValue = cl.getResourceAsStream(<span class="string">&quot;/&quot;</span> + resource);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="literal">null</span> != returnValue) &#123;</span><br><span class="line">          <span class="keyword">return</span> returnValue;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">	  &#125;</span><br></pre></td></tr></table></figure>

<p><strong>至此,MyBatis读取 mybatis-config.xml 配置文件也就是解析完毕,可以看到采用了自己写的 ClassLoaderWrapper来操作的, 传递一种 ClassLoader进来,其默认的&amp;系统&amp;线程的,加一起也是有四种. 最后挨个进来迭代，满足条件的会读取文件转化为InputStream,如果都是null的话,也会返回null.</strong></p>
<h2 id="获取SqlSessionFactory-解析配置文件"><a href="#获取SqlSessionFactory-解析配置文件" class="headerlink" title="获取SqlSessionFactory &amp; 解析配置文件"></a><strong>获取SqlSessionFactory &amp; 解析配置文件</strong></h2><p>new SqlSessionFactoryBuilder() 也是new了一个 SqlSessionFactoryBuild,个人理解 SqlSessionFactoryBuilder 就是专程用来构建出 SqlSessionFactory 来的,毕竟其后面有一个 build 方法.</p>
<p>Problem ? 这里有个问题,为什么不将 SqlSessionFactoryBuilder 的build 方法,修改为静态的 ? 如果修改为静态的话，那就不用new了,就可以直接 SqlSessionFactoryBuilder.build(mybatisInputStream);</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">SqlSessionFactory</span> <span class="variable">sqlSessionFactory</span> <span class="operator">=</span> <span class="keyword">new</span>                     <span class="title class_">SqlSessionFactoryBuilder</span>().build(mybatisInputStream);</span><br></pre></td></tr></table></figure>

<h2 id="SqlSessionFactory"><a href="#SqlSessionFactory" class="headerlink" title="SqlSessionFactory"></a><strong>SqlSessionFactory</strong></h2><p>接着我们来到 SqlSessionFactory 的 build 方法.</p>
<p>这里在 finnaly 中, 可以看到 ErrorContext 利用了 ThreadLocal</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> SqlSessionFactory <span class="title function_">build</span><span class="params">(InputStream inputStream, String environment, Properties properties)</span> &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// 利用传入进来的参数,new出来了一个 XMLConfigBuilder.</span></span><br><span class="line">    <span class="type">XMLConfigBuilder</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLConfigBuilder</span>(inputStream, environment, properties);</span><br><span class="line">    <span class="keyword">return</span> build(parser.parse());</span><br><span class="line">  &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    <span class="keyword">throw</span> ExceptionFactory.wrapException(<span class="string">&quot;Error building SqlSession.&quot;</span>, e);</span><br><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="comment">// 这里对 ThreadLocal 中进行 remove() 操作   </span></span><br><span class="line">    ErrorContext.instance().reset();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// 关闭流.  </span></span><br><span class="line">      inputStream.close();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">      <span class="comment">// Intentionally ignore. Prefer previous error.</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="new-XmlConfigBuilder-方法"><a href="#new-XmlConfigBuilder-方法" class="headerlink" title="new XmlConfigBuilder() 方法:"></a>new XmlConfigBuilder() 方法:</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">XMLConfigBuilder</span><span class="params">(InputStream inputStream, String environment, Properties props)</span> &#123;</span><br><span class="line">  <span class="comment">// 先new一个XMLMapperEntityResolver,再new一个XPathParser,然后就走到下面的构造函数.</span></span><br><span class="line">  <span class="built_in">this</span>(<span class="keyword">new</span> <span class="title class_">XPathParser</span>(inputStream, <span class="literal">true</span>, props, <span class="keyword">new</span> <span class="title class_">XMLMapperEntityResolver</span>()), environment, props);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最后还是走到这个构造方法中来.</span></span><br><span class="line"><span class="keyword">private</span> <span class="title function_">XMLConfigBuilder</span><span class="params">(XPathParser parser, String environment, Properties props)</span> &#123;</span><br><span class="line">  <span class="built_in">super</span>(<span class="keyword">new</span> <span class="title class_">Configuration</span>());</span><br><span class="line">  ErrorContext.instance().resource(<span class="string">&quot;SQL Mapper Configuration&quot;</span>);</span><br><span class="line">  <span class="built_in">this</span>.configuration.setVariables(props);</span><br><span class="line">  <span class="built_in">this</span>.parsed = <span class="literal">false</span>;</span><br><span class="line">  <span class="built_in">this</span>.environment = environment;</span><br><span class="line">  <span class="built_in">this</span>.parser = parser;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">----------------------------------------</span><br><span class="line"><span class="comment">// new XPathParser代码:</span></span><br><span class="line">    </span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">XPathParser</span><span class="params">(InputStream inputStream, <span class="type">boolean</span> validation, Properties variables, EntityResolver entityResolver)</span> &#123;</span><br><span class="line">    <span class="comment">// 普通的构造方法.</span></span><br><span class="line">    <span class="comment">// 对 XPathParser的validation/entityResolver/variables/xpath</span></span><br><span class="line">    <span class="comment">// 的属性进行赋值操作.</span></span><br><span class="line">    commonConstructor(validation, variables, entityResolver);</span><br><span class="line">    <span class="built_in">this</span>.document = createDocument(<span class="keyword">new</span> <span class="title class_">InputSource</span>(inputStream));</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">// createDocument 方法</span></span><br><span class="line">  <span class="keyword">private</span> Document <span class="title function_">createDocument</span><span class="params">(InputSource inputSource)</span> &#123;</span><br><span class="line">    <span class="comment">// important: this must only be called AFTER common constructor</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// 这里通过debug看,返回的对象是DocumentBuilderFactoryImpl</span></span><br><span class="line">      <span class="comment">// 也就是其实现类.  </span></span><br><span class="line">      <span class="type">DocumentBuilderFactory</span> <span class="variable">factory</span> <span class="operator">=</span> DocumentBuilderFactory.newInstance();</span><br><span class="line">     <span class="comment">// 对 factory 的 features(HashMap) 添加值,   </span></span><br><span class="line">      factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, <span class="literal">true</span>);</span><br><span class="line">     <span class="comment">// 对 factory 的 validating 进行赋值  </span></span><br><span class="line">      factory.setValidating(validation);</span><br><span class="line">	 <span class="comment">// 这下面都是对 factory的属性进行赋值操作.	</span></span><br><span class="line">      factory.setNamespaceAware(<span class="literal">false</span>);</span><br><span class="line">      factory.setIgnoringComments(<span class="literal">true</span>);</span><br><span class="line">      factory.setIgnoringElementContentWhitespace(<span class="literal">false</span>);</span><br><span class="line">      factory.setCoalescing(<span class="literal">false</span>);</span><br><span class="line">      factory.setExpandEntityReferences(<span class="literal">true</span>);</span><br><span class="line">		</span><br><span class="line">      <span class="comment">// 可以看到 return new DocumentBuilderImpl</span></span><br><span class="line">      <span class="comment">// 最后返回的也是其实现类. </span></span><br><span class="line">      <span class="type">DocumentBuilder</span> <span class="variable">builder</span> <span class="operator">=</span> factory.newDocumentBuilder();</span><br><span class="line">      builder.setEntityResolver(entityResolver);</span><br><span class="line">      <span class="comment">// 设置错误的handler,可以看到ErrorHandler是接口,这里是匿名实现的</span></span><br><span class="line">      <span class="comment">// 也就是直接new了接口,然后重写其方法.  </span></span><br><span class="line">      builder.setErrorHandler(<span class="keyword">new</span> <span class="title class_">ErrorHandler</span>() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">error</span><span class="params">(SAXParseException exception)</span> <span class="keyword">throws</span> SAXException &#123;</span><br><span class="line">          <span class="keyword">throw</span> exception;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">fatalError</span><span class="params">(SAXParseException exception)</span> <span class="keyword">throws</span> SAXException &#123;</span><br><span class="line">          <span class="keyword">throw</span> exception;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">warning</span><span class="params">(SAXParseException exception)</span> <span class="keyword">throws</span> SAXException &#123;</span><br><span class="line">          <span class="comment">// NOP</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;);</span><br><span class="line">      <span class="comment">//   DocumentBuilderImpl 的 parse 解析方法</span></span><br><span class="line">      <span class="keyword">return</span> builder.parse(inputSource);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error creating document instance.  Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">-------------</span><br><span class="line"><span class="comment">//   builder.parse(inputSource)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Document <span class="title function_">parse</span><span class="params">(InputSource is)</span> <span class="keyword">throws</span> SAXException, IOException &#123;</span><br><span class="line">        <span class="keyword">if</span> (is == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(</span><br><span class="line">                DOMMessageFormatter.formatMessage(DOMMessageFormatter.DOM_DOMAIN,</span><br><span class="line">                <span class="string">&quot;jaxp-null-input-source&quot;</span>, <span class="literal">null</span>));</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="comment">// fSchemaValidator 是 null ,跳过.</span></span><br><span class="line">        <span class="keyword">if</span> (fSchemaValidator != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (fSchemaValidationManager != <span class="literal">null</span>) &#123;</span><br><span class="line">                fSchemaValidationManager.reset();</span><br><span class="line">                fUnparsedEntityHandler.reset();</span><br><span class="line">            &#125;</span><br><span class="line">            resetSchemaValidator();</span><br><span class="line">        &#125;</span><br><span class="line">  <span class="comment">// 使用 xml 的相关类对 is 进行解析  </span></span><br><span class="line">        domParser.parse(is);</span><br><span class="line"> <span class="comment">//  ?   </span></span><br><span class="line">        <span class="type">Document</span> <span class="variable">doc</span> <span class="operator">=</span> domParser.getDocument();</span><br><span class="line"> <span class="comment">// ? 这些解析 Document 的地方.....   </span></span><br><span class="line">        domParser.dropDocumentReferences();</span><br><span class="line">        <span class="keyword">return</span> doc;</span><br><span class="line">    &#125;    </span><br><span class="line"></span><br><span class="line">---------------</span><br><span class="line"><span class="comment">// 最后看到 this 构造函数.</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="title function_">XMLConfigBuilder</span><span class="params">(XPathParser parser, String environment, Properties props)</span> &#123;</span><br><span class="line">    <span class="comment">/** new Configuration() 中,TypeAliasRegistry typeAliasRegistry中的 typeAliases,</span></span><br><span class="line"><span class="comment">    *   在初始化这个对象的时候,就默认设置了一些别名配置.</span></span><br><span class="line"><span class="comment">    *   初始化的时候,还有对 LanguageDriverRegistry 的 LANGUAGE_DRIVER_MAP 赋值.</span></span><br><span class="line"><span class="comment">    *  父类 :  BaseBuilder抽象类.</span></span><br><span class="line"><span class="comment">    *  然后调用super方法,将configuration赋值父类的configuration</span></span><br><span class="line"><span class="comment">    *  同时将 configuration的typeAliasRegistry和typeHandlerRegistry也赋值</span></span><br><span class="line"><span class="comment">    *  给当前的这个对象.</span></span><br><span class="line"><span class="comment">    *   </span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="built_in">super</span>(<span class="keyword">new</span> <span class="title class_">Configuration</span>());</span><br><span class="line">    <span class="comment">// instance() 方法是往 ThreadLocal里面去set了一个ErrorContext</span></span><br><span class="line">    <span class="comment">// 最后会在finnaly中进行remove掉.</span></span><br><span class="line">    ErrorContext.instance().resource(<span class="string">&quot;SQL Mapper Configuration&quot;</span>);</span><br><span class="line">    <span class="comment">// 将 props 赋值到 configuration 的 variable 参数.</span></span><br><span class="line">    <span class="built_in">this</span>.configuration.setVariables(props);</span><br><span class="line">    <span class="comment">// 表示还没有被解析</span></span><br><span class="line">    <span class="built_in">this</span>.parsed = <span class="literal">false</span>;</span><br><span class="line">    <span class="built_in">this</span>.environment = environment;</span><br><span class="line">    <span class="built_in">this</span>.parser = parser;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>到这里,就可以看到 this构造方法以及其之前还有new对象的方法,都已经走完了. 这上面的方法,基本都是再为后面的解析xml文件做准备, 并且还有一些初始化数据的赋值操作.</p>
<p><strong>Note</strong> : 注意这里的 BaseBuilder是抽象类,其实现类是有好几个的. 这种写法,其实是将子类的一些common的方法,写入到 BaseBuilder父类中,然后不同的方法,需要子类自己去重写这个方法实现自己的业务逻辑. 当然一些参数也是可以放在抽象类中.</p>
<h2 id="build-parser-parse-方法"><a href="#build-parser-parse-方法" class="headerlink" title="build(parser.parse())方法"></a><strong>build(parser.parse())方法</strong></h2><p>parser.parse() 方法 :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Configuration <span class="title function_">parse</span><span class="params">()</span> &#123;</span><br><span class="line">  <span class="comment">// 用 parsed 来控制是否解析过,如果已经解析过了,那就抛出异常.  </span></span><br><span class="line">  <span class="keyword">if</span> (parsed) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Each XMLConfigBuilder can only be used once.&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  parsed = <span class="literal">true</span>;</span><br><span class="line">  <span class="comment">//   </span></span><br><span class="line">  parseConfiguration(parser.evalNode(<span class="string">&quot;/configuration&quot;</span>));</span><br><span class="line">  <span class="keyword">return</span> configuration;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">---------------------------------</span><br><span class="line"><span class="comment">// parseConfiguration</span></span><br><span class="line"><span class="comment">// 这里 debug 可以看到 root 是 configuration 的配置文件信息.   </span></span><br><span class="line"><span class="comment">// 这里可以初步看到实对 我们的配置文件mybatis-config.xml进行解析,并且加载到 configuration中.</span></span><br><span class="line"><span class="comment">// 后面我们跟着官网文档一步一步的阅读,会有专门对解析配置的源码进行分析.    </span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">parseConfiguration</span><span class="params">(XNode root)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">//issue #117 read properties first</span></span><br><span class="line">      <span class="comment">//   </span></span><br><span class="line">      propertiesElement(root.evalNode(<span class="string">&quot;properties&quot;</span>));</span><br><span class="line">      <span class="type">Properties</span> <span class="variable">settings</span> <span class="operator">=</span> settingsAsProperties(root.evalNode(<span class="string">&quot;settings&quot;</span>));</span><br><span class="line">      loadCustomVfs(settings);</span><br><span class="line">      loadCustomLogImpl(settings);</span><br><span class="line">      typeAliasesElement(root.evalNode(<span class="string">&quot;typeAliases&quot;</span>));</span><br><span class="line">      pluginElement(root.evalNode(<span class="string">&quot;plugins&quot;</span>));</span><br><span class="line">      objectFactoryElement(root.evalNode(<span class="string">&quot;objectFactory&quot;</span>));</span><br><span class="line">      objectWrapperFactoryElement(root.evalNode(<span class="string">&quot;objectWrapperFactory&quot;</span>));</span><br><span class="line">      reflectorFactoryElement(root.evalNode(<span class="string">&quot;reflectorFactory&quot;</span>));</span><br><span class="line">      settingsElement(settings);</span><br><span class="line">      <span class="comment">// read it after objectFactory and objectWrapperFactory issue #631</span></span><br><span class="line">      environmentsElement(root.evalNode(<span class="string">&quot;environments&quot;</span>));</span><br><span class="line">      databaseIdProviderElement(root.evalNode(<span class="string">&quot;databaseIdProvider&quot;</span>));</span><br><span class="line">      typeHandlerElement(root.evalNode(<span class="string">&quot;typeHandlers&quot;</span>));</span><br><span class="line">      mapperElement(root.evalNode(<span class="string">&quot;mappers&quot;</span>));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BuilderException</span>(<span class="string">&quot;Error parsing SQL Mapper Configuration. Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这里是对 parser.parse() 调用玩返回的 Configuration 传入到新创建的 DefaultSqlSessionFactory 对象中.</p>
<p>也就是说,我们拿到的 SqlSessionFactory 是 DefaultSqlSessionFactory.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> SqlSessionFactory <span class="title function_">build</span><span class="params">(Configuration config)</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">DefaultSqlSessionFactory</span>(config);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="获取-SqlSession"><a href="#获取-SqlSession" class="headerlink" title="获取 SqlSession"></a><strong>获取 SqlSession</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">SqlSession</span> <span class="variable">session</span> <span class="operator">=</span> sqlSessionFactory.openSession();</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.apache.ibatis.session.defaults.DefaultSqlSessionFactory#openSessionFromDataSource</span></span><br><span class="line"><span class="comment">// 看到这个方法,直接跟进到这个方法来.</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> SqlSession <span class="title function_">openSessionFromDataSource</span><span class="params">(ExecutorType execType, TransactionIsolationLevel level, <span class="type">boolean</span> autoCommit)</span> &#123;</span><br><span class="line">    <span class="type">Transaction</span> <span class="variable">tx</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        </span><br><span class="line"><span class="comment">// 从 configuration中获取出environment来,这里的 getEnvironment对应的是</span></span><br><span class="line"><span class="comment">// 标签的 &lt;environment&gt;  里面的内容</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.mapping.Environment</span></span><br><span class="line"><span class="comment">// 可以看到这个对象,id对应mybatis-config.xml中的environment id</span></span><br><span class="line"><span class="comment">// datasource 对应  environment &gt; dataSource 字段.</span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">Environment</span> <span class="variable">environment</span> <span class="operator">=</span> configuration.getEnvironment();</span><br><span class="line"><span class="comment">// 根据    environment 来获取 TransactionFactory,也就是MyBatis的事务工厂.</span></span><br><span class="line"><span class="comment">// debug 是可以看到  environment 中是有一个JdbcTransactionFactory的,</span></span><br><span class="line"><span class="comment">// 如果没用的话,就会自己new一个 ManagedTransactionFactory 来.        </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">TransactionFactory</span> <span class="variable">transactionFactory</span> <span class="operator">=</span> getTransactionFactoryFromEnvironment(environment);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在 JdbcTransactionFactory 中new出了一个 JdbcTransaction</span></span><br><span class="line"><span class="comment">// 也就是new了一个JDBC事务.</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.transaction.jdbc.JdbcTransaction,</span></span><br><span class="line"><span class="comment">// 可以看到 JdbcTransaction 中有commit / rollback的方法,</span></span><br><span class="line"><span class="comment">// 也就是说这个地方就是对事务进行操作的地方        </span></span><br><span class="line">      tx = transactionFactory.newTransaction(environment.getDataSource(), level, autoCommit);</span><br><span class="line"><span class="comment">// 这里是获取是执行器,</span></span><br><span class="line"><span class="comment">// 具体代码: org.apache.ibatis.session.Configuration#newExecutor(org.apache.ibatis.transaction.Transaction, org.apache.ibatis.session.ExecutorType)</span></span><br><span class="line"><span class="comment">// 这里有 SIMPLE, REUSE, BATCH ,CachingExecutor 还可以在 plugin 中自己定义.</span></span><br><span class="line"><span class="comment">//executor = (Executor) interceptorChain.pluginAll(executor); 从这行代码可以看到,</span></span><br><span class="line"><span class="comment">// 其实还是可以自己扩展的.        </span></span><br><span class="line"><span class="comment">//org.apache.ibatis.plugin.InterceptorChain        </span></span><br><span class="line">      <span class="keyword">final</span> <span class="type">Executor</span> <span class="variable">executor</span> <span class="operator">=</span> configuration.newExecutor(tx, execType);</span><br><span class="line"><span class="comment">// 最后 new 出了一个默认的 SqlSession 会话.</span></span><br><span class="line"><span class="comment">// 该会话中存有 configuration / executor 等核心东西.        </span></span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">DefaultSqlSession</span>(configuration, executor, autoCommit);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      closeTransaction(tx); <span class="comment">// may have fetched a connection so lets call close()</span></span><br><span class="line">      <span class="keyword">throw</span> ExceptionFactory.wrapException(<span class="string">&quot;Error opening session.  Cause: &quot;</span> + e, e);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line"><span class="comment">// 最后还是不忘记对使用过的ThreadLocal 进行remove 操作.        </span></span><br><span class="line">      ErrorContext.instance().reset();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>至此, 可以看到 MyBatis 从SqlSessionFactory中获取出来SqlSession会话, 也可以理解为几个步骤.</p>
<p>首先获取事务工厂, 然后再从事务工厂中获取一个事务来, JdbcTransaction 有兴趣的同学可以看下这个类,里面也是封装了写 commit &#x2F; rollback等方法. 再接着获取出 执行器(Executor),这里从代码哪里看,执行器还是有几种类型的,也执行自定义. 最后new了一个 DefaultSqlSession 回去.</p>
<h2 id="session-getMapper-BlogMapper-class"><a href="#session-getMapper-BlogMapper-class" class="headerlink" title="session.getMapper(BlogMapper.class)"></a><strong>session.getMapper(BlogMapper.class)</strong></h2><p>接着看,上一步返回的session,是怎么获取到我们写的Mapper接口文件(Mapper这种文件,在解析配置文件的时候,其实就已经解析到MyBatis的configuration里面去了).</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getMapper</span><span class="params">(Class&lt;T&gt; type, SqlSession sqlSession)</span> &#123;</span><br><span class="line"> <span class="comment">// knownMappers  中 key 是我们定义接口的Class,value是MapperProxyFactory,</span></span><br><span class="line"><span class="comment">// MapperProxyFactory中的mapperInterface中存放了我们的接口class    </span></span><br><span class="line">  <span class="keyword">final</span> MapperProxyFactory&lt;T&gt; mapperProxyFactory = (MapperProxyFactory&lt;T&gt;) knownMappers.get(type);</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 如果获取出来的是null,那么MyBatis就认为你传入进来的接口是不存在的,就会抛出异常来.    </span></span><br><span class="line">  <span class="keyword">if</span> (mapperProxyFactory == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Type &quot;</span> + type + <span class="string">&quot; is not known to the MapperRegistry.&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 满足条件的话,调用newInstance方法,从方法名字上看,是创建一个instance的实例.      </span></span><br><span class="line">    <span class="keyword">return</span> mapperProxyFactory.newInstance(sqlSession);</span><br><span class="line">  &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Error getting mapper instance. Cause: &quot;</span> + e, e);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">-------------------------------------------</span><br><span class="line"><span class="comment">// mapperProxyFactory.newInstance(sqlSession) 代码</span></span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">  <span class="keyword">public</span> T <span class="title function_">newInstance</span><span class="params">(SqlSession sqlSession)</span> &#123;</span><br><span class="line">    <span class="comment">// new 了一个 MapperProxy对象.</span></span><br><span class="line">    <span class="keyword">final</span> MapperProxy&lt;T&gt; mapperProxy = <span class="keyword">new</span> <span class="title class_">MapperProxy</span>&lt;&gt;(sqlSession, mapperInterface, methodCache);</span><br><span class="line">    <span class="keyword">return</span> newInstance(mapperProxy);</span><br><span class="line">  &#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">// 最后可以看到使用 Proxy.newProxyInstance方法来创建的一个对象.</span></span><br><span class="line">  <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">  <span class="keyword">protected</span> T <span class="title function_">newInstance</span><span class="params">(MapperProxy&lt;T&gt; mapperProxy)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (T) Proxy.newProxyInstance(mapperInterface.getClassLoader(), <span class="keyword">new</span> <span class="title class_">Class</span>[] &#123; mapperInterface &#125;, mapperProxy);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">-------</span><br><span class="line"><span class="comment">// 如果你是debug模式的话,那么你可以看到BlogMapper的对象地址池在 debug 中显示的值.</span></span><br><span class="line"><span class="comment">// org.apache.ibatis.binding.MapperProxy@ef9296d    </span></span><br><span class="line"><span class="type">BlogMapper</span> <span class="variable">blogMapper</span> <span class="operator">=</span> session.getMapper(BlogMapper.class);</span><br></pre></td></tr></table></figure>

<p>从SqlSession 中获取 BlogMapper我们写的mapper流程, 先从 knownMappers 中根据key获取出来之前加载配置已经加载完毕的信息,如果没用的话,就会抛出没有的异常. 最后使用 Proxy.newProxyIntsance来生成的一个类似接口实现类的代码,不同的是, 在 new MapperProxy 的时候,就已经将接下来需要的信息全部传入进去.</p>
<h2 id="blogMapper-selectBlog-1-方法"><a href="#blogMapper-selectBlog-1-方法" class="headerlink" title="blogMapper.selectBlog(1) 方法"></a><strong>blogMapper.selectBlog(1) 方法</strong></h2><p>竟然 BlogMapper是通过Proxy.newInstance获取出来的,那它是怎么查询的数据库? 又是怎么将字段给映射到 Object一一对应的呢 ?</p>
<p>debug会走到 MapperProxy的invoke方法来</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Object <span class="title function_">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (Object.class.equals(method.getDeclaringClass())) &#123;</span><br><span class="line">      <span class="keyword">return</span> method.invoke(<span class="built_in">this</span>, args);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> cachedInvoker(method).invoke(proxy, method, args, sqlSession);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">    <span class="keyword">throw</span> ExceptionUtil.unwrapThrowable(t);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">------------------</span><br><span class="line"><span class="comment">// 通过 invoke 方法, 走 mapperMethod的execute方法,来到了这里.</span></span><br><span class="line"><span class="comment">// switch 有 INSERT/UPDATE/DELETE/SELECT/FLUSH,如果这几种没有匹配到的话,就会抛出异常来.    </span></span><br><span class="line">  <span class="keyword">public</span> Object <span class="title function_">execute</span><span class="params">(SqlSession sqlSession, Object[] args)</span> &#123;</span><br><span class="line">    Object result;</span><br><span class="line">    <span class="keyword">switch</span> (command.getType()) &#123;</span><br><span class="line">            </span><br><span class="line"><span class="comment">// 不难看到 INSERT/UPDATE/DELETE都是先调用 convertArgsToSqlCommandParam 方法,</span></span><br><span class="line"><span class="comment">// 也就是先将参数转化为sql,然后将执行的结果 赋值 给 result 参数.            </span></span><br><span class="line">      <span class="keyword">case</span> INSERT: &#123;</span><br><span class="line">        <span class="type">Object</span> <span class="variable">param</span> <span class="operator">=</span> method.convertArgsToSqlCommandParam(args);</span><br><span class="line">        result = rowCountResult(sqlSession.insert(command.getName(), param));</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">case</span> UPDATE: &#123;</span><br><span class="line">        <span class="type">Object</span> <span class="variable">param</span> <span class="operator">=</span> method.convertArgsToSqlCommandParam(args);</span><br><span class="line">        result = rowCountResult(sqlSession.update(command.getName(), param));</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">case</span> DELETE: &#123;</span><br><span class="line">        <span class="type">Object</span> <span class="variable">param</span> <span class="operator">=</span> method.convertArgsToSqlCommandParam(args);</span><br><span class="line">        result = rowCountResult(sqlSession.delete(command.getName(), param));</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 如果是 select 语句,可以根据返回值来分类,如果是void&amp;&amp;method.hasResultHandler,就会返回null</span></span><br><span class="line"><span class="comment">// 多个 / Map类型  /    Cursor 类型   /  最后查询一个        </span></span><br><span class="line">      <span class="keyword">case</span> SELECT:</span><br><span class="line">        <span class="keyword">if</span> (method.returnsVoid() &amp;&amp; method.hasResultHandler()) &#123;</span><br><span class="line">          executeWithResultHandler(sqlSession, args);</span><br><span class="line">          result = <span class="literal">null</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsMany()) &#123;</span><br><span class="line">          result = executeForMany(sqlSession, args);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsMap()) &#123;</span><br><span class="line">          result = executeForMap(sqlSession, args);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsCursor()) &#123;</span><br><span class="line">          result = executeForCursor(sqlSession, args);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="type">Object</span> <span class="variable">param</span> <span class="operator">=</span> method.convertArgsToSqlCommandParam(args);</span><br><span class="line">          result = sqlSession.selectOne(command.getName(), param);</span><br><span class="line">          <span class="keyword">if</span> (method.returnsOptional()</span><br><span class="line">              &amp;&amp; (result == <span class="literal">null</span> || !method.getReturnType().equals(result.getClass()))) &#123;</span><br><span class="line">            result = Optional.ofNullable(result);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line"> <span class="comment">// 刷新会话.           </span></span><br><span class="line">      <span class="keyword">case</span> FLUSH:</span><br><span class="line">        result = sqlSession.flushStatements();</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">default</span>:</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Unknown execution method for: &quot;</span> + command.getName());</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 如果result 是 null, 方法返回的修饰符是private并且 返回值不是void的话,就会抛出异常.    </span></span><br><span class="line">    <span class="keyword">if</span> (result == <span class="literal">null</span> &amp;&amp; method.getReturnType().isPrimitive() &amp;&amp; !method.returnsVoid()) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BindingException</span>(<span class="string">&quot;Mapper method &#x27;&quot;</span> + command.getName()</span><br><span class="line">          + <span class="string">&quot; attempted to return null from a method with a primitive return type (&quot;</span> + method.getReturnType() + <span class="string">&quot;).&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这里可以看到,先是对 INSERT &#x2F; UPDATE &#x2F; DELETE &#x2F; SELECT 进行分类处理, 然后对再分别根据不同的类型进行处理. 都是先有转化为sql,然后将执行结果赋值给result.</p>
<p>至于里面详细的查询执行sql,还有动态sql,每次会话缓存等,后面看到详细的情况再一一说明. 这里只是对MyBatis的基本工作进行了一个梳理. 然后后面再根据基础梳理,再来挨个击碎他们.</p>
<p>至此, MyBatis的入门分析流程是结束的. 理解起来,应该还不是那么难.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<p>根据 com.iyang.mybatis.InitHelloMyBatis , 也就是入门的demo来梳理下流程.</p>
<ol>
<li>读取配置文件,也就是将配置文件读取,转化为inptStream流.</li>
<li>利用 SqlSessionFactoryBuilder 来 解析流, 起内部又利用 BaseBuilder(其又很多实现类,这里用的XMLConfigBuilder)也解析xml配置文件. Configuration configuration 该类中是保存着xml配置文件的很多信息. 然后 DefaultSqlSessionFactory 中有configuration字段,也就是属性.</li>
<li>然后从 DefaultSqlSessionFactory 中获取 SqlSession来, 并且也会是否开启事务(参考:org.apache.ibatis.transaction.jdbc.JdbcTransaction)类,然后获取 Executor,Executor也是有几种种类的,也可以自己自定义,最后返回一个 DefaultSqlSession 来.</li>
<li>然后从 SqlSession 中获取我们的接口Mapper, 最后也是利用 Proxy.newProxyInstance 来生成的接口,也就是代理(这里打印出地址池或者debug看地址池,就会很明显的看到是代理对象).</li>
<li>最后走查询的方法, 也就是走到了 MapperProxy 来. 可以看到MapperProxy里面是有sqlSession的,而SqlSession是有 Executor&#x2F;configuration&#x2F;autoCommit等信息的, 有了sqlSession,就剩下执行sql和映射sql查询出来的结果来了(这里是 mapperMethod.execute(sqlSession, args) —&gt; org.apache.ibatis.binding.MapperMethod#execute 走到这里来了,这里之后就会分类进行处理,然后映射sql语句).</li>
<li>至此,一个 MyBatis 的 HelloWorld分析流程是完毕的.</li>
</ol>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>mybatis与spring整合阅读</title>
    <url>//article/mybatis_spring_all_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 题记</h1><p>MyBatis 与 Spring 整合操作. 在我们入门学习 SSM 等东西的时候，就发现了任何东西，最后都是逃不过与Spring整合起来的道路. 然后这里看完 MyBatis 整合完 Spring 之后，那么之后一些其他的第三方，比如axon&#x2F;redis&#x2F;apollo&#x2F;shiro 等这些东西，如果要整合 Spring 的时候，是不是也是相似的整合方式呢？</p>
<p>这个需要我们看完 MyBatis 与 Spring 之后，探究其整合的操作.</p>
<h1 id="入门"><a href="#入门" class="headerlink" title="入门"></a>入门</h1><p>分几个步骤，操作一把即可,带你回到哪个 SM 时代，不过这回是没有了 tomcat 的.</p>
<p>先放上一个完成的整合地址 : <a href="https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello</a> 如果不要看下面流程的,一步跳过即可.</p>
<ol>
<li>先创建一个 maven 项目，引入依赖. 依赖参考地址 : <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/pom.xml">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/pom.xml</a></li>
<li>db配置 : <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/src/main/resources/db.properties">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/src/main/resources/db.properties</a></li>
<li>MyBatis配置: <a href="https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src/main/resources/mybatis">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src/main/resources/mybatis</a></li>
<li>Spring 配置: <a href="https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/src/main/resources/spring-beans.xml">https://github.com/baoyang23/mybtatis-analysis/blob/master/mybatis-spring-hello/src/main/resources/spring-beans.xml</a></li>
<li>最后,来份我们熟悉的 <a href="https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src/main/resources/sql">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src/main/resources/sql</a> mapper.xml 文件.</li>
<li>不忘记再来一份代码 : <a href="https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src">https://github.com/baoyang23/mybtatis-analysis/tree/master/mybatis-spring-hello/src</a> 这些直接跑测试类即可.</li>
</ol>
<p>跟着这上面的几个步骤，就可以搭建完一个项目. 然后喊上我们的 永哥， 打上传说中的 debug , 疯狂的调试看每步干了什么事.</p>
<p>这个的时候，可以跑下测试类，是ok的.</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>这里我们首先想到的是我们引入的依赖,是不是有个 mybatis-spring 的依赖. 从这个依赖，可以很明显的看出来，就是通过这个依赖，将 MyBatis 和 Spring 整合起来的。</p>
<p>然后再想想，我们除了这个依赖的话，还再哪里有使用到一些 Spring 和 MyBatis 的东西呢？ 然后看到 spring-beans.xml 这个xml配置, 可以看到 org.mybatis.spring.SqlSessionFactoryBean 给注入到 bean 里面来了.org.mybatis.spring.mapper.MapperScannerConfigurer也是给注入到 bean 里面来了. 并且二者都有通过来进行属性设置值操作.</p>
<p>那么,我们就基于这二个类的源码开始阅读.</p>
<h1 id="方法阅读"><a href="#方法阅读" class="headerlink" title="方法阅读"></a>方法阅读</h1><h2 id="SqlSessionFactoryBean-org-mybatis-spring-SqlSessionFactoryBean"><a href="#SqlSessionFactoryBean-org-mybatis-spring-SqlSessionFactoryBean" class="headerlink" title="SqlSessionFactoryBean (org.mybatis.spring.SqlSessionFactoryBean)"></a><strong>SqlSessionFactoryBean (org.mybatis.spring.SqlSessionFactoryBean)</strong></h2><p>这里 SqlSessionFactoryBean 是实现了很多接口,这些接口都是Spring的.</p>
<p>FactoryBean 工厂bean,点进去可以看到,其有方法getObject()&#x2F;getObjectType等方法获取bean的,然后加上泛型,也就是这里获取的 getObject就是泛型.</p>
<p>InitializingBean: afterPropertiesSet 初始化 bean 的时候，会调用该方法.</p>
<p>ApplicationEvent: Spring的事件传播机制，就是使用的这种方式.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**  可以看到这个类实现了 Spring 这个多接口,那么就有个问题,实现了这么多接口的方法,到底是哪个方法先执行的呢？ 如果你对Spring源码很熟悉的话,是有可能清楚的,但是还是会有点绕的. </span></span><br><span class="line"><span class="comment">这里我们给 getObject/afterPropertiesSet/onApplicationEvent这三个方法打上断点来进行debug,</span></span><br><span class="line"><span class="comment">debug每走的一步,就是执行的先后顺序。如果不是特别熟悉源码的执行顺序,这种笨方法其实也是可以的.</span></span><br><span class="line"><span class="comment">*</span></span><br><span class="line"><span class="comment">* 所以这里debug的执行顺序是 : afterPropertiesSet --&gt; getObject  ---&gt; onApplicationEvent</span></span><br><span class="line"><span class="comment">* 于是我们就跟着这个顺序来阅读.</span></span><br><span class="line"><span class="comment">* 注意在调用这些方法之前,&lt;property&gt;标签的值都是已经赋值进来了的,是通过反射走的set 方法进来的.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SqlSessionFactoryBean</span></span><br><span class="line">    <span class="keyword">implements</span> <span class="title class_">FactoryBean</span>&lt;SqlSessionFactory&gt;, InitializingBean, ApplicationListener&lt;ApplicationEvent&gt; &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现FactoryBean 方法,这里是实现了该接口的三个方法. 其实这里的 isSingle是可以不用实现的</span></span><br><span class="line"><span class="comment">// 因为接口是用 default 来修饰的.</span></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * 该方法是判断并且再次确认 SqlSessionFactory是不是有了. 如果没有的话,就会调用afterProperties来初始化.</span></span><br><span class="line"><span class="comment">   * &#123;<span class="doctag">@inheritDoc</span>&#125;</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> SqlSessionFactory <span class="title function_">getObject</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.sqlSessionFactory == <span class="literal">null</span>) &#123;</span><br><span class="line">      afterPropertiesSet();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.sqlSessionFactory;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> Class&lt;? <span class="keyword">extends</span> <span class="title class_">SqlSessionFactory</span>&gt; getObjectType() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.sqlSessionFactory == <span class="literal">null</span> ? SqlSessionFactory.class : <span class="built_in">this</span>.sqlSessionFactory.getClass();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isSingleton</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// InitializingBean 实现的方法</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"><span class="comment">// 先对 dataSource/sqlSessionFactoryBuilder进行非null的判断.</span></span><br><span class="line">    notNull(dataSource, <span class="string">&quot;Property &#x27;dataSource&#x27; is required&quot;</span>);</span><br><span class="line">    notNull(sqlSessionFactoryBuilder, <span class="string">&quot;Property &#x27;sqlSessionFactoryBuilder&#x27; is required&quot;</span>);</span><br><span class="line">    state((configuration == <span class="literal">null</span> &amp;&amp; configLocation == <span class="literal">null</span>) || !(configuration != <span class="literal">null</span> &amp;&amp; configLocation != <span class="literal">null</span>),</span><br><span class="line">        <span class="string">&quot;Property &#x27;configuration&#x27; and &#x27;configLocation&#x27; can not specified with together&quot;</span>);</span><br><span class="line"><span class="comment">// 这里构建出 一个 sqlSessionFactory工厂来,想想我们最初再看单个MyBatis项目的时候,是不是也有一个获取SqlSessionFactroy的方法,然后从sqlSessionFactory会话中获取出SqlSession来.</span></span><br><span class="line">    <span class="built_in">this</span>.sqlSessionFactory = buildSqlSessionFactory();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ApplicationListener实现方法</span></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * failFast 时ture 并且传过来的 event是 ContextRefreshedEvent的话,就会进来.</span></span><br><span class="line"><span class="comment">   *  这里目前都是调用get方法,没有很仔细看出其作用.</span></span><br><span class="line"><span class="comment">   * &#123;<span class="doctag">@inheritDoc</span>&#125;</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onApplicationEvent</span><span class="params">(ApplicationEvent event)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (failFast &amp;&amp; event <span class="keyword">instanceof</span> ContextRefreshedEvent) &#123;</span><br><span class="line">      <span class="comment">// fail-fast -&gt; check all statements are completed</span></span><br><span class="line">      <span class="built_in">this</span>.sqlSessionFactory.getConfiguration().getMappedStatementNames();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="org-mybatis-spring-SqlSessionFactoryBean-buildSqlSessionFactory"><a href="#org-mybatis-spring-SqlSessionFactoryBean-buildSqlSessionFactory" class="headerlink" title="org.mybatis.spring.SqlSessionFactoryBean#buildSqlSessionFactory"></a><strong>org.mybatis.spring.SqlSessionFactoryBean#buildSqlSessionFactory</strong></h2><p>该方法需要单独拿出来说下,因为内容还是比较多的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Build a &#123;<span class="doctag">@code</span> SqlSessionFactory&#125; instance.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * The default implementation uses the standard MyBatis &#123;<span class="doctag">@code</span> XMLConfigBuilder&#125; API to build a</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> SqlSessionFactory&#125; instance based on a Reader. Since 1.3.0, it can be specified a &#123;<span class="doctag">@link</span> Configuration&#125;</span></span><br><span class="line"><span class="comment"> * instance directly(without config file).</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> SqlSessionFactory</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> Exception</span></span><br><span class="line"><span class="comment"> *           if configuration is failed</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> SqlSessionFactory <span class="title function_">buildSqlSessionFactory</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">final</span> Configuration targetConfiguration;</span><br><span class="line"></span><br><span class="line">  <span class="type">XMLConfigBuilder</span> <span class="variable">xmlConfigBuilder</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    </span><br><span class="line"> <span class="comment">// 这里分为configuration/ configLocation / 非前二者(可以理解为默认的).</span></span><br><span class="line"> <span class="comment">// 三种处理方式.   </span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.configuration != <span class="literal">null</span>) &#123;</span><br><span class="line">    targetConfiguration = <span class="built_in">this</span>.configuration;</span><br><span class="line">    <span class="keyword">if</span> (targetConfiguration.getVariables() == <span class="literal">null</span>) &#123;</span><br><span class="line">      targetConfiguration.setVariables(<span class="built_in">this</span>.configurationProperties);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">this</span>.configurationProperties != <span class="literal">null</span>) &#123;</span><br><span class="line">      targetConfiguration.getVariables().putAll(<span class="built_in">this</span>.configurationProperties);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">this</span>.configLocation != <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">// 这里就是我们配置的情况 </span></span><br><span class="line"><span class="comment">// org.apache.ibatis.builder.xml.XMLConfigBuilder#XMLConfigBuilder(java.io.InputStream, java.lang.String, java.util.Properties),可以看到这个熟悉的操作,也就是我们单个解析 MyBatis的时候有进行分析过的.      </span></span><br><span class="line">    xmlConfigBuilder = <span class="keyword">new</span> <span class="title class_">XMLConfigBuilder</span>(<span class="built_in">this</span>.configLocation.getInputStream(), <span class="literal">null</span>, <span class="built_in">this</span>.configurationProperties);</span><br><span class="line"><span class="comment">// 获取出 configuration 配置信息.      </span></span><br><span class="line">    targetConfiguration = xmlConfigBuilder.getConfiguration();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    LOGGER.debug(</span><br><span class="line">        () -&gt; <span class="string">&quot;Property &#x27;configuration&#x27; or &#x27;configLocation&#x27; not specified, using default MyBatis Configuration&quot;</span>);</span><br><span class="line">    targetConfiguration = <span class="keyword">new</span> <span class="title class_">Configuration</span>();</span><br><span class="line">    Optional.ofNullable(<span class="built_in">this</span>.configurationProperties).ifPresent(targetConfiguration::setVariables);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里采用 Optional,如果objectFactory不是null的话,就会调用targetConfiguration的 setObjectFactory方法.下面这二个是同理.</span></span><br><span class="line">  Optional.ofNullable(<span class="built_in">this</span>.objectFactory).ifPresent(targetConfiguration::setObjectFactory);</span><br><span class="line">  Optional.ofNullable(<span class="built_in">this</span>.objectWrapperFactory).ifPresent(targetConfiguration::setObjectWrapperFactory);</span><br><span class="line">  Optional.ofNullable(<span class="built_in">this</span>.vfs).ifPresent(targetConfiguration::setVfsImpl);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里如果有配置typeAliasesPackage这个参数的话,就会对该包下进行扫描,进行一系列的过滤,</span></span><br><span class="line"><span class="comment">// 如果都满足条件的话,targetConfiguration.getTypeAliasRegistry()::registerAlias就会注册到这里. </span></span><br><span class="line">  <span class="keyword">if</span> (hasLength(<span class="built_in">this</span>.typeAliasesPackage)) &#123;</span><br><span class="line">    scanClasses(<span class="built_in">this</span>.typeAliasesPackage, <span class="built_in">this</span>.typeAliasesSuperType).stream()</span><br><span class="line">        .filter(clazz -&gt; !clazz.isAnonymousClass()).filter(clazz -&gt; !clazz.isInterface())</span><br><span class="line">        .filter(clazz -&gt; !clazz.isMemberClass()).forEach(targetConfiguration.getTypeAliasRegistry()::registerAlias);</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// 是否有typeAliases这个参数,如果有的话,也是可以看到是注册到上面哪一步的里面来.</span></span><br><span class="line">  <span class="keyword">if</span> (!isEmpty(<span class="built_in">this</span>.typeAliases)) &#123;</span><br><span class="line">    Stream.of(<span class="built_in">this</span>.typeAliases).forEach(typeAlias -&gt; &#123;</span><br><span class="line">      targetConfiguration.getTypeAliasRegistry().registerAlias(typeAlias);</span><br><span class="line">      LOGGER.debug(() -&gt; <span class="string">&quot;Registered type alias: &#x27;&quot;</span> + typeAlias + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//判断是否有插件,如果有插件的话,也会添加到configuration中来.    </span></span><br><span class="line">  <span class="keyword">if</span> (!isEmpty(<span class="built_in">this</span>.plugins)) &#123;</span><br><span class="line">    Stream.of(<span class="built_in">this</span>.plugins).forEach(plugin -&gt; &#123;</span><br><span class="line">      targetConfiguration.addInterceptor(plugin);</span><br><span class="line">      LOGGER.debug(() -&gt; <span class="string">&quot;Registered plugin: &#x27;&quot;</span> + plugin + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (hasLength(<span class="built_in">this</span>.typeHandlersPackage)) &#123;</span><br><span class="line">    scanClasses(<span class="built_in">this</span>.typeHandlersPackage, TypeHandler.class).stream().filter(clazz -&gt; !clazz.isAnonymousClass())</span><br><span class="line">        .filter(clazz -&gt; !clazz.isInterface()).filter(clazz -&gt; !Modifier.isAbstract(clazz.getModifiers()))</span><br><span class="line">        .forEach(targetConfiguration.getTypeHandlerRegistry()::register);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!isEmpty(<span class="built_in">this</span>.typeHandlers)) &#123;</span><br><span class="line">    Stream.of(<span class="built_in">this</span>.typeHandlers).forEach(typeHandler -&gt; &#123;</span><br><span class="line">      targetConfiguration.getTypeHandlerRegistry().register(typeHandler);</span><br><span class="line">      LOGGER.debug(() -&gt; <span class="string">&quot;Registered type handler: &#x27;&quot;</span> + typeHandler + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  targetConfiguration.setDefaultEnumTypeHandler(defaultEnumTypeHandler);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!isEmpty(<span class="built_in">this</span>.scriptingLanguageDrivers)) &#123;</span><br><span class="line">    Stream.of(<span class="built_in">this</span>.scriptingLanguageDrivers).forEach(languageDriver -&gt; &#123;</span><br><span class="line">      targetConfiguration.getLanguageRegistry().register(languageDriver);</span><br><span class="line">      LOGGER.debug(() -&gt; <span class="string">&quot;Registered scripting language driver: &#x27;&quot;</span> + languageDriver + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">  Optional.ofNullable(<span class="built_in">this</span>.defaultScriptingLanguageDriver)</span><br><span class="line">      .ifPresent(targetConfiguration::setDefaultScriptingLanguage);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.databaseIdProvider != <span class="literal">null</span>) &#123;<span class="comment">// fix #64 set databaseId before parse mapper xmls</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      targetConfiguration.setDatabaseId(<span class="built_in">this</span>.databaseIdProvider.getDatabaseId(<span class="built_in">this</span>.dataSource));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NestedIOException</span>(<span class="string">&quot;Failed getting a databaseId&quot;</span>, e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  Optional.ofNullable(<span class="built_in">this</span>.cache).ifPresent(targetConfiguration::addCache);</span><br><span class="line"><span class="comment">// 这这之前,都是对一些配置信息的读取,如果有的话,就会进行相应的赋值之类的操作.</span></span><br><span class="line">    </span><br><span class="line">  <span class="keyword">if</span> (xmlConfigBuilder != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 最后这里的 parse 解析方法,是和单个 Mybatis的解读是一样的.        </span></span><br><span class="line">      xmlConfigBuilder.parse();</span><br><span class="line">      LOGGER.debug(() -&gt; <span class="string">&quot;Parsed configuration file: &#x27;&quot;</span> + <span class="built_in">this</span>.configLocation + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NestedIOException</span>(<span class="string">&quot;Failed to parse config resource: &quot;</span> + <span class="built_in">this</span>.configLocation, ex);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      ErrorContext.instance().reset();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//这里可以看事务工厂,是使用了mybatis-spring包下的.</span></span><br><span class="line">  targetConfiguration.setEnvironment(<span class="keyword">new</span> <span class="title class_">Environment</span>(<span class="built_in">this</span>.environment,</span><br><span class="line">      <span class="built_in">this</span>.transactionFactory == <span class="literal">null</span> ? <span class="keyword">new</span> <span class="title class_">SpringManagedTransactionFactory</span>() : <span class="built_in">this</span>.transactionFactory,</span><br><span class="line">      <span class="built_in">this</span>.dataSource));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里是处理 mapper.xml 文件的配置,如果在这里是有配置的话,那么也是会被解析到的.    </span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.mapperLocations != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.mapperLocations.length == <span class="number">0</span>) &#123;</span><br><span class="line">      LOGGER.warn(() -&gt; <span class="string">&quot;Property &#x27;mapperLocations&#x27; was specified but matching resources are not found.&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">for</span> (Resource mapperLocation : <span class="built_in">this</span>.mapperLocations) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mapperLocation == <span class="literal">null</span>) &#123;</span><br><span class="line">          <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          <span class="type">XMLMapperBuilder</span> <span class="variable">xmlMapperBuilder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XMLMapperBuilder</span>(mapperLocation.getInputStream(),</span><br><span class="line">              targetConfiguration, mapperLocation.toString(), targetConfiguration.getSqlFragments());</span><br><span class="line">          xmlMapperBuilder.parse();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NestedIOException</span>(<span class="string">&quot;Failed to parse mapping resource: &#x27;&quot;</span> + mapperLocation + <span class="string">&quot;&#x27;&quot;</span>, e);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">          ErrorContext.instance().reset();</span><br><span class="line">        &#125;</span><br><span class="line">        LOGGER.debug(() -&gt; <span class="string">&quot;Parsed mapper file: &#x27;&quot;</span> + mapperLocation + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    LOGGER.debug(() -&gt; <span class="string">&quot;Property &#x27;mapperLocations&#x27; was not specified.&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">//org.apache.ibatis.session.defaults.DefaultSqlSessionFactory,最后到这里也是new了一个mybatis包下的默认SqlSessionFactory类.</span></span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">this</span>.sqlSessionFactoryBuilder.build(targetConfiguration);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到该方法给人感觉, 先是判断一些配置信息是不是有值，如果是有值的话，就会进行相应的处理。最后调用我们在看单个 mybatis 的 parse 解析方法,最后new了一个默认的sqlSessionFactory工厂类出来.</p>
<h2 id="MapperScannerConfigurer-org-mybatis-spring-mapper-MapperScannerConfigurer"><a href="#MapperScannerConfigurer-org-mybatis-spring-mapper-MapperScannerConfigurer" class="headerlink" title="MapperScannerConfigurer(org.mybatis.spring.mapper.MapperScannerConfigurer)"></a><strong>MapperScannerConfigurer(org.mybatis.spring.mapper.MapperScannerConfigurer)</strong></h2><p>接着看,spring-beans.xml 里面的第二个配置.</p>
<p>可以看到该类，也是实现了 spring 的很多接口.</p>
<p>BeanDefinitionRegistryPostProcessor : 注册BeanDefinition到Spring容器中来.</p>
<p>ApplicationContextAware : 获取 ApplicationContext</p>
<p>BeanNameAware : 设置 beanName名字.</p>
<p>这里也可以按照上面的笨方法，一次对重写的方法打上断点. 然后开启我们的debug来看看方法的执行顺序.</p>
<p>其执行顺序 : setBeanName —&gt; setApplicationContext —&gt; afterPropertiesSet —&gt; postProcessBeanDefinitionRegistry , 跟着这四个方法执行的顺序来看.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MapperScannerConfigurer</span></span><br><span class="line">    <span class="keyword">implements</span> <span class="title class_">BeanDefinitionRegistryPostProcessor</span>, InitializingBean, ApplicationContextAware, BeanNameAware &#123; &#125;</span><br></pre></td></tr></table></figure>

<p>赋值给 beanName 值. org.mybatis.spring.mapper.MapperScannerConfigurer#0</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setBeanName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">  <span class="built_in">this</span>.beanName = name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 然后这里是给到 ApplicationContext. 这也就说这个类现在有了 ApplicationContext,可以根据context提供的api来进行相应的操作.</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setApplicationContext</span><span class="params">(ApplicationContext applicationContext)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.applicationContext = applicationContext;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 检验配置包的值不能为null.</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    notNull(<span class="built_in">this</span>.basePackage, <span class="string">&quot;Property &#x27;basePackage&#x27; is required&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * &#123;<span class="doctag">@inheritDoc</span>&#125;</span></span><br><span class="line"><span class="comment">   * 可以感受到这个方法, 在拿到了BeanDefinitionRegistry的情况下,往里面注册bd.</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@since</span> 1.0.2</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanDefinitionRegistry</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.processPropertyPlaceHolders) &#123;</span><br><span class="line">      processPropertyPlaceHolders();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这段是创建了一个 ClassPathMapperScanner 对象,然后往里面set属性.      </span></span><br><span class="line">    <span class="type">ClassPathMapperScanner</span> <span class="variable">scanner</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ClassPathMapperScanner</span>(registry);</span><br><span class="line">    scanner.setAddToConfig(<span class="built_in">this</span>.addToConfig);</span><br><span class="line">    scanner.setAnnotationClass(<span class="built_in">this</span>.annotationClass);</span><br><span class="line">    scanner.setMarkerInterface(<span class="built_in">this</span>.markerInterface);</span><br><span class="line">    scanner.setSqlSessionFactory(<span class="built_in">this</span>.sqlSessionFactory);</span><br><span class="line">    scanner.setSqlSessionTemplate(<span class="built_in">this</span>.sqlSessionTemplate);</span><br><span class="line">    scanner.setSqlSessionFactoryBeanName(<span class="built_in">this</span>.sqlSessionFactoryBeanName);</span><br><span class="line">    scanner.setSqlSessionTemplateBeanName(<span class="built_in">this</span>.sqlSessionTemplateBeanName);</span><br><span class="line">    scanner.setResourceLoader(<span class="built_in">this</span>.applicationContext);</span><br><span class="line">    scanner.setBeanNameGenerator(<span class="built_in">this</span>.nameGenerator);</span><br><span class="line">    scanner.setMapperFactoryBeanClass(<span class="built_in">this</span>.mapperFactoryBeanClass);</span><br><span class="line">      </span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(lazyInitialization)) &#123;</span><br><span class="line">      scanner.setLazyInitialization(Boolean.valueOf(lazyInitialization));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(defaultScope)) &#123;</span><br><span class="line">      scanner.setDefaultScope(defaultScope);</span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line"><span class="comment">// 对register里的信息进行过滤      </span></span><br><span class="line">    scanner.registerFilters();</span><br><span class="line"><span class="comment">// org.springframework.context.annotation.ClassPathBeanDefinitionScanner#scan</span></span><br><span class="line"><span class="comment">// 这里主要看扫描的方法. 根据,来切割我们写的 basePackage 信息.扫描类的信息,最后还是借用了 org.springframework.context.annotation.ClassPathBeanDefinitionScanner#doScan 来进行扫描的. //  doScan(basePackages) 是对 xml 进行扫描的.</span></span><br><span class="line"><span class="comment">// AnnotationConfigUtils.registerAnnotationConfigProcessors(this.registry); 是对注解进行扫描的.</span></span><br><span class="line"><span class="comment">// 最后返回注册到 Spring 容器中的 bean 个数</span></span><br><span class="line"><span class="comment">// 所以如果我们配置了下面的标签,那么在这里都会被扫描到并且注册到Spring容器中.</span></span><br><span class="line"><span class="comment">//     &lt;bean class=&quot;org.mybatis.spring.mapper.MapperScannerConfigurer&quot;&gt;</span></span><br><span class="line"><span class="comment">//        &lt;property name=&quot;basePackage&quot; value=&quot;com.iyang.sm.mapper&quot; &gt;&lt;/property&gt;</span></span><br><span class="line"><span class="comment">//    &lt;/bean&gt;</span></span><br><span class="line"><span class="comment">// 这里需要注意的是:  org.mybatis.spring.mapper.ClassPathMapperScanner#processBeanDefinitions</span></span><br><span class="line"><span class="comment">//   definition.setBeanClass(this.mapperFactoryBeanClass);  这里的这行代码,是给bd的beanClass给换成了 MapperFactoryBean.class , </span></span><br><span class="line"><span class="comment">//  definition.getConstructorArgumentValues().addGenericArgumentValue(beanClassName);     // 这句代码,将 beanClassName 给到 db之后, 然后就才用 beanClassName来new一个 MapperFactoryBean 对象来, 所以这里并不是使用无参构造函数.</span></span><br><span class="line"><span class="comment">// 也许会问,怎么证实没有走无参数构造函数呢 ? 而是去走的 set 方法呢 ? </span></span><br><span class="line"><span class="comment">// 再不能动源码的情况下, 面对这种情况情况最好的办法就是, 在无参构造函数上打上断点.</span></span><br><span class="line"><span class="comment">// 如果没走到断点上,那就说明不是走的无参构造函数来初始化的.      </span></span><br><span class="line">    scanner.scan(</span><br><span class="line">        StringUtils.tokenizeToStringArray(<span class="built_in">this</span>.basePackage, ConfigurableApplicationContext.CONFIG_LOCATION_DELIMITERS));</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>所以到这里, 可以看到 MyBatis 与 Spring 整合的过程就已经完成了.</p>
<p>我们这里是主要对 SqlSessionFactoryBean 和 MapperScannerConfigurer 来进行分析的, 可以很明显的感觉到,我们是配置好这二个bean后,就可以使用了. 着重看第二个, org.mybatis.spring.mapper.MapperScannerConfigurer 这个bean,就是做了如何将 MyBatis 的 mapper接口文件给加载到 Spring 中来的. <strong>那么这里我在想, 如果有天我自己开发出一个好用的框架来,要与 Spring 进行整合的话,是不是也这样整合就可以了？</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;!-- 配置sqlSessionFactory，SqlSessionFactoryBean是用来产生sqlSessionFactory的 --&gt;</span><br><span class="line">&lt;bean id=<span class="string">&quot;sqlSessionFactory&quot;</span> class=<span class="string">&quot;org.mybatis.spring.SqlSessionFactoryBean&quot;</span>&gt;</span><br><span class="line">    &lt;!-- 加载mybatis的全局配置文件，放在classpath下的mybatis文件夹中了 --&gt;</span><br><span class="line">    &lt;property name=<span class="string">&quot;configLocation&quot;</span> value=<span class="string">&quot;mybatis/SqlMapConfig.xml&quot;</span> /&gt;</span><br><span class="line">    &lt;!-- 加载数据源，使用上面配置好的数据源 --&gt;</span><br><span class="line">    &lt;property name=<span class="string">&quot;dataSource&quot;</span> ref=<span class="string">&quot;dataSource&quot;</span> /&gt;</span><br><span class="line">&lt;/bean&gt;</span><br><span class="line"></span><br><span class="line">&lt;!--  配置扫描 MyBatis 接口的包 --&gt;</span><br><span class="line">&lt;bean class=<span class="string">&quot;org.mybatis.spring.mapper.MapperScannerConfigurer&quot;</span>&gt;</span><br><span class="line">    &lt;property name=<span class="string">&quot;basePackage&quot;</span> value=<span class="string">&quot;com.iyang.sm.mapper&quot;</span> &gt;&lt;/property&gt;</span><br><span class="line">&lt;/bean&gt;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>可以看到 MyBatis 与 Spring 整合后, 对于解析 MyBatis 的 mapper 配置文件等，都是走的之前单个 mybatis 的逻辑, 是没有什么变化的. 主要的是将 , SqlSessionFactory 和 Mapper.class(接口类) 给注入到 Spring 容器中.然后接口的话, 是怎么使用的代理类来进行实例化完后, 将对象给注入到 Spring 容器中的呢 ？ 这里看 org.mybatis.spring.mapper.MapperScannerConfigurer 做的事情就明白了.</p>
<p>不过在看 mybatis 与 Spring 整合的时候, 还是建议要有对 BeanDefinitionRegistryPostProcessor &#x2F; InitializingBean &#x2F; ApplicationContextAware &#x2F; BeanNameAware 有一定的了接. 就是有了了解后, 你就会很明显的感受到， mybatis 为什么是实现这个接口，实现这个接口并且重写这个方法，在后面是什么时候被调用的. 意思也就是，你至少得明白点 Spring 对外提供的一些扩展点，才能很好的理解这些东西.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>ORM框架</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark基础知识</title>
    <url>//article/spark_base_info/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Hadoop: 海量数据的存储和海量数据的分析计算<br>Spark是一种基于内存的快速,通用,可扩展的大数据分析引擎</p>
</blockquote>
<h1 id="📝-Spark基础概念"><a href="#📝-Spark基础概念" class="headerlink" title="📝 Spark基础概念"></a>📝 Spark基础概念</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>Spark是一种基于内存的快速和通用,可扩展的大数据分析计算引擎</p>
<details><summary>与MR进行对比</summary>

</details>

<p>hadoop MR 框架溢出写磁盘次数多,不合适迭代算,只适合一次计算;Spark框架计算块的原因是中间结果不罗盘,spark的shuffle也是要罗盘的</p>
<h2 id="基础模块"><a href="#基础模块" class="headerlink" title="基础模块"></a>基础模块</h2><ul>
<li>Spark Core : 实现了Spark的基本功能,包含了任务调度,内存管理,错误恢复,与存储系统交互等模块.Spark Core中还包含了对弹性分布式数据集(Resilient Distrubuted DataSet,简称RDD)的API定义</li>
<li>Spark SQL : Spark用来操作结构化数据的程序包,通过Spark SQL,我们可以使用SQL或者Hive版本的HQL来查询数据;SparkSQL支持多种数据源,比如Hive表,Parquet及Join等</li>
<li>Spakr Streaming : Spark提供的对实时的数据进行流式计算组件,提供了用来操作数据流的API,并且与Spark Core中的RDD API高度对应</li>
<li>Spark MLlib : 提供常见的机器学习功能的程序库。包括分类、回归、聚类、协同过滤等，还提供了模型评估、数据 导入等额外的支持功能</li>
<li>Spark GraphX : 主要用于图形并行计算和图挖掘系统的组件</li>
</ul>
<h2 id="运行模式"><a href="#运行模式" class="headerlink" title="运行模式"></a>运行模式</h2><p>   Local : 本地</p>
<details><summary>Standalone模式</summary>

</details>

<details><summary>Mesos模式</summary>

</details>

<details><summary>Yarn模式</summary>

</details>

<details><summary>Spark on yarn client模式</summary>

</details>

<details><summary>Spark on yarn cluster模式</summary>

</details>

<p>SparkSubmit常用参数</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>参数</td>
<td>作用</td>
<td>说明</td>
</tr>
<tr>
<td>master</td>
<td>指定任务提交到哪个资源调度器中</td>
<td>local模式: local&#x2F;local[*]&#x2F;local[N]<br><br>stanadalone： spark:&#x2F;&#x2F;master主机名:7077,..<br><br>yarn模式: yarn</td>
</tr>
<tr>
<td>class</td>
<td>指定待运行的带有main方法object全类名</td>
<td></td>
</tr>
<tr>
<td>deploy-mode</td>
<td>指定部署模式[client&#x2F;cluster]</td>
<td></td>
</tr>
<tr>
<td>driver-memory</td>
<td>指定Driver的内存大小</td>
<td></td>
</tr>
<tr>
<td>executor-memory</td>
<td>指定每个executor的内存大小</td>
<td></td>
</tr>
<tr>
<td>executor-cores</td>
<td>指定每个executor的cpu个数</td>
<td></td>
</tr>
<tr>
<td>total-executor-cores</td>
<td>指定所有executor的cpu总个数[仅用于standalone模式]</td>
<td></td>
</tr>
<tr>
<td>num-executors</td>
<td>指定需要的executor总个数[仅用于yarn模式]</td>
<td></td>
</tr>
<tr>
<td>queue</td>
<td>指定任务提交到哪个资源队列中[仅用于yarn模式]</td>
<td></td>
</tr>
</tbody></table>
<h1 id="📝-Spark-RDD"><a href="#📝-Spark-RDD" class="headerlink" title="📝 Spark RDD"></a>📝 Spark RDD</h1><h2 id="基本概念-1"><a href="#基本概念-1" class="headerlink" title="基本概念"></a>基本概念</h2><p>RDD是弹性分布式数据集,RDD代表弹性,可分区,不可变,元素可并行计算的计算</p>
<details><summary>弹性</summary>

</details>

<details><summary>不可变</summary>

</details>

<details><summary>可分区</summary>

</details>

<details><summary>可并行计算</summary>

</details>

<details><summary>不存储数据</summary>

</details>

<ul>
<li>一组分区列表: RDD是分布式的,会根据文件切片划分分区,由多少切片就有多少分区</li>
<li>作用在每个分区上的计算函数:  RDD每个分区处理不同的切片数据,每个分区计算逻辑是一样</li>
<li>对其他RDD的依赖关系: RDD会记录父RDD,后续如果RDD分区数据丢失可以根据依赖关系重新计算得到数据</li>
<li>分区器[可选]: RDD的分区分布在不同的机器上,后续如果想要将key相同的数据聚在一起,此时必须使用分区器规划key的数据在shuffle之后续落在RDD哪个分区上</li>
<li>优先位置: spark在分配task的时候会考虑将计算逻辑分配在数据所在的位置,避免计算的时候通过网络拉取数据影响效率</li>
</ul>
<h2 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h2><p>通过本地集合创建RDD分区数:</p>
<p>如果有设置numSlices参数,此时RDD分区数 &#x3D; 设置numSlices参数</p>
<p>如果没有设置numSlices参数,此时RDD分区数 &#x3D; defaultParallelism:</p>
<ol>
<li>如果在sparkConf中有设置spark.default.parallelism参数的值,此时defaultParallelism&#x3D;spark.default.parallelism参数值</li>
<li>如果没有在sparkconf中设置spark.default.parallelism参数的值</li>
</ol>
<ul>
<li>master&#x3D;local,此时defaultParallelism&#x3D;1</li>
<li>master&#x3D;local[N],此时defaultParallelism&#x3D;N</li>
<li>master&#x3D;local[*],此时defaultParallelism&#x3D;本地cpu个数</li>
<li>master&#x3D;spark:&#x2F;&#x2F;…,此时defaultParallelism &#x3D; max( 所有executor总核数,2 )</li>
</ul>
<p>通过读取文件创建RDD分区数</p>
<ol>
<li>如果有指定minPartition参数值,此时RDD分区数 ≥ 指定minPartition参数值</li>
<li>如果没有指定minPartition参数值,此时RDD分区数 ≥ min(defaultParallelism,2)</li>
</ol>
<p>通过其它RDD衍生出的新RDD的分区数 &#x3D; 依赖第一个父RDD的分区数</p>
<h2 id="算子"><a href="#算子" class="headerlink" title="算子"></a>算子</h2><p>Spark的算子分为两类: Transformation转换算子(生成的是新RDD,不会触发任务计算);Action行动算子(没有返回值或者返回scala数据类型,会触发任务的计算)</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>算子名字</td>
<td>作用</td>
<td>备注</td>
</tr>
<tr>
<td>map</td>
<td>map（func：RDD元素类型你&#x3D;&gt;B（任意类型）</td>
<td>map里面的函数是针对RDD每个元素操作，元素有多少个，函数就执行多少次<br>map生成新的RDD元素个数和原RDD的元素个数相同</td>
</tr>
<tr>
<td>flatMap</td>
<td>flatMap（func:RDD元素类型&#x3D;&gt;B(集合)）：转换 + 压平</td>
<td>flatMap里面的函数是针对每个元素操作的<br>flatMap生成新RDD元素的个数一般是大于等于原RDD元素个数（除非是空集合，可能会是小于）<br>flatMap的应用场景：一对多</td>
</tr>
<tr>
<td>filter</td>
<td>filter（func：RDD元素类型 &#x3D; Boolean）：根据指定条件过滤</td>
<td>filter里面的函数是针对每个元素操作的<br>filter保留的是函数返回值为true的元素<br>filter的应用场景：过滤数据</td>
</tr>
<tr>
<td>mapPartitons()</td>
<td>mapPartitons(func:Iterator[Rdd元素类型]&#x3D;&gt;Iterator[B])：一对一转换，原RDD的一个分区计算得到一个新RDD分区</td>
<td>函数是针对每个分区操作的，多少分区操作多少次<br>一般用于MySQL&#x2F;Hbase&#x2F;redis查询数据，可以减少链接创建与销毁的次数</td>
</tr>
<tr>
<td>mapPartitons()</td>
<td>与map的区别<br>1、函数针对的对象不一样，一个是单个元素，一个是整个分区<br>2、函数的返回值类型不一样<br>3、元素内存回收的实际不一样</td>
<td>map的函数返回的是新RDD的元素，新Rdd的元素个数&#x3D;原RDD的个数<br>mapPartitions里面的函数是针对没分翻去，返回的是新RDD分区所有数据，新RDD元素个数不一定 &#x3D; 原RDD元素个数，但是分区数相等<br>map每个元素操作完成之后就可以进行垃圾回收<br>mapPartitions必须等到分区迭代器遍历完成之后才会垃圾回收，如果RDD分区数据量过大，则可能出现内存溢出的现象（OOM），此时应用map代替之</td>
</tr>
<tr>
<td>groupBy</td>
<td>groupBy(func: RDD元素类型&#x3D;&gt;K ): 按照指定字段进行分组</td>
<td>groupBy里面函数是针对每个元素操作,元素有多少个,函数就执行多少次<br>groupBy是根据函数的返回值对元素进行分组<br>groupBy返回RDD是KV键值对,K是函数的返回值,V是原RDD中K对应的所有元素的集合<br>groupBy会产生shuffle操作</td>
</tr>
<tr>
<td>distinct</td>
<td>去重</td>
<td>distinct会产生shuffle操作</td>
</tr>
<tr>
<td>coalesce</td>
<td>coalesce(分区数): 合并分区</td>
<td>coalesce默认只能减少分区数,此时没有shuffle操作<br>coalesce如果想要增大分区数,需要设置shuffle&#x3D;true，此时会产生shuffle操作<br>coalesce一般用于减少分区数,一般是搭配filter使用</td>
</tr>
<tr>
<td>repartition</td>
<td>重分区</td>
<td>repartition既可以增大分区数也可以减少分区数,但是都有shuffle操作<br>repartition的使用场景: 增大分区数的时候</td>
</tr>
<tr>
<td>coalesce与repartition的区别</td>
<td></td>
<td>coalesce默认只能减少分区数,此时没有shuffle操作,coalesce如果想要增大分区数,需要设置shuffle&#x3D;true，此时会产生shuffle操作<br>repartition既可以增大分区数也可以减少分区数,但是都有shuffle操作</td>
</tr>
<tr>
<td>sortBy</td>
<td>sortBy(func: RDD元素类型&#x3D;&gt;K，ascending&#x3D;true&#x2F;false): 根据指定字段排序</td>
<td>sortBy里面的函数是针对每个元素,元素有多少个,函数执行多少次<br>sortBy是根据函数的返回值对RDD元素进行排序(true是升序,false是降序)<br>sortBy会产生shuffle</td>
</tr>
<tr>
<td>intersecetion交集</td>
<td>intersecetion会产生shuffle操作</td>
<td></td>
</tr>
<tr>
<td>subtract()</td>
<td>交集</td>
<td></td>
</tr>
<tr>
<td>union</td>
<td>并集</td>
<td></td>
</tr>
<tr>
<td>zip</td>
<td>拉链</td>
<td>两个RDD要想拉链要求数量条数与分区数都必须一致。</td>
</tr>
<tr>
<td>partitionBy</td>
<td>partitionBy(partitioner): 根据指定的分区器重分区</td>
<td></td>
</tr>
<tr>
<td>groupByKey</td>
<td>groupByKey： 根据key分组</td>
<td>groupByKey生成的新RDD的元素类型是KV键值对<br>K是分组的key<br>V是key在原RDD中对应的所有的value值</td>
</tr>
<tr>
<td>reduceByKey</td>
<td>reduceByKey(func: (value值类型,value值类型)&#x3D;&gt;Value值类型): 按照key分组,对value值聚合</td>
<td>reduceByKey里面的函数的第一个参数代表该组上一次的聚合结果,第一个聚合的时候初始值 &#x3D; 该组第一个value值<br>reduceByKey里面的函数的第二个参数代表该组当前待聚合的value值</td>
</tr>
<tr>
<td>reduceByKey与groupByKey的区别</td>
<td>reduceByKey与groupByKey的区别</td>
<td>reduceByKey有预聚合操作,性能更高,工作中推荐使用这种高性能shuffle算子<br>groupByKey没有预聚合操作</td>
</tr>
<tr>
<td>mapPartitionsWithIndex</td>
<td>mapPartitionsWithIndex</td>
<td>mapPartitionsWithIndex((index,it)</td>
</tr>
<tr>
<td>自定义分区器</td>
<td>自定义class继承Partitioner</td>
<td>override def numPartitions: Int &#x3D; num      &#x2F;&#x2F; 获取新RDD分区<br>override def getPartition(key: Any): Int   &#x2F;&#x2F; 获取key获取分区号</td>
</tr>
</tbody></table>
<h1 id="📝-Spark优化"><a href="#📝-Spark优化" class="headerlink" title="📝 Spark优化"></a>📝 Spark优化</h1><p>根据木桶效应,最短的木板决定了木桶的容量,因此,对于一只有短板的木桶,其它木板调节得再高也无济于事,最短的木板才是木桶容量的瓶颈</p>
<details><summary>性能优化本质</summary>

</details>

<p>Spark UI提供了丰富的面板,来展示DAG,Stages划分,执行计划,Executor负载均衡情况,GC时间,内存缓存消耗等等详尽的运行时状态数据;对于硬件消耗资源,可利用监控应用进行查看</p>
<p><strong>性能调优目的: 所有参数计算的硬件资源之间寻求协调与平衡,让硬件资源达到一种平衡,无瓶颈的状态</strong></p>
<h1 id="📝-Spark参数"><a href="#📝-Spark参数" class="headerlink" title="📝 Spark参数"></a>📝 Spark参数</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>配置项</td>
<td>含义</td>
<td>使用说明</td>
</tr>
<tr>
<td>spark.cores.max</td>
<td>集群范围内满配CPU核数</td>
<td></td>
</tr>
<tr>
<td>spark.executor.cores</td>
<td>单个Executor内CPU参数</td>
<td></td>
</tr>
<tr>
<td>spark.task.cpus</td>
<td>单个任务消耗的CPU核数</td>
<td></td>
</tr>
<tr>
<td>spark.default.parallelism</td>
<td>未指定分区数时的默认并行度</td>
<td></td>
</tr>
<tr>
<td>spark.sql.shuffle.partitions</td>
<td>数据关联,聚合操作中Reducer的并行度</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.executor.memory</td>
<td>单个Executor内堆内内存总大小</td>
<td></td>
</tr>
<tr>
<td>spark.memory.offHeap.size</td>
<td>单个Executor内堆外内存总大小(spark.memory.offHeap.enabled为true才生效)</td>
<td></td>
</tr>
<tr>
<td>spark.menory.fraction</td>
<td>堆内内存中,用于缓存RDD和执行计算的内存比例</td>
<td></td>
</tr>
<tr>
<td>spark.menory.storageFraction</td>
<td>用于缓存RDD的内存占比,执行内存占比为1-spark.memory.stroageFraction</td>
<td></td>
</tr>
<tr>
<td>spark.rdd.compress</td>
<td>RDD缓存是否压缩,默认不压缩</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.local.dir</td>
<td>用于缓存RDD和Shuffle中间文件的磁盘目录</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.shuffle.file.buffer</td>
<td>Map输出端的写入缓冲区大小</td>
<td>Map端</td>
</tr>
<tr>
<td>spark.reducer.maxSizeInFlight</td>
<td>Reduce输入端的读取缓冲区大小</td>
<td>Reduce端</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.shuffle.sort.bypassMergeThreshold</td>
<td>Map阶段不进行排序的分区阈值</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.enabled</td>
<td>是否启用AQE(Adaptive query execution);默认是禁用的</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.coalescePartition.enabled</td>
<td>是否启用AQE中的自动分区合并,默认启用</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.advisoryParitionSizeInBytes</td>
<td>由开发者指定分区合并后的推荐尺寸</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.coalescePartitions.minPartitionNum</td>
<td>分区合并后,数据集的分区数不低于该值</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.shewJoin.enabled</td>
<td>是否开启AQE中自动处理数据倾斜的功能,默认开启</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.shewJoin.shewdPartitionFactor</td>
<td>判断数据分区是否倾斜的比例系数</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.shewJoin.shewdPartitionThresholdInBytes</td>
<td>判断数据分区是否倾斜的最低阈值</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.advisoryPartitionSizeInBytes</td>
<td>以字节为单位,拆分倾斜分区的数据粒度</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>spark.sql.autoBroadcastJoinThreadld</td>
<td>数据表采用broadcast join实现方式的最低阈值</td>
<td></td>
</tr>
<tr>
<td>spark.sql.adaptive.nonEmptyPartitionRatioForBoradcasejoin</td>
<td>非空分区比例小于该值,才会调整Join策略</td>
<td></td>
</tr>
</tbody></table>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
        <tag>使用入门</tag>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring扩展</title>
    <url>//article/Spring_extend_read/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-Spring扩展"><a href="#📝-Spring扩展" class="headerlink" title="📝 Spring扩展"></a>📝 Spring扩展</h1><p>前面讲到了这么多的什么BeanPostProcessor,事件什么的. 如果不写几下代码这里怕是很难弄清楚是个怎么回事. 所以只有看到代码跑,就大致可以看到其效果还是很明显的.</p>
<h1 id="BeanFactoryPostProcessor"><a href="#BeanFactoryPostProcessor" class="headerlink" title="BeanFactoryPostProcessor"></a><strong>BeanFactoryPostProcessor</strong></h1><p>我们这里实现一下 BeanFactoryPostProcessor 来看一下效果</p>
<p>我们就写一个简单的类, 然后实现下接口 BeanFactoryPostProcessor, 我们这里就获取下 beanFactory中的所有beanDefinitionNames的数组,然后给打印出来看效果. 可以看下下面打印的接口. 然后我们看下源码怎么执行的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangBeanFactoryPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanFactoryPostProcessor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        String[] beanDefinitionNames = beanFactory.getBeanDefinitionNames();</span><br><span class="line">        System.out.println(Arrays.toString(beanDefinitionNames));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//[org.springframework.context.annotation.internalConfigurationAnnotationProcessor, org.springframework.context.annotation.internalAutowiredAnnotationProcessor, org.springframework.context.annotation.internalCommonAnnotationProcessor, org.springframework.context.event.internalEventListenerProcessor, org.springframework.context.event.internalEventListenerFactory, yangBeanOne, gavinYangBeanFactoryPostProcessor, yangBeanPostProcessor, gavinYangLifeImpl]</span></span><br></pre></td></tr></table></figure>

<p>直接看这个类的这个方法，我这里只截取了一部分代码,也就是和 BeanFactoryPostProcessor 有关的代码. 和它没关系的代码,这里就没有去截取了</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List&lt;org.springframework.beans.factory.config.BeanFactoryPostProcessor&gt;)</p>
<p>看源码看是如何调用到这个方法的,还是比较好理解。先是根据 BeanFactoryPostProcessor.class获取出beanName的集合,然后老规矩进行一些特定的排序,当然我这里什么都没做,也就是最后处理哦</p>
<p>然后跟到invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);这个方法里面,可以看到它迭代这个集合的元素,当然了,我们只需要关注我们自己定义的哪个就可以了,然后就会走其postProcessBeanFactory方法,也就是走到了我们定义的类的这个方法上来了</p>
<p>OK啦。大致流程就是这个样子的,还是比较好理解的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line"><span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line"><span class="comment">// 这个地方获取出来的数组里面的值,就有我们想看到的.</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.event.internalEventListenerProcessor</span></span><br><span class="line"><span class="comment">//gavinYangBeanFactoryPostProcessor</span></span><br><span class="line"><span class="comment">//当看到第三个是不是非常的熟悉.没错,这就是我们自己定义的.</span></span><br><span class="line">String[] postProcessorNames =</span><br><span class="line">      beanFactory.getBeanNamesForType(BeanFactoryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Separate between BeanFactoryPostProcessors that implement PriorityOrdered,</span></span><br><span class="line"><span class="comment">// Ordered, and the rest.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// 然后先经过一轮排序,就可以看到 我们自己定义的就放入到了nonOrderedPostProcessorNames这个集合中,</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">   <span class="keyword">if</span> (processedBeans.contains(ppName)) &#123;</span><br><span class="line">      <span class="comment">// skip - already processed in first phase above</span></span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">      priorityOrderedPostProcessors.add(beanFactory.getBean(ppName, BeanFactoryPostProcessor.class));</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">      orderedPostProcessorNames.add(ppName);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// First, invoke the BeanFactoryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Next, invoke the BeanFactoryPostProcessors that implement Ordered.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(orderedPostProcessorNames.size());</span><br><span class="line"><span class="keyword">for</span> (String postProcessorName : orderedPostProcessorNames) &#123;</span><br><span class="line">   orderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">&#125;</span><br><span class="line">sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">invokeBeanFactoryPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Finally, invoke all other BeanFactoryPostProcessors.</span></span><br><span class="line">List&lt;BeanFactoryPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(nonOrderedPostProcessorNames.size());</span><br><span class="line"><span class="keyword">for</span> (String postProcessorName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">   nonOrderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 然后我们直接看到处理nonOrderedPostProcessors这个集合的方法,</span></span><br><span class="line"><span class="comment">//因为这个方法也会走到我们自己定义的类中去</span></span><br><span class="line"><span class="comment">//方法里面对nonOrderedPostProcessors进行迭代,然后一次调用其postProcessBeanFactory方法,同时也传入了beanFactory到里面去.</span></span><br><span class="line">invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);</span><br></pre></td></tr></table></figure>

<h2 id="BeanPostProcessor"><a href="#BeanPostProcessor" class="headerlink" title="BeanPostProcessor"></a><strong>BeanPostProcessor</strong></h2><p>我们先写一个 Bean. 然后可以里面写一个属性,方便标识</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangBeanOne</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 无参构造函数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">YangBeanOne</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;YangBeanOne无参数构造函数&quot;</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后我们自定义一个BeanPostProcessor,其实现了BeanPostProcessor接口</p>
<p>可以看到的是,我们在if中做了beanName的判断,如果是的话,那么我们就会强转,然后给name字段赋值上GavinYang的值.</p>
<p>我们给断点打到 if这里，然后看进来的堆栈信息,发现其是在初始化bean中,然后调用beanPostProcessor的postProcessAfterInitialization的方法才会走到这里,也就是在doCreateBean这个方法里面.</p>
<p>然后这个 YangBeanPostProcessor 是什么时候给添加到 beanFactory中去的呢？</p>
<p>org.springframework.context.support.PostProcessorRegistrationDelegate#registerBeanPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, org.springframework.context.support.AbstractApplicationContext)在这个方法打上断点,然后你会发现其get出来的postProcessorNames数组,就有我们的这个YangBeanPostProcessor，然后走registerBeanPostProcessors方法的时候,就会给添加到beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangBeanPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanPostProcessor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">&quot;yangBeanOne&quot;</span>.equalsIgnoreCase(beanName))&#123;</span><br><span class="line">            ((YangBeanOne) bean).setName(<span class="string">&quot;GavinYang&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动类 :</p>
<p>然后我们这里用一个启动类.</p>
<p>这就打印出来的结果就可以看到的是GavinYang,也就是说我们初始化这个bean之后,然后给其name属性赋值上了GavinYang这个值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpringStartMain</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>(<span class="string">&quot;com.iyang.spring&quot;</span>);</span><br><span class="line">        <span class="type">YangBeanOne</span> <span class="variable">yangBeanOne</span> <span class="operator">=</span> context.getBean(YangBeanOne.class);</span><br><span class="line">        System.out.println(yangBeanOne.getName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Lifecycle-扩展"><a href="#Lifecycle-扩展" class="headerlink" title="Lifecycle 扩展"></a><strong>Lifecycle 扩展</strong></h2><p>Lifecycle 扩展要比起 BeanPostProcessor要好理解得多,因为你只用去实现这个接口(SmartLifecycle),然后在org.springframework.context.support.AbstractApplicationContext#finishRefresh到这个方法的时候,就会去调用实现这个接口的对用的方法.</p>
<p>这里我们自己写一个类,然后实现 SmartLifecycle 接口即可。</p>
<p>可以看到下面的打印参数.还是很清楚的明白.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangLifeImpl</span> <span class="keyword">implements</span> <span class="title class_">SmartLifecycle</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getPhase</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Integer.MAX_VALUE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">start</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;调用到了GavinYangLifeImpl.start()方法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">stop</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isRunning</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//YangBeanOne无参数构造函数</span></span><br><span class="line"><span class="comment">//调用到了GavinYangLifeImpl.start()方法</span></span><br><span class="line"><span class="comment">//GavinYang</span></span><br></pre></td></tr></table></figure>

<p>然后我们再看下源码,为什么要实现 SmartLifecycle 这个接口呢？</p>
<p>org.springframework.context.support.DefaultLifecycleProcessor</p>
<p>这里我们先看到 phases ,也就是最底下的代码, 这个集合是有值的情况下先排序,然后再迭代,然后调用到 start()方法, 这个start方法是不是在我们的实现类中可以看到,是不是非常的熟悉感觉.</p>
<p>然后我们在看下,怎么样让这个集合能有值呢？</p>
<p>phases.put(phase, group); 可以看到 put方法这里, autoStartupOnly 是false 或者bean是SmartLifecycle的子类,并且其isAutoStartup方法的是true. 点到SmartLifecycle源码中去看,可以发现这个方法默认是返回的true.</p>
<p>接着在调用getPhase方法, 该方法也就是判断.最最关键的phases集合来了,先从里面get出数据,然后判断数据是不是null,如果是null的话,就先new一个Group出来,然后调用phases的put方法,也就是放入到这个集合中去了</p>
<p>所以这里就是我们为什么要实现 SmartLifecycle 这个接口,就会有启动的效果了的原因.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Internal helpers</span></span><br><span class="line"><span class="comment">// 传入进来的  autoStartupOnly 参数是true.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">startBeans</span><span class="params">(<span class="type">boolean</span> autoStartupOnly)</span> &#123;</span><br><span class="line">   Map&lt;String, Lifecycle&gt; lifecycleBeans = getLifecycleBeans();</span><br><span class="line">   Map&lt;Integer, LifecycleGroup&gt; phases = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">   lifecycleBeans.forEach((beanName, bean) -&gt; &#123;</span><br><span class="line">      <span class="keyword">if</span> (!autoStartupOnly || (bean <span class="keyword">instanceof</span> SmartLifecycle &amp;&amp; ((SmartLifecycle) bean).isAutoStartup())) &#123;</span><br><span class="line">         <span class="type">int</span> <span class="variable">phase</span> <span class="operator">=</span> getPhase(bean);</span><br><span class="line">         <span class="type">LifecycleGroup</span> <span class="variable">group</span> <span class="operator">=</span> phases.get(phase);</span><br><span class="line">         <span class="keyword">if</span> (group == <span class="literal">null</span>) &#123;</span><br><span class="line">            group = <span class="keyword">new</span> <span class="title class_">LifecycleGroup</span>(phase, <span class="built_in">this</span>.timeoutPerShutdownPhase, lifecycleBeans, autoStartupOnly);</span><br><span class="line">            phases.put(phase, group);</span><br><span class="line">         &#125;</span><br><span class="line">         group.add(beanName, bean);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;);</span><br><span class="line">   <span class="keyword">if</span> (!phases.isEmpty()) &#123;</span><br><span class="line">      List&lt;Integer&gt; keys = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(phases.keySet());</span><br><span class="line">      Collections.sort(keys);</span><br><span class="line">      <span class="keyword">for</span> (Integer key : keys) &#123;</span><br><span class="line">         phases.get(key).start();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring的getBean方法阅读</title>
    <url>//article/Spring_getBean_source/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-方法"><a href="#📝-方法" class="headerlink" title="📝  方法"></a>📝  方法</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getBean</span><span class="params">(String name, Class&lt;T&gt; requiredType)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="keyword">return</span> doGetBean(name, requiredType, <span class="literal">null</span>, <span class="literal">false</span>);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">getBean</span><span class="params">(String name, Object... args)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="keyword">return</span> doGetBean(name, <span class="literal">null</span>, args, <span class="literal">false</span>);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到最后是走到了 doGetBean 这个方法里面来的. 这么看下来,doGetBean这个方法的代码还是蛮多的,其中有createBean()方法,这个方法从名字上看,就是创建bean的. 我们先看doGetBean这个方法</p>
<p>其实该方法从整体上来看,还是比较容易理解的(这里是先抛开了createBean这个方法)</p>
<p>先走 getSingleton方法,根据beanName来获取对象.如果获取出来的对象不为null的话,就会根据是否已经有正在创建了的还是没有,分别打印出来log出来,当然了这里的log级别是trace级别的. 最后走getObjectForBeanInstance方法,该方法不论是这里还是后面,都是有走到的</p>
<p>然后getSingleton方法获取出来的是null的情况下,会根据 单例 &#x2F; 多例 &#x2F; Scope去分别走不同的逻辑,看到if –&gt; else if –&gt; else 就是分别对应的这三种方式</p>
<p>最后还会检查一遍,判断requiredType不是null并且requiredType.isInstance(bean)返回的是false,也就是不相符的情况下,然后会T convertedBean &#x3D; getTypeConverter().convertIfNecessary(bean, requiredType);去弄一个bean出来,如果弄出来的bean是null的话,就会抛出异常来,如果不是的话,就返回</p>
<p>如果最后的检查是不满足的检查的话,就会直接返回这个bean的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Return an instance, which may be shared or independent, of the specified bean.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> name the name of the bean to retrieve</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> requiredType the required type of the bean to retrieve</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> args arguments to use when creating a bean instance using explicit arguments</span></span><br><span class="line"><span class="comment">	 * (only applied when creating a new instance as opposed to retrieving an existing one)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> typeCheckOnly whether the instance is obtained for a type check,</span></span><br><span class="line"><span class="comment">	 * not for actual use</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> an instance of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> BeansException if the bean could not be created</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">	<span class="keyword">protected</span> &lt;T&gt; T <span class="title function_">doGetBean</span><span class="params">(<span class="keyword">final</span> String name, <span class="meta">@Nullable</span> <span class="keyword">final</span> Class&lt;T&gt; requiredType,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> <span class="keyword">final</span> Object[] args, <span class="type">boolean</span> typeCheckOnly)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="comment">// 先获取出bean的名字</span></span><br><span class="line">		<span class="keyword">final</span> <span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> transformedBeanName(name);</span><br><span class="line">		<span class="comment">// 定义一个bean</span></span><br><span class="line">        Object bean;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Eagerly check singleton cache for manually registered singletons.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#getSingleton(java.lang.String, boolean),从DefaultSinglonBeanRegisttry中的singletonObjects根据bean获取出来Object.        </span></span><br><span class="line">		<span class="type">Object</span> <span class="variable">sharedInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line"><span class="comment">// 获取出来的bean不是null,并且传入进来的 args是null的话,就会进入到if逻辑中.        </span></span><br><span class="line">		<span class="keyword">if</span> (sharedInstance != <span class="literal">null</span> &amp;&amp; args == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 根据 trace级别的log来进行打印</span></span><br><span class="line">			<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">				<span class="keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">					logger.trace(<span class="string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName +</span><br><span class="line">							<span class="string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					logger.trace(<span class="string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="comment">//一: 如果name不是bull并且是&amp;开头的话,满足这个条件,</span></span><br><span class="line"><span class="comment">/** 如果满足上面的条件的话</span></span><br><span class="line"><span class="comment">  1: 如果是 beanInstance(sharedInstance) 是NullBean的话,就会直接返回回去.</span></span><br><span class="line"><span class="comment">  2: 如果1没有return回去的话,判断是不是FactoryBean,如果不是的话,就会抛一个BeanIsNotAFactoryException异常出来.</span></span><br><span class="line"><span class="comment">  3:  1和2都不满足的话,就会判断mbd是不是null,很明显我们这里传入进去的是null,如果不是null的话,</span></span><br><span class="line"><span class="comment">  	  就会 mbd.isFactoryBean = true; 最后返回 beanInstance;</span></span><br><span class="line"><span class="comment">*/</span>                     </span><br><span class="line"><span class="comment">//如果不是满足 一 的话,判断不是FactoryBean的话,就会直接返回回去.如果是的哈,就会继续往下走  </span></span><br><span class="line"><span class="comment">//定义一个Object object,如果RootBeanDefinition mbd是null的话,就会getCachedObjectForFactoryBean方法,否则的话,就会mbd.isFactoryBean = true;</span></span><br><span class="line"><span class="comment">//如果object不是null的话,就直接返回.  是null的话,就会先将beanInstance强转为FactoryBean,</span></span><br><span class="line"><span class="comment">//然后看到是null的话,就会调用getObjectFromFactoryBean这个方法,从名字上看,就是从FactoryBean中获取出object.   </span></span><br><span class="line"><span class="comment">// 如果是走了这里的话,就不会走else里面的含有 createBean这个方法的            </span></span><br><span class="line">			bean = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="literal">null</span>);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// Fail if we&#x27;re already creating this bean instance:</span></span><br><span class="line">			<span class="comment">// We&#x27;re assumably within a circular reference.</span></span><br><span class="line"><span class="comment">// 判断 beanName是不是正在创建,如果是正在创建的话,那么这里就会抛出异常来.</span></span><br><span class="line"><span class="comment">//Object curVal = this.prototypesCurrentlyInCreation.get();使用的是ThreadLocal来存储正在创建的bean信息.这里获取出来了,判断.如果是一样的话,那么就说名字这个bean是正在创建的.            </span></span><br><span class="line">			<span class="keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Check if bean definition exists in this factory.</span></span><br><span class="line">  <span class="comment">// 获取 parent BeanFacotry.          </span></span><br><span class="line">			<span class="type">BeanFactory</span> <span class="variable">parentBeanFactory</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">  <span class="comment">// 获取出来的parentBeanFactory不是null的话并且beanFactoy中存放bd的集合中没有这个beanName</span></span><br><span class="line">			<span class="keyword">if</span> (parentBeanFactory != <span class="literal">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">				<span class="comment">// Not found -&gt; check parent.</span></span><br><span class="line">                <span class="comment">// 这里也是获取beanName的</span></span><br><span class="line">				<span class="type">String</span> <span class="variable">nameToLookup</span> <span class="operator">=</span> originalBeanName(name);</span><br><span class="line">                <span class="comment">// 如果parentBeanFactroy是AbstractBeanFactory的话</span></span><br><span class="line">                <span class="comment">// 强转调用doGetBean方法,直接返回的.</span></span><br><span class="line">				<span class="keyword">if</span> (parentBeanFactory <span class="keyword">instanceof</span> AbstractBeanFactory) &#123;</span><br><span class="line">					<span class="keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(</span><br><span class="line">							nameToLookup, requiredType, args, typeCheckOnly);</span><br><span class="line">				&#125; <span class="keyword">else</span> <span class="keyword">if</span> (args != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="comment">// Delegation to parent with explicit args.</span></span><br><span class="line">                    <span class="comment">// 如果parentBeanFactory不是AbstractBeanFactory并且args不是null</span></span><br><span class="line">                   <span class="comment">// 直接调用getBean方法返回. 这里传入进去的是 beanName + args</span></span><br><span class="line">					<span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);</span><br><span class="line">				&#125;  <span class="keyword">else</span> <span class="keyword">if</span> (requiredType != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="comment">// No args -&gt; delegate to standard getBean method.</span></span><br><span class="line">                    <span class="comment">// 这里调用的getBean方法,传入进去的是 beanName + Class</span></span><br><span class="line">					<span class="keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 这里是直接根据beanName或取出来</span></span><br><span class="line">					<span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">            <span class="comment">// typeCheckOnly是false才会走进这个if里面</span></span><br><span class="line">			<span class="keyword">if</span> (!typeCheckOnly) &#123;</span><br><span class="line">                <span class="comment">// 标记这个bean已创建了</span></span><br><span class="line">				markBeanAsCreated(beanName);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 根据 beanName 获取出 bd.</span></span><br><span class="line"><span class="comment">//先从mergedBeanDefinitions中获取出bd,如果bd是有值的话并且bd的stale是false的话,就会直接返回.</span></span><br><span class="line"><span class="comment">//否则的话,就会走return getMergedBeanDefinition(beanName, getBeanDefinition(beanName));</span></span><br><span class="line"><span class="comment">//方法获取.                </span></span><br><span class="line">				<span class="keyword">final</span> <span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line"><span class="comment">// 该方法是检查bd的.如果bd是抽象的话,就会抛出异常来.                </span></span><br><span class="line">				checkMergedBeanDefinition(mbd, beanName, args);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Guarantee initialization of beans that the current bean depends on.</span></span><br><span class="line"><span class="comment">// 然后从bd中获取 dependsOn 这个注解                </span></span><br><span class="line">				String[] dependsOn = mbd.getDependsOn();</span><br><span class="line"><span class="comment">// 获取出来的 dependsOn 不是null的话,就会进行迭代操作                </span></span><br><span class="line">				<span class="keyword">if</span> (dependsOn != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">for</span> (String dep : dependsOn) &#123;</span><br><span class="line">                        <span class="comment">// 如果是自己依赖自己的话,就会抛出异常来.</span></span><br><span class="line">						<span class="keyword">if</span> (isDependent(beanName, dep)) &#123;</span><br><span class="line">							<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">									<span class="string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">						&#125;</span><br><span class="line">                       <span class="comment">// 注册dependsOn注解的值到 beanFactory中来 </span></span><br><span class="line">						registerDependentBean(dep, beanName);</span><br><span class="line">						<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 然后调用getBean. getBean方法如果没有的话,就会去创建,有的话就会直接返回</span></span><br><span class="line">							getBean(dep);</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;</span><br><span class="line">							<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">									<span class="string">&quot;&#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line"><span class="comment">// if --&gt; else if --&gt; else 分别判断了是不是单例的,是不是单例的, else也就是既不是单例,也不是多例的</span></span><br><span class="line"><span class="comment">//可以看到不管是进入到那个条件中,都会走 getObjectForBeanInstance 这个方法的                </span></span><br><span class="line">				<span class="comment">// Create bean instance.</span></span><br><span class="line">				<span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line"><span class="comment">// getSingleton()方法, 该方法也是使用synchronized (this.singletonObjects) &#123; .... &#125;来加锁的</span></span><br><span class="line"><span class="comment">// 从singletonObjects(Map)中根据beanName获取出值来,如果获取出来的值不是null的话,就会直接返回.       // 如果是null的话,显示判断this.singletonsCurrentlyInDestruction是true的话,就会抛出异常.         // 该方法给出的注释解释是:此时BeanFactory处理销毁状态,不许创建bean.(不要在destroy方法实现中从BeanFactory请求bean)</span></span><br><span class="line"><span class="comment">//beforeSingletonCreation()创建bean之前,inCreationCheckExclusions不包含该beanName并且添加到singletonsCurrentlyInCreation中是失败的,就会抛出一个BeanCurrentlyInCreationException异常来.   </span></span><br><span class="line"><span class="comment">//singletonObject = singletonFactory.getObject()中,singletonFactory是从creatBean方法返回回来的.有定义一个变量newSingleton,getObject这步没有任何异常的话,该变量就会设置为true.</span></span><br><span class="line"><span class="comment">//这里是catch了IllegalStateException/BeanCreationException这二种异常,并且后面还会根据相应的判断来决定抛不抛出异常来.如果是BeanCreationException异常的话,就一定会抛出异常来.</span></span><br><span class="line"><span class="comment">//finally模块代码:之前有一个boolean recordSuppressedExceptions = (this.suppressedExceptions == null);也就是根据this.suppressedExceptions是不是null来判断出来的值. 如果是有的话,finally中就又会讲这个集合给重置为null.  再走一个afterSingletonCreation方法,该方法中如果inCreationCheckExclusions不包含这个beanName并且从singletonsCurrentlyInCreation中移除失败的话,就会抛出相应的异常来.</span></span><br><span class="line"><span class="comment">//最后就是根据newSingleton来判断是不是要添加到singletonObjects(Map)集合中去,也就是走的addSingleton方法. return singletonObject出去, 也就是给 sharedInstance 这个变量了.            </span></span><br><span class="line">					sharedInstance = getSingleton(beanName, () -&gt; &#123;</span><br><span class="line">						<span class="keyword">try</span> &#123;</span><br><span class="line">                            /isInstance/ 创建bean,其中会走很多beanPostProcessor对这个bean进行扩展.</span><br><span class="line">                            <span class="comment">// 当然,你也可以自己定义.</span></span><br><span class="line">							<span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">							<span class="comment">// Explicitly remove instance from singleton cache: It might have been put there</span></span><br><span class="line">							<span class="comment">// eagerly by the creation process, to allow for circular reference resolution.</span></span><br><span class="line">							<span class="comment">// Also remove any beans that received a temporary reference to the bean.</span></span><br><span class="line">                            <span class="comment">// 创建的时候发生了异常,就会销毁这个单例并且会抛出异常来</span></span><br><span class="line">							destroySingleton(beanName);</span><br><span class="line">							<span class="keyword">throw</span> ex;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;);</span><br><span class="line"><span class="comment">//最后走的该方法,getObjectForBeanInstance()是在上面中有进行讲解到的.                    </span></span><br><span class="line">					bean = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (mbd.isPrototype()) &#123;</span><br><span class="line"><span class="comment">// 这里的if条件是多例的判断条件                  </span></span><br><span class="line">					<span class="comment">// It&#x27;s a prototype -&gt; create a new instance.</span></span><br><span class="line">					<span class="type">Object</span> <span class="variable">prototypeInstance</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 创建 bean之前,Object curVal = this.prototypesCurrentlyInCreation.get();</span></span><br><span class="line"><span class="comment">// prototypesCurrentlyInCreation 也是 ThreadLocal,先从中获取出值</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  如果是null的话,就给set到ThreadLocal中去.                       </span></span><br><span class="line"><span class="comment">  如果获取出来的值是 String 类型的话,就会new一个Set集合,然后给beanName和curVal值都给添加进去,最后放入到ThreadLocal中去.</span></span><br><span class="line"><span class="comment">  以上二种都不满足的话,那么获取出来的值就是Set集合,然后给beanName给添加进去</span></span><br><span class="line"><span class="comment">*/</span>                        </span><br><span class="line">						beforePrototypeCreation(beanName);</span><br><span class="line"><span class="comment">/** createBean这个  */</span>                        </span><br><span class="line">						prototypeInstance = createBean(beanName, mbd, args);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">finally</span> &#123;</span><br><span class="line"><span class="comment">//最后就是创建完bean后的逻辑.先从beforeXXX()方法中的ThreadLocal获取出set进去的值</span></span><br><span class="line"><span class="comment">//然后根据是String类型还是Set类型来remove掉ThreadLocal中的值.说明白了,这个地方就是对使用的ThreadLocal数据进行remove处理.</span></span><br><span class="line"><span class="comment">/** 结合before来看的,那么就是before是set值,after就是对set进去的值进行清除. */</span>                    </span><br><span class="line">						afterPrototypeCreation(beanName);</span><br><span class="line">					&#125;</span><br><span class="line">					bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 该条件里面是对既不是单例,也不是多例的的处理.</span></span><br><span class="line"><span class="comment">//显示获取出scope这个注解的值,根据获取出来的值从 scopes 这个Map集合中获取.                   </span></span><br><span class="line">					<span class="type">String</span> <span class="variable">scopeName</span> <span class="operator">=</span> mbd.getScope();</span><br><span class="line">					<span class="keyword">final</span> <span class="type">Scope</span> <span class="variable">scope</span> <span class="operator">=</span> <span class="built_in">this</span>.scopes.get(scopeName);</span><br><span class="line"><span class="comment">//如果从map集合中获取出来的值是null的话,那么这里就会抛出一个异常来.                    </span></span><br><span class="line">					<span class="keyword">if</span> (scope == <span class="literal">null</span>) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line">                      <span class="comment">// TODO get()方法阅读  </span></span><br><span class="line">						<span class="type">Object</span> <span class="variable">scopedInstance</span> <span class="operator">=</span> scope.get(beanName, () -&gt; &#123;</span><br><span class="line"><span class="comment">// 这里的beforePrototypeCreation/createBean/getObjectForBeanInstance</span></span><br><span class="line"><span class="comment">//可以很明显的看到是和上面的多例的逻辑相似的. 不同的是,这里还多走了一个scope.get方法.</span></span><br><span class="line">							beforePrototypeCreation(beanName);</span><br><span class="line">							<span class="keyword">try</span> &#123;</span><br><span class="line">								<span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">							&#125;</span><br><span class="line">							<span class="keyword">finally</span> &#123;</span><br><span class="line">								afterPrototypeCreation(beanName);</span><br><span class="line">							&#125;</span><br><span class="line">						&#125;);</span><br><span class="line">						bean = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">catch</span> (IllegalStateException ex) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName,</span><br><span class="line">								<span class="string">&quot;Scope &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27; is not active for the current thread; consider &quot;</span> +</span><br><span class="line">								<span class="string">&quot;defining a scoped proxy for this bean if you intend to refer to it from a singleton&quot;</span>isInstance,</span><br><span class="line">								ex);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">				cleanupAfterBeanCreationFailure(beanName);</span><br><span class="line">				<span class="keyword">throw</span> ex;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check if required type matches the type of the actual bean instance.</span></span><br><span class="line"><span class="comment">// requiredType不是null,判断调用class.isInstance()方法判断这个class是不是和这个bean相符合         </span></span><br><span class="line">		<span class="keyword">if</span> (requiredType != <span class="literal">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">//满足的话,可以看到这行代码是在做创建bean的操作.  <span class="doctag">TODO:</span> 具体的阅读需要看其底层的代码走向          </span></span><br><span class="line">				<span class="type">T</span> <span class="variable">convertedBean</span> <span class="operator">=</span> getTypeConverter().convertIfNecessary(bean, requiredType);</span><br><span class="line"><span class="comment">// 如果获取出来的 bean是null的话,就抛出一个异常来.                </span></span><br><span class="line">				<span class="keyword">if</span> (convertedBean == <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">				&#125;</span><br><span class="line"><span class="comment">//如果不是null的话,这里就返回.                </span></span><br><span class="line">				<span class="keyword">return</span> convertedBean;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (TypeMismatchException ex) &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">					logger.trace(<span class="string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="string">&quot;&#x27; to required type &#x27;&quot;</span> +</span><br><span class="line">							ClassUtils.getQualifiedName(requiredType) + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 返回bean        </span></span><br><span class="line">		<span class="keyword">return</span> (T) bean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="createBean方法"><a href="#createBean方法" class="headerlink" title="createBean方法"></a>createBean方法</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//---------------------------------------------------------------------</span></span><br><span class="line">	<span class="comment">// Implementation of relevant AbstractBeanFactory template methods</span></span><br><span class="line">	<span class="comment">//---------------------------------------------------------------------</span></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Central method of this class: creates a bean instance,</span></span><br><span class="line"><span class="comment">	 * populates the bean instance, applies post-processors, etc.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #doCreateBean</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">protected</span> Object <span class="title function_">createBean</span><span class="params">(String beanName, RootBeanDefinition mbd, <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">			<span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">			logger.trace(<span class="string">&quot;Creating instance of bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 将传入进来的bd给赋值给mbdToUse.      </span></span><br><span class="line">		<span class="type">RootBeanDefinition</span> <span class="variable">mbdToUse</span> <span class="operator">=</span> mbd;</span><br><span class="line">		<span class="comment">// Make sure bean class is actually resolved at this point, and</span></span><br><span class="line">		<span class="comment">// clone the bean definition in case of a dynamically resolved Class</span></span><br><span class="line">		<span class="comment">// which cannot be stored in the shared merged bean definition.</span></span><br><span class="line"><span class="comment">// 这里获取mbd的class返回回来        </span></span><br><span class="line">		Class&lt;?&gt; resolvedClass = resolveBeanClass(mbd, beanName);</span><br><span class="line"><span class="comment">//如果resolvedClass不是null,mbd的hasBeanClass是false(也就是没有beanClass),mbd调用的beanClassName不是null的情况,就会走到下面的这个方法中        </span></span><br><span class="line">		<span class="keyword">if</span> (resolvedClass != <span class="literal">null</span> &amp;&amp; !mbd.hasBeanClass() &amp;&amp; mbd.getBeanClassName() != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// new 一个bd出来</span></span><br><span class="line">			mbdToUse = <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(mbd);</span><br><span class="line">            <span class="comment">// 给bd设置上 resolvedClass</span></span><br><span class="line">			mbdToUse.setBeanClass(resolvedClass);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// Prepare method overrides.</span></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 重写的方法    </span></span><br><span class="line">			mbdToUse.prepareMethodOverrides();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionStoreException</span>(mbdToUse.getResourceDescription(),</span><br><span class="line">					beanName, <span class="string">&quot;Validation of method overrides failed&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			<span class="comment">// Give BeanPostProcessors a chance to return a proxy instead of the target bean instance.</span></span><br><span class="line"><span class="comment">// mbd.beforeInstantiationResolved是true就会满足条件,代码继续往下走.还有判断条件都好理解.</span></span><br><span class="line"><span class="comment">//主要看applyBeanPostProcessorsBeforeInstantiation方法,该方法先获取出全部的 beanPostProcessors,然后迭代集合,如果是InstantiationAwareBeanPostProcessor接口的子类的话,就会强转走postProcessBeforeInstantiation方法. 最后返回一个bean,该bean是有可能是null的. </span></span><br><span class="line"><span class="comment">//如果是null的话,就不会走到applyBeanPostProcessorsAfterInitialization方法</span></span><br><span class="line"><span class="comment">//如果不是null,救会走这个方法.所以我们接着看这个方法.</span></span><br><span class="line"><span class="comment">// applyBeanPostProcessorsAfterInitialization方法:获取出全部的BeanPostProcessors,然后迭代,挨个调用其postProcessAfterInitialization方法,最后返回bean回去            </span></span><br><span class="line">			<span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> resolveBeforeInstantiation(beanName, mbdToUse);</span><br><span class="line">            <span class="comment">// 如果bean不是null的话,就直接返回掉.</span></span><br><span class="line">			<span class="keyword">if</span> (bean != <span class="literal">null</span>) &#123;</span><br><span class="line">				<span class="keyword">return</span> bean;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbdToUse.getResourceDescription(), beanName,</span><br><span class="line">					<span class="string">&quot;BeanPostProcessor before instantiation of bean failed&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line"> <span class="comment">// 调用 doCreateBean() 返回一个bean,最后给这个bean返回回去(如果没出任何异常的情况下).           </span></span><br><span class="line">			<span class="type">Object</span> <span class="variable">beanInstance</span> <span class="operator">=</span> doCreateBean(beanName, mbdToUse, args);</span><br><span class="line">			<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">				logger.trace(<span class="string">&quot;Finished creating instance of bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> beanInstance;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (BeanCreationException | ImplicitlyAppearedSingletonException ex) &#123;</span><br><span class="line">			<span class="comment">// A previously detected exception with proper bean creation context already,</span></span><br><span class="line">			<span class="comment">// or illegal singleton state to be communicated up to DefaultSingletonBeanRegistry.</span></span><br><span class="line">			<span class="keyword">throw</span> ex;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">					mbdToUse.getResourceDescription(), beanName, <span class="string">&quot;Unexpected exception during bean creation&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="doCreateBean方法"><a href="#doCreateBean方法" class="headerlink" title="doCreateBean方法"></a>doCreateBean方法</h1><p>该方法就是真正创建bean的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Actually create the specified bean. Pre-creation processing has already happened</span></span><br><span class="line"><span class="comment">	 * at this point, e.g. checking &#123;<span class="doctag">@code</span> postProcessBeforeInstantiation&#125; callbacks.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;Differentiates between default bean instantiation, use of a</span></span><br><span class="line"><span class="comment">	 * factory method, and autowiring a constructor.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> mbd the merged bean definition for the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> args explicit arguments to use for constructor or factory method invocation</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a new instance of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> BeanCreationException if the bean could not be created</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #instantiateBean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #instantiateUsingFactoryMethod</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #autowireConstructor</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">protected</span> Object <span class="title function_">doCreateBean</span><span class="params">(<span class="keyword">final</span> String beanName, <span class="keyword">final</span> RootBeanDefinition mbd, <span class="keyword">final</span> <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">			<span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Instantiate the bean.</span></span><br><span class="line"><span class="comment">// bean包装		</span></span><br><span class="line">		<span class="type">BeanWrapper</span> <span class="variable">instanceWrapper</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="comment">// bd如果是单例的话,就从factoryBeanInstanceCache(Map集合)中remove出去        </span></span><br><span class="line">		<span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line">			instanceWrapper = <span class="built_in">this</span>.factoryBeanInstanceCache.remove(beanName);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// instanceWrapper是null的话,就创建一个BeanWrapper出来.</span></span><br><span class="line"><span class="comment">// 调用createBeanInstance方法来创建          </span></span><br><span class="line">		<span class="keyword">if</span> (instanceWrapper == <span class="literal">null</span>) &#123;</span><br><span class="line"><span class="comment">/** 先获取出bd的class信息,如果获取出来的class不是null并且beanClass.getModifiers()不是public并且</span></span><br><span class="line"><span class="comment">	mbd.isNonPublicAccessAllowed()是false的话,就会抛出一个异常来.</span></span><br><span class="line"><span class="comment">	获取bd的instanceSupplier,如果instanceSupplier不是null的话,就会走obtainFromSupplier(instanceSupplier, beanName);并且直接return掉. 从方法上来看,是从Supplier中获取.	obtainFromSupplier方法:从currentlyCreatedBean中获取,可以看到也是存储在ThreadLocal中,只不过这里Spring自己写了一个NamedThreadLocal是集成ThreadLocal,也就是换了一个马甲. 从Supplier的get方法获取出Obejct.  finally中,根据进来从ThreadLocal中get出来的值,如果获取出来的值不是null的话,这里就会又set进去. 如果获取是null,就给remove掉.  Supplier.get() 返回的是null的话,就会创建一个instance = new NullBean();  接着new一个BeanWrapper bw = new BeanWrapperImpl(instance)出来,再走initBeanWrapper方法,然后就return掉了.</span></span><br><span class="line"><span class="comment">initBeanWrapper()方法: 给bw(BeanWrapper) 先是获取出 ConversionService,然后赋值给bw. 然后调用</span></span><br><span class="line"><span class="comment">registerCustomEditors()方法,也就是注册Customer修改 TODO后面阅读				</span></span><br><span class="line"><span class="comment">*/</span>            </span><br><span class="line">			instanceWrapper = createBeanInstance(beanName, mbd, args);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">//从 beanWarpper中获取出 instance 和 class        </span></span><br><span class="line">		<span class="keyword">final</span> <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> instanceWrapper.getWrappedInstance();</span><br><span class="line">		Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();</span><br><span class="line"><span class="comment">// 获取出来的 beanTyoe 不是 NullBean.class，        </span></span><br><span class="line">		<span class="keyword">if</span> (beanType != NullBean.class) &#123;</span><br><span class="line">			mbd.resolvedTargetType = beanType;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Allow post-processors to modify the merged bean definition.</span></span><br><span class="line">		<span class="keyword">synchronized</span> (mbd.postProcessingLock) &#123;</span><br><span class="line">			<span class="keyword">if</span> (!mbd.postProcessed) &#123;</span><br><span class="line">				<span class="keyword">try</span> &#123;</span><br><span class="line">					applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">							<span class="string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);</span><br><span class="line">				&#125;</span><br><span class="line">				mbd.postProcessed = <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Eagerly cache singletons to be able to resolve circular references</span></span><br><span class="line">		<span class="comment">// even when triggered by lifecycle interfaces like BeanFactoryAware.</span></span><br><span class="line">		<span class="type">boolean</span> <span class="variable">earlySingletonExposure</span> <span class="operator">=</span> (mbd.isSingleton() &amp;&amp; <span class="built_in">this</span>.allowCircularReferences &amp;&amp;</span><br><span class="line">				isSingletonCurrentlyInCreation(beanName));</span><br><span class="line">		<span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">			<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">				logger.trace(<span class="string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +</span><br><span class="line">						<span class="string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Initialize the bean instance.</span></span><br><span class="line">		<span class="type">Object</span> <span class="variable">exposedObject</span> <span class="operator">=</span> bean;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			populateBean(beanName, mbd, instanceWrapper);</span><br><span class="line">			exposedObject = initializeBean(beanName, exposedObject, mbd);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">if</span> (ex <span class="keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;</span><br><span class="line">				<span class="keyword">throw</span> (BeanCreationException) ex;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">						mbd.getResourceDescription(), beanName, <span class="string">&quot;Initialization of bean failed&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">			<span class="type">Object</span> <span class="variable">earlySingletonReference</span> <span class="operator">=</span> getSingleton(beanName, <span class="literal">false</span>);</span><br><span class="line">			<span class="keyword">if</span> (earlySingletonReference != <span class="literal">null</span>) &#123;</span><br><span class="line">				<span class="keyword">if</span> (exposedObject == bean) &#123;</span><br><span class="line">					exposedObject = earlySingletonReference;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;</span><br><span class="line">					String[] dependentBeans = getDependentBeans(beanName);</span><br><span class="line">					Set&lt;String&gt; actualDependentBeans = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(dependentBeans.length);</span><br><span class="line">					<span class="keyword">for</span> (String dependentBean : dependentBeans) &#123;</span><br><span class="line">						<span class="keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;</span><br><span class="line">							actualDependentBeans.add(dependentBean);</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName,</span><br><span class="line">								<span class="string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; has been injected into other beans [&quot;</span> +</span><br><span class="line">								StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +</span><br><span class="line">								<span class="string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +</span><br><span class="line">								<span class="string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +</span><br><span class="line">								<span class="string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +</span><br><span class="line">								<span class="string">&quot;&#x27;getBeanNamesForType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Register bean as disposable.</span></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			registerDisposableBeanIfNecessary(beanName, bean, mbd);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(</span><br><span class="line">					mbd.getResourceDescription(), beanName, <span class="string">&quot;Invalid destruction signature&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> exposedObject;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring源码阅读</title>
    <url>//article/Spring_source1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Spring源码流程阅读</p>
</blockquote>
<h1 id="📝-阅读方式"><a href="#📝-阅读方式" class="headerlink" title="📝 阅读方式"></a>📝 阅读方式</h1><ul>
<li>创建一个 Maven项目,引入Spring相关的依赖, 然后写一个启动类,在类上打上debug一层一层的走进去,就可以看到源码是怎么走的,怎么加载的</li>
<li>从github上git Spring到自己本来,然后安装gradle,build 一下项目就可以了,这是基于Spring源码去跑的,可以直接在代码里面打印对应的输出语句,对于代码的走向是更加熟悉的</li>
<li>目前比较流行的是SpringBoot,也可以直接启动一个SpringBoot项目,然后在run方法上打上断点,跟着源码一步一步的走，最后每个方法都走一遍会很有效的理解</li>
</ul>
<h1 id="源码阅读"><a href="#源码阅读" class="headerlink" title="源码阅读"></a>源码阅读</h1><p>这里我们写一个读取从包位置扫描的类. 其实这个注解,你也可以加入到你启动类上面去扫描,效果也是一样的.这样的话,YangConfig就会当作为一个bean。如果你写在你的启动类上的话,那么你的启动类就会当作为一个bean</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yang.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.ComponentScan;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: Mu_Yi</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: 2020/1/5 21:45</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@qq</span>: 1411091515</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@ComponentScan(&quot;com.yang&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动类, 可以看到对于启动类,还是做了不少实验,但是这肯定是不够的,启动还是有很多的内容没有完全的写入进去的. 在这里打算断点就可以去跟着源码走。等等，这里是不是少了一个很明显的bean</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yang;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.yang.config.LwfConfig;</span><br><span class="line"><span class="keyword">import</span> com.yang.config.YangConfig;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.AnnotationConfigApplicationContext;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: Mu_Yi</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: 2020/1/5 21:45</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Version</span> 1.0</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@qq</span>: 1411091515</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">YangMain</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">      <span class="type">AnnotationConfigApplicationContext</span> <span class="variable">configApplicationContext</span> <span class="operator">=</span></span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">AnnotationConfigApplicationContext</span>(YangConfig.class<span class="comment">/*, LwfConfig.class*/</span>);</span><br><span class="line"></span><br><span class="line">      <span class="comment">//  构造方法中有调用这二个</span></span><br><span class="line">      <span class="comment">// configApplicationContext.register(YangConfig.class);</span></span><br><span class="line">      <span class="comment">// configApplicationContext.refresh();</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// System.out.println(configApplicationContext.getBean(&quot;one&quot;));</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="梦开始的地方"><a href="#梦开始的地方" class="headerlink" title="梦开始的地方"></a>梦开始的地方</h1><p>这里我们打上debug，跟着代码一步一步的走进去，看看是如何执行的. 想看想法是怎么走的话,就可以仔细看堆栈的信息。 然后我们跟着开始的方法,一步一步的跟着走进去</p>
<p>org.springframework.context.annotation.AnnotationConfigApplicationContext</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">(Class&lt;?&gt;... annotatedClasses)</span> &#123;</span><br><span class="line">   <span class="comment">/**</span></span><br><span class="line"><span class="comment">    *  先走父类的方法;再去走子类的方法</span></span><br><span class="line"><span class="comment">    *</span></span><br><span class="line"><span class="comment">    *  1 Step : 走无参构成方法 this() 然后会继续往上调用父类的无参构造函数</span></span><br><span class="line"><span class="comment">    *  2 Step :</span></span><br><span class="line"><span class="comment">    *  3 Step :</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="built_in">this</span>();</span><br><span class="line"></span><br><span class="line">   register(annotatedClasses);</span><br><span class="line"></span><br><span class="line">   refresh();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotationConfigApplicationContext</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		 *  创建一个读取注解的bean定义读取器</span></span><br><span class="line"><span class="comment">		 *  什么是bean  ??  BeanDefinition ??  被注解的BeanDefinition</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="comment">// 初始化一个 AnnotatedBeanDefinitionReader</span></span><br><span class="line">		<span class="built_in">this</span>.reader = <span class="keyword">new</span> <span class="title class_">AnnotatedBeanDefinitionReader</span>(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 可以用来扫描包或者类;继而转换为pd ; 但是实际不是用scanner 这个对象</span></span><br><span class="line">		<span class="comment">// 初始化一个 ClassPathBeanDefinitionScanner</span></span><br><span class="line">		<span class="built_in">this</span>.scanner = <span class="keyword">new</span> <span class="title class_">ClassPathBeanDefinitionScanner</span>(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里我们跟着 this() 这个构造方法一步一步的走.</p>
<p>new AnnotatedBeanDefinitionReader(this) 走到下面的地方</p>
<p>org.springframework.context.annotation.AnnotatedBeanDefinitionReader#AnnotatedBeanDefinitionReader(org.springframework.beans.factory.support.BeanDefinitionRegistry)</p>
<p>this.reader &#x3D; new AnnotatedBeanDefinitionReader(this) 的调用链分析,这里给我的感觉，就是对系统的一些数据，进行创建beanDefinition，然后放入到BeanFactory中去</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Create a new &#123;<span class="doctag">@code</span> AnnotatedBeanDefinitionReader&#125; for the given registry.</span></span><br><span class="line"><span class="comment"> * If the registry is &#123;<span class="doctag">@link</span> EnvironmentCapable&#125;, e.g. is an &#123;<span class="doctag">@code</span> ApplicationContext&#125;,</span></span><br><span class="line"><span class="comment"> * the &#123;<span class="doctag">@link</span> Environment&#125; will be inherited, otherwise a new</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> StandardEnvironment&#125; will be created and used.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> registry the &#123;<span class="doctag">@code</span> BeanFactory&#125; to load bean definitions into,</span></span><br><span class="line"><span class="comment"> * in the form of a &#123;<span class="doctag">@code</span> BeanDefinitionRegistry&#125;</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #AnnotatedBeanDefinitionReader(BeanDefinitionRegistry, Environment)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #setEnvironment(Environment)</span></span><br><span class="line"><span class="comment">   创建一个 AnnotatedBeanDefinitionReader 给注册表. </span></span><br><span class="line"><span class="comment">   最后走 AnnotatedBeanDefinitionReader,下面继续分析</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">AnnotatedBeanDefinitionReader</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>(registry, getOrCreateEnvironment(registry));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Get the Environment from the given registry if possible, otherwise return a new</span></span><br><span class="line"><span class="comment">	 * StandardEnvironment.</span></span><br><span class="line"><span class="comment">	  这里传入进来的 registry : org.springframework.context.annotation.AnnotationConfigApplicationContext@511baa65: startup date [Thu Jan 01 08:00:00 CST 1970]; root of context hierarchy.</span></span><br><span class="line"><span class="comment">	  可以看出 AnnotationConfigApplicationContext 是满足if条件的. 然后级会走到 AbstractApplicationContext 中的 getEnvironment() 方法,也是创建了一个 StandardEnvironment,不同的是,这个对象是 AbstractApplicationContext 中的变量.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> Environment <span class="title function_">getOrCreateEnvironment</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		<span class="keyword">if</span> (registry <span class="keyword">instanceof</span> EnvironmentCapable) &#123;</span><br><span class="line">			<span class="keyword">return</span> ((EnvironmentCapable) registry).getEnvironment();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">StandardEnvironment</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**	</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@code</span> AnnotatedBeanDefinitionReader&#125; for the given registry and using</span></span><br><span class="line"><span class="comment">	 * the given &#123;<span class="doctag">@link</span> Environment&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> registry the &#123;<span class="doctag">@code</span> BeanFactory&#125; to load bean definitions into,</span></span><br><span class="line"><span class="comment">	 * in the form of a &#123;<span class="doctag">@code</span> BeanDefinitionRegistry&#125;</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> environment the &#123;<span class="doctag">@code</span> Environment&#125; to use when evaluating bean definition</span></span><br><span class="line"><span class="comment">	 * profiles.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@since</span> 3.1</span></span><br><span class="line"><span class="comment">	 </span></span><br><span class="line"><span class="comment">	 执行参数代码: private BeanNameGenerator beanNameGenerator = new AnnotationBeanNameGenerator();</span></span><br><span class="line"><span class="comment">	 private ScopeMetadataResolver scopeMetadataResolver = new AnnotationScopeMetadataResolver();</span></span><br><span class="line"><span class="comment">	 这里可以看到的是执行了二个对象操作,在全局变量中.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">AnnotatedBeanDefinitionReader</span><span class="params">(BeanDefinitionRegistry registry, Environment environment)</span> &#123;</span><br><span class="line">        <span class="comment">// 对 registry 和 environment 进行不为null的判断.	</span></span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		Assert.notNull(environment, <span class="string">&quot;Environment must not be null&quot;</span>);</span><br><span class="line">		<span class="built_in">this</span>.registry = registry</span><br><span class="line">        <span class="comment">// 分割线一是对 new ConditionEvaluator的阅读;可以看到这个方放主要对一些registry,beanFactory等资源判断</span></span><br><span class="line">		<span class="built_in">this</span>.conditionEvaluator = <span class="keyword">new</span> <span class="title class_">ConditionEvaluator</span>(registry, environment, <span class="literal">null</span>);</span><br><span class="line">        <span class="comment">// 分割线一以后的就是对对应的进行分析</span></span><br><span class="line">		AnnotationConfigUtils.registerAnnotationConfigProcessors(<span class="built_in">this</span>.registry);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-----------------------------------</span><br><span class="line">    <span class="comment">// 分割线一</span></span><br><span class="line">    <span class="comment">// org.springframework.context.annotation.ConditionEvaluator</span></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@link</span> ConditionEvaluator&#125; instance.</span></span><br><span class="line"><span class="comment">	 这里可以看到将传入进来的参数,调用一个内部类的构造方放</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ConditionEvaluator</span><span class="params">(<span class="meta">@Nullable</span> BeanDefinitionRegistry registry,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> Environment environment, <span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line"></span><br><span class="line">		<span class="built_in">this</span>.context = <span class="keyword">new</span> <span class="title class_">ConditionContextImpl</span>(registry, environment, resourceLoader);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 内部类 ConditionContextImpl </span></span><br><span class="line">		<span class="keyword">public</span> <span class="title function_">ConditionContextImpl</span><span class="params">(<span class="meta">@Nullable</span> BeanDefinitionRegistry registry,</span></span><br><span class="line"><span class="params">				<span class="meta">@Nullable</span> Environment environment, <span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line"></span><br><span class="line">			<span class="built_in">this</span>.registry = registry;</span><br><span class="line">            <span class="comment">// 推断beanFactory;这里返回的beanFactory是DefaultListableBeanFactory</span></span><br><span class="line">            <span class="comment">// 可以看到是推断出了 beanFactory是 DefaultListableBeanFactory</span></span><br><span class="line">			<span class="built_in">this</span>.beanFactory = deduceBeanFactory(registry);</span><br><span class="line">            <span class="comment">// 如果environment是null的话,那么这里就会像beanFactory一样</span></span><br><span class="line">            <span class="comment">// Spring 自己进行推断下,但是从之前的构造函数来看,这个地方是正常情况是不会为null的</span></span><br><span class="line">            <span class="comment">// 所以不是很清楚什么情况下为null. 可能是从其他地方走进来 ?</span></span><br><span class="line">			<span class="built_in">this</span>.environment = (environment != <span class="literal">null</span> ? environment : deduceEnvironment(registry));</span><br><span class="line">            <span class="comment">// resourceLoader我们传入进来的是null,所以这里走了推断ResourceLoader方放.</span></span><br><span class="line">            <span class="comment">// 最后返回的是AnnotationConfigApplicationContext,也就是我们在启动Class里面使用的对象</span></span><br><span class="line">			<span class="built_in">this</span>.resourceLoader = (resourceLoader != <span class="literal">null</span> ? resourceLoader : deduceResourceLoader(registry));</span><br><span class="line">            <span class="comment">// 这里的classLoader是从 beanFacotry中获取出来的.</span></span><br><span class="line">			<span class="built_in">this</span>.classLoader = deduceClassLoader(resourceLoader, <span class="built_in">this</span>.beanFactory);</span><br><span class="line">		&#125;</span><br><span class="line">------------------</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.springframework.context.annotation.AnnotationConfigUtils    </span></span><br><span class="line">    </span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Register all relevant annotation post processors in the given registry.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> registry the registry to operate on</span></span><br><span class="line"><span class="comment">	 翻译 : 在给定的注册表中注册所有相关的注释后处理器。</span></span><br><span class="line"><span class="comment">	 这里个人认为是对一些加了注解的类等信息的读取。</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">registerAnnotationConfigProcessors</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">		registerAnnotationConfigProcessors(registry, <span class="literal">null</span>);</span><br><span class="line">	&#125;    </span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Register all relevant annotation post processors in the given registry.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> registry the registry to operate on</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> source the configuration source element (already extracted)</span></span><br><span class="line"><span class="comment">	 * that this registration was triggered from. May be &#123;<span class="doctag">@code</span> null&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a Set of BeanDefinitionHolders, containing all bean definitions</span></span><br><span class="line"><span class="comment">	 * that have actually been registered by this call</span></span><br><span class="line"><span class="comment">	 这里就是总结,就是将将一些类,创建对应的BeanDefinition,然后根据beanName注册到BeanFactory中去,也就是方法一个Map中,key是beanName,value就是对应的BeanDefintion对象.</span></span><br><span class="line"><span class="comment">     正常的情况下,这里是一共有六个的.</span></span><br><span class="line"><span class="comment">     分别是 : org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">     org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">     org.springframework.context.annotation.internalRequiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">     org.springframework.context.annotation.internalCommonAnnotationProcessor</span></span><br><span class="line"><span class="comment">     org.springframework.context.event.internalEventListenerProcessor</span></span><br><span class="line"><span class="comment">     org.springframework.context.event.internalEventListenerFactory</span></span><br><span class="line"><span class="comment">     根据 Processor 后缀可以大致猜测到,是对什么进行大致的处理.</span></span><br><span class="line"><span class="comment">     然后还有一个EventListenerFacotry的</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> Set&lt;BeanDefinitionHolder&gt; <span class="title function_">registerAnnotationConfigProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">			BeanDefinitionRegistry registry, <span class="meta">@Nullable</span> Object source)</span> &#123;</span><br><span class="line">       <span class="comment">// 根据 registry 获取出beanFactory,也就是获取出了 DefaultListableBeanFactory这个beanFactory. </span></span><br><span class="line">		<span class="type">DefaultListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> unwrapDefaultListableBeanFactory(registry);</span><br><span class="line">		<span class="keyword">if</span> (beanFactory != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="comment">// beanFactory.getDependencyComparator() 返回的是null,所以条件成立.</span></span><br><span class="line">            <span class="comment">// 传入 AnnotationAwareOrderComparator,走的 DefaultListableBeanFactory 的 setDependencyComparator,也就是将 AnnotationAwareOrderComparator 赋值给 this.dependencyComparator(DefaultListableBeanFactory全局变量)</span></span><br><span class="line">			<span class="keyword">if</span> (!(beanFactory.getDependencyComparator() <span class="keyword">instanceof</span> AnnotationAwareOrderComparator)) &#123;</span><br><span class="line">				beanFactory.setDependencyComparator(AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">			&#125;</span><br><span class="line">            <span class="comment">// beanFactory.getAutowireCandidateResolver() 返回的是 SimpleAutowireCandidateResolver这个,满足条件, 走 DefaultListableBeanFactory的setAutowireCandidateResolver方放,</span></span><br><span class="line">			<span class="keyword">if</span> (!(beanFactory.getAutowireCandidateResolver() <span class="keyword">instanceof</span> ContextAnnotationAutowireCandidateResolver)) &#123;</span><br><span class="line">                <span class="comment">// 下面有 setAutowireCandidateResolver 方放的阅读</span></span><br><span class="line">				beanFactory.setAutowireCandidateResolver(<span class="keyword">new</span> <span class="title class_">ContextAnnotationAutowireCandidateResolver</span>());</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Map 存储操作;方便传递参数</span></span><br><span class="line">		Set&lt;BeanDefinitionHolder&gt; beanDefs = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 如果 registry 没有包含 CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME,就会进入。</span></span><br><span class="line">        <span class="comment">// 这里是没有包含的,所以是进入的</span></span><br><span class="line">		<span class="keyword">if</span> (!registry.containsBeanDefinition(CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">            <span class="comment">// 这里走了RootBeanDefiniion的构造函数,然后调用super方放,于是就走到了AbstractBeanDefintion中来.这个二个BeanDefiniton里面是有很多全局参数的初始化的.最后返回一个bd.bd的beanClass也就是传入进去的 ConfigurationClassPostProcessor.class</span></span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(ConfigurationClassPostProcessor.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">            <span class="comment">// registerPostProcessor 方放, 给bd设置Role,值是2,</span></span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 与 这里可以看到和上面的CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME处理逻辑是一样的 </span></span><br><span class="line">		<span class="keyword">if</span> (!registry.containsBeanDefinition(AUTOWIRED_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(AutowiredAnnotationBeanPostProcessor.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">            <span class="comment">// registerPostProcessor() 方法是将 bd注册到 beanFactory中去,从代码中看,根据beanName存储到Map中,然后放入到一个List集合中.</span></span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, AUTOWIRED_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果 registry 不包含 REQUIRED_ANNOTATION_PROCESSOR_BEAN_NAME,	这里可以看到和上面的CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME处理逻辑是一样的</span></span><br><span class="line">		<span class="keyword">if</span> (!registry.containsBeanDefinition(REQUIRED_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(RequiredAnnotationBeanPostProcessor.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, REQUIRED_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check for JSR-250 support, and if present add the CommonAnnotationBeanPostProcessor.</span></span><br><span class="line">        <span class="comment">// 这里与上面是一样得，区别在于 判断条件多了一个jsr250Present的参数判断.</span></span><br><span class="line">		<span class="keyword">if</span> (jsr250Present &amp;&amp; !registry.containsBeanDefinition(COMMON_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(CommonAnnotationBeanPostProcessor.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, COMMON_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check for JPA support, and if present add the PersistenceAnnotationBeanPostProcessor.</span></span><br><span class="line">      <span class="comment">// 这里没有进入.</span></span><br><span class="line">		<span class="keyword">if</span> (jpaPresent &amp;&amp; !registry.containsBeanDefinition(PERSISTENCE_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>();</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				def.setBeanClass(ClassUtils.forName(PERSISTENCE_ANNOTATION_PROCESSOR_CLASS_NAME,</span><br><span class="line">						AnnotationConfigUtils.class.getClassLoader()));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (ClassNotFoundException ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(</span><br><span class="line">						<span class="string">&quot;Cannot load optional framework class: &quot;</span> + PERSISTENCE_ANNOTATION_PROCESSOR_CLASS_NAME, ex);</span><br><span class="line">			&#125;</span><br><span class="line">			def.setSource(source);</span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, PERSISTENCE_ANNOTATION_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 处理与上面类似.</span></span><br><span class="line">		<span class="keyword">if</span> (!registry.containsBeanDefinition(EVENT_LISTENER_PROCESSOR_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(EventListenerMethodProcessor.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, EVENT_LISTENER_PROCESSOR_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (!registry.containsBeanDefinition(EVENT_LISTENER_FACTORY_BEAN_NAME)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">def</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(DefaultEventListenerFactory.class);</span><br><span class="line">			def.setSource(source);</span><br><span class="line">			beanDefs.add(registerPostProcessor(registry, def, EVENT_LISTENER_FACTORY_BEAN_NAME));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> beanDefs;</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">// DefaultListableBeanFactory类中的方放,</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAutowireCandidateResolver</span><span class="params">(<span class="keyword">final</span> AutowireCandidateResolver autowireCandidateResolver)</span> &#123;</span><br><span class="line">		Assert.notNull(autowireCandidateResolver, <span class="string">&quot;AutowireCandidateResolver must not be null&quot;</span>);</span><br><span class="line">    <span class="comment">// 如果传入进来的 autowireCandidateResolver 是 BeanFactoryAware或者BeanFactoryAware的子类，就会满足条件进入.</span></span><br><span class="line">		<span class="keyword">if</span> (autowireCandidateResolver <span class="keyword">instanceof</span> BeanFactoryAware) &#123;</span><br><span class="line">            <span class="comment">// System.getSecurityManager() 方放返回的null,所以这里会走到else中</span></span><br><span class="line">			<span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span>) &#123;</span><br><span class="line">				AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">					((BeanFactoryAware) autowireCandidateResolver).setBeanFactory(DefaultListableBeanFactory.<span class="built_in">this</span>);</span><br><span class="line">					<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">				&#125;, getAccessControlContext());</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// org.springframework.beans.factory.support.GenericTypeAwareAutowireCandidateResolver</span></span><br><span class="line">                <span class="comment">// 这里会走到GenericTypeAwareAutowireCandidateResolver中,然后将DefaultListableBeanFactory赋值给其定义的beanFactory变量</span></span><br><span class="line">				((BeanFactoryAware) autowireCandidateResolver).setBeanFactory(<span class="built_in">this</span>);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">// 变量赋值</span></span><br><span class="line">		<span class="built_in">this</span>.autowireCandidateResolver = autowireCandidateResolver;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//---------------------------------------------------------------------</span></span><br><span class="line">	<span class="comment">// Implementation of BeanDefinitionRegistry interface</span></span><br><span class="line">	<span class="comment">//---------------------------------------------------------------------</span></span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerBeanDefinition</span><span class="params">(String beanName, BeanDefinition beanDefinition)</span></span><br><span class="line">			<span class="keyword">throws</span> BeanDefinitionStoreException &#123;</span><br><span class="line">		<span class="comment">// 对传入进来的参数进行检验.可以看到Spring还是蛮严格的</span></span><br><span class="line">		Assert.hasText(beanName, <span class="string">&quot;Bean name must not be empty&quot;</span>);</span><br><span class="line">		Assert.notNull(beanDefinition, <span class="string">&quot;BeanDefinition must not be null&quot;</span>);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (beanDefinition <span class="keyword">instanceof</span> AbstractBeanDefinition) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 走到 AbstractBeanDefinition 进行 validate,也就是检验.</span></span><br><span class="line">                <span class="comment">// validate中判断是否重写的方放和工厂方法放一起,否则就会报错.工厂方法必须创建具体的bean使用.  this.beanClass instanceof Class 判断 beanClass是不是Class,进入到 prepareMethodOverrides方法,判断是否有重写的放,如果有的话,就会进行相应的处理. </span></span><br><span class="line">				((AbstractBeanDefinition) beanDefinition).validate();</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionStoreException</span>(beanDefinition.getResourceDescription(), beanName,</span><br><span class="line">						<span class="string">&quot;Validation of bean definition failed&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从 beanDefinitionMap中获取db,根据beanName来获取. 这里获取出来的是null,所以先走到else方法中看看是怎么走的,</span></span><br><span class="line">		<span class="type">BeanDefinition</span> <span class="variable">existingDefinition</span> <span class="operator">=</span> <span class="built_in">this</span>.beanDefinitionMap.get(beanName);</span><br><span class="line">		<span class="keyword">if</span> (existingDefinition != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (!isAllowBeanDefinitionOverriding()) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionStoreException</span>(beanDefinition.getResourceDescription(), beanName,</span><br><span class="line">						<span class="string">&quot;Cannot register bean definition [&quot;</span> + beanDefinition + <span class="string">&quot;] for bean &#x27;&quot;</span> + beanName +</span><br><span class="line">						<span class="string">&quot;&#x27;: There is already [&quot;</span> + existingDefinition + <span class="string">&quot;] bound.&quot;</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (existingDefinition.getRole() &lt; beanDefinition.getRole()) &#123;</span><br><span class="line">				<span class="comment">// e.g. was ROLE_APPLICATION, now overriding with ROLE_SUPPORT or ROLE_INFRASTRUCTURE</span></span><br><span class="line">				<span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">					logger.warn(<span class="string">&quot;Overriding user-defined bean definition for bean &#x27;&quot;</span> + beanName +</span><br><span class="line">							<span class="string">&quot;&#x27; with a framework-generated bean definition: replacing [&quot;</span> +</span><br><span class="line">							existingDefinition + <span class="string">&quot;] with [&quot;</span> + beanDefinition + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (!beanDefinition.equals(existingDefinition)) &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isInfoEnabled()) &#123;</span><br><span class="line">					logger.info(<span class="string">&quot;Overriding bean definition for bean &#x27;&quot;</span> + beanName +</span><br><span class="line">							<span class="string">&quot;&#x27; with a different definition: replacing [&quot;</span> + existingDefinition +</span><br><span class="line">							<span class="string">&quot;] with [&quot;</span> + beanDefinition + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">					logger.debug(<span class="string">&quot;Overriding bean definition for bean &#x27;&quot;</span> + beanName +</span><br><span class="line">							<span class="string">&quot;&#x27; with an equivalent definition: replacing [&quot;</span> + existingDefinition +</span><br><span class="line">							<span class="string">&quot;] with [&quot;</span> + beanDefinition + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="built_in">this</span>.beanDefinitionMap.put(beanName, beanDefinition);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 检查beanFactory创建阶段是否启动,同时是否有bean被标记创建.这里返回的是false,我们继续往下来看else里面的逻辑</span></span><br><span class="line">			<span class="keyword">if</span> (hasBeanCreationStarted()) &#123;</span><br><span class="line">				<span class="comment">// Cannot modify startup-time collection elements anymore (for stable iteration)</span></span><br><span class="line">				<span class="keyword">synchronized</span> (<span class="built_in">this</span>.beanDefinitionMap) &#123;</span><br><span class="line">					<span class="built_in">this</span>.beanDefinitionMap.put(beanName, beanDefinition);</span><br><span class="line">					List&lt;String&gt; updatedDefinitions = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(<span class="built_in">this</span>.beanDefinitionNames.size() + <span class="number">1</span>);</span><br><span class="line">					updatedDefinitions.addAll(<span class="built_in">this</span>.beanDefinitionNames);</span><br><span class="line">					updatedDefinitions.add(beanName);</span><br><span class="line">					<span class="built_in">this</span>.beanDefinitionNames = updatedDefinitions;</span><br><span class="line">					<span class="keyword">if</span> (<span class="built_in">this</span>.manualSingletonNames.contains(beanName)) &#123;</span><br><span class="line">						Set&lt;String&gt; updatedSingletons = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="built_in">this</span>.manualSingletonNames);</span><br><span class="line">						updatedSingletons.remove(beanName);</span><br><span class="line">						<span class="built_in">this</span>.manualSingletonNames = updatedSingletons;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="comment">// Still in startup registration phase.  任然在解析阶段,并没有在创建bean阶段.</span></span><br><span class="line">				<span class="comment">// 将beanDefinition根据beanName存放在Map中,然后添加到beanDefinitionNames的List集合中。从 manualSingletonNames Set集合中 remove出去.</span></span><br><span class="line">                <span class="built_in">this</span>.beanDefinitionMap.put(beanName, beanDefinition);</span><br><span class="line">				<span class="built_in">this</span>.beanDefinitionNames.add(beanName);</span><br><span class="line">				<span class="built_in">this</span>.manualSingletonNames.remove(beanName);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="built_in">this</span>.frozenBeanDefinitionNames = <span class="literal">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// existingDefinition 是null,没有进入到这个方法中. 目测这是一个重置beanDefintion的方法，就好比有个beanDefintion已经在了,但是有些值什么是又修改的,然后可能要重新放入一遍的逻辑.</span></span><br><span class="line">		<span class="keyword">if</span> (existingDefinition != <span class="literal">null</span> || containsSingleton(beanName)) &#123;</span><br><span class="line">			resetBeanDefinition(beanName);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AnnotationConfigUtils类中,主要看registry.registerBeanDefinitio()方放,看下是怎么注册bd到BeanFacotry中去的.</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> BeanDefinitionHolder <span class="title function_">registerPostProcessor</span><span class="params">(</span></span><br><span class="line"><span class="params">			BeanDefinitionRegistry registry, RootBeanDefinition definition, String beanName)</span> &#123;</span><br><span class="line">       </span><br><span class="line">		definition.setRole(BeanDefinition.ROLE_INFRASTRUCTURE);</span><br><span class="line">    <span class="comment">// 上面有对 registerBeanDefinition 注册 bd 到 beanFactory中去的逻辑.</span></span><br><span class="line">		registry.registerBeanDefinition(beanName, definition);</span><br><span class="line">    <span class="comment">// 然后根据 bd, beanName 创建一个  BeanDefinitionHolder返回</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(definition, beanName);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>this.scanner &#x3D; new ClassPathBeanDefinitionScanner(this); 调用链分析org.springframework.context.annotation.ClassPathBeanDefinitionScanner</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Create a new &#123;<span class="doctag">@code</span> ClassPathBeanDefinitionScanner&#125; for the given bean factory.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> registry the &#123;<span class="doctag">@code</span> BeanFactory&#125; to load bean definitions into, in the form</span></span><br><span class="line"><span class="comment"> * of a &#123;<span class="doctag">@code</span> BeanDefinitionRegistry&#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>(registry, <span class="literal">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>(registry, useDefaultFilters, getOrCreateEnvironment(registry));</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line"><span class="comment">// 这里走到了if里面,是满足条件的</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> Environment <span class="title function_">getOrCreateEnvironment</span><span class="params">(BeanDefinitionRegistry registry)</span> &#123;</span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		<span class="keyword">if</span> (registry <span class="keyword">instanceof</span> EnvironmentCapable) &#123;</span><br><span class="line">			<span class="keyword">return</span> ((EnvironmentCapable) registry).getEnvironment();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">StandardEnvironment</span>();</span><br><span class="line">&#125;	</span><br><span class="line"></span><br><span class="line"><span class="comment">// </span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters,</span></span><br><span class="line"><span class="params">			Environment environment)</span> &#123;</span><br><span class="line"></span><br><span class="line">		<span class="built_in">this</span>(registry, useDefaultFilters, environment,</span><br><span class="line">				(registry <span class="keyword">instanceof</span> ResourceLoader ? (ResourceLoader) registry : <span class="literal">null</span>));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ClassPathBeanDefinitionScanner</span><span class="params">(BeanDefinitionRegistry registry, <span class="type">boolean</span> useDefaultFilters,</span></span><br><span class="line"><span class="params">			Environment environment, <span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line"></span><br><span class="line">		Assert.notNull(registry, <span class="string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);</span><br><span class="line">		<span class="built_in">this</span>.registry = registry;</span><br><span class="line">		<span class="comment">// 使用默认的 filters</span></span><br><span class="line">		<span class="keyword">if</span> (useDefaultFilters) &#123;</span><br><span class="line">            <span class="comment">// 注册默认的filters.</span></span><br><span class="line">			registerDefaultFilters();</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">//  这里主要调用父类的方法,然后对一些参数进行赋值</span></span><br><span class="line">		setEnvironment(environment);</span><br><span class="line">    <span class="comment">//  这个方法,也是走到父类中, 对 resourcePatternResolver / metadataReaderFactory / componentsIndex 进行调用相应的方法进行处理.</span></span><br><span class="line">		setResourceLoader(resourceLoader);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Set the &#123;<span class="doctag">@link</span> ResourceLoader&#125; to use for resource locations.</span></span><br><span class="line"><span class="comment">	 * This will typically be a &#123;<span class="doctag">@link</span> ResourcePatternResolver&#125; implementation.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;Default is a &#123;<span class="doctag">@code</span> PathMatchingResourcePatternResolver&#125;, also capable of</span></span><br><span class="line"><span class="comment">	 * resource pattern resolving through the &#123;<span class="doctag">@code</span> ResourcePatternResolver&#125; interface.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> org.springframework.core.io.support.ResourcePatternResolver</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> org.springframework.core.io.support.PathMatchingResourcePatternResolver</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setResourceLoader</span><span class="params">(<span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.resourcePatternResolver = ResourcePatternUtils.getResourcePatternResolver(resourceLoader);</span><br><span class="line">		<span class="built_in">this</span>.metadataReaderFactory = <span class="keyword">new</span> <span class="title class_">CachingMetadataReaderFactory</span>(resourceLoader);</span><br><span class="line">		<span class="built_in">this</span>.componentsIndex = CandidateComponentsIndexLoader.loadIndex(<span class="built_in">this</span>.resourcePatternResolver.getClassLoader());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里对类型进行判断,如果是满条件的话就进行进行转化,如果不满足并且不是Null,就是进入到 else if 中,最后都不满足的话,就会走入到else中</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> ResourcePatternResolver <span class="title function_">getResourcePatternResolver</span><span class="params">(<span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (resourceLoader <span class="keyword">instanceof</span> ResourcePatternResolver) &#123;</span><br><span class="line">			<span class="keyword">return</span> (ResourcePatternResolver) resourceLoader;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (resourceLoader != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">PathMatchingResourcePatternResolver</span>(resourceLoader);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">PathMatchingResourcePatternResolver</span>();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从这个类的名字上来,给我饿感觉就是做缓存的,然后缓存的应该是metadata reader的,就是读取出来的metadata数据</span></span><br><span class="line">org.springframework.core.type.classreading.CachingMetadataReaderFactory</span><br><span class="line">    </span><br><span class="line">    	<span class="keyword">public</span> <span class="title function_">CachingMetadataReaderFactory</span><span class="params">(<span class="meta">@Nullable</span> ResourceLoader resourceLoader)</span> &#123;</span><br><span class="line">		<span class="built_in">super</span>(resourceLoader);</span><br><span class="line">    <span class="comment">// 如果resouceLoader是 DefaultResourceLoader,满祝条件，调用DefaultResourceLoader 的 getResourceCache 方法,返回回来的是空的集合.</span></span><br><span class="line">		<span class="keyword">if</span> (resourceLoader <span class="keyword">instanceof</span> DefaultResourceLoader) &#123;</span><br><span class="line">			<span class="built_in">this</span>.metadataReaderCache =</span><br><span class="line">					((DefaultResourceLoader) resourceLoader).getResourceCache(MetadataReader.class);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			setCacheLimit(DEFAULT_CACHE_LIMIT);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="register-方法"><a href="#register-方法" class="headerlink" title="register 方法"></a><strong>register 方法</strong></h2><p>走完了 this() 方法就会走register方法, 从名字上可以看出意思,注册。那么 这个地方到底是注册什么呢？ 跟着代码继续往下看,到底是在读取些什么</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">register</span><span class="params">(Class&lt;?&gt;... annotatedClasses)</span> &#123;</span><br><span class="line">   Assert.notEmpty(annotatedClasses, <span class="string">&quot;At least one annotated class must be specified&quot;</span>);</span><br><span class="line">   <span class="built_in">this</span>.reader.register(annotatedClasses);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">org.springframework.context.annotation.AnnotatedBeanDefinitionReader</span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Register one or more annotated classes to be processed.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;Calls to &#123;<span class="doctag">@code</span> register&#125; are idempotent; adding the same</span></span><br><span class="line"><span class="comment">	 * annotated class more than once has no additional effect.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> annotatedClasses one or more annotated classes,</span></span><br><span class="line"><span class="comment">	 * e.g. &#123;<span class="doctag">@link</span> Configuration <span class="doctag">@Configuration</span>&#125; classes</span></span><br><span class="line"><span class="comment">	 处理一个或者耕读的注解类。</span></span><br><span class="line"><span class="comment">	 这里我们注册的是  YangConfig这个类.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">register</span><span class="params">(Class&lt;?&gt;... annotatedClasses)</span> &#123;</span><br><span class="line">		<span class="keyword">for</span> (Class&lt;?&gt; annotatedClass : annotatedClasses) &#123;</span><br><span class="line">			<span class="comment">// 登记Bean</span></span><br><span class="line">			registerBean(annotatedClass);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">     <span class="comment">// 往下继续走到 doRegisterBean 方法</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerBean</span><span class="params">(Class&lt;?&gt; annotatedClass)</span> &#123;</span><br><span class="line">		doRegisterBean(annotatedClass, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Register a bean from the given bean class, deriving its metadata from</span></span><br><span class="line"><span class="comment">	 * class-declared annotations.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> annotatedClass the class of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> instanceSupplier a callback for creating an instance of the bean</span></span><br><span class="line"><span class="comment">	 * (may be &#123;<span class="doctag">@code</span> null&#125;)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> name an explicit name for the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> qualifiers specific qualifier annotations to consider, if any,</span></span><br><span class="line"><span class="comment">	 * in addition to qualifiers at the bean class level</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> definitionCustomizers one or more callbacks for customizing the</span></span><br><span class="line"><span class="comment">	 * factory&#x27;s &#123;<span class="doctag">@link</span> BeanDefinition&#125;, e.g. setting a lazy-init or primary flag</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@since</span> 5.0</span></span><br><span class="line"><span class="comment">	 从给定的Bean类注册Bean,从类声明的注释派生它的元数据.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	&lt;T&gt; <span class="keyword">void</span> <span class="title function_">doRegisterBean</span><span class="params">(Class&lt;T&gt; annotatedClass, <span class="meta">@Nullable</span> Supplier&lt;T&gt; instanceSupplier, <span class="meta">@Nullable</span> String name,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> Class&lt;? extends Annotation&gt;[] qualifiers, BeanDefinitionCustomizer... definitionCustomizers)</span> &#123;</span><br><span class="line"></span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		*  根据传入进来的class来生成一个 BeanDefinition.</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="type">AnnotatedGenericBeanDefinition</span> <span class="variable">abd</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AnnotatedGenericBeanDefinition</span>(annotatedClass);</span><br><span class="line">		System.out.println(<span class="string">&quot;annotatedClass ---&gt; &quot;</span> + annotatedClass);</span><br><span class="line">		<span class="comment">// System.out.println(&quot;abd.getMetadata()  --&gt; &quot; + abd.getMetadata());</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">// 先是获取 bd 的元数据,然后走到 org.springframework.context.annotation.ConditionEvaluator中,来判断是不是要跳过,这里返回的是false,也就是说不跳过得意思.</span></span><br><span class="line">		<span class="type">boolean</span> <span class="variable">isCondition</span> <span class="operator">=</span> <span class="built_in">this</span>.conditionEvaluator.shouldSkip(abd.getMetadata());</span><br><span class="line">		System.out.println(<span class="string">&quot;isCondition ---&gt; &quot;</span> + isCondition);</span><br><span class="line">		<span class="keyword">if</span> (isCondition) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 给bd这是属性,不过这里是null.</span></span><br><span class="line">		abd.setInstanceSupplier(instanceSupplier);</span><br><span class="line">        <span class="comment">// org.springframework.context.annotation.AnnotationScopeMetadataResolver</span></span><br><span class="line">        <span class="comment">//  这里返回的ScopeMeatdata里面有 scopeName的值是singleton,scopedProxyMode中返回的name:NO , ordinal : 1   等信息.</span></span><br><span class="line">		<span class="type">ScopeMetadata</span> <span class="variable">scopeMetadata</span> <span class="operator">=</span> <span class="built_in">this</span>.scopeMetadataResolver.resolveScopeMetadata(abd);</span><br><span class="line">        <span class="comment">// bd 设置scope是singleton</span></span><br><span class="line">		abd.setScope(scopeMetadata.getScopeName());</span><br><span class="line"></span><br><span class="line">		<span class="comment">//  传递进来的 YangConfig 判断是否需要类名 转化为首字符小写</span></span><br><span class="line">		<span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> (name != <span class="literal">null</span> ? name : <span class="built_in">this</span>.beanNameGenerator.generateBeanName(abd, <span class="built_in">this</span>.registry));</span><br><span class="line">		<span class="comment">// System.out.println(&quot;name ---&gt; &quot; + name);</span></span><br><span class="line">		<span class="comment">// System.out.println(&quot;beanName ---&gt; &quot; + beanName);</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		 * 处理通用的注解</span></span><br><span class="line"><span class="comment">		 *  Lazy , DependOn , Primary Role等注解</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		AnnotationConfigUtils.processCommonDefinitionAnnotations(abd);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 单个是传递空的</span></span><br><span class="line">		<span class="keyword">if</span> (qualifiers != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">for</span> (Class&lt;? <span class="keyword">extends</span> <span class="title class_">Annotation</span>&gt; qualifier : qualifiers) &#123;</span><br><span class="line">				<span class="keyword">if</span> (Primary.class == qualifier) &#123;</span><br><span class="line">					abd.setPrimary(<span class="literal">true</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (Lazy.class == qualifier) &#123;</span><br><span class="line">					abd.setLazyInit(<span class="literal">true</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					abd.addQualifier(<span class="keyword">new</span> <span class="title class_">AutowireCandidateQualifier</span>(qualifier));</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这里也是没有迭代的,</span></span><br><span class="line">		<span class="keyword">for</span> (BeanDefinitionCustomizer customizer : definitionCustomizers) &#123;</span><br><span class="line">			customizer.customize(abd);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		 * 对象 BeanDefinitionHolder  可以理解为一个存放数据的结构  类似于 User 存放 userName age 等属性. 这里是将我们的 yangConfig 和 YangConfig对用的bd给传入进去,然后构造出一个 BeanDefinitionHolder来.</span></span><br><span class="line"><span class="comment">		 *</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="type">BeanDefinitionHolder</span> <span class="variable">definitionHolder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(abd, beanName);</span><br><span class="line"></span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		 * ScopedProxyMode  代理模型;  org.springframework.context.annotation.AnnotationConfigUtils 会走到这个类中来.</span></span><br><span class="line"><span class="comment">		 传入进入的是,metadata,bd也就是yangConfig,registry是AnnotationConfigApplicationContext这个.</span></span><br><span class="line"><span class="comment">		 *</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		definitionHolder = AnnotationConfigUtils.applyScopedProxyMode(scopeMetadata, definitionHolder, <span class="built_in">this</span>.registry);</span><br><span class="line"></span><br><span class="line">		<span class="comment">/**</span></span><br><span class="line"><span class="comment">		 *  上面的数据结构注册；对象注册给 registry</span></span><br><span class="line"><span class="comment">		 	这个方法就是将 beanDefinition(YangConfig)给添加到 BeanFactory中去.</span></span><br><span class="line"><span class="comment">		 	这里的将 bd给添加到 BeanFacotry中是在this()构造函数中有说明的,因为在构造函数里面,有五个也是这个一样的，使用的同样代码,走的逻辑都是一摸一样的.</span></span><br><span class="line"><span class="comment">		 	org.springframework.beans.factory.support.DefaultListableBeanFactory#registerBeanDefinition            还是会走到 DefaultListableBeanFactory 这个类中的 registerBeanDefinition 方法来.其实也就是根据beanName,bd 根据key是beanname,value是bd给添加到map集合中即可.</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		BeanDefinitionReaderUtils.registerBeanDefinition(definitionHolder, <span class="built_in">this</span>.registry);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">org.springframework.context.annotation.AnnotationScopeMetadataResolver</span><br><span class="line">    </span><br><span class="line">   </span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> ScopeMetadata <span class="title function_">resolveScopeMetadata</span><span class="params">(BeanDefinition definition)</span> &#123;</span><br><span class="line">        <span class="comment">// 先new一个需要返回的对象.</span></span><br><span class="line">		<span class="type">ScopeMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ScopeMetadata</span>();</span><br><span class="line">		<span class="keyword">if</span> (definition <span class="keyword">instanceof</span> AnnotatedBeanDefinition) &#123;</span><br><span class="line">            <span class="comment">// 满足条件,进行强转.</span></span><br><span class="line">			<span class="type">AnnotatedBeanDefinition</span> <span class="variable">annDef</span> <span class="operator">=</span> (AnnotatedBeanDefinition) definition;</span><br><span class="line">            <span class="comment">// AnnotationConfigUtils这个类,应该是判断这个类上的注解,如果有的话,就会返回封装的对应的AnnotationAttributes,由于这里返回的是null,所以就没有进入到if条件里面去. 于是就返回了一个new ScopeMetadata();</span></span><br><span class="line">			<span class="type">AnnotationAttributes</span> <span class="variable">attributes</span> <span class="operator">=</span> AnnotationConfigUtils.attributesFor(</span><br><span class="line">					annDef.getMetadata(), <span class="built_in">this</span>.scopeAnnotationType);</span><br><span class="line">			<span class="keyword">if</span> (attributes != <span class="literal">null</span>) &#123;</span><br><span class="line">				metadata.setScopeName(attributes.getString(<span class="string">&quot;value&quot;</span>));</span><br><span class="line">				<span class="type">ScopedProxyMode</span> <span class="variable">proxyMode</span> <span class="operator">=</span> attributes.getEnum(<span class="string">&quot;proxyMode&quot;</span>);</span><br><span class="line">				<span class="keyword">if</span> (proxyMode == ScopedProxyMode.DEFAULT) &#123;</span><br><span class="line">					proxyMode = <span class="built_in">this</span>.defaultProxyMode;</span><br><span class="line">				&#125;</span><br><span class="line">				metadata.setScopedProxyMode(proxyMode);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> metadata;</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">processCommonDefinitionAnnotations</span><span class="params">(AnnotatedBeanDefinition abd, AnnotatedTypeMetadata metadata)</span> &#123;</span><br><span class="line">        <span class="comment">//  根据bd的metadata来判断是不是又Lazy这个注解,这里返回的是null,也就是没有</span></span><br><span class="line">		<span class="type">AnnotationAttributes</span> <span class="variable">lazy</span> <span class="operator">=</span> attributesFor(metadata, Lazy.class);</span><br><span class="line">		<span class="keyword">if</span> (lazy != <span class="literal">null</span>) &#123;</span><br><span class="line">			abd.setLazyInit(lazy.getBoolean(<span class="string">&quot;value&quot;</span>));</span><br><span class="line">		&#125;</span><br><span class="line">        <span class="comment">// else if 中是相等,也就是没有走进去.</span></span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (abd.getMetadata() != metadata) &#123;</span><br><span class="line">			lazy = attributesFor(abd.getMetadata(), Lazy.class);</span><br><span class="line">			<span class="keyword">if</span> (lazy != <span class="literal">null</span>) &#123;</span><br><span class="line">				abd.setLazyInit(lazy.getBoolean(<span class="string">&quot;value&quot;</span>));</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 判断是不是有 Primary 这个注解,如果有的话,db对应的primary设置就设置为true.</span></span><br><span class="line">		<span class="keyword">if</span> (metadata.isAnnotated(Primary.class.getName())) &#123;</span><br><span class="line">			abd.setPrimary(<span class="literal">true</span>);</span><br><span class="line">		&#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 这里dependsOn也是null.</span></span><br><span class="line">		<span class="type">AnnotationAttributes</span> <span class="variable">dependsOn</span> <span class="operator">=</span> attributesFor(metadata, DependsOn.class);</span><br><span class="line">		<span class="keyword">if</span> (dependsOn != <span class="literal">null</span>) &#123;</span><br><span class="line">			abd.setDependsOn(dependsOn.getStringArray(<span class="string">&quot;value&quot;</span>));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// bd是 AbstractBeanDefinition的子类.</span></span><br><span class="line">		<span class="keyword">if</span> (abd <span class="keyword">instanceof</span> AbstractBeanDefinition) &#123;</span><br><span class="line">			<span class="type">AbstractBeanDefinition</span> <span class="variable">absBd</span> <span class="operator">=</span> (AbstractBeanDefinition) abd;</span><br><span class="line">            <span class="comment">// 这里对应的 Role 和 Description 注解也是没有的.</span></span><br><span class="line">			<span class="type">AnnotationAttributes</span> <span class="variable">role</span> <span class="operator">=</span> attributesFor(metadata, Role.class);</span><br><span class="line">			<span class="keyword">if</span> (role != <span class="literal">null</span>) &#123;</span><br><span class="line">				absBd.setRole(role.getNumber(<span class="string">&quot;value&quot;</span>).intValue());</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="type">AnnotationAttributes</span> <span class="variable">description</span> <span class="operator">=</span> attributesFor(metadata, Description.class);</span><br><span class="line">			<span class="keyword">if</span> (description != <span class="literal">null</span>) &#123;</span><br><span class="line">				absBd.setDescription(description.getString(<span class="string">&quot;value&quot;</span>));</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="refresh方法"><a href="#refresh方法" class="headerlink" title="refresh方法"></a>refresh方法</h2><p> refresh() 方法是最主要的方法，这里我们只是使用了 Spring, 到SpringBoot 的源码中, 还是有调用这个refresh 这个方法</p>
<p> 可以看到 refresh 方法里面还是走了蛮多的方法. 有一些方法是留给扩展的,比如 onRefresh() 这个方法,当你启动SpringBoot的话，这个方法就会走到去 new Tomcat的逻辑</p>
<p> sychronized 关键字，可以看到进入执行下面的代码，一次只容许一个线程进来操作</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException &#123;</span><br><span class="line">   <span class="keyword">synchronized</span> (<span class="built_in">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">      <span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">      prepareRefresh();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line">      <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">      prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">         <span class="comment">// 这个方法在当前版本的 Spring 是没有任何代码</span></span><br><span class="line">         postProcessBeanFactory(beanFactory);</span><br><span class="line">          </span><br><span class="line">         <span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">         invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">         registerBeanPostProcessors(beanFactory);</span><br><span class="line">         <span class="comment">// Initialize message source for this context.</span></span><br><span class="line">         initMessageSource();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">         initApplicationEventMulticaster();</span><br><span class="line">         <span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">         onRefresh();</span><br><span class="line">         <span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">         registerListeners();</span><br><span class="line">         <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">         finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">         finishRefresh();</span><br><span class="line">      &#125; <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">            logger.warn(<span class="string">&quot;Exception encountered during context initialization - &quot;</span> +</span><br><span class="line">                  <span class="string">&quot;cancelling refresh attempt: &quot;</span> + ex);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">         destroyBeans();</span><br><span class="line">         <span class="comment">// Reset &#x27;active&#x27; flag.</span></span><br><span class="line">         cancelRefresh(ex);</span><br><span class="line">         <span class="comment">// Propagate exception to caller.</span></span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">         <span class="comment">// Reset common introspection caches in Spring&#x27;s core, since we</span></span><br><span class="line">         <span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">         resetCommonCaches();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="prepareRefresh-方法"><a href="#prepareRefresh-方法" class="headerlink" title="prepareRefresh () 方法"></a>prepareRefresh () 方法</h3><p>org.springframework.context.support.AbstractApplicationContext</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Prepare this context for refreshing, setting its startup date and</span></span><br><span class="line"><span class="comment"> * active flag as well as performing any initialization of property sources.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Switch to active.</span></span><br><span class="line">   <span class="built_in">this</span>.startupDate = System.currentTimeMillis</span><br><span class="line">   <span class="comment">//  对closed设置false,active设置为true. closed 和 active 都是AtomicBoolean,这个是线程安全的.</span></span><br><span class="line">   <span class="built_in">this</span>.closed.set(<span class="literal">false</span>);</span><br><span class="line">   <span class="built_in">this</span>.active.set(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (logger.isInfoEnabled()) &#123;</span><br><span class="line">      logger.info(<span class="string">&quot;Refreshing &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize any placeholder property sources in the context environment.</span></span><br><span class="line">   <span class="comment">// 目前该方法没有调用;目前没有做任何事情. 留给子类去实现的方法.</span></span><br><span class="line">   initPropertySources();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Validate that all properties marked as required are resolvable:</span></span><br><span class="line">   <span class="comment">// see ConfigurablePropertyResolver#setRequiredProperties</span></span><br><span class="line">    <span class="comment">// 先走getEnvironment方法,返回的是this.environment,也就是StandardEnvironment,</span></span><br><span class="line">    <span class="comment">//  org.springframework.core.env.AbstractEnvironment里面的validateRequiredProperties方法,先new一个MissingRequiredPropertiesException,如果this.requeiredProperties中检验有问题的话,就会放入MissingRequiredPropertiesException中属性集合中,然后判断如果集合不是empty的话,就说明是有值的,然后就抛出异常来. 我们这里检验是没有问题的.</span></span><br><span class="line">   getEnvironment().validateRequiredProperties();</span><br><span class="line"></span><br><span class="line">   System.out.println(<span class="string">&quot;this.earlyApplicationListeners value is  - - - -&gt;&quot;</span> + <span class="built_in">this</span>.earlyApplicationListeners);</span><br><span class="line">   <span class="comment">// Store pre-refresh ApplicationListeners...</span></span><br><span class="line">    <span class="comment">// 如果earlyApplicationListeners是null的话,就初始化一个.</span></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.earlyApplicationListeners == <span class="literal">null</span>) &#123;</span><br><span class="line">       <span class="comment">// 这里的 applicationListeners 也是空集合.</span></span><br><span class="line">      <span class="built_in">this</span>.earlyApplicationListeners = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="built_in">this</span>.applicationListeners);</span><br><span class="line">   &#125;  <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Reset local application listeners to pre-refresh state.</span></span><br><span class="line">       <span class="comment">// 如果不为空集合的话,就先清空,然后将 earlyApplicationListeners添加进去.</span></span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.clear();</span><br><span class="line">      <span class="built_in">this</span>.applicationListeners.addAll(<span class="built_in">this</span>.earlyApplicationListeners);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for the collection of early ApplicationEvents,</span></span><br><span class="line">   <span class="comment">// to be published once the multicaster is available...</span></span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 prepareRefresh 显示对closed和active参数值设置,然后检验一下配置参数,然后是有问题的话,就会抛出异常来。 然后对 earlyApplicationListeners进行判断或者初始化,初始化 earlyApplicationEvents 集合</p>
<h3 id="obtainFreshBeanFactory-方法"><a href="#obtainFreshBeanFactory-方法" class="headerlink" title="obtainFreshBeanFactory() 方法"></a>obtainFreshBeanFactory() 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> the fresh BeanFactory instance</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #refreshBeanFactory()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #getBeanFactory()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> ConfigurableListableBeanFactory <span class="title function_">obtainFreshBeanFactory</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// org.springframework.context.support.GenericApplicationContext</span></span><br><span class="line">   refreshBeanFactory();</span><br><span class="line">    <span class="comment">// org.springframework.context.support.GenericApplicationContext,</span></span><br><span class="line">    <span class="comment">// 这里是走的GenericApplicationContext 返回的beanFactory是 DefaultListableBeanFactory,</span></span><br><span class="line">    <span class="comment">// 然后将 DefaultListableBeanFactory给返回去.</span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line">   <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">      logger.debug(<span class="string">&quot;Bean factory for &quot;</span> + getDisplayName() + <span class="string">&quot;: &quot;</span> + beanFactory);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> beanFactory;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里有一个 compareAndSet --&gt; CAS,主要是防止没有被更新,如果有的话,就说明有个线程在同时操作,这个地方就抛出异常来</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">refreshBeanFactory</span><span class="params">()</span> <span class="keyword">throws</span> IllegalStateException &#123;</span><br><span class="line">		<span class="keyword">if</span> (!<span class="built_in">this</span>.refreshed.compareAndSet(<span class="literal">false</span>, <span class="literal">true</span>)) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(</span><br><span class="line">					<span class="string">&quot;GenericApplicationContext does not support multiple refresh attempts: just call &#x27;refresh&#x27; once&quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    setSerializationId 方法走到了 DefaultListableBeanFactory里面,也就是将获取出来的getId()给赋值给serializationId这个属性，还有一个存放在Map里面的逻辑.private static final Map&lt;String, Reference&lt;DefaultListableBeanFactory&gt;&gt; = new ConcurrentHashMap&lt;&gt;(8); </span></span><br><span class="line"><span class="comment">    org.springframework.beans.factory.support.DefaultListableBeanFactory</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">		<span class="built_in">this</span>.beanFactory.setSerializationId(getId());</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="prepareBeanFactory-beanFactory-方法"><a href="#prepareBeanFactory-beanFactory-方法" class="headerlink" title="prepareBeanFactory(beanFactory) 方法"></a>prepareBeanFactory(beanFactory) 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Configure the factory&#x27;s standard context characteristics,</span></span><br><span class="line"><span class="comment"> * such as the context&#x27;s ClassLoader and post-processors.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the BeanFactory to configure</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">prepareBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Tell the internal bean factory to use the context&#x27;s class loader etc.</span></span><br><span class="line">   <span class="comment">//  然后将返回的classLoader 赋值给 this.beanClassLoader.</span></span><br><span class="line">   beanFactory.setBeanClassLoader(getClassLoader());</span><br><span class="line">    <span class="comment">// beanFactory.getBeanClassLoader() 这个应该是刚刚上面set进去的方法,然后new 一个StandardBeanExpressionResolver 对象,  org.springframework.expression.spel.SpelParserConfiguration#SpelParserConfiguration(org.springframework.expression.spel.SpelCompilerMode, java.lang.ClassLoader, boolean, boolean, int) 里面都是参数的设置. this.beanExpressionResolver = resolver;最后new出来的StandardBeanExpressionResolve赋值给beanExpressionResolver.</span></span><br><span class="line">   beanFactory.setBeanExpressionResolver(<span class="keyword">new</span> <span class="title class_">StandardBeanExpressionResolver</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   </span><br><span class="line">    <span class="comment">// org.springframework.beans.support.ResourceEditorRegistrar#ResourceEditorRegistrar</span></span><br><span class="line">    <span class="comment">// 先是get出来的Environmentde,然后走到ResourceEditorRegistrar#ResourceEditorRegistrar,</span></span><br><span class="line">    <span class="comment">// private final Set&lt;PropertyEditorRegistrar&gt; propertyEditorRegistrars = new LinkedHashSet&lt;&gt;(4); 然后addPropertyEditorRegistrar方法也是往 propertyEditorRegistrars里面添加</span></span><br><span class="line">   beanFactory.addPropertyEditorRegistrar(<span class="keyword">new</span> <span class="title class_">ResourceEditorRegistrar</span>(<span class="built_in">this</span>, getEnvironment()));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Configure the bean factory with context callbacks.</span></span><br><span class="line"><span class="comment">//org.springframework.context.support.ApplicationContextAwareProcessor#ApplicationContextAwareProcessor,这里的this就是AnnotationConfigApplicationContext,然后传入到构造函数中,</span></span><br><span class="line"><span class="comment">//addBeanPostProcessor添加的ApplicationContextAwareProcessor这个,</span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationContextAwareProcessor</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// private final Set&lt;Class&lt;?&gt;&gt; ignoredDependencyInterfaces = new HashSet&lt;&gt;();</span></span><br><span class="line">  <span class="comment">//   ignoreDependencyInterface 都是往这个集合中添加这些类,也就是添加了  EnvironmentAware / EmbeddedValueResolverAware / ResourceLoaderAware / ApplicationEventPublisherAware/ MessageSourceAware/ ApplicationContextAware 等这些类.</span></span><br><span class="line">   beanFactory.ignoreDependencyInterface(EnvironmentAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(EmbeddedValueResolverAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ResourceLoaderAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationEventPublisherAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(MessageSourceAware.class);</span><br><span class="line">   beanFactory.ignoreDependencyInterface(ApplicationContextAware.class);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// BeanFactory interface not registered as resolvable type in a plain factory.</span></span><br><span class="line">   <span class="comment">// MessageSource registered (and found for autowiring) as a bean.</span></span><br><span class="line"><span class="comment">// registerResolvableDependency方法,对传入进来的第一个参数进行检验,不能为null.</span></span><br><span class="line"><span class="comment">// private final Map&lt;Class&lt;?&gt;, Object&gt; resolvableDependencies = new ConcurrentHashMap&lt;&gt;(16);</span></span><br><span class="line"><span class="comment">// 然后根据BeanFactory.class为key,beanFactory为value,去放入到resolvableDependencies(Map)中</span></span><br><span class="line">   beanFactory.registerResolvableDependency(BeanFactory.class, beanFactory);</span><br><span class="line">   beanFactory.registerResolvableDependency(ResourceLoader.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationEventPublisher.class, <span class="built_in">this</span>);</span><br><span class="line">   beanFactory.registerResolvableDependency(ApplicationContext.class, <span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register early post-processor for detecting inner beans as ApplicationListeners.</span></span><br><span class="line"><span class="comment">// new ApplicationListenerDetector(),也是传入进去的 AnnotationConfigApplicationContext这个,然后this.application = application, 赋值给 ApplicationListenerDetector 中的application参数.</span></span><br><span class="line"><span class="comment">// addBeanPostProcessor 下面也是有对这个方法进行阅读的. 但是这个参数满足 DestructionAwareBeanPostProcessor 条件,于是就有了 this.hasDestructionAwareBeanPostProcessors这个参数是true的. 最后也是添加到 beanPostProcessors 这个集合中    </span></span><br><span class="line">   beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found.</span></span><br><span class="line">    <span class="comment">// 不满足条件,没有进入来</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      <span class="comment">// Set a temporary ClassLoader for type matching.</span></span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register default environment beans.</span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">       <span class="comment">// 这里将获取出来的 Environment对象,作为单例给注册到beanFactory中去,目前也是添加到集合中</span></span><br><span class="line">       <span class="comment">// 这里没有放入beanDefinitionMap中,而是添加到了manualSingletonName这个集合中</span></span><br><span class="line">      beanFactory.registerSingleton(ENVIRONMENT_BEAN_NAME, getEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_PROPERTIES_BEAN_NAME)) &#123;</span><br><span class="line">       <span class="comment">// 这里与 Register default environment beans. 逻辑是一样的</span></span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_PROPERTIES_BEAN_NAME, getEnvironment().getSystemProperties());</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.containsLocalBean(SYSTEM_ENVIRONMENT_BEAN_NAME)) &#123;</span><br><span class="line">       <span class="comment">// 这里与上面的逻辑也是一样的</span></span><br><span class="line">      beanFactory.registerSingleton(SYSTEM_ENVIRONMENT_BEAN_NAME, getEnvironment().getSystemEnvironment());</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  this.resourceLoader是null, customClassLoader 是false.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="keyword">public</span> ClassLoader <span class="title function_">getClassLoader</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">this</span>.resourceLoader != <span class="literal">null</span> &amp;&amp; !<span class="built_in">this</span>.customClassLoader) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="built_in">this</span>.resourceLoader.getClassLoader();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="built_in">super</span>.getClassLoader();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Create a new ApplicationContextAwareProcessor for the given context.</span></span><br><span class="line"><span class="comment">  传入进来的 applicationContext 赋值给 this.applicationContext,</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ApplicationContextAwareProcessor</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.applicationContext = applicationContext;</span><br><span class="line">		<span class="built_in">this</span>.embeddedValueResolver = <span class="keyword">new</span> <span class="title class_">EmbeddedValueResolver</span>(applicationContext.getBeanFactory());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.config.EmbeddedValueResolver#EmbeddedValueResolver,</span></span><br><span class="line"><span class="comment">//这里继续往下去new 对象,</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">EmbeddedValueResolver</span><span class="params">(ConfigurableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">	<span class="built_in">this</span>.exprContext = <span class="keyword">new</span> <span class="title class_">BeanExpressionContext</span>(beanFactory, <span class="literal">null</span>);</span><br><span class="line">	<span class="built_in">this</span>.exprResolver = beanFactory.getBeanExpressionResolver();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractBeanFactory</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addBeanPostProcessor</span><span class="params">(BeanPostProcessor beanPostProcessor)</span> &#123;</span><br><span class="line">        <span class="comment">// 显示对传入进来的 beanPostProcessor进行不为null的判断.</span></span><br><span class="line">		Assert.notNull(beanPostProcessor, <span class="string">&quot;BeanPostProcessor must not be null&quot;</span>);</span><br><span class="line">		<span class="comment">// Remove from old position, if any</span></span><br><span class="line">        <span class="comment">// private final List&lt;BeanPostProcessor&gt; beanPostProcessors = new CopyOnWriteArrayList&lt;&gt;();</span></span><br><span class="line">        <span class="comment">// 从beanPostProcessors中移除.</span></span><br><span class="line">		<span class="built_in">this</span>.beanPostProcessors.remove(beanPostProcessor);</span><br><span class="line">		<span class="comment">// Track whether it is instantiation/destruction aware</span></span><br><span class="line">        <span class="comment">// 这里由于传入进来的不满足条件,因此下面的二个都不会走.</span></span><br><span class="line">        <span class="comment">// 如果是 InstantiationAwareBeanPostProcessor,对应的参数就会设置为ture</span></span><br><span class="line">		<span class="keyword">if</span> (beanPostProcessor <span class="keyword">instanceof</span> InstantiationAwareBeanPostProcessor) &#123;</span><br><span class="line">			<span class="built_in">this</span>.hasInstantiationAwareBeanPostProcessors = <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (beanPostProcessor <span class="keyword">instanceof</span> DestructionAwareBeanPostProcessor) &#123;</span><br><span class="line">			<span class="built_in">this</span>.hasDestructionAwareBeanPostProcessors = <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// Add to end of list</span></span><br><span class="line">        <span class="comment">// 然后又往 beanPostProcessors中添加传入进来的beanPostPorcessor.</span></span><br><span class="line">		<span class="built_in">this</span>.beanPostProcessors.add(beanPostProcessor);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultListableBeanFactory#registerSingleton</span></span><br><span class="line"><span class="comment">// 这个方法在上面也是进行阅读的,就是在this()方法里面,对一些代码的饿</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerSingleton</span><span class="params">(String beanName, Object singletonObject)</span> <span class="keyword">throws</span> IllegalStateException &#123;</span><br><span class="line">		<span class="built_in">super</span>.registerSingleton(beanName, singletonObject);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (hasBeanCreationStarted()) &#123;</span><br><span class="line">			<span class="comment">// Cannot modify startup-time collection elements anymore (for stable iteration)</span></span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="built_in">this</span>.beanDefinitionMap) &#123;</span><br><span class="line">				<span class="keyword">if</span> (!<span class="built_in">this</span>.beanDefinitionMap.containsKey(beanName)) &#123;</span><br><span class="line">					Set&lt;String&gt; updatedSingletons = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(<span class="built_in">this</span>.manualSingletonNames.size() + <span class="number">1</span>);</span><br><span class="line">					updatedSingletons.addAll(<span class="built_in">this</span>.manualSingletonNames);</span><br><span class="line">					updatedSingletons.add(beanName);</span><br><span class="line">					<span class="built_in">this</span>.manualSingletonNames = updatedSingletons;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// Still in startup registration phase</span></span><br><span class="line">			<span class="keyword">if</span> (!<span class="built_in">this</span>.beanDefinitionMap.containsKey(beanName)) &#123;</span><br><span class="line">				<span class="built_in">this</span>.manualSingletonNames.add(beanName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		clearByTypeCache();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="postProcessBeanFactory-beanFactory-方法"><a href="#postProcessBeanFactory-beanFactory-方法" class="headerlink" title="postProcessBeanFactory(beanFactory)方法"></a>postProcessBeanFactory(beanFactory)方法</h3><p>&#x2F;&#x2F; 可以看到这个方法,是走到下面的方法,是没有做任何代码. 应该是留到子类进行实现走自己的逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Modify the application context&#x27;s internal bean factory after its standard</span></span><br><span class="line"><span class="comment"> * initialization. All bean definitions will have been loaded, but no beans</span></span><br><span class="line"><span class="comment"> * will have been instantiated yet. This allows for registering special</span></span><br><span class="line"><span class="comment"> * BeanPostProcessors etc in certain ApplicationContext implementations.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanFactory the bean factory used by the application context</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">postProcessBeanFactory</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="invokeBeanFactoryPostProcessors-beanFactory-方法"><a href="#invokeBeanFactoryPostProcessors-beanFactory-方法" class="headerlink" title="invokeBeanFactoryPostProcessors(beanFactory) 方法"></a>invokeBeanFactoryPostProcessors(beanFactory) 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Instantiate and invoke all registered BeanFactoryPostProcessor beans,</span></span><br><span class="line"><span class="comment"> * respecting explicit order if given.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Must be called before singleton instantiation.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//  getBeanFactoryPostProcessors()  这个方法直接获取一个list;这个list在 AnnotationConfigApplicationContext 定义的. 这里的getBeanFactoryPostProcessors()直接写在invokeBeanFactoryPostProcessors这个里面,编译了源码,于是就抽取出来了.</span></span><br><span class="line">    <span class="comment">// 这里目前抽取出来的集合是空集合,没有值的.</span></span><br><span class="line">   List&lt;BeanFactoryPostProcessor&gt; postProcessorsList = getBeanFactoryPostProcessors();</span><br><span class="line">   <span class="comment">//System.out.println(&quot;postProcessorsList value ---&gt; &quot; + postProcessorsList);</span></span><br><span class="line">   <span class="comment">// System.out.println(&quot;beanFactory  value 111111 ---&gt; &quot; + beanFactory);</span></span><br><span class="line"></span><br><span class="line">   <span class="comment">//  自定义就是自己写的</span></span><br><span class="line">   PostProcessorRegistrationDelegate.invokeBeanFactoryPostProcessors(beanFactory, postProcessorsList);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Detect a LoadTimeWeaver and prepare for weaving, if found in the meantime</span></span><br><span class="line">   <span class="comment">// (e.g. through an @Bean method registered by ConfigurationClassPostProcessor)</span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.getTempClassLoader() == <span class="literal">null</span> &amp;&amp; beanFactory.containsBean(LOAD_TIME_WEAVER_BEAN_NAME)) &#123;</span><br><span class="line">      beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">LoadTimeWeaverAwareProcessor</span>(beanFactory));</span><br><span class="line">      beanFactory.setTempClassLoader(<span class="keyword">new</span> <span class="title class_">ContextTypeMatchClassLoader</span>(beanFactory.getBeanClassLoader()));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// org.springframework.context.support.PostProcessorRegistrationDelegate,从这个方法的名字上看,调用 BeanFactroyPostProcessors. 传入进来的集合是空集合,beanFactoy是DefaultListableBeanFactory.</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">invokeBeanFactoryPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">			ConfigurableListableBeanFactory beanFactory, List&lt;BeanFactoryPostProcessor&gt; beanFactoryPostProcessors)</span> &#123;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Invoke BeanDefinitionRegistryPostProcessors first, if any.</span></span><br><span class="line">		Set&lt;String&gt; processedBeans = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line">		</span><br><span class="line">        <span class="comment">// 这里是满足条件的.</span></span><br><span class="line">		<span class="keyword">if</span> (beanFactory <span class="keyword">instanceof</span> BeanDefinitionRegistry) &#123;</span><br><span class="line">			<span class="comment">/**</span></span><br><span class="line"><span class="comment">			 * 如果beanFactory 是 BeanDefinitionRegistry;强转</span></span><br><span class="line"><span class="comment">			 */</span></span><br><span class="line">			<span class="type">BeanDefinitionRegistry</span> <span class="variable">registry</span> <span class="operator">=</span> (BeanDefinitionRegistry) beanFactory;</span><br><span class="line">			List&lt;BeanFactoryPostProcessor&gt; regularPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">			List&lt;BeanDefinitionRegistryPostProcessor&gt; registryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">			</span><br><span class="line">            <span class="comment">// 由于传入进来的集合是空集合,所以这里也就是上面可以迭代的.</span></span><br><span class="line">			<span class="keyword">for</span> (BeanFactoryPostProcessor postProcessor : beanFactoryPostProcessors) &#123;</span><br><span class="line">				<span class="comment">// 如果是 BeanDefinitionRegistryPostProcessor</span></span><br><span class="line">				<span class="keyword">if</span> (postProcessor <span class="keyword">instanceof</span> BeanDefinitionRegistryPostProcessor) &#123;</span><br><span class="line">					<span class="type">BeanDefinitionRegistryPostProcessor</span> <span class="variable">registryProcessor</span> <span class="operator">=</span></span><br><span class="line">							(BeanDefinitionRegistryPostProcessor) postProcessor;</span><br><span class="line">					<span class="comment">//  赋值进 beanFactory</span></span><br><span class="line">					registryProcessor.postProcessBeanDefinitionRegistry(registry);</span><br><span class="line">					<span class="comment">// 添加进入集合中</span></span><br><span class="line">					registryProcessors.add(registryProcessor);</span><br><span class="line">				&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="comment">//  否则就添加到宁外一个 registry的集合中</span></span><br><span class="line">					regularPostProcessors.add(postProcessor);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">			<span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line">			<span class="comment">// Separate between BeanDefinitionRegistryPostProcessors that implement</span></span><br><span class="line">			<span class="comment">// PriorityOrdered, Ordered, and the rest.</span></span><br><span class="line">			List&lt;BeanDefinitionRegistryPostProcessor&gt; currentRegistryProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">			<span class="comment">// First, invoke the BeanDefinitionRegistryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line"><span class="comment">// 这里调用beanFactory的getBeanNamesForType,获取出来的数组里面只有一个 org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line">			String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class,</span><br><span class="line">							<span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line"><span class="comment">// ppName : org.springframework.context.annotation.internalConfigurationAnnotationProcessor</span></span><br><span class="line"><span class="comment">//  beanFactory.isTypeMatch返回的是true,也就是代码继续往下走                </span></span><br><span class="line">				<span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractBeanFactory#doGetBean,这个方法是很重要的,                    </span></span><br><span class="line">					currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">					processedBeans.add(ppName);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">			registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line"></span><br><span class="line">			<span class="comment">//  这步执行完,BeanFactory 中的 beanDefinitionMap 中的个数 从 7 到 12. 说明Bean在这步是有执行操作的</span></span><br><span class="line">			invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">			currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Next, invoke the BeanDefinitionRegistryPostProcessors that implement Ordered.</span></span><br><span class="line">			postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">			<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">				<span class="keyword">if</span> (!processedBeans.contains(ppName) &amp;&amp; beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">					currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">					processedBeans.add(ppName);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">			registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">			invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">			currentRegistryProcessors.clear();</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Finally, invoke all other BeanDefinitionRegistryPostProcessors until no further ones appear.</span></span><br><span class="line">			<span class="type">boolean</span> <span class="variable">reiterate</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">			<span class="keyword">while</span> (reiterate) &#123;</span><br><span class="line">				reiterate = <span class="literal">false</span>;</span><br><span class="line">				postProcessorNames = beanFactory.getBeanNamesForType(BeanDefinitionRegistryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">				<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">					<span class="keyword">if</span> (!processedBeans.contains(ppName)) &#123;</span><br><span class="line">						currentRegistryProcessors.add(beanFactory.getBean(ppName, BeanDefinitionRegistryPostProcessor.class));</span><br><span class="line">						processedBeans.add(ppName);</span><br><span class="line">						reiterate = <span class="literal">true</span>;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">				sortPostProcessors(currentRegistryProcessors, beanFactory);</span><br><span class="line">				registryProcessors.addAll(currentRegistryProcessors);</span><br><span class="line">				invokeBeanDefinitionRegistryPostProcessors(currentRegistryProcessors, registry);</span><br><span class="line">				currentRegistryProcessors.clear();</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Now, invoke the postProcessBeanFactory callback of all processors handled so far.</span></span><br><span class="line">			<span class="comment">//  这步是有执行到的,可以 debug 进去看详细的执行流程操作</span></span><br><span class="line">			invokeBeanFactoryPostProcessors(registryProcessors, beanFactory);</span><br><span class="line">			invokeBeanFactoryPostProcessors(regularPostProcessors, beanFactory);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// Invoke factory processors registered with the context instance.</span></span><br><span class="line">			invokeBeanFactoryPostProcessors(beanFactoryPostProcessors, beanFactory);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">		<span class="comment">// uninitialized to let the bean factory post-processors apply to them!</span></span><br><span class="line">		String[] postProcessorNames =</span><br><span class="line">				beanFactory.getBeanNamesForType(BeanFactoryPostProcessor.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Separate between BeanFactoryPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">		<span class="comment">// Ordered, and the rest.</span></span><br><span class="line">		List&lt;BeanFactoryPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">			<span class="keyword">if</span> (processedBeans.contains(ppName)) &#123;</span><br><span class="line">				<span class="comment">// skip - already processed in first phase above</span></span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">				priorityOrderedPostProcessors.add(beanFactory.getBean(ppName, BeanFactoryPostProcessor.class));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">				orderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// First, invoke the BeanFactoryPostProcessors that implement PriorityOrdered.</span></span><br><span class="line">		sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line">		invokeBeanFactoryPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Next, invoke the BeanFactoryPostProcessors that implement Ordered.</span></span><br><span class="line">		List&lt;BeanFactoryPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String postProcessorName : orderedPostProcessorNames) &#123;</span><br><span class="line">			orderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">		&#125;</span><br><span class="line">		sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">		invokeBeanFactoryPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Finally, invoke all other BeanFactoryPostProcessors.</span></span><br><span class="line">		List&lt;BeanFactoryPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String postProcessorName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">			nonOrderedPostProcessors.add(beanFactory.getBean(postProcessorName, BeanFactoryPostProcessor.class));</span><br><span class="line">		&#125;</span><br><span class="line">		invokeBeanFactoryPostProcessors(nonOrderedPostProcessors, beanFactory);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Clear cached merged bean definitions since the post-processors might have</span></span><br><span class="line">		<span class="comment">// modified the original metadata, e.g. replacing placeholders in values...</span></span><br><span class="line">		beanFactory.clearMetadataCache();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-------------------</span><br><span class="line"><span class="comment">// org.springframework.core.ResolvableType</span></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Return a &#123;<span class="doctag">@link</span> ResolvableType&#125; for the specified &#123;<span class="doctag">@link</span> Class&#125;,</span></span><br><span class="line"><span class="comment">	 * doing assignability checks against the raw class only (analogous to</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@link</span> Class#isAssignableFrom&#125;, which this serves as a wrapper for.</span></span><br><span class="line"><span class="comment">	 * For example: &#123;<span class="doctag">@code</span> ResolvableType.forRawClass(List.class)&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> clazz the class to introspect (&#123;<span class="doctag">@code</span> null&#125; is semantically</span></span><br><span class="line"><span class="comment">	 * equivalent to &#123;<span class="doctag">@code</span> Object.class&#125; for typical use cases here)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a &#123;<span class="doctag">@link</span> ResolvableType&#125; for the specified class</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@since</span> 4.2</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #forClass(Class)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #getRawClass()</span></span><br><span class="line"><span class="comment">	 </span></span><br><span class="line"><span class="comment">	 clazz : org.springframework.core.PriorityOrdered</span></span><br><span class="line"><span class="comment">	 返回的对象 : clazz属性的值是 传入进来的clazz的值,这里可以debug进去.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> ResolvableType <span class="title function_">forRawClass</span><span class="params">(<span class="meta">@Nullable</span> Class&lt;?&gt; clazz)</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ResolvableType</span>(clazz) &#123;</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> ResolvableType[] getGenerics() &#123;</span><br><span class="line">				<span class="keyword">return</span> EMPTY_TYPES_ARRAY;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isAssignableFrom</span><span class="params">(Class&lt;?&gt; other)</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> (clazz == <span class="literal">null</span> || ClassUtils.isAssignable(clazz, other));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isAssignableFrom</span><span class="params">(ResolvableType other)</span> &#123;</span><br><span class="line">				Class&lt;?&gt; otherClass = other.resolve();</span><br><span class="line">				<span class="keyword">return</span> (otherClass != <span class="literal">null</span> &amp;&amp; (clazz == <span class="literal">null</span> || ClassUtils.isAssignable(clazz, otherClass)));</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;;</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line">--------------------</span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractBeanFactory</span></span><br><span class="line"><span class="comment">//    </span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isTypeMatch</span><span class="params">(String name, ResolvableType typeToMatch)</span> <span class="keyword">throws</span> NoSuchBeanDefinitionException &#123;</span><br><span class="line">    <span class="comment">// org.springframework.beans.factory.BeanFactoryUtils.transformedBeanName(),这个方法如果是以&amp;开头的话,然后就进行截取.</span></span><br><span class="line"><span class="comment">// org.springframework.core.SimpleAliasRegistry.canonicalName() 这个方法是判断是否有别名    </span></span><br><span class="line">		<span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> transformedBeanName(name);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check manually registered singletons.</span></span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.DefaultSingletonBeanRegistry.getSingleton()走的这个方法.这里返回的是beanInstance是null,</span></span><br><span class="line">		<span class="type">Object</span> <span class="variable">beanInstance</span> <span class="operator">=</span> getSingleton(beanName, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// 返回的null,于是就没有进入到if里面</span></span><br><span class="line">		<span class="keyword">if</span> (beanInstance != <span class="literal">null</span> &amp;&amp; beanInstance.getClass() != NullBean.class) &#123;</span><br><span class="line">			<span class="keyword">if</span> (beanInstance <span class="keyword">instanceof</span> FactoryBean) &#123;</span><br><span class="line">				<span class="keyword">if</span> (!BeanFactoryUtils.isFactoryDereference(name)) &#123;</span><br><span class="line">					Class&lt;?&gt; type = getTypeForFactoryBean((FactoryBean&lt;?&gt;) beanInstance);</span><br><span class="line">					<span class="keyword">return</span> (type != <span class="literal">null</span> &amp;&amp; typeToMatch.isAssignableFrom(type));</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">return</span> typeToMatch.isInstance(beanInstance);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (!BeanFactoryUtils.isFactoryDereference(name)) &#123;</span><br><span class="line">				<span class="keyword">if</span> (typeToMatch.isInstance(beanInstance)) &#123;</span><br><span class="line">					<span class="comment">// Direct match for exposed instance?</span></span><br><span class="line">					<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (typeToMatch.hasGenerics() &amp;&amp; containsBeanDefinition(beanName)) &#123;</span><br><span class="line">					<span class="comment">// Generics potentially only match on the target class, not on the proxy...</span></span><br><span class="line">					<span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line">					Class&lt;?&gt; targetType = mbd.getTargetType();</span><br><span class="line">					<span class="keyword">if</span> (targetType != <span class="literal">null</span> &amp;&amp; targetType != ClassUtils.getUserClass(beanInstance)) &#123;</span><br><span class="line">						<span class="comment">// Check raw class match as well, making sure it&#x27;s exposed on the proxy.</span></span><br><span class="line">						Class&lt;?&gt; classToMatch = typeToMatch.resolve();</span><br><span class="line">						<span class="keyword">if</span> (classToMatch != <span class="literal">null</span> &amp;&amp; !classToMatch.isInstance(beanInstance)) &#123;</span><br><span class="line">							<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="keyword">if</span> (typeToMatch.isAssignableFrom(targetType)) &#123;</span><br><span class="line">							<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="type">ResolvableType</span> <span class="variable">resolvableType</span> <span class="operator">=</span> mbd.targetType;</span><br><span class="line">					<span class="keyword">if</span> (resolvableType == <span class="literal">null</span>) &#123;</span><br><span class="line">						resolvableType = mbd.factoryMethodReturnType;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">return</span> (resolvableType != <span class="literal">null</span> &amp;&amp; typeToMatch.isAssignableFrom(resolvableType));</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 上面返回的null,else if的条件也不满足. </span></span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (containsSingleton(beanName) &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">			<span class="comment">// null instance registered</span></span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// No singleton instance found -&gt; check bean definition.</span></span><br><span class="line">    <span class="comment">// 这里返回的是null,所以下面的if方法也不会进去.</span></span><br><span class="line">		<span class="type">BeanFactory</span> <span class="variable">parentBeanFactory</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">		<span class="keyword">if</span> (parentBeanFactory != <span class="literal">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">			<span class="comment">// No bean definition found in this factory -&gt; delegate to parent.</span></span><br><span class="line">			<span class="keyword">return</span> parentBeanFactory.isTypeMatch(originalBeanName(name), typeToMatch);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Retrieve corresponding bean definition.</span></span><br><span class="line">    <span class="comment">//getMergedLocalBeanDefinition方法返回的mbd,beanClass:org.springframework.context.annotation.ConfigurationClassPostProcessor</span></span><br><span class="line">		<span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line"></span><br><span class="line">		Class&lt;?&gt; classToMatch = typeToMatch.resolve();</span><br><span class="line">		<span class="keyword">if</span> (classToMatch == <span class="literal">null</span>) &#123;</span><br><span class="line">			classToMatch = FactoryBean.class;</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">// 这里的 FactoryBean.class == classToMatch 是false,所以这个数组中就有二个值,分别是Factory和PriorityOrdered.</span></span><br><span class="line">		Class&lt;?&gt;[] typesToMatch = (FactoryBean.class == classToMatch ?</span><br><span class="line">				<span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[] &#123;classToMatch&#125; : <span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[] &#123;FactoryBean.class, classToMatch&#125;);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check decorated bean definition, if any: We assume it&#x27;ll be easier</span></span><br><span class="line">		<span class="comment">// to determine the decorated bean&#x27;s type than the proxy&#x27;s type.</span></span><br><span class="line">        <span class="comment">// 这里的dbd返回的是Null,由于是Null所以下面的if条件也不会进入</span></span><br><span class="line">		<span class="type">BeanDefinitionHolder</span> <span class="variable">dbd</span> <span class="operator">=</span> mbd.getDecoratedDefinition();</span><br><span class="line">    </span><br><span class="line">		<span class="keyword">if</span> (dbd != <span class="literal">null</span> &amp;&amp; !BeanFactoryUtils.isFactoryDereference(name)) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">tbd</span> <span class="operator">=</span> getMergedBeanDefinition(dbd.getBeanName(), dbd.getBeanDefinition(), mbd);</span><br><span class="line">			Class&lt;?&gt; targetClass = predictBeanType(dbd.getBeanName(), tbd, typesToMatch);</span><br><span class="line">			<span class="keyword">if</span> (targetClass != <span class="literal">null</span> &amp;&amp; !FactoryBean.class.isAssignableFrom(targetClass)) &#123;</span><br><span class="line">				<span class="keyword">return</span> typeToMatch.isAssignableFrom(targetClass);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这里传入的 beanName,mdb,typesToMatch数组,都是在上面有提到的,是一步一步走下来的.这里返回的是class org.springframework.context.annotation.ConfigurationClassPostProcessor,所以下面的if是不会进入的</span></span><br><span class="line">		Class&lt;?&gt; beanType = predictBeanType(beanName, mbd, typesToMatch);</span><br><span class="line">		<span class="keyword">if</span> (beanType == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check bean class whether we&#x27;re dealing with a FactoryBean.</span></span><br><span class="line">    <span class="comment">// 是否在处理FactoryBean</span></span><br><span class="line">		<span class="keyword">if</span> (FactoryBean.class.isAssignableFrom(beanType)) &#123;</span><br><span class="line">			<span class="keyword">if</span> (!BeanFactoryUtils.isFactoryDereference(name) &amp;&amp; beanInstance == <span class="literal">null</span>) &#123;</span><br><span class="line">				<span class="comment">// If it&#x27;s a FactoryBean, we want to look at what it creates, not the factory class.</span></span><br><span class="line">				beanType = getTypeForFactoryBean(beanName, mbd);</span><br><span class="line">				<span class="keyword">if</span> (beanType == <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">// name 不是null,并且是&amp;开头的.这里是不满足条件的,所以就返回false,也不会进入到下面的代码去处理对应的逻辑.</span></span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (BeanFactoryUtils.isFactoryDereference(name)) &#123;</span><br><span class="line">			<span class="comment">// Special case: A SmartInstantiationAwareBeanPostProcessor returned a non-FactoryBean</span></span><br><span class="line">			<span class="comment">// type but we nevertheless are being asked to dereference a FactoryBean...</span></span><br><span class="line">			<span class="comment">// Let&#x27;s check the original bean class and proceed with it if it is a FactoryBean.</span></span><br><span class="line">			beanType = predictBeanType(beanName, mbd, FactoryBean.class);</span><br><span class="line">			<span class="keyword">if</span> (beanType == <span class="literal">null</span> || !FactoryBean.class.isAssignableFrom(beanType)) &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// resolvableType 这里的值是null</span></span><br><span class="line">		<span class="type">ResolvableType</span> <span class="variable">resolvableType</span> <span class="operator">=</span> mbd.targetType;</span><br><span class="line">		<span class="keyword">if</span> (resolvableType == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 这里还是返回的还是null</span></span><br><span class="line">			resolvableType = mbd.factoryMethodReturnType;</span><br><span class="line">		&#125;</span><br><span class="line">    <span class="comment">//  resolvableType的值是null,所以就不会进入到这里.</span></span><br><span class="line">		<span class="keyword">if</span> (resolvableType != <span class="literal">null</span> &amp;&amp; resolvableType.resolve() == beanType) &#123;</span><br><span class="line">			<span class="keyword">return</span> typeToMatch.isAssignableFrom(resolvableType);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// org.springframework.util.ClassUtils#isAssignable , 这里返回的true    </span></span><br><span class="line">		<span class="keyword">return</span> typeToMatch.isAssignableFrom(beanType);</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line">---------</span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory    </span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">protected</span> Class&lt;?&gt; predictBeanType(String beanName, RootBeanDefinition mbd, Class&lt;?&gt;... typesToMatch) &#123;</span><br><span class="line">		Class&lt;?&gt; targetType = determineTargetType(beanName, mbd, typesToMatch);</span><br><span class="line">		<span class="comment">// Apply SmartInstantiationAwareBeanPostProcessors to predict the</span></span><br><span class="line">		<span class="comment">// eventual type after a before-instantiation shortcut.</span></span><br><span class="line"><span class="comment">// mdb.isSynthetic()是否是综合的,这里返回的是false.</span></span><br><span class="line"><span class="comment">//hasInstantiationAwareBeanPostProcessors(),是否已经注册了任何实体化InstantiationAwareBeanPostProcessors,返回的是false,所以就不满足条件进入到if里面    </span></span><br><span class="line">		<span class="keyword">if</span> (targetType != <span class="literal">null</span> &amp;&amp; !mbd.isSynthetic() &amp;&amp; hasInstantiationAwareBeanPostProcessors()) &#123;</span><br><span class="line">			<span class="keyword">for</span> (BeanPostProcessor bp : getBeanPostProcessors()) &#123;</span><br><span class="line">				<span class="keyword">if</span> (bp <span class="keyword">instanceof</span> SmartInstantiationAwareBeanPostProcessor) &#123;</span><br><span class="line">					<span class="type">SmartInstantiationAwareBeanPostProcessor</span> <span class="variable">ibp</span> <span class="operator">=</span> (SmartInstantiationAwareBeanPostProcessor) bp;</span><br><span class="line">					Class&lt;?&gt; predicted = ibp.predictBeanType(targetType, beanName);</span><br><span class="line">					<span class="keyword">if</span> (predicted != <span class="literal">null</span> &amp;&amp; (typesToMatch.length != <span class="number">1</span> || FactoryBean.class != typesToMatch[<span class="number">0</span>] ||</span><br><span class="line">							FactoryBean.class.isAssignableFrom(predicted))) &#123;</span><br><span class="line">						<span class="keyword">return</span> predicted;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> targetType;</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Determine the target type for the given bean definition.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean (for error handling purposes)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> mbd the merged bean definition for the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> typesToMatch the types to match in case of internal type matching purposes</span></span><br><span class="line"><span class="comment">	 * (also signals that the returned &#123;<span class="doctag">@code</span> Class&#125; will never be exposed to application code)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> the type for the bean if determinable, or &#123;<span class="doctag">@code</span> null&#125; otherwise</span></span><br><span class="line"><span class="comment">	 </span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">protected</span> Class&lt;?&gt; determineTargetType(String beanName, RootBeanDefinition mbd, Class&lt;?&gt;... typesToMatch) &#123;</span><br><span class="line">        <span class="comment">// targetType的值是 org.springframework.context.annotation.ConfigurationClassPostProcessor,target不是null,于是就直接返回了.</span></span><br><span class="line">		Class&lt;?&gt; targetType = mbd.getTargetType();</span><br><span class="line">		<span class="keyword">if</span> (targetType == <span class="literal">null</span>) &#123;</span><br><span class="line">			targetType = (mbd.getFactoryMethodName() != <span class="literal">null</span> ?</span><br><span class="line">					getTypeForFactoryMethod(beanName, mbd, typesToMatch) :</span><br><span class="line">					resolveBeanClass(mbd, beanName, typesToMatch));</span><br><span class="line">			<span class="keyword">if</span> (ObjectUtils.isEmpty(typesToMatch) || getTempClassLoader() == <span class="literal">null</span>) &#123;</span><br><span class="line">				mbd.resolvedTargetType = targetType;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> targetType;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">----------------------------</span><br><span class="line">doGetBean 还是真正的获取Bean方法,这里我们继续往下看.    </span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Return an instance, which may be shared or independent, of the specified bean.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> name the name of the bean to retrieve</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> requiredType the required type of the bean to retrieve</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> args arguments to use when creating a bean instance using explicit arguments</span></span><br><span class="line"><span class="comment">	 * (only applied when creating a new instance as opposed to retrieving an existing one)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> typeCheckOnly whether the instance is obtained for a type check,</span></span><br><span class="line"><span class="comment">	 * not for actual use</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> an instance of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> BeansException if the bean could not be created</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">	<span class="keyword">protected</span> &lt;T&gt; T <span class="title function_">doGetBean</span><span class="params">(<span class="keyword">final</span> String name, <span class="meta">@Nullable</span> <span class="keyword">final</span> Class&lt;T&gt; requiredType,</span></span><br><span class="line"><span class="params">			<span class="meta">@Nullable</span> <span class="keyword">final</span> Object[] args, <span class="type">boolean</span> typeCheckOnly)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 这里是对beanName进行处理,判断是不是有别名</span></span><br><span class="line">		<span class="keyword">final</span> <span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> transformedBeanName(name);</span><br><span class="line">		Object bean;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Eagerly check singleton cache for manually registered singletons.</span></span><br><span class="line">    <span class="comment">// 这里返回的 sharedInstance是null.</span></span><br><span class="line">		<span class="type">Object</span> <span class="variable">sharedInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">		<span class="keyword">if</span> (sharedInstance != <span class="literal">null</span> &amp;&amp; args == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="comment">/*if (logger.isDebugEnabled()) &#123;</span></span><br><span class="line"><span class="comment">				if (isSingletonCurrentlyInCreation(beanName)) &#123;</span></span><br><span class="line"><span class="comment">					logger.debug(&quot;Returning eagerly cached instance of singleton bean &#x27;&quot; + beanName +</span></span><br><span class="line"><span class="comment">							&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;);</span></span><br><span class="line"><span class="comment">				&#125; else &#123;</span></span><br><span class="line"><span class="comment">					logger.debug(&quot;Returning cached instance of singleton bean &#x27;&quot; + beanName + &quot;&#x27;&quot;);</span></span><br><span class="line"><span class="comment">				&#125;</span></span><br><span class="line"><span class="comment">			&#125;*/</span></span><br><span class="line">			bean = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="literal">null</span>);</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// Fail if we&#x27;re already creating this bean instance:</span></span><br><span class="line">			<span class="comment">// We&#x27;re assumably within a circular reference.</span></span><br><span class="line">			<span class="keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Check if bean definition exists in this factory.</span></span><br><span class="line">            <span class="comment">// 这里获取出来的 parentBeanFactory 是null</span></span><br><span class="line">			<span class="type">BeanFactory</span> <span class="variable">parentBeanFactory</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">			<span class="keyword">if</span> (parentBeanFactory != <span class="literal">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">				<span class="comment">// Not found -&gt; check parent.</span></span><br><span class="line">				<span class="type">String</span> <span class="variable">nameToLookup</span> <span class="operator">=</span> originalBeanName(name);</span><br><span class="line">				<span class="keyword">if</span> (parentBeanFactory <span class="keyword">instanceof</span> AbstractBeanFactory) &#123;</span><br><span class="line">					<span class="keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(</span><br><span class="line">							nameToLookup, requiredType, args, typeCheckOnly);</span><br><span class="line">				&#125; <span class="keyword">else</span> <span class="keyword">if</span> (args != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="comment">// Delegation to parent with explicit args.</span></span><br><span class="line">					<span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);</span><br><span class="line">				&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="comment">// No args -&gt; delegate to standard getBean method.</span></span><br><span class="line">					<span class="keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// typeCheckOnly 是false,也就进入了if里面</span></span><br><span class="line">			<span class="keyword">if</span> (!typeCheckOnly) &#123;</span><br><span class="line">                <span class="comment">// 标记一个标记,后面应该是有用到.</span></span><br><span class="line">				markBeanAsCreated(beanName);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="keyword">final</span> <span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line">                <span class="comment">// 检查mdb,也就是判断mbd是否是抽象类,如果是抽象类的话,就会抛出异常.	</span></span><br><span class="line">				checkMergedBeanDefinition(mbd, beanName, args);</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Guarantee initialization of beans that the current bean depends on.</span></span><br><span class="line">                <span class="comment">// mbd获取的dependsOn,这里是没有这个注解的.</span></span><br><span class="line">				String[] dependsOn = mbd.getDependsOn();</span><br><span class="line">				<span class="keyword">if</span> (dependsOn != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">for</span> (String dep : dependsOn) &#123;</span><br><span class="line">						<span class="keyword">if</span> (isDependent(beanName, dep)) &#123;</span><br><span class="line">							<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">									<span class="string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">						&#125;</span><br><span class="line">						registerDependentBean(dep, beanName);</span><br><span class="line">						<span class="keyword">try</span> &#123;</span><br><span class="line">							getBean(dep);</span><br><span class="line">						&#125; <span class="keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;</span><br><span class="line">							<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">									<span class="string">&quot;&#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">                </span><br><span class="line">				<span class="comment">// Create bean instance. 这里是单例的,然后进入到 createBean这个方法中来</span></span><br><span class="line">				<span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line">					sharedInstance = getSingleton(beanName, () -&gt; &#123;</span><br><span class="line">						<span class="keyword">try</span> &#123;</span><br><span class="line">							<span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">							<span class="comment">// Explicitly remove instance from singleton cache: It might have been put there</span></span><br><span class="line">							<span class="comment">// eagerly by the creation process, to allow for circular reference resolution.</span></span><br><span class="line">							<span class="comment">// Also remove any beans that received a temporary reference to the bean.</span></span><br><span class="line">							destroySingleton(beanName);</span><br><span class="line">							<span class="keyword">throw</span> ex;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;);</span><br><span class="line">					bean = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);</span><br><span class="line">				&#125; <span class="keyword">else</span> <span class="keyword">if</span> (mbd.isPrototype()) &#123;</span><br><span class="line">					<span class="comment">// It&#x27;s a prototype -&gt; create a new instance.</span></span><br><span class="line">					<span class="type">Object</span> <span class="variable">prototypeInstance</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line">						beforePrototypeCreation(beanName);</span><br><span class="line">						prototypeInstance = createBean(beanName, mbd, args);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">finally</span> &#123;</span><br><span class="line">						afterPrototypeCreation(beanName);</span><br><span class="line">					&#125;</span><br><span class="line">					bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);</span><br><span class="line">				&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="type">String</span> <span class="variable">scopeName</span> <span class="operator">=</span> mbd.getScope();</span><br><span class="line">					<span class="keyword">final</span> <span class="type">Scope</span> <span class="variable">scope</span> <span class="operator">=</span> <span class="built_in">this</span>.scopes.get(scopeName);</span><br><span class="line">					<span class="keyword">if</span> (scope == <span class="literal">null</span>) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line">						<span class="type">Object</span> <span class="variable">scopedInstance</span> <span class="operator">=</span> scope.get(beanName, () -&gt; &#123;</span><br><span class="line">							beforePrototypeCreation(beanName);</span><br><span class="line">							<span class="keyword">try</span> &#123;</span><br><span class="line">								<span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">							&#125;</span><br><span class="line">							<span class="keyword">finally</span> &#123;</span><br><span class="line">								afterPrototypeCreation(beanName);</span><br><span class="line">							&#125;</span><br><span class="line">						&#125;);</span><br><span class="line">						bean = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);</span><br><span class="line">					&#125; <span class="keyword">catch</span> (IllegalStateException ex) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName,</span><br><span class="line">								<span class="string">&quot;Scope &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27; is not active for the current thread; consider &quot;</span> +</span><br><span class="line">								<span class="string">&quot;defining a scoped proxy for this bean if you intend to refer to it from a singleton&quot;</span>,</span><br><span class="line">								ex);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125; <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">				cleanupAfterBeanCreationFailure(beanName);</span><br><span class="line">				<span class="keyword">throw</span> ex;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Check if required type matches the type of the actual bean instance.</span></span><br><span class="line">		<span class="keyword">if</span> (requiredType != <span class="literal">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="type">T</span> <span class="variable">convertedBean</span> <span class="operator">=</span> getTypeConverter().convertIfNecessary(bean, requiredType);</span><br><span class="line">				<span class="keyword">if</span> (convertedBean == <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">return</span> convertedBean;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (TypeMismatchException ex) &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">					logger.debug(<span class="string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="string">&quot;&#x27; to required type &#x27;&quot;</span> +</span><br><span class="line">							ClassUtils.getQualifiedName(requiredType) + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> (T) bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Return the (raw) singleton object registered under the given name.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;Checks already instantiated singletons and also allows for an early</span></span><br><span class="line"><span class="comment">	 * reference to a currently created singleton (resolving a circular reference).</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean to look for</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> allowEarlyReference whether early references should be created or not</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> the registered singleton object, or &#123;<span class="doctag">@code</span> null&#125; if none found</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">protected</span> Object <span class="title function_">getSingleton</span><span class="params">(String beanName, <span class="type">boolean</span> allowEarlyReference)</span> &#123;</span><br><span class="line">		<span class="type">Object</span> <span class="variable">singletonObject</span> <span class="operator">=</span> <span class="built_in">this</span>.singletonObjects.get(beanName);</span><br><span class="line">		<span class="keyword">if</span> (singletonObject == <span class="literal">null</span> &amp;&amp; isSingletonCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="built_in">this</span>.singletonObjects) &#123;</span><br><span class="line">				singletonObject = <span class="built_in">this</span>.earlySingletonObjects.get(beanName);</span><br><span class="line">				<span class="keyword">if</span> (singletonObject == <span class="literal">null</span> &amp;&amp; allowEarlyReference) &#123;</span><br><span class="line">					ObjectFactory&lt;?&gt; singletonFactory = <span class="built_in">this</span>.singletonFactories.get(beanName);</span><br><span class="line">					<span class="keyword">if</span> (singletonFactory != <span class="literal">null</span>) &#123;</span><br><span class="line">						singletonObject = singletonFactory.getObject();</span><br><span class="line">						<span class="built_in">this</span>.earlySingletonObjects.put(beanName, singletonObject);</span><br><span class="line">						<span class="built_in">this</span>.singletonFactories.remove(beanName);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> singletonObject;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Mark the specified bean as already created (or about to be created).</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;This allows the bean factory to optimize its caching for repeated</span></span><br><span class="line"><span class="comment">	 * creation of the specified bean.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment">	 这里将org.springframework.context.annotation.internalConfigurationAnnotationProcessor添加到alreadyCreated集合中</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">markBeanAsCreated</span><span class="params">(String beanName)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (!<span class="built_in">this</span>.alreadyCreated.contains(beanName)) &#123;</span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="built_in">this</span>.mergedBeanDefinitions) &#123;</span><br><span class="line">				<span class="keyword">if</span> (!<span class="built_in">this</span>.alreadyCreated.contains(beanName)) &#123;</span><br><span class="line">					<span class="comment">// Let the bean definition get re-merged now that we&#x27;re actually creating</span></span><br><span class="line">					<span class="comment">// the bean... just in case some of its metadata changed in the meantime.</span></span><br><span class="line">					clearMergedBeanDefinition(beanName);</span><br><span class="line">					<span class="built_in">this</span>.alreadyCreated.add(beanName);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Return a RootBeanDefinition for the given bean, by merging with the</span></span><br><span class="line"><span class="comment">	 * parent if the given bean&#x27;s definition is a child bean definition.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean definition</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> bd the original bean definition (Root/ChildBeanDefinition)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> containingBd the containing bean definition in case of inner bean,</span></span><br><span class="line"><span class="comment">	 * or &#123;<span class="doctag">@code</span> null&#125; in case of a top-level bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a (potentially merged) RootBeanDefinition for the given bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> BeanDefinitionStoreException in case of an invalid bean definition</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">protected</span> RootBeanDefinition <span class="title function_">getMergedBeanDefinition</span><span class="params">(</span></span><br><span class="line"><span class="params">			String beanName, BeanDefinition bd, <span class="meta">@Nullable</span> BeanDefinition containingBd)</span></span><br><span class="line">			<span class="keyword">throws</span> BeanDefinitionStoreException &#123;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">synchronized</span> (<span class="built_in">this</span>.mergedBeanDefinitions) &#123;</span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Check with full lock now in order to enforce the same merged instance.</span></span><br><span class="line">			<span class="keyword">if</span> (containingBd == <span class="literal">null</span>) &#123;</span><br><span class="line">				mbd = <span class="built_in">this</span>.mergedBeanDefinitions.get(beanName);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (mbd == <span class="literal">null</span>) &#123;</span><br><span class="line">				<span class="keyword">if</span> (bd.getParentName() == <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="comment">// Use copy of given root bean definition.</span></span><br><span class="line">					<span class="keyword">if</span> (bd <span class="keyword">instanceof</span> RootBeanDefinition) &#123;</span><br><span class="line">						mbd = ((RootBeanDefinition) bd).cloneBeanDefinition();</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">else</span> &#123;</span><br><span class="line">						mbd = <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(bd);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="comment">// Child bean definition: needs to be merged with parent.</span></span><br><span class="line">					BeanDefinition pbd;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line">						<span class="type">String</span> <span class="variable">parentBeanName</span> <span class="operator">=</span> transformedBeanName(bd.getParentName());</span><br><span class="line">						<span class="keyword">if</span> (!beanName.equals(parentBeanName)) &#123;</span><br><span class="line">							pbd = getMergedBeanDefinition(parentBeanName);</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="keyword">else</span> &#123;</span><br><span class="line">							<span class="type">BeanFactory</span> <span class="variable">parent</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">							<span class="keyword">if</span> (parent <span class="keyword">instanceof</span> ConfigurableBeanFactory) &#123;</span><br><span class="line">								pbd = ((ConfigurableBeanFactory) parent).getMergedBeanDefinition(parentBeanName);</span><br><span class="line">							&#125;</span><br><span class="line">							<span class="keyword">else</span> &#123;</span><br><span class="line">								<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NoSuchBeanDefinitionException</span>(parentBeanName,</span><br><span class="line">										<span class="string">&quot;Parent name &#x27;&quot;</span> + parentBeanName + <span class="string">&quot;&#x27; is equal to bean name &#x27;&quot;</span> + beanName +</span><br><span class="line">										<span class="string">&quot;&#x27;: cannot be resolved without an AbstractBeanFactory parent&quot;</span>);</span><br><span class="line">							&#125;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionStoreException</span>(bd.getResourceDescription(), beanName,</span><br><span class="line">								<span class="string">&quot;Could not resolve parent bean definition &#x27;&quot;</span> + bd.getParentName() + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="comment">// Deep copy with overridden values.</span></span><br><span class="line">					mbd = <span class="keyword">new</span> <span class="title class_">RootBeanDefinition</span>(pbd);</span><br><span class="line">					mbd.overrideFrom(bd);</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Set default singleton scope, if not configured before.</span></span><br><span class="line">				<span class="keyword">if</span> (!StringUtils.hasLength(mbd.getScope())) &#123;</span><br><span class="line">					mbd.setScope(RootBeanDefinition.SCOPE_SINGLETON);</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="comment">// A bean contained in a non-singleton bean cannot be a singleton itself.</span></span><br><span class="line">				<span class="comment">// Let&#x27;s correct this on the fly here, since this might be the result of</span></span><br><span class="line">				<span class="comment">// parent-child merging for the outer bean, in which case the original inner bean</span></span><br><span class="line">				<span class="comment">// definition will not have inherited the merged outer bean&#x27;s singleton status.</span></span><br><span class="line">				<span class="keyword">if</span> (containingBd != <span class="literal">null</span> &amp;&amp; !containingBd.isSingleton() &amp;&amp; mbd.isSingleton()) &#123;</span><br><span class="line">					mbd.setScope(containingBd.getScope());</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				<span class="comment">// Cache the merged bean definition for the time being</span></span><br><span class="line">				<span class="comment">// (it might still get re-merged later on in order to pick up metadata changes)</span></span><br><span class="line"><span class="comment">// 这里将传入进来的值,放入到mergedBeanDefinitions Map中                </span></span><br><span class="line">				<span class="keyword">if</span> (containingBd == <span class="literal">null</span> &amp;&amp; isCacheBeanMetadata()) &#123;</span><br><span class="line">					<span class="built_in">this</span>.mergedBeanDefinitions.put(beanName, mbd);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">return</span> mbd;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-----------------------------</span><br><span class="line">createBean     </span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Actually create the specified bean. Pre-creation processing has already happened</span></span><br><span class="line"><span class="comment">	 * at this point, e.g. checking &#123;<span class="doctag">@code</span> postProcessBeforeInstantiation&#125; callbacks.</span></span><br><span class="line"><span class="comment">	 * &lt;p&gt;Differentiates between default bean instantiation, use of a</span></span><br><span class="line"><span class="comment">	 * factory method, and autowiring a constructor.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> mbd the merged bean definition for the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> args explicit arguments to use for constructor or factory method invocation</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a new instance of the bean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> BeanCreationException if the bean could not be created</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #instantiateBean</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #instantiateUsingFactoryMethod</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #autowireConstructor</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">protected</span> Object <span class="title function_">doCreateBean</span><span class="params">(<span class="keyword">final</span> String beanName, <span class="keyword">final</span> RootBeanDefinition mbd, <span class="keyword">final</span> <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">			<span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Instantiate the bean.</span></span><br><span class="line">		<span class="type">BeanWrapper</span> <span class="variable">instanceWrapper</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">		<span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line">			instanceWrapper = <span class="built_in">this</span>.factoryBeanInstanceCache.remove(beanName);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (instanceWrapper == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="comment">/**</span></span><br><span class="line"><span class="comment">			 *</span></span><br><span class="line"><span class="comment">			 */</span></span><br><span class="line">			instanceWrapper = createBeanInstance(beanName, mbd, args);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">final</span> <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> instanceWrapper.getWrappedInstance();</span><br><span class="line">		Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();</span><br><span class="line">		<span class="keyword">if</span> (beanType != NullBean.class) &#123;</span><br><span class="line">			mbd.resolvedTargetType = beanType;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Allow post-processors to modify the merged bean definition.</span></span><br><span class="line">		<span class="keyword">synchronized</span> (mbd.postProcessingLock) &#123;</span><br><span class="line">			<span class="keyword">if</span> (!mbd.postProcessed) &#123;</span><br><span class="line">				<span class="keyword">try</span> &#123;</span><br><span class="line">					<span class="comment">// TODO 第三次调用后置处理器调用</span></span><br><span class="line">					applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);</span><br><span class="line">				&#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">							<span class="string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);</span><br><span class="line">				&#125;</span><br><span class="line">				mbd.postProcessed = <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Eagerly cache singletons to be able to resolve circular references</span></span><br><span class="line">		<span class="comment">// even when triggered by lifecycle interfaces like BeanFactoryAware.</span></span><br><span class="line">		<span class="type">boolean</span> <span class="variable">earlySingletonExposure</span> <span class="operator">=</span> (mbd.isSingleton() &amp;&amp; <span class="built_in">this</span>.allowCircularReferences &amp;&amp;</span><br><span class="line">				isSingletonCurrentlyInCreation(beanName));</span><br><span class="line">		<span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">			<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">				logger.debug(<span class="string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +</span><br><span class="line">						<span class="string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="comment">// TODO 第四次调用后置处理器</span></span><br><span class="line">			addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Initialize the bean instance.</span></span><br><span class="line">		<span class="type">Object</span> <span class="variable">exposedObject</span> <span class="operator">=</span> bean;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			<span class="comment">// TODO 第五次调用后置处理器 ; 第六次.  后置处理器调用了二个</span></span><br><span class="line">			populateBean(beanName, mbd, instanceWrapper);</span><br><span class="line"></span><br><span class="line">			<span class="comment">// TODO 第七次后置处理器执行 ; 和 第八次. 后置处理器调用了二个</span></span><br><span class="line">			exposedObject = initializeBean(beanName, exposedObject, mbd);</span><br><span class="line">		&#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">if</span> (ex <span class="keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;</span><br><span class="line">				<span class="keyword">throw</span> (BeanCreationException) ex;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName, <span class="string">&quot;Initialization of bean failed&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">			<span class="type">Object</span> <span class="variable">earlySingletonReference</span> <span class="operator">=</span> getSingleton(beanName, <span class="literal">false</span>);</span><br><span class="line">			<span class="keyword">if</span> (earlySingletonReference != <span class="literal">null</span>) &#123;</span><br><span class="line">				<span class="keyword">if</span> (exposedObject == bean) &#123;</span><br><span class="line">					exposedObject = earlySingletonReference;</span><br><span class="line">				&#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;</span><br><span class="line">					String[] dependentBeans = getDependentBeans(beanName);</span><br><span class="line">					Set&lt;String&gt; actualDependentBeans = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(dependentBeans.length);</span><br><span class="line">					<span class="keyword">for</span> (String dependentBean : dependentBeans) &#123;</span><br><span class="line">						<span class="keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;</span><br><span class="line">							actualDependentBeans.add(dependentBean);</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName,</span><br><span class="line">								<span class="string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; has been injected into other beans [&quot;</span> +</span><br><span class="line">								StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +</span><br><span class="line">								<span class="string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +</span><br><span class="line">								<span class="string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +</span><br><span class="line">								<span class="string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +</span><br><span class="line">								<span class="string">&quot;&#x27;getBeanNamesOfType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Register bean as disposable.</span></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			registerDisposableBeanIfNecessary(beanName, bean, mbd);</span><br><span class="line">		&#125; <span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName, <span class="string">&quot;Invalid destruction signature&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> exposedObject;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="registerBeanPostProcessors-beanFactory-方法"><a href="#registerBeanPostProcessors-beanFactory-方法" class="headerlink" title="registerBeanPostProcessors(beanFactory) 方法"></a>registerBeanPostProcessors(beanFactory) 方法</h3><p>从名字上看,是对BeanPostProcessors进行注册</p>
<p>可以总体看到 registerBeanPostProcessors 这个方法,根据BeanPostProcessor获取出postProcessorNames,然后对 postProcessorNames 进行迭代, 进行分类处理,分别装入不同的集合中.对 不同的集合进行先后迭代处理,排序，添加到beanFactory中去. 这里多了 BeanPostProcessorChecker 和 ApplicationListenerDetector这二个，是在这儿给添加进去的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Instantiate and register all BeanPostProcessor beans,</span></span><br><span class="line"><span class="comment"> * respecting explicit order if given.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Must be called before any instantiation of application beans.</span></span><br><span class="line"><span class="comment"> 这里使用 PostProcessorRegistrationDelegate 这个类来完成.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   PostProcessorRegistrationDelegate.registerBeanPostProcessors(beanFactory, <span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">--------------------------</span><br><span class="line"><span class="comment">// org.springframework.context.support.PostProcessorRegistrationDelegate#registerBeanPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, org.springframework.context.support.AbstractApplicationContext)</span></span><br><span class="line"><span class="comment">// 传入进来的beanFactory是DefaultListableBeanFactory,applicationContext:AnnotationConfigApplicationContext.    </span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">registerBeanPostProcessors</span><span class="params">(</span></span><br><span class="line"><span class="params">			ConfigurableListableBeanFactory beanFactory, AbstractApplicationContext applicationContext)</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里获取出来了  //org.springframework.context.annotation.internalAutowiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalRequiredAnnotationProcessor</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.internalCommonAnnotationProcessor</span></span><br><span class="line"><span class="comment">//yangBeanPostProcessor 这个是我自己写的类,实现了BeanPostProcessor这个接口,并且重写了其方法    </span></span><br><span class="line">		String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor.class,</span><br><span class="line">				<span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Register BeanPostProcessorChecker that logs an info message when</span></span><br><span class="line">		<span class="comment">// a bean is created during BeanPostProcessor instantiation, i.e. when</span></span><br><span class="line">		<span class="comment">// a bean is not eligible for getting processed by all BeanPostProcessors.</span></span><br><span class="line"><span class="comment">// 3 + 1 + 4 = 8    </span></span><br><span class="line">		<span class="type">int</span> <span class="variable">beanProcessorTargetCount</span> <span class="operator">=</span> beanFactory.getBeanPostProcessorCount() + <span class="number">1</span> + postProcessorNames.length;</span><br><span class="line"><span class="comment">// new 一个BeanPostProcessorChecker,传入beanFactory和上面的beanProcessorTargetCount值是8,然后又添加到beanFactory中去.    </span></span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">BeanPostProcessorChecker</span>(beanFactory, beanProcessorTargetCount));</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Separate between BeanPostProcessors that implement PriorityOrdered,</span></span><br><span class="line">		<span class="comment">// Ordered, and the rest.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; priorityOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;BeanPostProcessor&gt; internalPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; orderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		List&lt;String&gt; nonOrderedPostProcessorNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 对 postProcessorNames 进行遍历;同时使用不同类型的集合来存储数据</span></span><br><span class="line"><span class="comment">//这里对上面的 postProcessorNames 进行迭代,根据不同的条件放入到不同的集合中.我自定义的yangBeanPostProcessor就放入到nonOrderedPostProcessorNames这个集合中,其他三个都分别放入到了priorityOrderedPostProcessors和internalPostProcessors这二个集合中.</span></span><br><span class="line">		<span class="keyword">for</span> (String ppName : postProcessorNames) &#123;</span><br><span class="line">			<span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;</span><br><span class="line">				<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">				priorityOrderedPostProcessors.add(pp);</span><br><span class="line">				<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">					internalPostProcessors.add(pp);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125; <span class="keyword">else</span> <span class="keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;</span><br><span class="line">				orderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">				nonOrderedPostProcessorNames.add(ppName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// First, register the BeanPostProcessors that implement PriorityOrdered.</span></span><br><span class="line"> <span class="comment">//如果beanFactory是DefaultListableBeanFactory的话,就调用其getDependencyComparator()方法,然后对传入进来的priorityOrderedPostProcessors根据getDependencyComparator()返回的方法进行排序.</span></span><br><span class="line">		sortPostProcessors(priorityOrderedPostProcessors, beanFactory);</span><br><span class="line"> <span class="comment">//对priorityOrderedPostProcessors集合中的值,添加到beanFactory中去----&gt;beanFactory.addBeanPostProcessor(postProcessor); 也就是走的这个方法</span></span><br><span class="line">		registerBeanPostProcessors(beanFactory, priorityOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Next, register the BeanPostProcessors that implement Ordered.</span></span><br><span class="line">    <span class="comment">// orderedPostProcessorNames 这边是一个空的集合,也就是没有值.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; orderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : orderedPostProcessorNames) &#123;</span><br><span class="line">			<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">			orderedPostProcessors.add(pp);</span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		sortPostProcessors(orderedPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, orderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Now, register all regular BeanPostProcessors.</span></span><br><span class="line"><span class="comment">// nonOrderedPostProcessorNames 里面就是我们自定义的 YangBeanPostProcessor.</span></span><br><span class="line">		List&lt;BeanPostProcessor&gt; nonOrderedPostProcessors = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (String ppName : nonOrderedPostProcessorNames) &#123;</span><br><span class="line">			<span class="type">BeanPostProcessor</span> <span class="variable">pp</span> <span class="operator">=</span> beanFactory.getBean(ppName, BeanPostProcessor.class);</span><br><span class="line">			nonOrderedPostProcessors.add(pp);</span><br><span class="line">            <span class="comment">// 没有实现这个接口，所以也就不会走到这个方法来</span></span><br><span class="line">			<span class="keyword">if</span> (pp <span class="keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;</span><br><span class="line">				internalPostProcessors.add(pp);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 添加到 beanFactoryy中去    </span></span><br><span class="line">		registerBeanPostProcessors(beanFactory, nonOrderedPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Finally, re-register all internal BeanPostProcessors.</span></span><br><span class="line">       <span class="comment">// 排序,然后添加到beanFactoruy中去.</span></span><br><span class="line">		sortPostProcessors(internalPostProcessors, beanFactory);</span><br><span class="line">		registerBeanPostProcessors(beanFactory, internalPostProcessors);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Re-register post-processor for detecting inner beans as ApplicationListeners,</span></span><br><span class="line">		<span class="comment">// moving it to the end of the processor chain (for picking up proxies etc).</span></span><br><span class="line"><span class="comment">//将AnnotationConfigApplication传入ApplicationListenerDetector,new 一个 ApplicationListenerDetector,然后添加到beanFactory中去. </span></span><br><span class="line">		beanFactory.addBeanPostProcessor(<span class="keyword">new</span> <span class="title class_">ApplicationListenerDetector</span>(applicationContext));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="initMessageSource-方法"><a href="#initMessageSource-方法" class="headerlink" title="initMessageSource() 方法"></a>initMessageSource() 方法</h3><p>这个方法,如果beanFactory中是有 MESSAGE_SOURCE_BEAN_NAME 的话,就会进行判断, 都满足条件的话,就会走到hms.setParentMessageSource(getInternalParentMessageSource());这个方法</p>
<p>如果不包含的话,就会去new一个DelegatingMessageSource,然后添加到beanFactory中的bdMap中去</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the MessageSource.</span></span><br><span class="line"><span class="comment"> * Use parent&#x27;s if none defined in this context.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initMessageSource</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="comment">//获取出beanFactory是DefaultListableBeanFactory这个类,    </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line">   <span class="comment">// beanFactory 里面是没有包含 MESSAGE_SOURCE_BEAN_NAME 这个bean的,走到else中   </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(MESSAGE_SOURCE_BEAN_NAME)) &#123;</span><br><span class="line">      <span class="built_in">this</span>.messageSource = beanFactory.getBean(MESSAGE_SOURCE_BEAN_NAME, MessageSource.class);</span><br><span class="line">      <span class="comment">// Make MessageSource aware of parent MessageSource.</span></span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.parent != <span class="literal">null</span> &amp;&amp; <span class="built_in">this</span>.messageSource <span class="keyword">instanceof</span> HierarchicalMessageSource) &#123;</span><br><span class="line">         <span class="type">HierarchicalMessageSource</span> <span class="variable">hms</span> <span class="operator">=</span> (HierarchicalMessageSource) <span class="built_in">this</span>.messageSource;</span><br><span class="line">         <span class="keyword">if</span> (hms.getParentMessageSource() == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Only set parent context as parent MessageSource if no parent MessageSource</span></span><br><span class="line">            <span class="comment">// registered already.</span></span><br><span class="line">            hms.setParentMessageSource(getInternalParentMessageSource());</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Using MessageSource [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Use empty MessageSource to be able to accept getMessage calls.</span></span><br><span class="line">      <span class="type">DelegatingMessageSource</span> <span class="variable">dms</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DelegatingMessageSource</span>();</span><br><span class="line">      <span class="comment">// getInternalParentMessageSource 方法返回的是null</span></span><br><span class="line">      dms.setParentMessageSource(getInternalParentMessageSource()); </span><br><span class="line">      <span class="built_in">this</span>.messageSource = dms;</span><br><span class="line"><span class="comment">// 然后使用MESSAGE_SOURCE_BEAN_NAME,value是this.messageSource,添加到bdMap中去.        </span></span><br><span class="line">      beanFactory.registerSingleton(MESSAGE_SOURCE_BEAN_NAME, <span class="built_in">this</span>.messageSource);</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Unable to locate MessageSource with name &#x27;&quot;</span> + MESSAGE_SOURCE_BEAN_NAME +</span><br><span class="line">               <span class="string">&quot;&#x27;: using default [&quot;</span> + <span class="built_in">this</span>.messageSource + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="initApplicationEventMulticaster-方法"><a href="#initApplicationEventMulticaster-方法" class="headerlink" title="initApplicationEventMulticaster() 方法"></a>initApplicationEventMulticaster() 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Initialize the ApplicationEventMulticaster.</span></span><br><span class="line"><span class="comment"> * Uses SimpleApplicationEventMulticaster if none defined in the context.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> org.springframework.context.event.SimpleApplicationEventMulticaster</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initApplicationEventMulticaster</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="comment">// 获取 beanFactory:DefaultListableBeanFactory    </span></span><br><span class="line">   <span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> getBeanFactory();</span><br><span class="line"><span class="comment">//如果beanFactory中包含APPLICATION_EVENT_MULTICASTER_BEAN_NAME,    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsLocalBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME)) &#123;</span><br><span class="line"> <span class="comment">//从beanFactory中获取出bean,赋值给this.applicationEventMulticaster      </span></span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster =</span><br><span class="line">            beanFactory.getBean(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, ApplicationEventMulticaster.class);</span><br><span class="line"><span class="comment">// log 是 debug级别的话,就对进行log输出       </span></span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Using ApplicationEventMulticaster [&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 如果beanFactory中不包含的话,就new 一个SimpleApplicationEventMulticaster,并且赋值给this.applicationEventMulticaster这个属性        </span></span><br><span class="line">      <span class="built_in">this</span>.applicationEventMulticaster = <span class="keyword">new</span> <span class="title class_">SimpleApplicationEventMulticaster</span>(beanFactory);</span><br><span class="line"> <span class="comment">//将上面new出来的,注册到beanFactory中去,也是主要放入bdMao中,还有一些beanName的List中.      </span></span><br><span class="line">      beanFactory.registerSingleton(APPLICATION_EVENT_MULTICASTER_BEAN_NAME, <span class="built_in">this</span>.applicationEventMulticaster);</span><br><span class="line"> <span class="comment">// 如果是debug级别的话,这里就会打印出来.      </span></span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Unable to locate ApplicationEventMulticaster with name &#x27;&quot;</span> +</span><br><span class="line">               APPLICATION_EVENT_MULTICASTER_BEAN_NAME +</span><br><span class="line">               <span class="string">&quot;&#x27;: using default [&quot;</span> + <span class="built_in">this</span>.applicationEventMulticaster + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="onRefresh"><a href="#onRefresh" class="headerlink" title="onRefresh():"></a>onRefresh():</h3><p>留给子类扩展的. 比如SpringBoot中,这里就会初始化一些SpringMvc的信息,然后new Tomcat()等</p>
<h3 id="regeisterListeners-方法"><a href="#regeisterListeners-方法" class="headerlink" title="regeisterListeners() 方法"></a>regeisterListeners() 方法</h3><p>这个方法,目前在单纯的Spring中跑起来上面都没有,但是从名字上看的话,目测是对 Listeners进行注册到 beanFactory中去</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Add beans that implement ApplicationListener as listeners.</span></span><br><span class="line"><span class="comment"> * Doesn&#x27;t affect other listeners, which can be added without being beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">registerListeners</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Register statically specified listeners first.</span></span><br><span class="line"><span class="comment">//     getApplicationListeners 方法返回的是空集合.</span></span><br><span class="line">   <span class="keyword">for</span> (ApplicationListener&lt;?&gt; listener : getApplicationListeners()) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListener(listener);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Do not initialize FactoryBeans here: We need to leave all regular beans</span></span><br><span class="line">   <span class="comment">// uninitialized to let post-processors apply to them!</span></span><br><span class="line"><span class="comment">//  listenerBeanNames 返回的也是空数组</span></span><br><span class="line">   String[] listenerBeanNames = getBeanNamesForType(ApplicationListener.class, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">   <span class="keyword">for</span> (String listenerBeanName : listenerBeanNames) &#123;</span><br><span class="line">      getApplicationEventMulticaster().addApplicationListenerBean(listenerBeanName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish early application events now that we finally have a multicaster...</span></span><br><span class="line">   <span class="comment">// 空的集合.这 </span></span><br><span class="line">   Set&lt;ApplicationEvent&gt; earlyEventsToProcess = <span class="built_in">this</span>.earlyApplicationEvents;</span><br><span class="line">   <span class="built_in">this</span>.earlyApplicationEvents = <span class="literal">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (earlyEventsToProcess != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">for</span> (ApplicationEvent earlyEvent : earlyEventsToProcess) &#123;</span><br><span class="line">         getApplicationEventMulticaster().multicastEvent(earlyEvent);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="finishBeanFactoryInitializarion-方法"><a href="#finishBeanFactoryInitializarion-方法" class="headerlink" title="finishBeanFactoryInitializarion() 方法"></a>finishBeanFactoryInitializarion() 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the initialization of this context&#x27;s bean factory,</span></span><br><span class="line"><span class="comment"> * initializing all remaining singleton beans.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishBeanFactoryInitialization</span><span class="params">(ConfigurableListableBeanFactory beanFactory)</span> &#123;</span><br><span class="line">   <span class="comment">// Initialize conversion service for this context.</span></span><br><span class="line"><span class="comment">// 不包含的,也就不会走入到这个方法中.    </span></span><br><span class="line">   <span class="keyword">if</span> (beanFactory.containsBean(CONVERSION_SERVICE_BEAN_NAME) &amp;&amp;</span><br><span class="line">         beanFactory.isTypeMatch(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class)) &#123;</span><br><span class="line">      beanFactory.setConversionService(</span><br><span class="line">            beanFactory.getBean(CONVERSION_SERVICE_BEAN_NAME, ConversionService.class));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register a default embedded value resolver if no bean post-processor</span></span><br><span class="line">   <span class="comment">// (such as a PropertyPlaceholderConfigurer bean) registered any before:</span></span><br><span class="line">   <span class="comment">// at this point, primarily for resolution in annotation attribute values.</span></span><br><span class="line">   <span class="keyword">if</span> (!beanFactory.hasEmbeddedValueResolver()) &#123;</span><br><span class="line">      beanFactory.addEmbeddedValueResolver(strVal -&gt; getEnvironment().resolvePlaceholders(strVal));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize LoadTimeWeaverAware beans early to allow for registering their transformers early.</span></span><br><span class="line"><span class="comment">// 这里获取出来的 数组是空数组,所以下面的迭代自然也不会进去    </span></span><br><span class="line">   String[] weaverAwareNames = beanFactory.getBeanNamesForType(LoadTimeWeaverAware.class,</span><br><span class="line">         <span class="literal">false</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">for</span> (String weaverAwareName : weaverAwareNames) &#123;</span><br><span class="line">      getBean(weaverAwareName);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Stop using the temporary ClassLoader for type matching.</span></span><br><span class="line">   beanFactory.setTempClassLoader(<span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow for caching all bean definition metadata, not expecting further changes.</span></span><br><span class="line"><span class="comment">//这里面对一个参数设置为true,然后将beanName的集合转化为数组    </span></span><br><span class="line">   beanFactory.freezeConfiguration();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">   beanFactory.preInstantiateSingletons();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个方法是对bean进行实例化的.</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">preInstantiateSingletons</span><span class="params">()</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="comment">// debug级别才会打印的log     </span></span><br><span class="line">		<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">			logger.debug(<span class="string">&quot;Pre-instantiating singletons in &quot;</span> + <span class="built_in">this</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// Iterate over a copy to allow for init methods which in turn register new bean definitions.</span></span><br><span class="line">		<span class="comment">// While this may not be part of the regular factory bootstrap, it does otherwise work fine.</span></span><br><span class="line"><span class="comment">// 	相等于copy了一份beanDefinitionNames List,将数据全部放入到beanNames这个集合中   </span></span><br><span class="line">		List&lt;String&gt; beanNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(<span class="built_in">this</span>.beanDefinitionNames);</span><br><span class="line"><span class="comment">//对上面的beanNames集合进行迭代.        </span></span><br><span class="line">		<span class="comment">// Trigger initialization of all non-lazy singleton beans...</span></span><br><span class="line">		<span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line"><span class="comment">// org.springframework.beans.factory.support.AbstractBeanFactory 中mergedBeanDefinitions的Map是否已经有beanName了,Map&lt;String, RootBeanDefinition&gt; mergedBeanDefinitions,有的话这里就会返回bd回来.            </span></span><br><span class="line">			<span class="type">RootBeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line"><span class="comment">// bd不是抽象的,是单例的,不是赖加载(赖加载是需要使用时才会去走的逻辑).            </span></span><br><span class="line">			<span class="keyword">if</span> (!bd.isAbstract() &amp;&amp; bd.isSingleton() &amp;&amp; !bd.isLazyInit()) &#123;</span><br><span class="line">				<span class="comment">//  判断你这bd是不是实现了FactoryBean这个接口.这里我们定义的bean是没有实现这个接口的,所以也就不会走到这个if中来.</span></span><br><span class="line">				<span class="keyword">if</span> (isFactoryBean(beanName)) &#123;</span><br><span class="line"></span><br><span class="line">					<span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> getBean(FACTORY_BEAN_PREFIX + beanName);</span><br><span class="line">					<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> FactoryBean) &#123;</span><br><span class="line">						<span class="keyword">final</span> FactoryBean&lt;?&gt; factory = (FactoryBean&lt;?&gt;) bean;</span><br><span class="line">						<span class="type">boolean</span> isEagerInit;</span><br><span class="line">						<span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span> &amp;&amp; factory <span class="keyword">instanceof</span> SmartFactoryBean) &#123;</span><br><span class="line">							isEagerInit = AccessController.doPrivileged((PrivilegedAction&lt;Boolean&gt;)</span><br><span class="line">											((SmartFactoryBean&lt;?&gt;) factory)::isEagerInit,</span><br><span class="line">									getAccessControlContext());</span><br><span class="line">						&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">							isEagerInit = (factory <span class="keyword">instanceof</span> SmartFactoryBean &amp;&amp;</span><br><span class="line">									((SmartFactoryBean&lt;?&gt;) factory).isEagerInit());</span><br><span class="line">						&#125;<span class="keyword">if</span> (isEagerInit) &#123;</span><br><span class="line">							getBean(beanName);</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 获取bean,也就是创建bean的,需要一步一步往下走才可以看到很明白.</span></span><br><span class="line">					getBean(beanName);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Trigger post-initialization callback for all applicable beans...</span></span><br><span class="line"><span class="comment">// 迭代beanNames这个集合,        </span></span><br><span class="line">		<span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">         <span class="comment">// 根据beanName获取出一个单例实例.   </span></span><br><span class="line">			<span class="type">Object</span> <span class="variable">singletonInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">          <span class="comment">//如果单例实例是有是实现SmartInitializingSingleton这个接口的话,不管下面是if还是else,都会走到调用  afterSingletonsInstantiated 这个方法的逻辑代码,这里就走完了 preInstantiateSingletons 方法.  </span></span><br><span class="line">			<span class="keyword">if</span> (singletonInstance <span class="keyword">instanceof</span> SmartInitializingSingleton) &#123;</span><br><span class="line">				<span class="keyword">final</span> <span class="type">SmartInitializingSingleton</span> <span class="variable">smartSingleton</span> <span class="operator">=</span> (SmartInitializingSingleton) singletonInstance;</span><br><span class="line">				<span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span>) &#123;</span><br><span class="line">					AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">						smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">						<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">					&#125;, getAccessControlContext());</span><br><span class="line">				&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">					smartSingleton.afterSingletonsInstantiated();</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="doGetBean-方法"><a href="#doGetBean-方法" class="headerlink" title="doGetBean() 方法"></a>doGetBean() 方法</h3><p>do开头才是真正做事的方法</p>
<p>走完这个方法,基本一个bean的创建就走完了.可以看出来,这其中的逻辑还是很复杂的.做了各种BeanPostPrcoessor进行扩展,中间还走了一个Aware的接口判断,估摸着也是进行扩展的逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Return an instance, which may be shared or independent, of the specified bean.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> name the name of the bean to retrieve</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> requiredType the required type of the bean to retrieve</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> args arguments to use when creating a bean instance using explicit arguments</span></span><br><span class="line"><span class="comment"> * (only applied when creating a new instance as opposed to retrieving an existing one)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> typeCheckOnly whether the instance is obtained for a type check,</span></span><br><span class="line"><span class="comment"> * not for actual use</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> an instance of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeansException if the bean could not be created</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line"><span class="keyword">protected</span> &lt;T&gt; T <span class="title function_">doGetBean</span><span class="params">(<span class="keyword">final</span> String name, <span class="meta">@Nullable</span> <span class="keyword">final</span> Class&lt;T&gt; requiredType,</span></span><br><span class="line"><span class="params">      <span class="meta">@Nullable</span> <span class="keyword">final</span> Object[] args, <span class="type">boolean</span> typeCheckOnly)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 先对beanName进行处理,比如有别名或者首字母大写转小写	 </span></span><br><span class="line">   <span class="keyword">final</span> <span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> transformedBeanName(name);</span><br><span class="line">   Object bean;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Eagerly check singleton cache for manually registered singletons.</span></span><br><span class="line"><span class="comment">// 这里根据beanName从 org.springframework.beans.factory.support.DefaultSingletonBeanRegistry 中获取Object,在之前初始化的时候,是有注册到这个类的 singletonObjects(Map)中来    </span></span><br><span class="line">   <span class="type">Object</span> <span class="variable">sharedInstance</span> <span class="operator">=</span> getSingleton(beanName);</span><br><span class="line">   <span class="keyword">if</span> (sharedInstance != <span class="literal">null</span> &amp;&amp; args == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="comment">/*if (logger.isDebugEnabled()) &#123;</span></span><br><span class="line"><span class="comment">         if (isSingletonCurrentlyInCreation(beanName)) &#123;</span></span><br><span class="line"><span class="comment">            logger.debug(&quot;Returning eagerly cached instance of singleton bean &#x27;&quot; + beanName +</span></span><br><span class="line"><span class="comment">                  &quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;);</span></span><br><span class="line"><span class="comment">         &#125; else &#123;</span></span><br><span class="line"><span class="comment">            logger.debug(&quot;Returning cached instance of singleton bean &#x27;&quot; + beanName + &quot;&#x27;&quot;);</span></span><br><span class="line"><span class="comment">         &#125;</span></span><br><span class="line"><span class="comment">      &#125;*/</span></span><br><span class="line"><span class="comment">// 这个方放,由于bean没有实现FactoryBean这个接口,所以这里就直接返回了sharedInstance这个Object回来.       </span></span><br><span class="line">      bean = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="literal">null</span>);</span><br><span class="line">   &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Fail if we&#x27;re already creating this bean instance:</span></span><br><span class="line">      <span class="comment">// We&#x27;re assumably within a circular reference.</span></span><br><span class="line">      <span class="keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Check if bean definition exists in this factory.</span></span><br><span class="line">  <span class="comment">// 返回的值是null.所以下面的if自然也不会进入的     </span></span><br><span class="line">      <span class="type">BeanFactory</span> <span class="variable">parentBeanFactory</span> <span class="operator">=</span> getParentBeanFactory();</span><br><span class="line">      <span class="keyword">if</span> (parentBeanFactory != <span class="literal">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;</span><br><span class="line">         <span class="comment">// Not found -&gt; check parent.</span></span><br><span class="line">         <span class="type">String</span> <span class="variable">nameToLookup</span> <span class="operator">=</span> originalBeanName(name);</span><br><span class="line">         <span class="keyword">if</span> (parentBeanFactory <span class="keyword">instanceof</span> AbstractBeanFactory) &#123;</span><br><span class="line">            <span class="keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(</span><br><span class="line">                  nameToLookup, requiredType, args, typeCheckOnly);</span><br><span class="line">         &#125; <span class="keyword">else</span> <span class="keyword">if</span> (args != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Delegation to parent with explicit args.</span></span><br><span class="line">            <span class="keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);</span><br><span class="line">         &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// No args -&gt; delegate to standard getBean method.</span></span><br><span class="line">            <span class="keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (!typeCheckOnly) &#123;</span><br><span class="line">          <span class="comment">// 堆bean进行一个mark标记</span></span><br><span class="line">         markBeanAsCreated(beanName);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 从DefaultListableBeanFactory中的beanDefinitionMap中根据beanName获取出bd.          </span></span><br><span class="line">         <span class="keyword">final</span> <span class="type">RootBeanDefinition</span> <span class="variable">mbd</span> <span class="operator">=</span> getMergedLocalBeanDefinition(beanName);</span><br><span class="line"><span class="comment">// 对bd进行检查,如果是抽象类的话,这里就会抛出异常来.          </span></span><br><span class="line">         checkMergedBeanDefinition(mbd, beanName, args);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Guarantee initialization of beans that the current bean depends on.</span></span><br><span class="line">    <span class="comment">// 没有使用dependsOn这个注解,所以这里的值也就自然是null了      </span></span><br><span class="line">         String[] dependsOn = mbd.getDependsOn();</span><br><span class="line">         <span class="keyword">if</span> (dependsOn != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (String dep : dependsOn) &#123;</span><br><span class="line">               <span class="keyword">if</span> (isDependent(beanName, dep)) &#123;</span><br><span class="line">                  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                        <span class="string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">               &#125;</span><br><span class="line">               registerDependentBean(dep, beanName);</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                  getBean(dep);</span><br><span class="line">               &#125; <span class="keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;</span><br><span class="line">                  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                        <span class="string">&quot;&#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Create bean instance. 是单例就进来if中来</span></span><br><span class="line">         <span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line"><span class="comment">// 走完 createBean方法,就会走getSingleton方法,会对传入进来的beanName进行非null的判断.然后利用this.singletonObjects来进行加锁,Object singletonObject = this.singletonObjects.get(beanName);中获取出来Object(也就是bean对象),beforeSingletonCreation会进行检验,如果inCreationCheckExclusions是包含这个beanName或者singletonsCurrentlyInCreation停驾失败了的,就会抛出一个BeanCurrentlyInCreationException异常来.从singletonFactory也就是传入进来的参数中,调用getObject方法,返回Object. afterSingletonCreation()方法会继续检验下.addSingleton()方法将bean的信息给添加到 this.singletonObjects,this.registeredSingletons,前者Map,后者list的集合中去.最后返回  singletonFactory.getObject()方法的Object即可.   </span></span><br><span class="line">            sharedInstance = getSingleton(beanName, () -&gt; &#123;</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                  <span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">                  <span class="comment">// Explicitly remove instance from singleton cache: It might have been put there</span></span><br><span class="line">                  <span class="comment">// eagerly by the creation process, to allow for circular reference resolution.</span></span><br><span class="line">                  <span class="comment">// Also remove any beans that received a temporary reference to the bean.</span></span><br><span class="line">                  destroySingleton(beanName);</span><br><span class="line">                  <span class="keyword">throw</span> ex;</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;);</span><br><span class="line"><span class="comment">// 从private final NamedThreadLocal&lt;String&gt; currentlyCreatedBean = new NamedThreadLocal&lt;&gt;(&quot;Currently created bean&quot;);中获取当前bean,NamedThreadLocal是有继承ThreadLocal,ThreadLocal是与当前线程有关得一个存储的,是线程安全的,每个线程都可以使用一个或者多个ThreadLocal来进行存储数据.最后返回的bean赋值给bean。         </span></span><br><span class="line">            bean = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);</span><br><span class="line">         &#125; <span class="keyword">else</span> <span class="keyword">if</span> (mbd.isPrototype()) &#123;</span><br><span class="line">             <span class="comment">//这个是多列的</span></span><br><span class="line">            <span class="comment">// It&#x27;s a prototype -&gt; create a new instance.</span></span><br><span class="line">            <span class="type">Object</span> <span class="variable">prototypeInstance</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">               beforePrototypeCreation(beanName);</span><br><span class="line">               prototypeInstance = createBean(beanName, mbd, args);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">finally</span> &#123;</span><br><span class="line">               afterPrototypeCreation(beanName);</span><br><span class="line">            &#125;</span><br><span class="line">            bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);</span><br><span class="line">         &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">             <span class="comment">// 既不是单例也不是多例的</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">scopeName</span> <span class="operator">=</span> mbd.getScope();</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Scope</span> <span class="variable">scope</span> <span class="operator">=</span> <span class="built_in">this</span>.scopes.get(scopeName);</span><br><span class="line">            <span class="keyword">if</span> (scope == <span class="literal">null</span>) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27;&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">               <span class="type">Object</span> <span class="variable">scopedInstance</span> <span class="operator">=</span> scope.get(beanName, () -&gt; &#123;</span><br><span class="line">                  beforePrototypeCreation(beanName);</span><br><span class="line">                  <span class="keyword">try</span> &#123;</span><br><span class="line">                     <span class="keyword">return</span> createBean(beanName, mbd, args);</span><br><span class="line">                  &#125;</span><br><span class="line">                  <span class="keyword">finally</span> &#123;</span><br><span class="line">                     afterPrototypeCreation(beanName);</span><br><span class="line">                  &#125;</span><br><span class="line">               &#125;);</span><br><span class="line">               bean = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IllegalStateException ex) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName,</span><br><span class="line">                     <span class="string">&quot;Scope &#x27;&quot;</span> + scopeName + <span class="string">&quot;&#x27; is not active for the current thread; consider &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;defining a scoped proxy for this bean if you intend to refer to it from a singleton&quot;</span>,</span><br><span class="line">                     ex);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125; <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         cleanupAfterBeanCreationFailure(beanName);</span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Check if required type matches the type of the actual bean instance.</span></span><br><span class="line"><span class="comment">// 这里由于 requiredType 是null,所以就不会进入到代码中  </span></span><br><span class="line">   <span class="keyword">if</span> (requiredType != <span class="literal">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="type">T</span> <span class="variable">convertedBean</span> <span class="operator">=</span> getTypeConverter().convertIfNecessary(bean, requiredType);</span><br><span class="line">         <span class="keyword">if</span> (convertedBean == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> convertedBean;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (TypeMismatchException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">            logger.debug(<span class="string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="string">&quot;&#x27; to required type &#x27;&quot;</span> +</span><br><span class="line">                  ClassUtils.getQualifiedName(requiredType) + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanNotOfRequiredTypeException</span>(name, requiredType, bean.getClass());</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">    <span class="comment">// 最后返回这个bean</span></span><br><span class="line">   <span class="keyword">return</span> (T) bean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="doCreateBean-方法"><a href="#doCreateBean-方法" class="headerlink" title="doCreateBean() 方法"></a>doCreateBean() 方法</h3><p>可以看到这个是真正的create bean的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Actually create the specified bean. Pre-creation processing has already happened</span></span><br><span class="line"><span class="comment"> * at this point, e.g. checking &#123;<span class="doctag">@code</span> postProcessBeforeInstantiation&#125; callbacks.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Differentiates between default bean instantiation, use of a</span></span><br><span class="line"><span class="comment"> * factory method, and autowiring a constructor.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> mbd the merged bean definition for the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> args explicit arguments to use for constructor or factory method invocation</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> a new instance of the bean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> BeanCreationException if the bean could not be created</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #instantiateBean</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #instantiateUsingFactoryMethod</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #autowireConstructor</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> Object <span class="title function_">doCreateBean</span><span class="params">(<span class="keyword">final</span> String beanName, <span class="keyword">final</span> RootBeanDefinition mbd, <span class="keyword">final</span> <span class="meta">@Nullable</span> Object[] args)</span></span><br><span class="line">      <span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Instantiate the bean.</span></span><br><span class="line">   <span class="type">BeanWrapper</span> <span class="variable">instanceWrapper</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">  <span class="comment">// 再次判断mdb是不是单例的,是单例的化,就从factoryBeanInstanceCache(Map)中remove走.	  </span></span><br><span class="line">   <span class="keyword">if</span> (mbd.isSingleton()) &#123;</span><br><span class="line">      instanceWrapper = <span class="built_in">this</span>.factoryBeanInstanceCache.remove(beanName);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (instanceWrapper == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="comment">/**</span></span><br><span class="line"><span class="comment">       * 创建一个bean实例. 最后创建了一个bean的包装类回来.</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">      instanceWrapper = createBeanInstance(beanName, mbd, args);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">// 从上面的 instanceWrapper调用getWrappedClass()方法返回bean</span></span><br><span class="line">   <span class="keyword">final</span> <span class="type">Object</span> <span class="variable">bean</span> <span class="operator">=</span> instanceWrapper.getWrappedInstance();</span><br><span class="line">   <span class="comment">// 返回bean的type,其实也就是bean的类信息 </span></span><br><span class="line">   Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();</span><br><span class="line">   <span class="keyword">if</span> (beanType != NullBean.class) &#123;</span><br><span class="line">      <span class="comment">// 满足条件将上面的beanType赋值给resolvedTargetType的值 = beanType </span></span><br><span class="line">      mbd.resolvedTargetType = beanType;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Allow post-processors to modify the merged bean definition.</span></span><br><span class="line">  <span class="comment">// 利用 mbd的postProcessingLock 参数来进行加锁.</span></span><br><span class="line">   <span class="keyword">synchronized</span> (mbd.postProcessingLock) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!mbd.postProcessed) &#123;</span><br><span class="line">         <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// TODO 第三次调用后置处理器调用</span></span><br><span class="line">            <span class="comment">// 这个方法apply开头的,可以看到是对什么东西进行应用.</span></span><br><span class="line"><span class="comment">//点进去,org.springframework.beans.factory.support.AbstractBeanFactory中获取beanPostProcessors集合的值,也就是beanPostProcessors的集合值,迭代,如果是有实现 MergedBeanDefinitionPostProcessor 这个接口的化,就会进行强转,然后调用postProcessMergedBeanDefinition方法,这也是这个接口提供的方法.         </span></span><br><span class="line">            applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);</span><br><span class="line">         &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName,</span><br><span class="line">                  <span class="string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);</span><br><span class="line">         &#125;</span><br><span class="line">         mbd.postProcessed = <span class="literal">true</span>;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Eagerly cache singletons to be able to resolve circular references</span></span><br><span class="line">   <span class="comment">// even when triggered by lifecycle interfaces like BeanFactoryAware.</span></span><br><span class="line"><span class="comment">// earlySingletonExposure是ture,也就是说是单例的    </span></span><br><span class="line">   <span class="type">boolean</span> <span class="variable">earlySingletonExposure</span> <span class="operator">=</span> (mbd.isSingleton() &amp;&amp; <span class="built_in">this</span>.allowCircularReferences &amp;&amp;</span><br><span class="line">         isSingletonCurrentlyInCreation(beanName));</span><br><span class="line">   <span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +</span><br><span class="line">               <span class="string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// TODO 第四次调用后置处理器</span></span><br><span class="line"><span class="comment">// getEarlyBeanReference() 方法,也是调用getBeanPostProcessors()方法,获取返回的beanPostProcessor集合,迭代这个集合,如何是SmartInstantiationAwareBeanPostProcessor,就会进行强转,然后调用getEarlyBeanReference,如果是实现SmartInstantiationAwareBeanPostProcessor这个接口的话,是可以实现这个方法的,也是可以不是实现这个方法,因为有default来修饰</span></span><br><span class="line">      addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize the bean instance.</span></span><br><span class="line">   <span class="type">Object</span> <span class="variable">exposedObject</span> <span class="operator">=</span> bean;</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// TODO 第五次调用后置处理器 ; 第六次.  后置处理器调用了二个</span></span><br><span class="line"><span class="comment">// 构建bean,内部也是调用getBeanPostProcessors方法,获取出一个BeanPostProcessors的集合,然后迭代这个集合,如果是InstantiationAwareBeanPostProcessor,也就是说有实现这个接口,就会调用postProcessAfterInstantiation方法,当然这个方法也是不一定要重写的,因为方法是有default来修饰的,默认时返回的true.如何返回的是false的话,就会直接跳出这个方法.       </span></span><br><span class="line"><span class="comment">//如果是有Init方法,其内部的hasInstAwareBpps就是true,这个时候就会继续调用getBeanProcessores方法,迭代其集合,如果beanPostProcessor是InstantiationAwareBeanPostProcessor的话,就会进行强转,调用postProcessPropertyValues方法,如果返回的null的话,就会跳出这个方法.这个方法在接口中也是有默认值的,不是强制要一定实现的.   </span></span><br><span class="line">      populateBean(beanName, mbd, instanceWrapper);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// TODO 第七次后置处理器执行 ; 和 第八次. 后置处理器调用了二个</span></span><br><span class="line"><span class="comment">// 这个方法从名字上看,是实例化bean. invokeAwareMethods()方法中,如果bean是Aware接口的子类.然后判断是不是BeanNameAware这个接口,如果是这个接口的子类的话,就会强转调用setBeanName方法.如果是BeanClassLoaderAware子类的话,会先调用当前类的getBeanClassLoader方法,然后调用bean的setBeanClassLoader方法,传入bcl,也就是getBeanClassLoader获取出来的ClassLoader.如果是BeanFactoryAware的话,强转调用setBeanFactory方法,传入AbstractAutowireCapableBeanFactory.this    //接着会走applyBeanPostProcessorsBeforeInitialization方法,这就是调用beanPostProcessor方法。先获取出getBeanProcessors()方法,获取出beanProcessors的集合,然后迭代,每个都会调用postProcessBeforeInitialization()方法,返回回来的Object,如果object的值是null的话,就会直接return。如果不是null的话,就会返回最后一个处理的结果. </span></span><br><span class="line"><span class="comment">//再走invokeInitMethods方法,如果bean是有实现InitializingBean的话,就会强转调用afterPropertiesSet()方法.       </span></span><br><span class="line"><span class="comment">//继续走applyBeanPostProcessorsAfterInitialization()方法,获取出来的beanPostProcessors进行迭代,直接调用BeanPostProcessor的postProcessAfterInitialization方法,如何返回的值是null的话,就return掉这个方法,否则就是返回最后一个.最后返回的值,也就是 exposedObject 了.       </span></span><br><span class="line">      exposedObject = initializeBean(beanName, exposedObject, mbd);</span><br><span class="line">   &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      <span class="keyword">if</span> (ex <span class="keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;</span><br><span class="line">         <span class="keyword">throw</span> (BeanCreationException) ex;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName, <span class="string">&quot;Initialization of bean failed&quot;</span>, ex);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// earlySingletonExposure 的值是true,     </span></span><br><span class="line">   <span class="keyword">if</span> (earlySingletonExposure) &#123;</span><br><span class="line">     <span class="comment">//earlySingletonReference的值是null,所以也就不会走下面的if中去.</span></span><br><span class="line">      <span class="type">Object</span> <span class="variable">earlySingletonReference</span> <span class="operator">=</span> getSingleton(beanName, <span class="literal">false</span>);</span><br><span class="line">      <span class="keyword">if</span> (earlySingletonReference != <span class="literal">null</span>) &#123;</span><br><span class="line">         <span class="keyword">if</span> (exposedObject == bean) &#123;</span><br><span class="line">            exposedObject = earlySingletonReference;</span><br><span class="line">         &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;</span><br><span class="line">            String[] dependentBeans = getDependentBeans(beanName);</span><br><span class="line">            Set&lt;String&gt; actualDependentBeans = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(dependentBeans.length);</span><br><span class="line">            <span class="keyword">for</span> (String dependentBean : dependentBeans) &#123;</span><br><span class="line">               <span class="keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;</span><br><span class="line">                  actualDependentBeans.add(dependentBean);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCurrentlyInCreationException</span>(beanName,</span><br><span class="line">                     <span class="string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; has been injected into other beans [&quot;</span> +</span><br><span class="line">                     StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +</span><br><span class="line">                     <span class="string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;&#x27;getBeanNamesOfType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Register bean as disposable.</span></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">//从这个名字上看,如果有必要的话,就会注销到这个bean.       </span></span><br><span class="line">      registerDisposableBeanIfNecessary(beanName, bean, mbd);</span><br><span class="line">   &#125; <span class="keyword">catch</span> (BeanDefinitionValidationException ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(mbd.getResourceDescription(), beanName, <span class="string">&quot;Invalid destruction signature&quot;</span>, ex);</span><br><span class="line">   &#125;</span><br><span class="line">  <span class="comment">// 走到这里,这个方法就已经走完了.  </span></span><br><span class="line">   <span class="keyword">return</span> exposedObject;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="finishRefresh方法"><a href="#finishRefresh方法" class="headerlink" title="finishRefresh方法"></a>finishRefresh方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finish the refresh of this context, invoking the LifecycleProcessor&#x27;s</span></span><br><span class="line"><span class="comment"> * onRefresh() method and publishing the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> org.springframework.context.event.ContextRefreshedEvent&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">finishRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="comment">// Clear context-level resource caches (such as ASM metadata from scanning).</span></span><br><span class="line"><span class="comment">//清除Reource缓存.org.springframework.core.io.DefaultResourceLoader#clearResourceCaches,也就是走到了这个类里面的这个方法进行清除.    </span></span><br><span class="line">   clearResourceCaches();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Initialize lifecycle processor for this context.</span></span><br><span class="line"><span class="comment">// org.springframework.context.support.AbstractApplicationContext#initLifecycleProcessor,先获取出BeanFactory,如果beanFactory中包含LIFECYCLE_PROCESSOR_BEAN_NAME,就根据LIFECYCLE_PROCESSOR_BEAN_NAME和LifecycleProcessor.class获取出这个bean,并且赋值给this.lifecycleProcessor.  如果beanFactory中是不包含的话,就会new DefaultLifecycleProcessor一个出来,然后调用setBeanFactory方法,给beanFactory赋值进去,this.lifecycleProcessor = defaultProcessor;最后在调用beanFactory.registerSingleton来将这个bean给注册进去.    </span></span><br><span class="line">   initLifecycleProcessor();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Propagate refresh to lifecycle processor first.</span></span><br><span class="line"><span class="comment">//获取出 initLifecycleProcessor 这步骤的lifecycleProcessor,然后调用其 onRefresh 方法.</span></span><br><span class="line"><span class="comment">// onRefresh()方法,startBeans(true)中的集合都是空集合,所以并没有做什么具体的事情.this.running = true;    </span></span><br><span class="line">   getLifecycleProcessor().onRefresh();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Publish the final event.</span></span><br><span class="line"><span class="comment">// 先new一个ContextRefreshedEvent事件出来,可以发现的是,构造函数中是调用了super(source)方法,也就是一直调用到了ApplicationEvent这个类中.</span></span><br><span class="line"><span class="comment">// publishEvent方法:先对传入进入的event进行非空的判断.然后判断是不是 ApplicationEvent,如果是的话,就强转为ApplicationEvent,否则就会new一个PayloadApplicationEvent出来,eventTyp是null的话,就会调用getResolvableType()方法获取eventType. this.earlyApplicationEvents 是null,然后就使用this.applicationEventMulticaster.再接着走 multicastEvent 方法,由于这个里面是没什么listners的,所以是没有具体做什么事情的.    </span></span><br><span class="line">   publishEvent(<span class="keyword">new</span> <span class="title class_">ContextRefreshedEvent</span>(<span class="built_in">this</span>));</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Participate in LiveBeansView MBean, if active.</span></span><br><span class="line"><span class="comment">// String mbeanDomain = applicationContext.getEnvironment().getProperty(MBEAN_DOMAIN_PROPERTY_NAME);返回的mbeanDomain如果不是null的话,才会往里面走.是null的话,也就不会往里面走了.    </span></span><br><span class="line">   LiveBeansView.registerApplicationContext(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="resetCommonCaches方法"><a href="#resetCommonCaches方法" class="headerlink" title="resetCommonCaches方法"></a>resetCommonCaches方法</h3><p>这个方法可以看大是对一个工具类使用的集合进行clear.如果不清除的话,其中有些是再初始化后用不上的,就会浪费内存</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Reset Spring&#x27;s common reflection metadata caches, in particular the</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> ReflectionUtils&#125;, &#123;<span class="doctag">@link</span> AnnotationUtils&#125;, &#123;<span class="doctag">@link</span> ResolvableType&#125;</span></span><br><span class="line"><span class="comment"> * and &#123;<span class="doctag">@link</span> CachedIntrospectionResults&#125; caches.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 4.2</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ReflectionUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> AnnotationUtils#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> ResolvableType#clearCache()</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> CachedIntrospectionResults#clearClassLoader(ClassLoader)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">resetCommonCaches</span><span class="params">()</span> &#123;</span><br><span class="line">   ReflectionUtils.clearCache();</span><br><span class="line">   AnnotationUtils.clearCache();</span><br><span class="line">   ResolvableType.clearCache();</span><br><span class="line">   CachedIntrospectionResults.clearClassLoader(getClassLoader());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot初始化阅读</title>
    <url>//article/SpringBoot_Initalize_source/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-SpringBoot-中-Initializer"><a href="#📝-SpringBoot-中-Initializer" class="headerlink" title="📝 SpringBoot 中 Initializer"></a>📝 <strong>SpringBoot 中 Initializer</strong></h1><p>最近看 SpringBoot 源码,看源码这个这个东西是真的要细心, 当你看到觉得重要的东西的时候,就一定要仔细去看,代码的走向以及走到了那个类中. 只有掌握这些,接下来才会更好理解.</p>
<p> 然后我今天在看的时候, org.springframework.boot.SpringApplication#prepareContext —&gt; org.springframework.boot.SpringApplication#applyInitializers 当debug到这里的时候,就会看到 getInitializers() 方法返回的是一个集合,其中都是以Initializers 结尾的,并且都调用了相应的 initialize 方法. 所以就看下这个地方是要做写什么操作.</p>
<p>DelegatingApplicationContextInitializer</p>
<p>SharedMetadataReaderFactoryContextInitializer</p>
<p>ContextIdApplicationContextInitializer</p>
<p>ConfigurationWarningsApplicationContextInitializer</p>
<p>RSocketPortInfoApplicationContextInitializer</p>
<p>ServerPortInfoApplicationContextInitializer</p>
<p>ConditionEvaluationReportLoggingListener</p>
<p>如果我们没有进行扩展的话,这里就是默认的七个.</p>
<p>其中有五个是在 SpringBoot源码下的 META-INF&#x2F;spring.factories 下的</p>
<p>org.springframework.context.ApplicationListener.</p>
<p>还有二个应该是在之前给添加进来的. 执行顺序是按照上面: 一次从下执行.</p>
<h2 id="DelegatingApplicationContextInitializer"><a href="#DelegatingApplicationContextInitializer" class="headerlink" title="DelegatingApplicationContextInitializer"></a>DelegatingApplicationContextInitializer</h2><p>该方法就是获取 environment 中的 context.initializer.classes 指定的class信息</p>
<p>class信息也是要实现ApplicationContextInitializer接口的,然后实现完了,就会调用 initializer 方法.</p>
<p>也就是说该类,就是实例化配置 context.initializer.classes 中的子类,并且调用其 initializer 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取 Environment</span></span><br><span class="line">		<span class="type">ConfigurableEnvironment</span> <span class="variable">environment</span> <span class="operator">=</span> context.getEnvironment();</span><br><span class="line">        <span class="comment">// context.initializer.classes,从evn中获取 classNames集合信息.</span></span><br><span class="line">        <span class="comment">// 获取出来的classNames是有值的话,就会迭代,</span></span><br><span class="line">        <span class="comment">//挨个调用Class.forName这个方法获取Class信息,最后返回集合回来</span></span><br><span class="line">		List&lt;Class&lt;?&gt;&gt; initializerClasses = getInitializerClasses(environment);</span><br><span class="line">		<span class="keyword">if</span> (!initializerClasses.isEmpty()) &#123;</span><br><span class="line">         <span class="comment">// 获取出来的classNames不是empty的话,就会走这里.</span></span><br><span class="line">         <span class="comment">// 最后调用BeanUtils.instantiateClass(initializerClass)实例化</span></span><br><span class="line">         <span class="comment">//这里实例化的对象,是要实现ApplicationContextInitializer接口的</span></span><br><span class="line">  <span class="comment">//最后可以看到这个方法,DelegatingApplicationContextInitializer#applyInitializers   </span></span><br><span class="line">      <span class="comment">// 可以看到该方法也是先排序,然后调用initialize方法.      </span></span><br><span class="line">			applyInitializerClasses(context, initializerClasses);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="SharedMetadataReaderFactoryContextInitializer"><a href="#SharedMetadataReaderFactoryContextInitializer" class="headerlink" title="SharedMetadataReaderFactoryContextInitializer"></a><strong>SharedMetadataReaderFactoryContextInitializer</strong></h2><p>添加一个 CachingMetadataReaderFactoryPostProcessor 到 ApplicationContext 中去.</p>
<p>添加进去的 PostProcessor是其内部类,并且是私有的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line"> <span class="comment">// 添加一个 CachingMetadataReaderFactoryPostProcessor 到 applicationContext中去       </span></span><br><span class="line">		applicationContext.addBeanFactoryPostProcessor(<span class="keyword">new</span> <span class="title class_">CachingMetadataReaderFactoryPostProcessor</span>());</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ContextIdApplicationContextInitializer"><a href="#ContextIdApplicationContextInitializer" class="headerlink" title="ContextIdApplicationContextInitializer"></a><strong>ContextIdApplicationContextInitializer</strong></h2><p>可以看到该 Initializer 就是对 <a href="http://spring.application.name/">spring.application.name</a> 进行获取,获取不到的话,就使用application默认值给设置到context中, 同时将 ContextId 也给注册到 beanFactory中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">        <span class="comment">// ContextId 中的 id 字段的值,就是spring.application.name 这个字段对应的值</span></span><br><span class="line">        <span class="comment">// 如果没有的话,那么就是对用的值就是application</span></span><br><span class="line">		<span class="type">ContextId</span> <span class="variable">contextId</span> <span class="operator">=</span> getContextId(applicationContext);</span><br><span class="line">        <span class="comment">// 将id给设置到 applicationContext中去</span></span><br><span class="line">		applicationContext.setId(contextId.getId());</span><br><span class="line">        <span class="comment">// 将contextId 注册到 beanFactory 中去.</span></span><br><span class="line">		applicationContext.getBeanFactory().registerSingleton(ContextId.class.getName(), contextId);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ConfigurationWarningsApplicationContextInitializer"><a href="#ConfigurationWarningsApplicationContextInitializer" class="headerlink" title="ConfigurationWarningsApplicationContextInitializer"></a><strong>ConfigurationWarningsApplicationContextInitializer</strong></h2><p>该Initializer 也是添加一个 PostProcessor 到 context中去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line">  <span class="comment">// getCheck返回的一个ChWebServerInitializedEventeck数组,其只有一个ComponentScanPackageCheck类.</span></span><br><span class="line">  <span class="comment">// 然后在new一个PostProcessor给添加到 context 中去      </span></span><br><span class="line">		context.addBeanFactoryPostProcessor(<span class="keyword">new</span> <span class="title class_">ConfigurationWarningsPostProcessor</span>(getChecks()));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RSocketPortInfoApplicationContextInitializer"><a href="#RSocketPortInfoApplicationContextInitializer" class="headerlink" title="RSocketPortInfoApplicationContextInitializer"></a><strong>RSocketPortInfoApplicationContextInitializer</strong></h2><p>该方法主要是添加一个 Listener 到ApplicationContext 中去,其监听的event是 RSocketServerInitializedEvent</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">        <span class="comment">// new Listener中Listener是其内部类:</span></span><br><span class="line">        <span class="comment">// 可以看到 Listener 是有继承 ApplicationListener,</span></span><br><span class="line">        <span class="comment">// 并且监听的是 RSocketServerInitializedEvent 这个Event</span></span><br><span class="line">        <span class="comment">// 然后就将 listener给添加到 applicationContext 中去</span></span><br><span class="line">		applicationContext.addApplicationListener(<span class="keyword">new</span> <span class="title class_">Listener</span>(applicationContext));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ServerPortInfoApplicationContextInitializer"><a href="#ServerPortInfoApplicationContextInitializer" class="headerlink" title="ServerPortInfoApplicationContextInitializer"></a><strong>ServerPortInfoApplicationContextInitializer</strong></h2><p>该类不仅仅是一个 Initializer,而且还是一个 Listener监听器. 主要是用来监听 WebServerInitializedEvent 的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 该类实现了ApplicationContextInitializer和ApplicationListener二个接口	</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ServerPortInfoApplicationContextInitializer</span> <span class="keyword">implements</span></span><br><span class="line">		<span class="title class_">ApplicationContextInitializer</span>&lt;ConfigurableApplicationContext&gt;, ApplicationListener&lt;WebServerInitializedEvent&gt; &#123;</span><br><span class="line">		</span><br><span class="line">		&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">  <span class="comment">// 自己是监听器,监听WebServerInitializedEvent的event. 于是就将自己给添加到applicationContext中去.      </span></span><br><span class="line">		applicationContext.addApplicationListener(<span class="built_in">this</span>);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ConditionEvaluationReportLoggingListener"><a href="#ConditionEvaluationReportLoggingListener" class="headerlink" title="ConditionEvaluationReportLoggingListener"></a><strong>ConditionEvaluationReportLoggingListener</strong></h2><p>该 LoggingListener 虽然也是一个 Initializer, 但是其名字是没有包含任何Initializer的意思,反倒是作为监听器的,而且接受Event并不是其自身,而是内部类来接受,然后调用自己方法来处理Event来.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">        <span class="comment">// 赋值给该类定义的变量 applicationContext</span></span><br><span class="line">		<span class="built_in">this</span>.applicationContext = applicationContext;</span><br><span class="line">   <span class="comment">// 然后new一个ConditionEvaluationReportListener的监听器,添加到applicationContext中去.</span></span><br><span class="line"> <span class="comment">// 而且看到这个该类的匿名类监听器:该监听器是监听所有的event. </span></span><br><span class="line"> <span class="comment">//实测是ConditionEvaluationReportListener在获取event,真实是 ConditionEvaluationReportLoggingListener 在处理event.      </span></span><br><span class="line">		applicationContext.addApplicationListener(<span class="keyword">new</span> <span class="title class_">ConditionEvaluationReportListener</span>());</span><br><span class="line"> <span class="comment">// 如果 application是 GenericApplicationContext或其子类的话,就会先从application中获取出beanFactory来</span></span><br><span class="line"> <span class="comment">//ConditionEvaluationReport.get():判断传入进来的beanFactory中是否包含:autoConfigurationReport  //如果是包含的话,就从其中获取. 如果不包含的话,就自己new一个,然后给注册到beanFactory中去.</span></span><br><span class="line"><span class="comment">// locateParent(): 从beanFactory中获取parentBeanFactory来,如果parentBeanFactory不是null并且包含  autoConfigurationReport 的话,就赋给 report.parent       </span></span><br><span class="line">		<span class="keyword">if</span> (applicationContext <span class="keyword">instanceof</span> GenericApplicationContext) &#123;</span><br><span class="line">			<span class="comment">// Get the report early in case the context fails to load</span></span><br><span class="line">			<span class="built_in">this</span>.report = ConditionEvaluationReport.get(<span class="built_in">this</span>.applicationContext.getBeanFactory());</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-------------------  </span><br><span class="line"> <span class="comment">// 匿名内部监听器: 直接调用类ConditionEvaluationReportLoggingListener的方法   </span></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onApplicationEvent</span><span class="params">(ApplicationEvent event)</span> &#123;</span><br><span class="line">			ConditionEvaluationReportLoggingListener.<span class="built_in">this</span>.onApplicationEvent(event);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>

<h1 id="小战一把-自定义"><a href="#小战一把-自定义" class="headerlink" title="小战一把(自定义)"></a><strong>小战一把(自定义)</strong></h1><p>我们先自己写一个类实现 ApplicationContextInitializer 这个接口. 当然,这个initialize 中可以做的事件还是有点多的,我这里就选择注册一个bean 到 Spring的 BeanFactory 中去吧.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangSelfInializer</span> <span class="keyword">implements</span> <span class="title class_">ApplicationContextInitializer</span>&lt;ConfigurableApplicationContext&gt;, Ordered &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 注册监听器</span></span><br><span class="line"><span class="comment">     * 注册bean到BeanFactory中去.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> applicationContext</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">        applicationContext.getBeanFactory().registerSingleton(<span class="string">&quot;gavinYangInit&quot;</span>,<span class="keyword">new</span> <span class="title class_">InitServiceImpl</span>());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getOrder</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写一个实体类: 可以看到这个类是没什么其他的操作,就是一个方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">InitServiceImpl</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">say</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;GavinYang万年帅&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后我们在启动完成了之后,从 applicationContext 中获取出这个bean来,并且调用这个方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ConfigurableApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> application.run(args);</span><br><span class="line">        <span class="type">InitServiceImpl</span> <span class="variable">contextBean</span> <span class="operator">=</span>(InitServiceImpl) context.getBean(<span class="string">&quot;gavinYangInit&quot;</span>);</span><br><span class="line">        contextBean.say();</span><br></pre></td></tr></table></figure>

<p>最后就可以在控制台看到打印的: GavinYang万年帅</p>
<p>没错,我就是万年帅.</p>
<p>到这里, 自己定义一个就完成了.</p>
<p>除了这种定义方式外,还可以看到 DelegatingApplicationContextInitializer 做的事情,也就是获取配置中context.initializer.classes的信息,然后实例化,在调用 initialize 方法.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>可以看到, SpringBoot 中 在初始化 initialize这步的时候, 很多都是添加 postProcessor 和 listener 到 Spring 中去. 然后我们接着自己定义 initialize , 也就是添加一个对象到 beanFactory 中去. 最后获取出这个bean,然后调用其方法都是ok的.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot中BeanPostProcessor</title>
    <url>//article/SpringBoot_bean_postprocess/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-SpringBoot中BeanPostProcessor"><a href="#📝-SpringBoot中BeanPostProcessor" class="headerlink" title="📝 SpringBoot中BeanPostProcessor"></a>📝 <strong>SpringBoot中BeanPostProcessor</strong></h1><p>BeanPostProcessor 是在Spring中实例化bean的时候使用到的,也是对bean的一种扩展. 理解好 BeanPostProcessor是对理解好 bean 的初始化是很重要的. 每种BeanPostProcessor都有各自不同的用途.</p>
<p>看下 BeanPostProcessor 这个接口,是只有 postProcessBeforeInitialization 和 postProcessAfterInitialization 这二种方法的,并且都是用 default来进行修饰,也就是实现这个接口的时候,可以实现其方法,也可以不用实现其方法的,这就是接口上的方法使用 default 来进行修饰的情况.</p>
<p>ApplicationContextAwareProcessor</p>
<p>WebApplicationContextServletContextAwareProcessor</p>
<p>ConfigurationClassPostProcessor.ImportAwareBeanPostProcessor (内部类)</p>
<p>PostProcessorRegistrationDelegate.BeanPostProcessorChecker(内部类)</p>
<p>ConfigurationPropertiesBindingPostProcessor</p>
<p>WebServerFactoryCustomizerBeanPostProcessor</p>
<p>ErrorPageRegistrarBeanPostProcessor</p>
<p>CommonAnnotationBeanPostProcessor</p>
<p>AutowiredAnnotationBeanPostProcessor</p>
<p>ApplicationListenerDetector</p>
<p>如果没有对 SpringBoot 中的beanPostProcessor 进行扩展什么的话, 那么当你创建bean的时候,应该是十个的样子(当然了,这是基于目前的版本的).</p>
<p>如果你看看这十个 beanPostProcessor,其中有的是直接继承 BeanPostProcessor, 有的是 集成其BeanPostProceesor的子类,但是最顶层的话,就是BeanPostProcessor. 需要结合 doCreateBean这个方法一起看了,当其创建一个 bean 的时候, 是怎么使用这些 beanPostProcessor进行扩展的.</p>
<h2 id="beanPostProcessor-添加"><a href="#beanPostProcessor-添加" class="headerlink" title="beanPostProcessor 添加"></a>beanPostProcessor 添加</h2><p>AbstractBeanFactory#beanPostProcessors 可以看到beanFactory中的beanPostProcessors属性字段,该字段就是对BeanPostProcessor进行存储的集合.</p>
<p>在 refresh() 方法中进行添加 beanPostProcessor进来的 可以看到主要添加 beanPostProcessor进来的地方是 registerBeanPostProcessors 方法. 可以看到这里是添加了 十二个.</p>
<ul>
<li>走完 prepareBeanFactory方法: 添加了 ApplicationContextAwareProceesor和ApplicationListenerDetector</li>
<li>postProcessBeanFactory 走完 : 添加 WebApplicationContextServletContextAwareProceesor</li>
<li>invokeBeanFactoryPostProcessors 走完: 添加 ConfigurationClassPostProcessor$ImportAwareBeanPostProcessor 进来</li>
<li>registerBeanPostProcessors 走完 : PostProcessorRegisrationDelegate$BeanPostProceesorChecker&#x2F;ConfigurationPropertiesBindingPostProcessor&#x2F;AsyncAnnotationBeanPostProcessor&#x2F;WebServerFactoryCustomizerBeanPostProcessor&#x2F;ErrorPageRegistrarBeanPostProcessor&#x2F;CommonAnnotationBeanPostProcessor&#x2F;ScheduleAnnotationBeanPostProcessor&#x2F;ApplicationListenerDetector</li>
</ul>
<h2 id="ApplicationContextAwareProcessor"><a href="#ApplicationContextAwareProcessor" class="headerlink" title="ApplicationContextAwareProcessor"></a><strong>ApplicationContextAwareProcessor</strong></h2><p>该Processor的针对性还是比较强的,主要就是处理 EnvironmentAware&#x2F;EmbeddedValueResolverAware&#x2F;ResourceLoaderAware&#x2F;ApplicationEventPublisherAware&#x2F;MessageSourceAware&#x2F;ApplicationContextAware 这几种情况的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"><span class="comment">// bean 如果不是下面的几种的话,就直接返回. 这也就说明,该processor是只处理下面的</span></span><br><span class="line"><span class="comment">// 是在初始化bean 之前        </span></span><br><span class="line">		<span class="keyword">if</span> (!(bean <span class="keyword">instanceof</span> EnvironmentAware || bean <span class="keyword">instanceof</span> EmbeddedValueResolverAware ||</span><br><span class="line">				bean <span class="keyword">instanceof</span> ResourceLoaderAware || bean <span class="keyword">instanceof</span> ApplicationEventPublisherAware ||</span><br><span class="line">				bean <span class="keyword">instanceof</span> MessageSourceAware || bean <span class="keyword">instanceof</span> ApplicationContextAware))&#123;</span><br><span class="line">			<span class="keyword">return</span> bean;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="type">AccessControlContext</span> <span class="variable">acc</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">		<span class="keyword">if</span> (System.getSecurityManager() != <span class="literal">null</span>) &#123;</span><br><span class="line">			acc = <span class="built_in">this</span>.applicationContext.getBeanFactory().getAccessControlContext();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (acc != <span class="literal">null</span>) &#123;</span><br><span class="line">			AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;</span><br><span class="line">				invokeAwareInterfaces(bean);</span><br><span class="line">				<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">			&#125;, acc);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line"> <span class="comment">// 该方法对bean进行判断,</span></span><br><span class="line"> <span class="comment">//分别处理:EnvironmentAware/EmbeddedValueResolverAware/ResourceLoaderAware</span></span><br><span class="line"> <span class="comment">// ApplicationEventPublisherAware/MessageSourceAware/ApplicationContextAware</span></span><br><span class="line"> <span class="comment">//这六种情况,可以看到要不就是往bean中设置application,要么就是设置embeddedValueResolver,再不然就是Environment           </span></span><br><span class="line">			invokeAwareInterfaces(bean);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="WebApplicationContextServletContextAwareProcessor"><a href="#WebApplicationContextServletContextAwareProcessor" class="headerlink" title="WebApplicationContextServletContextAwareProcessor"></a><strong>WebApplicationContextServletContextAwareProcessor</strong></h2><p>该类继承了org.springframework.web.context.support.ServletContextAwareProcessor,先看其父类重写PostProcessor的方法.</p>
<p>可以看到这个 Processor 主要就是处理 web 环境的. 也就是在有web环境的情况下, 对 bean 是 ServletContextAware和ServletConfigAware的进行处理.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="comment">//getServletContext()方法,先从servletContext中拿,没有从servletConfig中拿,如果都没有的话,那返回的就应该是null了.</span></span><br><span class="line"> <span class="comment">// bean是   ServletContextAware 或者 ServletConfigAware 才会进行处理.    </span></span><br><span class="line"> <span class="comment">//如果bean是ServletContextAware的话,就将servletContext设置到ServletContextAware中去       </span></span><br><span class="line">		<span class="keyword">if</span> (getServletContext() != <span class="literal">null</span> &amp;&amp; bean <span class="keyword">instanceof</span> ServletContextAware) &#123;</span><br><span class="line">			((ServletContextAware) bean).setServletContext(getServletContext());</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 如果 bean 是servletConfig的话,就会将this.servletConfig给设置到 ServletConfigAware中去       </span></span><br><span class="line">		<span class="keyword">if</span> (getServletConfig() != <span class="literal">null</span> &amp;&amp; bean <span class="keyword">instanceof</span> ServletConfigAware) &#123;</span><br><span class="line">			((ServletConfigAware) bean).setServletConfig(getServletConfig());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 没有对 bean 进行处理,直接返回了.</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ImportAwareBeanPostProcessor"><a href="#ImportAwareBeanPostProcessor" class="headerlink" title="ImportAwareBeanPostProcessor"></a><strong>ImportAwareBeanPostProcessor</strong></h2><p>ImportAwareBeanPostProcessor –&gt; InstantiationAwareBeanPostProcessorAdapter –&gt; SmartInstantiationAwareBeanPostProcessor —&gt; SmartInstantiationAwareBeanPostProcessor –&gt; InstantiationAwareBeanPostProcessor —&gt; BeanPostProcessor</p>
<p>可以看到, 该 BeanPostProcessor 其上面的接口和类还是蛮多的. 从该 postProcessor的名字上看,其要做的功能还是很好晓得的,那就是做关于 @Import 这些相关的.</p>
<p>TODO : 后续 debug 进行跟进看</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> PropertyValues <span class="title function_">postProcessProperties</span><span class="params">(<span class="meta">@Nullable</span> PropertyValues pvs, Object bean, String beanName)</span> &#123;</span><br><span class="line">			<span class="comment">// Inject the BeanFactory before AutowiredAnnotationBeanPostProcessor&#x27;s</span></span><br><span class="line">			<span class="comment">// postProcessProperties method attempts to autowire other configuration beans.</span></span><br><span class="line">			<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> EnhancedConfiguration) &#123;</span><br><span class="line">				((EnhancedConfiguration) bean).setBeanFactory(<span class="built_in">this</span>.beanFactory);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> pvs;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line">			<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ImportAware) &#123;</span><br><span class="line">				<span class="type">ImportRegistry</span> <span class="variable">ir</span> <span class="operator">=</span> <span class="built_in">this</span>.beanFactory.getBean(IMPORT_REGISTRY_BEAN_NAME, ImportRegistry.class);</span><br><span class="line">				<span class="type">AnnotationMetadata</span> <span class="variable">importingClass</span> <span class="operator">=</span> ir.getImportingClassFor(ClassUtils.getUserClass(bean).getName());</span><br><span class="line">				<span class="keyword">if</span> (importingClass != <span class="literal">null</span>) &#123;</span><br><span class="line">					((ImportAware) bean).setImportMetadata(importingClass);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> bean;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="BeanPostProcessorChecker"><a href="#BeanPostProcessorChecker" class="headerlink" title="BeanPostProcessorChecker"></a><strong>BeanPostProcessorChecker</strong></h2><p>该 PostProcessor 是直接继承 BeanPostProcessor 的, 所以看这二个重写的方法即可.</p>
<p>可以看到该 BeanPostProcessor是对初始化完了后进行检查,如果满足条件的话,就会打印一句log出来. log里面的主要内容就是,不符合自动代理的条件.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实例化之前是没任何特殊的处理,直接返回	</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> bean;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line"> <span class="comment">// bean 不是 BeanPostProcessor 并且根据beanFactory中包含这个beanName,从beanFactory中获取出bd,并且判断bd的 role 级别是不是 2,如果是的话,isInfrastructureBean返回的就是true，否则就返回false.</span></span><br><span class="line"><span class="comment">//最后beanFactory的 beanPostProcessor是小于beanPostProcessorTargetCount的话,就会打印log       </span></span><br><span class="line">			<span class="keyword">if</span> (!(bean <span class="keyword">instanceof</span> BeanPostProcessor) &amp;&amp; !isInfrastructureBean(beanName) &amp;&amp;</span><br><span class="line">					<span class="built_in">this</span>.beanFactory.getBeanPostProcessorCount() &lt; <span class="built_in">this</span>.beanPostProcessorTargetCount) &#123;</span><br><span class="line">				<span class="keyword">if</span> (logger.isInfoEnabled()) &#123;</span><br><span class="line">					logger.info(<span class="string">&quot;Bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; of type [&quot;</span> + bean.getClass().getName() +</span><br><span class="line">							<span class="string">&quot;] is not eligible for getting processed by all BeanPostProcessors &quot;</span> +</span><br><span class="line">							<span class="string">&quot;(for example: not eligible for auto-proxying)&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> bean;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ConfigurationPropertiesBindingPostProcessor"><a href="#ConfigurationPropertiesBindingPostProcessor" class="headerlink" title="ConfigurationPropertiesBindingPostProcessor"></a><strong>ConfigurationPropertiesBindingPostProcessor</strong></h2><p>该PostProcessor是直接实现了BeanPostProcessor 接口的, 但是该类也实现了ApplicationContextAware和InitializingBean这二个方法的. ApplicationContextAware 是提供application的, InitializingBean 方法是在初始完该bean后,调用afterPropertiesSet进行回调的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//只重写了postProcessBeforeInitialization方法</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line"> <span class="comment">// bind方法:ConfigurationPropertiesBinder#bind,</span></span><br><span class="line"> <span class="comment">// TODO : bind方法的用途       </span></span><br><span class="line">		bind(ConfigurationPropertiesBean.get(<span class="built_in">this</span>.applicationContext, bean, beanName));</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">---------------------------------</span><br><span class="line"><span class="comment">// ConfigurationPropertiesBean 类    </span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> ConfigurationPropertiesBean <span class="title function_">get</span><span class="params">(ApplicationContext applicationContext, Object bean, String beanName)</span> &#123;</span><br><span class="line">       <span class="comment">// 获取 Method</span></span><br><span class="line">		<span class="type">Method</span> <span class="variable">factoryMethod</span> <span class="operator">=</span> findFactoryMethod(applicationContext, beanName);</span><br><span class="line"><span class="comment">// create方法: 先找 ConfigurationProperties 注解,如果是null的话,就直接返回.</span></span><br><span class="line"><span class="comment">// 如果不是null的话,就继续找Validated这个注解的,</span></span><br><span class="line"><span class="comment">//根据Validated是否有这个注解分为:&#123; annotation, validated &#125;/&#123; annotation &#125;这二种数组</span></span><br><span class="line"><span class="comment">//如果factoryMethod不是null的话,就直接获取其返回值.</span></span><br><span class="line"><span class="comment">//否则就是bean.getClass获取出ResolvableType来    </span></span><br><span class="line"> <span class="comment">//最后new一个ConfigurationPropertiesBean出来,直接return出去.   </span></span><br><span class="line">		<span class="keyword">return</span> create(beanName, bean, bean.getClass(), factoryMethod);</span><br><span class="line">	&#125;    </span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Method <span class="title function_">findFactoryMethod</span><span class="params">(ApplicationContext applicationContext, String beanName)</span> &#123;</span><br><span class="line">  <span class="comment">// 先对 applicationContext的类型进行判断      </span></span><br><span class="line">		<span class="keyword">if</span> (applicationContext <span class="keyword">instanceof</span> ConfigurableApplicationContext) &#123;</span><br><span class="line">			<span class="keyword">return</span> findFactoryMethod((ConfigurableApplicationContext) applicationContext, beanName);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Method <span class="title function_">findFactoryMethod</span><span class="params">(ConfigurableListableBeanFactory beanFactory, String beanName)</span> &#123;</span><br><span class="line">        <span class="comment">// beanFactory中如果包含这个beanName</span></span><br><span class="line">		<span class="keyword">if</span> (beanFactory.containsBeanDefinition(beanName)) &#123;</span><br><span class="line">  <span class="comment">// 从 beanFactory 中根据beanName获取出bd来.          </span></span><br><span class="line">			<span class="type">BeanDefinition</span> <span class="variable">beanDefinition</span> <span class="operator">=</span> beanFactory.getMergedBeanDefinition(beanName);</span><br><span class="line">        <span class="comment">// 如果bd是 RootBeanDefinition    </span></span><br><span class="line">			<span class="keyword">if</span> (beanDefinition <span class="keyword">instanceof</span> RootBeanDefinition) &#123;</span><br><span class="line">           <span class="comment">// 调用 bd 获取出Method 对应的 object     </span></span><br><span class="line">				<span class="type">Method</span> <span class="variable">resolvedFactoryMethod</span> <span class="operator">=</span> ((RootBeanDefinition) beanDefinition).getResolvedFactoryMethod();</span><br><span class="line">                <span class="comment">// 获取出来的方法如果不是null的话,就直接返回</span></span><br><span class="line">				<span class="keyword">if</span> (resolvedFactoryMethod != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">return</span> resolvedFactoryMethod;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">    <span class="comment">// 从方法的名字上看就是:通过反射获取出方法        </span></span><br><span class="line">			<span class="keyword">return</span> findFactoryMethodUsingReflection(beanFactory, beanDefinition);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Method <span class="title function_">findFactoryMethodUsingReflection</span><span class="params">(ConfigurableListableBeanFactory beanFactory,</span></span><br><span class="line"><span class="params">			BeanDefinition beanDefinition)</span> &#123;</span><br><span class="line">      <span class="comment">// 获取方法名字  </span></span><br><span class="line">		<span class="type">String</span> <span class="variable">factoryMethodName</span> <span class="operator">=</span> beanDefinition.getFactoryMethodName();</span><br><span class="line">      <span class="comment">// 获取bean名字  </span></span><br><span class="line">		<span class="type">String</span> <span class="variable">factoryBeanName</span> <span class="operator">=</span> beanDefinition.getFactoryBeanName();</span><br><span class="line">      <span class="comment">// 如果二者都是null的话,就直接返回null </span></span><br><span class="line">		<span class="keyword">if</span> (factoryMethodName == <span class="literal">null</span> || factoryBeanName == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">        <span class="comment">// 根据 factoryBeanName 从beanFactory中获取出Class来</span></span><br><span class="line">		Class&lt;?&gt; factoryType = beanFactory.getType(factoryBeanName);</span><br><span class="line">        <span class="comment">//如果class的名字是包含$$的话,就调用super来获取其父类的</span></span><br><span class="line">		<span class="keyword">if</span> (factoryType.getName().contains(ClassUtils.CGLIB_CLASS_SEPARATOR)) &#123;</span><br><span class="line">			factoryType = factoryType.getSuperclass();</span><br><span class="line">		&#125;</span><br><span class="line">		AtomicReference&lt;Method&gt; factoryMethod = <span class="keyword">new</span> <span class="title class_">AtomicReference</span>&lt;&gt;();</span><br><span class="line">		ReflectionUtils.doWithMethods(factoryType, (method) -&gt; &#123;</span><br><span class="line">            <span class="comment">// 如果method的名字和 factoryMethodName的值是一样的话,就添加到factoryMethod中</span></span><br><span class="line">			<span class="keyword">if</span> (method.getName().equals(factoryMethodName)) &#123;</span><br><span class="line">				factoryMethod.set(method);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;);</span><br><span class="line">        <span class="comment">// 从factoryMethod中获取出来</span></span><br><span class="line">		<span class="keyword">return</span> factoryMethod.get();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="WebServerFactoryCustomizerBeanPostProcessor"><a href="#WebServerFactoryCustomizerBeanPostProcessor" class="headerlink" title="WebServerFactoryCustomizerBeanPostProcessor"></a><strong>WebServerFactoryCustomizerBeanPostProcessor</strong></h2><p>该 PostProcessor 是有实现 BeanFactoryAware 的,实现该接口是可以获取到 beanFactory的. 然后 赋值给其自身的参数. 也就是说该类是有一个 beanFactory的.</p>
<p> 可以看到该 postProcessor 主要处理 bean 是 WebServerFactory的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">       <span class="comment">// 只处理 bean 是 WebServerFactory  </span></span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> WebServerFactory) &#123;</span><br><span class="line">            <span class="comment">// TODO</span></span><br><span class="line">			postProcessBeforeInitialization((WebServerFactory) bean);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ErrorPageRegistrarBeanPostProcessor"><a href="#ErrorPageRegistrarBeanPostProcessor" class="headerlink" title="ErrorPageRegistrarBeanPostProcessor"></a><strong>ErrorPageRegistrarBeanPostProcessor</strong></h2><p>该 PostProcessor 也是实现了 BeanFactoryAware 接口的,所以其内部也是有一个 ListableBeanFactory 的.</p>
<p>可以看到该 PostProcessor 主要就是 设置 ErrorPage 的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="comment">// bean 是 ErrorPageRegistry就处理 </span></span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ErrorPageRegistry) &#123;</span><br><span class="line"><span class="comment">// 该类内部维护了List&lt;ErrorPageRegistrar&gt; registrars</span></span><br><span class="line"><span class="comment">// 该方法先判断 registrars 是不是有值,如果没有的话,就从beanFactory中获取,</span></span><br><span class="line"><span class="comment">// 根据 ErrorPageRegistrar.class 来获取, 排序 ,最后赋值给 自己定义的 registrars </span></span><br><span class="line"><span class="comment">// 然后迭代 registrars 这个集合并且调用其registerErrorPages方法,传入进去的是registry </span></span><br><span class="line"> <span class="comment">/**  ErrorPageRegistrar 的 子类就是:ErrorPageCustomizer(ErrorMvcAutoConfiguration.ErrorPageCustomizer),可以看到这个类的registerErrorPages方法就是new了一个ErrorPage,然后给添加到ErrorPageRegistry中去,</span></span><br><span class="line"><span class="comment"> 类:ErrorPageCustomizer也是在 ErrorMvcAutoConfiguration </span></span><br><span class="line"><span class="comment">    这个类通过 <span class="doctag">@Bean</span> 注解注入到Spring中.</span></span><br><span class="line"><span class="comment"> */</span>           </span><br><span class="line">			postProcessBeforeInitialization((ErrorPageRegistry) bean);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="CommonAnnotationBeanPostProcessor"><a href="#CommonAnnotationBeanPostProcessor" class="headerlink" title="CommonAnnotationBeanPostProcessor"></a><strong>CommonAnnotationBeanPostProcessor</strong></h2><p> 该类继承了 InitDestroyAnnotationBeanPostProcessor , 实现了InstantiationAwareBeanPostProcessor&#x2F;BeanFactoryAware 接口,</p>
<p> 实现了 BeanFactoryAware 接口,就是相当于获取到了 beanFactory,可以看到下面的setBeanFactory方法.</p>
<p> 该PostProcessor 主要是处理一些 @Resource注解,其父类处理@PostConstruct和@PreDestroy注解修饰的方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setBeanFactory</span><span class="params">(BeanFactory beanFactory)</span> &#123;</span><br><span class="line">        <span class="comment">// 也是对 beanFactory进行了不为null的判断</span></span><br><span class="line">		Assert.notNull(beanFactory, <span class="string">&quot;BeanFactory must not be null&quot;</span>);</span><br><span class="line">        <span class="comment">// 赋值给 beanFactory</span></span><br><span class="line">		<span class="built_in">this</span>.beanFactory = beanFactory;</span><br><span class="line">        <span class="comment">// 如果 resourceFactory 是 null的话,就给beanFactory 赋值给他</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">this</span>.resourceFactory == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="built_in">this</span>.resourceFactory = beanFactory;</span><br><span class="line">		&#125;</span><br><span class="line">        <span class="comment">// beanFactory是ConfigurableBeanFactory的话,就new一个EmbeddedValueResolver</span></span><br><span class="line">        <span class="comment">// 然后new出来的给赋值给embeddedValueResolver</span></span><br><span class="line">		<span class="keyword">if</span> (beanFactory <span class="keyword">instanceof</span> ConfigurableBeanFactory) &#123;</span><br><span class="line">			<span class="built_in">this</span>.embeddedValueResolver = <span class="keyword">new</span> <span class="title class_">EmbeddedValueResolver</span>((ConfigurableBeanFactory) beanFactory);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;		</span><br><span class="line"></span><br><span class="line">-----------------------------------</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessMergedBeanDefinition</span><span class="params">(RootBeanDefinition beanDefinition, Class&lt;?&gt; beanType, String beanName)</span> &#123;</span><br><span class="line">       <span class="comment">// 调用父类的方法,也就是InitDestroyAnnotationBeanPostProcessor</span></span><br><span class="line">	  <span class="comment">//  父类的该方法: 找出有被@PostConstruct和@PreDestroy注解修饰的方法	    </span></span><br><span class="line">		<span class="built_in">super</span>.postProcessMergedBeanDefinition(beanDefinition, beanType, beanName);</span><br><span class="line">    <span class="comment">// 这里也是找被@Resource注解修饰的</span></span><br><span class="line">		<span class="type">InjectionMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> findResourceMetadata(beanName, beanType, <span class="literal">null</span>);</span><br><span class="line">		metadata.checkConfigMembers(beanDefinition);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">-------------------------------------------</span><br><span class="line"><span class="comment">// 处理 @Resource 注解的    </span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> PropertyValues <span class="title function_">postProcessProperties</span><span class="params">(PropertyValues pvs, Object bean, String beanName)</span> &#123;</span><br><span class="line">    <span class="comment">// 找到bean中有被@Resource修饰的属性和方法.</span></span><br><span class="line">		<span class="type">InjectionMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> findResourceMetadata(beanName, bean.getClass(), pvs);</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 有的话,就注入到 bean 中来.</span></span><br><span class="line">			metadata.inject(bean, beanName, pvs);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName, <span class="string">&quot;Injection of resource dependencies failed&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> pvs;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>其父类 InitDestroyAnnotationBeanPostProcessor</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 该方法是直接实现了 BeanPostProcessor 中的方法</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="type">LifecycleMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> findLifecycleMetadata(bean.getClass());</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 可以看到 invoke init methods 方法,也就是直接init方法.</span></span><br><span class="line">            <span class="comment">// 被@PostConstruct注解修饰的方法</span></span><br><span class="line">			metadata.invokeInitMethods(bean, beanName);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (InvocationTargetException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName, <span class="string">&quot;Invocation of init method failed&quot;</span>, ex.getTargetException());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName, <span class="string">&quot;Failed to invoke init method&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 该方法是重写了接口DestructionAwareBeanPostProcessor的,也就是销毁之前调用</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeforeDestruction</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">		<span class="type">LifecycleMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> findLifecycleMetadata(bean.getClass());</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// invoke destroy methods : 执行destroy方法</span></span><br><span class="line">			metadata.invokeDestroyMethods(bean, beanName);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (InvocationTargetException ex) &#123;</span><br><span class="line">			<span class="type">String</span> <span class="variable">msg</span> <span class="operator">=</span> <span class="string">&quot;Destroy method on bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; threw an exception&quot;</span>;</span><br><span class="line">			<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">				logger.warn(msg, ex.getTargetException());</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				logger.warn(msg + <span class="string">&quot;: &quot;</span> + ex.getTargetException());</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			logger.warn(<span class="string">&quot;Failed to invoke destroy method on bean with name &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27;&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="AutowiredAnnotationBeanPostProcessor"><a href="#AutowiredAnnotationBeanPostProcessor" class="headerlink" title="AutowiredAnnotationBeanPostProcessor"></a><strong>AutowiredAnnotationBeanPostProcessor</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> PropertyValues <span class="title function_">postProcessProperties</span><span class="params">(PropertyValues pvs, Object bean, String beanName)</span> &#123;</span><br><span class="line">        <span class="comment">// 找到@Autowird/@Value/@Inject注解修饰的方法和字段</span></span><br><span class="line">		<span class="type">InjectionMetadata</span> <span class="variable">metadata</span> <span class="operator">=</span> findAutowiringMetadata(beanName, bean.getClass(), pvs);</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 注入到该bean中来.</span></span><br><span class="line">			metadata.inject(bean, beanName, pvs);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (BeanCreationException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> ex;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName, <span class="string">&quot;Injection of autowired dependencies failed&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> pvs;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">--------------------</span><br><span class="line">    <span class="comment">// @Autowird 是可以使用在构造方法上,不一定只是字段嘛.所以这个构造方法是处理注解修饰在方法上的</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">public</span> Constructor&lt;?&gt;[] determineCandidateConstructors(Class&lt;?&gt; beanClass, <span class="keyword">final</span> String beanName)</span><br><span class="line">			<span class="keyword">throws</span> BeanCreationException &#123;</span><br><span class="line">    </span><br><span class="line">    ..................</span><br><span class="line">    		Constructor&lt;?&gt;[] candidateConstructors = <span class="built_in">this</span>.candidateConstructorsCache.get(beanClass);</span><br><span class="line">		<span class="keyword">if</span> (candidateConstructors == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="comment">// Fully synchronized resolution now...</span></span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="built_in">this</span>.candidateConstructorsCache) &#123;</span><br><span class="line">				candidateConstructors = <span class="built_in">this</span>.candidateConstructorsCache.get(beanClass);</span><br><span class="line">				<span class="keyword">if</span> (candidateConstructors == <span class="literal">null</span>) &#123;</span><br><span class="line">					Constructor&lt;?&gt;[] rawCandidates;</span><br><span class="line">					<span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="comment">// 获取出该类的全部的构造方法</span></span><br><span class="line">						rawCandidates = beanClass.getDeclaredConstructors();</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">						<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BeanCreationException</span>(beanName,</span><br><span class="line">								<span class="string">&quot;Resolution of declared constructors on bean Class [&quot;</span> + beanClass.getName() +</span><br><span class="line">								<span class="string">&quot;] from ClassLoader [&quot;</span> + beanClass.getClassLoader() + <span class="string">&quot;] failed&quot;</span>, ex);</span><br><span class="line">					&#125;</span><br><span class="line">					List&lt;Constructor&lt;?&gt;&gt; candidates = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(rawCandidates.length);</span><br><span class="line">					Constructor&lt;?&gt; requiredConstructor = <span class="literal">null</span>;</span><br><span class="line">					Constructor&lt;?&gt; defaultConstructor = <span class="literal">null</span>;</span><br><span class="line">					Constructor&lt;?&gt; primaryConstructor = BeanUtils.findPrimaryConstructor(beanClass);</span><br><span class="line">					<span class="type">int</span> <span class="variable">nonSyntheticConstructors</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">                    <span class="comment">// 对全部的构造方法进行迭代,获取出被@Autowird注解修饰的</span></span><br><span class="line">					<span class="keyword">for</span> (Constructor&lt;?&gt; candidate : rawCandidates) &#123;&#125;</span><br><span class="line">                    <span class="comment">// 找到了 @Autowird 修饰的就使用</span></span><br><span class="line">     					<span class="keyword">if</span> (!candidates.isEmpty()) &#123;</span><br><span class="line">........</span><br><span class="line">					<span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 没有找到的话,就使用默认的</span></span><br><span class="line">						candidateConstructors = <span class="keyword">new</span> <span class="title class_">Constructor</span>&lt;?&gt;[<span class="number">0</span>];</span><br><span class="line">					&#125;               </span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ApplicationListenerDetector"><a href="#ApplicationListenerDetector" class="headerlink" title="ApplicationListenerDetector"></a><strong>ApplicationListenerDetector</strong></h2><p>可以看到该类自己内部维护了一个 application 和 singletonNames(集合) 的.</p>
<p>该BeanPostProcess 可以看到初始完后调用的回调方法,根据beanName获取出true的哈,就往application中添加ApplicationListener进来.</p>
<p>销毁之前,也是对application中ApplicationListener进行remove</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实例化完 bean 后调用的方法</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line">        <span class="comment">// bean 是 ApplicationListener才会走到这里来的</span></span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ApplicationListener) &#123;</span><br><span class="line">			<span class="comment">// potentially not detected as a listener by getBeanNamesForType retrieval</span></span><br><span class="line">            <span class="comment">// 从自己维护的集合中根据beanName获取出来值</span></span><br><span class="line">			<span class="type">Boolean</span> <span class="variable">flag</span> <span class="operator">=</span> <span class="built_in">this</span>.singletonNames.get(beanName);</span><br><span class="line">            <span class="comment">// 如果是true的话,就往 applicationContext 中添加一个 listener</span></span><br><span class="line">			<span class="keyword">if</span> (Boolean.TRUE.equals(flag)) &#123;</span><br><span class="line">				<span class="comment">// singleton bean (top-level or inner): register on the fly</span></span><br><span class="line">				<span class="built_in">this</span>.applicationContext.addApplicationListener((ApplicationListener&lt;?&gt;) bean);</span><br><span class="line">			&#125; <span class="keyword">else</span> <span class="keyword">if</span> (Boolean.FALSE.equals(flag)) &#123;</span><br><span class="line">             <span class="comment">// 如果是false的话,判断log级别和application是不是包含这个beanName,如果不包含的话,就会点出warn级别的log来   </span></span><br><span class="line">				<span class="keyword">if</span> (logger.isWarnEnabled() &amp;&amp; !<span class="built_in">this</span>.applicationContext.containsBean(beanName)) &#123;</span><br><span class="line">					<span class="comment">// inner bean with other scope - can&#x27;t reliably process events</span></span><br><span class="line">					logger.warn(<span class="string">&quot;Inner bean &#x27;&quot;</span> + beanName + <span class="string">&quot;&#x27; implements ApplicationListener interface &quot;</span> +</span><br><span class="line">							<span class="string">&quot;but is not reachable for event multicasting by its containing ApplicationContext &quot;</span> +</span><br><span class="line">							<span class="string">&quot;because it does not have singleton scope. Only top-level listener beans are allowed &quot;</span> +</span><br><span class="line">							<span class="string">&quot;to be of non-singleton scope.&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">                <span class="comment">// 从自己维护的 集合中remove走</span></span><br><span class="line">				<span class="built_in">this</span>.singletonNames.remove(beanName);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 在调用销毁之前执行,</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">postProcessBeforeDestruction</span><span class="params">(Object bean, String beanName)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ApplicationListener) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 从application中获取出 ApplicationEventMulticaster来        </span></span><br><span class="line">				<span class="type">ApplicationEventMulticaster</span> <span class="variable">multicaster</span> <span class="operator">=</span> <span class="built_in">this</span>.applicationContext.getApplicationEventMulticaster();</span><br><span class="line">       <span class="comment">// ApplicationEventMulticaster中移除ApplicationListener</span></span><br><span class="line">       <span class="comment">// 并且移除 beanName         </span></span><br><span class="line">				multicaster.removeApplicationListener((ApplicationListener&lt;?&gt;) bean);</span><br><span class="line">				multicaster.removeApplicationListenerBean(beanName);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (IllegalStateException ex) &#123;</span><br><span class="line">				<span class="comment">// ApplicationEventMulticaster not initialized yet - no need to remove a listener</span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p><code>除了SpringBoot中没有做任何配置中有的这些之外,肯定是还有很多其他的.</code></p>
<p> 当你在debug的时候,你会发现,有一些 processor 在初始化之前的名字 :  比如:org.springframework.context.annotation.internalAutowiredAnnotationProcessor ,其实它就是一个AutowiredAnnotationProcessor才是正确的,但是该名字前面却是有 internal 这个单词的,也就是内部的意思. 所以从名字上看,还是可以通过名字进行初始化区分的.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot构造方法阅读</title>
    <url>//article/SpringBoot_init_source/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-SpringBoot构造函数"><a href="#📝-SpringBoot构造函数" class="headerlink" title="📝 SpringBoot构造函数"></a>📝 SpringBoot<strong>构造函数</strong></h1><p> SpringBoot可以说说目前Java人员都在使用的框架. 但是如果是仅仅停留在使用程度的话,那么还是比较可惜的.可以适当的阅读一下其底层代码是怎么实现的. 就其单Spring框架自身,阅读起来或者说是对其执行代码起来,还是需要大量的时间去理解的. 但是这不妨碍我们可以多阅读几遍,对其加强理解.</p>
<h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a><strong>构造方法</strong></h2><p>这是简单的启动一个 SpringBoot 项目. 这里先不看run方法,我们就先看这个 new SpringApplication 里面是怎么走的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">SpringApplication</span> <span class="variable">application</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SpringApplication</span>(BootSourceLearnApplication.class);</span><br><span class="line">        <span class="type">ConfigurableApplicationContext</span> <span class="variable">applicationContext</span> <span class="operator">=</span> application.run(args);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// SpringApplication.run(BootSourceLearnApplication.class, args);</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// DispatcherServlet</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@link</span> SpringApplication&#125; instance. The application context will load</span></span><br><span class="line"><span class="comment">	 * beans from the specified primary sources (see &#123;<span class="doctag">@link</span> SpringApplication class-level&#125;</span></span><br><span class="line"><span class="comment">	 * documentation for details. The instance can be customized before calling</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@link</span> #run(String...)&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> primarySources the primary bean sources</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #run(Class, String[])</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #SpringApplication(ResourceLoader, Class...)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #setSources(Set)</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">SpringApplication</span><span class="params">(Class&lt;?&gt;... primarySources)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>(<span class="literal">null</span>, primarySources);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Create a new &#123;<span class="doctag">@link</span> SpringApplication&#125; instance. The application context will load</span></span><br><span class="line"><span class="comment">	 * beans from the specified primary sources (see &#123;<span class="doctag">@link</span> SpringApplication class-level&#125;</span></span><br><span class="line"><span class="comment">	 * documentation for details. The instance can be customized before calling</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@link</span> #run(String...)&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> resourceLoader the resource loader to use</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> primarySources the primary bean sources</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #run(Class, String[])</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #setSources(Set)</span></span><br><span class="line"><span class="comment">	 传入进来的参数 : </span></span><br><span class="line"><span class="comment">	 可以传入进来的 resourceLoader的值是null,</span></span><br><span class="line"><span class="comment">	 primarySources :  BootSourceLearnApplication.class </span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@SuppressWarnings(&#123; &quot;unchecked&quot;, &quot;rawtypes&quot; &#125;)</span></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">SpringApplication</span><span class="params">(ResourceLoader resourceLoader, Class&lt;?&gt;... primarySources)</span> &#123;</span><br><span class="line">        <span class="comment">// this.resourceLoader的值在这里目前也是 null.</span></span><br><span class="line">		<span class="built_in">this</span>.resourceLoader = resourceLoader;</span><br><span class="line">        <span class="comment">// 对传入进来的 primarySources参数进行检验,如果是null的话,这里是会有异常抛出来的,当然了,有异常的话,那么程序在这里自然也就会终止了.</span></span><br><span class="line">		Assert.notNull(primarySources, <span class="string">&quot;PrimarySources must not be null&quot;</span>);</span><br><span class="line">        <span class="comment">// 将primarySources传入LinkedHashSet的集合中,并且赋值给this.primarySources.</span></span><br><span class="line">		<span class="built_in">this</span>.primarySources = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(Arrays.asList(primarySources));</span><br><span class="line">        <span class="comment">// org.springframework.boot.WebApplicationType , 枚举类, 这里返回的是 SERVLET</span></span><br><span class="line">		<span class="built_in">this</span>.webApplicationType = WebApplicationType.deduceFromClasspath();</span><br><span class="line">        <span class="comment">// 返回回来的 instances 集合 赋值给 this.initializers 参数</span></span><br><span class="line">		setInitializers((Collection) getSpringFactoriesInstances(ApplicationContextInitializer.class));</span><br><span class="line">        <span class="comment">// 这个与上面的 getSpringFactoriesInstances(ApplicationContextInitializer.class) 是一样的,只是去根据条件传入的参数值不一样,自然其获取出来的值也是不一样的,只不过的是,这步我们是从cache中获取出来的,也就是说,其实在上一步调用这个方法的时候,就已经将这次的值也给获取出来了,并且放入到了cache的缓存Map中,所以这次获取是直接从cache中获取出来的.  然后赋值给 this.listeners 参数</span></span><br><span class="line">		setListeners((Collection) getSpringFactoriesInstances(ApplicationListener.class));</span><br><span class="line"> <span class="comment">// 这里根据 &quot;main&quot;.equals(stackTraceElement.getMethodName())进行判断获取 MainApplicationClass. 这里我们就已经将启动类的构造函数给看完了.</span></span><br><span class="line">		<span class="built_in">this</span>.mainApplicationClass = deduceMainApplicationClass();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="setInitializers-Collection-getSpringFactoriesInstances-ApplicationContextInitizlier-class-方法"><a href="#setInitializers-Collection-getSpringFactoriesInstances-ApplicationContextInitizlier-class-方法" class="headerlink" title="setInitializers((Collection) getSpringFactoriesInstances(ApplicationContextInitizlier.class)) 方法"></a>setInitializers((Collection) getSpringFactoriesInstances(ApplicationContextInitizlier.class)) 方法</h2><p>该方法先走 getSpringFactoriesInstances()方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> &lt;T&gt; Collection&lt;T&gt; <span class="title function_">getSpringFactoriesInstances</span><span class="params">(Class&lt;T&gt; type)</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> getSpringFactoriesInstances(type, <span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[] &#123;&#125;);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> &lt;T&gt; Collection&lt;T&gt; <span class="title function_">getSpringFactoriesInstances</span><span class="params">(Class&lt;T&gt; type, Class&lt;?&gt;[] parameterTypes, Object... args)</span> &#123;</span><br><span class="line">        <span class="comment">// 返回的是ClassUtils.getDefaultClassLoader():也就是默认的类加载器</span></span><br><span class="line">		<span class="type">ClassLoader</span> <span class="variable">classLoader</span> <span class="operator">=</span> getClassLoader();</span><br><span class="line">		<span class="comment">// Use names and ensure unique to protect against duplicates</span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.ConfigurationWarningsApplicationContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.ContextIdApplicationContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.config.DelegatingApplicationContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.rsocket.context.RSocketPortInfoApplicationContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.web.context.ServerPortInfoApplicationContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.autoconfigure.SharedMetadataReaderFactoryContextInitializer</span></span><br><span class="line"><span class="comment">//org.springframework.boot.autoconfigure.logging.ConditionEvaluationReportLoggingListener //这里从META-INF/spring.factories里面读取出来的内容,其names对应的值就是上面这么多.       </span></span><br><span class="line">		Set&lt;String&gt; names = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(SpringFactoriesLoader.loadFactoryNames(type, classLoader));</span><br><span class="line"><span class="comment">// createSpringFactoriesInstances : 从方法名字上看,是要创建spring.factories里面的实例.       </span></span><br><span class="line">		List&lt;T&gt; instances = createSpringFactoriesInstances(type, parameterTypes, classLoader, args, names);</span><br><span class="line"> <span class="comment">// 然后将返回回来的instances排序下       </span></span><br><span class="line">		AnnotationAwareOrderComparator.sort(instances);</span><br><span class="line">		<span class="keyword">return</span> instances;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Load the fully qualified class names of factory implementations of the</span></span><br><span class="line"><span class="comment">	 * given type from &#123;<span class="doctag">@value</span> #FACTORIES_RESOURCE_LOCATION&#125;, using the given</span></span><br><span class="line"><span class="comment">	 * class loader.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> factoryType the interface or abstract class representing the factory</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> classLoader the ClassLoader to use for loading resources; can be</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@code</span> null&#125; to use the default</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@throws</span> IllegalArgumentException if an error occurs while loading factory names</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #loadFactories</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> List&lt;String&gt; <span class="title function_">loadFactoryNames</span><span class="params">(Class&lt;?&gt; factoryType, <span class="meta">@Nullable</span> ClassLoader classLoader)</span> &#123;</span><br><span class="line">        <span class="comment">// factoryTypeName : org.springframework.context.ApplicationContextInitializer</span></span><br><span class="line">        <span class="comment">// </span></span><br><span class="line">		<span class="type">String</span> <span class="variable">factoryTypeName</span> <span class="operator">=</span> factoryType.getName();</span><br><span class="line">        <span class="comment">// loadSpringFactories(classLoader) 获取 META-INF/spring.factories下的一些配置信息,然后读取的内容将其封装成一个集合, getOrDefault方法,如果获取出来,有的话,就是用有的,没有的话,就返回一个空的集合.</span></span><br><span class="line">		<span class="keyword">return</span> loadSpringFactories(classLoader).getOrDefault(factoryTypeName, Collections.emptyList());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建上面从spring.factroies中读取出来的内容. classLoader是在之前有获取的,是默认的.</span></span><br><span class="line"></span><br><span class="line">	<span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">	<span class="keyword">private</span> &lt;T&gt; List&lt;T&gt; <span class="title function_">createSpringFactoriesInstances</span><span class="params">(Class&lt;T&gt; type, Class&lt;?&gt;[] parameterTypes,</span></span><br><span class="line"><span class="params">			ClassLoader classLoader, Object[] args, Set&lt;String&gt; names)</span> &#123;</span><br><span class="line">		List&lt;T&gt; instances = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(names.size());</span><br><span class="line">		<span class="keyword">for</span> (String name : names) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 根据name,调用ClassUtils.forName来实例化,也就是通过反射的方法 </span></span><br><span class="line">				Class&lt;?&gt; instanceClass = ClassUtils.forName(name, classLoader);</span><br><span class="line">				Assert.isAssignable(type, instanceClass);</span><br><span class="line">                <span class="comment">// 获取起无参数构造函数</span></span><br><span class="line">				Constructor&lt;?&gt; constructor = instanceClass.getDeclaredConstructor(parameterTypes);</span><br><span class="line">                <span class="comment">// 这里才是真正的实例化了这个对象,根据构造函数</span></span><br><span class="line">				<span class="type">T</span> <span class="variable">instance</span> <span class="operator">=</span> (T) BeanUtils.instantiateClass(constructor, args);</span><br><span class="line">                <span class="comment">// 然后放入一个集合中</span></span><br><span class="line">				instances.add(instance);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Cannot instantiate &quot;</span> + type + <span class="string">&quot; : &quot;</span> + name, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> instances;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="添加-ApplicationContextInitializer-子类"><a href="#添加-ApplicationContextInitializer-子类" class="headerlink" title="添加 ApplicationContextInitializer 子类"></a><strong>添加 ApplicationContextInitializer 子类</strong></h2><p>来手动添加一个 ApplicationContextInitializer 的子类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.yang.bootsourcelearn.init;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.context.ApplicationContextInitializer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.ConfigurableApplicationContext;</span><br><span class="line"></span><br><span class="line"><span class="comment">/***********************************************************************</span></span><br><span class="line"><span class="comment"> *&lt;PRE&gt;</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  File Name       : </span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  Creation Date   : 20-6-25</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  Author          : Gavin</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  Purpose         : </span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  History         : </span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *&lt;/PRE&gt;</span></span><br><span class="line"><span class="comment"> ***************************************************************************/</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangApplicationContextInitializer</span> <span class="keyword">implements</span> <span class="title class_">ApplicationContextInitializer</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">GavinYangApplicationContextInitializer</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;GavinYang Init Bean&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后在 resources 目录下面创建一个 META-INF 目录 , 再在 META-INF目录下创建一个 spring.factories 文件, 然后文件里面的内容我们就仿照SpringBoot里面的. 也给他加上一行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Application Context Initializersorg.springframework.context.ApplicationContextInitializer</span><br><span class="line">=\com.yang.bootsourcelearn.init.GavinYangApplicationContextInitializer</span><br></pre></td></tr></table></figure>

<p>然后将判断给打到 setInitializers() 这个方法,根据去, 跟到断点: org.springframework.core.io.support.SpringFactoriesLoader#loadSpringFactories 到这里, 然后看 result 中,</p>
<p>org.springframework.context.ApplicationContextInitializer -&gt; {LinkedList@1641} size &#x3D; 8 , 这个 LinkedList中就有我们自定义的 com.yang.bootsourcelearn.init.GavinYangApplicationContextInitializer 的方法,</p>
<p>紧接着走完 createSpringFactoriesInstances 方法, 就可以看到我们无参构造函数中打印的 GavinYang Init Bean . 所以自顶一个还是很简单的. 由于我们不需要特别加什么,所以就没有对 重写的 initialize 进行什么添加.</p>
<p>同样的 ApplicationListener.class , 我们也可以使用这样的方法进行自定义.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<p>SpringApplication 构造函数方法,还是很好理解的, 代码阅读起来也很明白.</p>
<p>1 : 对参数的检验和判断不是null,然后转化为集合,赋值给其属性.</p>
<p>2 : 判断一些 WebApplicationType的类型. SERVLET. 当然Spring5好像引入了一个 REACTIVE这个,可能很早之前就有了,因为我个人也是看Spring5的新特性才看到的这个.</p>
<p>3 : 根据 ApplicationContextInitializer.class 和 ApplicationListener.class , 从 META-INF&#x2F;spring.factories 文件里面读取出对应的内容(这里的内容也就是一些类的全限定名字,然后分好类的),并且对其进行实例化.</p>
<p>4 : 最后获取一下启动类的class值,将其赋值给其内部的变量 mainApplicationClass .</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot的run方法阅读</title>
    <url>//article/SpringBoot_run_source/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-SpringBoot-之-run-方法启动"><a href="#📝-SpringBoot-之-run-方法启动" class="headerlink" title="📝 SpringBoot 之 run 方法启动"></a>📝 <strong>SpringBoot 之 run 方法启动</strong></h1><p>SpringBoot 无配置式启动,大大的减轻了以前需要配置很多xml中的bean&#x2F;aop&#x2F;事物等. 只需要使用@SpringBootApplication 和 SpringApplication.run() 方法就可以轻松的跑起来了. 想要看怎么这么神奇,就需要看其run() 方法的执行操作.</p>
<p> run() 方法又包含了之前分析的 refresh 方法,但是这里的refresh 方法是要比之前的 单独分析 Spring 的 refresh方法的内容要多得多.</p>
<h1 id="run方法整体"><a href="#run方法整体" class="headerlink" title="run方法整体"></a>run方法整体</h1><p>我们先看下 run 方法整体的流程. 其实也不难看出来, 也是new对象,调用方法. 但是人家可不仅仅是这二种就轻松搞定了的. 而且里面采用了大量的设计模式等知识.</p>
<p>从run 方法整体来看, 先是启动 StopWatch,然后获取出SpringApplicationRunListener对应的Listener,最后封装到SpringApplicationRunListeners这个Object类中, 接着调用这个类的 starting 方法,也就是启动方法.</p>
<p>再往下就是创建 DefaultApplicationArguments,准备环境,然后往System.setProperty给key为”spring.beaninfo.ignore” 设置ignore.toString() 方法进去. 打印Banner(也就是我们看到的SpringBoot，用很多杠杆给拼接起来的)</p>
<p>创建一个AnnotationConfigApplicationContext的Context(我个人觉得context是对beanFactory的一种扩展吧).</p>
<p>接着在 获取 SpringBootExceptionReporter 对应的信息(从META-INF&#x2F;spring.factiories中获取),返回的是一个集合对象.</p>
<p>再就是为context进行准备.</p>
<p>接着就是调用refresh 方法,这个方法会加载beanPostProcssor，初始化bean等操作.</p>
<p>afterRefresh 就是创建完容器之后执行一些额外的操作</p>
<p>stopWatch.stop 就是打印启动计时等信息</p>
<p>再就是执行 listeners.started() 方法, org.springframework.boot.context.event.EventPublishingRunListener#started,也就是new一个ApplicationStartedEvent给传播出去,然后监听器拿到这个事件后,执行相应的逻辑处理关系.</p>
<p>callRunners 获取出 ApplicationRunner和CommandLineRunner,从beanFactory中获取出来,然后调用其run方法</p>
<p>接着就是执行 listeners.running()方法:</p>
<p>org.springframework.boot.context.event.EventPublishingRunListener#running,其内部就是new一个ApplicationReadyEvent事件给传播出去,然后相应的监听器拿到这个事件,就会去执行其内部的逻辑代码.</p>
<p>return 一个 context回去，也就是run方法已经走完了.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Run the Spring application, creating and refreshing a new</span></span><br><span class="line"><span class="comment">	 * &#123;<span class="doctag">@link</span> ApplicationContext&#125;.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@param</span> args the application arguments (usually passed from a Java main method)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> a running &#123;<span class="doctag">@link</span> ApplicationContext&#125;</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> ConfigurableApplicationContext <span class="title function_">run</span><span class="params">(String... args)</span> &#123;</span><br><span class="line"><span class="comment">// 先写一个任务Watch , 可以看到启动时候调用了 start方法, afterRefresh之后又调用了stop方法.</span></span><br><span class="line"><span class="comment">// 这个时候不难想到,我们启动完程序后,就可以看到SpringBoot帮我们打印了一个启动的用时时间,然后想下是不是就可以联想到start和stop这二个方法与之相关.        </span></span><br><span class="line">		<span class="type">StopWatch</span> <span class="variable">stopWatch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StopWatch</span>();</span><br><span class="line">		stopWatch.start();</span><br><span class="line"><span class="comment">//先定义一个Context.        </span></span><br><span class="line">		<span class="type">ConfigurableApplicationContext</span> <span class="variable">context</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="comment">//new一个装SpringBootExceptionReporter对象的集合.然后可以看到这个集合的使用地方,</span></span><br><span class="line"><span class="comment">//都是在catch里面.走的handleRunFailure 方法.	        </span></span><br><span class="line">		Collection&lt;SpringBootExceptionReporter&gt; exceptionReporters = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		configureHeadlessProperty();</span><br><span class="line"><span class="comment">//如果我们没有在META-INF的spring.factories进行什么扩展的定义的话,只会获取下面的一个.</span></span><br><span class="line"><span class="comment">//SpringFactoriesLoader.loadFactoryNames(type, classLoader),该方法是从META-INF/spring.factories中进行读取相应的配置信息等.        </span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.event.EventPublishingRunListener</span></span><br><span class="line"><span class="comment">//最后获取出来的进行createSpringFactoriesInstances方法,也就是实例化.</span></span><br><span class="line"><span class="comment">//最后new一个SpringApplicationRunListeners,其内部封装了log和SpringApplicationRunListener的集合</span></span><br><span class="line">		<span class="type">SpringApplicationRunListeners</span> <span class="variable">listeners</span> <span class="operator">=</span> getRunListeners(args);</span><br><span class="line"><span class="comment">//上一步返回的listeners,这部先迭代,然后将this.application和this.args封装成ApplicationStartingEvent这个对象,然后传播给对应的listeners.</span></span><br><span class="line"><span class="comment">//监听器拿到了这里广播出去的event后,也就会走相应的 onApplicationEvent 的方法逻辑,不同的监听器其走的方法也是不同的,具体可以看到其监听器里面走的逻辑. 这里具体的方法,多少的监听器方法被执行,可自行debug进行查看 </span></span><br><span class="line">		listeners.starting();</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// new 一个启动参数的持有对象,也就是该对象其内部封装了args这个参数.         </span></span><br><span class="line">			<span class="type">ApplicationArguments</span> <span class="variable">applicationArguments</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DefaultApplicationArguments</span>(args);</span><br><span class="line"><span class="comment">//准备一下环境</span></span><br><span class="line">			<span class="type">ConfigurableEnvironment</span> <span class="variable">environment</span> <span class="operator">=</span> prepareEnvironment(listeners, applicationArguments);</span><br><span class="line"><span class="comment">//根据System.getProperty()方法获取spring.beaninfo.ignore对用的值,然后有根据spring.beaninfo.ignore给调用到System.setProperty中去.            </span></span><br><span class="line">			configureIgnoreBeanInfo(environment);</span><br><span class="line"><span class="comment">// 打印 banner , 比如我们看到 的 SpringBoot 那个用很多杠杆给拼成的样子,就是在这里走完之后打印的.</span></span><br><span class="line">			<span class="type">Banner</span> <span class="variable">printedBanner</span> <span class="operator">=</span> printBanner(environment);</span><br><span class="line"><span class="comment">//这里创建ApplicatioContext,这里创建的是默认的:</span></span><br><span class="line"><span class="comment">//org.springframework.context.annotation.AnnotationConfigApplicationContext,使用Class.forName来后去class信息,然后走BeanUtils.instantiateClass(contextClass);创建</span></span><br><span class="line"><span class="comment">//如果看过之前阅读单个Spring的话,不难看出来,我当时启动的总入口类就是这个AnnotationConfigApplicationContext这个类.            </span></span><br><span class="line">			context = createApplicationContext();</span><br><span class="line"><span class="comment">//仔细发现,getRunListeners中也是走的getSpringFactoriesInstances(SpringApplicationRunListener.class, types, this, args),只是这里我们传入的参数不同,也就是重载了.也是返回一个SpringBootExceptionReporter的集合对象.</span></span><br><span class="line">			exceptionReporters = getSpringFactoriesInstances(SpringBootExceptionReporter.class,</span><br><span class="line">					<span class="keyword">new</span> <span class="title class_">Class</span>[] &#123; ConfigurableApplicationContext.class &#125;, context);</span><br><span class="line"><span class="comment">//准备Context。            </span></span><br><span class="line">			prepareContext(context, environment, listeners, applicationArguments, printedBanner);</span><br><span class="line"><span class="comment">// 调用 refresh 方法.            </span></span><br><span class="line">			refreshContext(context);</span><br><span class="line"><span class="comment">// 创建完容器之后的一些额外操作.            </span></span><br><span class="line">			afterRefresh(context, applicationArguments);</span><br><span class="line">			stopWatch.stop();</span><br><span class="line"><span class="comment">// 根据logStartupInfo是否为true/false，来判断接下来是否打印.</span></span><br><span class="line"><span class="comment">// log 是info级别的打印.            </span></span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">this</span>.logStartupInfo) &#123;</span><br><span class="line">				<span class="keyword">new</span> <span class="title class_">StartupInfoLogger</span>(<span class="built_in">this</span>.mainApplicationClass).logStarted(getApplicationLog(), stopWatch);</span><br><span class="line">			&#125;</span><br><span class="line"><span class="comment">//org.springframework.boot.context.event.EventPublishingRunListener#started</span></span><br><span class="line"><span class="comment">//可以看到该方法内部new了一个ApplicationStartedEvent,然后广播出去给相应的监听者执执行相应的逻辑代码.</span></span><br><span class="line">			listeners.started(context);</span><br><span class="line"><span class="comment">//从context中获取出ApplicationRunner和CommandLineRunner,调用其values方法,其值给添加到runners中   </span></span><br><span class="line"><span class="comment">//org.springframework.boot.SpringApplication#callRunner(org.springframework.boot.ApplicationRunner, org.springframework.boot.ApplicationArguments),该方法可以执行相应对象的run方法.</span></span><br><span class="line">			callRunners(context, applicationArguments);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			handleRunFailure(context, ex, exceptionReporters, listeners);</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(ex);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// org.springframework.boot.context.event.EventPublishingRunListener#running</span></span><br><span class="line"><span class="comment">//其new了一个ApplicationReadyEvent，然后广播出去给相应的监听器进行获取，然后监听器拿到了相应的事件就会执行相应的业务逻辑代码            </span></span><br><span class="line">			listeners.running(context);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			handleRunFailure(context, ex, exceptionReporters, <span class="literal">null</span>);</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(ex);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 返回context出去.        </span></span><br><span class="line">		<span class="keyword">return</span> context;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-configureHeadlessProperty-方法"><a href="#run-中-configureHeadlessProperty-方法" class="headerlink" title="run 中 configureHeadlessProperty() 方法"></a><strong>run 中 configureHeadlessProperty() 方法</strong></h2><p>最后是给 java.awt.headless 设置了默认值 headless &#x3D; true, 所以也就是给 SYSTEM_PROPERTY_JAVA_AWT_HEADLESS 在System中设置上了true. 所以这个方法也没有特别重要的地方讲解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">configureHeadlessProperty</span><span class="params">()</span> &#123;</span><br><span class="line">  	System.setProperty(SYSTEM_PROPERTY_JAVA_AWT_HEADLESS,</span><br><span class="line">  			System.getProperty(SYSTEM_PROPERTY_JAVA_AWT_HEADLESS, Boolean.toString(<span class="built_in">this</span>.headless)));</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-getRunListeners-方法"><a href="#run-中-getRunListeners-方法" class="headerlink" title="run 中 getRunListeners() 方法"></a><strong>run 中 getRunListeners() 方法</strong></h2><p>方法依次往下执行的. 可以看出这个方法, 根据 SpringApplicationRunListener.class 从 META-INF&#x2F;spring.factories 中获取并且实例化获取出来的结果. 然后放入到 SpringApplicationRunListeners 这个对象中. 从 META-INF&#x2F;spring.factories中获取出来的数据,是有一个cache缓存的,也就是使用的Map.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> SpringApplicationRunListeners <span class="title function_">getRunListeners</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"><span class="comment">//构造出一个数组,放入了SpringApplication.class和String[].class</span></span><br><span class="line"><span class="comment">//主要看 getSpringFactoriesInstances() 这个方法       </span></span><br><span class="line">		Class&lt;?&gt;[] types = <span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[] &#123; SpringApplication.class, String[].class &#125;;</span><br><span class="line"><span class="comment">// 这里根据getSpringFactoriesInstances返回回来的listeners集合,去new一个SpringApplicationRunListeners,也就是将listeners放入到SpringApplicationRunListeners中的属性.  </span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">SpringApplicationRunListeners</span>(logger,</span><br><span class="line">				getSpringFactoriesInstances(SpringApplicationRunListener.class, types, <span class="built_in">this</span>, args));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> &lt;T&gt; Collection&lt;T&gt; <span class="title function_">getSpringFactoriesInstances</span><span class="params">(Class&lt;T&gt; type, Class&lt;?&gt;[] parameterTypes, Object... args)</span> &#123;</span><br><span class="line"> <span class="comment">// 获取 class加载器       </span></span><br><span class="line">		<span class="type">ClassLoader</span> <span class="variable">classLoader</span> <span class="operator">=</span> getClassLoader();</span><br><span class="line"> <span class="comment">//主要看 SpringFactoriesLoader.loadFactoryNames() 方法，因为该方法是从 META-INF/spring.factories中读取相应的信息. 如果我们没有进行扩展的话,这里就是默认只有一个.</span></span><br><span class="line"><span class="comment">// 当然了,我们是可以在自己项目中创建一个META-INF/spring.factories进行扩展的        </span></span><br><span class="line"><span class="comment">//# Run Listeners</span></span><br><span class="line"><span class="comment">//org.springframework.boot.SpringApplicationRunListener=\</span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.event.EventPublishingRunListener        </span></span><br><span class="line"><span class="comment">// Use names and ensure unique to protect against duplicates</span></span><br><span class="line">		Set&lt;String&gt; names = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(SpringFactoriesLoader.loadFactoryNames(type, classLoader));</span><br><span class="line"> <span class="comment">// 这里要说明的的一点是,instanceClass.getDeclaredConstructor(parameterTypes)走的不是无参数构造函数,而是走的有参数函数,最后是使用的 BeanUtils.instantiateClass(constructor, args) 来初始化这个bean的,最后存放到集合中并且返回回来.       </span></span><br><span class="line">		List&lt;T&gt; instances = createSpringFactoriesInstances(type, parameterTypes, classLoader, args, names);</span><br><span class="line"><span class="comment">// 排序下，返回.        </span></span><br><span class="line">		AnnotationAwareOrderComparator.sort(instances);</span><br><span class="line">		<span class="keyword">return</span> instances;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据 classLoader 来获取相应的相应的信息. 这里就是使用了一个 cache（Map） 来当做一个简单缓存操作.</span></span><br><span class="line"><span class="comment">// 也就是先从map中获取,获取出来有结果就返回,没有结果的话,就看classLoader.getResources/getSystemResource方法从META-INF/spring.factories中获取.然后在return之前就放入到cache这个Map中缓存一下</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> Map&lt;String, List&lt;String&gt;&gt; <span class="title function_">loadSpringFactories</span><span class="params">(<span class="meta">@Nullable</span> ClassLoader classLoader)</span> &#123;</span><br><span class="line">		MultiValueMap&lt;String, String&gt; result = cache.get(classLoader);</span><br><span class="line">		<span class="keyword">if</span> (result != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> result;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			Enumeration&lt;URL&gt; urls = (classLoader != <span class="literal">null</span> ?</span><br><span class="line">					classLoader.getResources(FACTORIES_RESOURCE_LOCATION) :</span><br><span class="line">					ClassLoader.getSystemResources(FACTORIES_RESOURCE_LOCATION));</span><br><span class="line">			result = <span class="keyword">new</span> <span class="title class_">LinkedMultiValueMap</span>&lt;&gt;();</span><br><span class="line">			<span class="keyword">while</span> (urls.hasMoreElements()) &#123;</span><br><span class="line">				<span class="type">URL</span> <span class="variable">url</span> <span class="operator">=</span> urls.nextElement();</span><br><span class="line">				<span class="type">UrlResource</span> <span class="variable">resource</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UrlResource</span>(url);</span><br><span class="line">				<span class="type">Properties</span> <span class="variable">properties</span> <span class="operator">=</span> PropertiesLoaderUtils.loadProperties(resource);</span><br><span class="line">				<span class="keyword">for</span> (Map.Entry&lt;?, ?&gt; entry : properties.entrySet()) &#123;</span><br><span class="line">					<span class="type">String</span> <span class="variable">factoryTypeName</span> <span class="operator">=</span> ((String) entry.getKey()).trim();</span><br><span class="line">					<span class="keyword">for</span> (String factoryImplementationName : StringUtils.commaDelimitedListToStringArray((String) entry.getValue())) &#123;</span><br><span class="line">						result.add(factoryTypeName, factoryImplementationName.trim());</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			cache.put(classLoader, result);</span><br><span class="line">			<span class="keyword">return</span> result;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Unable to load factories from location [&quot;</span> +</span><br><span class="line">					FACTORIES_RESOURCE_LOCATION + <span class="string">&quot;]&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="实战一把"><a href="#实战一把" class="headerlink" title="实战一把"></a>实战一把</h2><p>参考 org.springframework.boot.context.event.EventPublishingRunListener</p>
<p>先写一个类 :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GavinYangEventPublishingRunListener</span> <span class="keyword">implements</span> <span class="title class_">SpringApplicationRunListener</span>, Ordered &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">Logger</span> <span class="variable">LOGGER</span> <span class="operator">=</span> LoggerFactory.getLogger(GavinYangEventPublishingRunListener.class);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> SpringApplication application;</span><br><span class="line">    <span class="keyword">private</span> String[] args;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">GavinYangEventPublishingRunListener</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">GavinYangEventPublishingRunListener</span><span class="params">(SpringApplication application, String[] args)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.application = application;</span><br><span class="line">        <span class="built_in">this</span>.args = args;</span><br><span class="line">        System.out.println(<span class="string">&quot;调用到了 private final String[] args; 有参数构造方法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getOrder</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * org.springframework.boot.SpringApplication#run(java.lang.String...).</span></span><br><span class="line"><span class="comment">     * listeners.starting(); 当走到这个方法的时候,就会调用到.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">starting</span><span class="params">()</span> &#123;</span><br><span class="line">        LOGGER.info(<span class="string">&quot;GavinYangEventPublishingRunListener 调用 starting 方法成功&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">started</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line"></span><br><span class="line">        LOGGER.info(<span class="string">&quot;GavinYangEventPublishingRunListener 调用到了 started方法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">running</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line"></span><br><span class="line">        LOGGER.info(<span class="string">&quot;GavinYangEventPublishingRunListener 调用 running 方法成功&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后在META-INF&#x2F;spring.factories中配置下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Run Listenersorg.springframework.boot.SpringApplicationRunListener=\com.iyang.bootsourceread.listener.GavinYangEventPublishingRunListener</span><br></pre></td></tr></table></figure>

<p>控制台的log输出顺序: 可以看到打印的参数顺序, 也就是先反射调用实例化, 然后接着调用 starting 方法,接着就是started 方法, 最后就是 running方法. 可以看到执行的一次顺序.</p>
<p>调用到了 private final String[] args; 有参数构造方法 15:47:56.498 [main] INFO com.iyang.bootsourceread.listener.GavinYangEventPublishingRunListener - GavinYangEventPublishingRunListener 调用 starting 方法成功</p>
<p>2020-07-02 15:47:57.069 INFO 4063 — [ main] .b.l.GavinYangEventPublishingRunListener : GavinYangEventPublishingRunListener 调用到了 started方法 2020-07-02 15:47:57.070 INFO 4063 — [ main] .b.l.GavinYangEventPublishingRunListener : GavinYangEventPublishingRunListener 调用 running 方法成功</p>
<p>具体的执行,可自行debug进去看,是怎么具体走到哪步的. 与EventPublishingRunListener不同的是, 我们这里是没有new什么事件丢给监听器的. 就是简单的打印了几句log.</p>
<h2 id="run-中-starting-方法"><a href="#run-中-starting-方法" class="headerlink" title="run 中 starting() 方法"></a><strong>run 中 starting() 方法</strong></h2><p>org.springframework.boot.SpringApplicationRunListeners#starting</p>
<p>该方法就是直接走 SpringApplicationRunListeners中的starting方法. getRunListeners方法最后返回的是SpringApplicationRunListeners,该类中是包装了 我们SpringApplicationRunListener.class对应的Listener的.</p>
<p>然后这里就会迭代每个 listener 并且会调用到 starting 放. 这个结果在上面的方法小实战中,是可以很明显的体会到的.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void starting() &#123;</span><br><span class="line">	for (SpringApplicationRunListener listener : this.listeners) &#123;</span><br><span class="line">		listener.starting();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-prepareEnvironment-方法"><a href="#run-中-prepareEnvironment-方法" class="headerlink" title="run 中 prepareEnvironment() 方法"></a><strong>run 中 prepareEnvironment() 方法</strong></h2><p>可以看到这该方法最后处理的都是和 Environment有关的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> ConfigurableEnvironment <span class="title function_">prepareEnvironment</span><span class="params">(SpringApplicationRunListeners listeners, ApplicationArguments applicationArguments)</span> &#123;</span><br><span class="line">		<span class="comment">// Create and configure the environment</span></span><br><span class="line"><span class="comment">//如果没有获取到就创建,获取到就返回. 最后返回的是: StandraEnvironment这个    </span></span><br><span class="line">		<span class="type">ConfigurableEnvironment</span> <span class="variable">environment</span> <span class="operator">=</span> getOrCreateEnvironment();</span><br><span class="line"><span class="comment">// applicationArguments就是获取new DefaultApplicationArguments(args)中的args.</span></span><br><span class="line"><span class="comment">//先new一个ApplicationConversionService,最后赋值到org.springframework.core.env.AbstractPropertyResolver#setConversionService这个中.   </span></span><br><span class="line"><span class="comment">//接着就是配置property来源 和 profiles.    </span></span><br><span class="line">		configureEnvironment(environment, applicationArguments.getSourceArgs());</span><br><span class="line"><span class="comment">// 显示检验下ConfigurableEnvironment,是不是符合要求的.是的话,就会走getPropertySources()方法,sources这里返回的是null,所以就走到了new一个ConfigurationPropertySourcesPropertySource,然后添加到sources 中去.    </span></span><br><span class="line">		ConfigurationPropertySources.attach(environment);</span><br><span class="line"><span class="comment">// 如果看明白了之前的 starting()方法的话,那么这里就可以理解到很明白.</span></span><br><span class="line"><span class="comment">//这里就是抛出了 ApplicationEnvironmentPreparedEvent 这个事件出去,然后相应的监听器获取到这个事件,就会执行相应的代码逻辑.    </span></span><br><span class="line">		listeners.environmentPrepared(environment);</span><br><span class="line"><span class="comment">//    </span></span><br><span class="line">		bindToSpringApplication(environment);</span><br><span class="line"><span class="comment">// this.isCustomerEnvironment 是 false,    </span></span><br><span class="line">		<span class="keyword">if</span> (!<span class="built_in">this</span>.isCustomEnvironment) &#123;</span><br><span class="line"><span class="comment">// new一个EnvironmentConverter(也就是传入进去的classLoader给赋值到其属性), 然后赋值给environment. </span></span><br><span class="line">			environment = <span class="keyword">new</span> <span class="title class_">EnvironmentConverter</span>(getClassLoader()).convertEnvironmentIfNecessary(environment,</span><br><span class="line">					deduceEnvironmentClass());</span><br><span class="line">		&#125;</span><br><span class="line">		ConfigurationPropertySources.attach(environment);</span><br><span class="line">		<span class="keyword">return</span> environment;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-createApplicationContext-方法"><a href="#run-中-createApplicationContext-方法" class="headerlink" title="run 中 createApplicationContext() 方法"></a><strong>run 中 createApplicationContext() 方法</strong></h2><p>创建 ApplicationCntext, 该方法可以看到 根据 this.webApplicationType来创建 ApplicationContext的class的,然后使用BeanUtils.instantiateClass() 方法来实例化这个bean. 这里还是很好理解的,就是创建一个 ApplicationContext. 最后返回这个 context给赋值到 context 这个参数.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Strategy method used to create the &#123;<span class="doctag">@link</span> ApplicationContext&#125;. By default this</span></span><br><span class="line"><span class="comment">	 * method will respect any explicitly set application context or application context</span></span><br><span class="line"><span class="comment">	 * class before falling back to a suitable default.</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@return</span> the application context (not yet refreshed)</span></span><br><span class="line"><span class="comment">	 * <span class="doctag">@see</span> #setApplicationContextClass(Class)</span></span><br><span class="line"><span class="comment">这里我们看到 定义的三个参数	 </span></span><br><span class="line"><span class="comment">DEFAULT_SERVLET_WEB_CONTEXT_CLASS:org.springframework.boot.web.servlet.context.AnnotationConfigServletWebServerApplicationContext</span></span><br><span class="line"><span class="comment">DEFAULT_REACTIVE_WEB_CONTEXT_CLASS:org.springframework.boot.web.reactive.context.AnnotationConfigReactiveWebServerApplicationContext</span></span><br><span class="line"><span class="comment">DEFAULT_CONTEXT_CLASS:org.springframework.context.annotation.AnnotationConfigApplicationContext</span></span><br><span class="line"><span class="comment">	 </span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">protected</span> ConfigurableApplicationContext <span class="title function_">createApplicationContext</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// contextClass 是 null,所以就走入到下面的switch中. 这里是走到了默认default中去了.</span></span><br><span class="line">		Class&lt;?&gt; contextClass = <span class="built_in">this</span>.applicationContextClass;</span><br><span class="line">		<span class="keyword">if</span> (contextClass == <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="keyword">switch</span> (<span class="built_in">this</span>.webApplicationType) &#123;</span><br><span class="line">				<span class="keyword">case</span> SERVLET:</span><br><span class="line">					contextClass = Class.forName(DEFAULT_SERVLET_WEB_CONTEXT_CLASS);</span><br><span class="line">					<span class="keyword">break</span>;</span><br><span class="line">				<span class="keyword">case</span> REACTIVE:</span><br><span class="line">					contextClass = Class.forName(DEFAULT_REACTIVE_WEB_CONTEXT_CLASS);</span><br><span class="line">					<span class="keyword">break</span>;</span><br><span class="line">				<span class="keyword">default</span>:</span><br><span class="line">                   <span class="comment">// AnnotationConfigApplicationContext     </span></span><br><span class="line">					contextClass = Class.forName(DEFAULT_CONTEXT_CLASS);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (ClassNotFoundException ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(</span><br><span class="line">						<span class="string">&quot;Unable create a default ApplicationContext, please specify an ApplicationContextClass&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 所以这里实例化的也是 AnnotationConfigApplicationContext 这个 Context.        </span></span><br><span class="line">		<span class="keyword">return</span> (ConfigurableApplicationContext) BeanUtils.instantiateClass(contextClass);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-getSpringFactoriesInstances-SpringBootExceptionReporter-class…-方法"><a href="#run-中-getSpringFactoriesInstances-SpringBootExceptionReporter-class…-方法" class="headerlink" title="run 中 getSpringFactoriesInstances(SpringBootExceptionReporter.class…) 方法"></a><strong>run 中 getSpringFactoriesInstances(SpringBootExceptionReporter.class…) 方法</strong></h2><p>之前阅读到的 getRunListeners方法,其内部也是走了 getSpringFactoriesInstances 这个方法,然后这里也是根据SpringBootExceptionReporter.class来获取出相应的集合数据.</p>
<p>所以这里直接说,返回的集合中,如果没有进行扩展的话,那么这里就只有 FailureAnalyzers这一个.</p>
<p>org.springframework.boot.diagnostics.FailureAnalyzers</p>
<p>如果是有异常的话,就会放入到 SpringBootExceptionReporter 这个里,由其子类给打印出来. Reporter这个意思还是很好理解的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> &lt;T&gt; Collection&lt;T&gt; <span class="title function_">getSpringFactoriesInstances</span><span class="params">(Class&lt;T&gt; type, Class&lt;?&gt;[] parameterTypes, Object... args)</span> &#123;</span><br><span class="line">		<span class="type">ClassLoader</span> <span class="variable">classLoader</span> <span class="operator">=</span> getClassLoader();</span><br><span class="line">		<span class="comment">// Use names and ensure unique to protect against duplicates</span></span><br><span class="line">		Set&lt;String&gt; names = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(SpringFactoriesLoader.loadFactoryNames(type, classLoader));</span><br><span class="line">		List&lt;T&gt; instances = createSpringFactoriesInstances(type, parameterTypes, classLoader, args, names);</span><br><span class="line">		AnnotationAwareOrderComparator.sort(instances);</span><br><span class="line">		<span class="keyword">return</span> instances;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-中-prepareContext-方法"><a href="#run-中-prepareContext-方法" class="headerlink" title="run 中 prepareContext() 方法"></a><strong>run 中 prepareContext() 方法</strong></h2><p>可以看到这个方法基本都是一直在操作 context,先是给context设置environment.</p>
<p>然后判断internalConfigurationBeanNameGenerator要不要注册到 beanFactory中去. 如果this.resourceLoader不是null的话,就会分别设置ResourceLoader&#x2F;ClassLoader(看具体的值),给ConversionService set 到beanFactory中去.</p>
<p>调用 ApplicationContextInitializer 的 initialize 方法.</p>
<p>listeners.contextPrepared(context) : 广播一个 ApplicationContextInitializedEvent 出去</p>
<p>注册一个springApplicationArguments到 beanFactory中去. printedBanner 不是null的话,也注册到beanFactory中去. 如果beanFactory是DefaultListableBeanFactory的话,就this.allowBeanDefinitionOverriding赋值为false</p>
<p>如果是有lazyInitialization,就会添加一个LazyInitializationBeanFactoryPostProcessor到context中去.</p>
<p>getAllSources() 这里获取出来的就是只有我们的启动类,对获取出来的集合进行非空判断.</p>
<p>最后广播一个 ApplicationPreparedEvent 出去. 广播出去之前会有对listener都添加到 context中,如果是ApplicationContextAware的话,就会再调用一个 setApplicationContext 方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里传入的参数分别是 :  context : AnnotationConfigApplicationContext</span></span><br><span class="line"><span class="comment">// environment : StandardEnvironment</span></span><br><span class="line"><span class="comment">// listeners : SpringApplicationRunListeners</span></span><br><span class="line"><span class="comment">// applicationArguments : DefaultApplicationArguments</span></span><br><span class="line"><span class="comment">// printedBanner : SpringApplicationBannerPrinter$PrintedBanner</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">prepareContext</span><span class="params">(ConfigurableApplicationContext context, ConfigurableEnvironment environment,</span></span><br><span class="line"><span class="params">			SpringApplicationRunListeners listeners, ApplicationArguments applicationArguments, Banner printedBanner)</span> &#123;</span><br><span class="line"> <span class="comment">// 给 environment设置到context中去   </span></span><br><span class="line">		context.setEnvironment(environment);</span><br><span class="line"> <span class="comment">// this.beanNameGenerator 不是null的话,就会往beanFactory中注册org.springframework.context.annotation.internalConfigurationBeanNameGenerator</span></span><br><span class="line"><span class="comment">// this.resourceLoader 不是null的话,判断是GenericApplicationContext/DefaultResourceLoader</span></span><br><span class="line"><span class="comment">// 会分别走不同的set方法.    </span></span><br><span class="line"><span class="comment">// this.addConversionService是true的话,就会给ApplicationConversionService.getSharedInstance() set 到beanFactroy中去.  </span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.AbstractBeanFactory#setConversionService    </span></span><br><span class="line">		postProcessApplicationContext(context);</span><br><span class="line"><span class="comment">// getInitializers() 获取出 ApplicationContextInitializer的集合,然后满足条件的就会调用其 initialize() 方法    </span></span><br><span class="line">		applyInitializers(context);</span><br><span class="line"><span class="comment">// 看到 listeners.contextPrepared 方法,是不是又能想起之前的 listeners.starting();也是一样的逻辑.</span></span><br><span class="line"><span class="comment">//然后这里new一个ApplicationContextInitializedEvent事件给广播出去,然后相应的监听器监听到事件就会走相应的逻辑代码.    </span></span><br><span class="line">    	listeners.contextPrepared(context);</span><br><span class="line"><span class="comment">// this.logStartupInfo 就是判断的二个方法打不打印log</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">this</span>.logStartupInfo) &#123;</span><br><span class="line"><span class="comment">// 打印 启动相关的信息, PID, 编译后的class路径, 项目路径等信息.            </span></span><br><span class="line">			logStartupInfo(context.getParent() == <span class="literal">null</span>);</span><br><span class="line"><span class="comment">// 打印 profile,也就是配置文件等信息,这里打印的是 默认的.            </span></span><br><span class="line">			logStartupProfileInfo(context);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// Add boot specific singleton beans</span></span><br><span class="line"><span class="comment">// 获取出 beanFactory来    </span></span><br><span class="line">		<span class="type">ConfigurableListableBeanFactory</span> <span class="variable">beanFactory</span> <span class="operator">=</span> context.getBeanFactory();</span><br><span class="line"><span class="comment">//给beanName是springApplicationArguments,然后Object是applicationArguments注册到beanFactory中去. 最后也就是走到了下面的这个方法,调用集合添加进去.</span></span><br><span class="line"><span class="comment">//org.springframework.beans.factory.support.DefaultSingletonBeanRegistry#addSingleton    </span></span><br><span class="line">		beanFactory.registerSingleton(<span class="string">&quot;springApplicationArguments&quot;</span>, applicationArguments);</span><br><span class="line"><span class="comment">//打印的banner如果不是null的话,也会注册到beanFactory中去.    </span></span><br><span class="line">		<span class="keyword">if</span> (printedBanner != <span class="literal">null</span>) &#123;</span><br><span class="line">			beanFactory.registerSingleton(<span class="string">&quot;springBootBanner&quot;</span>, printedBanner);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// 如果 beanFactory 是 DefaultListableBeanFactory的话,强转然后调用setAllowBeanDefinitionOverriding方法,传入的参数的值是false.</span></span><br><span class="line">		<span class="keyword">if</span> (beanFactory <span class="keyword">instanceof</span> DefaultListableBeanFactory) &#123;</span><br><span class="line">			((DefaultListableBeanFactory) beanFactory)</span><br><span class="line">					.setAllowBeanDefinitionOverriding(<span class="built_in">this</span>.allowBeanDefinitionOverriding);</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">// this.lazyInitialization是true的话,就会添加一个 LazyInitializationBeanFactoryPostProcessor 到context中去.</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">this</span>.lazyInitialization) &#123;</span><br><span class="line">			context.addBeanFactoryPostProcessor(<span class="keyword">new</span> <span class="title class_">LazyInitializationBeanFactoryPostProcessor</span>());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// Load the sources</span></span><br><span class="line"><span class="comment">// 加载资源. 分别从this.primarySources/this.sources这二个集合中添加进去.</span></span><br><span class="line"><span class="comment">// 该集合中只有一个启动类的全路径名字    </span></span><br><span class="line">		Set&lt;Object&gt; sources = getAllSources();</span><br><span class="line"><span class="comment">// sources集合不能为空.    </span></span><br><span class="line">		Assert.notEmpty(sources, <span class="string">&quot;Sources must not be empty&quot;</span>);</span><br><span class="line"><span class="comment">// new 一个 BeanDefinitionLoader,然后调用其load方法.    </span></span><br><span class="line">		load(context, sources.toArray(<span class="keyword">new</span> <span class="title class_">Object</span>[<span class="number">0</span>]));</span><br><span class="line"><span class="comment">// 看到 listeners调用方法,是不是又很熟悉了.</span></span><br><span class="line"><span class="comment">//但是这个方法,比起之前的几个,这里多做了个事,那就是,先从application中获取Listeners,然后挨个迭代,如果是ApplicationContextAware的话,就走listener(这里会强转)的setApplicationContext方法,给context设置进去. 然后所有的listeners都会添加到context中去.</span></span><br><span class="line"><span class="comment">// 最后new一个ApplicationPreparedEvent事件出来,然后广播给相应的监听器,监听器拿到了相应的event,就会去执行对应的逻辑代码.    </span></span><br><span class="line"><span class="comment">//org.springframework.boot.context.event.EventPublishingRunListener#contextLoaded    </span></span><br><span class="line">		listeners.contextLoaded(context);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-refreshContext-方法"><a href="#run-refreshContext-方法" class="headerlink" title="run.refreshContext() 方法"></a><strong>run.refreshContext() 方法</strong></h2><p>最后是走到了 org.springframework.context.support.AbstractApplicationContext#refresh 这里,也就是走到refresh方法.</p>
<h2 id="run-listeners-started-context-方法"><a href="#run-listeners-started-context-方法" class="headerlink" title="run -&gt; listeners.started(context) 方法"></a><strong>run -&gt; listeners.started(context) 方法</strong></h2><p>这里可以看到是广播了一个 ApplicationStartedEvent 事件出去.</p>
<p>于是后面又广播了一个 AvailabilityChangeEvent 事件出去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">started</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line">		<span class="keyword">for</span> (SpringApplicationRunListener listener : <span class="built_in">this</span>.listeners) &#123;</span><br><span class="line">			listener.started(context);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">----------------------</span><br><span class="line">org.springframework.boot.context.event.EventPublishingRunListener#started    </span><br><span class="line">    </span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">started</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line">		context.publishEvent(<span class="keyword">new</span> <span class="title class_">ApplicationStartedEvent</span>(<span class="built_in">this</span>.application, <span class="built_in">this</span>.args, context));</span><br><span class="line">		AvailabilityChangeEvent.publish(context, LivenessState.CORRECT);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-callRunners-方法"><a href="#run-callRunners-方法" class="headerlink" title="run.callRunners() 方法"></a><strong>run.callRunners() 方法</strong></h2><p>该方法可以看到是, 分别获取出 ApplicationRunner 和 CommandLineRunner, 然后调用callRunner方法,该方法其内部也是去调用的run方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">callRunners</span><span class="params">(ApplicationContext context, ApplicationArguments args)</span> &#123;</span><br><span class="line">		List&lt;Object&gt; runners = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// 从 context中获取出来 	ApplicationRunner.class / CommandLineRunner.class的Object,</span></span><br><span class="line"><span class="comment">// 然后都添加到 runners集合中        </span></span><br><span class="line">		runners.addAll(context.getBeansOfType(ApplicationRunner.class).values());</span><br><span class="line">		runners.addAll(context.getBeansOfType(CommandLineRunner.class).values());</span><br><span class="line"><span class="comment">// 排序        </span></span><br><span class="line">		AnnotationAwareOrderComparator.sort(runners);</span><br><span class="line">  <span class="comment">// 迭代, 如果是 ApplicationRunner/CommandLineRunner,就分别强转为ApplicationRunner/CommandLineRunner</span></span><br><span class="line"> <span class="comment">//然后调用callRunner方法       </span></span><br><span class="line">		<span class="keyword">for</span> (Object runner : <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(runners)) &#123;</span><br><span class="line">			<span class="keyword">if</span> (runner <span class="keyword">instanceof</span> ApplicationRunner) &#123;</span><br><span class="line">				callRunner((ApplicationRunner) runner, args);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (runner <span class="keyword">instanceof</span> CommandLineRunner) &#123;</span><br><span class="line">				callRunner((CommandLineRunner) runner, args);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="run-listeners-running-context-方法"><a href="#run-listeners-running-context-方法" class="headerlink" title="run -&gt; listeners.running(context) 方法"></a><strong>run -&gt; listeners.running(context) 方法</strong></h2><p>可以看到 running是分别广播了二个事件出去. 然后相应的监听器分别获取到这个event,并且进行相应的逻辑处理.</p>
<p>走完该方法,如果是没有出现任何异常的话,就会返回一个context,所以该run方法也就是走到这里就算走完了.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">running</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line">		<span class="keyword">for</span> (SpringApplicationRunListener listener : <span class="built_in">this</span>.listeners) &#123;</span><br><span class="line">			listener.running(context);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	</span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">running</span><span class="params">(ConfigurableApplicationContext context)</span> &#123;</span><br><span class="line"><span class="comment">// 广播一个  ApplicationReadyEvent 出去.	</span></span><br><span class="line">		context.publishEvent(<span class="keyword">new</span> <span class="title class_">ApplicationReadyEvent</span>(<span class="built_in">this</span>.application, <span class="built_in">this</span>.args, context));</span><br><span class="line"> <span class="comment">// 广播一个 AvailabilityChangeEvent 出去.       </span></span><br><span class="line">		AvailabilityChangeEvent.publish(context, ReadinessState.ACCEPTING_TRAFFIC);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot创建Web环境</title>
    <url>//article/SpringBoot_web_create/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-SpringBoot创建Web环境"><a href="#📝-SpringBoot创建Web环境" class="headerlink" title="📝 SpringBoot创建Web环境"></a>📝 <strong>SpringBoot创建Web环境</strong></h1><p><code>启动一个SpringBoot项目就相当于启动了一个web项目, 内嵌的tomcat, 那 SpringBoot是怎么启动这个代码的呢?</code></p>
<p> 所以还是需要去看下其底层是怎么走的实现的.</p>
<h1 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h1><p>org.springframework.boot.web.servlet.context.ServletWebServerApplicationContext#onRefresh</p>
<p>当走 refresh 方法的时候, 如果只是单纯的Spring的话,那么这里就不会走到上面的子类.</p>
<p>如果是基于SpringBoot查看的代码,并且也引入了 web环境,那么走 refresh的之后,跟进 onRefresh 方法,就会走到这里来.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onRefresh</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="built_in">super</span>.onRefresh();</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 该方法主要看这个</span></span><br><span class="line">			createWebServer();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ApplicationContextException</span>(<span class="string">&quot;Unable to start web server&quot;</span>, ex);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>这里说下: ServletWebServerFactory 其子类是有一个抽象的ServletWebServerFactory,然后其抽象的中有三个子类分别是:</p>
<p>JettyServletWebServerFactory&#x2F;UndertowServletWebServerFactory &#x2F;TomcatServletWebServerFactory</p>
<p>当然我们这里返回的是 tomcatServletWebServerFactory这个.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">createWebServer</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// this.webServer 是 null</span></span><br><span class="line">		<span class="type">WebServer</span> <span class="variable">webServer</span> <span class="operator">=</span> <span class="built_in">this</span>.webServer;</span><br><span class="line">        <span class="comment">// getServletContext返回的也是null</span></span><br><span class="line">		<span class="type">ServletContext</span> <span class="variable">servletContext</span> <span class="operator">=</span> getServletContext();</span><br><span class="line">        <span class="comment">//上面二个参数都是null,满足进入到if中的条件.</span></span><br><span class="line">		<span class="keyword">if</span> (webServer == <span class="literal">null</span> &amp;&amp; servletContext == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 这里返回的是 tomcatServletWebServerFactory,</span></span><br><span class="line">            <span class="comment">// 并且还会调用 doGetBean方法,这里也是有实例化这个 bean 的</span></span><br><span class="line">			<span class="type">ServletWebServerFactory</span> <span class="variable">factory</span> <span class="operator">=</span> getWebServerFactory();</span><br><span class="line"> <span class="comment">//getSelfInitializer()方法返回的是一个 ServletWebServerApplicationContext$lamdba这个</span></span><br><span class="line"> <span class="comment">//getWebServer() 方法: new一个Tomcat,然和给 basedir 设置上值.</span></span><br><span class="line"> <span class="comment">//接着创建一个Connector,Connector的throwOnFailure字段给设置上false,</span></span><br><span class="line"> <span class="comment">//tomcat.getService.addConnector(connector);添加到tomcat的service中去</span></span><br><span class="line"> <span class="comment">//customizeConnector()方法,先是拿端口号与0进行比较,我们这里默认是8080,也就是8080.</span></span><br><span class="line"> <span class="comment">//serverHeader如有有值的话,就会赋值到connector中去.</span></span><br><span class="line"> <span class="comment">//connector.getProtocolHandler()是AbstractProtocol的话,就会先获取出来,然和走customizeProtocol方法.</span></span><br><span class="line"><span class="comment">//invokeProtocolHandlerCustomizers:从方法名字上理解为,调用ProtocolHandlerCustomizers         //设置编码UTF-8,默认的.</span></span><br><span class="line"><span class="comment">//最后就是迭代 tomcatConnectorCustomizers,可以看到其调用的customize方法,都是进行一些tomcat的设置,比如最大线程数,最小线程数,最大连接等.  具体可以看下面的这种,customize开头的方法</span></span><br><span class="line"><span class="comment">//org.springframework.boot.autoconfigure.web.embedded.TomcatWebServerFactoryCustomizer#customizeXXXXXX       </span></span><br><span class="line"><span class="comment">// prepareContext方法: TODO</span></span><br><span class="line"><span class="comment">//最后返回了一个 TomcatServer对象来,其内部是有一个Tomcat的,tomcat都是一些启动信息的配置.</span></span><br><span class="line"><span class="comment">//            </span></span><br><span class="line">			<span class="built_in">this</span>.webServer = factory.getWebServer(getSelfInitializer());</span><br><span class="line"><span class="comment">//添加一个WebServerGracefulShutdownLifecycle,name是webServerGracefulShutdown到beanFactory,   //可以发现WebServerGracefulShutdownLifecycle是实现了SmartLifecycle,也就是说后面会回调到这个类的start方法的 </span></span><br><span class="line">			getBeanFactory().registerSingleton(<span class="string">&quot;webServerGracefulShutdown&quot;</span>,</span><br><span class="line">					<span class="keyword">new</span> <span class="title class_">WebServerGracefulShutdownLifecycle</span>(<span class="built_in">this</span>.webServer));</span><br><span class="line"><span class="comment">//添加一个 webServerStartStop到beanFactory中,WebServerStartStopLifecycle也是实现了SmartLifecycle接口的,也就是后面调用到这个类的start方法的. </span></span><br><span class="line"><span class="comment">//可以看到其start方法,先是调用了this.webServer.start()方法,再是this.running=true设置为true,     //最后是广播出一个ServletWebServerInitializedEvent事件会相应的监听器.       </span></span><br><span class="line">			getBeanFactory().registerSingleton(<span class="string">&quot;webServerStartStop&quot;</span>,</span><br><span class="line">					<span class="keyword">new</span> <span class="title class_">WebServerStartStopLifecycle</span>(<span class="built_in">this</span>, <span class="built_in">this</span>.webServer));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (servletContext != <span class="literal">null</span>) &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				getSelfInitializer().onStartup(servletContext);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (ServletException ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ApplicationContextException</span>(<span class="string">&quot;Cannot initialize servlet context&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">//        </span></span><br><span class="line">		initPropertySources();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud中Eureka_Server端阅读</title>
    <url>//article/spring_eureka_server_code1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-题记"><a href="#📝-题记" class="headerlink" title="📝 题记"></a>📝 <strong>题记</strong></h1><p> 公司的注册中心一直使用的是Eureka,其实在就应该写一篇记录来记录一下或者专门来阅读一下Eureka的源码. SpringCloud全家桶中的组件,如果时间是充足的话,其中的源码阅读下,还是很不错的. 其中是可以学习到很多知识的.</p>
<p> 构建项目 : <a href="https://github.com/baoyang23/boot-case-self/tree/master/txlcninit">地址https://github.com/baoyang23/boot-case-self/tree/master/txlcninit</a> , 这里只用看txlcn-server模块的依赖和代码即可,仅仅只有依赖和启动类,并无其他的东西了.</p>
<h1 id="code"><a href="#code" class="headerlink" title="code"></a>code</h1><p> 项目构建完了,就先启动看下有没有问题,至少保证搭建是ok的. 如果启动没问题的话,那么我们就可以肆无忌惮的去阅读其源码了.</p>
<p>在配置文件中加上打印log的级别和包,这样可以更好的看到源码里面打印的更多的log.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">logging:</span><br><span class="line">	level:</span><br><span class="line">		com.netflix: DEBUG</span><br></pre></td></tr></table></figure>

<p>初次启动,就可以看下log的输出,定位下是那些类打印的,然后我们可以对那些类进行debug阅读. 这里我们可以看到AbstractInstanceRegistry 这个注册类的输出log</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2020-06-27 14:25:55.030  INFO 29855 --- [           main] c.n.e.registry.AbstractInstanceRegistry  : Finished initializing remote region registries. All known remote regions: []</span><br><span class="line">2020-06-27 14:25:55.031  INFO 29855 --- [           main] c.n.eureka.DefaultEurekaServerContext    : Initialized</span><br><span class="line">2020-06-27 14:25:55.040  INFO 29855 --- [           main] o.s.b.a.e.web.EndpointLinksResolver      : Exposing 2 endpoint(s) beneath base path &#x27;/actuator&#x27;</span><br><span class="line">2020-06-27 14:25:55.090  INFO 29855 --- [           main] o.s.c.n.e.s.EurekaServiceRegistry        : Registering application YANG-EUREKA-SERVER with eureka with status UP</span><br></pre></td></tr></table></figure>

<p>启动</p>
<p>我们在启动类加上 @EnableEurekaServer 这个注解,然后在application.yml中也好配置信息,启动即可. 那么这个注解有这么神奇,就可以直接加载出 Eureka 中的全部内容 ?</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@SpringBootApplication</span><br><span class="line">@EnableEurekaServer</span><br><span class="line">public class TxLcnEurekaServerApplication &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        SpringApplication.run(TxLcnEurekaServerApplication.class,args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 这里看 @Import这个注解,EurekaServerMarkerConfiguration这个类</span><br><span class="line">@Target(ElementType.TYPE)</span><br><span class="line">@Retention(RetentionPolicy.RUNTIME)</span><br><span class="line">@Documented</span><br><span class="line">@Import(EurekaServerMarkerConfiguration.class)</span><br><span class="line">public @interface EnableEurekaServer &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 这个使用一个配置的注解,然后注入一个marker的bean. 好像也没看到这个bean做了啥事情啊? 咋就能启动Eureka Server端的内容了呢? 真是让人百思不得姐.</span><br><span class="line">@Configuration(proxyBeanMethods = false)</span><br><span class="line">public class EurekaServerMarkerConfiguration &#123;</span><br><span class="line">	@Bean</span><br><span class="line">	public Marker eurekaServerMarkerBean() &#123;</span><br><span class="line">		return new Marker();</span><br><span class="line">	&#125;</span><br><span class="line">	class Marker &#123;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再来看一个东西, spring-cloud-netflix-eureka-server:2.2.1.RELEASE 这个maven的依赖. 2.2.1是引入的版本号.</p>
<p>然后看到 META-INF下spring.factories 这个文件. Spring在启动的时候,会读取这个地方的该文件.所以也就是会读取 org.springframework.cloud.netflix.eureka.server.EurekaServerAutoConfiguration 这个内容. 从名字上看,就是eureka的server端自动配置.</p>
<p>EurekaServerAutoConfiguration</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 先来看下这个类头上的注解是些什么意思.</span><br><span class="line">@Configuration(proxyBeanMethods = false)</span><br><span class="line">// 这个@Import注解,在出启动的注解中都是有说明使用到的.</span><br><span class="line">@Import(EurekaServerInitializerConfiguration.class)</span><br><span class="line">//有EurekaServerMarkerConfiguration.Marker.class这个bean的条件,该bean才会继续初始化到Spring容器中. 所以也就是说,如果我们没有加上@EnableEurekaServer注解的,也就不会有Marker这个bean了,所以这里也就不会有了.</span><br><span class="line">@ConditionalOnBean(EurekaServerMarkerConfiguration.Marker.class)</span><br><span class="line">//二个读取制定前缀的配置文件的类</span><br><span class="line">@EnableConfigurationProperties(&#123; EurekaDashboardProperties.class,</span><br><span class="line">		InstanceRegistryProperties.class &#125;)</span><br><span class="line">// 读取特定文件下的内容</span><br><span class="line">@PropertySource(&quot;classpath:/eureka/server.properties&quot;)</span><br><span class="line">public class EurekaServerAutoConfiguration implements WebMvcConfigurer &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 然后注入一些bean进来.</span><br><span class="line">	@Autowired</span><br><span class="line">	private ApplicationInfoManager applicationInfoManager;</span><br><span class="line"></span><br><span class="line">	/// 注入进来的EurekaServerConfig,可以点进去看到其子类:org.springframework.cloud.netflix.eureka.server.EurekaServerConfigBean,读取eureka.server前缀来头的配置</span><br><span class="line">	@Autowired</span><br><span class="line">	private EurekaServerConfig eurekaServerConfig;</span><br><span class="line">//类:org.springframework.cloud.netflix.eureka.EurekaClientConfigBean,这里是读取eureka.client开头的配置信息.</span><br><span class="line">	@Autowired</span><br><span class="line">	private EurekaClientConfig eurekaClientConfig;</span><br><span class="line"></span><br><span class="line">	@Autowired</span><br><span class="line">	private EurekaClient eurekaClient;</span><br><span class="line"></span><br><span class="line">// 注册一些实例的信息.读取的eureka.instance.registry开头的配置信息.</span><br><span class="line">	@Autowired</span><br><span class="line">	private InstanceRegistryProperties instanceRegistryProperties;</span><br><span class="line"></span><br><span class="line">然后就是注入一些bean到Spring容器中来.</span><br><span class="line">//  注入 PeerAwareInstanceRegistry到Spring容器中.</span><br><span class="line">	@Bean</span><br><span class="line">	public PeerAwareInstanceRegistry peerAwareInstanceRegistry(</span><br><span class="line">			ServerCodecs serverCodecs) &#123;</span><br><span class="line">		this.eurekaClient.getApplications(); // force initialization</span><br><span class="line">  // new InstanceRegistry() 方法</span><br><span class="line">		return new InstanceRegistry(this.eurekaServerConfig, this.eurekaClientConfig,</span><br><span class="line">				serverCodecs, this.eurekaClient,</span><br><span class="line">				this.instanceRegistryProperties.getExpectedNumberOfClientsSendingRenews(),</span><br><span class="line">				this.instanceRegistryProperties.getDefaultOpenForTrafficCount());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	/**</span><br><span class="line">	 * Register the Jersey filter.</span><br><span class="line">	 * @param eurekaJerseyApp an &#123;@link Application&#125; for the filter to be registered</span><br><span class="line">	 * @return a jersey &#123;@link FilterRegistrationBean&#125;</span><br><span class="line">注入FilterRegistrationBean到Spring容器中. 所有的/eureka/*的请求都要经过这个filter,其他的处理就会走ServletContainer.</span><br><span class="line">	 */</span><br><span class="line">	@Bean</span><br><span class="line">	public FilterRegistrationBean&lt;?&gt; jerseyFilterRegistration(</span><br><span class="line">			javax.ws.rs.core.Application eurekaJerseyApp) &#123;</span><br><span class="line">		FilterRegistrationBean&lt;Filter&gt; bean = new FilterRegistrationBean&lt;Filter&gt;();</span><br><span class="line">		bean.setFilter(new ServletContainer(eurekaJerseyApp));</span><br><span class="line">		bean.setOrder(Ordered.LOWEST_PRECEDENCE);</span><br><span class="line">		bean.setUrlPatterns(</span><br><span class="line">				Collections.singletonList(EurekaConstants.DEFAULT_PREFIX + &quot;/*&quot;));</span><br><span class="line"></span><br><span class="line">		return bean;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">// 此处省略了详细代码. 添加了 @Path 和 @Provider 二个注解的filter.</span><br><span class="line">// 然后添加 /eureka/(font/images/css/js)/*下面的文件信息. 最后注册到Spring容器中.</span><br><span class="line">	@Bean</span><br><span class="line">	public javax.ws.rs.core.Application jerseyApplication(Environment environment,</span><br><span class="line">			ResourceLoader resourceLoader) &#123;....&#125;</span><br></pre></td></tr></table></figure>

<p>org.springframework.cloud.netflix.eureka.server.EurekaServerInitializerConfiguration#start 该方法是如何被调用的? 可以看到这个方法是对 eurekaServerBootstrap进行初始化操作. 并且也有对应的event给发送出去. 在SpringBoot源码中是是可以看到有也有类似的event给publish出去.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">start</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="comment">// <span class="doctag">TODO:</span> is this class even needed now?</span></span><br><span class="line">				eurekaServerBootstrap.contextInitialized(</span><br><span class="line">						EurekaServerInitializerConfiguration.<span class="built_in">this</span>.servletContext);</span><br><span class="line">				log.info(<span class="string">&quot;Started Eureka Server&quot;</span>);</span><br><span class="line"></span><br><span class="line">				publish(<span class="keyword">new</span> <span class="title class_">EurekaRegistryAvailableEvent</span>(getEurekaServerConfig()));</span><br><span class="line">				EurekaServerInitializerConfiguration.<span class="built_in">this</span>.running = <span class="literal">true</span>;</span><br><span class="line">				publish(<span class="keyword">new</span> <span class="title class_">EurekaServerStartedEvent</span>(getEurekaServerConfig()));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">				<span class="comment">// Help!</span></span><br><span class="line">				log.error(<span class="string">&quot;Could not initialize Eureka servlet context&quot;</span>, ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;).start();</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker知识</title>
    <url>//article/docker_common1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>容器化时代,docker是一门必备的知识<br>安装手册: <a href="https://www.runoob.com/docker/docker-architecture.html">https://www.runoob.com/docker/docker-architecture.html</a></p>
</blockquote>
<h1 id="📝-docker原理"><a href="#📝-docker原理" class="headerlink" title="📝 docker原理"></a>📝 docker原理</h1><h2 id="NameSpace"><a href="#NameSpace" class="headerlink" title="NameSpace"></a>NameSpace</h2><p>NameSpace是Linux内核的一项功能,该功能对内核资源进行隔离，使得容器的进程都可以在单独的命名空间中运行，并且只可以访问当前容器命名空间的资源</p>
<p>NameSpace可以隔离进程ID，主机名，用户ID，文件名，网络访问和进程通信等相关资源</p>
<ul>
<li>pid namespace: 用于隔离进程ID</li>
<li>net namespace: 隔离网络接口，在虚拟的net namespace内用户可以拥有自己独立的ip，路由，端口等</li>
<li>mnt namespace: 文件系统挂载点隔离</li>
<li>ipc namespace: 信号量，消息队列和共享内存的隔离</li>
<li>uts namespace: 主机命和域名的隔离</li>
</ul>
<h2 id="CGroups"><a href="#CGroups" class="headerlink" title="CGroups"></a>CGroups</h2><p>Cgroups 是一种linux内核功能，可以限制和隔离进程的资源使用情况(CPU,内存,磁盘I&#x2F;O,网络等).在容器的实现中,Cgroups通常用来限制容器的cpu和内存等资源的使用</p>
<h2 id="联合文件系统"><a href="#联合文件系统" class="headerlink" title="联合文件系统"></a>联合文件系统</h2><p>UnionFS,是一种通过创建文件层进程操作的文件系统，因此，联合文件系统非常轻快</p>
<h2 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h2><p><img src="/images/posts/docker_common1/img-1.png" alt="Docker知识"></p>
<h2 id="docker进程查看"><a href="#docker进程查看" class="headerlink" title="docker进程查看"></a>docker进程查看</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(base) luohong<span class="meta">@luohong</span>:~$ ps aux | grep docker</span><br><span class="line">root        <span class="number">3566</span>  <span class="number">0.0</span>  <span class="number">0.4</span> <span class="number">2202812</span> <span class="number">74544</span> ?       Ssl  <span class="number">13</span>:<span class="number">30</span>   <span class="number">0</span>:<span class="number">00</span> /usr/bin/dockerd -H fd:<span class="comment">// --containerd=/run/containerd/containerd.sock</span></span><br><span class="line">root       <span class="number">47767</span>  <span class="number">0.0</span>  <span class="number">0.0</span> <span class="number">1230384</span> <span class="number">2916</span> ?        Sl   <span class="number">14</span>:<span class="number">25</span>   <span class="number">0</span>:<span class="number">00</span> /usr/bin/docker-proxy -proto tcp -host-ip <span class="number">0.0</span><span class="number">.0</span><span class="number">.0</span> -host-port <span class="number">2181</span> -container-ip <span class="number">172.17</span><span class="number">.0</span><span class="number">.2</span> -container-port <span class="number">2181</span></span><br><span class="line">root       <span class="number">47793</span>  <span class="number">0.0</span>  <span class="number">0.0</span> <span class="number">1230384</span> <span class="number">3152</span> ?        Sl   <span class="number">14</span>:<span class="number">25</span>   <span class="number">0</span>:<span class="number">00</span> /usr/bin/docker-proxy -proto tcp -host-ip :: -host-port <span class="number">2181</span> -container-ip <span class="number">172.17</span><span class="number">.0</span><span class="number">.2</span> -container-port <span class="number">2181</span></span><br><span class="line">luohong    <span class="number">48072</span>  <span class="number">0.0</span>  <span class="number">0.0</span>  <span class="number">13996</span>   <span class="number">896</span> pts/<span class="number">6</span>    S+   <span class="number">14</span>:<span class="number">26</span>   <span class="number">0</span>:<span class="number">00</span> grep docker</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(base) luohong<span class="meta">@luohong</span>:~$ sudo pstree -l -a -A <span class="number">3566</span></span><br><span class="line">dockerd -H fd:<span class="comment">// --containerd=/run/containerd/containerd.sock</span></span><br><span class="line">  |-docker-proxy -proto tcp -host-ip <span class="number">0.0</span><span class="number">.0</span><span class="number">.0</span> -host-port <span class="number">2181</span> -container-ip <span class="number">172.17</span><span class="number">.0</span><span class="number">.2</span> -container-port <span class="number">2181</span></span><br><span class="line">  |   `-<span class="number">7</span>*[&#123;docker-proxy&#125;]</span><br><span class="line">  |-docker-proxy -proto tcp -host-ip :: -host-port <span class="number">2181</span> -container-ip <span class="number">172.17</span><span class="number">.0</span><span class="number">.2</span> -container-port <span class="number">2181</span></span><br><span class="line">  |   `-<span class="number">7</span>*[&#123;docker-proxy&#125;]</span><br><span class="line">  `-<span class="number">19</span>*[&#123;dockerd&#125;]</span><br></pre></td></tr></table></figure>

<h2 id="docker运行状态"><a href="#docker运行状态" class="headerlink" title="docker运行状态"></a>docker运行状态</h2><p><img src="/images/posts/docker_common1/img-2.png" alt="Docker知识"></p>
<h1 id="NameSpace资源隔离"><a href="#NameSpace资源隔离" class="headerlink" title="NameSpace资源隔离"></a>NameSpace资源隔离</h1><p>Docker是使用Linux的NameSpace技术实现各种资源隔离的</p>
<p>NameSpace是Linux内核的一项功能，该功能对内核资源进行分区，以使一组进程看到一组资源，而另一组进程看到另一组资源</p>
<p>NameSpace的工作方式通过一组资源和进程设置相同的namesapce而起作用，但是这些namespace引用了不同的资源</p>
<p>资源可能存在于多个NameSpace中</p>
<p>这些资源可以是进程ID,主机名,用户名,与网络访问相关的名称和进程间通信</p>
<p>简单来说，Namespace 是 Linux 内核的一个特性，该特性可以实现在同一主机系统中，对进程 ID、主机名、用户 ID、文件名、网络和进程间通信等资源的隔离</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>Namespace 名称</td>
<td>作用</td>
<td>内核版本</td>
</tr>
<tr>
<td>Mount（mnt）</td>
<td>隔离挂载点</td>
<td>2.4.19</td>
</tr>
<tr>
<td>Process ID (pid)</td>
<td>隔离进程 ID</td>
<td>2.6.24</td>
</tr>
<tr>
<td>Network (net)</td>
<td>隔离网络设备，端口号等</td>
<td>2.6.29</td>
</tr>
<tr>
<td>Interprocess Communication (ipc)</td>
<td>隔离 System V IPC 和 POSIX message queues</td>
<td>2.6.19</td>
</tr>
<tr>
<td>UTS Namespace(uts)</td>
<td>隔离主机名和域名</td>
<td>2.6.19</td>
</tr>
<tr>
<td>User Namespace (user)</td>
<td>隔离用户和用户组</td>
<td>3.8</td>
</tr>
<tr>
<td>Control group (cgroup) Namespace</td>
<td>隔离 Cgroups 根目录</td>
<td>4.6</td>
</tr>
<tr>
<td>Time Namespace</td>
<td>隔离系统时间</td>
<td>5.6</td>
</tr>
</tbody></table>
<p>虽然 Linux 内核提供了8种 Namespace，但是最新版本的 Docker 只使用了其中的前6 种，分别为Mount Namespace、PID Namespace、Net Namespace、IPC Namespace、UTS Namespace、User Namespac</p>
<h2 id="Mount-NameSpace"><a href="#Mount-NameSpace" class="headerlink" title="Mount NameSpace"></a>Mount NameSpace</h2><p>它可以用来隔离不同的进程或进程组看到的挂载点。通俗地说，就是可以实现在不同的进程中看到不同的挂载目录。</p>
<p><strong>使用 unshare 命令可以新建 Mount Namespace，并且在新建的 Mount Namespace 内 mount 是和外部完全隔离的</strong></p>
<h2 id="PID-NameSpace"><a href="#PID-NameSpace" class="headerlink" title="PID NameSpace"></a>PID NameSpace</h2><p>隔离进程，在不同的PID NameSpace中,进程可以拥有相同的PID号，利用PID NameSpace可以实现每个容器的主进程为1号进程，而容器内的进程在主机上却拥有不同的pid</p>
<h2 id="UTS-NameSpace"><a href="#UTS-NameSpace" class="headerlink" title="UTS NameSpace"></a>UTS NameSpace</h2><p>隔离主机名的，它可以让每个 UTS NameSpace 拥有一个独立的主机名。</p>
<h2 id="IPC-NameSpace"><a href="#IPC-NameSpace" class="headerlink" title="IPC NameSpace"></a>IPC NameSpace</h2><p>隔离进程间通信的。</p>
<p>例如 PID Namespace 和 IPC Namespace 一起使用可以实现同一 IPC Namespace 内的进程彼此可以通信，不同 IPC Namespace 的进程却不能通信</p>
<h2 id="User-NameSpace"><a href="#User-NameSpace" class="headerlink" title="User NameSpace"></a>User NameSpace</h2><p>User Namespace 主要是用来隔离用户和用户组的</p>
<p>一个比较典型的应用场景就是在主机上以非 root 用户运行的进程可以在一个单独的 User Namespace 中映射成 root 用户</p>
<p>使用 User Namespace 可以实现进程在容器内拥有 root 权限，而在主机上却只是普通用户</p>
<h2 id="Net-NameSpace"><a href="#Net-NameSpace" class="headerlink" title="Net NameSpace"></a>Net NameSpace</h2><p>隔离网络设备，IP和端口等信息</p>
<p>Net Namespace 可以让每个进程拥有自己独立的ip地址，端口信息和网卡信息</p>
<h1 id="Cgroups"><a href="#Cgroups" class="headerlink" title="Cgroups"></a>Cgroups</h1><p>我们知道使用不同的 Namespace，可以实现容器中的进程看不到别的容器的资源，但是有一个问题你是否注意到？容器内的进程仍然可以任意地使用主机的 CPU 、内存等资源，如果某一个容器使用的主机资源过多，可能导致主机的资源竞争，进而影响业务</p>
<p>那如果我们想限制一个容器资源的使用(如CPU,内存等)应该如何做呢?</p>
<p>cgroups : control groups 是 Linux内核的一个功能,它可以实现限制进程或者资源组的资源（如CPU,内存,磁盘IO等）</p>
<details><summary>cgroups主要功能</summary>

</details>

<details><summary>cgroup主要概念</summary>

</details>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(base) luohong<span class="meta">@luohong</span>:~$ sudo mount -t cgroup</span><br><span class="line">请验证人脸或密码:</span><br><span class="line">验证成功</span><br><span class="line">cgroup on /sys/fs/cgroup/systemd type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,xattr,name=systemd)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/blkio type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,blkio)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/rdma type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,rdma)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/hugetlb type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,hugetlb)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/pids type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,pids)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/net_cls,net_prio type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,net_cls,net_prio)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/freezer type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,freezer)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/cpu,cpuacct type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,cpu,cpuacct)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/cpuset type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,cpuset)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/memory type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,memory)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/devices type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,devices)</span></span><br><span class="line">cgroup on /sys/fs/cgroup/perf_event type <span class="title function_">cgroup</span> <span class="params">(rw,nosuid,nodev,noexec,relatime,perf_event)</span></span><br></pre></td></tr></table></figure>

<h1 id="docker组成"><a href="#docker组成" class="headerlink" title="docker组成"></a>docker组成</h1><p>Docker 整体架构采用 C&#x2F;S（客户端 &#x2F; 服务器）模式，主要由客户端和服务端两大部分组成。客户端负责发送操作指令，服务端负责接收和处理指令。客户端和服务端通信有多种方式，即可以在同一台机器上通过<code>UNIX</code>套接字通信，也可以通过网络连接远程通信</p>
<p>如何查看本机安装路径: <code>sudo docker info | grep &quot;Docker Root Dir&quot;</code></p>
<p>docker 路径下的东西</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">root<span class="meta">@luohong</span>:~# cd /<span class="keyword">var</span>/lib/docker/</span><br><span class="line">root<span class="meta">@luohong</span>:/<span class="keyword">var</span>/lib/docker# ls</span><br><span class="line">buildkit  containers  engine-id  image  network  overlay2  plugins  runtimes  swarm  tmp  volumes</span><br><span class="line">root<span class="meta">@luohong</span>:/<span class="keyword">var</span>/lib/docker# ls -l</span><br><span class="line">总用量 <span class="number">52</span></span><br><span class="line">drwx--x--x  <span class="number">4</span> root root  <span class="number">4096</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> buildkit</span><br><span class="line">drwx--x--- <span class="number">12</span> root root  <span class="number">4096</span> <span class="number">5</span>月  <span class="number">25</span> <span class="number">15</span>:<span class="number">23</span> containers</span><br><span class="line">-rw-------  <span class="number">1</span> root root    <span class="number">36</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> engine-id</span><br><span class="line">drwx------  <span class="number">3</span> root root  <span class="number">4096</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> image</span><br><span class="line">drwxr-x---  <span class="number">3</span> root root  <span class="number">4096</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> network</span><br><span class="line">drwx--x--- <span class="number">86</span> root root <span class="number">12288</span> <span class="number">6</span>月   <span class="number">5</span> <span class="number">05</span>:<span class="number">29</span> overlay2</span><br><span class="line">drwx------  <span class="number">4</span> root root  <span class="number">4096</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> plugins</span><br><span class="line">drwx------  <span class="number">2</span> root root  <span class="number">4096</span> <span class="number">6</span>月   <span class="number">5</span> <span class="number">05</span>:<span class="number">29</span> runtimes</span><br><span class="line">drwx------  <span class="number">2</span> root root  <span class="number">4096</span> <span class="number">3</span>月   <span class="number">6</span> <span class="number">17</span>:<span class="number">33</span> swarm</span><br><span class="line">drwx------  <span class="number">2</span> root root  <span class="number">4096</span> <span class="number">6</span>月   <span class="number">5</span> <span class="number">05</span>:<span class="number">29</span> tmp</span><br><span class="line">drwx-----x <span class="number">10</span> root root  <span class="number">4096</span> <span class="number">6</span>月   <span class="number">5</span> <span class="number">05</span>:<span class="number">29</span> volumes</span><br></pre></td></tr></table></figure>

<details><summary>docker相关组件</summary>

</details>

<h2 id="docker"><a href="#docker" class="headerlink" title="docker"></a>docker</h2><p>docker是Docker客户端的一个完整实现,它是一个二进制文件</p>
<p>Docker 客户端与服务端的交互过程是：docker 组件向服务端发送请求后，服务端根据请求执行具体的动作并将结果返回给 docker，docker 解析服务端的返回结果，并将结果通过命令行标准输出展示给用户。这样一次完整的客户端服务端请求就完成了</p>
<h2 id="dockerd"><a href="#dockerd" class="headerlink" title="dockerd"></a>dockerd</h2><p>dockerd是docker服务端的后台进程常驻进程，用来接受客户端发送的请求，执行具体的处理任务，处理完后将结果返回给客户端</p>
<details><summary>Docker 客户端可以通过多种方式向 dockerd 发送请求，我们常用的 Docker 客户端与 dockerd 的交互方式有三种- </summary>

</details>

<h2 id="docker-init"><a href="#docker-init" class="headerlink" title="docker-init"></a>docker-init</h2><p>如果你熟悉 Linux 系统，你应该知道在 Linux 系统中，1 号进程是 init 进程，是所有进程的父进程。主机上的进程出现问题时，init 进程可以帮我们回收这些问题进程。同样的，在容器内部，当我们自己的业务进程没有回收子进程的能力时，在执行 docker run 启动容器时可以添加 –init 参数，此时 Docker 会使用 docker-init 作为1号进程，帮你管理容器内子进程，例如回收僵尸进程等</p>
<h2 id="docker-proxy"><a href="#docker-proxy" class="headerlink" title="docker-proxy"></a>docker-proxy</h2><p>docker-proxy 主要是用来做端口映射的。当我们使用 docker run 命令启动容器时，如果使用了 -p 参数，docker-proxy 组件就会把容器内相应的端口映射到主机上来，底层是依赖于 iptables 实现的</p>
<h2 id="containerd"><a href="#containerd" class="headerlink" title="containerd"></a><strong>containerd</strong></h2><p>containerd 不仅负责容器生命周期的管理，同时还负责一些其它的功能</p>
<ul>
<li>镜像的管理，例如容器镜像运行前从镜像仓库拉取到镜像本地</li>
<li>接受dockerd的请求，通过适当的参数调用runc启动容器</li>
<li>管理存储相关资源</li>
<li>管理网络相关资源</li>
</ul>
<p>containerd-shim 将 containerd和真正的进程解耦，使用containerd-shim作为容器进程的父进程，从而实现重启containerd不影响已经启动的容器进程</p>
<p>ctr , 他是 containerd 的客户端,主要用于开发和调试，在没有dockerd的环境中，ctr可以充当docker客户端的角色，直接向contanierd守护进程发送操作容器的请求</p>
<h2 id="runc"><a href="#runc" class="headerlink" title="runc"></a>runc</h2><p>runc是一个标准的OCI容器运行时的实现，它是一个命令行的工具，可以直接来创建和运行容器</p>
<p><img src="/images/posts/docker_common1/img-3.png" alt="Docker知识"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker搭建组建手册</title>
    <url>//article/docker_common_start/</url>
    <content><![CDATA[<blockquote>
<p>😀 </p>
</blockquote>
<blockquote>
<p>这里写文章的前言：<br>记录使用docker搭建的各种组建和手册</p>
</blockquote>
<h1 id="📝-RocketMQ"><a href="#📝-RocketMQ" class="headerlink" title="📝 RocketMQ"></a>📝 RocketMQ</h1><h2 id="4-9-1版本"><a href="#4-9-1版本" class="headerlink" title="4.9.1版本"></a>4.9.1版本</h2><h3 id="拉取镜像-运行namesrv"><a href="#拉取镜像-运行namesrv" class="headerlink" title="拉取镜像 &amp; 运行namesrv"></a>拉取镜像 &amp; 运行namesrv</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">sudo docker pull apache/rocketmq:<span class="number">4.9</span><span class="number">.3</span></span><br><span class="line"></span><br><span class="line">sudo docker network create rocketmq</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo docker run -d --name rocketmq-namesrv --network rocketmq -p <span class="number">9876</span>:<span class="number">9876</span> -v /home/luohong/coding/dockers/rocketmq/namesrv/logs:/root/logs -v /home/luohong/coding/dockers/rocketmq/namesrv/store:/root/store apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span> sh mqnamesrv</span><br></pre></td></tr></table></figure>

<details><summary>> 查看 rocketmq-namesrv 状态</summary>

</details>

<p>docker cp 容器ID:容器内文件位置 本地位置</p>
<p>4b6a5f523bfe : 是自己本地允许的 rocketmq-namesrv的 container id</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo docker cp 4b6a5f523bfe:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf /home/luohong/coding/dockers/rocketmq/broker</span><br></pre></td></tr></table></figure>

<h3 id="borker启动"><a href="#borker启动" class="headerlink" title="borker启动"></a>borker启动</h3><p>修broker.conf配置文件;添加如下内容</p>
<p>172.21.129.80 修改为自己的ip地址</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 设置broker节点所在服务器的ip地址（**这个非常重要,主从模式下，从节点会根据主节点的brokerIP2来同步数据，如果不配置，主从无法同步，brokerIP1设置为自己外网能访问的ip，服务器双网卡情况下必须配置，比如阿里云这种，主节点需要配置ip1和ip2，从节点只需要配置ip1即可）</span><br><span class="line">brokerIP1 = <span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span></span><br><span class="line">#nameServer地址，分号分割</span><br><span class="line">namesrvAddr=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span></span><br><span class="line">#Broker 对外服务的监听端口,</span><br><span class="line">listenPort = <span class="number">10911</span></span><br><span class="line">#是否允许Broker自动创建<span class="type">Topic</span></span><br><span class="line"><span class="variable">autoCreateTopicEnable</span> <span class="operator">=</span> <span class="literal">true</span></span><br><span class="line">#是否允许 Broker 自动创建订阅组</span><br><span class="line">autoCreateSubscriptionGroup = <span class="literal">true</span></span><br><span class="line">#linux开启<span class="type">epoll</span></span><br><span class="line"><span class="variable">useEpollNativeSelector</span> <span class="operator">=</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>默认启动 和 指定内存大小启动 , 二选一</p>
<p>默认启动</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">docker run -d --name rocketmq-broker-a --network rocketmq -p <span class="number">10909</span>:<span class="number">10909</span> -p <span class="number">10911</span>:<span class="number">10911</span> -v /home/docker/mount/rocketmq/broker/broker-a/logs:/root/logs -v /home/docker/mount/rocketmq/broker/broker-a/store:/root/store -v /home/docker/mount/rocketmq/broker/broker-a/conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.3</span>/conf apache/rocketmq:<span class="number">4.9</span><span class="number">.3</span> sh mqbroker -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.3</span>/conf/broker.conf</span><br></pre></td></tr></table></figure>

<p>指定内存大小启动</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo docker run -d --name rocketmq-broker-a --network rocketmq -p <span class="number">10909</span>:<span class="number">10909</span> -p <span class="number">10911</span>:<span class="number">10911</span> -v /home/luohong/coding/dockers/rocketmq/broker/logs:/root/logs -v /home/luohong/coding/dockers/rocketmq/broker/store:/root/store -v /home/luohong/coding/dockers/rocketmq/broker/conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf -e <span class="string">&quot;JAVA_OPT_EXT=-server -Xms1g -Xmx1g -Xmn512m&quot;</span> apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span> sh mqbroker -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br></pre></td></tr></table></figure>

<h3 id="控制台启动"><a href="#控制台启动" class="headerlink" title="控制台启动"></a>控制台启动</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo docker run -d --name rocketmq-console --network rocketmq -e <span class="string">&quot;JAVA_OPTS=-Drocketmq.namesrv.addr=rocketmq-namesrv:9876 -Dcom.rocketmq.sendMessageWithVIPChannel=false&quot;</span> -p <span class="number">8000</span>:<span class="number">8080</span> apacherocketmq/rocketmq-dashboard:latest</span><br></pre></td></tr></table></figure>

<h2 id="docker-compose搭建集群"><a href="#docker-compose搭建集群" class="headerlink" title="docker-compose搭建集群"></a>docker-<strong>compose搭建集群</strong></h2><p>以下ip和路径都根据自己真实的ip和路径进行调整</p>
<p>创建目录</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mkdir -p /home/luohong/coding/dockers/rocketmq/clusters/&#123;logs-nameserver-m,logs-nameserver-s,logs-a,logs-a-s,logs-b,logs-b-s,store-a,store-a-s,store-b,store-b-s,conf&#125;</span><br></pre></td></tr></table></figure>

<p><strong>broker-a.conf</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Licensed to the Apache Software <span class="title function_">Foundation</span> <span class="params">(ASF)</span> under one or more</span><br><span class="line"># contributor license agreements.  See the NOTICE file distributed with</span><br><span class="line"># <span class="built_in">this</span> work <span class="keyword">for</span> additional information regarding copyright ownership.</span><br><span class="line"># The ASF licenses <span class="built_in">this</span> file to You under the Apache License, Version <span class="number">2.0</span></span><br><span class="line"># (the <span class="string">&quot;License&quot;</span>); you may not use <span class="built_in">this</span> file except in compliance with</span><br><span class="line"># the License.  You may obtain a copy of the License at</span><br><span class="line">#</span><br><span class="line">#     http:<span class="comment">//www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line">#</span><br><span class="line"># Unless required by applicable law or agreed to in writing, software</span><br><span class="line"># distributed under the License is distributed on an <span class="string">&quot;AS IS&quot;</span> BASIS,</span><br><span class="line"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span><br><span class="line"># See the License <span class="keyword">for</span> the specific language governing permissions and</span><br><span class="line"># limitations under the License.</span><br><span class="line">#所属集群名字，同一个集群名字相同</span><br><span class="line">brokerClusterName=rocketmq-cluster</span><br><span class="line">#broker名字</span><br><span class="line">brokerName=broker-a</span><br><span class="line">#<span class="number">0</span>表示master &gt;<span class="number">0</span> 表示slave</span><br><span class="line">brokerId=<span class="number">0</span></span><br><span class="line">#删除文件的时间点，凌晨<span class="number">4</span>点</span><br><span class="line">deleteWhen=<span class="number">04</span></span><br><span class="line">#文件保留时间 默认是<span class="number">48</span>小时</span><br><span class="line">fileReservedTime=<span class="number">168</span></span><br><span class="line">#异步复制Master</span><br><span class="line">brokerRole=ASYNC_MASTER</span><br><span class="line">#刷盘方式，ASYNC_FLUSH=异步刷盘，SYNC_FLUSH=同步刷盘 </span><br><span class="line">flushDiskType=ASYNC_FLUSH</span><br><span class="line">#Broker 对外服务的监听端口</span><br><span class="line">listenPort=<span class="number">10911</span></span><br><span class="line">#nameServer地址，这里nameserver是单台，如果nameserver是多台集群的话，就用分号分割（即namesrvAddr=ip1:port1;ip2:port2;ip3:port3）</span><br><span class="line">namesrvAddr=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">#每个topic对应队列的数量，默认为<span class="number">4</span>，实际应参考consumer实例的数量，值过小不利于consumer负载均衡</span><br><span class="line">defaultTopicQueueNums=<span class="number">8</span></span><br><span class="line">#是否允许 Broker 自动创建Topic，生产建议关闭</span><br><span class="line">autoCreateTopicEnable=<span class="literal">true</span></span><br><span class="line">#是否允许 Broker 自动创建订阅组，生产建议关闭</span><br><span class="line">autoCreateSubscriptionGroup=<span class="literal">true</span></span><br><span class="line">#设置BrokerIP</span><br><span class="line">brokerIP1=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span></span><br></pre></td></tr></table></figure>

<p><strong>broker-b.conf</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Licensed to the Apache Software <span class="title function_">Foundation</span> <span class="params">(ASF)</span> under one or more</span><br><span class="line"># contributor license agreements.  See the NOTICE file distributed with</span><br><span class="line"># <span class="built_in">this</span> work <span class="keyword">for</span> additional information regarding copyright ownership.</span><br><span class="line"># The ASF licenses <span class="built_in">this</span> file to You under the Apache License, Version <span class="number">2.0</span></span><br><span class="line"># (the <span class="string">&quot;License&quot;</span>); you may not use <span class="built_in">this</span> file except in compliance with</span><br><span class="line"># the License.  You may obtain a copy of the License at</span><br><span class="line">#</span><br><span class="line">#     http:<span class="comment">//www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line">#</span><br><span class="line"># Unless required by applicable law or agreed to in writing, software</span><br><span class="line"># distributed under the License is distributed on an <span class="string">&quot;AS IS&quot;</span> BASIS,</span><br><span class="line"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span><br><span class="line"># See the License <span class="keyword">for</span> the specific language governing permissions and</span><br><span class="line"># limitations under the License.</span><br><span class="line">#所属集群名字</span><br><span class="line">brokerClusterName=rocketmq-cluster</span><br><span class="line">#broker名字，注意此处不同的配置文件填写的不一样  例如：在a.properties 文件中写 broker-a  在b.properties 文件中写 broker-b</span><br><span class="line">brokerName=broker-b</span><br><span class="line">#<span class="number">0</span> 表示 Master，&gt;<span class="number">0</span> 表示 Slave</span><br><span class="line">brokerId=<span class="number">0</span></span><br><span class="line">#删除文件时间点，默认凌晨 <span class="number">4</span>点</span><br><span class="line">deleteWhen=<span class="number">04</span></span><br><span class="line">#文件保留时间，默认 <span class="number">48</span> 小时</span><br><span class="line">fileReservedTime=<span class="number">168</span></span><br><span class="line">#Broker 的角色，ASYNC_MASTER=异步复制Master，SYNC_MASTER=同步双写Master，SLAVE=slave节点</span><br><span class="line">brokerRole=ASYNC_MASTER</span><br><span class="line">#刷盘方式，ASYNC_FLUSH=异步刷盘，SYNC_FLUSH=同步刷盘 </span><br><span class="line">flushDiskType=SYNC_FLUSH</span><br><span class="line">#Broker 对外服务的监听端口</span><br><span class="line">listenPort=<span class="number">11911</span></span><br><span class="line">#nameServer地址，这里nameserver是单台，如果nameserver是多台集群的话，就用分号分割（即namesrvAddr=ip1:port1;ip2:port2;ip3:port3）</span><br><span class="line">namesrvAddr=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">#每个topic对应队列的数量，默认为<span class="number">4</span>，实际应参考consumer实例的数量，值过小不利于consumer负载均衡</span><br><span class="line">defaultTopicQueueNums=<span class="number">8</span></span><br><span class="line">#是否允许 Broker 自动创建Topic，生产建议关闭</span><br><span class="line">autoCreateTopicEnable=<span class="literal">true</span></span><br><span class="line">#是否允许 Broker 自动创建订阅组，生产建议关闭</span><br><span class="line">autoCreateSubscriptionGroup=<span class="literal">true</span></span><br><span class="line">#设置BrokerIP</span><br><span class="line">brokerIP1=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span></span><br></pre></td></tr></table></figure>

<p><strong>broker-a-s.conf</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Licensed to the Apache Software <span class="title function_">Foundation</span> <span class="params">(ASF)</span> under one or more</span><br><span class="line"># contributor license agreements.  See the NOTICE file distributed with</span><br><span class="line"># <span class="built_in">this</span> work <span class="keyword">for</span> additional information regarding copyright ownership.</span><br><span class="line"># The ASF licenses <span class="built_in">this</span> file to You under the Apache License, Version <span class="number">2.0</span></span><br><span class="line"># (the <span class="string">&quot;License&quot;</span>); you may not use <span class="built_in">this</span> file except in compliance with</span><br><span class="line"># the License.  You may obtain a copy of the License at</span><br><span class="line">#</span><br><span class="line">#     http:<span class="comment">//www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line">#</span><br><span class="line"># Unless required by applicable law or agreed to in writing, software</span><br><span class="line"># distributed under the License is distributed on an <span class="string">&quot;AS IS&quot;</span> BASIS,</span><br><span class="line"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span><br><span class="line"># See the License <span class="keyword">for</span> the specific language governing permissions and</span><br><span class="line"># limitations under the License.</span><br><span class="line">#所属集群名字</span><br><span class="line">brokerClusterName=rocketmq-cluster</span><br><span class="line">#broker名字，注意此处不同的配置文件填写的不一样  例如：在a.properties 文件中写 broker-a  在b.properties 文件中写 broker-b</span><br><span class="line">brokerName=broker-a</span><br><span class="line">#<span class="number">0</span> 表示 Master，&gt;<span class="number">0</span> 表示 Slave</span><br><span class="line">brokerId=<span class="number">1</span></span><br><span class="line">#删除文件时间点，默认凌晨 <span class="number">4</span>点</span><br><span class="line">deleteWhen=<span class="number">04</span></span><br><span class="line">#文件保留时间，默认 <span class="number">48</span> 小时</span><br><span class="line">fileReservedTime=<span class="number">168</span></span><br><span class="line">#Broker 的角色，ASYNC_MASTER=异步复制Master，SYNC_MASTER=同步双写Master，SLAVE=slave节点</span><br><span class="line">brokerRole=SLAVE</span><br><span class="line">#刷盘方式，ASYNC_FLUSH=异步刷盘，SYNC_FLUSH=同步刷盘 </span><br><span class="line">flushDiskType=SYNC_FLUSH</span><br><span class="line">#Broker 对外服务的监听端口</span><br><span class="line">listenPort=<span class="number">12911</span></span><br><span class="line">#nameServer地址，这里nameserver是单台，如果nameserver是多台集群的话，就用分号分割（即namesrvAddr=ip1:port1;ip2:port2;ip3:port3）</span><br><span class="line">namesrvAddr=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">#每个topic对应队列的数量，默认为<span class="number">4</span>，实际应参考consumer实例的数量，值过小不利于consumer负载均衡</span><br><span class="line">defaultTopicQueueNums=<span class="number">8</span></span><br><span class="line">#是否允许 Broker 自动创建Topic，生产建议关闭</span><br><span class="line">autoCreateTopicEnable=<span class="literal">true</span></span><br><span class="line">#是否允许 Broker 自动创建订阅组，生产建议关闭</span><br><span class="line">autoCreateSubscriptionGroup=<span class="literal">true</span></span><br><span class="line">#设置BrokerIP</span><br><span class="line">brokerIP1=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span></span><br></pre></td></tr></table></figure>

<p><strong>broker-b-s.conf</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Licensed to the Apache Software <span class="title function_">Foundation</span> <span class="params">(ASF)</span> under one or more</span><br><span class="line"># contributor license agreements.  See the NOTICE file distributed with</span><br><span class="line"># <span class="built_in">this</span> work <span class="keyword">for</span> additional information regarding copyright ownership.</span><br><span class="line"># The ASF licenses <span class="built_in">this</span> file to You under the Apache License, Version <span class="number">2.0</span></span><br><span class="line"># (the <span class="string">&quot;License&quot;</span>); you may not use <span class="built_in">this</span> file except in compliance with</span><br><span class="line"># the License.  You may obtain a copy of the License at</span><br><span class="line">#</span><br><span class="line">#     http:<span class="comment">//www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line">#</span><br><span class="line"># Unless required by applicable law or agreed to in writing, software</span><br><span class="line"># distributed under the License is distributed on an <span class="string">&quot;AS IS&quot;</span> BASIS,</span><br><span class="line"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span><br><span class="line"># See the License <span class="keyword">for</span> the specific language governing permissions and</span><br><span class="line"># limitations under the License.</span><br><span class="line">brokerClusterName=rocketmq-cluster</span><br><span class="line">brokerName=broker-b</span><br><span class="line">#slave</span><br><span class="line">brokerId=<span class="number">1</span></span><br><span class="line">deleteWhen=<span class="number">04</span></span><br><span class="line">fileReservedTime=<span class="number">168</span></span><br><span class="line">brokerRole=SLAVE</span><br><span class="line">flushDiskType=ASYNC_FLUSH</span><br><span class="line">#Broker 对外服务的监听端口</span><br><span class="line">listenPort=<span class="number">13911</span></span><br><span class="line">#nameServer地址，这里nameserver是单台，如果nameserver是多台集群的话，就用分号分割（即namesrvAddr=ip1:port1;ip2:port2;ip3:port3）</span><br><span class="line">namesrvAddr=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">#每个topic对应队列的数量，默认为<span class="number">4</span>，实际应参考consumer实例的数量，值过小不利于consumer负载均衡</span><br><span class="line">defaultTopicQueueNums=<span class="number">8</span></span><br><span class="line">#是否允许 Broker 自动创建Topic，生产建议关闭</span><br><span class="line">autoCreateTopicEnable=<span class="literal">true</span></span><br><span class="line">#是否允许 Broker 自动创建订阅组，生产建议关闭</span><br><span class="line">autoCreateSubscriptionGroup=<span class="literal">true</span></span><br><span class="line">#设置BrokerIP</span><br><span class="line">brokerIP1=<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span></span><br></pre></td></tr></table></figure>

<p><strong>docker-compose.yml 文件</strong></p>
<p>volumes 挂载前的路径切换为自己的真实的路径</p>
<p>ip 和 端口等信息，也可以根据自己实际的情况来真实的调整</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">version: <span class="string">&#x27;2&#x27;</span></span><br><span class="line">services:</span><br><span class="line">  namesrv1:</span><br><span class="line">    image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">    container_name: rmqnamesrv1</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="number">9876</span>:<span class="number">9876</span></span><br><span class="line">    volumes:</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/logs-nameserver-m:/home/rocketmq/logs</span><br><span class="line">    environment:</span><br><span class="line">      JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">    command: sh mqnamesrv</span><br><span class="line">  namesrv2:</span><br><span class="line">    image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">    container_name: rmqnamesrv2</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="number">9877</span>:<span class="number">9877</span></span><br><span class="line">    volumes:</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/logs-nameserver-s:/home/rocketmq/logs</span><br><span class="line">    environment:</span><br><span class="line">      JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">    command: sh mqnamesrv</span><br><span class="line">  broker-a-m:</span><br><span class="line">      image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">      container_name: rmqbroker-a-master</span><br><span class="line">      ports:</span><br><span class="line">        - <span class="number">10909</span>:<span class="number">10909</span></span><br><span class="line">        - <span class="number">10911</span>:<span class="number">10911</span></span><br><span class="line">        - <span class="number">10912</span>:<span class="number">10912</span></span><br><span class="line">      volumes:</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/logs-a:/root/logs</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/store-a:/root/store</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/conf/broker-a.conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">      environment:</span><br><span class="line">        JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">        NAMESRV_ADDR: <span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">      command: sh mqbroker  -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">  broker-b-m:</span><br><span class="line">    image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">    container_name: rmqbroker-b-master</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="number">11909</span>:<span class="number">11909</span></span><br><span class="line">      - <span class="number">11911</span>:<span class="number">11911</span></span><br><span class="line">      - <span class="number">11912</span>:<span class="number">11912</span></span><br><span class="line">    volumes:</span><br><span class="line">    - /home/luohong/coding/dockers/rocketmq/clusters/logs-b:/root/logs</span><br><span class="line">    - /home/luohong/coding/dockers/rocketmq/clusters/store-b:/root/store</span><br><span class="line">    - /home/luohong/coding/dockers/rocketmq/clusters/conf/broker-b.conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">    environment:</span><br><span class="line">      JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">      NAMESRV_ADDR: <span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">    command: sh mqbroker  -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">  broker-a-s:</span><br><span class="line">    image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">    container_name: rmqbroker-a-slave</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="number">12909</span>:<span class="number">12909</span></span><br><span class="line">      - <span class="number">12911</span>:<span class="number">12911</span></span><br><span class="line">      - <span class="number">12912</span>:<span class="number">12912</span></span><br><span class="line">    volumes:</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/logs-a-s:/root/logs</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/store-a-s:/root/store</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/conf/broker-a-s.conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">    environment:</span><br><span class="line">      JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">      NAMESRV_ADDR: <span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">    command: sh mqbroker  -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">  broker-b-s:</span><br><span class="line">    image: apache/rocketmq:<span class="number">4.9</span><span class="number">.1</span></span><br><span class="line">    container_name: rmqbroker-b-slave</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="number">13909</span>:<span class="number">13909</span></span><br><span class="line">      - <span class="number">13911</span>:<span class="number">13911</span></span><br><span class="line">      - <span class="number">13912</span>:<span class="number">13912</span></span><br><span class="line">    volumes:</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/logs-b-s:/root/logs</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/store-b-s:/root/store</span><br><span class="line">      - /home/luohong/coding/dockers/rocketmq/clusters/conf/broker-b-s.conf:/home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">    environment:</span><br><span class="line">      JAVA_OPT_EXT: -server -Xms512m -Xmx512m -Xmn150m</span><br><span class="line">      NAMESRV_ADDR: <span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9876</span>;<span class="number">172.21</span><span class="number">.129</span><span class="number">.80</span>:<span class="number">9877</span></span><br><span class="line">    command: sh mqbroker  -c /home/rocketmq/rocketmq-<span class="number">4.9</span><span class="number">.1</span>/conf/broker.conf</span><br><span class="line">    depends_on:</span><br><span class="line">      - namesrv1</span><br><span class="line">      - namesrv2</span><br></pre></td></tr></table></figure>

<p><strong>172.21.129.80和端口修改为自己的;也就是ip和端口</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo docker run -d --name rocketmq-console1 -e <span class="string">&quot;JAVA_OPTS=-Drocketmq.namesrv.addr=172.21.129.80:9876;172.21.129.80:9877 -Dcom.rocketmq.sendMessageWithVIPChannel=false&quot;</span> -p <span class="number">8000</span>:<span class="number">8080</span> apacherocketmq/rocketmq-dashboard:latest</span><br></pre></td></tr></table></figure>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis基础</title>
    <url>//article/redis_basic/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<p><img src="/images/posts/redis_basic/img-1.png" alt="Redis基础"></p>
<h1 id="📝-redis结构"><a href="#📝-redis结构" class="headerlink" title="📝 redis结构"></a>📝 <strong>redis结构</strong></h1><p>高性能主线： 线程模型，数据结构，持久化 网络框架：高可靠主线，包括主从复制，哨兵机制 高可扩展主线：数据分片，负载均衡</p>
<p>Redis是一种运行速度很快，并发很强的跑在内存上的NOSQL数据库</p>
<ol>
<li>基于内存运行，CPU不是瓶颈，所以不用多线程，单线程实现简单，瓶颈在于内存</li>
<li>支持8种数据结构</li>
<li>支持分布式，理论上可以无线扩充，cluster集群模式</li>
<li>key-value存储模式</li>
<li>可持久化文件</li>
<li>最多每秒可以处理10w+请求</li>
</ol>
<p><img src="/images/posts/redis_basic/img-2.png" alt="Redis基础"></p>
<p><img src="/images/posts/redis_basic/img-3.png" alt="Redis基础"></p>
<h1 id="redis为什么快"><a href="#redis为什么快" class="headerlink" title="redis为什么快"></a>redis为什么快</h1><ol>
<li>完全基于内存，绝大部分请求是纯粹的内存操作，非常快速。数据存在内存中，类似HashMap，HashMap的优势就是查找和操作的时间复杂度都是O(1)</li>
<li>数据结构使用高效，Redis中的数据结构专门进行设计，并且操作的时间复杂度较低</li>
<li>采用单线程，避免了不必要的上下文切换和竞态条件，也不存在多进程或多线程导致的切换而消耗CPU，不用去考虑各种锁的问题，不存在加锁释放锁操作，没有因为可能出现死锁而导致的性能消耗;redis的IO模型是: client -&gt; io多路复用 –&gt; 事件分发器 –&gt; 事件处理器</li>
<li>使用 io 多路复用，非阻塞IO</li>
<li>使用底层模型不同，它们之间底层实现方式以及客户端之间通信的应用协议不一样，redis直接自己构建 VM 机制，因为有一些系统函数效率不高，内核态转换代价太高，会浪费一定的时间去移动和请求</li>
</ol>
<p>6.0 版本之后的多线程 Redis一直是多线程的，只是执行命令的事件处理器是单线程，所以线程安全 6.0之前网络处理器也是单线程，6.0之后处理网络，删除key的部分功能改成了异步线程去操作的</p>
<h1 id="Redis过期策略"><a href="#Redis过期策略" class="headerlink" title="Redis过期策略"></a><strong>Redis过期策略</strong></h1><h2 id="过期key删除策略"><a href="#过期key删除策略" class="headerlink" title="过期key删除策略"></a><strong>过期key删除策略</strong></h2><ol>
<li>惰性删除： 一个key过期后并不会直接删除，而是等到该key再次访问时，才执行删除，惰性删除对内存不友好，但是对CPU友好。只有到key被访问了，我们才会去判断key是否过期，过期就直接删除key，返回nil。但是如果有key长时间没有被访问到，内存就一直被占用</li>
<li>定期删除：多少秒去遍历下内存里面的key，如果过期了删除(核心在于不要去影响性能，时间不宜过长，次数不宜过多)</li>
</ol>
<p>定期删除时会遍历每个database(默认16个)，检查当前库中指定个数的key(默认是20个)。 随机抽查这些key，如果有过期的，就删除。 程序中有一个全局变量记录到秒到了哪个数据库</p>
<ol>
<li>定时删除： 绑定一个定时器，这个定时器会在时间到的时候，把这个key从内存中删除</li>
</ol>
<p>定时删除是对内存最友好的，但是对cpu不友好。键只要一过期，我们就立马去删除key，释放内存空间。但是这意味着cpu需要不断的去遍历所有的key，来判断对方有没有过期</p>
<p>总结： redis最终采用 惰性删除 + 定期删除 的方式搭配使用，从库不会主动执行定期删除等策略</p>
<h2 id="内存淘汰策略"><a href="#内存淘汰策略" class="headerlink" title="内存淘汰策略"></a><strong>内存淘汰策略</strong></h2><ol>
<li>volatile-lru： 从已设置过期时间的key中，移出最近最少使用的key进行淘汰</li>
<li>volatile-ttl： 从已设置过期时间的key中，移除将要过期的key</li>
<li>volatile-random： 从已设置过期的key中，随机选择key淘汰</li>
<li>allkeys-lru： 从key中选择最近最少使用的进行淘汰</li>
<li>allkeys-random： 从key中选择key进行淘汰</li>
<li>noeviction： 当内存达到阈值的时候，新写入的操作报错</li>
</ol>
<h1 id="redis分布式锁"><a href="#redis分布式锁" class="headerlink" title="redis分布式锁"></a>redis分布式锁</h1><h2 id="指令加锁"><a href="#指令加锁" class="headerlink" title="指令加锁"></a><strong>指令加锁</strong></h2><p><strong>set key value PX milliseconds NX</strong> <strong>参数意义如下：</strong> key、value：键值对 PX milliseconds：设置键的过期时间为 milliseconds 毫秒。 NX：只在键不存在时，才对键进行设置操作。SET key value NX 效果等同于 SETNX key value。 PX、expireTime 参数则是用于解决没有解锁导致的死锁问题。因为如果没有过期时间，万一程序员写的代码有 bug 导致没有解锁操作，则就出现了死锁，因此该参数起到了一个“兜底”的作用。 NX 参数用于保证在多个线程并发 set 下，只会有1个线程成功，起到了锁的“唯一”性。</p>
<p>为了防止死锁，我们会给分布式锁加一个过期时间，但是万一这个时间到了，我们的业务逻辑还没处理完怎么办？ 首先，我们在设置过期时间时要结合业务场景去考虑，尽量设置一个比较合理的值，就是理论上正常处理的话，在这个过期时间内是一定能处理完毕的</p>
<h2 id="redission"><a href="#redission" class="headerlink" title="redission"></a>redission</h2><p>锁上成功后，分线程时不时看锁还在不在，主要主线程不删除，会一直加时间</p>
<p><img src="/images/posts/redis_basic/img-4.png" alt="Redis基础"></p>
<p>加锁使用lua脚本来完成，lua脚本保证原子性</p>
<p><img src="/images/posts/redis_basic/img-5.png" alt="Redis基础"></p>
<p>判断主线程的锁还不在</p>
<p><img src="/images/posts/redis_basic/img-6.png" alt="Redis基础"></p>
<p>总结redis分布式锁续命如何实现：</p>
<ol>
<li>首先尝试加锁时，加锁返回值为一个RFuture，RFuture添加了一个futurelistener的回调，等待加锁操作的结果</li>
<li>加锁操作成功后，如果加锁成功，那么就会开启续命逻辑，续命逻辑是一个timeout方法</li>
<li>timeout时间到则运行run方法，run方法内部通过lua脚本给其续命</li>
<li>续命操作同样返回值为一个RFuture，RFuture添加了一个futurelistener的回调，等待续命结果</li>
<li>续命如果成功，接着调用timeout方法，实现循环续命</li>
</ol>
<h2 id="分布式锁优化"><a href="#分布式锁优化" class="headerlink" title="分布式锁优化"></a>分布式锁优化</h2><ol>
<li>分段锁，分开存储一部分数据</li>
<li>锁的颗粒度，尽量低，像银行这种，同1个银行卡或者ID使用一个锁</li>
</ol>
<h1 id="Redis的delete指令"><a href="#Redis的delete指令" class="headerlink" title="Redis的delete指令"></a><strong>Redis的delete指令</strong></h1><ol>
<li>如果删除String类型的话，时间复杂度是O(1),单个字符串(BigKey也会阻塞)</li>
<li>如果删除的是List,Set,Hash集合类型，时间复杂度是O(N).n是集合内有多少个元素数量</li>
<li>返回值：被删除了多少个元素</li>
<li>如果删除的元素非常大，可能会造成阻塞，大Key的删除会额外有线程去异步删除</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>使用入门</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka基础</title>
    <url>//article/kafka_basic/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-架构图"><a href="#📝-架构图" class="headerlink" title="📝 架构图"></a>📝 <strong>架构图</strong></h1><ul>
<li>producer： 生产者，消息的产生者。</li>
<li>Broker： broker是kafka实例，每个服务器上有一个或多个kafka实例，理想情况下，每个broker对应一台服务器。比如broker-0,broker-1,broker-2分别是不重复等编号</li>
<li>Topic：消息的主题，可以理解为消息分类，kafka的数据就保存在topic里。每个broker可以创建多个topic</li>
<li>Partition：Topic的分区，每个topic有多个分区，分区的作用是负载，提高kafka的吞吐量。同一个topic在不同的分区数据是不重复等，每个partition的表形式就是一个一个文件夹</li>
<li>Replication：每个分区都有副本，副本的作用是备胎。当主分区(Leader)故障的时候会选择一个Follower上位，成为Leader。</li>
<li>Message：每一条消息的主题</li>
<li>Consumer：消费者，消息的消费方</li>
<li>ConsumerGroup：将多个消费者组成一个消费者，同一个分区的数据只能被消费者组中的某一个消费者消费。同一个消费者组的消费可以消费同一个topic的不同分区的数据，提供kafka的吞吐量</li>
<li>Zookeeper：Kafka集群依赖zookeeper来保存集群的元数据，来保证系统的可用性</li>
</ul>
<p><img src="/images/posts/kafka_basic/img-1.png" alt="Kafka基础"></p>
<h1 id="工作流程分析"><a href="#工作流程分析" class="headerlink" title="工作流程分析"></a><strong>工作流程分析</strong></h1><h2 id="高性能"><a href="#高性能" class="headerlink" title="高性能"></a>高性能</h2><ul>
<li>顺序读写磁盘 ： 持久化消息的时候，仅仅是将消息追加到日志文件的结尾，也就是磁盘顺序写，性能极高</li>
<li>MMap内存映射技术</li>
<li>零拷贝技术</li>
<li>数据批处理</li>
</ul>
<p><img src="/images/posts/kafka_basic/img-2.png" alt="Kafka基础"></p>
<h2 id="MMap内存映射"><a href="#MMap内存映射" class="headerlink" title="MMap内存映射"></a><strong>MMap内存映射</strong></h2><p>MMap就是内存映射文件，在64位操作系统中一般可以表示20G的数据文件，它的工作原理是直接利用操作系统的Page来实现文件到物理内存的直接映射，完成映射之后对物理内存的操作会被同步到磁盘上 通过MMap技术，进程可以像读写磁盘一样读写内存（逻辑内存），不必关心内存的大小，因为有虚拟内存兜底。这种方式可以极大的提升IO能力，省去了数据从用户空间到内核空间的复制开销 注意：MMap中的数据并没有真正的写入到磁盘，操作系统会定时定时刷盘操作，将Page Cache的数据flush到磁盘。kafka提供 producer.type 参数来控制是否同步刷盘</p>
<p><img src="/images/posts/kafka_basic/img-3.png" alt="Kafka基础"></p>
<h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a><strong>零拷贝</strong></h2><p>consumer消费消息时，会请求kafka broker从磁盘文件里读取消息，然后通过网络发送出去，整个过程涉及零拷贝 kafka broker利用linux的sendfile函数，直接将读取操作交给os，os会查看Page Cache中是否有数据，如果没有就从磁盘上读取并缓存到Page Cache，如果有就直接将Os Cache里的数据拷贝给网卡引擎，这样减少了上下文切换和数据复制的开销</p>
<p><img src="/images/posts/kafka_basic/img-4.png" alt="Kafka基础"></p>
<h2 id="数据批处理"><a href="#数据批处理" class="headerlink" title="数据批处理"></a><strong>数据批处理</strong></h2><p>当Consumer需要消费数据时，首先想到的是消费一条，Kafka发送一条。但实际上，Kafka 会把一批消息压缩存储，当消费者拉取数据时，实际上是拉到一批数据。比如说100万条消息压缩放到一个文件中可能就是10M的数据量，如果消费者和Kafka之间网络良好，10MB大概1秒就能发送完，即Kafka每秒处理了100万条消息。 正是因为这种批处理的方式，Kafka才有了极高的吞吐量。</p>
<h2 id="发送数据"><a href="#发送数据" class="headerlink" title="发送数据"></a><strong>发送数据</strong></h2><p>producer写入数据的时候永远找leader，不会直接将数据写入follower 消息写入leader后，follower是主动的去leader进行同步的</p>
<p><img src="/images/posts/kafka_basic/img-5.png" alt="Kafka基础"></p>
<p>写入分区，分区的好处</p>
<ul>
<li>方便扩展：一个topic可以有多个partition，通过扩展机器去轻松的应对日益增长的数据量</li>
<li>提高并发：以partition为读写单位，可以多个消费者同时消费数据，提高了消息的处理效率</li>
</ul>
<p>写入partition原则</p>
<ul>
<li>partition在写入的时候可以指定需要写入的partition，如果有指定，则写入对应的partition</li>
<li>如果没有指定partition，但是设置了数据的key，则会根据key的值hash出一个partition</li>
<li>即没有指定partition，也没有设置key，则会轮询选出一个partition</li>
</ul>
<p>写入消息的ack机制</p>
<ul>
<li>0代表producer往集群发送数据不需要等到集群的返回，不确保消息发送成功。安全性最低但是效率最高</li>
<li>1代表producer往集群发送消息只要leader应答就可以发送下一条，只确保leader发送成功</li>
<li>all代表producer往集群发送数据需要所有的follower都完成从leader的同步才回发送下一条，确保leader发送称该和所有的副本都完成备份。安全性最高，但是效率最低</li>
</ul>
<p><img src="/images/posts/kafka_basic/img-6.png" alt="Kafka基础"></p>
<h2 id="保存数据"><a href="#保存数据" class="headerlink" title="保存数据"></a><strong>保存数据</strong></h2><p>kafka初始化会单独开辟一块磁盘空间，顺序写入数据（效率比随机写入高）</p>
<h2 id="partition结构"><a href="#partition结构" class="headerlink" title="partition结构"></a><strong>partition结构</strong></h2><p>partition在服务器上的表现形式就是一个一个文件夹，每个partition文件夹下面会有多组segment文件，每组segment文件又包含 .index文件 , .log文件,.timeindex文件三个文件。 log文件实际存储message的地发 index 和 timeindex 文件成为检索文件，用于检索消息 文件的命名是以该segment最小的offset来命名的，如00000.index存储等是 0~368795 的消息，kafka就是利用分段+索引的方法来解决查找效率的问题</p>
<p><img src="/images/posts/kafka_basic/img-7.png" alt="Kafka基础"></p>
<h2 id="Message结构"><a href="#Message结构" class="headerlink" title="Message结构"></a><strong>Message结构</strong></h2><p>log文件就实际存储message地方，我们在producer往kafka写入的也是一条一条的message，那存储log中的message是什么样子？消息主要包含消息体，消息大小，offset，压缩类型等</p>
<ul>
<li>offset： offset是一个占8字节的有序id号，它可以唯一确定每条消息在partition内的位置</li>
<li>消息大小：消息大小占4byte，用于描述消息的大小</li>
<li>消息体：消息体存放的是实际的消息数据（被压缩过），占用的空间根据具体的消息而不一样</li>
</ul>
<h2 id="存储策略"><a href="#存储策略" class="headerlink" title="存储策略"></a><strong>存储策略</strong></h2><ul>
<li>基于时间：默认配置是7天</li>
<li>基于大小：默认配置是1073741824</li>
</ul>
<h2 id="消费消息"><a href="#消费消息" class="headerlink" title="消费消息"></a><strong>消费消息</strong></h2><p>consumer存在两种消费模型：</p>
<ul>
<li>push：优势在于消息实时性高。劣势在于没有考虑consumer消费能力和饱和情况，容易导致producer压跨consumer情况</li>
<li>pull：优势在可以控制消费速度和消费数量，保证consumer不会出现饱和。劣势在于当没有数据，会出现空轮询，消耗cpu</li>
</ul>
<p>kafka是采用pull模式，消费者主动的去kafka集群拉取消息，与producer相同的是，消费者在拉取消息也是找leader去拉取的 多个消费者可以组成一个消费组，每个消费组都有一个组id，同一个消费者组的消费者可以消费同一个topic下不同分区的数据，但是不会组内多个消费者消费同一个分区的数据 图示是消费者组内的消费者小于partition数量的情况，所以会出现某个消费者消费多个partition数据的情况，消费的速度也就不及只处理一个partition的消费者的处理速度！如果是消费者组的消费者多于partition的数量，那会不会出现多个消费者消费同一个partition的数据呢？上面已经提到过不会出现这种情况！多出来的消费者不消费任何partition的数据。所以在实际的应用中，<strong>建议消费者组的consumer的数量与partition的数量一致</strong>！</p>
<p><img src="/images/posts/kafka_basic/img-8.png" alt="Kafka基础"></p>
<h2 id="查找消息"><a href="#查找消息" class="headerlink" title="查找消息"></a><strong>查找消息</strong></h2><p>查找消息的时候，是怎么利用 sagment + offset配合查找的？ 假设我们查找一个 offset为368801的message</p>
<ol>
<li>先找到offset为368801的segment（二分查找）</li>
<li>从segment中的.index文件（也就是368796.index文件，该文件起始偏移量为368796+1，我们要查找的offset为368801的message在该index内的偏移量为368796+5&#x3D;368801，所以这里要查找的相对offset为5）。由于该文件采用的是稀疏索引的方式存储着相对的offset及对应message物理偏移量的关系，所以直接找相对的offset为5的索引（同样二分查询）</li>
<li>根据 2 中的offset确定message存储的物理位置，开始顺序扫描找到对应的message</li>
</ol>
<p>这套机制是建立在offset为有序的基础上，利用 segment + 有序offset + 稀疏索引 + 二分查询 + 顺序查找等多种手段来高效查找数据</p>
<p><img src="/images/posts/kafka_basic/img-9.png" alt="Kafka基础"></p>
<h1 id="数据同步"><a href="#数据同步" class="headerlink" title="数据同步"></a><strong>数据同步</strong></h1><h3 id="ISR-OSR"><a href="#ISR-OSR" class="headerlink" title="ISR&#x2F;OSR"></a>ISR&#x2F;OSR</h3><p>每个分区的follow副本都会从leader同步消息，既然是同步，就一定会有滞后性。</p>
<ul>
<li>AR(Assigned Replicas)：在kafka中，分区中的所有副本（包含leader）统称AR</li>
<li>ISR(On-Sync Replicas)：所有与leader保持一定程度同步的副本（包括leader在内）组成ISR</li>
<li>OSR(Out-of-Sync Replicas)：与Leader副本同步滞后过多的副本（不包括leader副本）组成OSR</li>
</ul>
<p>AR &#x3D; ISR + OSR leader负责维护和跟踪ISR集合中所有follower副本的滞后状态(Leader会维护每个Follower的LEO，Follower来拉取消息时会带上自己的LEO)，当follow副本落后太多或者失效时，leader会把它从ISR集合中剔除，转移到OSR。默认情况下，当leader副本发生故障时，只有在ISR集合中的副本才有资格被选举新的leader</p>
<h3 id="HW-LEO"><a href="#HW-LEO" class="headerlink" title="HW&#x2F;LEO"></a>HW&#x2F;LEO</h3><ul>
<li>HW（High Watermark高水位）：标识了一个特定的消息偏移量（offset），消费者只能拉取到这个offset之前的数据。保证消费数据的一致性和副本数据的一致性。</li>
<li>LEO（Log End Offset）：标识了当前日志文件中的下一条写入消息的offset</li>
</ul>
<p><img src="/images/posts/kafka_basic/img-10.png" alt="Kafka基础"></p>
<p>ISR集合中每个副本都会维护自身的LEO，而ISR集合中最小的LEO即为这个分区的HW，对于消费者而言，只能消费HW之前的消息</p>
<h3 id="ISR"><a href="#ISR" class="headerlink" title="ISR"></a>ISR</h3><p>0.11.x 版本时，kafka 引入 Leader Epoch 机制，大致可以理解为每个leader的版本号，以及自己都是从offset开始写数据，类似[epoch&#x3D;0,offset&#x3D;0]</p>
<h3 id="replica-lag-max-messages"><a href="#replica-lag-max-messages" class="headerlink" title="replica.lag.max.messages"></a>replica.lag.max.messages</h3><p>0.9.x之前版本，kafka broker有一个核心参数， replica.lag.max.messages，默认是4000,表示如果follow落后Leader的消息数量超过了这个参数值，就认为follow是out-of-sync，就会从isr列表中移除 replica.lag.max.messages 这一机制，在瞬间高并发访问的情况下会出现问题：比如Leader瞬间接受到几万条消息，然后所有的follower还没来得及同步过去，此时所有的follower都会被剔除isr，然后同步完了，又再加入isr列表</p>
<h3 id="replica-lag-time-max-ms"><a href="#replica-lag-time-max-ms" class="headerlink" title="replica.lag.time.max.ms"></a>replica.lag.time.max.ms</h3><p>0.9.x开始，引入 replica.lag.max.ms 参数，默认值为 10 秒，表示如果某个 follower的LEO一直落后Leader超时了10秒，那么才判定这个follower是out-of-sync，会从ISR中移除 这样的话，即使出现瞬间的流量洪峰，一下子导致几个Follower都落后了不少数据，但是只要在限定的时间内尽快追上来，别一直落后，就不会认为是_<strong>out-of-sync</strong>_</p>
<h3 id="同步数据慢原因"><a href="#同步数据慢原因" class="headerlink" title="同步数据慢原因"></a>同步数据慢原因</h3><ol>
<li>follower所在的机器性能变差，比如网络负载过高，IO负载过高，CPU负载过高</li>
<li>follower所在机器的kafka broker进程出现卡顿，常见的发生了full gc</li>
<li>动态添加partition的副本，此时新加入的follower会拼命从leader上同步数据，但是这个是需要时间的，所以如果参数配置不当，会导致生产者等待同步完成</li>
</ol>
<h3 id=""><a href="#" class="headerlink" title=""></a></h3><h3 id="follower故障"><a href="#follower故障" class="headerlink" title="follower故障"></a>follower故障</h3><p>follower发生故障后会被临时踢出ISR（动态变化），待该follower恢复后，follower会读取本地磁盘记录的上次HW，并将该log文件高于HW的部分截取掉，从HW开始向leader进行同步，该follower的LEO大于等于该Parititon的HW，即follower追上leader后，可重新加入ISR</p>
<h3 id="leader故障"><a href="#leader故障" class="headerlink" title="leader故障"></a>leader故障</h3><p>leader发生故障后，会从ISR中选出一个新的leader，为了保证多个副本之间的数据一致性，其他的follower会先将各自的log高于hw的部分截掉(新leader不会截取)，然后从新leader同步数据</p>
<p><img src="/images/posts/kafka_basic/img-11.png" alt="Kafka基础"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis缓存常见问题</title>
    <url>//article/redis_common_problem/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-缓存更新"><a href="#📝-缓存更新" class="headerlink" title="📝 缓存更新"></a>📝 <strong>缓存更新</strong></h1><ul>
<li>定时更新，定时将底层数据库内的数据更新到缓存中，该方法比较简单，适合需要缓存的数据，不是很大的应用场景</li>
<li>过期更新：定时将缓存中过期的数据更新为最新数据并更新缓存的过期时间</li>
<li>写请求更新：在用户有写请求时先写数据库同时更新缓存，这使用于用户对缓存数据和数据库的数据有实时强制性要求的情况</li>
<li>读请求更新：在用户有读请求的时候，先判断该请求的数据是否存在或者过期，如果不存在或已过期，则进行底层数据库查询并将查询的时候更新到缓存中，将结果返回给用户</li>
</ul>
<h1 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a><strong>缓存穿透</strong></h1><p>缓存穿透是一些无效的请求，请求到redis，mysql中都没有的数据，这些无效的请求非常多的话，还会占用系统IO，影响整个系统 解决办法：</p>
<ol>
<li>缓存空值：你要判断A在不在数据库里面,最简单的就是把A存到缓存,但是这样存,数据太大了；缓存无效的数据，会被刷爆</li>
<li>布隆过滤器：用布隆过滤器映射A,下次查询只要先经过布隆过滤器是否存在,就可以了.(布隆过滤器用的是bit数组,用的内存很少；bitset + hash)</li>
<li>接口检验：在正常业务流程中可能会存在少量访问不存在 key 的情况，但是一般不会出现大量的情况，所以这种场景最大的可能性是遭受了非法攻击。可以在最外层先做一层校验：用户鉴权、数据合法性校验等，例如商品查询中，商品的ID是正整数，则可以直接对非正整数直接过滤等等。根据业务来校验数据是否合法。</li>
</ol>
<h2 id="布隆过滤器"><a href="#布隆过滤器" class="headerlink" title="布隆过滤器"></a><strong>布隆过滤器</strong></h2><p>布隆过滤器的特点是判断不存在的，则一定不存在；判断存在的，大概率存在，但也有小概率不存在。并且这个概率是可控的，我们可以让这个概率变小或者变高，取决于用户本身的需求。 布隆过滤器由 bitSet 和 一组 Hash 函数组成，是一种空间效率极高的概率型算法和数据结构，主要判断一个元素是否在集合中存在 查询流程：比如查询 “jionghui”</p>
<ol>
<li>首先将“jionghui”跟3组 Hash 函数分别计算，得到 bitSet 的下标为：1、7、10</li>
<li>查看 bitSet 的这3个下标是否都为1，如果这3个下标不都为1，则说明该值必然不存在，如果这3个下标都为1，则只能说明可能存在，并不能说明一定存在</li>
</ol>
<p>比如 jionghui 对应1,7,10; diaosi 对应 4,10,11; 此时查询 java 算出是 1,7,11;那么此时无法判断java 存不存在 其原因是不同的值跟hash函数计算后，可能会得到相同的的下标，所以某个值的标记位，可能也会被其它的值给标记上 这也是为啥布隆过滤器只能判断某个值可能存在，无法判断必然存在的原因。但是反过来，如果该值根据 Hash 函数计算的标记位没有全部都为1，那么则说明必然不存在，这个是肯定的。 降低这种误判率的思路也比较简单：</p>
<ol>
<li>加大bitset长度，这样不同的值出现的冲突的概率就降低了</li>
<li>提升hash函数的个数，hash函数越多，每个值对应的bit越多</li>
</ol>
<p><img src="/images/posts/redis_common_problem/img-1.png" alt="Redis缓存常见问题"></p>
<h1 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a><strong>缓存击穿</strong></h1><p>缓存击穿，一些key在接受请求的时候，突然失效了，会打到mysql上，如果这些key是热点key，可能会把数据库打挂掉 解决方法:</p>
<ol>
<li>加互斥锁，注意此时的锁颗粒度，最小颗粒度优化。在并发的多个请求中，只有第一个请求线程能拿到锁并执行数据库查询操作，其它的线程拿不到锁就阻塞，等到第一个线程将数据写入缓存后，直接走缓存</li>
<li>热点数据不过期；直接将缓存设置为不过期，然后定时任务异步加载数据，更新缓存</li>
</ol>
<h1 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a><strong>缓存雪崩</strong></h1><p>大量的热点key设置了相同的过期时间，导致缓存在同一时刻全部失效，造成瞬间数据库请求量大，压力骤增，引起雪崩，然后后续请求打入db，可能会被打挂掉 缓存雪崩其实有点像“升级版的缓存击穿”，缓存击穿是一个热点 key，缓存雪崩是一组热点 key 解决方法：</p>
<ol>
<li>过期时间打散：既然是大量缓存集中失效，那最容易想到就是让他们不集中生效。可以给缓存的过期时间时加上一个随机值时间，使得每个 key 的过期时间分布开来，不会集中在同一时刻失效</li>
<li>热点数据不过期：该方式和缓存击穿一样，也是要着重考虑刷新的时间间隔和数据异常如何处理的情况</li>
<li>加互斥锁：该方式和缓存击穿一样，按 key 维度加锁，对于同一个 key，只允许一个线程去计算，其他线程原地阻塞等待第一个线程的计算结果，然后直接走缓存即可</li>
</ol>
<h1 id="缓存数据一致性"><a href="#缓存数据一致性" class="headerlink" title="缓存数据一致性"></a><strong>缓存数据一致性</strong></h1><h2 id="查询通用流程"><a href="#查询通用流程" class="headerlink" title="查询通用流程"></a><strong>查询通用流程</strong></h2><p><img src="/images/posts/redis_common_problem/img-2.png" alt="Redis缓存常见问题"></p>
<p>缓存设计模式(Cache Aside Pattern) 的一种次流程会引出两个问题：</p>
<ol>
<li>为什么先操作数据库，可以先操作缓存嘛？</li>
<li>为什么是失效缓存，可以更新缓存嘛？</li>
</ol>
<h2 id="先操作数据库"><a href="#先操作数据库" class="headerlink" title="先操作数据库"></a><strong>先操作数据库</strong></h2><p>场景，有2个并发，一个写请求，一个读请求 脏数据范围：更新数据库后，失效缓存前。这个时间范围很小，通常不会超过几毫秒</p>
<p><img src="/images/posts/redis_common_problem/img-3.png" alt="Redis缓存常见问题"></p>
<h2 id="先操作缓存"><a href="#先操作缓存" class="headerlink" title="先操作缓存"></a><strong>先操作缓存</strong></h2><p>场景，有2个并发，一个写请求，一个读请求 脏数据时间范围：更新数据库后，下一次对该数据的更新前，这个时间范围不确定性很大，情况如下：</p>
<ol>
<li>如果下一次对该数据的更新马上来，那么会失效缓存，脏数据的时间很短</li>
<li>如果下一次对该数据的更新要很久才到来，那这期间缓存保存的一直是脏数据，时间范围很长。</li>
</ol>
<p><img src="/images/posts/redis_common_problem/img-4.png" alt="Redis缓存常见问题"></p>
<p>小总结： 无论是先操作数据库还是先操作缓存都会存在脏数据的情况下。但是相比之下，先操作数据库，再操作缓存是更优的方式，即使在极端情况下，也只会出现很小的脏数据。</p>
<h2 id="更新缓存"><a href="#更新缓存" class="headerlink" title="更新缓存"></a><strong>更新缓存</strong></h2><p>场景：有2个并发的写请求 数据库中的数据是请求B的，缓存中的数据是请求A的，数据库和缓存中数据不一致</p>
<p><img src="/images/posts/redis_common_problem/img-5.png" alt="Redis缓存常见问题"></p>
<h2 id="失效缓存"><a href="#失效缓存" class="headerlink" title="失效缓存"></a>失效缓存</h2><p>场景：有2个并发的写请求 由于是删除缓存，所以不存在不一致的情况</p>
<p><img src="/images/posts/redis_common_problem/img-6.png" alt="Redis缓存常见问题"></p>
<h1 id="如何保证数据一致性"><a href="#如何保证数据一致性" class="headerlink" title="如何保证数据一致性"></a><strong>如何保证数据一致性</strong></h1><p>无论是先操作数据库还是先操作缓存，都会存在脏数据的情况。 由于数据库和缓存是2个不同的数据源，要保证其数据一致性，其实就是典型的分布式事务场景，引入分布式事务解决，常见的有：2PC、TCC、MQ事务消息等 引入分布式事务必须会带来性能上的影响，这与当初我们引入缓存来提升性能的目的是相违背的 在实际使用中，通过不会去保证缓存和数据库的强一致性，而是做出一定的牺牲，保证二者的最终一致性。</p>
<p><img src="/images/posts/redis_common_problem/img-7.png" alt="Redis缓存常见问题"></p>
<h2 id="一致性思路"><a href="#一致性思路" class="headerlink" title="一致性思路"></a><strong>一致性思路</strong></h2><p><img src="/images/posts/redis_common_problem/img-8.png" alt="Redis缓存常见问题"></p>
<p><img src="/images/posts/redis_common_problem/img-9.png" alt="Redis缓存常见问题"></p>
<ol>
<li>延迟双删策略 - 会导致系统的吞吐量下降，所有的操作都延迟一点（坚决不用）</li>
<li>阅binlog日志,发生变更的时候推送到MQ,用MQ去消费删除缓存数据，可以使用阿里的canal将binlog日志采集发送到MQ队列里面，然后通过ACK机制确认处理这条更新消息，<strong>删除缓存(注意是删除缓存,而不是更新</strong>)，保证数据缓存一致性</li>
<li>被动更新缓存,(时效性一般的数据适合 , 因为时效性一般的数据, 错了问题也不大,不花额外资源去修正他 , 让它到期了再重新缓存新数据)</li>
<li><strong>读写锁(CAP中,短暂实现CP,能保证强一致性,不会发生数据一致性错乱问题, 写锁释放之后,继续保持AP,仍有高可用), 完美</strong></li>
<li><strong>每天某个时间点job更新</strong></li>
<li><strong>延迟校验：更新一个消息的时候，发送一条1分钟后延迟的MQ消息，这条MQ消息做的时间是校验现在数据库的值和改的那时相不相同，不同的话就更新缓存</strong></li>
</ol>
<h1 id="大key"><a href="#大key" class="headerlink" title="大key"></a><strong>大key</strong></h1><h2 id="什么是大key"><a href="#什么是大key" class="headerlink" title="什么是大key"></a><strong>什么是大key</strong></h2><ul>
<li>一个STRING类型的Key，它的值为5MB（数据过大）</li>
<li>一个LIST类型的Key，它的列表数量为20000个（列表数量过多）</li>
<li>一个ZSET类型的Key，它的成员数量为10000个（成员数量过多）</li>
<li>一个HASH格式的Key，它的成员数量虽然只有1000个但这些成员的value总大小为100MB（成员体积过大）</li>
</ul>
<p>需要注意的是，在以上的例子中，为了方便理解，我们对大Key的数据、成员、列表数给出了具体的数字。为了避免误导，在实际业务中，大Key的判定仍然需要根据Redis的实际使用场景、业务场景来进行综合判断</p>
<h2 id="大key危害"><a href="#大key危害" class="headerlink" title="大key危害"></a><strong>大key危害</strong></h2><ol>
<li>内存不均：单value较大时，可能会导致节点之间的内存使用不均匀，间接的影响key的部分和负载不均匀</li>
<li>阻塞请求：redis作为单线程，单value较大读写需要较长的时间处理，会阻塞后续的请求处理</li>
<li>阻塞网络：单value较大会占用服务器网卡较多宽带，可能会影响该服务器上的其它redis实例或者应用</li>
</ol>
<h2 id="大key场景"><a href="#大key场景" class="headerlink" title="大key场景"></a><strong>大key场景</strong></h2><ol>
<li>单个简单的key存储的value过大</li>
<li>hash，set，zset，list存储的元素过多</li>
</ol>
<h2 id="大key删除"><a href="#大key删除" class="headerlink" title="大key删除"></a><strong>大key删除</strong></h2><ol>
<li>异步删除，先把key清了</li>
<li>value丢到LRU队列尾部</li>
<li>然后fork线程去异步删除value</li>
</ol>
<p>好处： 如果异步线程挂了，LRU列队下次会清除，由于key已经删了，其他人get不到，不影响使用</p>
<h2 id="大key解决方法"><a href="#大key解决方法" class="headerlink" title="大key解决方法"></a><strong>大key解决方法</strong></h2><p>• 尽可能的拆分key，根据业务来设计或者打散</p>
<h1 id="热Key"><a href="#热Key" class="headerlink" title="热Key"></a>热Key</h1><h2 id="什么是热key"><a href="#什么是热key" class="headerlink" title="什么是热key"></a>什么是热key</h2><p>在某个Key接收到的访问次数、显著高于其它Key时，我们可以将其称之为热Key，常见的热Key如： 某Redis实例的每秒总访问量为10000，而其中一个Key的每秒访问量达到了7000（访问次数显著高于其它Key）</p>
<h2 id="热key的危害"><a href="#热key的危害" class="headerlink" title="热key的危害"></a><strong>热key的危害</strong></h2><ul>
<li>热key占用大量的redis CPU时间是其性能变差并影响其它请求</li>
<li>Redis Cluster中各node流量不均衡造成Redis Cluster的分布式优势无法被Client利用，一个分片负载很高而其它分片十分空闲从而产生读&#x2F;写热点问题</li>
<li>抢购、秒杀活动中，由于商品对应库存Key的请求量过大超出Redis处理能力造成超卖</li>
<li>热Key的请求压力数量超出Redis的承受能力造成缓存击穿，此时大量强求将直接指向后端存储将其打挂并影响到其它业务</li>
</ul>
<h2 id="发现热key"><a href="#发现热key" class="headerlink" title="发现热key"></a><strong>发现热key</strong></h2><ol>
<li>monitor指令：它的缺点同样为不可定制化输出报告，大量的信息会使你在分析结果时复杂度较大，另外，使用该方案的前提条件是将redis-server的maxmemory-policy参数设置为LFU</li>
</ol>
<h2 id="热key处理方式"><a href="#热key处理方式" class="headerlink" title="热key处理方式"></a><strong>热key处理方式</strong></h2><p>• 使用读写分离架构： 多个节点分担读写压力</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis数据结构</title>
    <url>//article/redis_data_structure/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Redis的8大数据结构: String,List,Map,Set,ZSet,bitmaps,hperloglogs,geo<br>Redis是C++写的,C的很多操作不是很高效,比如, C传统的字符串需要遍历整个长度得到一个长度,与Redis的高效是不符合的，所以需要自己设计一些数据结构</p>
</blockquote>
<p><img src="/images/posts/redis_data_structure/img-1.png" alt="Redis数据结构"></p>
<h1 id="📝-动态字符串-SDS"><a href="#📝-动态字符串-SDS" class="headerlink" title="📝 动态字符串(SDS)"></a>📝 动态字符串(SDS)</h1><p><img src="/images/posts/redis_data_structure/img-2.png" alt="Redis数据结构"></p>
<p>c语言的字符串频繁修改一个字符串时，会涉及到内存的重分配，比较消耗性能</p>
<p>Redis中的动态字符串就是利用free和len字段，采用内存预分配和惰性空间释放</p>
<ol>
<li>空间预分配： 减少修改字符串带来的内存重分配的次数，sds采用一次管够的策略，会多分配一些内存；比如上图的capacity就是实际分配的内存，会比字符串的长度要多</li>
<li>惰性空间释放：对字符串进行缩短操作时，程序不立刻使用内存重新分配回收缩短后多余的字节，而是使用free属性将这些字节的数量记录下来，等待后续使用</li>
</ol>
<ul>
<li>Redis的SDS API是安全的，拼接字符串不会造成缓冲区溢出。SDS在拼接字符串之前会检查SDS空间是否满足需求，如果空间不够会自动扩容，所以不会导致缓冲区溢出的问题</li>
<li>SDS获取字符串长度的时间复杂度是O(1).因为C语言的字符串并不记录自身的长度</li>
</ul>
<p>字符串对象底层数据结构实现为简单动态字符串(SDS)和直接内存</p>
<p><img src="/images/posts/redis_data_structure/img-3.png" alt="Redis数据结构"></p>
<h2 id="int编码"><a href="#int编码" class="headerlink" title="int编码"></a>int编码</h2><p>字符串保存的是整数值，并且这个正式可以用long类型来表示，那么其就会直接保存在redisObject的ptr属性里，并将编码设置为int，如图：</p>
<p><img src="/images/posts/redis_data_structure/img-4.png" alt="Redis数据结构"></p>
<h2 id="raw编码"><a href="#raw编码" class="headerlink" title="raw编码"></a>raw编码</h2><p>字符串保存的<strong>大于 44 字节的字符串值</strong>，则使用简单动态字符串（SDS）结构，并将编码设置为raw，此时内存结构与SDS结构一致，内存分配次数为两次，创建redisObject对象和sdshdr结构，如图：</p>
<p><img src="/images/posts/redis_data_structure/img-5.png" alt="Redis数据结构"></p>
<h2 id="embstr编码"><a href="#embstr编码" class="headerlink" title="embstr编码"></a>embstr编码</h2><p>字符串保存的<strong>小于等于 44 字节</strong>的字符串值，使用的也是简单的动态字符串（SDS结构），但是内存结构做了优化，内存分配也只需要一次就可完成，分配一块连续的空间即可，如图：</p>
<p>需要注意的是，redis并未提供任何修改embstr的方式，即embstr是<strong>只读</strong>的形式。对embstr的修改实际上是先转换为raw再进行修改</p>
<p><img src="/images/posts/redis_data_structure/img-6.png" alt="Redis数据结构"></p>
<h2 id="小总结"><a href="#小总结" class="headerlink" title="小总结"></a>小总结</h2><ol>
<li>Redis中,存储 long,double 类型的浮点数是先转换为字符串再进行存储</li>
<li>raw和embStr编码效果是相同的，不同在于内存的释放，raw两次,embSte一次</li>
<li>embStr内存块连续，能更好的利用缓存行的优势(有条件下，尽快控制大小小于39)</li>
<li>int编码和embStr编码如果能做到追加字符串等操作，满足条件下会被转换成raw编码,embstr编码的对象是只读的，一般修改会转码到raw</li>
</ol>
<h1 id="双向链表"><a href="#双向链表" class="headerlink" title="双向链表"></a>双向链表</h1><p>List类型的底层数据结构是由双向链表或压缩表实现的</p>
<p>数据少时用ziplist,数据量多时用linkedlist</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">type</span> struct listNode &#123;</span><br><span class="line">  // 前置节点</span><br><span class="line">   struct listNode *prev;</span><br><span class="line"></span><br><span class="line">  // 后置节点</span><br><span class="line">  struct listNode *next;</span><br><span class="line"></span><br><span class="line"> // 节点值</span><br><span class="line">  void *value</span><br><span class="line">&#125; listNode</span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/redis_data_structure/img-7.png" alt="Redis数据结构"></p>
<p>链表的特性：</p>
<ol>
<li>双端：链表两端有分别 pre 和 next 指针指向上下两个节点，找到该节点的上一个节点和下一个节点的复杂度是O(1)</li>
<li>无环：头部和尾部的节点的pre和next节点都是null,访问到null就是到末端了</li>
<li>表头和表尾:链表的头部和尾部分别有head指针和tail指针,能直接找到</li>
<li>长度计数器:直接记录长度信息</li>
<li>你可以添加一个元素到列表的<strong>头部（左边：LPUSH）<strong>或者</strong>尾部（右边：RPUSH）</strong></li>
<li>在社交网络中建立一个时间线模型，使用<strong>LPUSH</strong>去添加<strong>新的元素</strong>到<strong>用户时间线</strong>中，使用<strong>LRANGE</strong>去检索一些<strong>最近插入的条目</strong></li>
</ol>
<h1 id="压缩表"><a href="#压缩表" class="headerlink" title="压缩表"></a>压缩表</h1><p><strong>列表元素较少的情况下会使用一块连续的内存存储，这个结构是 ziplist，也即是压缩列表。它将所有的元素紧挨着一起存储，分配的是一块连续的内存,查询更快（用偏移量）,但是更新更耗时</strong></p>
<p><img src="/images/posts/redis_data_structure/img-8.png" alt="Redis数据结构"></p>
<ol>
<li>zlbytes: 整个ziplist占字节数</li>
<li>zltail:  尾节点相对于首地址偏移量</li>
<li>zllen:  节点数</li>
<li>entry： 保存了前一个节点长度 + 编码 + 内容</li>
<li>zlend: 代表结束</li>
</ol>
<details><summary>**压缩链表和双向链表总结**</summary>

</details>

<h1 id="哈希表"><a href="#哈希表" class="headerlink" title="哈希表"></a>哈希表</h1><p><strong>Redis 的字典相当于 Java 语言里面的 HashMap，它是无序字典。内部实现结构上同Java 的 HashMap 也是一致的，同样的数组 + 链表二维结构。第一维 hash 的数组位置碰撞时，就会将碰撞的元素使用链表串接起来</strong></p>
<h2 id="hash-ziplist"><a href="#hash-ziplist" class="headerlink" title="hash-ziplist"></a><strong>hash-ziplist</strong></h2><p><img src="/images/posts/redis_data_structure/img-9.png" alt="Redis数据结构"></p>
<h2 id="hash-hashtable"><a href="#hash-hashtable" class="headerlink" title="hash-hashtable"></a><strong>hash-hashtable</strong></h2><p><img src="/images/posts/redis_data_structure/img-10.png" alt="Redis数据结构"></p>
<h1 id="跳表"><a href="#跳表" class="headerlink" title="跳表"></a>跳表</h1><p>链表 + 二分法</p>
<p><img src="/images/posts/redis_data_structure/img-11.png" alt="Redis数据结构"></p>
<ol>
<li>一个跳表有几个层(level)组成</li>
<li>跳表的第一层包含所有的元素</li>
<li>每一层都是一个有序的链表</li>
<li>如果元素x出现在第i层,则所有比i小的层都包含x</li>
<li>第i层的元素都通过一个down指针指向下一层拥有相同值的元素</li>
<li>在每一层，-1和1两个元素都出现(分别表示INT_MIN和INT_MAX)</li>
<li>Top指针指向最高层的第一个元素</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis主从,哨兵，集群</title>
    <url>//article/redis_ha1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Redis的主从,哨兵,集群</p>
</blockquote>
<h1 id="📝-主从复制"><a href="#📝-主从复制" class="headerlink" title="📝 主从复制"></a>📝 主从复制</h1><p>主从复制是Redis分布式基石，也是Redis高可用的保障</p>
<p>被复制的服务器称为主服务器（Master）</p>
<p>对主服务器进行复制的服务器称为从服务器(Slave)</p>
<p>1个master节点，多个slave节点</p>
<p>master 节点负责写(可读可写)</p>
<p>slave  节点负责读(只读)，分担压力，slave接受master节点的同步信息一直保持同步</p>
<p><img src="/images/posts/redis_ha1/img-1.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="配置redis-config"><a href="#配置redis-config" class="headerlink" title="配置redis.config"></a>配置redis.config</h2><p>多个redis.config配置, 配置不同的端口启动, 从节点 slave 主节点. 需要手动配置</p>
<p>3种方式</p>
<p>replicaof（Redis 5.0 之前使用 slaveof）</p>
<ul>
<li>在 slave 直接执行命令：replicaof <masterip> <masterport></li>
<li>在 slave 配置文件中加入：replicaof <masterip> <masterport></li>
<li>使用启动命令：–replicaof <masterip> <masterport></li>
</ul>
<p>主节点不需要修改配置,从节点需要配置 replica of master ip poid</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><ul>
<li>2.8版本以前,从服务器对主服务器的同步需要从服务器向主服务器发送sync命令来完成，主从服务器需要占用的资源：每次都执行一次RDB持久化</li>
</ul>
<details><summary>缺点记录</summary>

</details>

<p><img src="/images/posts/redis_ha1/img-2.png" alt="Redis主从,哨兵，集群"></p>
<ul>
<li>2.8版本以后,减少全量同步(full resynchronizaztion)的发生，尽可能使用增量同(partial resynchronization). 使用psync命令代替了sync命令来执行同步操作;psync命令同时具备全量同步和增量同步的功能</li>
</ul>
<details><summary>psync命令</summary>

</details>

<p><img src="/images/posts/redis_ha1/img-3.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="主从复制过程"><a href="#主从复制过程" class="headerlink" title="主从复制过程"></a>主从复制过程</h2><ol>
<li>主从之间建立链接，由于从节点皮质了replica of主ip，此时是知道主节点ip地址的</li>
<li>从节点发送一个psync ？ -1 的命令给主节点，主节点接受到后执行bgsave，发送给从节点，psync包括二个参数： 主服务器的runID和复制进度offset;第一次同步时，其值为-1,主节点响应的fullersync代表全量复制</li>
<li>从节点接收到rdb文件，清空自己的数据，执行全量同步，后续有新的数据，通过replication Buffer进行增量同步</li>
<li>加入主从之间的差距还是过大，还是会进行全量同步，buffer超过了大小限制，也会触发一次全量同步</li>
</ol>
<p><img src="/images/posts/redis_ha1/img-4.png" alt="Redis主从,哨兵，集群"></p>
<details><summary>?如果是主服务切换时，是否也能进行增量同步</summary>

</details>

<details><summary>?主从库间网络断了怎么办</summary>

</details>

<details><summary>？为什么主从库的复制不使用AOF</summary>

</details>

<details><summary>如果主从之间，从节点过多，可能导致主节点传输RDB文件效率问题，可以采纳树结构</summary>

</details>

<h1 id="哨兵模式"><a href="#哨兵模式" class="headerlink" title="哨兵模式"></a>哨兵模式</h1><p>主从模式都是手动处理的，假设出了问题大部分不能第一时间解决主从故障等问题，哨兵模式可以很好的解决自动化监听并且处理主从故障的问题(保证服务的高可用);推选出新的master，并且将旧的master退级为slave</p>
<h2 id="监控"><a href="#监控" class="headerlink" title="监控"></a>监控</h2><p>哨兵会每隔1秒给所有的主从节点发送PING命令，当主从节点受到PING命令后，会发送一个响应命令给哨兵，这样就可以判断它们是否正常运行</p>
<p><img src="/images/posts/redis_ha1/img-5.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="发现故障"><a href="#发现故障" class="headerlink" title="发现故障"></a>发现故障</h2><ul>
<li>如果主节点或者从节点没有在规定的时间内响应哨兵，哨兵就会将它们标记为 [主观下线]</li>
<li>并且告知其他哨兵，ping该节点，超过半数确认是否 [客观下线]</li>
</ul>
<p><img src="/images/posts/redis_ha1/img-6.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="故障转移"><a href="#故障转移" class="headerlink" title="故障转移"></a>故障转移</h2><h3 id="1-选出leader哨兵"><a href="#1-选出leader哨兵" class="headerlink" title="1.选出leader哨兵"></a>1.选出leader哨兵</h3><p>为了更加“客观”的判断主节点故障了，一般不会只由单个哨兵的检测结果来判断，而是多个哨兵一起判断，这样可以减少误判概率，所以<strong>哨兵是以哨兵集群的方式存在的</strong></p>
<p>需要在哨兵集群中选出一个leader，让leader来执行主从切换;选举leader的过程其实是一个投票的过程，在投票开始前，肯定得有个  [候选者]</p>
<p>举个栗子：假设有三个哨兵;当哨兵B先判断到主节点[主管下线后],就会给其他实例发送is-master-down-by-addr命令;接着，其他哨兵会根据自己和主节点的网络连接情况,做出赞成投票或者拒绝投票的响应</p>
<p>当哨兵 B 收到赞成票数达到哨兵配置文件中的 quorum 配置项设定的值后，就会将主节点标记为「客观下线」，此时的哨兵 B 就是一个Leader 候选者</p>
<p>候选者会向其他哨兵发送命令，表明希望成为Leader来执行主从切换，让其他哨兵对它投票</p>
<p>每个哨兵只有一次投票机会，如果用完了就不能参与投票，可以投给自己或投给别人，但是只有候选者才能把票投给自己</p>
<p>那么在投票的过程中，任何一个 候选者 ， 要满足两个条件：</p>
<ul>
<li>第一，拿到半数以上的赞成票</li>
<li>第二，拿到的票数同时还需要大于等于哨兵配置文件中的quorum值</li>
</ul>
<p><img src="/images/posts/redis_ha1/img-7.png" alt="Redis主从,哨兵，集群"></p>
<details><summary>哨兵节点至少要有3个</summary>

</details>

<h3 id="2-选举新节点"><a href="#2-选举新节点" class="headerlink" title="2.选举新节点"></a>2.选举新节点</h3><details><summary>**第一步，从已下线的主节点属下的所有从节点中，挑选出一个状态良好，数据完整的从节点，然后向这个从节点发送 SLAVE no one 命令 ，将这个从节点转换为主节点**</summary>

</details>

<h3 id="3-从节点指向新主节点"><a href="#3-从节点指向新主节点" class="headerlink" title="3.从节点指向新主节点"></a>3.从节点指向新主节点</h3><p>当新主节点出现之后，哨兵 leader 下一步要做的就是，让已下线主节点属下的所有「从节点」指向「新主节点」，这一动作可以通过向「从节点」发送 <code>SLAVEOF</code> 命令来实现</p>
<p><img src="/images/posts/redis_ha1/img-8.png" alt="Redis主从,哨兵，集群"></p>
<p>所有从节点指向新主节点后的拓扑图如下：</p>
<p><img src="/images/posts/redis_ha1/img-9.png" alt="Redis主从,哨兵，集群"></p>
<h3 id="4-通知客户端的主节点已更换"><a href="#4-通知客户端的主节点已更换" class="headerlink" title="4.通知客户端的主节点已更换"></a>4.通知客户端的主节点已更换</h3><p>经过前面三步，哨兵集群终于完成主从节点切换的工作，那么新节点的信息如何通知给客户端呢？</p>
<p>这主要<strong>通过 Redis 的发布者&#x2F;订阅者机制来实现</strong>的。每个哨兵节点提供发布者&#x2F;订阅者机制，客户端可以从哨兵订阅消息</p>
<p><img src="/images/posts/redis_ha1/img-10.png" alt="Redis主从,哨兵，集群"></p>
<p>客户端和哨兵建立连接后，客户端会订阅哨兵提供的频道</p>
<p>主从切换完成后,哨兵就会向 +switch-master 频道发布新主节点的ip地址和端口信息，这个时候客户端就可以接收到这条信息，然后用这里面的新主节点的ip地址和端口进行通信</p>
<h3 id="5-将旧主节点变为从节点"><a href="#5-将旧主节点变为从节点" class="headerlink" title="5.将旧主节点变为从节点"></a>5.将旧主节点变为从节点</h3><p>故障转移操作最后要做的是，继续监视旧主节点，当旧主节点重新上线时，哨兵集群就会向它发送 <code>SLAVEOF</code> 命令，让它成为新主节点的从节点，如下图</p>
<p><img src="/images/posts/redis_ha1/img-11.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="哨兵集群如何工作"><a href="#哨兵集群如何工作" class="headerlink" title="哨兵集群如何工作"></a>哨兵集群如何工作</h2><p>前面提到了 Redis 的发布者&#x2F;订阅者机制，那就不得不提一下哨兵集群的组成方式，因为它也用到了这个技术</p>
<p>在我第一次搭建哨兵集群的时候，当时觉得很诧异。因为在配置哨兵的信息时，竟然只需要填下面这几个参数，设置主节点名字、主节点的 IP 地址和端口号以及 quorum 值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sentinel monitor &lt;master-name&gt; &lt;ip&gt; &lt;redis-port&gt; &lt;quorum&gt;</span><br></pre></td></tr></table></figure>

<p>不需要填其他哨兵节点信息，是如何感知对方的，又是如何组成哨兵集群的？</p>
<p>哨兵节点之间通过redis的发布者&#x2F;订阅者机制来互相发现的</p>
<p>在主从集群中，主节点上有一个名为<code>__sentinel__:hello</code>的频道，不同哨兵就是通过它来相互发现，实现互相通信的</p>
<p>在下图中，哨兵 A 把自己的 IP 地址和端口的信息发布到<code>__sentinel__:hello</code> 频道上，哨兵 B 和 C 订阅了该频道。那么此时，哨兵 B 和 C 就可以从这个频道直接获取哨兵 A 的 IP 地址和端口号。然后，哨兵 B、C 可以和哨兵 A 建立网络连接</p>
<p><img src="/images/posts/redis_ha1/img-12.png" alt="Redis主从,哨兵，集群"></p>
<p>通过这个方式，哨兵 B 和 C 也可以建立网络连接，这样一来，哨兵集群就形成了</p>
<h2 id="脑裂问题"><a href="#脑裂问题" class="headerlink" title="脑裂问题"></a>脑裂问题</h2><p>旧的master出了问题，有一些还没同步到redis上，从redis通过选举成为一个新的master，有新的master起来后，由于一些问题，哨兵通知不到旧的master，导致2个master，就出现了脑裂</p>
<p>本质是2个master，新master是可以接受写入数据的， 但是有一些数据写到旧的mater上了, 但是从redis 只会同步新的mater, 当旧mater的恢复正常后, 哨兵会把旧的mater降级为从, 降级是要清除数据的,清了数据 之前写在旧mater上的数据还没同步的也就没了</p>
<p>问题出在,当从升级为master的时候，有2个master，并且2个都在接受写入数据;旧的 新的master都在写入数据，但是从的只同步了新的master，写进旧的master的数据没同步，并且随着自己的降级也没了，丢失了</p>
<details><summary>?脑裂如何处理</summary>

</details>

<p>CAP理论认为，一个分布式系统不可能同时满足一致性（Consistency）、可用性（Availability）和分区容错性（Partition tolerance）这三个基本需求，最多只能同时满足其中两项。</p>
<p>在Redis哨兵模式下出现脑裂问题时，主从节点之间的分区容错性得以保证，但同时需要在一致性和可用性之间做出取舍。</p>
<p>首先，由于数据复制和同步的延迟可能会导致主从节点之间的数据不一致，因此在Redis的哨兵模式中，我们需要配置 <code>min-slaves-to-write</code> 和 <code>min-slaves-max-lag</code> 这两个参数，以保证数据的一致性。</p>
<p>其次，在节点出现脑裂时，如果旧的主节点和新的主节点都在接受写入数据，那么可能会出现数据写入不一致的情况。为了解决这个问题，我们可以在哨兵模式中配置 <code>min-replicas-to-write</code> 和 <code>min-replicas-max-lag</code> 这两个参数，以保证数据的一致性和可用性。</p>
<p>但是需要注意的是，以上的配置都只能减少发生脑裂的可能性，而不能完全避免。因此，对于Redis的哨兵模式来说，我们需要在一致性和可用性之间做出取舍，根据具体的业务需求来设置合理的参数。</p>
<ul>
<li>redis集群是可以让脑裂存在的,其实是一种高可用的特性，但不保证数据一致</li>
<li>redis通过设置两个参数，一定程度上其实是降低可用性，以提供数据一致性</li>
<li>为什么愿意降低可用性？因为那部分的数据会因为主从切换而丢失，所以宁愿不可用</li>
</ul>
<h1 id="Cluster集群"><a href="#Cluster集群" class="headerlink" title="Cluster集群"></a>Cluster集群</h1><p>哨兵模式最大的缺点就是所有的数据都放在同一台服务器上，无法较好的进行水平扩展</p>
<p>为了解决哨兵模式存在的问题，集群模式应运而生。在高可用上，集群基本是直接复用的哨兵模式的逻辑，并且针对水平扩展进行了优化</p>
<p>Redis Cluster集群模式具备的特点如下:</p>
<ol>
<li>采取去中心化的集群模式，将数据按照槽存储分布在多个redis节点上。集群共有16382槽，每个节点负责处理部分槽</li>
<li>使用CRC16算法来计算key所属的槽，CRC16（key,key len） &amp;16383</li>
<li>所有Redis节点批次互联，通过PING-PONG机制来节点间的心跳检测</li>
<li>分片内采用一主多从保证高可用，并提供复制和故障恢复功能。在实际中，通过会将主从分布在不同的机房，避免机房出现故障导致整个分片出问题</li>
<li>客户端与Redis节点直连，不需要中间代理层(Proxy);客户端不需要连接集群所有节点，连接集群中任何一个可用的节点即可</li>
</ol>
<p><img src="/images/posts/redis_ha1/img-13.png" alt="Redis主从,哨兵，集群"></p>
<p>目前redis的分区规则是： hash分区，分成了 16384 个哈希槽</p>
<p>RedisCluster采用了哈希分区的“虚拟槽分区”方式（哈希分区分节点取余，一致性哈希分区和虚拟槽分区）</p>
<p><img src="/images/posts/redis_ha1/img-14.png" alt="Redis主从,哨兵，集群"></p>
<details><summary>集群之后的扩容</summary>

</details>

<h2 id="故障发现"><a href="#故障发现" class="headerlink" title="故障发现"></a>故障发现</h2><ol>
<li>Redis Cluster通过ping&#x2F;pong消息实现故障发现：<strong>不需要sentinel</strong></li>
<li>ping&#x2F;pong不仅能传递节点与槽的对应消息，也能传递其他状态，比如：节点主从状态，节点故障等</li>
<li>故障发现就是通过这种模式来实现，分为主观下线和客观下线</li>
</ol>
<h3 id="主观下线"><a href="#主观下线" class="headerlink" title="主观下线"></a>主观下线</h3><ul>
<li>某个节点认为宁外一个节点不可用，‘偏见’，只代表一个节点对另外一个节点的判断，不是所有节点的认知</li>
<li>如果节点1发现与节点2最后通信时间超过node-timeout，则把节点2标识为pfail状态</li>
</ul>
<p><img src="/images/posts/redis_ha1/img-15.png" alt="Redis主从,哨兵，集群"></p>
<h3 id="客观下线"><a href="#客观下线" class="headerlink" title="客观下线"></a>客观下线</h3><ol>
<li>当半数以上持有槽的主节点都标记某节点主观下线时，可以保证判断的公平性</li>
<li>集群模式下，只有主节点(master)才有读写权限和集群槽的维护权限，从节点(slave)只有复制的权限</li>
<li>客观下线流程:</li>
</ol>
<p>1.某个节点接收到其他节点发送的ping消息，如果接收到的ping消息中包含了其他pfail节点，这个节点会将主观下线的消息内容添加到自身的故障列表中，故障列表中包含了当前节点接收到的每一个节点对其他节点的状态信息</p>
<p>2.当前节点把主观下线的消息内容添加到自身的故障列表之后，会尝试对故障节点进行客观下线操作</p>
<p><img src="/images/posts/redis_ha1/img-16.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="准备选举时间"><a href="#准备选举时间" class="headerlink" title="准备选举时间"></a>准备选举时间</h2><p><strong>使偏移量最大的从节点具备优先级成为主节点的条件</strong></p>
<p><img src="/images/posts/redis_ha1/img-17.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="替换主节点"><a href="#替换主节点" class="headerlink" title="替换主节点"></a>替换主节点</h2><p>当前从节点取消复制变成离节点(slave of no one)<br>执行cluster del slot撤销故障主节点负责的槽，并执行cluster add slot把这些槽分配给自己<br>向集群广播自己的pong消息，表明已经替换了故障从节点</p>
<h2 id="集群选举流程"><a href="#集群选举流程" class="headerlink" title="集群选举流程"></a>集群选举流程</h2><p>故障转移的第一步就是选举出新的主节点，以下是集群选举新的主节点的方法：</p>
<p>1）当从节点发现自己正在复制的主节点进入已下线状态时，会发起一次选举：将 currentEpoch（配置纪元）加1，然后向集群广播一条 CLUSTERMSG_TYPE_FAILOVER_AUTH_REQUEST 消息，要求所有收到这条消息、并且具有投票权的主节点向这个从节点投票。</p>
<p>2）其他节点收到消息后，会判断是否要给发送消息的节点投票，判断流程如下：</p>
<ol>
<li>当前节点是 slave，或者当前节点是 master，但是不负责处理槽，则当前节点没有投票权，直接返回。</li>
<li>请求节点的 currentEpoch 小于当前节点的 currentEpoch，校验失败返回。因为发送者的状态与当前集群状态不一致，可能是长时间下线的节点刚刚上线，这种情况下，直接返回即可。</li>
<li>当前节点在该 currentEpoch 已经投过票，校验失败返回。</li>
<li>请求节点是 master，校验失败返回。</li>
<li>请求节点的 master 为空，校验失败返回。</li>
<li>请求节点的 master 没有故障，并且不是手动故障转移，校验失败返回。因为手动故障转移是可以在 master 正常的情况下直接发起的。</li>
<li>上一次为该master的投票时间，在cluster_node_timeout的2倍范围内，校验失败返回。这个用于使获胜从节点有时间将其成为新主节点的消息通知给其他从节点，从而避免另一个从节点发起新一轮选举又进行一次没必要的故障转移</li>
<li>请求节点宣称要负责的槽位，是否比之前负责这些槽位的节点，具有相等或更大的 configEpoch，如果不是，校验失败返回。</li>
</ol>
<p>如果通过以上所有校验，那么主节点将向要求投票的从节点返回一条 CLUSTERMSG_TYPE_FAILOVER_AUTH_ACK 消息，表示这个主节点支持从节点成为新的主节点。</p>
<p>3）每个参与选举的从节点都会接收 CLUSTERMSG_TYPE_FAILOVER_AUTH_ACK 消息，并根据自己收到了多少条这种消息来统计自己获得了多少个主节点的支持。</p>
<p>4）如果集群里有N个具有投票权的主节点，那么当一个从节点收集到大于等于N&#x2F;2+1 张支持票时，这个从节点就会当选为新的主节点。因为在每一个配置纪元里面，每个具有投票权的主节点只能投一次票，所以如果有 N个主节点进行投票，那么具有大于等于 N&#x2F;2+1 张支持票的从节点只会有一个，这确保了新的主节点只会有一个。</p>
<p>5）如果在一个配置纪元里面没有从节点能收集到足够多的支持票，那么集群进入一个新的配置纪元，并再次进行选举，直到选出新的主节点为止。</p>
<p>这个选举新主节点的方法和选举领头 Sentinel 的方法非常相似，因为两者都是基于 Raft 算法的领头选举（leader election）方法来实现的</p>
<h2 id="ASK错误"><a href="#ASK错误" class="headerlink" title="ASK错误"></a>ASK错误</h2><p>当redis<strong>正在</strong>对集群进行扩容和缩容时，需要对槽及槽中数据进行迁移</p>
<p>当客户端向某个节点发送命令，节点向客户端返回moved异常，告诉客户端数据对应的槽的节点信息</p>
<p>如果此时正在进行集群扩展或者缩空操作，当客户端向正确的节点发送命令时，槽及槽中数据已经被迁移到别的节点了，就会返回ask，这就是ask重定向机制</p>
<p><img src="/images/posts/redis_ha1/img-18.png" alt="Redis主从,哨兵，集群"></p>
<h2 id="moved重定向"><a href="#moved重定向" class="headerlink" title="moved重定向"></a>moved重定向</h2><p>是路由层面的，直接修改迁移之后的槽映射关系，不用像ask错误一样，返回错误再重新路由</p>
<p><img src="/images/posts/redis_ha1/img-19.png" alt="Redis主从,哨兵，集群"></p>
<details><summary>**moved异常与ask异常的相同点和不同点**</summary>

</details>

<h2 id="集群不可用时机"><a href="#集群不可用时机" class="headerlink" title="集群不可用时机"></a>集群不可用时机</h2><ul>
<li>挂掉的master没有对应的slave，即有任一slot不可用，集群都不可用</li>
<li>超过半数master都挂掉了，不管有没有slave，都将不可用</li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://juejin.cn/post/7017017990667304973">https://juejin.cn/post/7017017990667304973</a></li>
<li><a href="https://www.xiaolincoding.com/redis/cluster/sentinel.html">https://www.xiaolincoding.com/redis/cluster/sentinel.html</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis的过期策略和内存淘汰</title>
    <url>//article/redis_thy_delete/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Redis的过期删除和内存淘汰策略</p>
</blockquote>
<h1 id="📝-过期删除策略"><a href="#📝-过期删除策略" class="headerlink" title="📝 过期删除策略"></a>📝 过期删除策略</h1><details><summary>Redis的过期删除策略有3种</summary>

</details>

<h2 id="惰性删除"><a href="#惰性删除" class="headerlink" title="惰性删除"></a>惰性删除</h2><p>是指⼀个 key 过期后，并不会直接删除，⽽是等到该 key 被再次访问时，才执⾏删除</p>
<p>惰性删除对内存不友好，但是对cpu很友好。只有到key被访问了，我们才会去先判断key是否过期，过期就直接删除key，并返回nil。但是如果有key长时间没有被访问到，内存就会一直被占用</p>
<p>Redis在访问或者修改Key之前，都会调用expireIfNeeded函数对其进行检查，检查Key是否过期：</p>
<ul>
<li>如果过期，则删除该 key，至于选择异步删除，还是选择同步删除，根据 <code>lazyfree_lazy_expire</code> 参数配置决定（Redis 4.0版本开始提供参数），然后返回 null 客户端</li>
<li>如果没有过期，不做任何处理，然后返回正常的键值对给客户端</li>
</ul>
<p>redis的惰性删除策略由db.c文件中的 <code>expireIfNeeded</code> 实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">expireIfNeeded</span><span class="params">(redisDb *db, robj *key)</span> &#123;</span><br><span class="line"><span class="comment">//判断 key 是否过期</span></span><br><span class="line"><span class="keyword">if</span> (!keyIsExpired(db,key)) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">....<span class="comment">/* 删除过期键 */</span></span><br><span class="line">....<span class="comment">// 如果 server.lazyfree_lazy_expire 为 1 表示异步删除，反之同步删除；</span></span><br><span class="line"><span class="keyword">return</span> server.lazyfree_lazy_expire ? dbAsyncDelete(db,key) : dbSyncDelete(db,key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="定期删除"><a href="#定期删除" class="headerlink" title="定期删除"></a>定期删除</h2><p><strong>多少秒</strong>去遍历下内存里的key. 如果过期了删除 (<strong>核心在于不要去影响性能， 时间不宜过长， 次数不宜过多</strong>)</p>
<p>定期删除，对CPU和内存都是折中的选择</p>
<p>Redis实现定期删除： 每轮抽查时，会随机选择20个key判断是否过期</p>
<p>Redis定期删除流程：</p>
<ol>
<li>定期删除时会遍历每个database（16个），检查当前库中指定个数key</li>
<li>检查每个库中这20个key是否过期，并删除已过期的key</li>
<li>如果本轮检查的已过期 key 的数量，超过 5 个（20&#x2F;4），也就是「已过期 key 的数量」占比「随机抽取 key 的数量」大于 25%，则继续重复步骤 1；如果已过期的 key 比例小于 25%，则停止继续删除过期 key，然后等待下一轮再检查</li>
<li>程序中有一个全局变量记录到秒到了哪个数据库</li>
</ol>
<p>定期删除是一个循环的流程，Redis为了保证定期删除不会出现循环过度，导致线程卡死现象，为此增加了定期删除流程的时间上限，默认不会超过25ms</p>
<h2 id="定时删除"><a href="#定时删除" class="headerlink" title="定时删除"></a>定时删除</h2><p><strong>绑定一个定时器</strong>，这个定时器会在时间到的时候，把这个Key从内存中删了</p>
<p>定时删除是对内存最友好的，但是对 cpu不友好。键只要一过期，我们就立马去删除key，释放内存空间。但是这意味着cpu需要不断的去遍历所有的key，来判断对方有没有过期</p>
<h2 id="Redis选择"><a href="#Redis选择" class="headerlink" title="Redis选择"></a>Redis选择</h2><p><strong>综合下来，redis最终采用的是惰性删除+定期删除的方式搭配使用，从库不会主动执行定期删除等策略</strong></p>
<h1 id="内存淘汰策略"><a href="#内存淘汰策略" class="headerlink" title="内存淘汰策略"></a>内存淘汰策略</h1><p>进行数据淘汰」这一类策略</p>
<p>又可以细分为「在设置了过期时间的数据中进行淘汰」和「在所有数据范围内进行淘汰」这两类策略</p>
<ul>
<li><strong>volatile</strong>lru：从已设置过期时间的key中，移出最近最少使用的key进行淘汰</li>
<li><strong>volatile</strong>ttl：从已设置过期时间的key中， 移出将要过期的key</li>
<li><strong>volatile</strong>random：从已设置过期时间的key中随机选择key淘汰</li>
<li><strong>allkeys</strong>lru：从key中选择最近最少使用的进行淘汰</li>
<li><strong>allkeys</strong>random：从key中随机选择key进行淘汰</li>
<li><strong>noeviction (默认)</strong>：当内存达到阈值的时候，新写入操作报错, 触发 OOM</li>
</ul>
<h1 id="如何判定Key已过期"><a href="#如何判定Key已过期" class="headerlink" title="如何判定Key已过期"></a>如何判定Key已过期</h1><p>每当我们对一个 key 设置了过期时间时，Redis 会把该 key 带上过期时间存储到一个过期字典（expires dict）中，也就是说「过期字典」保存了数据库中所有 key 的过期时间</p>
<p>RedisDB结构：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">typedef struct redisDb &#123;</span><br><span class="line">    dict dict;    / 数据库键空间，存放着所有的键值对 */</span><br><span class="line">    dict *expires; <span class="comment">/* 键的过期时间 */</span></span><br><span class="line">    ....</span><br><span class="line">&#125; redisDb;</span><br></pre></td></tr></table></figure>

<p>过期字典数据结构结构如下：</p>
<ul>
<li>过期字典的 key 是一个指针，指向某个键对象；</li>
<li>过期字典的 value 是一个 long long 类型的整数，这个整数保存了 key 的过期时间；</li>
</ul>
<p><img src="/images/posts/redis_thy_delete/img-1.png" alt="Redis的过期策略和内存淘汰"></p>
<p>字典表实际上是hash表，hash的最大好处就是让我们可以用 O(1) 的时间复杂度快速查找。当我们查询一个key时，redis首先检查该key是否存在于过期字典中</p>
<ul>
<li>如果不存在，则正常读取键值</li>
<li>如果存在，则会获取该key的过期时间，然后与当前系统时间进行对比;如果比系统时间大，那就没有过期，否则判定该key已过期，在根据过期删除策略执行对应的操作</li>
</ul>
<p><img src="/images/posts/redis_thy_delete/img-2.png" alt="Redis的过期策略和内存淘汰"></p>
<h1 id="LRU淘汰算法"><a href="#LRU淘汰算法" class="headerlink" title="LRU淘汰算法"></a>LRU淘汰算法</h1><p>LRU 全称是 Least Recently Used 翻译为最近最少使用，会选择淘汰最近最少使用的数据</p>
<p>~~ 传统的LRU算法的实现都是基于 链表 结构，链表中的元素按照操作顺序从前往后排列，最新操作的键会被移动到表头，当需要淘汰的时候，只需要删除链表尾部的元素即可，因为链表尾部的元素就代表最久未被使用的元素 ~~</p>
<p>但是Redis并没有采用这种方法实现 LRU 算法，传统的LRU存在两个问题：</p>
<ul>
<li>需要用链表管理所有的缓存数据，这会带来额外的开销</li>
<li>当有数据被访问时，需要在链表上把该数据移动到头端，如果大量数据被访问，就会带来很多链表移动操作，会很耗时，进而降低Redis缓存性能</li>
</ul>
<p>Redis是如何实现LRU的呢？</p>
<p>Redis实现一种近似LRU算法，目的是为了更好的节约内存，它的实现方式是Redis对象结构体中添加一个额外的字段，用于记录此数据的最后一次访问时间</p>
<p>当redis进行内存淘汰的时候，会在字典表中使用随即采样的方法来淘汰，它是随机5个值（可配置），然后淘汰最久没有使用的那个</p>
<p>Redis实现LRU的优点：</p>
<ul>
<li>不用为所有的数据维护一个大链表，节省了内存空间</li>
<li>不用在每次数据访问时都移动链表项，提升了缓存的性能</li>
</ul>
<details><summary>问题（缓存污染）：</summary>

</details>

<h1 id="LFU"><a href="#LFU" class="headerlink" title="LFU"></a>LFU</h1><p>LFU 全称是 Least Frequently Used 翻译为最近最不常用，LFU 算法是根据数据访问次数来淘汰数据的</p>
<p>如果数据过去被访问多次，那么将来被访问的频率也更高</p>
<p>所以， LFU 算法会记录每个数据的访问次数。当一个数据被再次访问时，就会增加该数据的访问次数。这样就解决了偶尔被访问一次之后，数据留存在缓存中很长一段时间的问题，相比于 LRU 算法也更合理一些</p>
<p>LFU算法相比LRU，多记录了 数据访问的频次 ， Redis对象结构如下：</p>
<p>Redis对象头的24 bits 的lru字段被分成两段来存储，高 16 bits 存储ldt(Last Decrement Time),低8bit存储logc(Logistic Counter)</p>
<ul>
<li>ldt: 用来记录key的访问时间戳</li>
<li>logc: 记录key的访问频次，它的值越小表示使用的频率越低，越容易淘汰，每个新加入的key的logc初始值为5</li>
</ul>
<p>注意，logc 并不是单纯的访问次数，而是访问频次（访问频率），因为 logc 会随时间推移而衰减的</p>
<p>在每次 key 被访问时，会先对 logc 做一个衰减操作，衰减的值跟前后访问时间的差距有关系，如果上一次访问的时间与这一次访问的时间差距很大，那么衰减的值就越大，这样实现的 LFU 算法是根据访问频率来淘汰数据的，而不只是访问次数。访问频率需要考虑 key 的访问是多长时间段内发生的。key 的先前访问距离当前时间越长，那么这个 key 的访问频率相应地也就会降低，这样被淘汰的概率也会更大</p>
<p>对logc做完衰减操作后，就开始对logc进行增加操作，增加操作并不是单纯的+1，而是根据概率增加，如果logc越大的key，它的logc就越难再加</p>
<ol>
<li>先按照上次访问距离当前的时长，来对 logc 进行衰减；</li>
<li>然后，再按照一定概率增加 logc 的值</li>
</ol>
<p>redis.conf 提供了两个配置项，用于调整 LFU 算法从而控制 logc 的增长和衰减：</p>
<ul>
<li><code>lfu-decay-time</code> 用于调整 logc 的衰减速度，它是一个以分钟为单位的数值，默认值为1，lfu-decay-time 值越大，衰减越慢；</li>
<li><code>lfu-log-factor</code> 用于调整 logc 的增长速度，lfu-log-factor 值越大，logc 增长越慢</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">typedef struct redisObject &#123;</span><br><span class="line">...</span><br><span class="line"><span class="comment">// 24 bits，用于记录对象的访问信息</span></span><br><span class="line">unsigned lru:<span class="number">24</span>;  </span><br><span class="line">    ...</span><br><span class="line">&#125; robj;</span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/redis_thy_delete/img-3.png" alt="Redis的过期策略和内存淘汰"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ基础知识</title>
    <url>//article/rocker_base1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>RocketMQ 基础知识</p>
</blockquote>
<h1 id="📝-整理流程"><a href="#📝-整理流程" class="headerlink" title="📝 整理流程"></a>📝 整理流程</h1><p><img src="/images/posts/rocker_base1/img-1.png" alt="RocketMQ基础知识"></p>
<h1 id="📝-模型概念"><a href="#📝-模型概念" class="headerlink" title="📝 模型概念"></a>📝 模型概念</h1><p><img src="/images/posts/rocker_base1/img-2.png" alt="RocketMQ基础知识"></p>
<h1 id="📔主题（Topic）"><a href="#📔主题（Topic）" class="headerlink" title="📔主题（Topic）"></a><strong>📔主题（Topic）</strong></h1><p>主题是 Apache RocketMQ 中消息传输和存储的顶层容器，用于标识同一类业务逻辑的消息</p>
<ul>
<li>定义数据的分类隔离;在Apache RocketMQ的方案设计中，建议将不同业务的数据拆分到不同的主题中管理，通过主题实现存储的隔离性和订阅隔离性</li>
<li>主题下存在 MessageQueue : 队列，才是topic实际的操作单元，一个topic可能有多个队列，方便扩容</li>
</ul>
<p>在 Apache RocketMQ 架构中，主题属于顶层资源和容器，拥有独立的权限管理、可观测性指标采集和监控等能力，创建和管理主题会占用一定的系统资源。因此，生产环境需要严格管理主题资源，请勿随意进行增、删、改、查操作</p>
<h1 id="📔队列（MessageQueue）"><a href="#📔队列（MessageQueue）" class="headerlink" title="📔队列（MessageQueue）"></a><strong>📔队列（MessageQueue）</strong></h1><p>队列是RocketMQ实际存储和传输的实际容器，也是RocketMQ消息的最小存储单元。Apache的一个主题由多个队列组成，以实现队列数量的水平拆分和队列内部的流式存储，在向内发送消息，但实际消息发送到该主题下的某个队列里</p>
<details><summary>存储顺序性</summary>

</details>

<details><summary>流式操作语义</summary>

</details>

<p>队列数量的设置应遵循少用够用原则，避免随意添加队列数量，造成下列问题</p>
<ul>
<li>集群元数据膨胀：RocketMQ会以队列的粒度采集指标和监控数据，队列过多容易造成管控元数据膨胀</li>
<li>客户端压力过大：RocketMQ的消息读写都是针对队列进行操作，队列过多容易产生空轮询请求，增加系统负荷</li>
</ul>
<h1 id="📔消息（Message）"><a href="#📔消息（Message）" class="headerlink" title="📔消息（Message）"></a><strong>📔消息（Message）</strong></h1><ul>
<li>消息由生产者初始化并发送到RocketMQ服务端</li>
<li>消息按照达到RocketMQ服务端的顺序存储到队列中</li>
<li>消费者按照订阅的关系从RocketMQ服务端中获取消息并消费</li>
</ul>
<p>四种类型：</p>
<ul>
<li>Normal： 普通消息，消息本身无特殊语义，消息之间也没有任何关联</li>
<li>FIFO：顺序消息，RocketMQ通过消息分组 MessageGroup 标记一组特定消息的先后顺序，可以保证消息的投递顺序严格按照消息发送时的顺序</li>
<li>Delay：定时&#x2F;延时消息，通过指定延时时间控制消息生产后不要立即投递，而是在延时间隔后才对消费者可见</li>
<li>Transaction：RocketMq支持事务消息，支持应用数据库更新和消息的事务一致性保障</li>
</ul>
<h1 id="📔生产者（Producer）"><a href="#📔生产者（Producer）" class="headerlink" title="📔生产者（Producer）"></a><strong>📔生产者（Producer）</strong></h1><p>生产者是RocketMQ系统中用来构建并传输消息服务端的运行实体</p>
<p>生产者通常被集成在业务系统中，将业务消息按照要求封装成RocketMQ的消息(Message)并发送到服务端</p>
<ul>
<li>发送方式：生产者可通过API接口设置消息发送的方式。Apache RocketMQ 支持同步传输和异步传输。</li>
<li>批量发送：生产者可通过API接口设置消息批量传输的方式。例如，批量发送的消息条数或消息大小。</li>
<li>事务行为：Apache RocketMQ 支持事务消息，对于事务消息需要生产者配合进行事务检查等行为保障事务的最终一致性</li>
</ul>
<h1 id="📔消费者分组（ConsumerGroup）"><a href="#📔消费者分组（ConsumerGroup）" class="headerlink" title="📔消费者分组（ConsumerGroup）"></a><strong>📔消费者分组（ConsumerGroup）</strong></h1><p>消费者分组是 Apache RocketMQ 系统中承载多个消费行为一致的消费者的负载均衡分组</p>
<p>和消费者不同，消费者分组并不是运行实体，而是一个逻辑资源。在 Apache RocketMQ 中，通过消费者分组内初始化多个消费者实现消费性能的水平扩展以及高可用容灾</p>
<p>在消费者分组中，统一定义以下消费行为，同一分组下的多个消费者将按照分组内统一的消费行为和负载均衡策略消费消息</p>
<ul>
<li>订阅关系：Apache RocketMQ 以消费者分组的粒度管理订阅关系，实现订阅关系的管理和追溯</li>
<li>投递顺序性：Apache RocketMQ 的服务端将消息投递给消费者消费时，支持顺序投递和并发投递，投递方式在消费者分组中统一配置</li>
<li>消费重试策略： 消费者消费消息失败时的重试策略，包括重试次数、死信队列设置等</li>
</ul>
<p>整体流程：</p>
<ol>
<li>消息有生产者初始化并发送到RocketMQ服务端</li>
<li>消息按照到达RocketMQ服务端顺序存储到主题指定队列中</li>
<li>消费者按照指定的订阅关系从RocketMQ服务端中获取消息并消费</li>
</ol>
<h1 id="📔消费者（Consumer）"><a href="#📔消费者（Consumer）" class="headerlink" title="📔消费者（Consumer）"></a><strong>📔消费者（Consumer）</strong></h1><p>消费者是 Apache RocketMQ 中用来接收并处理消息的运行实体。 消费者通常被集成在业务系统中，从 Apache RocketMQ 服务端获取消息，并将消息转化成业务可理解的信息，供业务逻辑处理</p>
<h1 id="📔订阅关系（Subscription）"><a href="#📔订阅关系（Subscription）" class="headerlink" title="📔订阅关系（Subscription）"></a><strong>📔订阅关系（Subscription）</strong></h1><p>定义：消息过滤规则的类型。订阅关系中设置消息过滤规则后，系统将按照过滤规则匹配主题中的消息，只将符合条件的消息投递给消费者消费，实现消息的再次分类</p>
<ul>
<li>TAG过滤：按照tag字符串进行全文过滤匹配</li>
<li>SQL92过滤：按照SQL语法对消息属性进行过滤匹配</li>
</ul>
<h1 id="消息持久化"><a href="#消息持久化" class="headerlink" title="消息持久化"></a>消息持久化</h1><p><img src="/images/posts/rocker_base1/img-3.png" alt="RocketMQ基础知识"></p>
<ul>
<li>Commitlog: 存储消息的元数据，所有的消息都会按照顺序存入到commitLog文件当中;CommitLog是由多个文件组成，每个文件固定大小1G;以第一条偏移量为名</li>
<li>ConsumerQueue：存储消息在CommitLog的索引；一个MessageQueue一个文件，记录当前MessageQueue被那些消费者组消费到了那条数据</li>
<li>IndexFile: 为了消息查询提供了一种通过key或者时间区间来查询消息的方法，这种通过IndexFile来查找消息的方法不影响发送和消费消息的主流程</li>
<li>checkpoint:数据存盘检查;主要记录commitLog文件,consumerQueue文件以及IndexFile文件最后一次刷盘的时间戳</li>
<li>config&#x2F;*.json: 这些文件是将RocketMQ的一些关键配置信息进行存盘保存。例如Topic配置，消费者组配置，消费者组消息偏移量offset等等一些信息</li>
<li>abort: 该文件用来RocketMQ判断程序是否正常关闭的一个标识文件。正常情况下，会再启动创建，而关闭服务时删除。但是如果遇到一些宕机，或者kill -9这些非正常的关闭服务，这个abort文件就不会被删除；因此RocketMQ就可以判断上一次服务是非正常关闭，后续就会做一些数据恢复的操作</li>
</ul>
<h2 id="commitLog"><a href="#commitLog" class="headerlink" title="commitLog"></a>commitLog</h2><p>CommitLog存储了所有的消息实体；所有生产者发过来的消息，都会无差别的依次的存储到CommitLog文件当中；</p>
<p>好处： 减少查找目标文件的时间，让消息以最快的速度落盘</p>
<p>对比Kafka存文件，需要寻找消息所属的Partition文件，在完成写入，当Topic较多的时候，这样的Partition寻址就会比较浪费时间，所以Kafka不适合多Topic的场景</p>
<p>RocketMQ的这种快速落盘的方式在多Topic的场景下，优势会比较明显</p>
<p>commitLog的文件大小是固定的，但是其存储的每个消息单元长度是不固定的</p>
<p>正因为消息的记录大小不固定，所以RocketMQ在每次存CommitLog文件时，都会去检查当前CommitLog文件空间是否足够，如果不够的话，会重新创建一个CommitLog文件</p>
<h2 id="ConsumeQueue"><a href="#ConsumeQueue" class="headerlink" title="ConsumeQueue"></a>ConsumeQueue</h2><p>consumeQueue文件主要是加速消费者的消费索引；它的每个文件夹对应RocketMQ中的一个MessageQueue，文件夹下的文件记录了每个MessageQueue中的消息在CommitLog文件当中的偏移量；这样，消费者通过ConsumeQueue文件，就可以快速找到CommitLog文件中感兴趣的消息记录；而消费者在ConsumeQueue文件当中的消费进度，会保存在config&#x2F;consumerOffset.json文件中</p>
<p>每个ConsumeQueue文件由固定30万个固定大小20byte的数组块组成，数据块的内容包括：msgPhyOffset(8 byte,消息在文件中的起始位置) + msgSize(4 byte,消息在文件中占用的长度) + msgTagCode(8 byte,消息的tag的hash值)</p>
<h2 id="IndexFile"><a href="#IndexFile" class="headerlink" title="IndexFile"></a>IndexFile</h2><p>辅助消息的检索；消费者进行消息消费时，通过ConsumeQueue文件就足够完成消息检索了，但是如果按照MessageId或者messageKey来检索文件，就不够了（比如RocketMQ的管理台）；IndexFile文件就是用来辅助这类消息检索的</p>
<p>他的文件名比较特殊，不是以消息偏移量命名，而是用的时间命名。但是其实，他也是一个固定大小的文件</p>
<p>文件由 indexHeader(固定40byte) + slot(固定500w个，每个固定20byte) + index(最多500w*4个,每个固定20byte)三部分组成</p>
<h1 id="过期文件删除"><a href="#过期文件删除" class="headerlink" title="过期文件删除"></a>过期文件删除</h1><p>消息既然要持久化,就必然有对应的删除机制</p>
<p>RocketMQ内置了一套过期文件的删除机制</p>
<h2 id="如何判断过期文件"><a href="#如何判断过期文件" class="headerlink" title="如何判断过期文件"></a>如何判断过期文件</h2><p>RocketMQ中,CommitLog文件和ConsumeQueue文件是以偏移量命名的,对于非当前写的文件，如果超过了一定的保留时间，那么这些文件都会被认为是过期文件，随时可以删除。</p>
<p>保留时间就是broker.conf中配置的fileReservedTime属性</p>
<p>注意，RocketMQ判断文件是否过期的唯一标准就是非当前写文件的保留时间，而并不关心文件当中的消息是否被消费过。所以，RocketMQ的消息堆积也是有时间限度的</p>
<h2 id="如何删除过期文件"><a href="#如何删除过期文件" class="headerlink" title="如何删除过期文件"></a>如何删除过期文件</h2><p>RocketMQ内部有一个定时任务，对文件进行扫描，并且触发文件删除的操作。用户可以指定文件删除操作的执行时间。在broker.conf中deleteWhen属性指定。默认是凌晨四点</p>
<p>RocketMQ还会检查服务器的磁盘空间是否足够，如果磁盘空间的使用率达到一定阈值，也会触发过期文件删除；所以RocketMQ官方就特别建议，borker的磁盘空间不要少于4G</p>
<h1 id="高效写文件"><a href="#高效写文件" class="headerlink" title="高效写文件"></a>高效写文件</h1><h2 id="零拷贝加速文件读写"><a href="#零拷贝加速文件读写" class="headerlink" title="零拷贝加速文件读写"></a>零拷贝加速文件读写</h2><p>零拷贝是操作系统层面提供的一种加速文件读写的操作机制，对应多的开源项目都在大量使用零拷贝，来提升IO操作的性能</p>
<p>对于java层面，对应着mmap和sendFile两种方式</p>
<h3 id="理解CPU拷贝和DMA拷贝"><a href="#理解CPU拷贝和DMA拷贝" class="headerlink" title="理解CPU拷贝和DMA拷贝"></a>理解CPU拷贝和DMA拷贝</h3><p>操作系统对于内存空间，是分为用户态和内核态的。用户态的应用程序无法直接操作硬件，需要通过内核空间进行操作转换，才能真正操作硬件。这其实是为了保护操作系统的安全。正因为如此，应用程序需要与网卡、磁盘等硬件进行数据交互时，就需要在用户态和内核态之间来回的复制数据。而这些操作，原本都是需要由CPU来进行任务的分配、调度等管理步骤的，早先这些IO接口都是由CPU独立负责，所以当发生大规模的数据读写操作时，CPU的占用率会非常高</p>
<p>之后，操作系统为了避免CPU完成被各种IO调用给占用，引入了DMA(直接存储器).有DMA来负责这些频繁的IO操作</p>
<p>DMA是一套独立的指令集，不会直接占用CPU的计算资源；这样CPU就不需要参与具体的数据复制的工作，只需要管理DMA权限即可</p>
<p>DMA拷贝极大的释放了CPU的性能，因此他的拷贝速度会比CPU拷贝要快很多；但是DMA拷贝本身，也在不断优化</p>
<p><img src="/images/posts/rocker_base1/img-4.png" alt="RocketMQ基础知识"></p>
<p>引入DMA拷贝之后，在读写请求的过程中，CPU不需要参与具体的工作，DMA可以独立完成数据在系统内部的复制。但是数据的复制，依然需要借助数据总线进行；但系统内的IO操作过多时，还是会占用过多的数据总线，造成总线冲突，最终还是会影响数据读写的性能</p>
<p>为了避免DMA总线冲突对性能的影响，后来又引入了Channel通道的方式。Channel，是一个完全独立的处理器，专门负责IO操作。既然是处理器，Channel就有自己的IO指令，与CPU无关，他也更适合大型的IO操作，性能更高</p>
<p><img src="/images/posts/rocker_base1/img-5.png" alt="RocketMQ基础知识"></p>
<p>这也是java层面的零拷贝相关的操作都是通过Channel的子类实现的</p>
<p>零拷贝技术，其实并不是不拷贝，而是经历减少了CPU拷贝</p>
<h3 id="mmap文件映射机制"><a href="#mmap文件映射机制" class="headerlink" title="mmap文件映射机制"></a>mmap文件映射机制</h3><p>主要是通过java.nio.channels.FileChannel的map方法完成映射</p>
<p>以一次文件的读写操作为例子，应用程序对磁盘文件的读与写，都需要经过内核态与用户态之间的的状态切换，每次状态切换的过程中，都需要大量的数据复制</p>
<p>在这个过程中,总共需要进行四次数据拷贝；而磁盘与内核态之间的数据拷贝，在操作系统层面已经由CPU拷贝优化为了DMA拷贝；而内核态与用户态之间拷贝依然是CPU拷贝；在这个场景下，零拷贝技术优化的重点，就是内核态与用户态之间的两次拷贝</p>
<p>而mmap文件映射的方式，就是在用户态不在保存文件的内容，而只保存文件的映射，包括文件的内存起始地址，文件大小等</p>
<p>真实的数据，也不需要在用户态留存，可以直接通过操作映射，在内核态完成数据复制</p>
<p><img src="/images/posts/rocker_base1/img-6.png" alt="RocketMQ基础知识"></p>
<p><img src="/images/posts/rocker_base1/img-7.png" alt="RocketMQ基础知识"></p>
<p>这种mmap的映射机制由于还是需要用户态保存文件的映射信息，数据复制的过程也需要用户态的参与，这其中的变数是非常多的。</p>
<p>所以mmap机制非常适合操作小文件，如果文件太大，映射信息也会过大，容易造成问多问题</p>
<p>通过mmap机制建议的映射文件不要超过2g,而RocketMQ做大的commitLog文件保持在1g固定大小,也是为了方便文件映射</p>
<h3 id="sendFile机制运行"><a href="#sendFile机制运行" class="headerlink" title="sendFile机制运行"></a>sendFile机制运行</h3><p>主要是通过java.nio.channels.FileChannel的transferTo方法完成</p>
<p>早期的sendFile机制其实还是依靠CPU进行页缓存与socket缓存区之间的数据拷贝；但是在后期不断改进过程中，sendfile优化了实现机制</p>
<p>在拷贝过程中，并不直接拷贝文件的内容，而是只拷贝一个带有文件位置和长度等信息的文件描述符FD，这样就大大减少了需要传递的数据；而真实的数据内容，会交由DMA控制器，从缓存页中打包异步发送到socket中</p>
<p><img src="/images/posts/rocker_base1/img-8.png" alt="RocketMQ基础知识"></p>
<p><img src="/images/posts/rocker_base1/img-9.png" alt="RocketMQ基础知识"></p>
<h2 id="顺序写加速文件写入磁盘"><a href="#顺序写加速文件写入磁盘" class="headerlink" title="顺序写加速文件写入磁盘"></a>顺序写加速文件写入磁盘</h2><p>通常应用程序往磁盘写文件时，由于磁盘空间不是连续的，会有很多碎片。所以我们去写一个文件时，也就无法把一个文件写在一块连续的磁盘空间中，而需要在磁盘多个扇区之间进行大量的随机写。这个过程中有大量的寻址操作，会严重影响写数据的性能。而顺序写机制是在磁盘中提前申请一块连续的磁盘空间，每次写数据时，就可以避免这些寻址操作，直接在之前写入的地址后面接着写就行</p>
<p>Kafka官方详细分析过顺序写的性能提升问题。Kafka官方曾说明，顺序写的性能基本能够达到内存级别。而如果配备固态硬盘，顺序写的性能甚至有可能超过写内存。而RocketMQ很大程度上借鉴了Kafka的这种思想</p>
<p>org.apache.rocketmq.store.CommitLog#DefaultAppendMessageCallback中的doAppend方法。在这个方法中，会以追加的方式将消息先写入到一个堆外内存byteBuffer中，然后再通过fileChannel写入到磁盘</p>
<h2 id="刷盘机制保证消息不丢失"><a href="#刷盘机制保证消息不丢失" class="headerlink" title="刷盘机制保证消息不丢失"></a>刷盘机制保证消息不丢失</h2><p>在操作系统层面，当应用程序写入一个文件时，文件内容并不会直接写入到硬件当中，而是会先写入到操作系统中的一个缓存PageCache中。PageCache缓存以4K大小为单位，缓存文件的具体内容</p>
<p>这些写入PageCache中的文件,在应用程序看来,是已经完全落盘保存好了的，可以修改，复制等等</p>
<p>但是，本质上，PageCache依然是内存状态，所以一断电就会丢失。因此，需要将内存状态的数据写入到磁盘当中，这样数据才能真正完成持久化，断电也不会丢失。这个过程就称为刷盘</p>
<p>PageCache是源源不断产生的，而Linux操作系统显然不可能时时刻刻往硬盘写文件。所以，操作系统只会在某些特定的时刻将PageCache写入到磁盘。例如当我们正常关机时，就会完成PageCache刷盘。另外，在Linux中，对于有数据修改的PageCache，会标记为Dirty(脏页)状态。当Dirty Page的比例达到一定的阈值时，就会触发一次刷盘操作。例如在Linux操作系统中，可以通过&#x2F;proc&#x2F;meminfo文件查看到Page Cache的状态</p>
<p>但是操作系统的刷盘不是时时刻刻执行的，那么对于用户态的应用程序来说，那就避免不了非正常宕机时的数据丢失问题。因此，操作系统也提供了一个系统调用，应用程序可以自行调用这个系统调用，完成page cache的强制刷盘</p>
<p>RocketMQ对何时刷盘也提供了两种刷盘机制,同步刷盘和异步刷盘</p>
<ul>
<li>同步刷盘： 在返回成功状态前,消息被写入磁盘;具体流程是，消息写入内存的PAGECACHE后，立刻通知刷盘线程刷盘， 然后等待刷盘完成，刷盘线程执行完成后唤醒等待的线程，返回消息写 成功的状态</li>
<li>异步刷盘:  在返回写成功状态时，消息可能只是被写入了内存的PAGECACHE，写操作的返回快，吞吐量大；当内存里的消息量积累到一定程度时，统一触发写磁盘动作，快速写入</li>
<li>配置方式： borker配置文件里的flushDiskType参数设置的,这个参数被配置成 SYNC_FLUSH , ASYNC_FLUSH 中的一个</li>
</ul>
<p>同步刷盘机制会更频繁的调用fsync，所以吞吐量相比异步刷盘会降低，但是数据的安全性会得到提高</p>
<p><img src="/images/posts/rocker_base1/img-10.png" alt="RocketMQ基础知识"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>官网地址： <a href="https://rocketmq.apache.org/">https://rocketmq.apache.org/</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis存储原理和持久化</title>
    <url>//article/redis_storage_write/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Redis的存储和持久</p>
</blockquote>
<h1 id="📝-Redis存储原理"><a href="#📝-Redis存储原理" class="headerlink" title="📝 Redis存储原理"></a>📝 Redis存储原理</h1><h2 id="📔数据库"><a href="#📔数据库" class="headerlink" title="📔数据库"></a><strong>📔数据库</strong></h2><p>Redis服务端一般有<strong>16</strong>个数据库，默认情况下都在<strong>0号</strong>数据库操作，我们也可以通过命令来实现数据库的切换   –select 1-16 命令</p>
<p>对数据库的增删改查，本质上就是通过客户端指向的数据库，查找里面的字典，匹配对应的key，取出对应的value</p>
<p><img src="/images/posts/redis_storage_write/img-1.png" alt="Redis存储原理和持久化"></p>
<h2 id="📔字典表-dict"><a href="#📔字典表-dict" class="headerlink" title="📔字典表 dict"></a><strong>📔字典表 dict</strong></h2><p>每个数据库都有一个字典结构，这个字典里存着两个hash表（为了之后的扩缩容）</p>
<p><strong>整体使用了一个大的 hash 表, 因为 hash 能够尽可能的提供 O1 时间复杂度的效率</strong></p>
<p>而这个hash表里有一个dictEntry 组成的数组，里面存放的就是所有的键值对。这个dictEntry还有指向下一个节点的指针，就是为了在hash冲突的情况，采用拉链法扩展出一个链表</p>
<p>向字典表再添加一个元素 <code>set name xxx</code>我们会先对key做散列运算，将得到的值再对哈希表的大小做一个取余，假设得到的值是<code>1</code>，那么这个key就会落在<code>1</code>的位置,如果多个元素都落在同一个元素，就会形成一个链表，比如：</p>
<details><summary>字典表属性解读</summary>

</details>

<p><img src="/images/posts/redis_storage_write/img-2.png" alt="Redis存储原理和持久化"></p>
<h3 id="字典表扩容"><a href="#字典表扩容" class="headerlink" title="字典表扩容"></a>字典表扩容</h3><ol>
<li>Redis 会检查字典表的负载因子，如果超过了负载因子的阈值(默认0.8),则进行扩容</li>
<li>Redis会计算出一个新的字典表大小,通常为原来的2倍</li>
<li>Redis会遍历原来的字典表，将所有的键值对rehash到新的字典表中,在rehash过程中,Redis会计算每个键值对在新的字典表中的位置，并插入</li>
<li>rehash完成后，Redis释放原来的字典表，并使用新的字典表替代</li>
<li>重新计算字典表的负载因子，如果仍然超过了阈值，则重复1到4的过程</li>
</ol>
<h3 id="扩容的影响"><a href="#扩容的影响" class="headerlink" title="扩容的影响"></a>扩容的影响</h3><h2 id="📔RedisObject"><a href="#📔RedisObject" class="headerlink" title="📔RedisObject"></a><strong>📔RedisObject</strong></h2><p>Redis数据库中的每个键值对的键和值都是RedisObject对象,对应字典表的dictEntry</p>
<ul>
<li>Type: 类型，可以是String, List，Hash，Set, sortSet其中一种</li>
<li>Encoding: 底层数据结构的编码类型， 比如String可能是int，embStr，Raw其中一种，不同编码可以在不同场景上优化使用效率</li>
<li>lru: 记录对象最后一次被访问的时间</li>
</ul>
<details><summary>ptr: 如果是整数直接存储数据，否则表示指向该数据的指针</summary>

</details>

<p>比如我们执行一个命令 <code>set user zhao</code>在字典上的数据结构大概是这样</p>
<p><img src="/images/posts/redis_storage_write/img-3.png" alt="Redis存储原理和持久化"></p>
<h1 id="💾-Redis持久化"><a href="#💾-Redis持久化" class="headerlink" title="💾 Redis持久化"></a>💾 Redis持久化</h1><p>持久化是指将内存中的数据保存到磁盘中，以保证数据持久性。Redis支持两种持久化方式：</p>
<h2 id="RDB"><a href="#RDB" class="headerlink" title="RDB"></a>RDB</h2><p>RDB持久化是指将Redis在内存中的数据以快照的形式写入到磁盘中。快照持久化的优点是备份的数据文件比较小，启动时恢复速度快。RDB持久化采用的是单独的进程进行持久化操作，其优点是对系统的IO压力和性能影响很小，同时可以在指定的时间间隔内产生多个数据快照。而缺点就是由于是定时快照，所以如果Redis意外宕机，就会丢失最后一次快照后的所有数据，因此RDB持久化方式主要适用于数据相对重要性不高，可以承受一定数据丢失的场景。</p>
<p>客户端可向服务端发送save或bgsave命令让服务器生成 rdb 文件;或者通过服务器配置文件(<strong>redis.config</strong>)指定触发RDB条件</p>
<p><img src="/images/posts/redis_storage_write/img-4.png" alt="Redis存储原理和持久化"></p>
<h3 id="save"><a href="#save" class="headerlink" title="save"></a>save</h3><ol>
<li>当客户端向服务器发送save命令请求进行持久化时，服务器会阻塞save命令之后的其他客户端的请求，直到数据同步完成</li>
<li>如果数据量太大，同步数据会执行很久，而这期间Redis服务器也无法接收其他请求，所以，<strong>最好不要在生产环境使用</strong><code>save</code><strong>命令。</strong></li>
</ol>
<p><img src="/images/posts/redis_storage_write/img-5.png" alt="Redis存储原理和持久化"></p>
<h3 id="bgSave"><a href="#bgSave" class="headerlink" title="bgSave"></a>bgSave</h3><p><strong>与save命令不同，bgsave命令是一个异步操作</strong></p>
<p>当客户端发服务发出bgsave命令时，<strong>Redis服务器主进程会forks一个子进程来数据同步问题，在将数据保存到rdb文件之后，子进程会退出</strong>。</p>
<p>所以，与save命令相比，Redis服务器在处理bgsave采用子线程进行IO写入，而主进程仍然可以接收其他请求，<strong>但forks子进程是同步的，所以forks子进程时，一样不能接收其他请求，这意味着，如果forks一个子进程花费的时间太久(一般是很快的)，bgsave命令仍然有阻塞其他客户的请求的情况发生</strong></p>
<p><strong>fork</strong>：在 Linux 系统中，调用 fork() 时，会创建出一个新进程，称为子进程，子进程会拷贝父进程的 page table。如果进程占用的内存越大，进程的 page table 也会越大，那么 fork 也会占用更多的时间。如果 Redis 占用的内存很大，那么在 fork 子进程时，则会出现明显的停顿现象</p>
<p><img src="/images/posts/redis_storage_write/img-6.png" alt="Redis存储原理和持久化"></p>
<details><summary>**快照生成期间，数据被修改，如何同步最新的数据**</summary>

</details>

<h3 id="RDB文件"><a href="#RDB文件" class="headerlink" title="RDB文件"></a>RDB文件</h3><ol>
<li>生成临时rdb文件，并写入数据。</li>
<li>完成数据写入，用临时文代替代正式rdb文件。</li>
<li>删除原来的db文件。</li>
</ol>
<h3 id="RDB优点"><a href="#RDB优点" class="headerlink" title="RDB优点"></a>RDB优点</h3><ol>
<li>与AOF方式相比，通过rdb文件恢复数据比较快。(因为是二进制压缩文件)</li>
<li>rdb文件非常紧凑，适合于数据备份。(因为是二进制压缩文件)</li>
<li>通过RDB进行数据备，由于使用子进程生成，所以对Redis服务器性能影响较小。</li>
</ol>
<h3 id="RDB缺点"><a href="#RDB缺点" class="headerlink" title="RDB缺点"></a>RDB缺点</h3><ol>
<li>如果服务器宕机的话，采用RDB的方式会造成某个时段内数据的丢失，比如我们设置10分钟同步一次或5分钟达到1000次写入就同步一次，那么如果还没达到触发条件服务器就死机了，那么这个时间段的数据会丢失。</li>
<li>使用save命令会造成服务器阻塞，直接数据同步完成才能接收后续请求。</li>
<li>使用bgsave命令在forks子进程时，如果数据量太大，forks的过程也会发生阻塞，另外，forks子进程会耗费内存。</li>
</ol>
<h2 id="AOF"><a href="#AOF" class="headerlink" title="AOF"></a>AOF</h2><p>AOF持久化是指将Redis的写操作以日志的形式记录到磁盘中。AOF持久化在性能和数据完整性方面要优于RDB持久化方式。AOF持久化的优点是数据完整性高，即使Redis宕机，也只会丢失最后一次写操作之后的数据，而对于数据相对重要性比较高的场景，AOF持久化方式是首选。AOF持久化的缺点是对于大量的写操作，日志文件会变得非常大，从而影响恢复速度。</p>
<p>append only file,<strong>AOF持久化方式会记录客户端对服务器的每一次写操作命令，并将这些写操作以Redis协议追加保存到以后缀为aof文件末尾，在Redis服务器重启时，会加载并运行aof文件的命令，以达到恢复数据的目的</strong></p>
<p>AOF的持久化功能的实现可以分为三个步： 命令追加，文件写入，文件同步</p>
<p><strong>命令追加: 当 AOF 持久化功能打开时，服务器在执行完一个写命令之后，会将被执行的写命令追加到服务器状态的 aof 缓冲区（aof_buf）的末尾</strong></p>
<p><strong>文件写入</strong>与<strong>文件同步: 可能有人不明白为什么将 aof_buf 的内容写到磁盘上需要两步操作，这边简单解释一下</strong></p>
<p>Linux 操作系统中为了提升性能，使用了页缓存（page cache）。当我们将 aof_buf的内容写到磁盘上时，此时数据并没有真正的落盘，而是在 page cache 中，为了将 page cache 中的数据真正落盘，需要执行 fsync &#x2F; fdatasync 命令来强制刷盘。这边的文件同步做的就是刷盘操作，或者叫文件刷盘可能更容易理解一些</p>
<p><img src="/images/posts/redis_storage_write/img-7.png" alt="Redis存储原理和持久化"></p>
<h3 id="开启AOF持久化方式"><a href="#开启AOF持久化方式" class="headerlink" title="开启AOF持久化方式"></a>开启AOF持久化方式</h3><p><strong>Redis默认不开启AOF持久化方式，我们可以在配置文件中开启并进行更加详细的配置，如下面的redis.conf文件</strong></p>
<p><img src="/images/posts/redis_storage_write/img-8.png" alt="Redis存储原理和持久化"></p>
<h3 id="三种写入策略"><a href="#三种写入策略" class="headerlink" title="三种写入策略"></a>三种写入策略</h3><p>以上三种策略只是在不同的时机去调用 OS的 fsync()函数;</p>
<ol>
<li>write 只要把日志记录写到内核缓冲区，就返回了, 并不需要等待日志实际写回到磁盘；</li>
<li>fsync 需要把日志记录写回到磁盘后才能返回，时间较长。</li>
</ol>
<ul>
<li>Always 策略就是每次写入 AOF 文件数据后，就执行 <strong>fsync</strong>() 函数； (<strong>主线程操作</strong>)</li>
<li>Everysec 策略就会创建一个异步任务来执行 <strong>fsync</strong>() 函数； (<strong>子线程操作</strong>)</li>
<li>No 策略就是不执行 <strong>fsync</strong>() 函数, 执行 <strong>write</strong>()函数 , 交给 OS 自己决定调用时机</li>
</ul>
<p><strong>如果你不小心执行了 FLUSHALL 命令把所有数据刷掉了</strong></p>
<p><strong>但只要 AOF 文件没有被重写，那么只要停止服务器， 移除 AOF 文件末尾的 FLUSHALL 命令， 并重启 Redis ， 就可以将数据集恢复到 FLUSHALL 执行之前的状态</strong></p>
<p><img src="/images/posts/redis_storage_write/img-9.png" alt="Redis存储原理和持久化"></p>
<h3 id="AOF文件重写"><a href="#AOF文件重写" class="headerlink" title="AOF文件重写"></a>AOF文件重写</h3><p>AOF将客户端的每一个写操作都追加到aof文件末尾，比如对一个key多次执行incr命令，这时候，aof保存每一次命令到aof文件中，aof文件会变得非常大</p>
<p><img src="/images/posts/redis_storage_write/img-10.png" alt="Redis存储原理和持久化"></p>
<p>为了解决这个问题，Redis支持aof文件重写，通过重写aof，可以生成一个恢复当前数据的最少命令集，比如上面的例子中那么多条命令，可以重写为</p>
<p><img src="/images/posts/redis_storage_write/img-11.png" alt="Redis存储原理和持久化"></p>
<h3 id="重写时机"><a href="#重写时机" class="headerlink" title="重写时机"></a>重写时机</h3><ol>
<li>auto-aof-rewrite-min-size: 表示运行AOF重写时文件的最小大小，默认为64MB</li>
<li>auto-aof-rewrite-percentage: 这个值的计算方法是：当前AOF文件大小和上一次重写后AOF文件大小的差值，再除以上一次重写后AOF文件大小。也就是当前AOF文件比上一次重写后AOF文件的增量大小，和上一次重写后AOF文件大小的比值</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">auto-aof-rewrite-percentage 100</span><br><span class="line">auto-aof-rewrite-min-size 64mb</span><br></pre></td></tr></table></figure>

<p>在重写即将开始之际，redis会创建（fork）一个”重写子进程”，这个子进程会首先读取现有的AOF文件，并将其包含的指令进行分析压缩并写入到一个临时文件中</p>
<p>与此同时，主工作进程会将新接收到的写指令一边累积到内存缓冲区中，一边继续写入到原有的AOF文件中，这样做是保证原有的AOF文件的可用性，避免在重写过程中出现意外</p>
<p>当”重写子进程”完成重写工作后，它会给父进程发一个信号，父进程收到信号后就会将内存中缓存的写指令追加到新AOF文件中,当追加结束后，redis就会用新AOF文件来代替旧AOF文件，之后再有新的写指令，就都会追加到新的AOF文件中了</p>
<p><img src="/images/posts/redis_storage_write/img-12.png" alt="Redis存储原理和持久化"></p>
<h3 id="AOF后台重写存在的问题"><a href="#AOF后台重写存在的问题" class="headerlink" title="AOF后台重写存在的问题"></a>AOF后台重写存在的问题</h3><p>AOF 后台重写使用子进程进行从写，解决了主进程阻塞的问题，但是仍然存在另一个问题：子进程在进行 AOF 重写期间，服务器主进程还需要继续处理命令请求，新的命令可能会对现有的数据库状态进行修改，从而使得当前的数据库状态和重写后的 AOF 文件保存的数据库状态不一致</p>
<h3 id="如何解决-AOF-后台重写存在的数据不一致问题"><a href="#如何解决-AOF-后台重写存在的数据不一致问题" class="headerlink" title="如何解决 AOF 后台重写存在的数据不一致问题"></a><strong>如何解决 AOF 后台重写存在的数据不一致问题</strong></h3><p>为了解决上述问题，<strong>Redis 引入了 AOF 重写缓冲区（aof_rewrite_buf_blocks）</strong>，这个缓冲区在服务器创建子进程之后开始使用，当 Redis 服务器执行完一个写命令之后，它会同时将这个写命令追加到 AOF 缓冲区和 AOF 重写缓冲区</p>
<p><img src="/images/posts/redis_storage_write/img-13.png" alt="Redis存储原理和持久化"></p>
<h3 id="AOF优点"><a href="#AOF优点" class="headerlink" title="AOF优点"></a>AOF优点</h3><p>AOF只是追加日志文件，因此对服务器性能影响较小，<strong>速度比RDB要快，消耗的内存较少</strong></p>
<h3 id="AOF缺点"><a href="#AOF缺点" class="headerlink" title="AOF缺点"></a>AOF缺点</h3><ul>
<li>AOF方式生成的<strong>日志文件太大，即使通过AOF重写，文件体积仍然很大</strong><strong>。</strong></li>
<li><strong>恢复数据的速度比RDB慢，需要解析语句</strong></li>
</ul>
<h2 id="混合持久化"><a href="#混合持久化" class="headerlink" title="混合持久化"></a>混合持久化</h2><h3 id="描述"><a href="#描述" class="headerlink" title="描述:"></a>描述:</h3><p>混合持久化并不是一种全新的持久化方式，而是对已有方式的优化。混合持久化只发生于 AOF 重写过程。使用了混合持久化，重写后的新 AOF 文件前半段是 RDB 格式的全量数据，后半段是 AOF 格式的增量数据</p>
<p>整体格式为：[RDB file][AOF tail]</p>
<h3 id="开启"><a href="#开启" class="headerlink" title="开启"></a>开启</h3><p>混合持久化的配置参数为 aof-use-rdb-preamble，配置为 yes 时开启混合持久化，在 redis 4 刚引入时，默认是关闭混合持久化的，但是在 redis 5 中默认已经打开了</p>
<p><strong>混合持久化本质是通过 AOF 后台重写（bgrewriteaof 命令）完成的</strong>，不同的是当开启混合持久化时，fork 出的子进程先将当前全量数据以 RDB 方式写入新的 AOF 文件，然后再将 AOF 重写缓冲区（aof_rewrite_buf_blocks）的增量命令以 AOF 方式写入到文件，写入完成后通知主进程将新的含有 RDB 格式和 AOF 格式的 AOF 文件替换旧的的 AOF 文件</p>
<h3 id="优点-结合-RDB-和-AOF-的优点-更快的重写和恢复"><a href="#优点-结合-RDB-和-AOF-的优点-更快的重写和恢复" class="headerlink" title="优点: 结合 RDB 和 AOF 的优点, 更快的重写和恢复"></a>优点: 结合 RDB 和 AOF 的优点, 更快的重写和恢复</h3><h3 id="缺点：AOF-文件里面的-RDB-部分不再是-AOF-格式，可读性差"><a href="#缺点：AOF-文件里面的-RDB-部分不再是-AOF-格式，可读性差" class="headerlink" title="缺点：AOF 文件里面的 RDB 部分不再是 AOF 格式，可读性差"></a><strong>缺点</strong>：AOF 文件里面的 RDB 部分不再是 AOF 格式，可读性差</h3><h2 id="📔持久化文件损坏"><a href="#📔持久化文件损坏" class="headerlink" title="📔持久化文件损坏"></a><strong>📔持久化文件损坏</strong></h2><p>在写入aof日志文件时，如果Redis服务器宕机，则aof日志文件文件会出格式错误，在重启Redis服务器时，Redis服务器会拒绝载入这个aof文件，可以通过以下步骤修复aof并恢复数据</p>
<ol>
<li>备份现在aof文件，以防万一。</li>
<li>使用redis-check-aof -fix 命令修复aof文件，该命令格式如下</li>
</ol>
<p>修复aof日志文件</p>
<details><summary>redis-check-aof -fix file.aof</summary>

</details>

<h2 id="📔RDB和AOF总结"><a href="#📔RDB和AOF总结" class="headerlink" title="📔RDB和AOF总结"></a><strong>📔RDB和AOF总结</strong></h2><ol>
<li>RDB优点：采用了子进程生成RDB文件，减少对Redis主线程的阻塞，保证Redis的性能;RDB是内存快照，恢复效率高</li>
<li>RDB不足: RDB是间隔一段时间生成一次,两次RDB创建之前,如果Redis发生故障,会发生数据丢失</li>
<li>AOF优点：采用always配置项时，可以每个命令做一次持久化，可靠性相比RDB更高</li>
<li>AOF不足: AOF文件较大,会触发AOF重写，重写时会竞争内部资源；AOF恢复是回放命令操作，速度慢于RDB</li>
</ol>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>中间件</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>hadoop基础知识</title>
    <url>//article/hadoop_base/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>hadoop基础知识学习概念<br>大数据的特点: volume大量;velcity高速;variety多样;value低价值密度</p>
</blockquote>
<h1 id="📝-hadoop框架组件"><a href="#📝-hadoop框架组件" class="headerlink" title="📝 hadoop框架组件"></a>📝 hadoop框架组件</h1><h2 id="Hdfs"><a href="#Hdfs" class="headerlink" title="Hdfs"></a>Hdfs</h2><p>Hadoop Distributed File System:分布式文件系统</p>
<ul>
<li>NameNode: 存储文件的元数据</li>
<li>DataNode: 存储文件的块数据,以及校验和</li>
<li>Secondary NameNode: 每隔一段时间备份元数据</li>
</ul>
<h2 id="Yarn"><a href="#Yarn" class="headerlink" title="Yarn"></a>Yarn</h2><p>Yet Another Resource Negotiator: 另一种资源协调者;hadoop的资源管理器</p>
<ul>
<li>Resource: 管理整个集群资源</li>
<li>DataManager: 单个节点资源管理器的老大</li>
<li>Application Master: 单个运行任务的老大</li>
<li>Container: 容器,封装了一个任务运行所需的资源</li>
</ul>
<h2 id="MapReduce"><a href="#MapReduce" class="headerlink" title="MapReduce"></a>MapReduce</h2><p>Map阶段并行处理输入数据</p>
<p>Reduce阶段对Map结果进行汇总</p>
<h1 id="📝-HDFS"><a href="#📝-HDFS" class="headerlink" title="📝 HDFS"></a>📝 HDFS</h1><p>一种系统来管理多台机器上的文件,分布式文件管理;适合一次写入,多次读出的场景</p>
<details><summary>优点</summary>

</details>

<details><summary>缺点</summary>

</details>

<h2 id="NameNode"><a href="#NameNode" class="headerlink" title="NameNode"></a>NameNode</h2><p>管理hdfs的名称空间,配置副本策略,管理数据块映射信息,处理客户端的读写请求</p>
<h2 id="DataNode"><a href="#DataNode" class="headerlink" title="DataNode"></a>DataNode</h2><p>存储实际块;NameNode发出命令,DataNode执行客户端的读写指令</p>
<p>一个数据块在DataNode上以文件形式存储在磁盘上,包括两个文件,一个是数据本身,一个是元数据包括数据块的长度,块数据的检验以及时间戳</p>
<p>DataNode启动后向NameNode注册,通过后,周期性(6小时)向NameNode上报所有的快信息;心跳是每3秒一次,心跳返回结果带有NameNode给该DataNode的命令如复制块数据到另一台机器,或删除某个数据块;如果超过10分钟+30s没有收到某个DataNode的心跳,则认为该节点不可用</p>
<details><summary>数据完整性</summary>

</details>

<details><summary>**块大小或者块太多问题**</summary>

</details>

<p>块大小的设置,最好根据磁盘的传输速度来决定</p>
<h2 id="Yarn-1"><a href="#Yarn-1" class="headerlink" title="Yarn"></a>Yarn</h2><p>Yarn是一个资源调度平台,负责为程序提供服务器运行资源;相当于一个分布式操作系统平台;而MapReduce等运行程序则相当于运行于操作系统之上的应用程序</p>
<h3 id="resourceManager"><a href="#resourceManager" class="headerlink" title="resourceManager"></a>resourceManager</h3><ul>
<li>集群资源的分配和调度</li>
<li>管理nodeManager</li>
<li>管理&#x2F;启动AppMaster</li>
<li>处理客户端请求</li>
</ul>
<h3 id="nodeManager"><a href="#nodeManager" class="headerlink" title="nodeManager"></a>nodeManager</h3><ul>
<li>处理resoureManager命令</li>
<li>处理AppMaster命令</li>
<li>管理单个节点上的资源调配</li>
</ul>
<h3 id="AppMaster"><a href="#AppMaster" class="headerlink" title="AppMaster"></a>AppMaster</h3><ul>
<li>管理单个任务的资源申请和分配</li>
<li>对任务的监控和容错</li>
</ul>
<h3 id="Container"><a href="#Container" class="headerlink" title="Container"></a>Container</h3><ul>
<li>封装某个节点上的资源,cpu,ram,rom等</li>
</ul>
<h3 id="调度器和调度算法"><a href="#调度器和调度算法" class="headerlink" title="调度器和调度算法"></a>调度器和调度算法</h3><ul>
<li>先进先出调度器(FIFO): 单队列,根据作业提交的顺序,先来先处理;不支持多队列,紧急的job无法插队</li>
<li>容量调度器(Capacity Scheduler): 多队列,每个队列采用fifo策略,分配一定的资源;容量可进行设置;多租户,支持多用户共享集群,多任务并行防止同一用户作业占据所有资源</li>
<li>公平调度器(Fair Scheduler): 优先资源缺额比较大的;fifo,fair,drf(同时考虑不同资源将不同资源按比例分配)</li>
</ul>
<h3 id="一个作业流程"><a href="#一个作业流程" class="headerlink" title="一个作业流程"></a>一个作业流程</h3><details><summary>作业提交</summary>

</details>

<details><summary>作业初始化</summary>

</details>

<details><summary>任务分配</summary>

</details>

<details><summary>任务运行</summary>

</details>

<details><summary>进度和状态更新</summary>

</details>

<details><summary>作业完成</summary>

</details>

<h3 id="yarn工作机制"><a href="#yarn工作机制" class="headerlink" title="yarn工作机制"></a>yarn工作机制</h3><ol>
<li>MR程序提交到客户端所在节点</li>
<li>YarnRunner向ResourceManager申请一个Application</li>
<li>RM将该应用程序的资源路径返回YarnRunner</li>
<li>该程序运行所需要资源提交到HDFS上</li>
<li>程序资源提交完毕后,申请运行mrAppMaster</li>
<li>RM将用户的请求初始化成一个Task</li>
<li>其中一个NodeManager领取到Task任务</li>
<li>该NodeManager创建容器Container,并产生MRAppmaster</li>
<li>Container从HDFS上拷贝资源到本地</li>
<li>MRAppMaster向RM申请运行MapTask资源</li>
<li>RM将运行MapTask任务分配给另外两个NodeManager,另两个NodeManager分别领取任务并创建容器</li>
<li>MR向两个接受到任务的NodeManager发送程序启动脚本,这两个NodeManager分别启动MapTask,MapTask对数据分区排序</li>
<li>MrAppMaster等待所有MapTask运行完毕后,向RM申请容器,运行ReduceTask</li>
<li>ReduceTask向MapTask获取相应分区的数据</li>
<li>程序运行完毕后,MR会向RM申请注销自己</li>
</ol>
<h2 id="Secondary-NameNode"><a href="#Secondary-NameNode" class="headerlink" title="Secondary NameNode"></a>Secondary NameNode</h2><p>辅助NameNode,定期合并fsimages和edits,并推送给nn;紧急情况辅助恢复NameNode</p>
<h2 id="HDFS写流程"><a href="#HDFS写流程" class="headerlink" title="HDFS写流程"></a>HDFS写流程</h2><p><img src="/images/posts/hadoop_base/img-1.png" alt="hadoop基础知识"></p>
<h1 id="📝-HA模式"><a href="#📝-HA模式" class="headerlink" title="📝 HA模式"></a>📝 HA模式</h1><p><img src="/images/posts/hadoop_base/img-2.png" alt="hadoop基础知识"></p>
<p>HA集群中包含的进程职业各不同,为了使主节点和备用节点的状态一致,采用了Quorum Journal Manager(QJM)方案解决了主备节点共享存储问题</p>
<ul>
<li>Active NameNode: 负责执行整个文件系统中命名空间的所有操作;维护着数据的元数据,包括文件名,副本数,文件的blockId以及Block块所对应的节点信息;另外还接受Client端读写请求和DataNode汇报Block信息</li>
<li>Standby NameNode: 它是Active NameNode的备用节点,一旦主节点宕机,备节点会切换成主节点对外提供服务.它主要是监听JournalNode Cluster上editlog变化,以保证当前命名空间尽可能的与主节点同步。</li>
<li>JouranlNode Cluster: 用于主备节点共享editlog日志文件的共享存储系统;它负责存储editlog日志文件,当Active Node执行了修改命名空间操作时,它会定期将执行的操作记录在editlog中,并写入journalNode Cluster中;Standby NameNode会一直监听JournalNode Cluster上editlog的变化;如果发现editlog有改动,备用主节点会读取journalNode上的editlog并于自己当前的命名空间合并,从而实现了主备节点的数据一致</li>
<li>ZKFailoverController: ZKFC以独立进程进行,每个ZKFC都监控自己负责的NameNode,它可以实现NameNode自动故障切换:即当前主节点的ZKFC则会断开与Zookeeper的连接,释放分布式锁;监控备用节点的ZKFC进程会去释放,同步把备用NameNode切换成主NameNode</li>
<li>Zookeeper: 为ZKFC进行实现自动故障转移提供统一协调服务;通过Zookeeper中Watcher监听机制,通知ZKFC异常NameNode下线;保证同一时刻只有一个主节点</li>
</ul>
<h2 id="Fsimage"><a href="#Fsimage" class="headerlink" title="Fsimage"></a>Fsimage</h2><p>让一个NameNode生成数据,让其它机器NameNode同步</p>
<h2 id="Edits"><a href="#Edits" class="headerlink" title="Edits"></a>Edits</h2><p>需要引进新的模块JournalNode来保证edits的文件数据一致性</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>大数据</category>
      </categories>
      <tags>
        <tag>大数据</tag>
        <tag>hdfs</tag>
        <tag>hadoop</tag>
        <tag>yarn</tag>
      </tags>
  </entry>
  <entry>
    <title>hive基础知识</title>
    <url>//article/hive_base/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>HDFS来存储海量数据,MapReduce来对海量数据进行分布式并行计算,yarn来实现资源管理和作业调度<br>开发人员需要编写MR来对数据进行统计分析难度极大,效率极低,并且对开发者的java功底要求<br>Hive可以帮助人员来完成这些苦活(将SQL语句转化MapReduce在yarn上跑)</p>
</blockquote>
<h1 id="📝-Hive架构模块"><a href="#📝-Hive架构模块" class="headerlink" title="📝 Hive架构模块"></a>📝 Hive架构模块</h1><p><img src="/images/posts/hive_base/img-1.png" alt="hive基础知识"></p>
<p><img src="/images/posts/hive_base/img-2.png" alt="hive基础知识"></p>
<h2 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h2><p>Cli: command-line interface</p>
<p>JDBC&#x2F;ODBC: jdbc访问hive</p>
<details><summary>Hive客户端</summary>

</details>

<h2 id="Metastore"><a href="#Metastore" class="headerlink" title="Metastore"></a>Metastore</h2><p>元数据: 表命,表所属的数据库(默认是default),表的拥有者,列&#x2F;分区字段,表的类型(是否是外部表),表的数据所在的目录</p>
<h2 id="Hadoop"><a href="#Hadoop" class="headerlink" title="Hadoop"></a>Hadoop</h2><p>使用hadoop进行存储,使用MapReduce进行计算</p>
<h2 id="驱动器"><a href="#驱动器" class="headerlink" title="驱动器"></a>驱动器</h2><ul>
<li>SQL Parse(解析器): 将SQL字符串转换成抽象语法树AST(第三方工具);对AST进行语法分析,比如表是否存在,字段是否存在,SQL语义是否错误</li>
<li>Physical Plan(编译器): 将AST编译生成逻辑执行计划</li>
<li>Query Optimizer(优化器): 对逻辑执行计划进行优化<ul>
<li>Execution(执行器): 把逻辑执行计划转换成可以运行的屋里计划,对于Hive来说,就是MR&#x2F;Spark</li>
</ul>
</li>
</ul>
<h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><details><summary>优点</summary>

</details>

<details><summary>缺点</summary>

</details>

<p>Hive 3.x版本之上是可以支持acid的</p>
<h1 id="📝-Hive数据类型"><a href="#📝-Hive数据类型" class="headerlink" title="📝 Hive数据类型"></a>📝 Hive数据类型</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>类型</td>
<td>比如</td>
<td>备注</td>
<td>存储例子</td>
</tr>
<tr>
<td>TINYINT</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>SMALINT</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>INT</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>BIGINT</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>BOOLEAN</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>FLOAT</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>DOUBLE</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>STRING</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>TIMESTAMP</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>BINARY</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>STRUCT</td>
<td>struct&lt;street:string, city:string&gt;</td>
<td>和c语言中的struct类似，都可以通过“点”符号访问元素内容</td>
<td>如果某个列的数据类型是STRUCT{first STRING, last STRING},那么第1个元素可以通过字段.first来引用</td>
</tr>
<tr>
<td>MAP</td>
<td>map&lt;string, int&gt;</td>
<td>MAP是一组键-值对元组集合，使用数组表示法可以访问数据</td>
<td>如果某个列的数据类型是MAP，其中键-&gt;值对是’first’-&gt;’John’和’last’-&gt;’Doe’，那么可以通过字段名[‘last’]获取最后一个元素</td>
</tr>
<tr>
<td>ARRAY</td>
<td>array<string></td>
<td>数组是一组具有相同类型和名称的变量的集合;这些变量称为数组的元素，每个数组元素都有一个编号，编号从零开始</td>
<td>数组值为[‘John’, ‘Doe’]，那么第2个元素可以通过数组名[1]进行引用</td>
</tr>
</tbody></table>
<h2 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h2><details><summary>隐式类型转换规则</summary>

</details>

<details><summary>CAST操作显示进行数据类型转换</summary>

</details>

<h1 id="📝-Hive语句"><a href="#📝-Hive语句" class="headerlink" title="📝 Hive语句"></a>📝 Hive语句</h1><h2 id="数据库语句"><a href="#数据库语句" class="headerlink" title="数据库语句"></a>数据库语句</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE [IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span>] database_name</span><br><span class="line">[COMMENT database_comment]</span><br><span class="line">[LOCATION hdfs_path]</span><br><span class="line">[<span class="keyword">WITH</span> DBPROPERTIES (property_name<span class="operator">=</span>property_value, ...)];</span><br></pre></td></tr></table></figure>

<p>避免要创建的数据库已经存在错误,增加if nof exists判断</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> database if <span class="keyword">not</span> <span class="keyword">exists</span> bigdata;</span><br></pre></td></tr></table></figure>

<h3 id="查询数据库的信息"><a href="#查询数据库的信息" class="headerlink" title="查询数据库的信息"></a>查询数据库的信息</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 过滤显示查询的数据库</span></span><br><span class="line"><span class="keyword">show</span> databases <span class="keyword">like</span> <span class="string">&#x27;bigdata*&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 显示数据库信息</span></span><br><span class="line"><span class="keyword">desc</span> database bigdata;</span><br></pre></td></tr></table></figure>

<h3 id="操作数据库"><a href="#操作数据库" class="headerlink" title="操作数据库"></a>操作数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- bigdata2可能不存在,加 ifx exists 进行判断</span></span><br><span class="line"><span class="keyword">drop</span> database if <span class="keyword">exists</span> bigdata2;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 如果数据库不为空,可以使用 cascade 强制删除</span></span><br><span class="line"><span class="keyword">drop</span> database bigdata cascade;</span><br></pre></td></tr></table></figure>

<h2 id="表语句"><a href="#表语句" class="headerlink" title="表语句"></a>表语句</h2><h3 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> [<span class="keyword">EXTERNAL</span>] <span class="keyword">TABLE</span> [IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span>] table_name </span><br><span class="line">[(col_name data_type [COMMENT col_comment], ...)] </span><br><span class="line">[COMMENT table_comment] </span><br><span class="line">[PARTITIONED <span class="keyword">BY</span> (col_name data_type [COMMENT col_comment], ...)] </span><br><span class="line">[CLUSTERED <span class="keyword">BY</span> (col_name, col_name, ...) </span><br><span class="line">[SORTED <span class="keyword">BY</span> (col_name [<span class="keyword">ASC</span><span class="operator">|</span><span class="keyword">DESC</span>], ...)] <span class="keyword">INTO</span> num_buckets BUCKETS] </span><br><span class="line">[<span class="type">ROW</span> FORMAT row_format] </span><br><span class="line">[STORED <span class="keyword">AS</span> file_format] </span><br><span class="line">[LOCATION hdfs_path]</span><br><span class="line">[TBLPROPERTIES (property_name<span class="operator">=</span>property_value, ...)]</span><br><span class="line">[<span class="keyword">AS</span> select_statement]</span><br><span class="line">[LIKES existing_table_or_view_name]</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改表</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> table_name RENAME <span class="keyword">TO</span> new_table_name</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 更新列</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> table_name CHANGE [<span class="keyword">COLUMN</span>] col_old_name col_new_name column_type [COMMENT col_comment] [<span class="keyword">FIRST</span><span class="operator">|</span>AFTER column_name]</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 增删除列</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> table_name <span class="keyword">ADD</span><span class="operator">|</span>REPLACE COLUMNS (col_name data_type [COMMENT col_comment], ...)</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>建表字段</td>
<td>属性意义</td>
</tr>
<tr>
<td>LOCATION</td>
<td>在建表的同时可以指定一个指向实际数据的路径</td>
</tr>
<tr>
<td>COMMENT</td>
<td>为表和列添加注释</td>
</tr>
<tr>
<td>PARTITIONED BY</td>
<td>创建分区表</td>
</tr>
<tr>
<td>CLUSTERED BY</td>
<td>创建分桶表</td>
</tr>
<tr>
<td>SORTED BY</td>
<td>对桶中的一个或多个列另外排序</td>
</tr>
<tr>
<td>STORE AS</td>
<td>指定存储文件类型</td>
</tr>
</tbody></table>
<h3 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h3><p>清除表中数据（Truncate） : Truncate只能删除管理表，不能删除外部表中数据</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">drop</span> <span class="keyword">table</span> test2;</span><br></pre></td></tr></table></figure>

<h2 id="DML语句"><a href="#DML语句" class="headerlink" title="DML语句"></a>DML语句</h2><h3 id="load语句"><a href="#load语句" class="headerlink" title="load语句"></a>load语句</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">load data [<span class="keyword">local</span>] inpath <span class="string">&#x27;数据的path&#x27;</span> [overwrite] <span class="keyword">into</span> <span class="keyword">table</span> table_name [<span class="keyword">partition</span> (partcol1<span class="operator">=</span>val1,…)];</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建表时,通过Location指定加载数据路径</span></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">external</span> <span class="keyword">table</span> if <span class="keyword">not</span> <span class="keyword">exists</span> student5(</span><br><span class="line">              id <span class="type">int</span>,</span><br><span class="line">              name string</span><br><span class="line">)</span><br><span class="line"><span class="type">row</span> format delimited fields terminated <span class="keyword">by</span> <span class="string">&#x27;\t&#x27;</span></span><br><span class="line">location <span class="string">&#x27;/input/student&#x27;</span>;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>属性</td>
<td>属性含义</td>
</tr>
<tr>
<td>load data</td>
<td>加载数据</td>
</tr>
<tr>
<td>local</td>
<td>表示从本地加载数据到hive表，否则是从HDFS加载数据到Hive表</td>
</tr>
<tr>
<td>Inpath</td>
<td>加载数据的路径</td>
</tr>
<tr>
<td>Overwrite</td>
<td>表示覆盖表中已有数据,否则表示追加</td>
</tr>
<tr>
<td>Into table</td>
<td>加载数据到那张表中</td>
</tr>
<tr>
<td>Partition</td>
<td>加载数据到指定的分区</td>
</tr>
</tbody></table>
<h2 id="分区表"><a href="#分区表" class="headerlink" title="分区表"></a>分区表</h2><h3 id="分区表-1"><a href="#分区表-1" class="headerlink" title="分区表"></a>分区表</h3><ul>
<li>分区表实际上就是对应一个HDFS文件系统上的独立的文件夹</li>
<li>该文件夹下是该分区所有的数据文件</li>
<li>Hive中的分区就是分目录，把一个大的数据集根据业务需要分割成小的数据集</li>
<li>在查询时通过WHERE子句中的表达式选择查询所需要的指定的分区，这样的查询效率会提高很多</li>
</ul>
<h3 id="分区操作"><a href="#分区操作" class="headerlink" title="分区操作"></a>分区操作</h3><p>创建表指定分区字段 : 在查询时通过WHERE子句中的表达式选择查询所需要的指定的分区，这样的查询效率会提高很多</p>
<p>增加分区 : alter table dept_partition add partition(day&#x3D;’20200404’) </p>
<p>删除分区 : alter table dept_partition drop partition (day&#x3D;’20200406’)</p>
<h3 id="二级分区"><a href="#二级分区" class="headerlink" title="二级分区"></a>二级分区</h3><p>创建表指定分区字段 : partitioned by (day string, hour string)</p>
<p>hdfs存储的路径 : &#x2F;user&#x2F;hive&#x2F;warehouse&#x2F;mydb.db&#x2F;dept_partition2&#x2F;day&#x3D;20200401&#x2F;hour&#x3D;14;</p>
<h3 id="动态分区"><a href="#动态分区" class="headerlink" title="动态分区"></a>动态分区</h3><p>对分区表insert数据时候,数据库自动会根据分区字段的值,将数据插入到相应的分区中</p>
<p>设置开启动态分区 : set hive.exec.dynamic.partition&#x3D;true;</p>
<p>设置为非严格模式 : set hive.exec.dynamic.partition.mode&#x3D;nonstrict</p>
<p>整个MR Job中,最大可以创建多少个HDFS文件,默认是100000; set hive.exec.max.created.files&#x3D;100000;</p>
<p>在每个执行MR的节点上,最大可以创建多少个动态分区: set hive.exec.max.dynamic.partitions.pernode&#x3D;100;</p>
<p>在所有执行MR的节点上,最大一共可以创建多少个动态分区,默认是1000; set hive.exec.max.dynamic.partitions&#x3D;1000;</p>
<p>动态分区的模式,默认strict,表示必须指定至少一个分区为静态分区,nonstrict模式表示许所有的分区字段都可以使用动态分区</p>
<p>当有空分区生成时,是否抛出异常;一般不需要设置,默认false; set hive.error.on.empty.partition&#x3D;false;</p>
<h2 id="分桶表"><a href="#分桶表" class="headerlink" title="分桶表"></a>分桶表</h2><p>分桶是将数据集分解成更容易管理的若干部分的另一种技术</p>
<p>分区针对的是数据存储路径(细分文件夹);分桶针对的是数据文件(按规则多文件放一起)</p>
<p>创建分桶表</p>
<p>要想将表创建为4个桶,需要将hive中mapreduce.job.reduces参数设置为 ≥ 4或设置为 -1</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create table</span> stu_bucket(id <span class="type">int</span>, name string)</span><br><span class="line">clustered <span class="keyword">by</span>(id) </span><br><span class="line"><span class="keyword">into</span> <span class="number">4</span> buckets</span><br><span class="line"><span class="type">row</span> format delimited fields terminated <span class="keyword">by</span> <span class="string">&#x27;\t&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>分桶规则: Hive的分桶采用对分桶字段的值进行哈希(hash),然后除以桶的个数求余的方 式决定该条记录存放在哪个桶当中</p>
<p>mapreduce.job.reduces&#x3D;-1,让Job自行决定需要用多少个reduce或者将reduce的个数设置为大于等于分桶表的桶数</p>
<h1 id="📝-Hive函数"><a href="#📝-Hive函数" class="headerlink" title="📝 Hive函数"></a>📝 Hive函数</h1><h2 id="内置函数"><a href="#内置函数" class="headerlink" title="内置函数"></a>内置函数</h2><p>show function: 查询系统自带的函数</p>
<p>desc function 函数名: 显示自带的函数的用法</p>
<p>desc function extended 函数名: 展开详细说明</p>
<table>
<thead>
<tr>
<th>作用</th>
<th>函数</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>空字段赋值-NVL（防止空字段参与计算）</td>
<td>NVL(value,default_value)</td>
<td>如果value为NULL，则NVL函数返回default_value的值，否则返回value的值<br>如果两个参数都为NULL ，则返回NULL</td>
</tr>
<tr>
<td>CASE WHEN THEN ELSE END</td>
<td>CASE a WHEN b THEN c [WHEN d THEN e]* [ELSE f] END</td>
<td>根据不同的数据,返回不同的值<br>当a&#x3D;b时，返回c；当a&#x3D;d时，返回d；当a&#x3D;e时，放回e；其他情况返回f</td>
</tr>
<tr>
<td>行转列（组函数）</td>
<td>CONCAT(string A&#x2F;col, string B&#x2F;col…)</td>
<td>返回输入字符串连接后的结果，支持任意个输入字符串</td>
</tr>
<tr>
<td>行转列（组函数）</td>
<td>CONCAT_WS(separator, str1, str2,…)</td>
<td>指定字符separator，连接str<br>sparator: 分割符<br>分割符将被加到被连接的字符串之间</td>
</tr>
<tr>
<td>行转列（组函数）</td>
<td>COLLECT_SET(col)</td>
<td>函数只接受基本数据类型;<br>它的主要作用是将某字段的值进行去重汇总,产生array类型字段</td>
</tr>
<tr>
<td>行转列（组函数）</td>
<td>COLLECT_LIST(col)</td>
<td>函数值接受基本数据类型;<br>它的主要作用是将某字段的值进行不去重汇总,产生array类型字段</td>
</tr>
<tr>
<td>列转行</td>
<td>EXPLODE(col)</td>
<td>将hive表的一列中复杂的array或者map结构拆分成多行;<br>会出现改行与其它字段行数不匹配报错<br>因此<br>必须和lateral view连用</td>
</tr>
<tr>
<td>列转行</td>
<td>SPLIT(string str, string regex)</td>
<td>按照regex字符串分割str，会返回分割后的字符串数组</td>
</tr>
<tr>
<td>列转行</td>
<td>LATERAL VIEW</td>
<td>用于和split,explode等UDF一起使用,它能够将一列数据拆成多行数据,在次基础上可以对拆分后的数据进行聚合<br>lateral view首先为原始表的每行调用UDTF,UDTF会报一行拆分成一行或者多行,lateral view再把结果组合,产生一个支持别名表的虚拟表</td>
</tr>
</tbody></table>
<h2 id="窗口函数（开窗函数）"><a href="#窗口函数（开窗函数）" class="headerlink" title="窗口函数（开窗函数）"></a>窗口函数（开窗函数）</h2><p>为每行数据进行一次计算,返回一个值</p>
<p>灵活运用窗口函数可以解决很多复杂的问题,如去重,排名,同步以及环比,连续登录等</p>
<details><summary>语法</summary>

</details>

<details><summary>聚合函数</summary>

</details>

<details><summary>排序函数</summary>

</details>

<details><summary>统计比较函数</summary>

</details>

<h2 id="自定义函数"><a href="#自定义函数" class="headerlink" title="自定义函数"></a>自定义函数</h2><ul>
<li>UDF(User-Defined-Function) : 一进一出</li>
<li>UDAF(User-Defined-Aggregation Function): 聚合函数,多进一出,类似:max,count,min</li>
<li>UDTF(User-Defined Table-Generating Functions): 炸裂函数,一进多出</li>
</ul>
<p>引入过程:</p>
<ol>
<li>添加jar: add jar linux_jar_path</li>
<li>创建function: function [dbname.]function_name AS class_name;</li>
<li>在hive的命令行窗口删除函数: drop [temporary] function [if exists] [dbname.]function_name;</li>
</ol>
<h2 id="创建临时函数"><a href="#创建临时函数" class="headerlink" title="创建临时函数"></a>创建临时函数</h2><p>create temporary function my_len as “com.atguigu.hive. MyStringLength”;</p>
<p>临时函数只和会话有关系,跟库没有关系,只要创建临时函数的会话不断,再当前会话下,任意一个库都可以使用,其它会话全部不能使用</p>
<h1 id="📝-Hive优化"><a href="#📝-Hive优化" class="headerlink" title="📝 Hive优化"></a>📝 Hive优化</h1><h2 id="执行计划"><a href="#执行计划" class="headerlink" title="执行计划"></a>执行计划</h2><p>Explain : EXPLAIN [EXTENDED | DEPENDENCY | AUTHORIZATION] query</p>
<p>查看执行语句运行时的信息&#x2F;详细信息</p>
<h2 id="Hive建表优化"><a href="#Hive建表优化" class="headerlink" title="Hive建表优化"></a>Hive建表优化</h2><p>分区表: 先用where过滤缩小查询范围,减小数据量</p>
<p>分桶表: 提供一个隔离数据和优化查询的便利方式</p>
<p>合格的文件格式: 采用压缩: 输入端&#x2F;map输出端&#x2F;reduce输出端</p>
<h2 id="HQl语法优化"><a href="#HQl语法优化" class="headerlink" title="HQl语法优化"></a>HQl语法优化</h2><details><summary>列裁剪和分区裁剪</summary>

</details>

<details><summary>group by</summary>

</details>

<details><summary>CBO优化</summary>

</details>

<details><summary>谓词下推</summary>

</details>

<details><summary>MapJoin</summary>

</details>

<details><summary>大表,小表SMB JOIN</summary>

</details>

<details><summary>数据倾斜</summary>

</details>

<details><summary>表单数据倾斜</summary>

</details>

<details><summary>join数据倾斜优化</summary>

</details>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>大数据</category>
      </categories>
      <tags>
        <tag>大数据</tag>
        <tag>hive</tag>
      </tags>
  </entry>
  <entry>
    <title>插件推荐</title>
    <url>//article/plugs_records_info/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>记录各种软件好用的插件</p>
</blockquote>
<h1 id="📝-Google浏览器插件"><a href="#📝-Google浏览器插件" class="headerlink" title="📝 Google浏览器插件"></a>📝 Google浏览器插件</h1><h2 id="沉浸式翻译"><a href="#沉浸式翻译" class="headerlink" title="沉浸式翻译"></a>沉浸式翻译</h2><p>这里我已 Spring 官网为例，它会翻译每段，效果还是很好的</p>
<p><img src="/images/posts/plugs_records_info/img-1.png" alt="插件推荐"></p>
<h2 id="Sexy-Undo-Close-Tab"><a href="#Sexy-Undo-Close-Tab" class="headerlink" title="Sexy Undo Close Tab"></a>Sexy Undo Close Tab</h2><p>记录你刚刚关闭的网页，不小心关错了，再也不用去历史记录中进行查找</p>
<p><img src="/images/posts/plugs_records_info/img-2.png" alt="插件推荐"></p>
<h2 id="SuperCopy-超级复制"><a href="#SuperCopy-超级复制" class="headerlink" title="SuperCopy 超级复制"></a>SuperCopy 超级复制</h2><p>一键破解禁止右键、破解禁止选择、破解禁止复制、破解禁止粘贴，启用复制，启用右键，启用选择，启用粘贴</p>
<h2 id="Chrono下载管理器"><a href="#Chrono下载管理器" class="headerlink" title="Chrono下载管理器"></a>Chrono下载管理器</h2><p>比如我们有时候找一个网站的静态资源，就可以直接用这个插件的 资源嗅探 功能</p>
<p><img src="/images/posts/plugs_records_info/img-3.png" alt="插件推荐"></p>
<h2 id="Octotree-GitHub-code-tree"><a href="#Octotree-GitHub-code-tree" class="headerlink" title="Octotree - GitHub code tree"></a>Octotree - GitHub code tree</h2><p>当我们访问 github 项目的时候，就不需要挨个点进去看，通过旁边的 ctotree 就可以看到项目的层级等</p>
<p><img src="/images/posts/plugs_records_info/img-4.png" alt="插件推荐"></p>
<h2 id="微信公众号同步助手"><a href="#微信公众号同步助手" class="headerlink" title="微信公众号同步助手"></a>微信公众号同步助手</h2><p>微信公众号文章同步助手，支持头条号、简书、知乎、WordPress</p>
<h2 id="WebChatGPT"><a href="#WebChatGPT" class="headerlink" title="WebChatGPT"></a>WebChatGPT</h2><h1 id="📝-Notion插件"><a href="#📝-Notion插件" class="headerlink" title="📝 Notion插件"></a>📝 Notion插件</h1><h2 id="Notion流程图插件"><a href="#Notion流程图插件" class="headerlink" title="Notion流程图插件"></a>Notion流程图插件</h2><ol>
<li>Diagrams.net：这个插件是Notion的官方合作伙伴插件，它提供了丰富的绘图工具，包括流程图、组织结构图、UML图等。您可以使用它来在Notion中创建专业的流程图。</li>
<li>Excalidraw：这个插件允许您在Notion中创建手绘风格的流程图和草图。它提供了简单易用的绘图工具，可以让您快速绘制流程图并进行协作。</li>
<li>Miro：Miro是一个在线协作白板工具，它提供了丰富的绘图功能，包括流程图、思维导图、用户旅程图等。您可以在Notion中使用Miro插件，将Miro的流程图嵌入到您的Notion页面中。</li>
</ol>
<h1 id="📝-IDEA插件"><a href="#📝-IDEA插件" class="headerlink" title="📝 IDEA插件"></a>📝 IDEA插件</h1><ul>
<li>Bito</li>
<li>jclasslib Bytecode Viewer</li>
<li>Maven Helper</li>
<li>CodeGlance Pro</li>
<li>Lombok Builder Helper</li>
<li>MyBatisX</li>
<li>SonarLint</li>
</ul>
<h1 id="google浏览器前端插件"><a href="#google浏览器前端插件" class="headerlink" title="google浏览器前端插件"></a>google浏览器前端插件</h1><h2 id="React-Developer-Tools"><a href="#React-Developer-Tools" class="headerlink" title="React Developer Tools"></a>React Developer Tools</h2><h2 id="Vue-js-devtools"><a href="#Vue-js-devtools" class="headerlink" title="Vue.js devtools"></a>Vue.js devtools</h2><h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>插件</category>
      </categories>
      <tags>
        <tag>工具</tag>
        <tag>插件</tag>
      </tags>
  </entry>
  <entry>
    <title>SonarQube安装使用手册(docker版)</title>
    <url>//article/sonarqube_docker/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>SonarQube代码质量检测的工具，对代码进行一定规则的校验等等，算是代码层面的质量和规范                                                             基于docker来进行安装                                                              </p>
</blockquote>
<h1 id="📝-基于docker安装"><a href="#📝-基于docker安装" class="headerlink" title="📝 基于docker安装"></a>📝 基于docker安装</h1><h2 id="组件版本"><a href="#组件版本" class="headerlink" title="组件版本"></a>组件版本</h2><ul>
<li>MySql： 5.7</li>
<li>SonarQube: 7.8-community</li>
</ul>
<h2 id="启动过程"><a href="#启动过程" class="headerlink" title="启动过程"></a>启动过程</h2><h3 id="MySql初始化"><a href="#MySql初始化" class="headerlink" title="MySql初始化"></a>MySql初始化</h3><p>创建一个sonar的数据库(编码最好是utf-8,避免中文相关的乱码问题)</p>
<p>名字也可以自己来取</p>
<h3 id="docker拉镜像配置"><a href="#docker拉镜像配置" class="headerlink" title="docker拉镜像配置"></a>docker拉镜像配置</h3><p>如果你拉取镜像存在问题的话,配置一下加速的地址来拉取,避免一直卡顿等问题</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;registry-mirrors&quot;</span>: [</span><br><span class="line">  <span class="string">&quot;https://0dj0t5fb.mirror.aliyuncs.com&quot;</span>,</span><br><span class="line">  <span class="string">&quot;https://docker.mirrors.ustc.edu.cn&quot;</span>,</span><br><span class="line">  <span class="string">&quot;https://6kx4zyno.mirror.aliyuncs.com&quot;</span>,</span><br><span class="line">  <span class="string">&quot;https://registry.docker-cn.com&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<h3 id="docker启动指令"><a href="#docker启动指令" class="headerlink" title="docker启动指令"></a>docker启动指令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> docker run -itd --name  sonarqube78 -p 10002:9000 \\n  -e SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=<span class="literal">true</span> \\n  -e SONARQUBE_JDBC_USERNAME=root \\n  -e <span class="string">&#x27;SONARQUBE_JDBC_PASSWORD=passwd&#x27;</span> \\n  -e <span class="string">&#x27;SONARQUBE_JDBC_URL=jdbc:mysql://ip:port/database?useUnicode=true&amp;characterEncoding=utf8&#x27;</span> \\n  -p 9000:9000 sonarqube:7.8-community</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>需要替换的参数</p>
<ul>
<li>SONARQUBE_JDBC_USERNAME： 替换为自己数据库的名字</li>
<li>SONARQUBE_JDBC_PASSWORD:  替换为自己数据库的密码</li>
<li>SONARQUBE_JDBC_URL:    MySql的连接ip，port，database都替换为自己的</li>
</ul>
<p>9000:9000： 前面的宿主机的端口映射，可以根据自己的需求来进行配置</p>
<h2 id="启动成功"><a href="#启动成功" class="headerlink" title="启动成功"></a>启动成功</h2><p>启动成功的初始化的账号和密码: admin&#x2F;admin</p>
<p>同时也可以看到Mysql的数据库中初始化了许多表相关的信息</p>
<p><img src="/images/posts/sonarqube_docker/img-1.png" alt="SonarQube安装使用手册(docker版)"></p>
<h2 id="本地使用层面"><a href="#本地使用层面" class="headerlink" title="本地使用层面"></a>本地使用层面</h2><h3 id="sonarQube界面key创建"><a href="#sonarQube界面key创建" class="headerlink" title="sonarQube界面key创建"></a>sonarQube界面key创建</h3><p>sonar创建一个项目，同时也会有一个key的信息;</p>
<p>比如我这里创建一个项目，对你分析的token也是可以一起生成的</p>
<p><img src="/images/posts/sonarqube_docker/img-2.png" alt="SonarQube安装使用手册(docker版)"></p>
<h3 id="项目本地maven配置"><a href="#项目本地maven配置" class="headerlink" title="项目本地maven配置"></a>项目本地maven配置</h3><p>在本地的setting文件,加上sonarqube访问的地址信息</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">proxies</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">profile</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>sonar<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">activation</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">activeByDefault</span>&gt;</span>true<span class="tag">&lt;/<span class="name">activeByDefault</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">activation</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">                <span class="comment">&lt;!-- 配置 Sonar Host地址，默认：http://localhost:9000 --&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">sonar.host.url</span>&gt;</span></span><br><span class="line">                  http://127.0.0.1:10002</span><br><span class="line">                <span class="tag">&lt;/<span class="name">sonar.host.url</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">profile</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">proxies</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="分析指令"><a href="#分析指令" class="headerlink" title="分析指令"></a>分析指令</h3><p>对项目进行mvn sonar分析，但是在分析之前，项目需要进行打包或者build成功才可以，访问会有一个访问不到的错误</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn sonar:sonar -Dsonar.projectKey=user-platfor -Dsonar.host.url=http://127.0.0.1:10002 -Dsonar.login=bff12399cf34352faa90cf0c062b64cebe718ad8</span><br></pre></td></tr></table></figure>

<h3 id="查看结果"><a href="#查看结果" class="headerlink" title="查看结果"></a>查看结果</h3><p>查看整个项目的分析结果</p>
<p><img src="/images/posts/sonarqube_docker/img-3.png" alt="SonarQube安装使用手册(docker版)"></p>
<p>点进去可以看到具体的错误原因和代码的问题</p>
<p><img src="/images/posts/sonarqube_docker/img-4.png" alt="SonarQube安装使用手册(docker版)"></p>
<p>到此一个基本的流程结束，同时我们也可以通过配置jenkis的流水线或者gitlab ci&#x2F;cd等钩子函数来对sonarqube进行一个触发</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>插件</category>
      </categories>
      <tags>
        <tag>工具</tag>
        <tag>插件</tag>
        <tag>开发</tag>
        <tag>SonarQube</tag>
      </tags>
  </entry>
  <entry>
    <title>开源数据平台_技术篇章</title>
    <url>//article/data_middle_tech/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>计划一个开源的数据平台; 参考许多数据中台的公司产品 以及 开源项目，进行一个整体的总结;可以大致归纳到如下一些点</p>
<ul>
<li>离线</li>
<li>实时</li>
<li>算法</li>
<li>数据服务开发</li>
<li>数据质量检查</li>
<li>数据安全</li>
<li>数据资产</li>
<li>调度任务</li>
<li>环境模块</li>
</ul>
</blockquote>
<h1 id="📝系统配置"><a href="#📝系统配置" class="headerlink" title="📝系统配置"></a>📝系统配置</h1><p>最大力度以项目为隔离,来进行隔离 资源&#x2F;用户&#x2F;环境 等信息</p>
<p>进行如下的内容配置:</p>
<ul>
<li>部门  &amp;  租户</li>
<li>用户  &amp;  角色</li>
<li>项目   ——&gt;   项目资源(比如yarn,hdfs存储资源,数据源,集群资源)</li>
<li>业务流程(每个项目下有多个业务流程)</li>
<li>项目配置(比如这个项目的默认参数等)</li>
</ul>
<h1 id="📝离线开发"><a href="#📝离线开发" class="headerlink" title="📝离线开发"></a>📝离线开发</h1><h2 id="开发系统设置"><a href="#开发系统设置" class="headerlink" title="开发系统设置"></a>开发系统设置</h2><ul>
<li>数据交换 和 数据集成 ： 数据的同步，数据库的同步，csv&#x2F;execl同步,非结构化数据同步</li>
<li>数据开发 <ol>
<li>传统数据库 ： MySql &#x2F; GaussDB &#x2F; Oracle &#x2F; PostgreSQL &#x2F; SqlServer &#x2F; DB2 等数据库</li>
<li>大数据组件： Spark(Sparl&#x2F;SparkSql&#x2F;pySpark) &#x2F; Hive &#x2F; HBase 等</li>
<li>语言层面: python&#x2F;shell&#x2F;perl</li>
<li>建表的DDL</li>
<li>资源文件 : 不同文件格式的类型</li>
</ol>
</li>
</ul>
<h2 id="表信息"><a href="#表信息" class="headerlink" title="表信息"></a>表信息</h2><p>主要分为数据源类型,以及每种数据源类型的对应的数据库(也就是在项目配置中进行配置了的)</p>
<p>不同的数据库类型,比如下面的:</p>
<p>hive&#x2F;mysql&#x2F;oracle等</p>
<h2 id="运行记录"><a href="#运行记录" class="headerlink" title="运行记录"></a>运行记录</h2><p>对执行的信息进行记录</p>
<h2 id="资源文件"><a href="#资源文件" class="headerlink" title="资源文件"></a>资源文件</h2><p>对资源文件进行上传,保存以及展示等信息处理(主要是上传到hdfs上)</p>
<h2 id="自定义函数开发"><a href="#自定义函数开发" class="headerlink" title="自定义函数开发"></a>自定义函数开发</h2><ul>
<li>Hive函数</li>
<li>其它函数</li>
</ul>
<h2 id="离线作业"><a href="#离线作业" class="headerlink" title="离线作业"></a>离线作业</h2><h3 id="新建业务流程-可以简单的理解为一个工作流"><a href="#新建业务流程-可以简单的理解为一个工作流" class="headerlink" title="新建业务流程(可以简单的理解为一个工作流)"></a>新建业务流程(可以简单的理解为一个工作流)</h3><p>比如 大屏A , 大屏B , 大屏 C 都可以分别建设一条工作流</p>
<h3 id="新建离线作业"><a href="#新建离线作业" class="headerlink" title="新建离线作业"></a>新建离线作业</h3><ul>
<li><p>数据同步,抽取数据</p>
<ol>
<li>数据源配置:   源头表信息  ——&gt;  目的表信息</li>
<li>字段映射关系</li>
<li>参数控制</li>
</ol>
<p>属性配置:</p>
<ul>
<li>使用的资源组</li>
<li>运行参数</li>
<li>本节点输入参数</li>
<li>本节点输出参数</li>
<li>依赖资源</li>
</ul>
<p>调度配置:</p>
<ul>
<li>生成实体的方法</li>
<li>生效日期</li>
<li>定时corn表达式(调度周期,根据天&#x2F;星期&#x2F;月等进行配置)</li>
<li>调度优先级别</li>
<li>调度依赖: 添加上下游节点 或者 将自身重置为根节点</li>
<li>调度作用的范围: 当前业务流程 &#x2F; 当前项目 &#x2F; 当前租户</li>
</ul>
</li>
<li><p>脚本开发: 针对不同的数据库进行不同的脚本(还可以选择数据库);不同类型</p>
<ol>
<li>Spark : 提交作业的方式,是否数据湖的format文件格式等,使用的版本,spark的参数; 调度的配置依赖上下游的作业</li>
<li>Hive : 无需多的选择,默认的hive设置即可</li>
<li>MySql: 数据源类型的选择(假设我配置了多个数据源,是可以进行选择执行和提交;提交版本和发布)</li>
<li>hbase:  无需多的配置; 本节点输入参数,本节点输出参数</li>
<li>传统数据库: 支持多个数据源的,都是可以进行选择的,然后进行脚本的编写</li>
<li>常规语言开发: 对常规的语言进行开发设置(python,shell)</li>
</ol>
</li>
<li><p>建表作业: 不同数据库类型的建表脚本进行开发(不参与工作流进行发布)</p>
</li>
</ul>
<h1 id="📝实时开发"><a href="#📝实时开发" class="headerlink" title="📝实时开发"></a>📝实时开发</h1><h3 id="系统属性设置"><a href="#系统属性设置" class="headerlink" title="系统属性设置"></a>系统属性设置</h3><p>开发系统属性设置:</p>
<ul>
<li>数据集成,比如 Flink CDC 等同步</li>
</ul>
<p>实时计算</p>
<ul>
<li>FlinkSQL</li>
<li>Flink</li>
<li>Flink On 数据湖</li>
</ul>
<p>建表语句</p>
<ul>
<li>Flink DDL</li>
<li>Flink On 数据湖 DDL</li>
</ul>
<p>资源文件类型</p>
<ul>
<li>jar</li>
<li>py</li>
<li>txt</li>
<li>json</li>
<li>xml</li>
<li>等等</li>
</ul>
<h3 id="实时运行的历史"><a href="#实时运行的历史" class="headerlink" title="实时运行的历史"></a>实时运行的历史</h3><p>对实时运行的历史进行一个记录</p>
<h3 id="实时表的管理"><a href="#实时表的管理" class="headerlink" title="实时表的管理"></a>实时表的管理</h3><p>对实时的表进行获取和管理</p>
<h3 id="函数管理"><a href="#函数管理" class="headerlink" title="函数管理"></a>函数管理</h3><ul>
<li>系统函数</li>
<li>自定义函数</li>
</ul>
<h3 id="实时任务开发"><a href="#实时任务开发" class="headerlink" title="实时任务开发"></a>实时任务开发</h3><p>系统参数</p>
<ul>
<li>checkpoint 是否开启</li>
<li>checkpoint的模式是什么配置</li>
<li>checkpoint的超时设置</li>
<li>checkpoint的路径设置</li>
<li>checkpoint保留的个数</li>
<li>日志配置</li>
</ul>
<h1 id="📝服务开发"><a href="#📝服务开发" class="headerlink" title="📝服务开发"></a>📝服务开发</h1><h2 id="自定义函数开发-1"><a href="#自定义函数开发-1" class="headerlink" title="自定义函数开发"></a>自定义函数开发</h2><p>对应一些便捷的函数等进行设置自定义开发</p>
<h2 id="自定义SQL的API开发"><a href="#自定义SQL的API开发" class="headerlink" title="自定义SQL的API开发"></a>自定义SQL的API开发</h2><ul>
<li>对应业务流程的选择,也是将什么样的业务给暴露给外侧</li>
<li>API的名称和请求路径</li>
<li>请求方式 和 返回类型的配置</li>
<li>调用的频率和超时时间的设置</li>
</ul>
<p>环境的选择:</p>
<ul>
<li>数据源类型(比如Mysql,Oracle等;关系型或者mpp型居多,如果是hive或者是spark的话，不是特别建议)</li>
<li>然后选择对应的 数据源 下面的数据库 ， 来进行脚本的开发，将脚本执行的结果拿到返回数据给返回回去</li>
<li>API常见的一些分页设置等</li>
</ul>
<h1 id="📝运维中心"><a href="#📝运维中心" class="headerlink" title="📝运维中心"></a>📝运维中心</h1><ul>
<li>对我们的DDL进行统一运维处理</li>
<li>对我们的离线和实时,统一的任务调度和处理</li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据中台</category>
      </categories>
      <tags>
        <tag>源码分析</tag>
        <tag>Iceberg</tag>
        <tag>Spark</tag>
        <tag>使用入门</tag>
        <tag>数据开发</tag>
        <tag>数据中台</tag>
      </tags>
  </entry>
  <entry>
    <title>数据中台(技术篇章)</title>
    <url>//article/data_middle_work_tech1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>数据中台技术记录</p>
</blockquote>
<h1 id="📝-数据底座"><a href="#📝-数据底座" class="headerlink" title="📝 数据底座"></a>📝 数据底座</h1><ul>
<li>CDH闭源前的最后一个版本</li>
<li>CDP</li>
<li>USDP</li>
<li>DataSophon</li>
</ul>
<h1 id="📝-数据交换-数据集成"><a href="#📝-数据交换-数据集成" class="headerlink" title="📝 数据交换(数据集成)"></a>📝 数据交换(数据集成)</h1><p>从各种数据源采集结构化和非结构化数据，包括实时数据采集和批量数据采集</p>
<p>将来自不同系统和不同格式的数据进行整合，实现一个统一个的数据视图</p>
<h2 id="数据交换"><a href="#数据交换" class="headerlink" title="数据交换"></a>数据交换</h2><ul>
<li>API: 采集第三方的API的数据</li>
<li>日志采集： 采集应用日志</li>
<li>爬虫:  采集网页数据</li>
<li>数据源抽取:  直接通过数据源进行抽取</li>
</ul>
<h2 id="数据集成"><a href="#数据集成" class="headerlink" title="数据集成"></a>数据集成</h2><h3 id="数据提取-Data-Extraction"><a href="#数据提取-Data-Extraction" class="headerlink" title="数据提取(Data Extraction)"></a>数据提取(Data Extraction)</h3><h3 id="数据清洗（Data-Cleaning"><a href="#数据清洗（Data-Cleaning" class="headerlink" title="数据清洗（Data Cleaning)"></a>数据清洗（Data Cleaning)</h3><h3 id="数据转换（Data-Transformation）"><a href="#数据转换（Data-Transformation）" class="headerlink" title="数据转换（Data Transformation）"></a>数据转换（Data Transformation）</h3><h3 id="数据加载（Data-Loading）"><a href="#数据加载（Data-Loading）" class="headerlink" title="数据加载（Data Loading）"></a>数据加载（Data Loading）</h3><h1 id="数据清洗"><a href="#数据清洗" class="headerlink" title="数据清洗"></a>数据清洗</h1><p>对采集的数据进行过滤、修正、归一化等,提高数据质量,修复脏数据。</p>
<h1 id="📝-数据开发"><a href="#📝-数据开发" class="headerlink" title="📝 数据开发"></a>📝 数据开发</h1><h2 id="离线开发"><a href="#离线开发" class="headerlink" title="离线开发"></a>离线开发</h2><ul>
<li>Spark</li>
<li>Hive</li>
<li>Flink</li>
</ul>
<h2 id="实时开发"><a href="#实时开发" class="headerlink" title="实时开发"></a>实时开发</h2><ul>
<li>Kafka</li>
<li>Spark Streaming</li>
<li>Flink</li>
</ul>
<h2 id="算法开发"><a href="#算法开发" class="headerlink" title="算法开发"></a>算法开发</h2><p>暂不支持</p>
<h1 id="📝-任务调度"><a href="#📝-任务调度" class="headerlink" title="📝 任务调度"></a>📝 任务调度</h1><ul>
<li>Azkaban</li>
<li>Airflow</li>
<li>海滩调度器</li>
</ul>
<h1 id="📝-数据可视化"><a href="#📝-数据可视化" class="headerlink" title="📝 数据可视化"></a>📝 数据可视化</h1><p>通告报表,dashboard等对数据和洞见进行直观的展示;帮助用户理解和决策</p>
<ul>
<li>Apache Superset:开源的数据可视化和探索工具</li>
<li>Grafana:开源的数据监测和可视化工具,用于构建监控面板和报表</li>
<li>Tableau:商业数据可视化工具,用于数据分析和报表制作</li>
</ul>
<h1 id="数据存储"><a href="#数据存储" class="headerlink" title="数据存储"></a>数据存储</h1><p>数据湖,HDFS,数据库等进行存储</p>
<ul>
<li>Apache Hive:数据仓库,基于HDFS存储</li>
<li>Apache HBase:NoSQL数据库,基于HDFS存储</li>
<li>Apache Kudu:列式存储,基于HDFS存储</li>
<li>Elasticsearch:搜索引擎,文档型数据库</li>
</ul>
<h1 id="数据治理"><a href="#数据治理" class="headerlink" title="数据治理"></a>数据治理</h1><p>管理数据中台的用户、权限、流程等,确保数据中台的高效和稳定运行</p>
<h1 id="元数据管理"><a href="#元数据管理" class="headerlink" title="元数据管理"></a>元数据管理</h1><ul>
<li>Apache Atlas:统一元数据管理平台</li>
<li>datahub[[嵌入对象]]</li>
</ul>
<h1 id="数据管理和数据权限"><a href="#数据管理和数据权限" class="headerlink" title="数据管理和数据权限"></a>数据管理和数据权限</h1><ul>
<li>Apache Atlas:数据治理平台</li>
<li>Apache Ranger:实现权限管理</li>
</ul>
<h1 id="数据应用"><a href="#数据应用" class="headerlink" title="数据应用"></a>数据应用</h1><p>通过API、数据服务等形式,将数据和分析能力提供给各业务系统使用</p>
<h1 id="开源项目记录"><a href="#开源项目记录" class="headerlink" title="开源项目记录"></a>开源项目记录</h1><ul>
<li>[<a href="https://github.com/DTStack">https://github.com/DTStack</a>]</li>
<li><a href="https://github.com/DTStack/chunjun">https://github.com/DTStack/chunjun</a></li>
<li><a href="https://github.com/DTStack/Taier">https://github.com/DTStack/Taier</a></li>
<li><a href="https://github.com/DataLinkDC/dinky">https://github.com/DataLinkDC/dinky</a></li>
<li><a href="https://github.com/apache/incubator-streampark">https://github.com/apache/incubator-streampark</a></li>
<li><a href="https://github.com/apache/dolphinscheduler">https://github.com/apache/dolphinscheduler</a></li>
<li><a href="https://github.com/ververica/flink-cdc-connectors">https://github.com/ververica/flink-cdc-connectors</a></li>
<li><a href="https://github.com/alldatacenter/alldata">https://github.com/alldatacenter/alldata</a></li>
<li><a href="https://github.com/WeBankFinTech/WeDataSphere">https://github.com/WeBankFinTech/WeDataSphere</a></li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据中台</category>
      </categories>
      <tags>
        <tag>思考</tag>
        <tag>工具</tag>
        <tag>数据中台</tag>
      </tags>
  </entry>
  <entry>
    <title>MySql日志</title>
    <url>//article/mysql_log/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-MySql日志"><a href="#📝-MySql日志" class="headerlink" title="📝 MySql日志"></a>📝 MySql日志</h1><p>分为 undo log(回滚日志), redo log(重做日志),binlog(归档日志)</p>
<ul>
<li>undo log(回滚日志)：innodb存储引擎层生成的日志，实现了事务中的原子性，主要用于事务回滚和MVCC</li>
<li>redo log(重做日志)：innodb存储引擎层生成的日志，实现了事务中的持久性，主要用于掉电等故障恢复</li>
<li>binlog(归档日志)：是Server层生成的日志，主要用于数据备份和主从复制</li>
</ul>
<h2 id="undo-log"><a href="#undo-log" class="headerlink" title="undo log"></a>undo log</h2><p>每次执行事务的过程中，都记录下回滚时需要的信息到一个日志里，那么在事务执行中发生了MYSQL崩溃后，就不用担心无法回滚到事务之前的数据，我们可以通过这个日志回滚到事务之前的数据 undo log 这个机制，它保证了事务的ACID中的原子性 每当innodb引擎对一条记录进行操作（增加，修改，删除）时，需要把回滚时需要的信息都记录到undo log里：</p>
<ul>
<li>在插入一条记录时，把这条记录的主键值记录下来，这样回滚时，只需要把这个主键值对应的记录删掉即可</li>
<li>在删除一条记录时，把这条记录的内容都记下来，这样回滚时，再把这些内容组成记录插入到表中</li>
<li>更新一条记录时，要把被更新的列的旧值记下来，这样回滚时，再把这些列更新为旧值即可</li>
</ul>
<p>在回滚时，就读取undo log里的数据，然后做原先相反操作。比如当delete一条记录时，undo log中会把记录中的内容都记下来，然后执行回滚操作的时候，就读取undo log里的数据，然后进行insert操作 undo log 还有一个作用，通过 ReadView + undo log 实现 MVCC(多版本并发控制) 对于 读提交 与 可重复读 隔离级别的事务来说，它们的快照读(普通select语句)是通过ReadView + undo log来实现的，区别在于</p>
<ul>
<li>读提交：每个select都会生成一个新的ReadView，也意味着，事务期间的多次读取同一条数据，前后两次读的数据可能会出现不一致，因为可能这期间另外一个事务修改了该记录，并提交了事务</li>
<li>可重复读：事务启动的时候，生成一个ReadView，整个事务期间都在用这个Read View，这样就保证了在事务期间读取到的数据都是事务启动前的记录</li>
</ul>
<p>这两个隔离级别实现是通过 事务的 ReadView 里的字段，记录中的两个隐藏列 (trx_id 和 roll_pointer)的对比，如果不满足可见性，就会顺着 undo log 版本链里找到满足其可见性的记录，从而控制并发事务访问同一条记录的行为，这就叫 MVCC(多版本并发控制)</p>
<p><img src="/images/posts/mysql_log/img-1.png" alt="MySql日志"></p>
<h2 id="redo-log"><a href="#redo-log" class="headerlink" title="redo log"></a>redo log</h2><p>Buffer Pool 提高了独写效率，但是Buffer Pool是基于内存的，而内存总是不可靠的，万一断电重启，还没来得及落盘的脏页数据就会丢失 为了防止断电导致数据丢失的问题，当有一条记录需要更新的时候，innodb引擎就会先更新内存（同时标记为脏页），然后将本次对这个页的修改以 redo log 的形式记录下来，这时候更新算是完成 InnoDB会在适当的时候，由后台线程将缓存在Buffer Pool的脏页刷新到磁盘里，这就是WAL(Write-Ahead Logging)技术 WAL：MYSQL的写操作并不是立马写到磁盘上，而是先写日志，然后在合适的时间再写到磁盘上。 WAL优点：MYSQL的写操作从磁盘的随机写变成了顺序写，提升语句的执行性能。 redo log是物理日志，记录了某个数据页做了什么修改，比如对XXX表空间中的YYY数据页ZZZ偏移量的地方做了AAA更新，每当执行一个事务就会产生这样的一条或者多条物理日志 在提交事务的时候，只要先将 redo log持久化到磁盘，可以不需要等待将缓存在 Buffer Pool 里的脏页数据持久化到磁盘。当系统奔溃的时候，虽然脏页数据没有持久化，但是redo log持久化了，接着MYSQL重启，可以根据 redo log的内容，将所有的数据恢复到最新状态 写入 redo log的方式是追加写，所以磁盘操作是顺序写，而写入数据需要先找到写入位置，然后才写入磁盘，所以磁盘是随机写。 磁盘的顺序写 比 随机写 要高效很多，因此 redo log 写入磁盘的开销更小</p>
<p><img src="/images/posts/mysql_log/img-2.png" alt="MySql日志"></p>
<h3 id="redo-log-buffer"><a href="#redo-log-buffer" class="headerlink" title="redo log buffer"></a>redo log buffer</h3><p>执行一个事务的过程中，产生的redo log也不是直接写入磁盘的，因为这样会产生大量的IO操作，而且磁盘的运行速度远慢于内存。所以 redo log 也有自己的缓存， redo log buffer，每产生一条 redo log时，会先写入到 redo log buffer redo log buffer 默认大小 16 MB，可以通过 innodb_log_buffer_size 参数动态调整大小，增大它的大小可以让 MYSQL处理 大事务 是不必写入磁盘，进而提升写 IO 性能</p>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<h3 id="redo-log刷盘机制"><a href="#redo-log刷盘机制" class="headerlink" title="redo log刷盘机制"></a>redo log刷盘机制</h3><p>缓存在 redo log buffer 里的 redo log 还是在内存中，它是什么时候刷盘的？</p>
<ul>
<li>MYSQL正常关闭时</li>
<li>当 redo log buffer 中记录的写入量大于 redo log buffer 内存空间的一半时，会触发落盘</li>
<li>InnoDB的后台线程每隔1秒，将redo log buffer 持久化到磁盘</li>
<li>每次事务提交的时都将缓存在 redo log buffer 里的 redo log 直接持久化到磁盘(这个策略可由innodb_flush_log_at_trx_commit 参数控制)</li>
</ul>
<p>单独执行一个更新语句的时候，InnoDB引擎会自己启动一个事务，在执行更新语句的过程中，生成的redo log先写入到redo log buffer中，然后等事务提交的时候，再将缓存在redo log buffer中的redo log按照顺序写到磁盘 innodb_flush_log_at_trx_commit 含义:</p>
<ol>
<li>设置为0时，表示每次事务提交，还是将 redo log 留在 redo log buffer 中，该模式下在事务提交时不会主动触发写入磁盘的顺序</li>
<li>设置为1时，表示每次事务提交时，都将缓存在 redo log buffer 里的redo log直接持久化到磁盘，这样可以保证MYSQL异常重启之后数据不丢失</li>
<li>设置为2时，表示每次事务提交时，都只缓存在 redo log buffer 里的 redo log 写到 redo log 文件,写入到 redo log 并不意味着写入到磁盘，因为操作系统的文件中有个Page Cache。Page Cache是专门用来缓存文件数据的，所以写入 redo log 文件意味着写入到操作系统的文件缓存</li>
</ol>
<p><img src="/images/posts/mysql_log/img-3.png" alt="MySql日志"></p>
<h3 id="redo-log写满"><a href="#redo-log写满" class="headerlink" title="redo log写满"></a>redo log写满</h3><p>InnoDB存储引擎有1个重做日志组(redo log Group),重做日志文件组由2个redo log文件组成，这两个redo日志文件名叫: ib_logfile0 和 ib_logfile1,重做日志是以循环写的方式工作的，从头开始写，写到尾部就又回到头部，相当于一个环形，比如 InnoDB存储就会写io_logfile0,当io_logfile0被写满了，会切换到 ib_logfile1,当io_logfile1也被写满时，会切换到ib_logfile0文件</p>
<p>redo log是为了防止Buffer Pool中的脏页丢失而设计的，那么如果随着系统运行，Buffer Pool的脏页刷新到磁盘中，那么 redo log对应的记录也就没用了，这时候我们擦除这些旧记录，腾出空间记录新的更新操作 redo log是循环写的方式，相当于一个环形，InnoDB用write pos表示redo log当前记录写到的位置，用checkpoint表示当前要擦除的位置</p>
<ul>
<li>write pos 和 checkpoint 的移动都是顺时针方向</li>
<li>write pos ~ checkpoint 之间的部分，红色部分，用来记录新的更新记录</li>
<li>checkpoint ~ write pos 之间的部分，蓝色部分，待落盘的脏数据页记录</li>
</ul>
<p>当 write pos 追上了 checkpoint , 意味着 redo log 文件满了，这时mysql不能再执行新的更新操作，也就是说mysql会被阻塞，此时会停下来将 Buffer Pool中脏页的数据刷新到磁盘中，然后redo log那些记录可以被擦除，接着对旧的redo log进行擦除，等擦除旧记录腾出了空间，checkpoint就会往后移动，然后mysql恢复正常运行，继续执行新的更新操作</p>
<p><img src="/images/posts/mysql_log/img-4.png" alt="MySql日志"></p>
<p><a href="https://camo.githubusercontent.">https://camo.githubusercontent.</a></p>
<h3 id="undo-log-和-redo-log区别"><a href="#undo-log-和-redo-log区别" class="headerlink" title="undo log 和 redo log区别"></a>undo log 和 redo log区别</h3><ul>
<li>redo log 记录了此次事务 完成后 的数据状态，记录的是更新之后的值</li>
<li>undo log 记录了此次事务 开始前 的数据状态，记录的是更新之前的值</li>
</ul>
<p><img src="/images/posts/mysql_log/img-5.png" alt="MySql日志"></p>
<h2 id="binlog"><a href="#binlog" class="headerlink" title="binlog"></a>binlog</h2><ul>
<li>statement模式: 每一条会修改数据的sql都会记录在binlog中. 不需要记录每一行的变化,减少 binlog 日志量,节省IO,提高性能. 由于sql的执行是有上下文的,因此在保存的时候需要保存相关信息,同时还有一些使用了函数之类的使用的语句无法被复制和记录</li>
<li>row级别: 不记录sql语句上下文信息,仅保存那条记录被修改.记录单元为每一行的改动,基本是可以全部记录下来的但是由于很多操作，会导致大量的改动(比如alter table),因此这种模式的文件保存的信息太多,日志量太大</li>
<li>mixed: 这种方案,普通操作使用的 statement记录,当无法使用 stetement的时候,使用row</li>
</ul>
<h2 id="主从复制原理"><a href="#主从复制原理" class="headerlink" title="主从复制原理"></a>主从复制原理</h2><p>MySql主从复制依赖binlog,也就是记录mysql上所有变化并以二进制形式保存在磁盘上。复制过程就是将binlog中的数据从主库传输到从库上 这个过程是异步的，也就是从库上执行事务操作的线程不会等待binlog的线程完成同步 这个过程一般是异步的，也是就主库上执行事务操作的线程不会等待复制binlog的线程完成同步</p>
<ul>
<li>写入binlog: 主库写binlog日志,提交事务,并更新本地存储数据</li>
<li>同步binlog: 把binlog复制到所有的从库上，每个从库把binlog写到暂存日志中</li>
<li>回放binlog: 回放binlog,并更新存储引擎中的数据</li>
</ul>
<p><img src="/images/posts/mysql_log/img-6.png" alt="MySql日志"></p>
<h3 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h3><ul>
<li>mysql主库在接受客户端提交事务的请求之后，会先写入binlog，再提交事务，更新存储引擎中的数据，事务提交完成后，返回客户端 “操作成功” 的响应</li>
<li>从库会创建一个专门的IO线程，连接从库的log dump线程，来接受主库的binlog日志，再把binlog信息写入relay log的中继日志里，再返回给主库”复制成功”的响应</li>
<li>从库会创建一个用于回放binlog的线程，去读relay log中继日志，然后放回binlog更新存储引擎中的数据，最终实现主从的数据一致性</li>
</ul>
<p><img src="/images/posts/mysql_log/img-7.png" alt="MySql日志"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://xiaolincoding.com/mysql/">https://xiaolincoding.com/mysql/</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>MySql分库分表</title>
    <url>//article/mysql_partition_databases_and_tables/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>分库分表</p>
</blockquote>
<h1 id="📝-分库分表要解决的问题"><a href="#📝-分库分表要解决的问题" class="headerlink" title="📝 分库分表要解决的问题"></a>📝 分库分表要解决的问题</h1><ol>
<li>解决B+树设计上本身的瓶颈问题(数据过多, 以及内存不够放索引,需要发生IO之类的问题)</li>
<li>解决技术问题之后,要考虑业务的拆分情况,尽可能的减少业务代码的改动逻辑</li>
<li>分库分表的本质就是把B+树的设计瓶颈重新拆散,变成无瓶颈</li>
</ol>
<h1 id="📔-垂直切分"><a href="#📔-垂直切分" class="headerlink" title="📔  垂直切分"></a><strong>📔  垂直切分</strong></h1><p>通过将不同业务的数据，放到不同的数据库中，实现不同业务之间数据库层面的隔离</p>
<p><img src="/images/posts/mysql_partition_databases_and_tables/img-1.png" alt="MySql分库分表"></p>
<h1 id="📔-水平切分"><a href="#📔-水平切分" class="headerlink" title="📔  水平切分"></a><strong>📔  水平切分</strong></h1><p>可以按照某种规则将某些字段分散到多个库中，每个表中只包含一部分数据 ,字段是一样的(比如日期字段，用户id字段，区域字字段等，比较理想的切分字段)</p>
<p><img src="/images/posts/mysql_partition_databases_and_tables/img-2.png" alt="MySql分库分表"></p>
<h1 id="📔-如何实施"><a href="#📔-如何实施" class="headerlink" title="📔   如何实施"></a><strong>📔   如何实施</strong></h1><p><img src="/images/posts/mysql_partition_databases_and_tables/img-3.png" alt="MySql分库分表"></p>
<h2 id="1-先评估是否需要拆分"><a href="#1-先评估是否需要拆分" class="headerlink" title="1. 先评估是否需要拆分"></a>1. <strong>先评估是否需要拆分</strong></h2><p>能不能有其他更好的优化方案，分库分表的开发周期比较长，一般只作为兜底方案去执行</p>
<h2 id="2-定制拆分的详细方案"><a href="#2-定制拆分的详细方案" class="headerlink" title="2. 定制拆分的详细方案"></a>2. <strong>定制拆分的详细方案</strong></h2><ol>
<li>选择合适的分表策略</li>
<li>再通知可能要更改的下游</li>
<li>尽量做好设计，避免分库分表带来的常见问题</li>
</ol>
<h2 id="3-目的评估"><a href="#3-目的评估" class="headerlink" title="3. 目的评估"></a>3. 目的评估</h2><ol>
<li>评估：拆成几个表，几个库</li>
<li>目标：读写能力提升多少，负载降低多少，<strong>容量支持未来N年的发展</strong></li>
</ol>
<p>举个例子：</p>
<p>当前30亿数据，如何拆分</p>
<p>解答：</p>
<p>找一个合理的拆分情况，比如按区域，按门店去拆分，不影响原来的业务，并且颗粒度细致到足够未来N年的发展就可以了</p>
<h2 id="4-拆分完成后防止问题兜底方案"><a href="#4-拆分完成后防止问题兜底方案" class="headerlink" title="4. 拆分完成后防止问题兜底方案"></a>4. 拆分完成后防止问题兜底方案</h2><p>**作用：**保证切换数据源之后，正常的增量数据也同步写入一份到旧数据源，这样能保证新数据发生不可短时间解决的问题的时候，避免回滚可以通过开关配置切换到旧数据源上正常执行业务</p>
<p><strong>方案：</strong></p>
<ol>
<li>同步双写：同步写新、旧数据源（代码层面上直接改）</li>
<li>异步双写：通过监听binlog的方式去写旧数据源</li>
<li>中间件同步工具：通过中间件工具去实施同步数据</li>
</ol>
<p><strong>注意点：做好补偿机制， 因为可能写其中一个库失败，需要做好最终一致性问题</strong></p>
<h2 id="5-存量数据的迁移问题"><a href="#5-存量数据的迁移问题" class="headerlink" title="5.存量数据的迁移问题"></a>5.存量数据的迁移问题</h2><p><strong>作用</strong>：迁移旧数据源的数据同步到新的数据源上，保证原有业务的正常CRUD</p>
<p><strong>方案：</strong></p>
<ol>
<li>自己写job开发，查询旧数据源，写入新库</li>
</ol>
<p>2.用中间件去同步</p>
<p>3.交给DBA去同步</p>
<p>注意点：一定要校验数据的准确性，因为这是分库分表后的最后一步，一定要再三确认数据的一致性校验问题，并且要有相关的job做好补偿方案</p>
<h1 id="分库分表后存在的问题"><a href="#分库分表后存在的问题" class="headerlink" title="分库分表后存在的问题"></a>分库分表后存在的问题</h1><h2 id="分布式id问题"><a href="#分布式id问题" class="headerlink" title="分布式id问题"></a>分布式id问题</h2><ol>
<li>雪花算法，</li>
<li>全局的自增id，</li>
<li>美团分布式id方案</li>
</ol>
<h2 id="跨表查询问题"><a href="#跨表查询问题" class="headerlink" title="跨表查询问题"></a>跨表查询问题</h2><ol>
<li>从设计上解决跨表查询的情况，设计上基本能解决95%以上的问题，比如冗余字段，拆分的时候按业务隔离性去拆分</li>
<li>分开查询，通过java代码去组合数据</li>
<li>同一个线程需要跨库的，可以通过JDBC的方式去查询</li>
<li>用ES等查询工具查询出主要信息，再组装数据</li>
</ol>
<h2 id="分布式事务问题"><a href="#分布式事务问题" class="headerlink" title="分布式事务问题"></a>分布式事务问题</h2><ol>
<li>做好补偿方案</li>
<li>最大努力通知（使用MQ）</li>
<li>人工兜底补偿 - 保底</li>
</ol>
<h1 id="📔-切分策略"><a href="#📔-切分策略" class="headerlink" title="📔  切分策略"></a><strong>📔  切分策略</strong></h1><details><summary>范围切片</summary>

</details>

<details><summary>中间映射表</summary>

</details>

<details><summary>hash切分</summary>

</details>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>思考</tag>
        <tag>使用入门</tag>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>MySql BufferPool</title>
    <url>//article/mysql_bufferPool/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-Innodb-体系图"><a href="#📝-Innodb-体系图" class="headerlink" title="📝 Innodb 体系图"></a>📝 Innodb 体系图</h1><p><img src="/images/posts/mysql_bufferPool/img-1.png" alt="MySql BufferPool"></p>
<h1 id="Buffer-Pool"><a href="#Buffer-Pool" class="headerlink" title="Buffer Pool"></a>Buffer Pool</h1><p>Innodb为了缓存磁盘的页，在mysql服务器启动向操作系统申请了一片连续的内存，该内存叫 Buffer pool（缓冲池）。默认情况下 128M 大小</p>
<p>查看大小指令</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">show variables like <span class="string">&#x27;innodb_buffer_pool_size&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/mysql_bufferPool/img-2.png" alt="MySql BufferPool"></p>
<p>读取数据时，先从缓冲池中查看是否数据的页(page)存在，不存在的话去磁盘上检索，查到后缓存到innodb_buffer_pool中，同理，插入修改删除操作也是先操作缓存里数据，之后再以一定的频率更新到磁盘上，这个刷盘的机制叫checkpoint</p>
<p>Innodb引擎设计一个缓冲池(Buffer Pool)，来提高数据库的读写性能 有Buffer Pool后：</p>
<ul>
<li>当读取数据时，如果数据存于 Buffer Pool中，客户端就会直接读取 Buffer Pool中的数据，否则再去磁盘中读取</li>
<li>当修改数据时，如果数据存在于Buffer Pool中，那直接修改 Buffer Pool中数据所在的页，然后将其页设置为脏页(该内的内存数据和磁盘上的数据已经不一致)，为了减少磁盘IO，不会立即将脏页写入磁盘，后续由后台线程选择一个合适的时机将脏页写入到磁盘</li>
</ul>
<h2 id="Change-Buffer"><a href="#Change-Buffer" class="headerlink" title="Change Buffer"></a>Change Buffer</h2><ul>
<li>修改数据库数据时，如果数据页刚还在缓存页，可以修改缓存区的数据页为脏页</li>
<li>如果数据页不在缓冲区，就需要把数据页从磁盘加载到缓存区，然后进行修改。对于写多读少的场景，会产生大量的磁盘IO，影响数据库的性能。</li>
</ul>
<p>Change Buffer对数据更新过程有加速作用，如果数据页没有在内存中，会将更新操作缓存到Change buffer中，这样就不需要从磁盘读入这个数据页，减少IO操作，提高了性能。先将更新操作，记录在Change Buffer 中，之后再进行 merge,真正进行数据更新</p>
<h2 id="内部组成"><a href="#内部组成" class="headerlink" title="内部组成"></a>内部组成</h2><p>Buffer pool中默认缓存页大小和磁盘上默认的页大小是一样的，16k。</p>
<p>为了更好的管理buffer pool的缓存页，innodb为每一个缓存页都创建了一些所谓的控制信息，这些控制信息包括页所属的表空间编号，页号，缓存页在buffer pool中的地址，链表节点信息，一些锁信息以及LSN信息，还有一些别的控制信息。</p>
<p>每个页缓存页对应的控制信息占用的内存大小是相同的，称为 控制块。控制块和缓存页是一一对应的，都被存放在buffer pool中</p>
<p>每个控制块占缓存页大小的5%</p>
<p><img src="/images/posts/mysql_bufferPool/img-3.png" alt="MySql BufferPool"></p>
<h2 id="free链表"><a href="#free链表" class="headerlink" title="free链表"></a>free链表</h2><p>问题: 怎么区分Buffer Pool中那些缓存页是空闲的,那些是已经被使用了的呢?</p>
<p> 我们将所有空闲的缓存页对应的控制块作为一个节点,放在一个链表中,那么这个链表就可以被称作为free链表(空闲列表).</p>
<p><img src="/images/posts/mysql_bufferPool/img-4.png" alt="MySql BufferPool"></p>
<h2 id="flush链表"><a href="#flush链表" class="headerlink" title="flush链表"></a>flush链表</h2><p>如果我们修改了buffer pool中某个缓存页的数据,那它和磁盘上的页不一致了,这样的缓存页也成为 脏页(dirty page).</p>
<p>问题:</p>
<ul>
<li>如果每发生一次修改的话,就立刻同步到磁盘上对应的页上,但是频繁的往磁盘上中写数据会严重影响程序的性能.所以每次修改缓存页的时候,并不着急立刻把修改的同步到磁盘上,而是在未来某个时间点进行同步.</li>
<li>如果不立刻同步的话,那怎么区别buffer pool中那些页是脏页,那些页从来没被修改过呢?</li>
</ul>
<p>所以就需要一个存储脏页的链表来进行记录,凡是修改过的缓存页对应的控制块,都作为一个节点加入到一个链表中,这个链表节点对应的缓存页都需要被刷新到磁盘上,所以也叫flush链表,其结构和free链表一样</p>
<p><img src="/images/posts/mysql_bufferPool/img-5.png" alt="MySql BufferPool"></p>
<h2 id="LRU链表-Least-Recently-Used"><a href="#LRU链表-Least-Recently-Used" class="headerlink" title="LRU链表(Least Recently Used)"></a><strong>LRU链表(Least Recently Used)</strong></h2><p>问题: Buffer Pool对应的内存大小也是有限的,如果缓存的页占用大小超过了Buffer Pool大小,也就是free链表中也没有多余的空闲缓存页时候,该怎么处理?</p>
<p> 将旧的缓存页从buffer pool中移除,然后再将新的放入进来.</p>
<p>普通的LRU产生的问题:</p>
<ul>
<li><p>容易出现预读失效</p>
<p>预读(Read-Ahead),提前把页放入到缓冲池,但最终mysql并没有从页中读取数据,称为预读失败</p>
</li>
<li><p>容易出现缓冲池污染</p>
<p>缓冲池污染: 当某一个sql的时候,要扫描大量数据时,可能导致缓冲池的所有页都替换出去,导致大量热数据被换出,mysql性能急剧下降,这种情况叫缓冲池污染</p>
</li>
</ul>
<p>Mysql的buffer pool为了处理这2个问题,在普通的LRU基础上做了一些优化,尽量让真正的热点页可以在buffer pool里存活时间更长</p>
<p>Mysql的buffer pool处理方法: 头部的是新生代,尾部的是老生代</p>
<ul>
<li>如果不在Buffer Pool中,在把该页从磁盘加载到Buffer Pool中的缓存页时,就把该缓存页对应的控制块作为节点放入LRU链表的头部</li>
<li>如果该缓存页存在Buffer Pool中,则直接把该页对应的控制块移动到LRU链表头部</li>
</ul>
<p>只要我们使用到某个缓存页,就把该缓存页调整到LRU链表的头部,这样LRU链表尾部就是最近最少使用的缓存页,所以当Buffer Pool中的空闲缓存页使用完时,到LRU链表的尾部找些缓存页进行淘汰</p>
<h2 id="刷新脏页到磁盘"><a href="#刷新脏页到磁盘" class="headerlink" title="刷新脏页到磁盘"></a><strong>刷新脏页到磁盘</strong></h2><ol>
<li>从LRU链表的冷数据中刷新一部分缓存页到磁盘.</li>
</ol>
<p>  从LRU尾部开始扫描一些缓存页,扫描的缓存页数量可以通过系统变量innodb_lru_scan_depth来指定,如果从这里发现了脏页,会把他们刷新到磁盘.称为BUF_FLUSH_LRU</p>
<ol>
<li>从flush链表中刷新一部分页面到磁盘</li>
</ol>
<p>  后台线程会定时从flush刷新一部分缓存页到磁盘,刷新的速率取决于当时系统是不是很繁忙.称为BUF_FLUSH_LIST</p>
<p>InnoDB的更新操作采用 Write Ahead Log 策略，即先写入日志，再写入磁盘，通过redo log日志让mysql拥有了恢复能力 触发脏页的刷新:</p>
<ul>
<li>当 redo log 日志满了后，会主动触发脏页刷新到磁盘</li>
<li>Buffer Pool空间不足时，需要将一部分数据页淘汰掉，如果淘汰的是脏页，需要先将脏页同步到磁盘</li>
<li>Mysql认为空闲时，后台线程会定期将适量的脏页刷新到磁盘</li>
<li>Mysql正常关闭后，会把所有的脏页刷新到磁盘</li>
</ul>
<h2 id="提高缓存命中率"><a href="#提高缓存命中率" class="headerlink" title="提高缓存命中率"></a>提高缓存命中率</h2><p>Buffer Pool大小是有限的，对于一些频繁访问的数据希望可以一直留在Buffer Pool,而一些很少访问的数据希望可以在某些时机可以淘汰掉，从而保证Buffer Pool不会因为太满而导致无法再缓存新的数据，同时还保留常用的数据在Buffer Pool中 实现思路：</p>
<ul>
<li>当访问的页在 Buffer Pool里，就直接把该页对应的 LRU 链节点移动到链表的头部</li>
<li>当访问的页不在Buffer Pool里，除了要把页放入到 LRU 链表的头部，还要淘汰 LRU 链表尾部的节点</li>
</ul>
<p>Buffer Pool使用三种页和链表管理数据</p>
<ul>
<li>Free Page(空闲页)，表示此页面未被使用，位于 Free 链表</li>
<li>Clean Page(干净页)，表示此页已被使用，但是页面未发生修改，位于LRU链表</li>
<li>Dirty Page(脏页)，表示此页 已被使用 且 已经被修改，起数据和磁盘上的数据已经不一致了.当脏页上的数据写入磁盘后,内存数据和磁盘数据一致,那么该页就变成了干净页.脏页同时存在于 LRU 链表 和 Flush 链表</li>
</ul>
<p><img src="/images/posts/mysql_bufferPool/img-6.png" alt="MySql BufferPool"></p>
<h2 id="预读失败"><a href="#预读失败" class="headerlink" title="预读失败"></a>预读失败</h2><p>MySql在加载数据页的时候,会把相邻的数据页一并加载进来,目的是为了减少磁盘IO 但是可能这些数据页,并没有被访问,相当于这个预读是白做的,这就是预读失效</p>
<h2 id="预读命中率低"><a href="#预读命中率低" class="headerlink" title="预读命中率低"></a><strong>预读命中率低</strong></h2><p>要避免缓存失效带来的影响,最好就是让预读的停留在Buffer Pool里的时间要尽可能的短,让真正被访问的页才移动到LRU链表的头部,从而保证真正被读取的热数据保留在Buffer Pool里的时间尽可能的长 将LRU划分为 2 个区域, old区域 和 young 区域 young 区域在 LRU 链表的前半部分, old在后半部分 old 区域占整个 LRU 链表长度的比例 可以通过 innodb_old_blocks_pct 参数来设置, 代表整个 LRU 链表的 young 区域 和 old 区域比例是 63:37 划分 2 个区域后,预读的页就只需要加入到 old 区域的头部,当真正被访问的时候,才将页插入到young区域的头部.如果预读的页一致没有被访问,就会从old区域移除,这样就不影响young区域中的热点数据</p>
<p><img src="/images/posts/mysql_bufferPool/img-7.png" alt="MySql BufferPool"></p>
<h2 id="BufferPool污染"><a href="#BufferPool污染" class="headerlink" title="BufferPool污染"></a>BufferPool污染</h2><p>全表扫描,很多缓存页其实只会被访问一次,但是它却只因为被访问一次而进入到young区域,从而导致热点数据被替换了 LRU 链表中 young 区域就是热点数据,只要我们提高进入到 young 区域的门槛,就能有效的保证young区域里的热点数据不会被替换掉 进入到 young 区域条件增加一个停留在 old 区域的时间判断</p>
<ul>
<li>如果后续的访问时间与第一次访问的时间在某个时间间隔内,那么该缓存页就不会被从old区域移动到young区域的头部</li>
<li>如果后续的访问时间与第一次访问的时间不在某个时间间隔内,那么该缓存页移动到young区域的头部</li>
</ul>
<p>这个时间间隔由 innodb_old_blocks_time 控制的,默认是 1000 ms</p>
<h2 id="多个Buffer-Pool实例"><a href="#多个Buffer-Pool实例" class="headerlink" title="多个Buffer Pool实例"></a><strong>多个Buffer Pool实例</strong></h2><p>本质： Innodb向操作系统申请了一块连续的内存空间，多线程的情况下，访问buffer pool中的各链表都需要加锁处理，在Buffer pool特别大而且多线程并发访问情况下，单一的buffer pool可能会影响请求的访问速度</p>
<p> 所以在Buffer Pool特别大的时候，可以拆分成若干个小的Buffer pool，每个buffer pool都称为一个实例，都是独立的，独立的申请内存空间，独立的管理各种链表，所以在多线程并发访问时并不会相互影响，从而提高并发处理能力</p>
<h2 id="查看Buffer-Pool的状态信息"><a href="#查看Buffer-Pool的状态信息" class="headerlink" title="查看Buffer Pool的状态信息"></a><strong>查看Buffer Pool的状态信息</strong></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">show engine innodb status\G;</span><br></pre></td></tr></table></figure>

<p>将Buffer Pool And Memory结果复制出来：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">----------------------</span><br><span class="line">BUFFER POOL AND MEMORY</span><br><span class="line">----------------------</span><br><span class="line">Total large memory allocated <span class="number">0</span></span><br><span class="line">Dictionary memory allocated <span class="number">2523782</span></span><br><span class="line">Buffer pool size   <span class="number">8192</span></span><br><span class="line">Free buffers       <span class="number">1039</span></span><br><span class="line">Database pages     <span class="number">7060</span></span><br><span class="line">Old database pages <span class="number">2588</span></span><br><span class="line">Modified db pages  <span class="number">0</span></span><br><span class="line">Pending reads      <span class="number">0</span></span><br><span class="line">Pending writes: LRU <span class="number">0</span>, flush list <span class="number">0</span>, single page <span class="number">0</span></span><br><span class="line">Pages made young <span class="number">5477</span>, not young <span class="number">20908</span></span><br><span class="line"><span class="number">0.00</span> youngs/s, <span class="number">0.00</span> non-youngs/s</span><br><span class="line">Pages read <span class="number">1331</span>, created <span class="number">10341</span>, written <span class="number">57134</span></span><br><span class="line"><span class="number">0.00</span> reads/s, <span class="number">0.00</span> creates/s, <span class="number">0.00</span> writes/s</span><br><span class="line">Buffer pool hit rate <span class="number">1000</span> / <span class="number">1000</span>, young-making rate <span class="number">0</span> / <span class="number">1000</span> not <span class="number">0</span> / <span class="number">1000</span></span><br><span class="line">Pages read ahead <span class="number">0.00</span>/s, evicted without access <span class="number">0.00</span>/s, Random read ahead <span class="number">0.00</span>/s</span><br><span class="line">LRU len: <span class="number">7060</span>, unzip_LRU len: <span class="number">0</span></span><br><span class="line">I/O sum[<span class="number">0</span>]:cur[<span class="number">0</span>], unzip sum[<span class="number">0</span>]:cur[<span class="number">0</span>]</span><br></pre></td></tr></table></figure>

<ul>
<li>Total large memory allocated： 代表Buffer Pool向操作系统申请的连续内存空间大小，包括全部控制块，缓存页，以及碎片的大小</li>
<li>Dictionary memory allocated： 为数据字典信息分配的内存空间大小，注意这个内存空间和 Buffer Pool 没啥关系，不包括在 Total memory allocated 中。</li>
<li>Buffer pool size： 代表该Buffer Pool可以容纳多少缓存页，单位是页</li>
<li>Free buffers： 当前Buffer Pool还有多少空闲缓存页，也就是free链表中还有多少节点</li>
<li>Database pages：代表LRU链表中的页的数量，包含了young和old两个区域的节点数量</li>
<li>Old database pages： LRU链表old区域的节点数量</li>
<li>Modified db pages： 脏页数量，包含了young和old两个区域的节点数量</li>
<li>Pending reads： 正在等待从磁盘上加载到Buffer Pool中的页面数量</li>
<li>Pending writes：<ul>
<li>LRU： 即将从LRU链表中刷新到磁盘中的页面数量</li>
<li>flush list：即将从flush链表中刷新到磁盘中的页面数量</li>
<li>single page：即将以单个页面的形式刷新到磁盘中的页面数量</li>
</ul>
</li>
<li>Pages made young： LRU链表中曾经从old区域移动到young区域头部的节点数量</li>
<li>Pages made not young： 在将innodb_old_blocks_time设置的值大于0时，首次访问或者后续访问某个处在old区域的节点时由于不符合时间间隔的限制而不能将其移动到young区域头部时，page made not young的值会加1</li>
<li>youngs&#x2F;s： 每秒从old区域移动到young区域头部的节点数量</li>
<li>non-youngs&#x2F;s： 每秒由于不满足时间限制而不能从old区域移动到young区域头部的节点数量</li>
<li>Pages read,created,written: 代表读取，创建，写入了多少页，后续跟着读取，创建，写入的速率</li>
<li>Buffer pool hit rate：代表在过去某段时间，平均访问1000次页面，有多少次该页面已经被缓存到Buffer Pool了</li>
<li>young-making rate： 表示在过去某段时间，平均访问 1000 次页面，有多少次访问使页面移动到 young 区域的头部了</li>
<li>not：表示在过去某段时间，平均访问 1000 次页面，有多少次访问没有使页面移动到 young 区域的头部。</li>
<li>LRU len： 代表LRU链表节点的数量</li>
<li>unzip_LRU len： 代表unzip_LRU链表中节点数量</li>
<li>I&#x2F;O sum：最近 50s 读取磁盘页的总数。</li>
<li>I&#x2F;O cur：现在正在读取的磁盘页数量。</li>
<li>I&#x2F;O unzip sum：最近 50s 解压的页面数量。</li>
<li>I&#x2F;O unzip cur：正在解压的页面数量。</li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>Buffer Pool来 提高 数据库的 读写性能 InnoDB通过三种链表来管理缓存页：</p>
<ul>
<li>Free List(空闲页链表)：管理空闲页</li>
<li>Flush List(脏页链表)：管理脏页</li>
<li>LRU List(管理脏页+干净页)：将最近且经常查询的数据缓存其中，而不是经常查询的数据就淘汰出去</li>
</ul>
<p>InnoDB对LRU做了一些优化，</p>
<ul>
<li>将LRU链表 分为 young和old两个区域，加入缓存池的页，优先插入old区域；页被访问时，才进入young区域，目的是为了解决预读失效的问题</li>
<li>当**「页被访问」且「 old 区域停留时间超过 innodb_old_blocks_time 阈值（默认为1秒）」**时，才会将页插入到 young 区域，否则还是插入到 old 区域，目的是为了解决批量数据访问，大量热数据淘汰的问题</li>
</ul>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://xiaolincoding.com/mysql/">https://xiaolincoding.com/mysql/</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>数据开发经历(一)</title>
    <url>//article/data_dev_experience1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-简介"><a href="#📝-简介" class="headerlink" title="📝 简介"></a>📝 简介</h1><p> 在公司的数据平台上,进行一些简单的指标开发也有一段时间了,对于是否有预期的成长的还是取决于个人的欲望以及行动,不排除我是行动的巨婴.</p>
<p> 也简单的记录下自己这段时间的一些总结语录以及如果我下次还在数据方面的项目上怎么处理?</p>
<h1 id="需求和调研"><a href="#需求和调研" class="headerlink" title="需求和调研"></a><strong>需求和调研</strong></h1><p> 由于目前的项目都是在乙方的场景,所以需要去与甲方进行沟通了解他们的需求以及系统,并且同时还要去推动相关需求指标的进度。这里涉及到的东西其实很多。</p>
<p> 需求以及调研: 这个阶段一定要好好梳理该对行业或者同样类似的系统,进行梳理，最好是能明白业务场景。 就举个例子: 财务系统,这是目前大公司都会具备的吧,那换个角度去思考的话,如果我只晓得了财务系统的话，那我是不是在梳理相关场景或者相关指标的情况下，是不是就轻松很多了呢？ 然后在整理需求和调研阶段的话，对应的坑以及客户的业务痛点就会很清晰和明白了。</p>
<p> 也就是说句直白的话: 对应业务场景和需求痛点,不论是不是站在产品的角度或者开发的角度视角，一定要对其进行吃透，而不能模糊二口，特别是在调研阶段，如果你存在很多业务场景不是很清晰的情况,就会非常容易出现你在梳理需求的时候,吃不透业务痛点,对应客户需要的真是东西很难把握，甚至说，你可能就把握不住。</p>
<h1 id="指标梳理"><a href="#指标梳理" class="headerlink" title="指标梳理"></a><strong>指标梳理</strong></h1><p> 指标梳理这步很关键的,根据你目前的需求等场景进行指标的梳理,梳理出来的结果就是你想要的或者业务方想要的.</p>
<p> 指标梳理这个步骤中,我们一定要吃透或者理解透目前业务方或者需要梳理的指标的系统里面的一定业务场景,这会关系到你接下来的取数以及数据要怎么聚合的操作和设计这块.</p>
<p> 维度梳理: 最基本的维度梳理,也就是业务场景下,需要梳理的维度是有那些场景的. 比如:时间维度,汇总维度,组织维度等.</p>
<p> 每种不同的维度,都会对你的sql产生很大的影响,比如你的组织维度是一层一层的,那你在组织维度进行汇总的时候,你是不是还要考虑到时间的维度? 月到季度?月到年等这种时间维度的概念汇总.</p>
<p> 基于上面,业务需求+指标的梳理,这些都昨完了的话,那其实业务调研和指标梳理要做的事情,就已经做完了，于是就可以放开了的去设计接入数据源的时间维度，sql进行汇总的逻辑操作.</p>
<p> 其实,你的指标梳理到好的话,你开发后的工作会舒服很多</p>
<p> 指标文档: 这里的调研,对应要输出出来的,你每个指标对应的时间维度或者是其他的维度,同时也要一并输出出来的是:业务口径和计算口径</p>
<h1 id="数据核算-数据字典输出"><a href="#数据核算-数据字典输出" class="headerlink" title="数据核算 &amp; 数据字典输出"></a><strong>数据核算 &amp; 数据字典输出</strong></h1><p> 再根据需求或者业务方提供了相应的指标之后,那么我们紧接着要去做的事情就是,通过数据源去查看,去查看数据之类的东西是否符合预期。 比如财务指标: 不同的记账科目编码,那你就要去梳理下是否有科目编码这个字典表。</p>
<p> 然后你在核算数据的同时，还要去输出一个比较关键的文档.</p>
<p> 数据字段文档: 这个数据字段文档的话,其实就是已经设计到数仓方面的数据。</p>
<p> ods &#x2F; dwd &#x2F; dws &#x2F; dim &#x2F; adm</p>
<p> 设计到这五层的数仓建模的字典表的输出,每一层的表怎么接入,全量还是增量? 触发时间是什么时候(也就是调度周期)？如果是增量的话,依赖的增量字段是什么？还是利用类似flink cdc这类的框架来进行实时数据的接入?</p>
<p> 这个文档输出后,还要与客户进行确认.</p>
<h1 id="开发注意点"><a href="#开发注意点" class="headerlink" title="开发注意点"></a><strong>开发注意点</strong></h1><p> 全量: 如果数据是全量每天或者每月拉入进来的话,要涉及考虑的问题是数据量的大小(避免数据量太大影响到可能在凌晨运行的job).</p>
<p> 伪增量: 是否有可信的更新字段,来进行伪增量的获取. 比如更新时间, 我每天或者多久的时间范围内,我就去更新时间是在这个范围的数据.</p>
<p> 实时增量: 类似flick cdc 或者 cancl去监听mysql的binlog这样的功能来实现实时增量的功能.</p>
<h1 id="开发规范"><a href="#开发规范" class="headerlink" title="开发规范"></a><strong>开发规范</strong></h1><p> 这个地方其实对应的就是无规矩不方圆, 也就是开发之前要定义好规矩,这样不仅仅对程序很有益,对后面来的同事或者随时看你程序的涉及等方案的时候,也是一目了然的.</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据开发</category>
      </categories>
      <tags>
        <tag>大数据</tag>
        <tag>数据开发</tag>
      </tags>
  </entry>
  <entry>
    <title>数据开发经历(二)</title>
    <url>//article/data_dev_experience2/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-数据开发流程"><a href="#📝-数据开发流程" class="headerlink" title="📝 数据开发流程"></a>📝 <strong>数据开发流程</strong></h1><p> 其实数据开发中遇见的问题和我们应用层面的问题存在部分的相似之处，但是在进行数据问题排错的时候，又存在些许的不相似。总体上的感觉,得去做过和排查过,自身才能对问题理解到比较清晰</p>
<h1 id="任务调度"><a href="#任务调度" class="headerlink" title="任务调度"></a><strong>任务调度</strong></h1><p> 任务调度,这个是有时序的,也是存在先后顺序的。 一般最晚的时间点就是我们拉取最晚那批数据的时间了。</p>
<p> 理想情况下(基于离线数仓): 比较理想的情况下,就是我们最好每一层的处理时间预算都是停留在一个小时左右,但是具体的时间量等情况，得看数据量和具体的业务需要了.</p>
<p> 0点1点: 执行ods的数据,具体的调度周期就跟你业务上的数据变化或者数仓中的数据需要,来进行调度周期是天&#x2F;周&#x2F;月等情况的调度处理.</p>
<p> 1点到2点: 执行dim或者dwd的数据,这个组织维度的数据,看是否有变化的调整(一般公司的人员组织或者集团下的预取,城市,项目之类的会存在有变化的调整).</p>
<p> 2点到3点: dws 层的数据处理,一般dws的数据可以是由dim和dwd进行轻度汇总获取出来的. 其实到了这层的话,需要的指标对应的最小维度基本已经可以看到数据了。只是这个维度的数据,是最小的时间维度和组织维度汇聚出来的。</p>
<p> 3点到4点: adm层的数据汇总,也就是我们的数仓最终给输出出来的大宽表数据.</p>
<p> 有了大宽表后,看下数据还需不需要同步到mmp这类的数据库中,来进行一个高效率的查询(比如clickhouse,drios等).</p>
<h1 id="表的设计"><a href="#表的设计" class="headerlink" title="表的设计"></a><strong>表的设计</strong></h1><p> 表的设计也是很关键的,目前虽然我们的数据设计来源于业务需求&#x2F;报表需求&#x2F;商业报表需求等场景,但是更好的联系起实际的设计也很重要的。</p>
<p> 举个例子: 我们的组织维度, 存在 组织1 , 组织2 ， 组织3 (这三者的关系就是我们平常看到的树的概念,1是2的上级,2是3的上级),同时还存在时间维度的(月,季度,年). 此时,梳理完指标的计算口径,我们发现,1的数据由2汇聚,2的数据由3进行汇聚, 年由月汇聚,季度也是月汇聚出来的, 于是我们设计dws或者adm最后汇总的表样时候,可以冗余 组织2和组织1对应到组织3,时间维度冗余最小的月份，于是在组织这个维度就可以清晰的看到，根据组织1和组织2进行sum和group的时候,就获取初对应组织的值了(冗余组织的关系,聚合的时候就可以减少去需要上下级之间的关系了).</p>
<p> 前置条件: 数据汇聚的计算口径得清晰和明白,方案并不能适应所有的场景. 比如这里的组织得明确最多多少层,一般一个集团或者一个公司,不会出现很多级得概念.</p>
<h1 id="数据重复"><a href="#数据重复" class="headerlink" title="数据重复"></a><strong>数据重复</strong></h1><p> 数据重复这个问题也是很常见的话,比如你拉取过来的组织怎么重复了呀? 我这个指标的字段怎么在帆软报表上也是重复的了啊? 比如有时候我们为了去冗余一些多余的字段进来的时候,并没有很明白该字段对应的场景或者说是业务变化的场景,导致重复的问题</p>
<p> 举个例子: 更新时间字段, 我们聚合sum数据的时候,可能会考虑到冗余该字段进来,于是并没有考虑到时间变化可能会有不同的日期的时候的场景,group就会出现多条数据的情况</p>
<p> 任务重跑导致的数据重复问题:</p>
<p> 一般hive中,我们都会对表进行分区之类的操作. 我们在插入数据之前,会覆盖数据的. 什么意思呢? 就是insert overwrite 会在插入之前,删除对应分区的数据,再进行数据的插入. 但是有时候写漏掉了或者job跑的时候出现同时多跑的情况,就有可能出现数据重复的问题</p>
<h1 id="实时数据接入"><a href="#实时数据接入" class="headerlink" title="实时数据接入"></a><strong>实时数据接入</strong></h1><p> flink cdc : 待使用</p>
<p> canal: 正常情况下,我们会使用canal+kafka来进行与程序的结构,意思就是canal假装成mysql的重节点,获取mysql的binlog变化的数据,然后再对其进行解析,写入到kafka中. 数据到了kafka,后面的程序要怎么消费就怎么消费.</p>
<p> 后面待更新</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<p> 不论去做什么陌生的领域也好,我们一定要有自己的理解和分析技巧. 要有自己的韧性和耐心,去面对在你不知道的领域上的问题和难点.</p>
<p> 保持冷静的思维,是面对未知的场景和难题.</p>
<p> 保持好奇和坚持的心态和行为,去持续学习和成长.</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据开发</category>
      </categories>
      <tags>
        <tag>大数据</tag>
        <tag>数据开发</tag>
      </tags>
  </entry>
  <entry>
    <title>数仓开发规范</title>
    <url>//article/data_warehouse_guide/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>数仓开发规范</p>
</blockquote>
<h1 id="📝-ETL规范-Execl文件"><a href="#📝-ETL规范-Execl文件" class="headerlink" title="📝 ETL规范(Execl文件)"></a>📝 ETL规范(Execl文件)</h1><h2 id="文档规范"><a href="#文档规范" class="headerlink" title="文档规范"></a>文档规范</h2><p>execl表头记录所有表的信息，类似mysql的索引</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>序号</td>
<td>层次类别</td>
<td>表名</td>
<td>中文名称</td>
<td>备注</td>
</tr>
<tr>
<td>1</td>
<td>ods</td>
<td>user_list</td>
<td>用户信息</td>
<td>用户信息</td>
</tr>
</tbody></table>
<h2 id="单个表记录-单个execl页"><a href="#单个表记录-单个execl页" class="headerlink" title="单个表记录(单个execl页)"></a>单个表记录(单个execl页)</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>表英文名</td>
<td>dwd_ls_md_item_store_df</td>
<td>设计人</td>
<td>鲍洋</td>
</tr>
<tr>
<td>表备注</td>
<td>商品门店级别</td>
<td></td>
<td></td>
</tr>
<tr>
<td>备注</td>
<td>每天做快照，根据后期需求，再扩展计算指标</td>
<td>ETL策略</td>
<td>日全量</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>源表基本信息</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>表中文名称</td>
<td>表英文名称</td>
<td>别名</td>
<td>数据总量</td>
</tr>
<tr>
<td>商品所有基本信息</td>
<td>ods_erp.o_bas_club_item</td>
<td>ci</td>
<td>835943</td>
</tr>
<tr>
<td>商品所有基本信息1</td>
<td>ods_erp.o_bas_club_item1</td>
<td>ci1</td>
<td>835943</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>表关联关系描述</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>源表别名1</td>
<td>关联类型</td>
<td>源表别名2</td>
<td>备注</td>
</tr>
<tr>
<td>ci</td>
<td>inner join</td>
<td>ci1</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>字段映射描述(组1)</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>目标表</td>
<td>源表</td>
<td>规则</td>
<td></td>
</tr>
<tr>
<td>列名</td>
<td>字段类型</td>
<td>列描述</td>
<td>列名</td>
</tr>
<tr>
<td>club_nbr</td>
<td>bigint</td>
<td>门店号</td>
<td>ci.club_nbr</td>
</tr>
<tr>
<td>item_nbr</td>
<td>bigint</td>
<td>商品号</td>
<td>ci.item_nbr</td>
</tr>
<tr>
<td>item_on_shelf_date</td>
<td>date</td>
<td>上架日期</td>
<td>ci.item_on_shelf_date</td>
</tr>
<tr>
<td>item_off_shelf_dt</td>
<td>date</td>
<td>下架日期</td>
<td>ci.item_off_shelf_dt</td>
</tr>
<tr>
<td>markdown_status_cd</td>
<td>bigint</td>
<td></td>
<td>ci.markdown_status_cd</td>
</tr>
<tr>
<td>item_status_code</td>
<td>string</td>
<td>商品状态</td>
<td>ci.item_status_code</td>
</tr>
<tr>
<td>unit_retail_amt</td>
<td>decimal(25,10)</td>
<td>单位零售金额</td>
<td>ci.unit_retail_amt</td>
</tr>
<tr>
<td>link_item_nbr</td>
<td>bigint</td>
<td>链接上号号</td>
<td>ci.link_item_nbr</td>
</tr>
<tr>
<td>last_change_ts</td>
<td>timestamp</td>
<td>最近变更时间</td>
<td>ci.last_change_ts</td>
</tr>
<tr>
<td>last_change_userid</td>
<td>string</td>
<td>最近变更人</td>
<td>ci.last_change_userid</td>
</tr>
<tr>
<td>non_mbr_upchrg_ind</td>
<td>string</td>
<td></td>
<td>ci.non_mbr_upchrg_ind</td>
</tr>
<tr>
<td>unit_retail_chg_dt</td>
<td>date</td>
<td></td>
<td>ci.unit_retail_chg_dt</td>
</tr>
<tr>
<td>last_markdown_ind</td>
<td>string</td>
<td></td>
<td>ci.last_markdown_ind</td>
</tr>
<tr>
<td>whpk_sell_amt</td>
<td>decimal(25,10)</td>
<td></td>
<td>ci.whpk_sell_amt</td>
</tr>
<tr>
<td>vnpk_cost_amt</td>
<td>decimal(25,10)</td>
<td></td>
<td>ci.vnpk_cost_amt</td>
</tr>
<tr>
<td>last_update_pgm_id</td>
<td>string</td>
<td></td>
<td>ci1.last_update_pgm_id</td>
</tr>
<tr>
<td>item_create_dt</td>
<td>date</td>
<td></td>
<td>ci.item_create_dt</td>
</tr>
<tr>
<td>cancel_whn_out_dt</td>
<td>date</td>
<td></td>
<td>ci.cancel_whn_out_dt</td>
</tr>
<tr>
<td>cncl_unit_rtl_amt</td>
<td>decimal(25,10)</td>
<td></td>
<td>ci.cncl_unit_rtl_amt</td>
</tr>
<tr>
<td>lead_time_qty</td>
<td>smallint</td>
<td></td>
<td>ci.lead_time_qty</td>
</tr>
<tr>
<td>prompt_price_ind</td>
<td>string</td>
<td>促销价</td>
<td>ci.prompt_price_ind</td>
</tr>
<tr>
<td>lease_sales_pct</td>
<td>decimal(25,10)</td>
<td></td>
<td>ci.lease_sales_pct</td>
</tr>
<tr>
<td>lease_eff_date</td>
<td>date</td>
<td></td>
<td>ci.lease_eff_date</td>
</tr>
<tr>
<td>lease_exp_date</td>
<td>date</td>
<td></td>
<td>ci.lease_exp_date</td>
</tr>
<tr>
<td>last_sold_date</td>
<td>date</td>
<td></td>
<td>ci.last_sold_date</td>
</tr>
<tr>
<td>lease_dflt_sls_pct</td>
<td>decimal(25,10)</td>
<td></td>
<td>ci.lease_dflt_sls_pct</td>
</tr>
<tr>
<td>itemfile_source_nm</td>
<td>string</td>
<td></td>
<td>ci.itemfile_source_nm</td>
</tr>
<tr>
<td>max_retail_amt</td>
<td>decimal(25,10)</td>
<td>最大零售金额</td>
<td>ci.max_retail_amt</td>
</tr>
<tr>
<td>whpk_sell_chg_rsn_cd</td>
<td>bigint</td>
<td></td>
<td>ci.whpk_sell_chg_rsn_cd</td>
</tr>
</tbody></table>
<h1 id="ODS作业规范-Execl文件"><a href="#ODS作业规范-Execl文件" class="headerlink" title="ODS作业规范(Execl文件)"></a>ODS作业规范(Execl文件)</h1><h2 id="模板说明-首页execl"><a href="#模板说明-首页execl" class="headerlink" title="模板说明(首页execl)"></a>模板说明(首页execl)</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>基本信息</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>制定人</td>
<td>鲍某</td>
<td>编写时间</td>
<td>2022&#x2F;9&#x2F;28</td>
</tr>
<tr>
<td>审核人</td>
<td></td>
<td>审核时间</td>
<td></td>
</tr>
<tr>
<td>版本</td>
<td>V1.0</td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>文档修订历史</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>序号</td>
<td>版本号</td>
<td>修订章节</td>
<td>修订原因</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h2 id="工程情况-单页execl"><a href="#工程情况-单页execl" class="headerlink" title="工程情况(单页execl)"></a>工程情况(单页execl)</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>版本时间</td>
<td>工程</td>
<td>调度数</td>
<td>&gt;&#x3D;100W数量表</td>
<td>备注</td>
</tr>
<tr>
<td>2022&#x2F;9&#x2F;1</td>
<td>ods_user</td>
<td>2</td>
<td>0</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h2 id="ods作业梳理"><a href="#ods作业梳理" class="headerlink" title="ods作业梳理"></a>ods作业梳理</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>TASK</td>
<td>属主</td>
<td>SRC_TBL</td>
<td>TGT_DB</td>
<td>TGT_TBL</td>
<td>抽取方式</td>
<td>上线时间</td>
<td>调度频率</td>
<td>调度时间</td>
<td>更新时间</td>
<td>平台</td>
<td>当前状态</td>
<td>数据量</td>
<td>备注</td>
</tr>
<tr>
<td>目标表名</td>
<td>userdb</td>
<td>源表名</td>
<td>ods_userdb</td>
<td>o_[源表名]_di</td>
<td>增量</td>
<td>2022&#x2F;9&#x2F;1</td>
<td>每天一次</td>
<td>每天02:01</td>
<td></td>
<td>dolphinscheduler</td>
<td>激活</td>
<td>1379</td>
<td></td>
</tr>
<tr>
<td>目标表名</td>
<td>userdb</td>
<td>源表名</td>
<td>ods_userdb</td>
<td>o_[源表名]_df</td>
<td>全量</td>
<td>2022&#x2F;9&#x2F;1</td>
<td>每天一次</td>
<td>每天02:01</td>
<td></td>
<td>dolphinscheduler</td>
<td>激活</td>
<td>1379</td>
<td></td>
</tr>
</tbody></table>
<h1 id="数仓命名规范"><a href="#数仓命名规范" class="headerlink" title="数仓命名规范"></a>数仓命名规范</h1><h2 id="库命名规范"><a href="#库命名规范" class="headerlink" title="库命名规范"></a>库命名规范</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td></td>
<td>正式库</td>
<td>临时库</td>
</tr>
<tr>
<td></td>
<td>ods_[source database name ]</td>
<td>ods_[source database name ]_tmp</td>
</tr>
<tr>
<td></td>
<td>dwd_db</td>
<td>dwd_db_tmp</td>
</tr>
<tr>
<td></td>
<td>dws_db</td>
<td>dws_db_tmp</td>
</tr>
<tr>
<td></td>
<td>ads_db</td>
<td>ads_db_tmp</td>
</tr>
<tr>
<td></td>
<td>dim_db</td>
<td>dim_db_tmp</td>
</tr>
</tbody></table>
<h2 id="表命名规范"><a href="#表命名规范" class="headerlink" title="表命名规范"></a>表命名规范</h2><ul>
<li>所有层级表必须要加字段包括：etl_load_ts</li>
<li>表命名使用英文小写字母，单词之间用下划线分开，总长度不能超过 40 个字符</li>
</ul>
<h2 id="表类型"><a href="#表类型" class="headerlink" title="表类型"></a>表类型</h2><p>明细表：dtl 中间表：mid 信息：info 配置表：cfg<br>拉链表：zip 临时表：tmp 汇总表：sum 日志表：log<br>手工填报：imp(开头) 实时表：re</p>
<h2 id="数据周期"><a href="#数据周期" class="headerlink" title="数据周期"></a>数据周期</h2><p>时全量：hf 时增量：hi 时快照：hs<br>日全量：df 日增量：di 日快照：ds<br>周全量：wf 周增量：wi 周快照：ws<br>月全量：mf 月增量：mi 月快照：ms<br>注： df 每天全量（表没有分区），di 每天增量（每天一个分区），ds 每天增量保留<br>历史到目前记录（一天一个分区）</p>
<h2 id="ods命名规范"><a href="#ods命名规范" class="headerlink" title="ods命名规范"></a>ods命名规范</h2><ul>
<li>增量：ods_源库名.o_{表名}_di</li>
<li>全量：ods_源库名.o_{表名}_df</li>
<li>接口：ods_源库名.o_api_[api 数据名称]_[数据周期]</li>
<li>文件：ods_源库名.o_flie_[业务数据名称]_[数据周期]</li>
<li>数据量：历史数据量小于 50W 条记录，且未来增长慢的全量同步</li>
<li>字段命名规范：字段默认使用源系统的字段名</li>
<li>字段名与关键字冲突：在源字段名后加上_col，即源字段名_col</li>
</ul>
<h2 id="dwd命名规范"><a href="#dwd命名规范" class="headerlink" title="dwd命名规范"></a>dwd命名规范</h2><p>{库}.{层级}<em>{业务域}</em>{数据域}<em>{业务内容}</em>{表类型}<em>{数据周期} 例如：dwd_db.dwd_ds_user</em>{order_info}_dtl_di</p>
<h2 id="dws命名规范"><a href="#dws命名规范" class="headerlink" title="dws命名规范"></a>dws命名规范</h2><p>{库}.{层级}<em>{业务域}</em>{数据域}<em>{业务内容}</em>{表类型}<em>{数据周期} 例如：dws_db.dws_ds_user</em>{order_info}_sum_di</p>
<h2 id="dim命名规范"><a href="#dim命名规范" class="headerlink" title="dim命名规范"></a>dim命名规范</h2><p>{库}.{层级}<em>{pub&#x2F;维度内容}</em>{数据周期} </p>
<p>例如：dim_db.dim_pub_area_df </p>
<p><strong>其中的 pub 与具体业务无关，各个业务部都可以共用，例如时间维度</strong></p>
<h2 id="ads命名规范"><a href="#ads命名规范" class="headerlink" title="ads命名规范"></a>ads命名规范</h2><p>{库}.{层级}<em>{项目内容|自定义}</em>{表类型}_{数据周期} 例如：ads_db.ads_user_dmp_sum_df</p>
<h2 id="临时表"><a href="#临时表" class="headerlink" title="临时表"></a>临时表</h2><p>{临时库}.{表名}_tmp{序号} 例如：</p>
<p>dwd_db_tmp.dwd_order_info_dtl_di_tmp01 dwd_db_tmp.dwd_order_info_dtl_di_tmp02</p>
<h2 id="分区表"><a href="#分区表" class="headerlink" title="分区表"></a>分区表</h2><p>分区字段：</p>
<ul>
<li>分 mi（00-59） 如 28</li>
<li>时 hh（00-23） 如 10</li>
<li>日 yyyy-mm-dd 如 2022-09-28</li>
<li>月 yyyy-mm    如 2022-09</li>
<li>年 yyyy       如 2022</li>
</ul>
<p>区分表：分区表取数必须要加分区限制,分区注释必须注明根据那个字段进行分区</p>
<p>表分区到<strong>时、分</strong>分区级别时，表设计使用多级分区 ，如 时分区表；ts：表示日，hh： 表示时；分分区表：ts：表示日，hh：表示时；mi:表示分</p>
<h2 id="实时表命名规范"><a href="#实时表命名规范" class="headerlink" title="实时表命名规范"></a>实时表命名规范</h2><ul>
<li>各数仓层次表设计以 re_ 开头</li>
<li>ODS 层表设计：{库}.re_{ods}<em>{业务域}</em>{数据域}<em>{业务内容}</em>{数据周期}，其他命 名规范见</li>
<li>DWD 层表设计：{库}.re_{dwd}<em>{业务域}</em>{数据域}<em>{业务内容}</em>{数据周期}，其他命 名规范见</li>
<li>DWS 层表设计：{库}.re_{dws}<em>{业务域}</em>{数据域}<em>{业务内容}</em>{数据周期}，其他命 名规范见</li>
<li>DIM 层表设计：{库}.re_{dim}<em>{pub&#x2F;维度内容}</em>{数据周期}，其他命名规范见</li>
</ul>
<h1 id="字段类型规范"><a href="#字段类型规范" class="headerlink" title="字段类型规范"></a>字段类型规范</h1><ul>
<li>字符：字符串类型使用string</li>
<li>数字：整形最大值 10 位以上：bigint ; 整形最大值在 3 位到 9 位：int（不考虑）; 非整形数字类型：decimal(25,10)</li>
<li>时间类型：最小粒度为天： date ；时间： timestamp</li>
<li>布尔类型是否判断：is_{业务}，不允许出现空值 ; 0: 表示否， 1：表示是</li>
</ul>
<h1 id="工作流命名"><a href="#工作流命名" class="headerlink" title="工作流命名"></a>工作流命名</h1><h2 id="ods"><a href="#ods" class="headerlink" title="ods"></a>ods</h2><p>工作流: job_{ods_库名}_{序号} 例如：job_ods_ams</p>
<p>task: 目标表名 例如：o_order_info_df</p>
<h2 id="dwd"><a href="#dwd" class="headerlink" title="dwd"></a>dwd</h2><p>工作流: job_dwd_{业务域}<em>{数据域}</em>{序号} 例如：job_dwd_ds_user</p>
<p>task: 目标表名 例如：dwd_ds_user_xxx_df</p>
<h2 id="dws"><a href="#dws" class="headerlink" title="dws"></a>dws</h2><p>工作流：job_dws_{业务域}<em>{数据域}</em>{序号} 例如：job_dws_ds_user</p>
<p>task：目标表名 例如：dws_ds_user_xxx_df</p>
<h2 id="ads"><a href="#ads" class="headerlink" title="ads"></a>ads</h2><p>工作流：job_ads_{业务域}_{序号} 例如：job_ads_ds</p>
<p>task: 目标表名 例如：ads_user_dmp_xxx_df</p>
<h1 id="数据模型设计"><a href="#数据模型设计" class="headerlink" title="数据模型设计"></a>数据模型设计</h1><h2 id="横向分层"><a href="#横向分层" class="headerlink" title="横向分层"></a>横向分层</h2><h3 id="ods-1"><a href="#ods-1" class="headerlink" title="ods"></a>ods</h3><p>操作数据层，即原始数据层。ODS 层有些时候会细分为两层，一个 STG 数据缓冲层，存原 始数据，一个 ODS，存简单清洗的数据</p>
<h3 id="dwd-1"><a href="#dwd-1" class="headerlink" title="dwd"></a>dwd</h3><p>明细数据层，对数据进行清洗、代码统一、字段统一、格式统一、简单聚合等工作。DWD 层存在的意义是做数据的标准化，为后续的处理提供干净、统一、标准的数据</p>
<h3 id="dws-1"><a href="#dws-1" class="headerlink" title="dws"></a>dws</h3><p>数据汇总层，按照业务目标，对已经处理好的数据进行横向汇聚、纵向汇总。按照宽表模型 进行数据冗余和预计算，以空间换时间</p>
<h3 id="dim"><a href="#dim" class="headerlink" title="dim"></a>dim</h3><p>维度层，即存储所有维度（可以理解为码表）。其实 DIM 不应该单独作为一层，因为从 DWD 层开始向上的所有层都需要用到 DIM 中的内容，是横跨多层的</p>
<h3 id="ads-1"><a href="#ads-1" class="headerlink" title="ads"></a>ads</h3><p>应用服务层，一般就直接对接 OLAP 分析，或者业务层数据调用接口</p>
<h2 id="纵向分域"><a href="#纵向分域" class="headerlink" title="纵向分域"></a>纵向分域</h2><p>主题域通常是联系较为紧密的数据主题的集合，方便寻找和使用数据</p>
<h3 id="dw层"><a href="#dw层" class="headerlink" title="dw层"></a>dw层</h3><p>正式库： dw_db 临时库：dw_db</p>
<p>划分依据：按照业务或业务过程划分,比如一个靠销售广告位置的门户网站主题域可能会有 广告域，客户域等，而广告域可能就会有广告的库存，销售分析、内部投放分析等主题</p>
<h3 id="DIM层"><a href="#DIM层" class="headerlink" title="DIM层"></a>DIM层</h3><p>正式库： dim_db 临时库：dim_db_tmp </p>
<p>划分依据：按照公司主数据划分</p>
<h3 id="DM层"><a href="#DM层" class="headerlink" title="DM层"></a>DM层</h3><p>正式库： dm_db 临时库：dm_db_tmp </p>
<p>划分依据：按照主题域进行划分</p>
<h3 id="ADS层"><a href="#ADS层" class="headerlink" title="ADS层"></a>ADS层</h3><p>正式库： ads_db_tables 临时库：ads_db_tmp</p>
<p>划分依据：</p>
<ol>
<li>根据需求方划分：比如需求方为财务部，就可以设定对应的财务主题域（业务场景）， 而财务主题域里面可能就会有员工工资分析</li>
<li>根据项目划分</li>
</ol>
<p><img src="/images/posts/data_warehouse_guide/img-1.png" alt="数仓开发规范"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据开发</category>
      </categories>
      <tags>
        <tag>数据开发</tag>
        <tag>规范</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark3.0.x合并Iceberg小文件</title>
    <url>//article/spark_3_0_x_iceberg_small_file/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Spark3.0.1合并Iceberg的小文件</p>
</blockquote>
<h1 id="📝-Iceberg小文件处理"><a href="#📝-Iceberg小文件处理" class="headerlink" title="📝 Iceberg小文件处理"></a>📝 Iceberg小文件处理</h1><h2 id="Spark3-0-1"><a href="#Spark3-0-1" class="headerlink" title="Spark3.0.1"></a>Spark3.0.1</h2><p>Spark3.0.1版本的sql执行</p>
<p>参数说明:</p>
<ul>
<li>如果有 catalog 的设置的话,在system前面加上 catalog. (catalog是你具体的catalog名字,比如spark_hadoop)</li>
<li>ods_bigscreen.o_ecuser_list: 具体的数据库名字和表名字</li>
<li>TIMESTAMP ‘2022-09-08 00:00:00.000’: 该时间之前的数据进行清除</li>
<li>1 是保留的版本数量，比如这里的1就是1</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">CALL system.expire_snapshots(<span class="string">&#x27;ods_bigscreen.o_ecuser_list&#x27;</span>, TIMESTAMP <span class="string">&#x27;2022-09-08 00:00:00.000&#x27;</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<p>执行结果如下图:</p>
<p>参数解析:</p>
<ul>
<li>deleted_data_files_count 删除对应表的data文件夹个数。如果我们的文件格式选择的是 parquet，那么文件是以 .parquet 结尾，比如 00000-0-0eca9076-9c03-4077-baa9-e68769e15c58-00001.parquet 就是一个数据文件。</li>
<li>deleted_manifest_files_count: 删除元数据metadata文件夹的数据。其里面列出了组成某个快照（snapshot）的数据文件列表。每行都是每个数据文件的详细描述，包括数据文件的状态、文件路径、分区信息、列级别的统计信息（比如每列的最大最小值、空值数等）、文件的大小以及文件里面数据的行数等信息。其中列级别的统计信息在 Scan 的时候可以为算子下推提供数据，以便可以过滤掉不必要的文件。每次更新会产生多个清单文件。</li>
<li>deleted_manifest_lists_count: 清单列表也是元数据文件，其里面存储的是清单文件的列表，每个清单文件占据一行。每行中存储了清单文件的路径、清单文件里面存储数据文件的分区范围、增加了几个数据文件、删除了几个数据文件等信息。这些信息可以用来在查询时提供过滤。清单列表也是 avro 格式进行存储的，所以是以 .avro 后缀结尾的；而且这个文件是以 snap- 开头的，比如 snap-7389540589641972921-1-aa90f6ed-aee2-49c7-a61c-bd13ed411c66.avro，其中 7389540589641972921 这串数字是代表快照 id（snapshot_id）。每次更新都会产生一个清单列表文件。</li>
</ul>
<p><img src="/images/posts/spark_3_0_x_iceberg_small_file/img-1.png" alt="Spark3.0.x合并Iceberg小文件"></p>
<h2 id="pySpark进行处理"><a href="#pySpark进行处理" class="headerlink" title="pySpark进行处理"></a>pySpark进行处理</h2><p>使用 use 获取 某个数据库的表，然后对这些表进行 合并小文件处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">#!/user/bin/env spark-python</span><br><span class="line">#-*-coding:utf-<span class="number">8</span> -*-</span><br><span class="line"><span class="keyword">import</span> sys,os</span><br><span class="line">from pyspark.sql <span class="keyword">import</span> SparkSession</span><br><span class="line"></span><br><span class="line"># init spark</span><br><span class="line">def <span class="title function_">getSpark</span><span class="params">()</span>:</span><br><span class="line">    # spark连接初始化</span><br><span class="line">    spark = SparkSession \</span><br><span class="line">        .builder \</span><br><span class="line">        .appName(<span class="string">&quot;PySpark_Platform_DataLake&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.jars.packages&quot;</span>,<span class="string">&quot;org.apache.iceberg:iceberg-spark3:0.13.2&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.extensions&quot;</span>,<span class="string">&quot;org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.catalog.spark_catalog&quot;</span>,<span class="string">&quot;org.apache.iceberg.spark.SparkSessionCatalog&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.catalog.spark_catalog.type&quot;</span>,<span class="string">&quot;hive&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.catalog.iceberg&quot;</span>,<span class="string">&quot;org.apache.iceberg.spark.SparkCatalog&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.catalog.iceberg.type&quot;</span>,<span class="string">&quot;hadoop&quot;</span>) \</span><br><span class="line">        .config(<span class="string">&quot;spark.sql.catalog.iceberg.warehouse&quot;</span>,<span class="string">&quot;hdfs:///yundeecluster/datalake&quot;</span>) \</span><br><span class="line">        .enableHiveSupport() \</span><br><span class="line">        .getOrCreate()</span><br><span class="line">    <span class="keyword">return</span> spark</span><br><span class="line"></span><br><span class="line">        </span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">        spark = getSpark()</span><br><span class="line">        spark.sql(<span class="string">&quot;use xiaobao_bigscreent&quot;</span>)</span><br><span class="line">        df = spark.sql(<span class="string">&quot;show tables&quot;</span>)</span><br><span class="line">        <span class="keyword">for</span> row in df.collect():</span><br><span class="line">                <span class="keyword">if</span> row[<span class="string">&#x27;tableName&#x27;</span>] == <span class="string">&#x27;login_record_info&#x27;</span>:</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                baseTable = <span class="string">&#x27;xiaobao_bigscreent.&#x27;</span> + row[<span class="string">&#x27;tableName&#x27;</span>]</span><br><span class="line">                # 重写数据文件, data 目录</span><br><span class="line">                rewrite_manifests_sql = <span class="string">&quot;CALL system.rewrite_manifests(&#x27;&#123;0&#125;&#x27;)&quot;</span>.format(baseTable)</span><br><span class="line">                # 重写 metadata 目录</span><br><span class="line">                rewrite_data_files_sql = <span class="string">&quot;call system.rewrite_data_files(&#x27;&#123;0&#125;&#x27;)&quot;</span>.format(baseTable)</span><br><span class="line">                # 清除快照过期文件</span><br><span class="line">                snapshots_sql = <span class="string">&quot;CALL system.expire_snapshots(&#x27;&#123;0&#125;&#x27;, TIMESTAMP &#x27;2122-12-30 00:00:00.000&#x27;, 1)&quot;</span>.format(baseTable)</span><br><span class="line">                # 清除未关联上数据的 metedata 目录</span><br><span class="line">                remove_orphan_files_sql = <span class="string">&quot;CALL system.remove_orphan_files(table =&gt; &#x27;&#123;0&#125;&#x27;)&quot;</span>.format(baseTable)</span><br><span class="line">                print(<span class="string">&#x27;开始库和表是:&#x27;</span>)</span><br><span class="line">                print(baseTable)</span><br><span class="line">                spark.sql(rewrite_data_files_sql)</span><br><span class="line">                spark.sql(rewrite_manifests_sql)</span><br><span class="line">                spark.sql(snapshots_sql)</span><br><span class="line">                spark.sql(remove_orphan_files_sql)</span><br></pre></td></tr></table></figure>

<h2 id="建表语句"><a href="#建表语句" class="headerlink" title="建表语句"></a>建表语句</h2><p>在见表语句上加上对应的参数配置</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td><strong>Property</strong></td>
<td><strong>Description</strong></td>
</tr>
<tr>
<td>write.metadata.delete-after-commit.enabled</td>
<td>每次表提交后是否删除旧的元数据文件</td>
</tr>
<tr>
<td>write.metadata.previous-versions-max</td>
<td>要保留旧的元数据文件数量</td>
</tr>
</tbody></table>
<p><code>CREATE TABLE $&#123;CataLog名称&#125;.$&#123;库名&#125;.$&#123;表名&#125; (  id bigint,    name string) using icebergPARTITIONED BY (  loc string) TBLPROPERTIES (    &#39;write.metadata.delete-after-commit.enabled&#39;= true,	&#39;write.metadata.previous-versions-max&#39; = 3)</code></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据湖</category>
      </categories>
      <tags>
        <tag>Iceberg</tag>
        <tag>Spark</tag>
        <tag>大数据</tag>
        <tag>数据湖</tag>
      </tags>
  </entry>
  <entry>
    <title>hadoop以及Iceberg在win10运行环境配置</title>
    <url>//article/hadoop_iceberg_in_win/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-下载包准备"><a href="#📝-下载包准备" class="headerlink" title="📝 下载包准备"></a>📝 下载包准备</h1><h2 id="hadoop下载"><a href="#hadoop下载" class="headerlink" title="hadoop下载"></a>hadoop下载</h2><p>下载地址：<a href="https://archive.apache.org/dist/hadoop/common/">https://archive.apache.org/dist/hadoop/common/</a></p>
<p>选择我们对应的hadoop版本;比如我这里是是3.1.1就选择3.1.1</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-1.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h2 id="win10-需要的脚本下载"><a href="#win10-需要的脚本下载" class="headerlink" title="win10 需要的脚本下载"></a>win10 需要的脚本下载</h2><p><a href="https://github.com/cdarlint/winutils/tree/master">https://github.com/cdarlint/winutils/tree/master</a></p>
<p>选择我们对应的hadoop的版本</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-2.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h2 id="对应版本的脚本拷贝"><a href="#对应版本的脚本拷贝" class="headerlink" title="对应版本的脚本拷贝"></a>对应版本的脚本拷贝</h2><h3 id="操作一"><a href="#操作一" class="headerlink" title="操作一"></a>操作一</h3><p>将 hadoop.ddl 和 winutils.exe 拷贝到 下载的hadoop的版本的bin中</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-3.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h3 id="操作二"><a href="#操作二" class="headerlink" title="操作二"></a>操作二</h3><p>将 hadoop.ddl 文件同时拷贝一份到 C:\Windows\System32 目录</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-4.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h3 id="操作三"><a href="#操作三" class="headerlink" title="操作三"></a>操作三</h3><p>配置hadoop中的etc\hadoop的 hadoop-env.cmd 文件的java地址;切换为自己的java路径;</p>
<p>如果是C盘的话，Program Files需要替换为 PROGRA~1 ; 如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">set <span class="variable constant_">JAVA_HOME</span>=<span class="attr">C</span>:\\<span class="variable constant_">PROGRA</span>~<span class="number">1</span>\\<span class="title class_">Java</span>\\jdk1<span class="number">.8</span><span class="number">.0_161</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p><img src="/images/posts/hadoop_iceberg_in_win/img-5.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h3 id="操作四-配置环境变量"><a href="#操作四-配置环境变量" class="headerlink" title="操作四: 配置环境变量"></a>操作四: 配置环境变量</h3><p>HADOO_HOME配置</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-6.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<p>PATH中配置%HADOOP_HOME%\bin</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-7.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h3 id="操作五：测试是否成功"><a href="#操作五：测试是否成功" class="headerlink" title="操作五：测试是否成功"></a>操作五：测试是否成功</h3><p>cmd中输入hadoop version;如果可以看到版本，就说明配置是成功的</p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-8.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h2 id="操作hdfs以及Iceberg"><a href="#操作hdfs以及Iceberg" class="headerlink" title="操作hdfs以及Iceberg"></a>操作hdfs以及Iceberg</h2><h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点:"></a>注意点:</h3><ol>
<li>通过iceberg和hive去读取hdfs的时候，会读取集群的名字;所以这里需要配置根据集群的名字能映射到NameNode(状态为Active的最保险)</li>
<li>比如这里的usdp215对应的active namenode是 172.21.129.218 节点的话;则在host里进行配置即可</li>
<li>到底就可以通过iceberg或者hdfs等api在win10上读取hadoop的数据等</li>
</ol>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-9.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-10.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<p><img src="/images/posts/hadoop_iceberg_in_win/img-11.png" alt="hadoop以及Iceberg在win10运行环境配置"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://blog.csdn.net/qq_42970173/article/details/123714605">https://blog.csdn.net/qq_42970173&#x2F;article&#x2F;details&#x2F;123714605</a></li>
<li><a href="https://blog.csdn.net/yy8623977/article/details/124047743">https://blog.csdn.net/yy8623977/article/details/124047743</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>数据湖</category>
      </categories>
      <tags>
        <tag>hadoop</tag>
        <tag>hive</tag>
        <tag>数据湖</tag>
      </tags>
  </entry>
  <entry>
    <title>CDH6.3.2搭建</title>
    <url>//article/cdh6_3_2_create1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>CDH搭建，减少了hadoop集群生态的维护</p>
</blockquote>
<h1 id="📝-CDH端口开放"><a href="#📝-CDH端口开放" class="headerlink" title="📝 CDH端口开放"></a>📝 CDH端口开放</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>端口号</td>
<td>组件</td>
<td>备注</td>
</tr>
<tr>
<td>7180</td>
<td>CDH web UI</td>
<td></td>
</tr>
<tr>
<td>9870</td>
<td>HDFS web UI</td>
<td></td>
</tr>
<tr>
<td>8088</td>
<td>Yarn web UI</td>
<td></td>
</tr>
<tr>
<td>19888</td>
<td>Historyserver web UI</td>
<td></td>
</tr>
<tr>
<td>8080</td>
<td></td>
<td></td>
</tr>
<tr>
<td>8888</td>
<td>Hue（未优化）</td>
<td></td>
</tr>
<tr>
<td>8889</td>
<td>Hue（优化）</td>
<td></td>
</tr>
<tr>
<td>3306</td>
<td>MySQL</td>
<td></td>
</tr>
<tr>
<td>6379</td>
<td>Redis</td>
<td></td>
</tr>
<tr>
<td>9092</td>
<td>kafka</td>
<td></td>
</tr>
<tr>
<td>2181</td>
<td>zk</td>
<td></td>
</tr>
<tr>
<td>4040</td>
<td>Spark</td>
<td></td>
</tr>
<tr>
<td>18088</td>
<td>Spark</td>
<td></td>
</tr>
<tr>
<td>10000</td>
<td>Hiveserve2</td>
<td></td>
</tr>
<tr>
<td>10002</td>
<td>Hive</td>
<td></td>
</tr>
<tr>
<td>11000</td>
<td>Oozie</td>
<td></td>
</tr>
<tr>
<td>51000</td>
<td>Sentry</td>
<td></td>
</tr>
<tr>
<td>8020</td>
<td>HDFS</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="修改主机hosts配置"><a href="#修改主机hosts配置" class="headerlink" title="修改主机hosts配置"></a>修改主机hosts配置</h1><h2 id="host"><a href="#host" class="headerlink" title="host"></a>host</h2><p>比如这里的配置,每台集群的机器都需要配置, 具体自身的ip和hostname等进行配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 在文件末尾添加每个实例的内网IP及对应的hostname</span><br><span class="line"></span><br><span class="line"><span class="number">172.17</span><span class="number">.79</span><span class="number">.117</span>   hadoop102       hadoop102</span><br><span class="line"><span class="number">172.17</span><span class="number">.79</span><span class="number">.116</span>   hadoop103       hadoop103</span><br><span class="line"><span class="number">172.17</span><span class="number">.79</span><span class="number">.118</span>   hadoop104       hadoop104</span><br></pre></td></tr></table></figure>

<h2 id="ssh免密配置"><a href="#ssh免密配置" class="headerlink" title="ssh免密配置"></a>ssh免密配置</h2><p>配置所有机器之间相互的免密<br>CDH的服务是有server和agent概念的，agent收集到往server进行汇报情况</p>
<p>执行ssh-keygen -t rsa并敲三次回车，会在我们的&#x2F;root&#x2F;.ssh目录下生成两个文件id_rsa（私钥）、id_rsa.pub（公钥）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa</span><br><span class="line">cd .ssh</span><br></pre></td></tr></table></figure>

<p>将公钥拷贝到免密登录的目标机器上</p>
<p>ssh-copy-id hadoop102</p>
<p>ssh-copy-id hadoop103</p>
<p>ssh-copy-id hadoop104</p>
<p>每台机器上重复ssh等操作</p>
<h2 id="集群同步脚步"><a href="#集群同步脚步" class="headerlink" title="集群同步脚步"></a>集群同步脚步</h2><p>vim xsync : 将下面的脚步内容给复制到 xsync 中</p>
<p>chmod u+x xsync : 复制完值，给脚步加上对应的权限</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">#<span class="number">1.</span> 判断参数个数</span><br><span class="line"><span class="keyword">if</span> [ $# -lt <span class="number">1</span> ]</span><br><span class="line">then</span><br><span class="line">  echo Not Enough Arguement!</span><br><span class="line">  exit;</span><br><span class="line">fi</span><br><span class="line">#<span class="number">2.</span> 遍历集群所有机器</span><br><span class="line"><span class="keyword">for</span> host in hadoop102 hadoop103 hadoop104</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">  echo ====================  $host  ====================</span><br><span class="line">  #<span class="number">3.</span> 遍历所有目录，挨个发送</span><br><span class="line">  <span class="keyword">for</span> file in $@</span><br><span class="line">  <span class="keyword">do</span></span><br><span class="line">    #<span class="number">4.</span> 判断文件是否存在</span><br><span class="line">    <span class="keyword">if</span> [ -e $file ]</span><br><span class="line">    then</span><br><span class="line">      #<span class="number">5.</span> 获取父目录</span><br><span class="line">      pdir=$(cd -P $(dirname $file); pwd)</span><br><span class="line">      #<span class="number">6.</span> 获取当前文件的名称</span><br><span class="line">      fname=$(basename $file)</span><br><span class="line">      ssh $host <span class="string">&quot;mkdir -p $pdir&quot;</span></span><br><span class="line">      rsync -av $pdir/$fname $host:$pdir</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      echo $file does not exists!</span><br><span class="line">    fi</span><br><span class="line">  done</span><br><span class="line">done</span><br></pre></td></tr></table></figure>

<h2 id="同步执行指令脚步"><a href="#同步执行指令脚步" class="headerlink" title="同步执行指令脚步"></a>同步执行指令脚步</h2><p>vim <a href="http://xcall.sh/">xcall.sh</a></p>
<p>chmod u+x <a href="http://xcall.sh/">xcall.sh</a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">#! /bin/bash</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i in hadoop102 hadoop103 hadoop104</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">        echo --------- $i ----------</span><br><span class="line">        ssh $i <span class="string">&quot;$*&quot;</span></span><br><span class="line">done</span><br></pre></td></tr></table></figure>

<h2 id="防火墙检测是否关闭"><a href="#防火墙检测是否关闭" class="headerlink" title="防火墙检测是否关闭"></a>防火墙检测是否关闭</h2><p>查看状态: sudo systemctl status firewalld</p>
<p>禁用：    sudo systemctl disable firewalld</p>
<p>停止：    sudo systemctl disable firewalld</p>
<h2 id="检查hostname是否成功"><a href="#检查hostname是否成功" class="headerlink" title="检查hostname是否成功"></a>检查hostname是否成功</h2><p>hostname</p>
<p>hostnamectl status</p>
<h2 id="禁用SELinux-mode"><a href="#禁用SELinux-mode" class="headerlink" title="禁用SELinux mode"></a>禁用SELinux mode</h2><p><strong>安全增强的Linux (SELinux)允许通过策略设置访问控制。如果在部署CDH时遇到困难，请在将CDH部署到集群之前，在每个主机上将SELinux设置为permissive模式</strong></p>
<p>getenforce</p>
<details><summary>如果输出结果为Permissive 或Disabled</summary>

</details>

<h2 id="禁用透明大页配置"><a href="#禁用透明大页配置" class="headerlink" title="禁用透明大页配置"></a>禁用透明大页配置</h2><p>在每台服务器上分别执行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">echo never &gt; /sys/kernel/mm/transparent_hugepage/defrag</span><br><span class="line">echo never &gt; /sys/kernel/mm/transparent_hugepage/enabled</span><br><span class="line">echo <span class="string">&#x27;echo never &gt; /sys/kernel/mm/transparent_hugepage/defrag&#x27;</span> &gt;&gt; /etc/rc.local</span><br><span class="line">echo <span class="string">&#x27;echo never &gt; /sys/kernel/mm/transparent_hugepage/enabled&#x27;</span> &gt;&gt; /etc/rc.local</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 前两行代码只能暂时生效，当重启后配置会消失</span><br><span class="line">[root<span class="meta">@hadoop102</span> parcel-repo]# echo never &gt; /sys/kernel/mm/transparent_hugepage/defrag</span><br><span class="line">[root<span class="meta">@hadoop102</span> parcel-repo]# echo never &gt; /sys/kernel/mm/transparent_hugepage/enabled</span><br><span class="line"></span><br><span class="line"># 永久生效需要把信息加到启动项中</span><br><span class="line">[root<span class="meta">@hadoop102</span> parcel-repo]# echo <span class="string">&#x27;echo never &gt; /sys/kernel/mm/transparent_hugepage/defrag&#x27;</span> &gt;&gt; /etc/rc.local</span><br><span class="line">[root<span class="meta">@hadoop102</span> parcel-repo]# echo <span class="string">&#x27;echo never &gt; /sys/kernel/mm/transparent_hugepage/enabled&#x27;</span> &gt;&gt; /etc/rc.loca</span><br></pre></td></tr></table></figure>

<h2 id="启动NTP服务"><a href="#启动NTP服务" class="headerlink" title="启动NTP服务"></a>启动NTP服务</h2><p>CDH要求在集群中的每台机器上配置一个网络时间协议(NTP)服务。大多数操作系统使用时间同步的ntpd服务。<br>注：RHEL 7兼容的操作系统默认使用chronyd而不是ntpd。如果chronyd正在运行，Cloudera Manager将使用它来确定主机时钟是否同步。否则，Cloudera Manager使用ntpd</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ps -ef |grep chronyd</span><br></pre></td></tr></table></figure>

<h1 id="yum需要安装的源"><a href="#yum需要安装的源" class="headerlink" title="yum需要安装的源"></a>yum需要安装的源</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">yum -y install httpd</span><br><span class="line"></span><br><span class="line">systemctl status httpd</span><br><span class="line"></span><br><span class="line">systemctl start httpd</span><br><span class="line"></span><br><span class="line"># 设置开机自启</span><br><span class="line">systemctl enable httpd.service</span><br></pre></td></tr></table></figure>

<h2 id="yum源工具安装"><a href="#yum源工具安装" class="headerlink" title="yum源工具安装"></a>yum源工具安装</h2><p>yum -y install yum-utils createrepo</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">yum -y install yum-utils createrepo</span><br></pre></td></tr></table></figure>

<h2 id="导入-cm-资源包"><a href="#导入-cm-资源包" class="headerlink" title="导入 cm 资源包"></a>导入 cm 资源包</h2><p>创建&#x2F;var&#x2F;www&#x2F;html&#x2F;cm文件夹</p>
<p>将资料包中cm文件夹下的文件放入&#x2F;var&#x2F;www&#x2F;html&#x2F;cm文件夹中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">创建/<span class="keyword">var</span>/www/html/cm文件夹</span><br><span class="line">mkdir cm</span><br></pre></td></tr></table></figure>

<h2 id="创建-repo"><a href="#创建-repo" class="headerlink" title="创建 repo"></a>创建 repo</h2><p>使用createrepo命令创建本地yum源;在&#x2F;etc&#x2F;yum.repos.d&#x2F;文件夹下创建新的yum repo文件</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">使用createrepo命令创建本地yum源</span><br></pre></td></tr></table></figure>

<p>配置了hosts文件的情况下我们可以直接使用hadoop102作为域名访问</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">===========手动编写repo============</span><br><span class="line">vim /etc/yum.repos.d/cloudera-manager.repo</span><br><span class="line">===========把下面五行代码写入repo文件中=========</span><br><span class="line">[cloudera-manager]</span><br><span class="line">name=Cloudera Manager, Version yum</span><br><span class="line">baseurl=http:<span class="comment">//hadoop102/cm</span></span><br><span class="line">gpgcheck=<span class="number">0</span></span><br><span class="line">enabled=<span class="number">1</span></span><br></pre></td></tr></table></figure>

<h2 id="清除缓存"><a href="#清除缓存" class="headerlink" title="清除缓存"></a>清除缓存</h2><p>清除缓存并建立新的元数据缓存</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 清除缓存</span></span><br><span class="line">yum clean all</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立元数据缓存</span></span><br><span class="line">yum makecache</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">xsync /etc/yum.repos.d/cloudera-manager.repo </span><br><span class="line">yum clean all</span><br><span class="line">yum makecache</span><br><span class="line"></span><br><span class="line"># 验证源是否配置生效</span><br><span class="line">yum list | grep cloudera-manager</span><br></pre></td></tr></table></figure>

<h1 id="CM及依赖组件安装"><a href="#CM及依赖组件安装" class="headerlink" title="CM及依赖组件安装"></a>CM及依赖组件安装</h1><h2 id="java环境"><a href="#java环境" class="headerlink" title="java环境"></a>java环境</h2><p>xcall yum -y install oracle-j2sdk1.8.x86_64</p>
<p>配置java的环境变量，因为我们在上面已经把&#x2F;etc&#x2F;profile中的内容追加到&#x2F;root&#x2F;.bashrc中，所以我们现在只需要配置&#x2F;root&#x2F;.bashrc文件即可</p>
<p>vim &#x2F;root&#x2F;bin&#x2F;setjavahome.sh</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sed -i <span class="string">&#x27;$a\# JAVA_HOME&#x27;</span> /root/.bashrc</span><br><span class="line">sed -i <span class="string">&#x27;$a\export JAVA_HOME=/usr/java/jdk1.8.0_181-cloudera&#x27;</span> /root/.bashrc</span><br><span class="line">sed -i <span class="string">&#x27;$a\export PATH=$JAVA_HOME/bin:$PATH&#x27;</span> /root/.bashrc</span><br><span class="line">sed -i <span class="string">&#x27;$a\export CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar&#x27;</span> /root/.bashrc</span><br><span class="line"></span><br><span class="line">xsync /root/bin/setjavahome.sh</span><br><span class="line"></span><br><span class="line"># 在三台上同时执行添加环境变量的和同步环境变量的操作</span><br><span class="line">xcall  bash /root/bin/setjavahome.sh &amp;&amp; source /root/.bashrc</span><br><span class="line"></span><br><span class="line"># 验证java是否安装成功</span><br><span class="line">xcall.sh java -version</span><br></pre></td></tr></table></figure>

<h2 id="mysql安装"><a href="#mysql安装" class="headerlink" title="mysql安装"></a>mysql安装</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">wget -v http:<span class="comment">//repo.mysql.com/mysql-community-release-el7-5.noarch.rpm</span></span><br><span class="line">sudo rpm -ivh mysql-community-release-el7-<span class="number">5.</span>noarch.rpm</span><br><span class="line">sudo yum -y update</span><br><span class="line">sudo yum -y install mysql-server</span><br><span class="line">sudo systemctl start mysqld</span><br><span class="line"></span><br><span class="line">sudo systemctl stop mysqld</span><br></pre></td></tr></table></figure>

<p>InnoDB log files 做备份</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mkdir /root/backup/</span><br><span class="line">mv /<span class="keyword">var</span>/lib/mysql/ib_logfile0 /<span class="keyword">var</span>/lib/mysql/ib_logfile1 /root/backup/</span><br></pre></td></tr></table></figure>

<p>&#x2F;etc&#x2F;my.cnf文件修改</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">datadir=/<span class="keyword">var</span>/lib/mysql</span><br><span class="line">socket=/<span class="keyword">var</span>/lib/mysql/mysql.sock</span><br><span class="line"># To prevent deadlocks, set the isolation level to READ-COMMITTED.</span><br><span class="line">transaction-isolation = READ-COMMITTED</span><br><span class="line"># Disabling symbolic-links is recommended to prevent assorted security risks;</span><br><span class="line"># to <span class="keyword">do</span> so, uncomment <span class="built_in">this</span> line:</span><br><span class="line">symbolic-links = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">key_buffer_size = 32<span class="type">M</span></span><br><span class="line"><span class="variable">max_allowed_packet</span> <span class="operator">=</span> 16<span class="type">M</span></span><br><span class="line"><span class="variable">thread_stack</span> <span class="operator">=</span> 256<span class="type">K</span></span><br><span class="line"><span class="variable">thread_cache_size</span> <span class="operator">=</span> <span class="number">64</span></span><br><span class="line">query_cache_limit = 8<span class="type">M</span></span><br><span class="line"><span class="variable">query_cache_size</span> <span class="operator">=</span> 64<span class="type">M</span></span><br><span class="line"><span class="variable">query_cache_type</span> <span class="operator">=</span> <span class="number">1</span></span><br><span class="line"></span><br><span class="line">#Allow <span class="number">100</span> maximum connections <span class="keyword">for</span> each database and then add 50extra connections. For example, <span class="keyword">for</span> two databases, set the maximum connections to <span class="number">250.</span> If you store five databases on one <span class="title function_">host</span> <span class="params">(the databases <span class="keyword">for</span> Cloudera Manager Server, Activity Monitor, Reports Manager, Cloudera Navigator, and Hive metastore)</span>, set the maximum connections to <span class="number">550.</span></span><br><span class="line">max_connections = <span class="number">550</span></span><br><span class="line">#expire_logs_days = <span class="number">10</span></span><br><span class="line">#max_binlog_size = 100M</span><br><span class="line"></span><br><span class="line">#log_bin should be on a disk with enough free space.</span><br><span class="line">#Replace <span class="string">&#x27;/var/lib/mysql/mysql_binary_log&#x27;</span> with an appropriate path <span class="keyword">for</span> your</span><br><span class="line">#system and chown the specified folder to the mysql user.</span><br><span class="line">#如果需要替换mysql的log文件存放位置，需要此处拥有足够的空间并且把文件夹的属主改为mysql用户</span><br><span class="line">log_bin=/<span class="keyword">var</span>/lib/mysql/mysql_binary_log</span><br><span class="line"></span><br><span class="line">#In later versions of MySQL, <span class="keyword">if</span> you enable the binary log and <span class="keyword">do</span> not set</span><br><span class="line">#a server_id, MySQL will not start. The server_id must be unique within</span><br><span class="line">#the replicating group.</span><br><span class="line">server_id=<span class="number">1</span></span><br><span class="line"></span><br><span class="line">binlog_format = <span class="type">mixed</span></span><br><span class="line"></span><br><span class="line"><span class="variable">read_buffer_size</span> <span class="operator">=</span> 2<span class="type">M</span></span><br><span class="line"><span class="variable">read_rnd_buffer_size</span> <span class="operator">=</span> 16<span class="type">M</span></span><br><span class="line"><span class="variable">sort_buffer_size</span> <span class="operator">=</span> 8<span class="type">M</span></span><br><span class="line"><span class="variable">join_buffer_size</span> <span class="operator">=</span> 8M</span><br><span class="line"></span><br><span class="line"># InnoDB <span class="type">settings</span></span><br><span class="line"><span class="variable">innodb_file_per_table</span> <span class="operator">=</span> <span class="number">1</span></span><br><span class="line">innodb_flush_log_at_trx_commit  = <span class="number">2</span></span><br><span class="line">innodb_log_buffer_size = 64<span class="type">M</span></span><br><span class="line"><span class="variable">innodb_buffer_pool_size</span> <span class="operator">=</span> 4<span class="type">G</span></span><br><span class="line"><span class="variable">innodb_thread_concurrency</span> <span class="operator">=</span> <span class="number">8</span></span><br><span class="line">#The <span class="keyword">default</span> settings in the MySQL installations in most distributions use conservative buffer sizes and memory usage. Cloudera Management Service roles need high write throughput because they might insert many records in the database. Cloudera recommends that you set the innodb_flush_method property to O_DIRECT.</span><br><span class="line">innodb_flush_method = <span class="type">O_DIRECT</span></span><br><span class="line"><span class="variable">innodb_log_file_size</span> <span class="operator">=</span> 512M</span><br><span class="line"></span><br><span class="line">[mysqld_safe]</span><br><span class="line">log-error=/<span class="keyword">var</span>/log/mysqld.log</span><br><span class="line">pid-file=/<span class="keyword">var</span>/run/mysqld/mysqld.pid</span><br><span class="line">sql_mode=STRICT_ALL_TABLES</span><br></pre></td></tr></table></figure>

<p>初始化mysql</p>
<p>配置my.cnf完成后，启动MySQL;需要注意:</p>
<ol>
<li>因为当前无密码，所以启动时直接回车</li>
<li>Disallow root login remotely 选项需要输入 N , 这样 root 用户才能远程访问</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sudo systemctl enable mysqld</span><br><span class="line"></span><br><span class="line">sudo systemctl start mysqld</span><br><span class="line"></span><br><span class="line">sudo /usr/bin/mysql_secure_installation</span><br><span class="line"></span><br><span class="line">[...]这里需要配置密码，密码可以为简单密码，例如<span class="number">123456</span></span><br><span class="line">Enter current password <span class="keyword">for</span> <span class="title function_">root</span> <span class="params">(enter <span class="keyword">for</span> none)</span>:（这里需要直接回车）</span><br><span class="line">OK, successfully used password, moving on...</span><br><span class="line">[...]</span><br><span class="line">Set root password? [Y/n] Y</span><br><span class="line">New password:（这里输入自定义的密码）</span><br><span class="line">Re-enter <span class="keyword">new</span> <span class="title class_">password</span>:（这里输入自定义的密码）</span><br><span class="line">Remove anonymous users? [Y/n] Y</span><br><span class="line">[...]</span><br><span class="line">Disallow root login remotely? [Y/n] N</span><br><span class="line">[...]</span><br><span class="line">Remove test database and access to it [Y/n] Y</span><br><span class="line">[...]</span><br><span class="line">Reload privilege tables now? [Y/n] Y</span><br><span class="line">All done!</span><br></pre></td></tr></table></figure>

<p>将JDBC的jar放入到&#x2F;usr&#x2F;share&#x2F;java文件夹中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mkdir -p /usr/share/java/</span><br><span class="line"></span><br><span class="line">cd /usr/share/java/</span><br><span class="line">wget https:<span class="comment">//cdh-sgg.oss-cn-shenzhen.aliyuncs.com/fileshare/mysql-connector-java-5.1.46-bin.jar</span></span><br><span class="line"></span><br><span class="line">mv mysql-connector-java-<span class="number">5.1</span><span class="number">.46</span>-bin.jar mysql-connector-java.jar</span><br></pre></td></tr></table></figure>

<p>创建各用户数据库（注意hive的数据库叫metastore）</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>Service</td>
<td>Database</td>
<td>User</td>
</tr>
<tr>
<td>Cloudera Manager Server</td>
<td>scm</td>
<td>scm</td>
</tr>
<tr>
<td>Activity Monitor</td>
<td>amon</td>
<td>amon</td>
</tr>
<tr>
<td>Reports Manager</td>
<td>rman</td>
<td>rman</td>
</tr>
<tr>
<td>Hue</td>
<td>hue</td>
<td>hue</td>
</tr>
<tr>
<td>Hive Metastore Server</td>
<td>metastore</td>
<td>hive</td>
</tr>
<tr>
<td>Sentry Server</td>
<td>sentry</td>
<td>sentry</td>
</tr>
<tr>
<td><del>Cloudera Navigator Audit Server</del></td>
<td><del>nav</del></td>
<td><del>nav</del></td>
</tr>
<tr>
<td><del>Cloudera Navigator Metadata Server</del></td>
<td><del>navms</del></td>
<td><del>navms</del></td>
</tr>
<tr>
<td>Oozie</td>
<td>oozie</td>
<td>oozie</td>
</tr>
</tbody></table>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mysql -u root -p</span><br><span class="line">Enter password:</span><br><span class="line"># 建表及修改权限的格式：</span><br><span class="line">CREATE DATABASE &lt;database&gt; DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">GRANT ALL ON &lt;database&gt;.* TO <span class="string">&#x27;&lt;user&gt;&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;&lt;password&gt;&#x27;</span>;</span><br><span class="line"># 以下是需要执行的SQL语句</span><br><span class="line"># 替换之后需要执行的SQL为：</span><br><span class="line">CREATE DATABASE scm DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE amon DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE rman DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE hue DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE metastore DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE sentry DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE nav DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE navms DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">CREATE DATABASE oozie DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;</span><br><span class="line">GRANT ALL ON scm.* TO <span class="string">&#x27;scm&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;scm&#x27;</span>;</span><br><span class="line">GRANT ALL ON amon.* TO <span class="string">&#x27;amon&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;amon&#x27;</span>;</span><br><span class="line">GRANT ALL ON rman.* TO <span class="string">&#x27;rman&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;rman&#x27;</span>;</span><br><span class="line">GRANT ALL ON hue.* TO <span class="string">&#x27;hue&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;hue&#x27;</span>;</span><br><span class="line">GRANT ALL ON metastore.* TO <span class="string">&#x27;hive&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;hive&#x27;</span>;</span><br><span class="line">GRANT ALL ON sentry.* TO <span class="string">&#x27;sentry&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;sentry&#x27;</span>;</span><br><span class="line">GRANT ALL ON nav.* TO <span class="string">&#x27;nav&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;nav&#x27;</span>;</span><br><span class="line">GRANT ALL ON navms.* TO <span class="string">&#x27;navms&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;navms&#x27;</span>;</span><br><span class="line">GRANT ALL ON oozie.* TO <span class="string">&#x27;oozie&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED BY <span class="string">&#x27;oozie&#x27;</span>;</span><br><span class="line">#####注意此处再授权一个本主机名地址，不然web页面配置很容易出错，注意修改本地主机名hostname</span><br><span class="line">GRANT ALL ON amon.* TO <span class="string">&#x27;amon&#x27;</span>@<span class="string">&#x27;hadoop102&#x27;</span> IDENTIFIED BY <span class="string">&#x27;amon&#x27;</span>;</span><br><span class="line"></span><br><span class="line">FLUSH PRIVILEGES;</span><br><span class="line"></span><br><span class="line">show grants <span class="keyword">for</span> <span class="string">&#x27;amon&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;  </span><br><span class="line">show grants <span class="keyword">for</span> <span class="string">&#x27;rman&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;  </span><br><span class="line">show grants <span class="keyword">for</span> <span class="string">&#x27;hive&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;</span><br><span class="line">show grants <span class="keyword">for</span> <span class="string">&#x27;hue&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;</span><br><span class="line">show grants <span class="keyword">for</span> <span class="string">&#x27;oozie&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;</span><br><span class="line"></span><br><span class="line">quit</span><br><span class="line"></span><br><span class="line">systemctl restart mysql.service</span><br></pre></td></tr></table></figure>

<h2 id="通过yum安装daemons-agent-server"><a href="#通过yum安装daemons-agent-server" class="headerlink" title="通过yum安装daemons,agent,server"></a>通过yum安装daemons,agent,server</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">yum list | grep cloudera-manager</span><br><span class="line">yum -y  install cloudera-manager-daemons cloudera-manager-agent cloudera-manager-server</span><br><span class="line"></span><br><span class="line">yum list | grep cloudera-manager</span><br><span class="line">yum -y  install cloudera-manager-daemons cloudera-manager-agent</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">[root<span class="meta">@hadoop102</span> ~]# sudo /opt/cloudera/cm/schema/scm_prepare_database.sh mysql scm scm</span><br><span class="line">Enter SCM password: (scm)</span><br><span class="line"></span><br><span class="line">JAVA_HOME=/usr/java/jdk1<span class="number">.8</span><span class="number">.0_181</span>-cloudera</span><br><span class="line">Verifying that we can write to /etc/cloudera-scm-server</span><br><span class="line">Creating SCM configuration file in /etc/cloudera-scm-server</span><br><span class="line">Executing:  /usr/java/jdk1<span class="number">.8</span><span class="number">.0_181</span>-cloudera/bin/java -cp /usr/share/java/mysql-connector-java.jar:/usr/share/java/oracle-connector-java.jar:/usr/share/java/postgresql-connector-java.jar:/opt/cloudera/cm/schema/../lib<span class="comment">/* com.cloudera.enterprise.dbutil.DbCommandExecutor /etc/cloudera-scm-server/db.properties com.cloudera.cmf.db.</span></span><br><span class="line"><span class="comment">log4j:ERROR Could not find value for key log4j.appender.Alog4j:ERROR Could not instantiate appender named &quot;A&quot;.</span></span><br><span class="line"><span class="comment">[2021-06-22 21:14:01,762] INFO     0[main] - com.cloudera.enterprise.dbutil.DbCommandExecutor.testDbConnection(DbCommandExecutor.java) - Successfully connected to database.</span></span><br><span class="line"><span class="comment">All done, your SCM database is configured correctly!</span></span><br></pre></td></tr></table></figure>

<h2 id="配置本地Parcel存储库"><a href="#配置本地Parcel存储库" class="headerlink" title="配置本地Parcel存储库"></a>配置本地Parcel存储库</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">systemctl start cloudera-scm-server</span><br><span class="line"># 重启命令为：systemctl start cloudera-scm-server</span><br><span class="line"></span><br><span class="line"># 可以用下面命令查看server的信息</span><br><span class="line">tail -f /<span class="keyword">var</span>/log/cloudera-scm-server/cloudera-scm-server.log</span><br><span class="line"># 另开启一个窗口，监控日志中是否提示启动成功</span><br><span class="line">tail -f /<span class="keyword">var</span>/log/cloudera-scm-server/cloudera-scm-server.log|grep <span class="string">&quot;Started Jetty server&quot;</span></span><br><span class="line"># 也可以用下面命令查看端口号占用情况</span><br><span class="line">netstat -tunlp | grep <span class="number">7180</span></span><br><span class="line"></span><br><span class="line"># 当你看到以下信息时候 证明启动成功</span><br><span class="line">INFO WebServerImpl:com.cloudera.server.cmf.WebServerImpl: Started Jetty server. </span><br><span class="line">下面可能会出现报错信息，这个是因为<span class="number">2021</span>年CDH开始正式收费，从官方网站渠道已经无法拉取到任何安装包和校验文件，所以我们在<span class="number">4.4</span>章节中手动上传文件进行校验。</span><br><span class="line"><span class="number">2022</span>-<span class="number">03</span>-<span class="number">01</span> <span class="number">14</span>:08:<span class="number">23</span>,<span class="number">203</span> ERROR ParcelUpdateService:com.cloudera.parcel.components.ParcelDownloaderImpl: Failed to download manifest. Status code: <span class="number">401</span> URI: https:<span class="comment">//archive.cloudera.com/p/cdh6/6.3.4/parcels/manifest.json</span></span><br><span class="line"><span class="number">2022</span>-<span class="number">03</span>-<span class="number">01</span> <span class="number">14</span>:08:<span class="number">23</span>,<span class="number">203</span> ERROR ParcelUpdateService:com.cloudera.parcel.components.ParcelDownloaderImpl: Could not retrieve repository info <span class="keyword">for</span> repo https:<span class="comment">//archive.cloudera.com/cdh6/6.3/parcels/. Got HTTP response code 401</span></span><br><span class="line"></span><br><span class="line"># 此时登录网页</span><br><span class="line">http:<span class="comment">//你的hadoop102的IP地址:7180</span></span><br><span class="line"># 就能访问CDH集群 </span><br><span class="line">默认的用户名：admin</span><br><span class="line">默认的密码为：admin</span><br></pre></td></tr></table></figure>

<h1 id="参数配置"><a href="#参数配置" class="headerlink" title="参数配置"></a>参数配置</h1><ul>
<li>dfs.client.use.datanode.hostname</li>
<li>yarn.nodemanager.resource.cpu-vcores</li>
<li>yarn.scheduler.maximum-allocation-vcore</li>
<li>yarn.scheduler.maximum-allocation-mb</li>
<li>yarn.nodemanager.resource.memory-mb</li>
<li>spark.dynamicAllocation.enabled</li>
</ul>
<p>调度相关参数设置:</p>
<p>yarn.scheduler.capacity.root.queues<br>yarn.scheduler.capacity.root.capacity<br>yarn.scheduler.capacity.root.spark.capacity<br>yarn.scheduler.capacity.root.hive.capacity</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>flink本地编译</title>
    <url>/2021/12/30/%E6%9C%AA%E5%88%86%E7%B1%BB/flink%E6%9C%AC%E5%9C%B0%E7%BC%96%E8%AF%91/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Hadoop_HA机制</title>
    <url>//article/hadoop_ha1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>HA : High Availablity,高可用<br>高可用最关键的策略是 消除单点故障</p>
</blockquote>
<h1 id="📝-HA"><a href="#📝-HA" class="headerlink" title="📝 HA"></a>📝 HA</h1><p>同时出现两个 active 状态,namenode的术语叫脑裂</p>
<details><summary>防止脑裂</summary>

</details>

<p><img src="/images/posts/hadoop_ha1/img-1.png" alt="Hadoop_HA机制"></p>
<h2 id="问题记录"><a href="#问题记录" class="headerlink" title="问题记录"></a>问题记录</h2><p>怎么保证三台namenode的数据一致:</p>
<ol>
<li>Fsimages : 让一台 nn 生成数据，让其它机器nn同步</li>
<li>Edits:     需要引进新的模块JournalNode来保证edits的文件和数据一致性</li>
</ol>
<p>怎么让同时只有一台nn是active,其它的都是standby</p>
<ul>
<li>手动分配</li>
<li>自动分配</li>
</ul>
<p>2nn在ha架构中并不存在，定期合并fsimage和edtis的活谁来干</p>
<p>由standby的nn来干</p>
<p>如果nn真的发生了问题，怎么让其他的nn上位干活</p>
<ul>
<li>手动故障转移</li>
<li>自动故障转移</li>
</ul>
<h1 id="YARN-HA"><a href="#YARN-HA" class="headerlink" title="YARN HA"></a>YARN HA</h1><p><img src="/images/posts/hadoop_ha1/img-2.png" alt="Hadoop_HA机制"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>MySql执行语句过程</title>
    <url>//article/mysql_process/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>MySql 执行一条 upate,select等语句期间发生了什么</p>
</blockquote>
<h1 id="📝-执行一条Select语句"><a href="#📝-执行一条Select语句" class="headerlink" title="📝 执行一条Select语句"></a>📝 执行一条Select语句</h1><p>可以看到， MySQL 的架构共分为两层：Server 层和存储引擎层，</p>
<ul>
<li>Server 层负责建立连接、分析和执行 SQL。MySQL 大多数的核心功能模块都在这实现，主要包括连接器，查询缓存、解析器、预处理器、优化器、执行器等。另外，所有的内置函数（如日期、时间、数学和加密函数等）和所有跨存储引擎的功能（如存储过程、触发器、视图等。）都在 Server 层实现。</li>
<li>存储引擎层负责数据的存储和提取。支持 InnoDB、MyISAM、Memory 等多个存储引擎，不同的存储引擎共用一个 Server 层。现在最常用的存储引擎是 InnoDB，从 MySQL 5.5 版本开始， InnoDB 成为了 MySQL 的默认存储引擎。我们常说的索引数据结构，就是由存储引擎层实现的，不同的存储引擎支持的索引类型也不相同，比如 InnoDB 支持索引类型是 B+树 ，且是默认使用，也就是说在数据表中创建的主键索引和二级索引默认使用的是 B+ 树索引</li>
</ul>
<p><img src="/images/posts/mysql_process/img-1.png" alt="MySql执行语句过程"></p>
<h2 id="连接器"><a href="#连接器" class="headerlink" title="连接器"></a>连接器</h2><p>连接的过程需要先经过 TCP 三次握手，因为 MySQL 是基于 TCP 协议进行传输的，如果 MySQL 服务并没有启动，则会收到如下的报错：</p>
<p><img src="/images/posts/mysql_process/img-2.png" alt="MySql执行语句过程"></p>
<p>如果 MySQL 服务正常运行，完成 TCP 连接的建立后，连接器就要开始验证你的用户名和密码，如果用户名或密码不对，就收到一个”Access denied for user”的错误，然后客户端程序结束执行:</p>
<p><img src="/images/posts/mysql_process/img-3.png" alt="MySql执行语句过程"></p>
<h2 id="查询缓存"><a href="#查询缓存" class="headerlink" title="查询缓存"></a>查询缓存</h2><p>连接器得工作完成后，客户端就可以向 MySQL 服务发送 SQL 语句了，MySQL 服务收到 SQL 语句后，就会解析出 SQL 语句的第一个字段，看看是什么类型的语句。<br>如果 SQL 是查询语句（select 语句），MySQL 就会先去查询缓存（ Query Cache ）里查找缓存数据，看看之前有没有执行过这一条命令，这个查询缓存是以 key-value 形式保存在内存中的，key 为 SQL 查询语句，value 为 SQL 语句查询的结果。<br>如果查询的语句命中查询缓存，那么就会直接返回 value 给客户端。如果查询的语句没有命中查询缓存中，那么就要往下继续执行，等执行完后，查询的结果就会被存入查询缓存中。<br>这么看，查询缓存还挺有用，但是其实查询缓存挺鸡肋的。<br>对于更新比较频繁的表，查询缓存的命中率很低的，因为只要一个表有更新操作，那么这个表的查询缓存就会被清空。如果刚缓存了一个查询结果很大的数据，还没被使用的时候，刚好这个表有更新操作，查询缓冲就被清空了，相当于缓存了个寂寞。<br>所以，MySQL 8.0 版本直接将查询缓存删掉了，也就是说 MySQL 8.0 开始，执行一条 SQL 查询语句，不会再走到查询缓存这个阶段了。<br>对于 MySQL 8.0 之前的版本，如果想关闭查询缓存，我们可以通过将参数 query_cache_type 设置成 DEMAND。<br>TIP<br>这里说的查询缓存是 server 层的，也就是 MySQL 8.0 版本移除的是 server 层的查询缓存，并不是 Innodb 存储引擎中的 buffer poo</p>
<h2 id="解析SQL"><a href="#解析SQL" class="headerlink" title="解析SQL"></a>解析SQL</h2><p>在正式执行 SQL 查询语句之前， MySQL 会先对 SQL 语句做解析，这个工作交由「解析器」来完成</p>
<h3 id="解析器"><a href="#解析器" class="headerlink" title="解析器"></a>解析器</h3><h1 id="执行SQL"><a href="#执行SQL" class="headerlink" title="执行SQL"></a>执行SQL</h1><p>经过解析器后，接着就要进入执行 SQL 查询语句的流程了，每条<code>SELECT</code> 查询语句流程主要可以分为下面这三个阶段：</p>
<ul>
<li>prepare 阶段，也就是预处理阶段；</li>
<li>optimize 阶段，也就是优化阶段；</li>
<li>execute 阶段，也就是执行阶段</li>
</ul>
<h3 id="预处理器"><a href="#预处理器" class="headerlink" title="预处理器"></a>预处理器</h3><h3 id="优化器"><a href="#优化器" class="headerlink" title="优化器"></a>优化器</h3><p>经过预处理阶段后，还需要为 SQL 查询语句先制定一个执行计划，这个工作交由「优化器」来完成的。</p>
<p>优化器主要负责将 SQL 查询语句的执行方案确定下来，比如在表里面有多个索引的时候，优化器会基于查询成本的考虑，来决定选择使用哪个索引。</p>
<p>当然，我们本次的查询语句（select * from product where id &#x3D; 1）很简单，就是选择使用主键索引。</p>
<p>要想知道优化器选择了哪个索引，我们可以在查询语句最前面加个 <code>explain</code> 命令，这样就会输出这条 SQL 语句的执行计划，然后执行计划中的 key 就表示执行过程中使用了哪个索引，比如下图的 key 为 <code>PRIMARY</code> 就是使用了主键索引。</p>
<p><img src="/images/posts/mysql_process/img-4.png" alt="MySql执行语句过程"></p>
<p>如果查询语句的执行计划里的 key 为 null 说明没有使用索引，那就会全表扫描（type &#x3D; ALL），这种查询扫描的方式是效率最低档次的，如下图：</p>
<p><img src="/images/posts/mysql_process/img-5.png" alt="MySql执行语句过程"></p>
<p>这张 product 表只有一个索引就是主键，现在我在表中将 name 设置为普通索引（二级索引）。</p>
<p><img src="/images/posts/mysql_process/img-6.png" alt="MySql执行语句过程"></p>
<p>这时 product 表就有主键索引（id）和普通索引（name）。假设执行了这条查询语句：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="built_in">id</span> from product <span class="built_in">where</span> <span class="built_in">id</span> &gt; 1  and name like <span class="string">&#x27;i%&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>这条查询语句的结果既可以使用主键索引，也可以使用普通索引，但是执行的效率会不同。这时，就需要优化器来决定使用哪个索引了。<br>很显然这条查询语句是覆盖索引，直接在二级索引就能查找到结果（因为二级索引的 B+ 树的叶子节点的数据存储的是主键值），就没必要在主键索引查找了，因为查询主键索引的 B+ 树的成本会比查询二级索引的 B+ 的成本大，优化器基于查询成本的考虑，会选择查询代价小的普通索引。<br>在下图中执行计划，我们可以看到，执行过程中使用了普通索引（name），Exta 为 Using index，这就是表明使用了覆盖索引优化。</p>
<p><img src="/images/posts/mysql_process/img-7.png" alt="MySql执行语句过程"></p>
<h1 id="一条Update语句"><a href="#一条Update语句" class="headerlink" title="一条Update语句"></a>一条Update语句</h1><p><img src="/images/posts/mysql_process/img-8.png" alt="MySql执行语句过程"></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li><a href="https://xiaolincoding.com/mysql/">https://xiaolincoding.com/mysql/</a></li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>Kafka核心参数</title>
    <url>//article/post-1e000916fa/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-主旨内容"><a href="#📝-主旨内容" class="headerlink" title="📝 主旨内容"></a>📝 主旨内容</h1><h2 id="观点1"><a href="#观点1" class="headerlink" title="观点1"></a>观点1</h2><blockquote>
<p>引用的话语</p>
</blockquote>
<h2 id="观点2"><a href="#观点2" class="headerlink" title="观点2"></a>观点2</h2><blockquote>
<p>引用的话语</p>
</blockquote>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>大数据组件知识_记录</title>
    <url>//article/post-27c2a1ce38/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>大数据组件需学知识记录</p>
</blockquote>
<h1 id="📝-Zookeeper"><a href="#📝-Zookeeper" class="headerlink" title="📝 Zookeeper"></a>📝 Zookeeper</h1><ul>
<li>选举机制</li>
<li>使用场景</li>
<li>Paxos算法</li>
<li>CAP原则</li>
</ul>
<h1 id="📝-Kafka"><a href="#📝-Kafka" class="headerlink" title="📝 Kafka"></a>📝 Kafka</h1><ul>
<li>基础架构</li>
<li>副本数</li>
<li>压测</li>
<li>台数</li>
<li>分区数</li>
<li>保存策略</li>
<li>消费者策略</li>
<li>监控</li>
<li>topic数</li>
<li>数据量</li>
<li>积压&#x2F;borker&#x2F;数据重复&#x2F;kafka宕机&#x2F;可重复&#x2F;高效读写</li>
</ul>
<h1 id="📝-Hive"><a href="#📝-Hive" class="headerlink" title="📝 Hive"></a>📝 Hive</h1><ul>
<li>客户端，JDBC，编译器，优化器，执行器</li>
<li>内部表，外部表</li>
<li>order by ： 全局排序    sort by ： 分区内排序   distribute by ： 分区      clsuter by 相当于 distribute by &amp; sort by 相同字段</li>
<li>系统函数</li>
<li>自定义函数</li>
<li>Hive优化</li>
<li>数据倾斜</li>
</ul>
<h1 id="📝-Spark"><a href="#📝-Spark" class="headerlink" title="📝 Spark"></a>📝 Spark</h1><ul>
<li>部署模式</li>
<li>RDD</li>
<li>计算分区&#x2F;计算逻辑&#x2F;血缘依赖&#x2F;分区器&#x2F;移动数据</li>
<li>共享变量: 累加器&#x2F;广播变量&#x2F;cache&amp;Checkpoint</li>
<li>算子</li>
<li>手动重分区</li>
<li>行动算子</li>
<li>几种划分： 划分job&#x2F;划分stage&#x2F;划分task set&#x2F;划分task</li>
<li>宽窄依赖</li>
<li>SparkSQL: hive on spark &#x2F; spark on hive</li>
<li>Spark Streaming</li>
</ul>
<h1 id="📝-Flink"><a href="#📝-Flink" class="headerlink" title="📝 Flink"></a>📝 Flink</h1><ul>
<li>部署模式： standalone &#x2F; yarn &#x2F; k8s &#x2F; mesos</li>
<li>基本算子:  map&#x2F;flatmap&#x2F;filter&#x2F;connect&#x2F;union&#x2F;shffled&#x2F;rebalance&#x2F;rescale</li>
<li>keyBy算子</li>
<li>连接算子:  connect&#x2F;union</li>
<li>interval join</li>
<li>WaterMark</li>
<li>窗口</li>
<li>端到端的一致性</li>
<li>Flink CEP</li>
<li>Flink SQL</li>
<li>Flink优化,反压,数据倾斜</li>
</ul>
<h1 id="数据调度"><a href="#数据调度" class="headerlink" title="数据调度"></a>数据调度</h1><ul>
<li>Azkaban</li>
<li>海豚调度器</li>
</ul>
<h1 id="数据治理"><a href="#数据治理" class="headerlink" title="数据治理"></a>数据治理</h1><ul>
<li>元数据管理: altas</li>
<li>数据治理:  1.数据治理监控  2.重复数据监控 3.空值监控</li>
</ul>
<p>指标: 完整行，及时性，重复性，一致性，合规性，准确性</p>
<details><summary>描述事情</summary>

</details>

<h1 id="权限管理"><a href="#权限管理" class="headerlink" title="权限管理"></a>权限管理</h1><ul>
<li>sentry</li>
<li>ranger</li>
</ul>
<h1 id="离线数仓"><a href="#离线数仓" class="headerlink" title="离线数仓"></a>离线数仓</h1><p>ods: 分区表,避免全表扫描;压缩;保持数据原貌,起到备份作用</p>
<p>dwd+dim: 数据清洗,分区表,压缩,列式存储parquet,维度退化</p>
<p>dws: 对一段时间指标的积累,避免重复计算,开始时间,结束时间,这段时间</p>
<p>ads: 指标(活跃,复购,GMV,转换,留存,拉新)</p>
<details><summary>事实表</summary>

</details>

<details><summary>同步策略</summary>

</details>

<h1 id="实时数仓"><a href="#实时数仓" class="headerlink" title="实时数仓"></a>实时数仓</h1><p>ods: MaxWell,到kafka的topic来</p>
<p>dwd+dim: 测输出流,分topic;</p>
<p>业务数据:</p>
<ul>
<li>事实表: 存kafka,动态分流(1.MySql创建一张配置表,定制了那些表要去哪里;2.FlinkCDC读取配置表,广播出去⇒动态分流)</li>
<li>维度表: 存Hbase,Kafka不支持随机查询,hbase支持随机查询,长远考虑,没有做维度退化</li>
</ul>
<p>dwm层: 预先处理的宽表,过滤层,避免拉宽表的时候,重复计算事实表与事实表的关联</p>
<ul>
<li>事实表与事实表: 双流 join ⇒ interval join</li>
<li>事实表与维度表: 通过Phoenix查询HBase, 优化一: 旁路缓存, 优化二:异步IO</li>
</ul>
<p>dws层: 拉宽表,join查维度;宽表存clickhouse</p>
<p>ads层: </p>
<p>监控:</p>
<ul>
<li>kafka: eagle</li>
<li>Flink: prometheus + grafana</li>
<li>clickhouse: prometheus + grafana</li>
</ul>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>摄影笔记</title>
    <url>//article/post-fd4d0ada97/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>看摄影文章进行一个笔记记录<br>一个合格的摄影师可以恰当利用镜头虚化所带来的视觉效果,主动控制虚化范围</p>
</blockquote>
<p>文件格式:</p>
<ul>
<li>RAW: 相机能记录的不加任何渲染的原始图片格式</li>
<li>JPEG: 相机经过自己的系统和算法算出来的图片</li>
</ul>
<h1 id="📝-光圈"><a href="#📝-光圈" class="headerlink" title="📝 光圈"></a>📝 光圈</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>光圈是镜头里用来控制镜头孔径大小的部件</p>
<p>孔越小,通过的光自然就越少,相机的感光原件接收到的光少了,画面就会越暗;孔越大,通光量越大,画面就越亮</p>
<p>f值小的是大光圈,f值大的是小光圈</p>
<p><img src="/images/posts/post-fd4d0ada97/img-1.png" alt="摄影笔记"></p>
<p>在不考虑镜头焦段的情况下,同一枚镜头,光圈越大,景深越浅(照片中清晰的范围越小);光圈越小,景深越大(照片中清晰的范围越大)</p>
<p>看背景后的问题;可以看到f越大,背景其越清晰</p>
<p><img src="/images/posts/post-fd4d0ada97/img-2.png" alt="摄影笔记"></p>
<p>物距: 相机离物理的距离;物距越近,景深也就越浅</p>
<p>长焦镜头: 压缩画面,压缩空间感</p>
<p>焦段越长,视角越窄,景深越浅,透视畸变越弱;焦段越短,视角越宽,景深越大,透视畸变越强</p>
<h1 id="📝-ISO"><a href="#📝-ISO" class="headerlink" title="📝 ISO"></a>📝 ISO</h1><p>感光度: 感光度就是相机对光的敏感程度; ISO其实是把ASA和DIN捏合在一起的一种计量单位</p>
<details><summary>比如</summary>

</details>

<p>拉高ISO的代价就是会让画面产生噪点,过高的ISO会直接影响画面的纯净度</p>
<p><img src="/images/posts/post-fd4d0ada97/img-3.png" alt="摄影笔记"></p>
<p>白平衡:</p>
<p>当我们在昏黄的灯光下拍一张白纸，白纸因为灯光照射的原因，会是黄色的；我们在日落之后拍一张白纸，白纸因为天空光线的原因，大概率会是蓝色的。虽然都是白纸，但在不同的光线条件下，它会变成不同的颜色，而相机上的白平衡就是用来平衡这个白色的，让白色在任何情况下都是白色。当白色是准确的白色，而不是乳白、象牙白、冷白、粉白这些飘忽不定的白色，其他颜色也就会跟着该是什么颜色就是什么颜色，以此来矫正一张照片的色温，防止照片偏色</p>
<p><img src="/images/posts/post-fd4d0ada97/img-4.png" alt="摄影笔记"></p>
<p>宽容度:</p>
<p>指的是感光元件所能正确容纳的画面亮度反差的范围,说白了就是,画面最亮的部分和画面最暗的部分,能不能被相机以不损失成像细节的方式给拍下来</p>
<p>如果能,叫宽容度大;如果不能,扩容度小</p>
<p>HDR:高动态范围成像</p>
<p>亮的地方不过曝,暗的地方不死黑;实现的基本原理,在相机上,都是靠多帧合成</p>
<h1 id="📝-测光"><a href="#📝-测光" class="headerlink" title="📝 测光"></a>📝 测光</h1><p>光比:</p>
<p>照明环境下,被摄物体暗面与亮面的受光比例</p>
<p>光比越大,明暗反差也就越大,也就是俗称的”对比度”越高,画面中明亮的部分和阴影部分之间的对比就越明显;光比越小,明暗反差就越小,对比度越低,画面中明亮部分和阴影部分之间的对比就越不明显</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>bin/yarn-session.sh -nm testFlink_流批基础入门</title>
    <url>//article/post-fe71009a9d/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>Flink基础知识入门案例</p>
</blockquote>
<h1 id="📝-基础知识"><a href="#📝-基础知识" class="headerlink" title="📝 基础知识"></a>📝 基础知识</h1><p>Flink分布式处理引擎，用于有界和无界数据流进行有状态计算</p>
<details><summary>Flink特点</summary>

</details>

<details><summary>无界数据流：</summary>

</details>

<details><summary>有界数据流：</summary>

</details>

<p>把流处理需要的额外数据保存成一个“状态”，然后针对这条数据进行处理，并且更新状态，这就是有所的有状态的流处理</p>
<ul>
<li>状态在内存中：优点，速度快;缺点：可靠性差</li>
<li>状态在分布式中：优点：可靠性高;缺点：速度慢</li>
</ul>
<h1 id="📝-Flink部署"><a href="#📝-Flink部署" class="headerlink" title="📝 Flink部署"></a>📝 Flink部署</h1><ul>
<li>客户端(Client): 代码由客户端获取并作转换，之后交给JobManager</li>
<li>JobManager是Flink集群里的管事人，对作业进行中央调度管理;而它获取到要执行的作业后，会进一步处理转换，然后分发给真正的TaskManager</li>
<li>TaskManager：真正干活的人，数据的处理操作都是他们来做的</li>
</ul>
<h2 id="Session-Mode-会话模式"><a href="#Session-Mode-会话模式" class="headerlink" title="Session Mode(会话模式)"></a>Session Mode(会话模式)</h2><p>先启动一个集群，然后保持会话，在这个会话中通过客户端提交作业。集群启动时所有的资源都已经确定了，所有提交的作业会竞争集群中的资源</p>
<p>会话模式比较适合单个规模小，执行时间短的大量作业</p>
<p><img src="/images/posts/post-fe71009a9d/img-1.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h2 id="单作业任务-Per-Job-Mode"><a href="#单作业任务-Per-Job-Mode" class="headerlink" title="单作业任务(Per-Job Mode)"></a>单作业任务(Per-Job Mode)</h2><p>回话模式因为资源共享会导致很多问题，所以为了更好的资源隔离，考虑为每个提交的作业启动一个集群，这就是所谓的Per-Job模式</p>
<p>作业完成后，集群就会关闭，所有资源就会释放</p>
<p>这些特性使得单作业模式在生产环境更加稳定，所以是实际应用的首选模式</p>
<p>Flink本身无法直接这样的运行，所以单作业模式一般需要借助一些资源管理框架来启动集群，比如YARN，K8S等</p>
<p><img src="/images/posts/post-fe71009a9d/img-2.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h2 id="应用模式"><a href="#应用模式" class="headerlink" title="应用模式"></a>应用模式</h2><p>会话模式和单作业模式下，代码都是在客户端上执行;然后由客户端提交到JobManager的;这种方式客户端需要占用大量的网络宽带，去下载依赖和二进制数据发送给JobManager;加上很多情况下我们提交的作业用的是同一个客户端，就会加中客户端所有节点的损耗</p>
<p>所以我们不要客户端了，直接把应用提交到JobManager上运行。</p>
<p>我们需要为每一个提交的应用单独启动JobManager，也就是创建一个集群。</p>
<p>这个JobManager只为执行这一个应用而存在，执行结束之后JobManager关闭了，这就是所谓的应用模式</p>
<p><img src="/images/posts/post-fe71009a9d/img-3.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h1 id="Yarn运行模式"><a href="#Yarn运行模式" class="headerlink" title="Yarn运行模式"></a>Yarn运行模式</h1><p>客户端把Flink应用提交给Yarn的ResourceManager，Yarn的ResourceManager会向Yarn申请容器;在这些容器里，Flink会部署JobManager和TaskManager的实体，从而启动集群</p>
<p>Flink会根据运行在JobManager的作业所需要的slot数量动态的分配TaskManager资源</p>
<h1 id="部署方式"><a href="#部署方式" class="headerlink" title="部署方式"></a>部署方式</h1><h2 id="会话模式部署"><a href="#会话模式部署" class="headerlink" title="会话模式部署"></a>会话模式部署</h2><p>Yarn的会话模式与独立集群有不同，需要首先申请一个Yarn会话(Yarn Session)来启动集群</p>
<p>执行脚本向Yarn申请资源，开启一个Yarn会话</p>
<details><summary>参数解读</summary>

</details>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/yarn-session.sh -nm test</span><br></pre></td></tr></table></figure>

<p>将Jar上传到集群，通过指令对该任务进行提交</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink run -c com.atguigu.wc.SocketStreamWordCount FlinkTutorial-<span class="number">1.0</span>-SNAPSHOT.jar</span><br></pre></td></tr></table></figure>

<h2 id="单作业模式"><a href="#单作业模式" class="headerlink" title="单作业模式"></a>单作业模式</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink run -d -t yarn-per-job -c com.atguigu.wc.SocketStreamWordCount FlinkTutorial-<span class="number">1.0</span>-SNAPSHOT.jar</span><br></pre></td></tr></table></figure>

<details><summary>如果有错误</summary>

</details>

<p>可以使用命令查看和取消任务</p>
<p>查看: application_XXXX_YY是当前应用的ID</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink list -t yarn-per-job -Dyarn.application.id=application_XXXX_YY</span><br></pre></td></tr></table></figure>

<p>取消: <jobId>是作业的ID</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink cancel -t yarn-per-job -Dyarn.application.id=application_XXXX_YY &lt;jobId&gt;</span><br></pre></td></tr></table></figure>

<h2 id="应用模式部署"><a href="#应用模式部署" class="headerlink" title="应用模式部署"></a>应用模式部署</h2><p>应用模式同样非常简单，与单作业模式类似，直接执行flink run-application命令即可</p>
<p>执行命令提交作业：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink run-application -t yarn-application -c com.atguigu.wc.SocketStreamWordCount FlinkTutorial-<span class="number">1.0</span>-SNAPSHOT.jar</span><br></pre></td></tr></table></figure>

<p>在命令中查看或取消作业</p>
<p>查看：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink list -t yarn-application -Dyarn.application.id=application_XXXX_YY</span><br></pre></td></tr></table></figure>

<p>取消：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/flink cancel -t yarn-application -Dyarn.application.id=application_XXXX_YY &lt;jobId&gt;</span><br></pre></td></tr></table></figure>

<details><summary>上传jar包等环境到hdfs</summary>

</details>

<h2 id="历史服务器"><a href="#历史服务器" class="headerlink" title="历史服务器"></a>历史服务器</h2><p>运行Flink Job的集群一旦停止，只能去Yarn或者本地磁盘上查看日志，不再可以查看作业挂掉之前的运行的Web UI,很难清楚之前作业挂的那一刻发送了什么</p>
<p>1：创建存储目录</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">hadoop fs -mkdir -p /logs/flink-job</span><br></pre></td></tr></table></figure>

<p>2：在flink-config.yml中添加如下配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">jobmanager.archive.fs.dir: hdfs:<span class="comment">//hadoop102:8020/logs/flink-job</span></span><br><span class="line">historyserver.web.address: hadoop102</span><br><span class="line">historyserver.web.port: <span class="number">8082</span></span><br><span class="line">historyserver.archive.fs.dir: hdfs:<span class="comment">//hadoop102:8020/logs/flink-job</span></span><br><span class="line">historyserver.archive.fs.refresh-interval: <span class="number">5000</span></span><br></pre></td></tr></table></figure>

<p>3：启动历史服务器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/historyserver.sh start</span><br></pre></td></tr></table></figure>

<p>4：停止历史服务器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bin/historyserver.sh stop</span><br></pre></td></tr></table></figure>

<h1 id="Flink运行架构"><a href="#Flink运行架构" class="headerlink" title="Flink运行架构"></a>Flink运行架构</h1><p><img src="/images/posts/post-fe71009a9d/img-4.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h2 id="JobManager：作业管理器"><a href="#JobManager：作业管理器" class="headerlink" title="JobManager：作业管理器"></a>JobManager：作业管理器</h2><p>JobManager是一个Flink集群中任务管理和调度的核心，是控制应用执行的主进程。也就是，每个应用都应该被唯一的JobManager所控制执行</p>
<h3 id="JobMaster"><a href="#JobMaster" class="headerlink" title="JobMaster"></a>JobMaster</h3><p>JobMaster是JobManager的核心组件，负责处理单独的作业（Job）;所以 JobMaster和具体的Job是一一对应的，多个Job可以同时运行在一个Flink集群中，每个Job都有自己的JobMaster</p>
<p>早期的Flink版本中，没有JobMaster的概念，而JobManager的概念范围较小，实际指的就是现在所说的JobMaster</p>
<p>作业提交时，JobMaster会接收到要执行的应用。JobMaster会把JobGraph转换成一个物理层面的数据流图，这个图被叫做“执行图”（ExecutionGraph）,它包含了所有可以并发执行的任务</p>
<p>JobMaster会向资源管理器(ResourceManager)发出请求，申请执行任务必要的资源</p>
<p>一旦获取到足够的资源，就会将执行图分发到真正运用它们的TaskManager上</p>
<p>而在运行过程中，JobMaster会负责所有需要中央协调的操作，比如说检查点（checkpoints）的协调</p>
<h3 id="ResourceManager"><a href="#ResourceManager" class="headerlink" title="ResourceManager"></a>ResourceManager</h3><p>主要负责资源的分配和管理，在flink集群中只有一个;所谓“资源”，主要是指TaskManager的任务槽(task slots)</p>
<p>任务槽就是Flink集群中的资源调配单元，包含了机器用来执行计算的一组CPU和内存资源;每个task都需要分配到一个slot上执行</p>
<h3 id="Dispatcher分发器"><a href="#Dispatcher分发器" class="headerlink" title="Dispatcher分发器"></a>Dispatcher分发器</h3><p>Dispather主要负责提供一个REST接口，用来提交应用，并且负责为每一个新提交的作业启动一个新的JobManager组件</p>
<p>Dispatcher也会启动一个Web UI，用来方便地展示和监控作业执行的信息。Dispatcher在架构中并不是必需的，在不同的部署模式下可能会被忽略掉</p>
<h2 id="TaskManager（任务管理器）"><a href="#TaskManager（任务管理器）" class="headerlink" title="TaskManager（任务管理器）"></a>TaskManager（任务管理器）</h2><p>TaskManager是Flink中的工作进程，数据流的具体计算就是它来做的;Flink集群必须有一个TaskManager，每个TaskManager都包含了一定数量的任务槽（task slots）。slot是资源调度最小单位，slot的数量限制了TaskManager能够并行处理的任务数量</p>
<p>启动之后，taskManager会向资源管理器注册它的slots，收到资源管理器的指令后，TaskManager就会将一个或多个槽位提供给JobManager调用，JobManager就可以分配任务来执行</p>
<p>在执行过程中，TaskManager可以缓冲数据，还可以跟其他运行同一应用的TaskManager交换数据</p>
<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="并行度"><a href="#并行度" class="headerlink" title="并行度"></a>并行度</h3><p>当处理的数据量特别大的时候，可以把一个算子操作，“复制”到多个节点上，数据来了之后就可以到其中任意一个来执行。这样一来，一个算子任务就被拆分成了多个并行的“子任务”（subTask），再将它们分发到不同节点，就真正实现了并行计算</p>
<p>一个特定的算子的子任务(subTask)的个数被称之为并行度(parallelism)</p>
<p>包含并行子任务的数据，就是并行数据流，它需要多个分区(stream partition)来分配并行任务</p>
<p><img src="/images/posts/post-fe71009a9d/img-5.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<p>并行设置</p>
<ul>
<li>stream.map(word -&gt; Tuple2.of(word, 1L)).setParallelism(2); 代码中进行设置</li>
<li>bin&#x2F;flink run –p 2 –c com.atguigu.wc.SocketStreamWordCount .&#x2F;FlinkTutorial-1.0-SNAPSHOT.jar 提交应用的时候指定</li>
<li>flink-conf.yaml 中 parallelism.default: 2 配置文件修改指定</li>
</ul>
<h3 id="算子链"><a href="#算子链" class="headerlink" title="算子链"></a>算子链</h3><p>算子间的数据传输</p>
<p>一个数据在算子之间传输数据的形式可以是一对一的(one to one)的直通(forwarding),也可以是重分区（redistributing）模式</p>
<details><summary>一对一模式</summary>

</details>

<details><summary>重分区</summary>

</details>

<p><img src="/images/posts/post-fe71009a9d/img-6.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h3 id="合并算子链"><a href="#合并算子链" class="headerlink" title="合并算子链"></a>合并算子链</h3><p>Flink中，并行度相同的一对一(one to one)算子操作，可以直接链接在一起形成一个”大”的任务（task），这样原来的原子就成为了真正任务里的一部分。</p>
<p>每个task会被一个线程执行，这样的技术成为“算子链”</p>
<p>将算子链接成task是非常有效的优化，可以减少线程之间的切换和基于缓存区的数据交换，在减少时延的同时提升吞吐量</p>
<p><img src="/images/posts/post-fe71009a9d/img-7.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<h2 id="任务槽（Task-Slots）"><a href="#任务槽（Task-Slots）" class="headerlink" title="任务槽（Task Slots）"></a>任务槽（Task Slots）</h2><p>Flink中的TaskManager都是一个JVM进程，它可以启动多个独立的线程，来并行的执行多个子任务(subTask)</p>
<p>TaskManager的计算资源是有限的，并行的任务越多，每个线程的资源就会越少。</p>
<p>为了控制并发量，我们需要在TaskManager上对每个任务运行所占用的资源作出明确的划分，这就是所谓的任务槽(task slots)</p>
<p>每个task slot其实表示了TaskManager拥有计算资源的一个固定大小的子集。这些资源就是用来独立执行一个子任务的</p>
<p><img src="/images/posts/post-fe71009a9d/img-8.png" alt="bin&#x2F;yarn-session.sh -nm testFlink_流批基础入门"></p>
<details><summary>任务槽配置</summary>

</details>

<details><summary>任务槽和并行度</summary>

</details>

<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>x-ui翻墙安装</title>
    <url>//article/x_ui_1/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>利用 x-ui 在进行购买的外国服务器进行翻 </p>
</blockquote>
<h1 id="📝-主旨内容"><a href="#📝-主旨内容" class="headerlink" title="📝 主旨内容"></a>📝 主旨内容</h1><p>Vultr注册链接：<a href="https://www.vultr.com/?ref=8753714">https://www.vultr.com/?ref=8753714</a></p>
<p>VPS服务器系统选择 Debian 10 x64</p>
<p>X-UI 安装代码：<br>wget -N <a href="https://gitlab.com/rwkgyg/x-ui-yg/raw/main/install.sh">https://gitlab.com/rwkgyg/x-ui-yg/raw/main/install.sh</a> &amp;&amp; bash in<br><a href="http://stall.sh/">stall.sh</a></p>
<p>#放行端口<br>iptables -I INPUT -p tcp –dport 443 -j ACCEPT</p>
<p>FinalShell下载：<a href="https://wa6.lanzoui.com/ihjg2y3h14j">https://wa6.lanzoui.com/ihjg2y3h14j</a><br>FinalShell备用下载（含MAC版）：<a href="http://www.hostbuf.com/t/988.html">http://www.hostbuf.com/t/988.html</a><br>科学上网软件下载：<a href="https://github.com/Kejifaxian/welcome/">https://github.com/Kejifaxian/welcome/</a><br>订阅转换：<a href="https://acl4ssr-sub.github.io/">https://acl4ssr-sub.github.io/</a></p>
<p>Vultr注册链接：<a href="https://www.秃鹫/">https://www.秃鹫</a>。。.com&#x2F;？ref&#x3D;8753714</p>
<p>VPS服务器系统选择 Debian 10 x64</p>
<p>X-UI 安装代码：<br>wget -N <a href="https://gitlab.com/rwkgyg/x-ui-yg/raw/main/install.sh&&">https://gitlab.com/rwkgyg/x-ui-yg/raw/main/install.sh&amp;&amp;</a> bash 安<br>装。sh</p>
<p>#放行端口<br>iptables -I INPUT -p tcp –dport 443 -j ACCEPT</p>
<p>FinalShell下载：<a href="https://wa6.兰祖伊/">https://wa6.兰祖伊</a>。com&#x2F;ihjg2y3h14j<br>FinalShell备用下载（含MAC版）：<a href="http://www.霍斯布夫/">http://www.霍斯布夫</a>。COM&#x2F;T&#x2F;988.htm<br>l<br>科学上网软件下载：<a href="https://github.com/Kejifaxian/welcome/">https://github.com/Kejifaxian/welcome/</a><br>订阅转换：<a href="https://acl4ssr-s/">https://acl4ssr-s</a></p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>戴尔笔记本售后</title>
    <url>//article/dell_after_sales/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>一个简单的开头,简述这篇文章讨论的问题、目标、人物、背景是什么？并简述你给出的答案。</p>
</blockquote>
<blockquote>
<p>可以说说你的故事：阻碍、努力、结果成果，意外与转折。</p>
</blockquote>
<h1 id="📝-说明"><a href="#📝-说明" class="headerlink" title="📝 说明"></a>📝 <strong>说明</strong></h1><p> 先要说明下, 这是一个悲伤的故事开始. 也不晓得从什么时候开始, 我用公司的笔记本突然坏了几个按键，这几个按键的位置也是奇怪. 分别是 BN长空格右侧的2按键</p>
<p> 讲道理,在这之前,我都不清楚售后这么牛逼的 , 还是太缺乏生活经验所导致</p>
<p> 主要是中间还遇见一个贼有一个的事情, 这事情让我对 “百度” 这款索搜引擎所严重吐槽. 奉劝大家用百度查询什么啊，一定只认证官网。而且，现在一些官网，千万不走要 “百度” , 走官网的公众号什么都可以，意思就是直接对标官网即可</p>
<p> 不过，像百度这种,存在的价值也很好，给你成长不至于以后还被坑，有严重的意义</p>
<h1 id="坑"><a href="#坑" class="headerlink" title="坑"></a>坑</h1><p> 由于没有经验，我就百度 , DELL 官方售后，我不清楚小伙伴们有没有在手机浏览器上百度这种情况，如果有的话，会发现没有特别明显的广告，而且我点击的明明就是 DELL 的官网，哪儿晓得给我跳到一个其他网站的链接地址去，还给你窗口聊天，看着蛮像的，然后你问他是不是官网售后，贼有意思的，他居然说是</p>
<p> 等我到哪儿去的时候，我就发现不对劲，压根就没看到任何和DELL有关的东西，而且他们看电脑什么，都是不让你人进去的，我就瞬间晓得有问题</p>
<p> 果不其然，直接说笔记本什么有问题啊乱七八槽的，然后就说换键盘要700+，我就瞬间就走了，没任何停留的兴趣</p>
<p> 然后第二天，我通过笔记本上的扫码，联系上了 DELL 的售后，去维修，态度好，一毛钱没花(保修内), 只能说保修内的售后是真的舒服。 这次亲身的体验，告诉了我： 主要是生活经验不足，其次差点被 “百度” 搜索出来的东西给骗了，不过没足够了我的生活成长经验，还是蛮感谢百度的</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<p> 最后这里总结下最后的感觉，买东西，特别是电子产品这东西，保修啊&#x2F;好的售后啊，这种东西是很重要的，千万不要觉得不重要啊&#x2F;我可能用不上啊，等你用得上的时候，就来不急了</p>
<p>最后的最后，感谢百度给了我这次成长经验</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>生活碎碎念</category>
      </categories>
      <tags>
        <tag>生活碎碎念</tag>
      </tags>
  </entry>
  <entry>
    <title>生活碎碎念_多做少想</title>
    <url>//article/more_do_less_think/</url>
    <content><![CDATA[<blockquote>
<p>😀 这是一篇记录生活的碎碎念：<br>欢迎你阅读我的碎碎念,非常感谢<br>稍微学会下记录生活或者想法，毕竟年纪大了，很容易忘记一些事包括之前一些有的没得</p>
</blockquote>
<h1 id="📝-多做少想的实际行动"><a href="#📝-多做少想的实际行动" class="headerlink" title="📝 多做少想的实际行动"></a>📝 多做少想的实际行动</h1><p>想多了全是问题，做多了全是答案。想得再多都是焦虑，做得再少也算进步</p>
<details><summary>个人含义理解</summary>

</details>

<h1 id="学会主动远离一些人，告别自己的恋爱脑"><a href="#学会主动远离一些人，告别自己的恋爱脑" class="headerlink" title="学会主动远离一些人，告别自己的恋爱脑"></a>学会主动远离一些人，告别自己的恋爱脑</h1><h2 id="远离那些让你内耗的人"><a href="#远离那些让你内耗的人" class="headerlink" title="远离那些让你内耗的人"></a>远离那些让你内耗的人</h2><p>经历了漫长的一段时间的自我内耗，就搁那么一丢丢给自己整没了;索性好在突然的醒悟;从醒悟到现在差不多快2个月了吧，坚持着做许多自己当初没做的事情</p>
<p>去慢慢逐渐告别了内耗</p>
<h2 id="远离那些背叛过的人"><a href="#远离那些背叛过的人" class="headerlink" title="远离那些背叛过的人"></a>远离那些背叛过的人</h2><p>此处借助张爱玲的一句话，来完全诠释这个二级标题的含义：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">人家撇下你的那一刻，一定是觉得某个瞬间没有你，他会生活得更好，那一刻，一辈子都不值得原谅</span><br></pre></td></tr></table></figure>

<p>所以对于有过背叛的人，只会给你非常清醒的上一课然后外加pua并且他已经走出教室，而空留你在座位上不断以我怀疑;于是也请你自己大胆承认这一切,大大方方的走出来，去找寻自己</p>
<h2 id="远离那些故意激怒你的人"><a href="#远离那些故意激怒你的人" class="headerlink" title="远离那些故意激怒你的人"></a>远离那些故意激怒你的人</h2><p>人在最初相识的时候，总是带着好奇和新鲜感并且此时估计也是他对你最尊重的时候;当他和你熟了之后，对你知根知底后，往往随着他的“高人一等”而不那么尊重你的时候，其实是你没透过本质去理解，因为他本质就是这样</p>
<p>所以那么故意激怒和恶心你的人，大可不必继续往下走;越往下走，越难理解</p>
<h2 id="远离那些对你一而再，再而三撒谎的人"><a href="#远离那些对你一而再，再而三撒谎的人" class="headerlink" title="远离那些对你一而再，再而三撒谎的人"></a>远离那些对你一而再，再而三撒谎的人</h2><p>假设你认为你最喜欢的人对你是一而再，再而三的撒谎，并且瞒天过海的做着许多过分的事情，也尽可以大方的告别，没必要浪费自己的时间和生命</p>
<p>坦诚是一个人的为人之本，而不是任何的加分项</p>
<p>另外，也不要为了遮掩自己的错误，去用 善意的谎言将其替换，其实这是一种本末倒置的操作，事后当局人理解过来后，只会让人更恶心</p>
<p>做法：戳穿最后所谓的“善意谎言”,让自己彻底失去对他的任何信任信任;同时也会还一个清晰的自己</p>
<h1 id="保持热情，行动起来，继续前行"><a href="#保持热情，行动起来，继续前行" class="headerlink" title="保持热情，行动起来，继续前行"></a>保持热情，行动起来，继续前行</h1><p>无论我们经历过什么，也要让自己保持自己最初的热爱和选择；在苟且的生活中，慢慢需要和拼凑属于自己的生活碎片;当最后的结果哪怕是不尽如意的时候，也是有碎片可以支撑我们继续前行</p>
<p>在属于自己的萌芽期和黑暗期，去坚持自己</p>
<p>在看不到前方任何希望的时候，我们不妨让自己多做点和多坚持下，也许下次就能看到前进的方向和目标</p>
<p>一定要记住,永远要记住，找寻自己的道路上，始终让自己一个人继续前行，哪怕会经历漫长的黑暗，哪怕只有一瞬间的光芒，就是那一瞬间会变成自己内心永远的光</p>
<h1 id="保持自己善良和良知"><a href="#保持自己善良和良知" class="headerlink" title="保持自己善良和良知"></a>保持自己善良和良知</h1><p>我这一生,自从明白事理后，自己内心始终有一杠称，永远不要做没有良知的事情以及千万不要抛弃自己的善良</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>我会继续完善自己,去贯穿自己;同时也希望自己要尽快的彻底恢复过来,虽然已经好了很多，但是还是缺少许多，得慢慢的恢复过来</p>
<p>我会在漫长的黑暗期以及萌芽期，去坚持和贯彻自己</p>
<p>最后，此篇碎碎念写于2023-12-08;把时间交还自己,我们回头后再来看看这个碎碎念,是不是会有不同的感悟和理解</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 如果你也认为这篇生活碎碎念帮助到你的话，</p>
</blockquote>
]]></content>
      <categories>
        <category>生活碎碎念</category>
      </categories>
      <tags>
        <tag>生活碎碎念</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习_介绍篇章</title>
    <url>//article/machine_base/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>机器学习三大范式（监督&#x2F;无监督&#x2F;强化）与深度网络的关系；回归 vs 分类；输入类型（表格、序列、图像、图结构）；模型作为函数族 + 训练数据选择具体函数；生成模型与潜变量</p>
</blockquote>
<h1 id="📝-机器学习的三大范式与深度网络关系"><a href="#📝-机器学习的三大范式与深度网络关系" class="headerlink" title="📝 机器学习的三大范式与深度网络关系"></a>📝 机器学习的三大范式与深度网络关系</h1><h2 id="核心关系"><a href="#核心关系" class="headerlink" title="核心关系"></a>核心关系</h2><p>深度神经网络是目前最强大、最实用的一类机器学习模型。将深度网络应用到数据上的过程称为深度学习。深度网络并非三大范式本身，而是实现这三大范式的核心工具&#x2F;模型家族。三大范式定义了“学什么、如何学”的问题类型和目标，而深度网络提供了强大的函数逼近能力，使这些范式在复杂、高维、结构化数据上取得当前最佳性能。</p>
<ul>
<li><strong>监督学习</strong>：深度网络学习从输入 x 到输出 y 的映射 y&#x3D;f[x,ϕ]。</li>
<li><strong>无监督学习</strong>：深度网络学习数据的内在结构或概率分布（生成模型）。</li>
<li><strong>强化学习</strong>：深度网络常被用来表示策略（从状态到动作的映射）或价值函数。</li>
</ul>
<h3 id="三大范式是否可以通过深度网络实现？"><a href="#三大范式是否可以通过深度网络实现？" class="headerlink" title="三大范式是否可以通过深度网络实现？"></a>三大范式是否可以通过深度网络实现？</h3><p><strong>是的，完全可以，而且目前最前沿的实现几乎都依赖深度网络。</strong></p>
<p>具体说明如下：</p>
<h4 id="（1）监督学习"><a href="#（1）监督学习" class="headerlink" title="（1）监督学习"></a>（1）监督学习</h4><ul>
<li><strong>定义</strong>：从带标签的输入-输出对中学习映射关系。</li>
<li><strong>深度网络实现方式</strong>：<ul>
<li>网络作为函数 f[x,ϕ]，参数 ϕ 通过最小化损失函数（如平方损失、交叉熵）来训练。</li>
<li>可处理回归（连续输出）、分类（概率输出）、结构化输出（语义分割、深度估计、翻译、图像生成描述等）。</li>
<li>优势：能处理高维、变长、具有内部结构的输入（图像、文本、音频、分子图等），并输出同样复杂的结构。</li>
</ul>
</li>
<li>书中例子：房价预测、情感分类、音乐类型识别、图像物体分类、语义分割、单目深度估计、语音转文字、机器翻译、文本生成图像等。</li>
</ul>
<h4 id="（2）无监督学习"><a href="#（2）无监督学习" class="headerlink" title="（2）无监督学习"></a>（2）无监督学习</h4><ul>
<li><strong>定义</strong>：只有输入数据，没有对应输出标签。目标是描述或理解数据的结构（而非学习映射）。</li>
<li><strong>深度网络实现方式</strong>（重点是生成式模型）：<ul>
<li>学习合成与训练数据在统计上难以区分的新样本。</li>
<li>一些模型直接建模数据的概率分布并从中采样；另一些只学习生成机制。</li>
<li>常引入<strong>潜变量</strong>（latent variables）来捕捉数据的低维本质结构（例如人脸表情可用约42个肌肉参数近似描述）。</li>
<li>条件生成：在部分约束下生成（图像修复、文本补全等）。</li>
</ul>
</li>
<li>书中例子：逼真的猫&#x2F;建筑图像生成、短篇故事合成、图像修复、文本续写（如GPT系列）。</li>
</ul>
<h4 id="（3）强化学习"><a href="#（3）强化学习" class="headerlink" title="（3）强化学习"></a>（3）强化学习</h4><ul>
<li><strong>定义</strong>：智能体（agent）在环境中执行动作，改变状态并获得奖励。目标是学习能最大化长期累积奖励的策略。存在时间信用分配问题（奖励延迟）以及探索-利用权衡。</li>
<li><strong>深度网络实现方式</strong>：<ul>
<li>最常见的是<strong>策略网络</strong>：深度网络直接将观察到的状态映射到动作（或动作分布）。</li>
<li>也可用于近似价值函数（状态价值或动作价值）。</li>
</ul>
</li>
<li>书中例子：<ul>
<li>类人机器人行走：从传感器状态到关节动作的映射。</li>
<li>下棋：从棋盘状态到走法选择的映射。</li>
</ul>
</li>
</ul>
<h1 id="回归和分类"><a href="#回归和分类" class="headerlink" title="回归和分类"></a>回归和分类</h1><h2 id="回归"><a href="#回归" class="headerlink" title="回归"></a>回归</h2><p>定义:</p>
<p>回归问题的目标是预测<strong>连续数值</strong>（或一组连续数值）。模型输出的是实数，而不是离散的类别标签。</p>
<ul>
<li>单变量回归：输出一个连续数值。</li>
<li>多变量回归：输出多个连续数值。</li>
</ul>
<p>直观理解:<br>把模型想象成一条（或一组）曲线&#x2F;函数，它根据输入计算出一个“多少”的量。</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>案例</td>
<td>输入</td>
<td>输出</td>
<td>类型</td>
<td>说明</td>
</tr>
<tr>
<td>房价预测</td>
<td>房屋特征（面积、卧室数量等）组成的固定长度向量</td>
<td>房价（一个连续数字）</td>
<td>单变量回归</td>
<td>最经典的例子。模型返回的是一个实数，而不是“便宜&#x2F;贵”这类类别。</td>
</tr>
<tr>
<td>分子性质预测</td>
<td>分子的化学结构（原子及连接方式）</td>
<td>熔点 + 沸点（两个连续数字）</td>
<td>多变量回归</td>
<td>同时预测多个连续数值。</td>
</tr>
<tr>
<td>单目深度估计（结构化输出）</td>
<td>RGB 图像</td>
<td>每个像素的深度值（连续数值矩阵）</td>
<td>多变量&#x2F;结构化回归</td>
<td>输出是一张“深度图”，每个像素都是一个连续的深度数字。</td>
</tr>
</tbody></table>
<h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><p>定义:</p>
<p>分类问题的目标是把输入分配到<strong>离散的类别</strong>中。模型通常输出每个类别的概率，然后取概率最高的类别作为最终预测。</p>
<ul>
<li><strong>二元分类（Binary Classification）</strong>：只有两个类别（是&#x2F;否、正面&#x2F;负面、猫&#x2F;狗等）。</li>
<li><strong>多类别分类（Multi-class Classification）</strong>：有三个或以上的互斥类别。</li>
</ul>
<p>直观理解:</p>
<p>把模型想象成一个“投票器”或“概率分配器”，它告诉你输入更像哪一类。</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>案例</td>
<td>输入</td>
<td>输出</td>
<td>类型</td>
<td>说明</td>
</tr>
<tr>
<td>餐厅评论情感分析</td>
<td>评论文本字符串</td>
<td>正面 or 负面（两个概率）</td>
<td>二元分类</td>
<td>输出是一个长度为 2 的概率向量，例如 [0.12, 0.88] 表示 88% 概率是负面。</td>
</tr>
<tr>
<td>音乐类型识别</td>
<td>音频片段</td>
<td>N 种音乐类型中的一种（N 个概率）</td>
<td>多类别分类</td>
<td>模型返回大小为 N 的概率向量，表示属于每种类型的可能性。</td>
</tr>
<tr>
<td>图像物体分类</td>
<td>图片（像素 RGB 值）</td>
<td>N 种物体中的一种（N 个概率）</td>
<td>多类别分类</td>
<td>经典 ImageNet 风格任务。</td>
</tr>
<tr>
<td>语义分割（结构化输出）</td>
<td>RGB 图像</td>
<td>每个像素属于“牛”还是“背景”（每个像素做一次二元分类）</td>
<td>多变量二元分类 &#x2F; 结构化分类</td>
<td>输出是一张与输入同样大小的标签图。</td>
</tr>
</tbody></table>
<h2 id="回归-vs-分类-的核心对比"><a href="#回归-vs-分类-的核心对比" class="headerlink" title="回归 vs 分类 的核心对比"></a>回归 vs 分类 的核心对比</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>对比维度</td>
<td>回归（Regression）</td>
<td>分类（Classification）</td>
</tr>
<tr>
<td>输出类型</td>
<td>连续数值</td>
<td>离散类别（或类别概率）</td>
</tr>
<tr>
<td>典型问题问法</td>
<td>“是多少？”</td>
<td>“是哪一类？”</td>
</tr>
<tr>
<td>模型输出示例</td>
<td>房价 &#x3D; 325.6 万元</td>
<td>正面概率 0.15，负面概率 0.85</td>
</tr>
<tr>
<td>常用损失</td>
<td>平方损失、MAE</td>
<td>交叉熵损失</td>
</tr>
<tr>
<td>决策方式</td>
<td>直接使用预测的数值</td>
<td>通常取概率最大的类别</td>
</tr>
<tr>
<td>代表例子</td>
<td>房价预测、分子熔点沸点</td>
<td>评论情感、音乐类型、图像物体分类</td>
</tr>
</tbody></table>
<h1 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h1><p>现实世界的输入（文本、音频、图像、分子等）必须先被转换成数字向量，模型才能接受并处理。不同输入类型的结构差异很大，处理方式也因此不同。</p>
<h2 id="表格数据"><a href="#表格数据" class="headerlink" title="表格数据"></a>表格数据</h2><p><strong>特点</strong></p>
<ul>
<li>固定长度的特征向量</li>
<li><strong>没有内在结构</strong>（改变特征顺序，模型预期结果不变）</li>
<li>典型例子：房屋特征（面积、卧室数、楼层、位置编码等）</li>
</ul>
<p><strong>处理方式</strong></p>
<ul>
<li>直接把每个特征变成一个数字</li>
<li>类别特征需要编码（One-hot 或 Embedding）</li>
<li>数值特征通常做标准化&#x2F;归一化（让数值在相似范围）</li>
</ul>
<p><strong>处理后的效果示例</strong><br>假设预测房价，原始数据：</p>
<p>text</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">面积: 120㎡, 卧室: 3, 楼层: 8, 是否学区: 是</span><br></pre></td></tr></table></figure>

<p>处理后变成固定长度向量：</p>
<p>text</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[120.0, 3.0, 8.0, 1.0]          # 或标准化后 [0.85, 0.6, 0.4, 1.0]</span><br></pre></td></tr></table></figure>

<p>模型直接接收这个向量。</p>
<h2 id="文本数据-Text"><a href="#文本数据-Text" class="headerlink" title="文本数据(Text)"></a>文本数据(Text)</h2><p><strong>特点</strong></p>
<ul>
<li>长度不固定</li>
<li><strong>顺序非常重要</strong>（“我的妻子吃了鸡肉” ≠ “鸡肉吃了我的妻子”）</li>
<li>必须先编码成数字</li>
</ul>
<p><strong>处理方式</strong>（书中举例 + 现代常用方法）</p>
<ol>
<li><strong>分词（Tokenization）</strong>：把句子拆成词或子词</li>
<li><strong>建立词汇表</strong>：固定大小（书中例子用 10,000 个词）</li>
<li><strong>转换成索引序列</strong>：每个词对应一个整数 ID</li>
<li>现代方法会进一步用 Embedding 层把索引变成稠密向量，或直接用 Transformer 的 tokenizer（如 BPE）</li>
</ol>
<p><strong>处理后的效果示例</strong><br>原始文本：</p>
<p>text</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&quot;这家餐厅的服务很好&quot;</span><br></pre></td></tr></table></figure>

<p>处理后（简单索引方式，书中例子）：</p>
<p>text</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[4521, 89, 3302, 17, 908]     # 每个数字是词汇表中的索引</span><br></pre></td></tr></table></figure>

<p>或经过 Embedding 后变成矩阵（每个词变成一个向量）：</p>
<p>text</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[[0.12, -0.45, 0.78, ...],    # “这家”的向量</span><br><span class="line"> [0.33, 0.21, -0.09, ...],    # “餐厅”的向量</span><br><span class="line"> ... ]</span><br></pre></td></tr></table></figure>

<p>长度会不同，实际训练时常做 padding（补零）或截断，变成固定长度</p>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><p>总结文章的内容</p>
<h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><ul>
<li>一些引用</li>
<li>引用文章</li>
</ul>
<blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>神经网络</tag>
      </tags>
  </entry>
  <entry>
    <title>编译问题记录和各种环境配置</title>
    <url>//article/all_env_project_complied/</url>
    <content><![CDATA[<blockquote>
<p>😀 这里写文章的前言：<br>记录编译各种项目以及开源项目遇到的问题</p>
<p>本地安装jar包<br>pentaho-aggdesigner-algorithm-5.1.5-jhyde<br>eigenbase-properties-1.1.4<br>greenplum-jdbc-5.1.4</p>
</blockquote>
<h1 id="📝-缺少jar包本地编译"><a href="#📝-缺少jar包本地编译" class="headerlink" title="📝 缺少jar包本地编译"></a>📝 缺少jar包本地编译</h1><h2 id="pentaho-aggdesigner-algorithm-5-1-5-jhyde"><a href="#pentaho-aggdesigner-algorithm-5-1-5-jhyde" class="headerlink" title="pentaho-aggdesigner-algorithm-5.1.5-jhyde"></a>pentaho-aggdesigner-algorithm-5.1.5-jhyde</h2><p>jar包</p>
<p><a href="https://github.com/ruY9527/source-notes/blob/master/java/localhostJars/pentaho-aggdesigner-algorithm-5.1.5-jhyde.jar">📎 pentaho-aggdesigner-algorithm-5.1.5-jhyde.jar</a></p>
<p>本地安装指令:  -Dfile 切换为自己本地下载存放jar的位置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mvn install:install-file -Dfile=/home/luohong/Downloads/pentaho-aggdesigner-algorithm-<span class="number">5.1</span><span class="number">.5</span>-jhyde.jar -DgroupId=org.pentaho -DartifactId=pentaho-aggdesigner-algorithm -Dversion=<span class="number">5.1</span><span class="number">.5</span>-jhyde -Dpackaging=jar</span><br></pre></td></tr></table></figure>

<h2 id="eigenbase-properties-1-1-4"><a href="#eigenbase-properties-1-1-4" class="headerlink" title="eigenbase-properties-1.1.4"></a>eigenbase-properties-1.1.4</h2><p>jar包</p>
<p><a href="https://github.com/ruY9527/source-notes/blob/master/java/localhostJars/eigenbase-properties-1.1.4.jar">📎 eigenbase-properties-1.1.4.jar</a></p>
<p>本地安装指令: -Dfile 切换为自己本地下载存放jar的位置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mvn install:install-file -DgroupId=eigenbase -DartifactId=eigenbase-properties -Dversion=<span class="number">1.1</span><span class="number">.4</span> -Dpackaging=jar -Dfile=/home/luohong/Downloads/eigenbase-properties-<span class="number">1.1</span><span class="number">.4</span>.jar</span><br></pre></td></tr></table></figure>

<h2 id="greenplum-jdbc-5-1-4"><a href="#greenplum-jdbc-5-1-4" class="headerlink" title="greenplum-jdbc-5.1.4"></a>greenplum-jdbc-5.1.4</h2><p>jar包</p>
<p><a href="https://github.com/ruY9527/source-notes/blob/master/java/localhostJars/greenplum-jdbc-5.1.4.jar">📎 greenplum-jdbc-5.1.4.jar</a></p>
<p>本地安装指令: -Dfile 切换为自己本地下载存放jar的位置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mvn install:install-file -DgroupId=com.pivotal -DartifactId=greenplum-jdbc -Dversion=<span class="number">5.1</span><span class="number">.4</span> -Dpackaging=jar -Dfile=/home/luohong/Downloads/greenplum-jdbc-<span class="number">5.1</span><span class="number">.4</span>.jar</span><br></pre></td></tr></table></figure>

<h1 id="📝-前端环境配置"><a href="#📝-前端环境配置" class="headerlink" title="📝 前端环境配置"></a>📝 前端环境配置</h1><p>查看镜像源设置:  npm config get registry</p>
<h2 id="设置镜像源"><a href="#设置镜像源" class="headerlink" title="设置镜像源"></a>设置镜像源</h2><p>阿里: npm config set registry <a href="https://registry.npm.taobao.org/">https://registry.npm.taobao.org</a></p>
<h2 id="vite问题"><a href="#vite问题" class="headerlink" title="vite问题"></a>vite问题</h2><p>‘vite’ 不是内部或外部命令，也不是可运行的程序 或批处理文件</p>
<p>处理方法:  npm i 进行安装</p>
<h1 id="📝-python环境配置"><a href="#📝-python环境配置" class="headerlink" title="📝 python环境配置"></a>📝 python环境配置</h1><h2 id="设置镜像源-1"><a href="#设置镜像源-1" class="headerlink" title="设置镜像源"></a>设置镜像源</h2><p><code>pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple</code></p>
<p>其它镜像源地址</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">清华：https://pypi.tuna.tsinghua.edu.cn/simple</span><br><span class="line"></span><br><span class="line">阿里云：http://mirrors.aliyun.com/pypi/simple/</span><br><span class="line"></span><br><span class="line">中国科技大学 https://pypi.mirrors.ustc.edu.cn/simple/</span><br><span class="line"></span><br><span class="line">华中理工大学：http://pypi.hustunique.com/</span><br><span class="line"></span><br><span class="line">山东理工大学：http://pypi.sdutlinux.org/ </span><br><span class="line"></span><br><span class="line">豆瓣：http://pypi.douban.com/simple/</span><br></pre></td></tr></table></figure>

<h2 id="python去除背景方法"><a href="#python去除背景方法" class="headerlink" title="python去除背景方法"></a>python去除背景方法</h2><ul>
<li>rembg 框架</li>
</ul>
<h2 id="python相关资源学习"><a href="#python相关资源学习" class="headerlink" title="python相关资源学习"></a>python相关资源学习</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>作用</td>
<td>地址</td>
</tr>
<tr>
<td></td>
<td>python爬虫学习资料</td>
<td><a href="https://vip.fxxkpython.com/?p=4699">https://vip.fxxkpython.com/?p=4699</a></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="📝-资源记录相关-主要学习资源记录"><a href="#📝-资源记录相关-主要学习资源记录" class="headerlink" title="📝 资源记录相关(主要学习资源记录)"></a>📝 资源记录相关(主要学习资源记录)</h1><h2 id="AI相关"><a href="#AI相关" class="headerlink" title="AI相关"></a>AI相关</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>作用</td>
<td>网址</td>
</tr>
<tr>
<td>内容生成</td>
<td></td>
<td><a href="http://writesonic.com/">writesonic.com</a></td>
</tr>
<tr>
<td>针对网络的研究操作系统</td>
<td></td>
<td><a href="http://nette.io/">nette.io</a></td>
</tr>
<tr>
<td>生成病毒式推文和话题串</td>
<td></td>
<td><a href="http://postwise.ai/">postwise.ai</a></td>
</tr>
<tr>
<td>YouTube视频编辑器</td>
<td></td>
<td><a href="http://flexclip.com/">FlexClip.com</a></td>
</tr>
<tr>
<td>文本转视频</td>
<td></td>
<td><a href="http://synthesia.io/">synthesia.io</a></td>
</tr>
<tr>
<td>设计工具</td>
<td></td>
<td><a href="http://designer.microsoft.com/">designer.microsoft.com</a></td>
</tr>
</tbody></table>
<h2 id="AI绘画"><a href="#AI绘画" class="headerlink" title="AI绘画"></a>AI绘画</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>作用</td>
<td>网址</td>
</tr>
<tr>
<td>光影文字</td>
<td>光影文字</td>
<td><a href="https://mp.weixin.qq.com/s/zbQCwOHsMS4GmMLJJIU_1A">https://mp.weixin.qq.com/s/zbQCwOHsMS4GmMLJJIU_1A</a></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h2 id="区块链"><a href="#区块链" class="headerlink" title="区块链"></a>区块链</h2><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>作用</td>
<td>网址</td>
</tr>
<tr>
<td>区块链学习</td>
<td></td>
<td><a href="https://learnweb3.io/">https://learnweb3.io/</a></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="📝-IDEA插件记录"><a href="#📝-IDEA插件记录" class="headerlink" title="📝 IDEA插件记录"></a>📝 IDEA插件记录</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>插件名称</td>
<td>插件作用</td>
<td>插件备注</td>
</tr>
<tr>
<td><strong>Simple Object Copy</strong></td>
<td></td>
<td>一键生成 dto,vo 等插件</td>
</tr>
<tr>
<td><strong>Maven Helper</strong></td>
<td></td>
<td>查看maven依赖树情况</td>
</tr>
<tr>
<td><strong>EasyCode</strong></td>
<td></td>
<td>一键生成逆向工程</td>
</tr>
<tr>
<td><strong>Iedis</strong></td>
<td></td>
<td>Redis连接插件</td>
</tr>
<tr>
<td><strong>Lombok</strong></td>
<td></td>
<td>方法自动生成</td>
</tr>
<tr>
<td><strong>Jrebel</strong></td>
<td></td>
<td>热部署相关的插件</td>
</tr>
<tr>
<td><strong>Alibaba Java Coding Guidelines</strong></td>
<td></td>
<td>阿里巴巴代码检测规范</td>
</tr>
<tr>
<td><strong>VisualVM Launcher</strong></td>
<td></td>
<td>性能监控的插件</td>
</tr>
<tr>
<td><strong>CodeGlance</strong></td>
<td></td>
<td>左侧窗口的上下滑动框</td>
</tr>
<tr>
<td><strong>Material Theme UI</strong></td>
<td></td>
<td>IDEA的UI框架</td>
</tr>
<tr>
<td><strong>stackoverflow</strong></td>
<td></td>
<td>链接到   stackoverflow 检查问题</td>
</tr>
<tr>
<td><strong>Codota</strong></td>
<td></td>
<td>好</td>
</tr>
<tr>
<td><strong>jclasslib bytecode viewer</strong></td>
<td></td>
<td>字节码编译</td>
</tr>
<tr>
<td><strong>PlantUML</strong></td>
<td></td>
<td>对象类设计</td>
</tr>
<tr>
<td><strong>Stack trace to UML</strong></td>
<td></td>
<td>调用链图</td>
</tr>
<tr>
<td><strong>Git Commit Template</strong></td>
<td></td>
<td>git提交规范代码</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="📝-资讯资源"><a href="#📝-资讯资源" class="headerlink" title="📝 资讯资源"></a>📝 资讯资源</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>作用</td>
<td>网址</td>
</tr>
<tr>
<td>新闻资源</td>
<td>国外的新闻资源</td>
<td><a href="https://mgreader.com/">https://mgreader.com/</a></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="📝-想看纪录片"><a href="#📝-想看纪录片" class="headerlink" title="📝 想看纪录片"></a>📝 想看纪录片</h1><table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>名字</td>
<td>状态</td>
<td>总结</td>
<td></td>
</tr>
<tr>
<td>诈骗王</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>富豪谷底求翻身</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>货币崛起</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>但是还有书籍</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>宇宙</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>急诊室的故事</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>心智斗争</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>女人</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>解码比尔盖茨</td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>绿色星球</td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="🤗-总结归纳"><a href="#🤗-总结归纳" class="headerlink" title="🤗 总结归纳"></a>🤗 总结归纳</h1><h1 id="📎-参考文章"><a href="#📎-参考文章" class="headerlink" title="📎 参考文章"></a>📎 参考文章</h1><blockquote>
<p>💡 有关文章的问题，欢迎您在底部评论区留言，一起交流~</p>
</blockquote>
]]></content>
      <categories>
        <category>问题记录</category>
      </categories>
      <tags>
        <tag>使用入门</tag>
        <tag>框架</tag>
      </tags>
  </entry>
</search>
